openapi: 3.0.3
info:
  title: KMU Hub API
  description: All-in-One CRM for DACH SMEs with EU data sovereignty
  version: 0.1.0

servers:
  - url: http://localhost:8080
    description: Local development

tags:
  - name: auth
    description: Authentication and authorization
  - name: users
    description: User management
  - name: health
    description: Health checks
  - name: custom-fields
    description: Custom field definitions
  - name: tags
    description: Tags for entities
  - name: contacts
    description: Contact management
  - name: companies
    description: Company management
  - name: pipeline-stages
    description: Pipeline stage configuration
  - name: deals
    description: Deal management
  - name: activities
    description: Activity management (calls, meetings, notes, emails, tasks)
  - name: search
    description: Global search across CRM entities
  - name: saved-filters
    description: Saved filter configurations
  - name: reports
    description: CRM reporting and analytics
  - name: chat-channels
    description: Chat channel management
  - name: chat-messages
    description: Chat message management
  - name: chat-files
    description: Chat file sharing and uploads
  - name: chat-search
    description: Full-text search across chat messages and files
  - name: notifications
    description: Notification management (list, read, preferences, mutes, quiet hours)
  - name: dialer
    description: Outbound dialer (campaigns, calls, supervisor overview, contact call history)
  - name: dashboard
    description: Dashboard layout management (user layouts, role defaults)
  - name: projects
    description: Project management (CRUD, members, templates, statuses)
  - name: tasks
    description: Task management (CRUD, move, subtasks, dependencies)
  - name: task-comments
    description: Task comments
  - name: task-files
    description: Task file attachments
  - name: task-links
    description: Task entity links (CRM integration)
  - name: work-search
    description: Full-text search across tasks
  - name: time-tracking
    description: Task timer and time entry management
  - name: calendars
    description: Calendar management (personal, shared, subscriptions, preferences)
  - name: events
    description: Calendar event CRUD, recurring events, attendees, RSVP, reminders
  - name: resources
    description: Resource management (rooms, equipment) and booking
  - name: holidays
    description: Public holiday management (DACH region)
  - name: video-calls
    description: Video and voice call management (1:1 and group, LiveKit-based)
  - name: recordings
    description: Call and meeting recording with DSGVO consent management
  - name: presence
    description: User presence status (online, away, DND, in-call, offline)
  - name: reactions
    description: Emoji reactions on chat messages
  - name: meetings
    description: Meeting lifecycle (schedule, start, end, notes, action items)
  - name: meeting-notes
    description: Meeting notes (shared and private)
  - name: action-items
    description: Meeting action items with task conversion
  - name: webhooks
    description: External service webhooks (LiveKit)
  - name: finance-settings
    description: Company settings for invoicing and finance
  - name: quotes
    description: Quote (Angebot) management
  - name: invoices
    description: Invoice (Rechnung) management
  - name: credit-notes
    description: Credit note (Gutschrift) management
  - name: payments
    description: Payment recording and tracking
  - name: dunning
    description: Dunning (Mahnwesen) management
  - name: finance-dashboard
    description: Finance dashboard and analytics
  - name: datev
    description: DATEV Buchungsstapel CSV export
  - name: inbox-messages
    description: Unified inbox message operations (list, read, star, archive, snooze, reply, assign)
  - name: inbox-teams
    description: Team inbox management (create, update, delete, members, claim)
  - name: inbox-routing
    description: Inbox routing rules (create, update, delete, test)
  - name: automations
    description: Automation workflow management (CRUD, enable/disable, execution logs, templates, catalog, testing)
  - name: public-booking
    description: Public booking pages and slot reservation (unauthenticated)
  - name: helpdesk-tickets
    description: Helpdesk ticket management (create, update, close, reopen, assign, merge)
  - name: helpdesk-messages
    description: Helpdesk ticket messages (agent and customer replies)
  - name: helpdesk-queues
    description: Helpdesk queue management
  - name: helpdesk-canned-responses
    description: Helpdesk canned (template) responses
  - name: helpdesk-sla
    description: Helpdesk SLA policy management and ticket SLA status
  - name: helpdesk-kb
    description: Helpdesk knowledge-base articles
  - name: helpdesk-routing
    description: Helpdesk ticket-routing rules
  - name: helpdesk-stats
    description: Helpdesk dashboard statistics
  - name: wiki-articles
    description: Wiki article management (CRUD, versions, attachments, search)
  - name: wiki-categories
    description: Wiki category management
  - name: schichten
    description: Shift planning (shifts, assignments, templates, compliance, swap requests)
  - name: produktion
    description: Production order and machine booking management
  - name: fuhrpark
    description: Fleet vehicle management (vehicles, services, damages, fuel logs, trip logs, GPS)
  - name: einkauf
    description: Purchasing management (suppliers, purchase orders, lines)
  - name: security
    description: Security management (audit, vault, GDPR/DSAR, password policy, IP rules)
  - name: 2fa
    description: Two-factor authentication management and admin policy
  - name: formulare
    description: Form schema and submission management (forms builder, webhooks, delivery logs)
  - name: vertraege
    description: Contract management (parties, reminders, digital signatures, export)
  - name: mails
    description: Email account management, IMAP sync, message operations, signatures, CRM links, contact import/export
  - name: inventar
    description: Inventory management (items, stock movements, low-stock warnings, locations, stocktake sessions)
  - name: vermietung
    description: Rental management (rental objects, rentals, inspections, availability checks, calendar, export)

  # === openapi-drift sweep: berichte ===
  - name: berichte
    description: >-
      Custom report builder (definitions, ad-hoc runs, PDF/CSV/XLSX exports,
      scheduled delivery, dashboard KPIs). Distinct from the CRM `reports`
      tag. Mounted at /api/v1/berichte/* behind the modules.berichte feature
      flag (route_berichte.go); routes do not register at all when the flag
      is disabled.

  # === openapi-drift sweep: documents ===
  - name: documents
    description: Document management (folders, files, versions, sharing, tags, search, virtual files, OnlyOffice WOPI tokens)

  # === openapi-drift sweep: plugins ===
  - name: plugins
    description: >-
      Config-based plugin system — manifests, per-tenant installations,
      permission approval, plugin settings, validation/workflow rules,
      industry templates, and execution logs. All routes require auth; most
      mutations additionally require the admin role. The WASM plugin type is
      registered end-to-end but gated OFF via the plugins.wasm feature flag.

  # === openapi-drift sweep: produktion-ext ===
# produktion-ext: no new tags — all 17 endpoints reuse the existing
# `produktion` tag (already defined in openapi.yaml, line ~138).

  # === openapi-drift sweep: wp3-integration-datev ===
# New tags for WP3 (route_integration.go + route_datev_upload.go).
# NOTE: the `datev` tag already exists in the main spec ("DATEV Buchungsstapel CSV
# export", tags block ~L104) and is REUSED as-is for the route_datev_upload.go
# endpoints below -- it is intentionally NOT redefined here.
  - name: integrations
    description: Generic Teams/Slack integration admin config, channel mappings, account linking, and inbound webhooks

  # === openapi-drift sweep: wp4-bexio-lexware ===
  - name: bexio
    description: Bexio accounting integration (OAuth connect, sync config/status/logs, field mappings, manual invoice/quote push)
  - name: lexware
    description: Lexware accounting integration (API-key connect, webhook events, sync config/status/logs, field mappings, manual invoice/quote push)

  # === openapi-drift sweep: wp5-caldav-guest-misc ===
  - name: caldav
    description: >-
      CalDAV/CardDAV app-specific password management (JWT auth) and org-wide
      admin configuration. The protocol endpoints themselves (/caldav/*,
      /carddav/*, HTTP Basic Auth, WebDAV verbs) and the /.well-known
      discovery redirects are out of scope for this OpenAPI document.
  - name: guest
    description: >-
      Public guest chat API for external/unauthenticated visitors. Session
      creation and token validation are fully unauthenticated; message and
      config endpoints require a guest session token in the X-Guest-Token
      header (not a JWT bearer token).
  - name: feature-flags
    description: Feature flag registry — resolved flag values for the authenticated user

  # === openapi-drift sweep: wp8-partial-gaps ===
# wp8-partial-gaps: no new tags — all 26 endpoints reuse existing tags.
# - CRM duplicate-detection/merge/timeline -> `contacts` / `companies`
# - GDPR consent CRUD + deletion-request processing -> `security`
# - GoBD journal summary + GoBD CSV export -> `GoBD Archive` (undeclared tag,
#   already used as-is by the /api/v1/finance/gobd-archive/* endpoints)
# - Invoice lock/validate-number -> `invoices`; dunning status/notice ->
#   `dunning`; payment stats -> `finance-dashboard`; deal-to-quote -> `quotes`
# - Time-tracking-to-invoice -> `hr-time` (undeclared tag, already used as-is
#   by the /api/v1/hr/time/* endpoints)
# - Module-access grants -> `Settings` (undeclared tag, already used as-is by
#   the /api/v1/tenant/module-leads and /api/v1/settings/* endpoints)
# - Presigned file upload/download -> `documents`

  # === openapi-drift sweep: r2a-video-meetings ===
# r2a-video-meetings: gap-fill for breakout rooms, cohosts, lock, moderation,
# chat, ai-summary and recording sub-ops. No new tags — reuses existing
# [meetings] and [recordings] tags (declared above).

  # === openapi-drift sweep: r2b-hr-time ===
  - name: hr-time
    description: >-
      HR time tracking — clock in/out, breaks, corrections, time categories,
      time templates, time projects, weekly submission/approval, and
      analytics. Previously used only as an undeclared tag (wp8-partial-gaps
      time-tracking-to-invoice endpoint); now formally declared since this
      sweep adds the dedicated category/template/project/week/analytics
      endpoints under /api/v1/hr/time/*.

  # === openapi-drift sweep: r2c-misc-gaps ===
# r2c-misc-gaps: no new tags — all endpoints reuse existing tags already
# declared in the top-level tags: block: 2fa (~line 146), contacts (~line 22),
# helpdesk-sla (~line 124), inbox-messages (~line 106), inventar (~line 154).

paths:
  /health:
    get:
      summary: Health check
      tags: [health]
      operationId: healthCheck
      security: []
      responses:
        "200":
          description: Service is healthy
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: ok

  /api/v1/auth/register:
    post:
      summary: Register a new user
      tags: [auth]
      operationId: register
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RegisterRequest"
      responses:
        "201":
          description: User registered successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "409":
          $ref: "#/components/responses/Conflict"

  /api/v1/auth/login:
    post:
      summary: Login with email and password
      tags: [auth]
      operationId: login
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoginRequest"
      responses:
        "200":
          description: Login successful
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/auth/refresh:
    post:
      summary: Refresh access token using refresh token
      tags: [auth]
      operationId: refreshToken
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RefreshRequest"
      responses:
        "200":
          description: Token refreshed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RefreshResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/auth/logout:
    post:
      summary: Logout and invalidate refresh token
      tags: [auth]
      operationId: logout
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LogoutRequest"
      responses:
        "200":
          description: Logged out
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StatusResponse"

  /api/v1/auth/me:
    get:
      summary: Get current user profile
      description: Returns the authenticated user's profile based on JWT token.
      tags: [auth]
      operationId: getProfile
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Current user profile
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetUserResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/auth/me/permissions:
    get:
      summary: Get own effective permissions
      description: >
        Resolved multi-role union of the authenticated account: the roles it
        holds and, per fine-grained capability key, the widest scope any of
        those roles grants. The account is taken from the JWT, never from the
        request. No permission guard — every signed-in user may read their own
        rights.
      tags: [auth]
      operationId: getMyEffectivePermissions
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Resolved effective permissions
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EffectivePermissionsResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/auth/profile:
    patch:
      summary: Update own profile
      description: >
        Self-service update of the authenticated user's first_name, last_name
        and/or avatar_url. The user is resolved from the JWT, never the body.
        Cannot change is_active.
      tags: [auth]
      operationId: updateProfile
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateProfileRequest"
      responses:
        "200":
          description: Updated user profile
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetUserResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/auth/change-password:
    post:
      summary: Change password
      description: Changes the current user's password. Requires old password verification. Revokes all refresh tokens.
      tags: [auth]
      operationId: changePassword
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ChangePasswordRequest"
      responses:
        "200":
          description: Password changed successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StatusResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/auth/forgot-password:
    post:
      summary: Request password reset
      description: >
        Initiates a password-reset flow. Always returns 200 regardless of whether
        the email is registered (prevents user enumeration). Rate-limited per email.
      tags: [auth]
      operationId: forgotPassword
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ForgotPasswordRequest"
      responses:
        "200":
          description: Reset email sent (or silently suppressed if email unknown)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "429":
          description: Too many reset requests for this email

  /api/v1/auth/reset-password:
    post:
      summary: Reset password with token
      description: >
        Validates the single-use reset token (1-hour expiry), checks password strength,
        updates the password, and revokes all active refresh tokens.
      tags: [auth]
      operationId: resetPassword
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ResetPasswordRequest"
      responses:
        "200":
          description: Password updated successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StatusResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/tenants:
    post:
      summary: Provision a tenant
      description: >-
        Creates a tenant, activates its modules and issues the invitation for
        its first administrator, all in one transaction. Requires the
        tenants:write permission, which only the platform_admin role holds —
        a tenant administrator cannot create tenants. The plaintext invitation
        token is returned once and never again; without it the tenant has no
        way in. An admin_email that already has an account, or an open
        invitation in any tenant, is rejected with 409.
      tags: [users]
      operationId: provisionTenant
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProvisionTenantRequest"
      responses:
        "201":
          description: Tenant provisioned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProvisionTenantResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/Conflict"

  /api/v1/invitations:
    post:
      summary: Create user invitation
      description: >-
        Creates an invitation to join the caller's tenant. Requires admin or
        manager role. An active user and a pending, unexpired invitation each
        occupy one seat; if the tenant has a seat limit booked and no seat is
        free, the request is rejected with 409.
      tags: [users]
      operationId: createInvitation
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateInvitationRequest"
      responses:
        "201":
          description: Invitation created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InvitationResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/Conflict"

    get:
      summary: List pending invitations
      description: Returns all pending invitations. Requires admin or manager role.
      tags: [users]
      operationId: listInvitations
      security:
        - bearerAuth: []
      responses:
        "200":
          description: List of pending invitations
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListInvitationsResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /api/v1/invitations/{token}/accept:
    post:
      summary: Accept invitation and create account
      description: Creates a user account from an invitation. No authentication required.
      tags: [users]
      operationId: acceptInvitation
      security: []
      parameters:
        - name: token
          in: path
          required: true
          description: The invitation token received via email
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AcceptInvitationRequest"
      responses:
        "201":
          description: Account created successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: >-
            Invitation expired, already used, or the tenant has no free seat
            left. The backend maps these cases to gRPC FailedPrecondition,
            which the gateway returns as 409 Conflict (changed from 410 in the
            R3 hardening wave).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /api/v1/invitations/{id}:
    delete:
      summary: Cancel invitation
      description: Cancels a pending invitation. Requires admin role.
      tags: [users]
      operationId: cancelInvitation
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Invitation cancelled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StatusResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/users:
    get:
      summary: List users
      description: Requires admin or manager role.
      tags: [users]
      operationId: listUsers
      security:
        - bearerAuth: []
      parameters:
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: page_size
          in: query
          schema:
            type: integer
            default: 20
      responses:
        "200":
          description: Users list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListUsersResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /api/v1/users/{id}:
    get:
      summary: Get user by ID
      tags: [users]
      operationId: getUser
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/UserId"
      responses:
        "200":
          description: User details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetUserResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

    put:
      summary: Update user
      description: Requires admin role.
      tags: [users]
      operationId: updateUser
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/UserId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateUserRequest"
      responses:
        "200":
          description: User updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetUserResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/users/{id}/roles:
    post:
      summary: Assign a role to an account
      description: >
        Adds a role — a system preset or a tenant-owned custom role — to the
        account and answers with the role ids it holds afterwards. Roles are
        n:m, so the assignment adds to the existing ones; assigning a role the
        account already holds is a no-op that still returns the list. Requires
        roles:manage (legacy) or admin:role:assign (fine-grained). An account
        or a role outside the caller's tenant is invisible rather than
        forbidden and answers 404.
        Assigning a role to SOMEONE ELSE is not capped by the caller's own
        rights — staffing roles richer than one's own is what
        admin:role:assign delegates. Assigning one to ONESELF is capped: a
        role whose grants exceed the caller's own answers 403
        privilege_escalation.
      tags: [users]
      operationId: assignRole
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/UserId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssignRoleRequest"
      responses:
        "200":
          description: Role assigned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserRolesResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: >
            insufficient permissions, or privilege_escalation — the caller
            assigned themselves a role reaching past their own rights
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: not_found — no such account or role visible to the caller
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /api/v1/users/{id}/roles/{roleId}:
    delete:
      summary: Remove a role from an account
      description: >
        Answers with the role ids the account still holds. Removing a role the
        account does not hold is a no-op. Requires roles:manage (legacy) or
        admin:role:assign (fine-grained).
        Two guardrails apply to roles that carry role administration itself
        (roles:manage or admin:role:assign): the tenant's last administrator
        cannot lose it (last_admin), and callers cannot take it off themselves
        while other administrators exist (self_lockout). Both answer 409.
      tags: [users]
      operationId: removeRole
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/UserId"
        - $ref: "#/components/parameters/RoleId"
      responses:
        "200":
          description: Role removed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserRolesResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          description: not_found — no such account or role visible to the caller
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: >
            last_admin — the tenant's last administrator would lose role
            administration; or self_lockout — the caller would remove their own
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  # Account roster (phase 3 admin surface). Handler: internal/gateway/route_auth.go (AuthRoutes)
  /api/v1/admin/users:
    get:
      summary: List the tenant's account roster
      description: >
        Every real account (active/deactivated, from users) plus every
        still-open invitation (invited, from invitations) merged into one
        list. jobTitle is always "" — the backend has no job-title data on an
        account. hasOverrides is omitted — per-user permission overrides
        (R-6) do not exist yet. lastLoginAt is derived from the most recent
        user_sessions row and is therefore only as complete as session
        history: a session removed by logout/termination no longer counts.
        Requires the admin role.
      tags: [users]
      operationId: listAdminUsers
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Account roster
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminUsersResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  # Effective permissions of any account (admin audit view). Under /admin/users
  # rather than /users because that is where the RBAC frontend addresses it.
  # Handler: internal/gateway/route_auth.go (AuthRoutes)
  /api/v1/admin/users/{id}/permissions:
    get:
      summary: Get any account's effective permissions
      description: >
        Same resolved multi-role union as /api/v1/auth/me/permissions, for an
        arbitrary account. Requires admin role. A user id outside the caller's
        tenant returns 404, not an empty result — the target account is fetched
        under the users RLS policy before its rights are resolved.
      tags: [users]
      operationId: getUserEffectivePermissions
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/UserId"
        - name: base
          in: query
          required: false
          description: >
            Reserved for the role union without per-user overrides. Per-user
            overrides do not exist server-side yet, so the parameter is
            currently accepted and ignored.
          schema:
            type: string
            enum: ["1"]
      responses:
        "200":
          description: Resolved effective permissions
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EffectivePermissionsResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  # Role administration (RBAC Phase 1 wave 1b). Handler: internal/gateway/route_auth.go (AuthRoutes)
  /api/v1/admin/roles:
    get:
      summary: List roles
      description: >
        System presets (tenant_id IS NULL) plus the calling tenant's custom
        roles — exactly the rows the roles table's RLS read policy admits.
        memberCount is scoped to the caller's tenant even for a preset shared
        across tenants. Requires roles:manage (legacy) or admin:role:read
        (fine-grained; also held by the it_admin and hr_admin presets).
      tags: [users]
      operationId: listRoles
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Presets and tenant custom roles
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RolesResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      summary: Create a custom role
      description: >
        Clones basedOn into a new role owned by the calling tenant, copying
        every grant of the source role including its scope. A tenant may own
        at most 20 custom roles; the system presets do not count against that.
        Requires roles:manage (legacy) or admin:role:create (fine-grained).
        The clone may not reach past the caller's own rights: a source role
        carrying a capability the caller lacks — or a wider scope than they
        hold — answers 403 privilege_escalation. In practice only a caller
        with the admin preset can clone every preset.
      tags: [users]
      operationId: createRole
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateRoleRequest"
      responses:
        "201":
          description: Role created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RoleResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: >
            insufficient permissions, or privilege_escalation — the clone
            source carries rights the caller does not hold
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: not_found — basedOn names no role visible to the caller
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: >
            role_name_exists — a preset or an own role already carries the name
            (compared case-insensitively) — or role_limit_reached when the
            tenant already owns 20 custom roles.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: not_found — name or basedOn missing
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /api/v1/admin/roles/{id}:
    patch:
      summary: Rename/re-describe/recolor a custom role
      description: >
        Every field is optional; an absent one is left unchanged. System
        presets answer 403 preset_immutable. Requires roles:manage (legacy)
        or admin:role:edit (fine-grained).
      tags: [users]
      operationId: updateRole
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateRoleRequest"
      responses:
        "200":
          description: Role updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RoleResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: preset_immutable — system presets cannot be edited
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: not_found — no such role visible to the caller
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: role_name_exists — a preset or an own role already carries the name
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

    delete:
      summary: Delete a custom role
      description: >
        System presets answer 403 preset_immutable; a role still assigned to
        at least one account answers 409 role_has_members. Requires
        roles:manage (legacy) or admin:role:delete (fine-grained).
      tags: [users]
      operationId: deleteRole
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "204":
          description: Role deleted
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: preset_immutable — system presets cannot be deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: not_found — no such role visible to the caller
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: role_has_members — the role still has at least one holder
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /api/v1/admin/roles/{id}/permissions:
    get:
      summary: Grant set of a role
      description: >
        Presets are readable too — the builder needs to see what it is
        cloning. Requires roles:manage (legacy) or admin:role:read
        (fine-grained).
      tags: [users]
      operationId: getRolePermissions
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Grant set
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RolePermissionsResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          description: not_found — no such role visible to the caller
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    put:
      summary: Replace the grant set of a custom role
      description: >
        Full replacement: every key missing from grants is revoked, every key
        present is (re)inserted with its scope, in one transaction. System
        presets answer 403 preset_immutable. Every key must exist in the
        capability catalogue — an unknown key aborts the whole write with 422.
        Requires roles:manage (legacy) or admin:role:edit (fine-grained).
      tags: [users]
      operationId: setRolePermissions
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateRolePermissionsRequest"
      responses:
        "200":
          description: Grant set replaced
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RolePermissionsResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: >
            preset_immutable — system presets cannot be edited; or
            privilege_escalation — the grant set names a capability the caller
            does not hold, or widens a scope past their own
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: not_found — no such role visible to the caller
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: >
            self_lockout — the caller wears this role and the new grant set
            would strip their own role administration
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: unknown_capability_key — a grant key is not in the permissions catalogue
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  # Custom Fields
  /api/v1/custom-fields:
    post:
      summary: Create custom field definition
      tags: [custom-fields]
      operationId: createCustomField
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateCustomFieldRequest"
      responses:
        "201":
          description: Custom field created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomFieldResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

    get:
      summary: List custom field definitions
      tags: [custom-fields]
      operationId: listCustomFields
      security:
        - bearerAuth: []
      parameters:
        - name: entity_type
          in: query
          schema:
            type: string
            enum: [contact, company, deal, activity]
      responses:
        "200":
          description: List of custom fields
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListCustomFieldsResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/custom-fields/{id}:
    get:
      summary: Get custom field by ID
      tags: [custom-fields]
      operationId: getCustomField
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Custom field details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomFieldResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

    patch:
      summary: Update custom field
      tags: [custom-fields]
      operationId: updateCustomField
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateCustomFieldRequest"
      responses:
        "200":
          description: Custom field updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomFieldResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

    delete:
      summary: Delete custom field
      tags: [custom-fields]
      operationId: deleteCustomField
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Custom field deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StatusResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  # Tags
  /api/v1/tags:
    post:
      summary: Create tag
      tags: [tags]
      operationId: createTag
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateTagRequest"
      responses:
        "201":
          description: Tag created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TagResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/Conflict"

    get:
      summary: List tags
      tags: [tags]
      operationId: listTags
      security:
        - bearerAuth: []
      parameters:
        - name: entity_type
          in: query
          schema:
            type: string
            enum: [contact, company, deal, activity]
      responses:
        "200":
          description: List of tags
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListTagsResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/tags/{id}:
    get:
      summary: Get tag by ID
      tags: [tags]
      operationId: getTag
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Tag details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TagResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

    patch:
      summary: Update tag
      tags: [tags]
      operationId: updateTag
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateTagRequest"
      responses:
        "200":
          description: Tag updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TagResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

    delete:
      summary: Delete tag
      tags: [tags]
      operationId: deleteTag
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Tag deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StatusResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Tag is in use
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  # Contacts
  /api/v1/contacts:
    post:
      summary: Create contact
      tags: [contacts]
      operationId: createContact
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateContactRequest"
      responses:
        "201":
          description: Contact created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ContactResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/Conflict"

    get:
      summary: List contacts
      tags: [contacts]
      operationId: listContacts
      security:
        - bearerAuth: []
      parameters:
        - name: company_id
          in: query
          schema:
            type: string
            format: uuid
        - name: tag_ids
          in: query
          schema:
            type: array
            items:
              type: string
              format: uuid
        - name: search
          in: query
          schema:
            type: string
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: page_size
          in: query
          schema:
            type: integer
            default: 20
      responses:
        "200":
          description: List of contacts
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListContactsResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/leads:
    get:
      summary: List leads
      description: >
        The lead inbox. A lead is a contact at an earlier lifecycle stage, not
        a separate entity — these are contacts rows filtered by
        lifecycle_stage. Guarded by the contacts read permission.
      tags: [contacts]
      operationId: listLeads
      security:
        - bearerAuth: []
      parameters:
        - name: stage
          in: query
          description: Empty means every non-customer stage.
          schema:
            type: string
            enum: [lead, qualified]
        - name: status
          in: query
          schema:
            type: string
            enum: [new, contacted, qualified, disqualified]
        - name: search
          in: query
          schema:
            type: string
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: page_size
          in: query
          schema:
            type: integer
            default: 20
      responses:
        "200":
          description: List of leads
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListLeadsResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

    post:
      summary: Create lead
      description: >
        Records a raw prospect as a contact at the "lead" lifecycle stage. The
        score is computed server-side from source and data completeness.
      tags: [contacts]
      operationId: createLead
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateLeadRequest"
      responses:
        "201":
          description: Lead created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LeadResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/Conflict"

  /api/v1/leads/{id}:
    patch:
      summary: Update lead status or temperature
      description: >
        Moves a lead through the inbox or pins its temperature. An empty
        temperature clears the manual override and hands the displayed value
        back to the computed score. Setting status to "qualified" also moves
        the lifecycle stage, exactly like the convert endpoint.
      tags: [contacts]
      operationId: updateLead
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateLeadRequest"
      responses:
        "200":
          description: Lead updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LeadResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/leads/{id}/convert:
    post:
      summary: Convert lead to qualified
      description: >
        Qualifies a lead. The row already is the contact, so this is a stage
        change rather than a copy into a second table.
      tags: [contacts]
      operationId: convertLead
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Lead converted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LeadResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/contacts/{id}:
    get:
      summary: Get contact by ID
      tags: [contacts]
      operationId: getContact
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Contact details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ContactResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

    patch:
      summary: Update contact
      tags: [contacts]
      operationId: updateContact
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateContactRequest"
      responses:
        "200":
          description: Contact updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ContactResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

    delete:
      summary: Delete contact
      tags: [contacts]
      operationId: deleteContact
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Contact deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StatusResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/contacts/{id}/files:
    get:
      summary: List contact files
      tags: [contact-files]
      operationId: listContactFiles
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: File list
          content:
            application/json:
              schema:
                type: object
                properties:
                  files:
                    type: array
                    items:
                      $ref: "#/components/schemas/ContactFileResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    post:
      summary: Attach file to contact
      tags: [contact-files]
      operationId: createContactFile
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [filename, storage_key, file_size]
              properties:
                filename:
                  type: string
                mime_type:
                  type: string
                storage_key:
                  type: string
                file_size:
                  type: integer
                  format: int64
      responses:
        "201":
          description: File attached
          content:
            application/json:
              schema:
                type: object
                properties:
                  file:
                    $ref: "#/components/schemas/ContactFileResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/contacts/{id}/tags:
    post:
      summary: Add tags to contact
      tags: [contacts]
      operationId: addContactTags
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TagIdsRequest"
      responses:
        "200":
          description: Tags added
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ContactResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

    delete:
      summary: Remove tags from contact
      tags: [contacts]
      operationId: removeContactTags
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TagIdsRequest"
      responses:
        "200":
          description: Tags removed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ContactResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  # Companies
  /api/v1/companies:
    post:
      summary: Create company
      tags: [companies]
      operationId: createCompany
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateCompanyRequest"
      responses:
        "201":
          description: Company created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CompanyResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

    get:
      summary: List companies
      tags: [companies]
      operationId: listCompanies
      security:
        - bearerAuth: []
      parameters:
        - name: tag_ids
          in: query
          schema:
            type: array
            items:
              type: string
              format: uuid
        - name: search
          in: query
          schema:
            type: string
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: page_size
          in: query
          schema:
            type: integer
            default: 20
      responses:
        "200":
          description: List of companies
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListCompaniesResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/companies/{id}:
    get:
      summary: Get company by ID
      tags: [companies]
      operationId: getCompany
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Company details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CompanyResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

    patch:
      summary: Update company
      tags: [companies]
      operationId: updateCompany
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateCompanyRequest"
      responses:
        "200":
          description: Company updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CompanyResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

    delete:
      summary: Delete company
      tags: [companies]
      operationId: deleteCompany
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Company deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StatusResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/companies/{id}/contacts:
    get:
      summary: Get company contacts
      tags: [companies]
      operationId: getCompanyContacts
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: List of contacts
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListContactsResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  # Pipeline Stages
  /api/v1/pipeline-stages:
    post:
      summary: Create pipeline stage
      tags: [pipeline-stages]
      operationId: createPipelineStage
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreatePipelineStageRequest"
      responses:
        "201":
          description: Pipeline stage created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PipelineStageResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

    get:
      summary: List pipeline stages
      description: Returns stages sorted by sort_order
      tags: [pipeline-stages]
      operationId: listPipelineStages
      security:
        - bearerAuth: []
      responses:
        "200":
          description: List of pipeline stages
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListPipelineStagesResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/pipeline-stages/{id}:
    get:
      summary: Get pipeline stage by ID
      tags: [pipeline-stages]
      operationId: getPipelineStage
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Pipeline stage details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PipelineStageResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

    patch:
      summary: Update pipeline stage
      tags: [pipeline-stages]
      operationId: updatePipelineStage
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdatePipelineStageRequest"
      responses:
        "200":
          description: Pipeline stage updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PipelineStageResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

    delete:
      summary: Delete pipeline stage
      description: Cannot delete stages that have deals
      tags: [pipeline-stages]
      operationId: deletePipelineStage
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Pipeline stage deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StatusResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Stage has deals
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /api/v1/pipeline-stages/reorder:
    post:
      summary: Reorder pipeline stages
      description: Provide complete list of stage IDs in desired order
      tags: [pipeline-stages]
      operationId: reorderPipelineStages
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReorderPipelineStagesRequest"
      responses:
        "200":
          description: Stages reordered
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListPipelineStagesResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  # Deals
  /api/v1/deals:
    post:
      summary: Create deal
      tags: [deals]
      operationId: createDeal
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateDealRequest"
      responses:
        "201":
          description: Deal created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DealResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

    get:
      summary: List deals
      tags: [deals]
      operationId: listDeals
      security:
        - bearerAuth: []
      parameters:
        - name: stage_id
          in: query
          schema:
            type: string
            format: uuid
        - name: contact_id
          in: query
          schema:
            type: string
            format: uuid
        - name: company_id
          in: query
          schema:
            type: string
            format: uuid
        - name: owner_id
          in: query
          schema:
            type: string
            format: uuid
        - name: tag_ids
          in: query
          schema:
            type: array
            items:
              type: string
              format: uuid
        - name: search
          in: query
          schema:
            type: string
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: page_size
          in: query
          schema:
            type: integer
            default: 20
        - name: sort_by
          in: query
          schema:
            type: string
            enum: [name, value, created_at, updated_at, expected_close_date]
        - name: sort_desc
          in: query
          schema:
            type: boolean
            default: false
      responses:
        "200":
          description: List of deals
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListDealsResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/deals/{id}:
    get:
      summary: Get deal by ID
      tags: [deals]
      operationId: getDeal
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Deal details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DealResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

    patch:
      summary: Update deal
      tags: [deals]
      operationId: updateDeal
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateDealRequest"
      responses:
        "200":
          description: Deal updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DealResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

    delete:
      summary: Delete deal
      tags: [deals]
      operationId: deleteDeal
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Deal deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StatusResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/deals/{id}/stage:
    post:
      summary: Move deal to stage
      description: Automatically sets closed_at when moving to Won/Lost stages
      tags: [deals]
      operationId: moveDealToStage
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MoveDealToStageRequest"
      responses:
        "200":
          description: Deal moved to stage
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DealResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/deals/{id}/tags:
    post:
      summary: Add tags to deal
      tags: [deals]
      operationId: addDealTags
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TagIdsRequest"
      responses:
        "200":
          description: Tags added
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DealResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

    delete:
      summary: Remove tags from deal
      tags: [deals]
      operationId: removeDealTags
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TagIdsRequest"
      responses:
        "200":
          description: Tags removed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DealResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  # ============================================================================
  # Activities
  # ============================================================================

  /api/v1/activities:
    get:
      summary: List activities with filters
      tags: [activities]
      operationId: listActivities
      parameters:
        - name: activity_type
          in: query
          schema:
            type: string
            enum: [call, meeting, note, email, task]
        - name: contact_id
          in: query
          schema:
            type: string
            format: uuid
        - name: company_id
          in: query
          schema:
            type: string
            format: uuid
        - name: deal_id
          in: query
          schema:
            type: string
            format: uuid
        - name: assigned_to
          in: query
          schema:
            type: string
            format: uuid
        - name: is_completed
          in: query
          schema:
            type: boolean
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: page_size
          in: query
          schema:
            type: integer
            default: 20
            maximum: 100
        - name: sort_by
          in: query
          schema:
            type: string
            enum: [created_at, due_date, subject]
        - name: sort_desc
          in: query
          schema:
            type: boolean
      responses:
        "200":
          description: Activities list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListActivitiesResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

    post:
      summary: Create a new activity
      tags: [activities]
      operationId: createActivity
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateActivityRequest"
      responses:
        "201":
          description: Activity created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ActivityResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/activities/{id}:
    get:
      summary: Get activity by ID
      tags: [activities]
      operationId: getActivity
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Activity details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ActivityResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

    put:
      summary: Update an activity
      tags: [activities]
      operationId: updateActivity
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateActivityRequest"
      responses:
        "200":
          description: Activity updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ActivityResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

    delete:
      summary: Delete an activity
      tags: [activities]
      operationId: deleteActivity
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Activity deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/activities/{id}/complete:
    post:
      summary: Mark activity as completed
      tags: [activities]
      operationId: completeActivity
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Activity marked as completed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ActivityResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/activities/{id}/tags:
    post:
      summary: Add tags to activity
      tags: [activities]
      operationId: addActivityTags
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TagIdsRequest"
      responses:
        "200":
          description: Tags added
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ActivityResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

    delete:
      summary: Remove tags from activity
      tags: [activities]
      operationId: removeActivityTags
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TagIdsRequest"
      responses:
        "200":
          description: Tags removed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ActivityResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  # ============================================================================
  # Search
  # ============================================================================

  /api/v1/search:
    get:
      summary: Global search across CRM entities
      tags: [search]
      operationId: search
      parameters:
        - name: q
          in: query
          required: true
          description: Search query (2-200 characters)
          schema:
            type: string
            minLength: 2
            maxLength: 200
        - name: types
          in: query
          description: Entity types to search (comma-separated). Default searches all.
          schema:
            type: string
            example: contact,company,deal,activity
        - name: limit
          in: query
          description: Maximum results (default 20, max 100)
          schema:
            type: integer
            default: 20
            maximum: 100
      responses:
        "200":
          description: Search results
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SearchResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  # ============================================================================
  # Saved Filters
  # ============================================================================

  /api/v1/saved-filters:
    get:
      summary: List saved filters for current user
      tags: [saved-filters]
      operationId: listSavedFilters
      parameters:
        - name: entity_type
          in: query
          description: Filter by entity type (contact, company, deal, activity)
          schema:
            type: string
            enum: [contact, company, deal, activity]
      responses:
        "200":
          description: List of saved filters
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListSavedFiltersResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

    post:
      summary: Create a new saved filter
      tags: [saved-filters]
      operationId: createSavedFilter
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateSavedFilterRequest"
      responses:
        "201":
          description: Saved filter created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SavedFilterResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/saved-filters/{id}:
    get:
      summary: Get a saved filter by ID
      tags: [saved-filters]
      operationId: getSavedFilter
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Saved filter details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SavedFilterResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

    patch:
      summary: Update a saved filter
      tags: [saved-filters]
      operationId: updateSavedFilter
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateSavedFilterRequest"
      responses:
        "200":
          description: Saved filter updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SavedFilterResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

    delete:
      summary: Delete a saved filter
      tags: [saved-filters]
      operationId: deleteSavedFilter
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Saved filter deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: saved filter deleted
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  # ============================================================================
  # Reports
  # ============================================================================

  /api/v1/reports/pipeline:
    get:
      summary: Get pipeline report with deal aggregations by stage
      tags: [reports]
      operationId: getPipelineReport
      parameters:
        - name: owner_id
          in: query
          description: Filter by deal owner (UUID)
          schema:
            type: string
            format: uuid
        - name: start_date
          in: query
          required: true
          description: Start date (YYYY-MM-DD)
          schema:
            type: string
            format: date
        - name: end_date
          in: query
          required: true
          description: End date (YYYY-MM-DD)
          schema:
            type: string
            format: date
      responses:
        "200":
          description: Pipeline report
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PipelineReportResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/reports/conversion:
    get:
      summary: Get conversion report with stage transition metrics
      tags: [reports]
      operationId: getConversionReport
      parameters:
        - name: start_date
          in: query
          required: true
          description: Start date (YYYY-MM-DD)
          schema:
            type: string
            format: date
        - name: end_date
          in: query
          required: true
          description: End date (YYYY-MM-DD)
          schema:
            type: string
            format: date
      responses:
        "200":
          description: Conversion report
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConversionReportResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/reports/activities:
    get:
      summary: Get activity report with metrics by type
      tags: [reports]
      operationId: getActivityReport
      parameters:
        - name: user_id
          in: query
          description: Filter by user (creator or assignee)
          schema:
            type: string
            format: uuid
        - name: start_date
          in: query
          required: true
          description: Start date (YYYY-MM-DD)
          schema:
            type: string
            format: date
        - name: end_date
          in: query
          required: true
          description: End date (YYYY-MM-DD)
          schema:
            type: string
            format: date
      responses:
        "200":
          description: Activity report
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ActivityReportResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  # ==========================================================================
  # Chat: Channels
  # ==========================================================================

  /api/v1/channels:
    post:
      summary: Create a new channel
      tags: [chat-channels]
      operationId: createChannel
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateChannelRequest"
      responses:
        "201":
          description: Channel created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChannelResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
    get:
      summary: List channels the current user is a member of
      tags: [chat-channels]
      operationId: listChannels
      parameters:
        - name: include_archived
          in: query
          schema:
            type: boolean
            default: false
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: page_size
          in: query
          schema:
            type: integer
            default: 20
      responses:
        "200":
          description: List of channels
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListChannelsResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/channels/{id}:
    get:
      summary: Get a channel by ID
      tags: [chat-channels]
      operationId: getChannel
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Channel details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChannelResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    put:
      summary: Update a channel (owner/admin only)
      tags: [chat-channels]
      operationId: updateChannel
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateChannelRequest"
      responses:
        "200":
          description: Channel updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChannelResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      summary: Delete a channel (owner only)
      tags: [chat-channels]
      operationId: deleteChannel
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Channel deleted
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/channels/{id}/archive:
    post:
      summary: Archive a channel (owner/admin only)
      tags: [chat-channels]
      operationId: archiveChannel
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Channel archived
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChannelResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/channels/{id}/join:
    post:
      summary: Join a public channel
      tags: [chat-channels]
      operationId: joinChannel
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Joined channel
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MemberResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"

  /api/v1/channels/{id}/leave:
    post:
      summary: Leave a channel (not owner)
      tags: [chat-channels]
      operationId: leaveChannel
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Left channel
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/channels/{id}/members:
    get:
      summary: List channel members
      tags: [chat-channels]
      operationId: getChannelMembers
      parameters:
        - $ref: "#/components/parameters/ResourceId"
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: page_size
          in: query
          schema:
            type: integer
            default: 50
      responses:
        "200":
          description: List of members
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListMembersResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/channels/{id}/members/{userId}/role:
    put:
      summary: Update a member's role (owner only)
      tags: [chat-channels]
      operationId: updateMemberRole
      parameters:
        - $ref: "#/components/parameters/ResourceId"
        - name: userId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [role]
              properties:
                role:
                  type: string
                  enum: [admin, member]
      responses:
        "200":
          description: Member role updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MemberResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  # ==========================================================================
  # Chat: Messages
  # ==========================================================================

  /api/v1/channels/{id}/messages:
    get:
      summary: Get messages in a channel (cursor-based pagination)
      tags: [chat-messages]
      operationId: getMessages
      parameters:
        - $ref: "#/components/parameters/ResourceId"
        - name: limit
          in: query
          schema:
            type: integer
            default: 50
            maximum: 100
        - name: before
          in: query
          description: Get messages before this message ID
          schema:
            type: string
            format: uuid
        - name: after
          in: query
          description: Get messages after this message ID
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: List of messages
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListMessagesResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    post:
      summary: Send a message to a channel
      tags: [chat-messages]
      operationId: sendMessage
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [content]
              properties:
                content:
                  type: string
                parent_message_id:
                  type: string
                  format: uuid
                  description: Optional parent message ID for thread replies
                mentioned_user_ids:
                  type: array
                  items:
                    type: string
                    format: uuid
                  description: User IDs to mention in the message
                mention_everyone:
                  type: boolean
                  default: false
                  description: If true, mentions all channel members
      responses:
        "201":
          description: Message sent
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/messages/{id}:
    put:
      summary: Update a message (author only)
      tags: [chat-messages]
      operationId: updateMessage
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [content]
              properties:
                content:
                  type: string
      responses:
        "200":
          description: Message updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      summary: Delete a message (author or channel admin/owner)
      tags: [chat-messages]
      operationId: deleteMessage
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Message deleted
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/messages/{id}/thread:
    get:
      summary: Get thread replies for a message
      tags: [chat-messages]
      operationId: getThreadReplies
      parameters:
        - $ref: "#/components/parameters/ResourceId"
        - name: limit
          in: query
          schema:
            type: integer
            default: 50
            maximum: 100
        - name: before
          in: query
          schema:
            type: string
            format: uuid
          description: Cursor - get replies before this message ID
        - name: after
          in: query
          schema:
            type: string
            format: uuid
          description: Cursor - get replies after this message ID
      responses:
        "200":
          description: Thread replies
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ThreadRepliesResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/channels/dm:
    post:
      summary: Get or create a direct message channel with another user
      tags: [chat-channels]
      operationId: getOrCreateDM
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [other_user_id]
              properties:
                other_user_id:
                  type: string
                  format: uuid
      responses:
        "200":
          description: Existing DM channel found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetOrCreateDMResponse"
        "201":
          description: New DM channel created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetOrCreateDMResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    get:
      summary: List direct message channels for the current user
      tags: [chat-channels]
      operationId: listDMs
      parameters:
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: page_size
          in: query
          schema:
            type: integer
            default: 20
            maximum: 100
      responses:
        "200":
          description: List of DM channels
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListChannelsResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/channels/unread:
    get:
      summary: Get unread message counts for all channels
      tags: [chat-channels]
      operationId: getUnreadCounts
      responses:
        "200":
          description: Unread counts per channel
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UnreadCountsResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/channels/{id}/read:
    post:
      summary: Mark a channel as read up to a specific message
      tags: [chat-channels]
      operationId: markChannelRead
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [message_id]
              properties:
                message_id:
                  type: string
                  format: uuid
                  description: The ID of the last read message
      responses:
        "204":
          description: Channel marked as read
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/messages/mentions:
    get:
      summary: Get messages where the current user was mentioned
      tags: [chat-messages]
      operationId: getUserMentions
      parameters:
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: page_size
          in: query
          schema:
            type: integer
            default: 20
            maximum: 100
      responses:
        "200":
          description: List of mentions
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserMentionsResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/files/upload:
    post:
      summary: Upload a file to a channel
      tags: [chat-files]
      operationId: uploadFile
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file, channel_id]
              properties:
                file:
                  type: string
                  format: binary
                  description: The file to upload (max 50MB)
                channel_id:
                  type: string
                  format: uuid
                  description: Channel to attach the file to
                message_id:
                  type: string
                  format: uuid
                  description: Optional message to attach the file to
      responses:
        "201":
          description: File uploaded successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FileInfo"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: Channel is archived
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "413":
          description: File too large
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "415":
          description: Unsupported media type
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "507":
          description: Storage quota exceeded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /api/v1/files/{id}/download:
    get:
      summary: Get a presigned download URL for a file
      tags: [chat-files]
      operationId: getFileDownloadURL
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Presigned download URL
          content:
            application/json:
              schema:
                type: object
                properties:
                  url:
                    type: string
                    format: uri
                    description: Presigned URL valid for 15 minutes
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/files/{id}/thumbnail:
    get:
      summary: Get a presigned thumbnail URL for an image file
      tags: [chat-files]
      operationId: getFileThumbnailURL
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Presigned thumbnail URL
          content:
            application/json:
              schema:
                type: object
                properties:
                  url:
                    type: string
                    format: uri
                    description: Presigned URL for the thumbnail (200x200 JPEG)
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/files/{id}:
    delete:
      summary: Delete a file (soft delete)
      tags: [chat-files]
      operationId: deleteFile
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "204":
          description: File deleted
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/channels/{id}/files:
    get:
      summary: List files in a channel
      tags: [chat-files]
      operationId: listChannelFiles
      parameters:
        - $ref: "#/components/parameters/ResourceId"
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: page_size
          in: query
          schema:
            type: integer
            default: 20
            maximum: 100
      responses:
        "200":
          description: Paginated list of files
          content:
            application/json:
              schema:
                type: object
                properties:
                  files:
                    type: array
                    items:
                      $ref: "#/components/schemas/FileInfo"
                  total:
                    type: integer
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /api/v1/chat/search:
    get:
      summary: Search chat messages and files
      tags: [chat-search]
      operationId: searchChat
      parameters:
        - name: q
          in: query
          required: true
          schema:
            type: string
            minLength: 2
            maxLength: 200
          description: Search query (min 2, max 200 characters)
        - name: channel_id
          in: query
          schema:
            type: string
            format: uuid
          description: Optional filter to a specific channel
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: page_size
          in: query
          schema:
            type: integer
            default: 20
            maximum: 100
      responses:
        "200":
          description: Search results (messages and files merged by relevance)
          content:
            application/json:
              schema:
                type: object
                properties:
                  results:
                    type: array
                    items:
                      $ref: "#/components/schemas/ChatSearchResult"
                  total:
                    type: integer
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  # ===========================================================================
  # Notifications
  # ===========================================================================

  # Dialer: only the supervisor overview + contact-call-history endpoints are
  # documented here (mock-exit wiring). The remaining /api/v1/dialer/* routes
  # (campaigns, calls, agent-status, outcomes) are implemented but not yet specced.
  /api/v1/dialer/supervisor:
    get:
      summary: Supervisor overview (tenant-wide dialer aggregate)
      description: >-
        Aggregated live view for supervisors: enriched agent list, recent calls,
        and tenant totals. Serialized via response.Proto (protojson with
        UseProtoNames -> snake_case and UseEnumNumbers -> integer enums);
        timestamps are RFC3339 strings; optional fields are omitted when unset.
      tags: [dialer]
      operationId: getDialerSupervisorOverview
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Supervisor overview
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SupervisorOverview"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /api/v1/dialer/contacts/{id}/calls:
    get:
      summary: Call history for a campaign contact
      description: >-
        Returns the dialer call history for the given campaign contact id.
        Serialized via response.Proto (snake_case fields, integer enums, RFC3339
        timestamps, optional fields omitted).
      tags: [dialer]
      operationId: getDialerContactCalls
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: Campaign contact id
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Contact call history
          content:
            application/json:
              schema:
                type: object
                properties:
                  calls:
                    type: array
                    items:
                      $ref: "#/components/schemas/ContactCall"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /api/v1/notifications:
    get:
      summary: List notifications for the current user
      tags: [notifications]
      operationId: listNotifications
      security:
        - bearerAuth: []
      parameters:
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: page_size
          in: query
          schema:
            type: integer
            default: 20
            maximum: 100
        - name: module_id
          in: query
          description: Filter by module (e.g. chat, crm)
          schema:
            type: string
        - name: is_read
          in: query
          description: Filter by read status
          schema:
            type: boolean
      responses:
        "200":
          description: Paginated list of notifications
          content:
            application/json:
              schema:
                type: object
                properties:
                  notifications:
                    type: array
                    items:
                      $ref: "#/components/schemas/Notification"
                  total:
                    type: integer
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/notifications/unread-count:
    get:
      summary: Get unread notification count
      tags: [notifications]
      operationId: getUnreadCount
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Unread count
          content:
            application/json:
              schema:
                type: object
                properties:
                  count:
                    type: integer
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/notifications/{id}/read:
    post:
      summary: Mark a single notification as read
      tags: [notifications]
      operationId: markNotificationRead
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Notification marked as read
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Notification"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/notifications/{id}/pin:
    post:
      summary: Pin a notification
      description: Pins the notification for the current user so it sorts first. Response wraps the updated notification under `notification`.
      tags: [notifications]
      operationId: pinNotification
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Notification pinned
          content:
            application/json:
              schema:
                type: object
                properties:
                  notification:
                    $ref: "#/components/schemas/Notification"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      summary: Unpin a notification
      description: Removes the pin for the current user. Response wraps the updated notification under `notification`.
      tags: [notifications]
      operationId: unpinNotification
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Notification unpinned
          content:
            application/json:
              schema:
                type: object
                properties:
                  notification:
                    $ref: "#/components/schemas/Notification"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/notifications/{id}/dismiss:
    post:
      summary: Dismiss a notification
      description: Dismisses the notification for the current user, removing it from default lists and the unread count. Response wraps the updated notification under `notification`.
      tags: [notifications]
      operationId: dismissNotification
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Notification dismissed
          content:
            application/json:
              schema:
                type: object
                properties:
                  notification:
                    $ref: "#/components/schemas/Notification"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/notifications/{id}/snooze:
    post:
      summary: Snooze a notification
      description: >-
        Hides the notification from the list and the unread count until the
        given time, and marks it read. Response is the flat
        `{id, snoozed_until}` shape (not the wrapped Notification used by
        pin/dismiss).
      tags: [notifications]
      operationId: snoozeNotification
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [until]
              properties:
                until:
                  type: string
                  format: date-time
                  description: Must be in the future.
      responses:
        "200":
          description: Notification snoozed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SnoozeNotificationResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          description: Snooze time is in the past

  /api/v1/notifications/read-all:
    post:
      summary: Mark all notifications as read
      tags: [notifications]
      operationId: markAllNotificationsRead
      security:
        - bearerAuth: []
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                module_id:
                  type: string
                  description: Optional module filter
      responses:
        "200":
          description: Count of marked notifications
          content:
            application/json:
              schema:
                type: object
                properties:
                  marked_count:
                    type: integer
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/notifications/preferences:
    get:
      summary: Get notification preferences
      tags: [notifications]
      operationId: getNotificationPreferences
      security:
        - bearerAuth: []
      parameters:
        - name: module_id
          in: query
          description: Filter preferences by module
          schema:
            type: string
      responses:
        "200":
          description: List of notification preferences
          content:
            application/json:
              schema:
                type: object
                properties:
                  preferences:
                    type: array
                    items:
                      $ref: "#/components/schemas/NotificationPreference"
        "401":
          $ref: "#/components/responses/Unauthorized"
    put:
      summary: Update a notification preference
      tags: [notifications]
      operationId: updateNotificationPreference
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateNotificationPreferenceRequest"
      responses:
        "200":
          description: Preference updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NotificationPreference"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/notifications/event-types:
    get:
      summary: List available notification event types
      tags: [notifications]
      operationId: listEventTypes
      security:
        - bearerAuth: []
      parameters:
        - name: module_id
          in: query
          description: Filter by module
          schema:
            type: string
      responses:
        "200":
          description: List of event types
          content:
            application/json:
              schema:
                type: object
                properties:
                  event_types:
                    type: array
                    items:
                      $ref: "#/components/schemas/EventType"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/notifications/mutes:
    post:
      summary: Mute notifications for a resource
      tags: [notifications]
      operationId: muteResource
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [module_id, resource_id]
              properties:
                module_id:
                  type: string
                resource_id:
                  type: string
      responses:
        "201":
          description: Resource muted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NotificationMute"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
    get:
      summary: List muted resources
      tags: [notifications]
      operationId: listMutedResources
      security:
        - bearerAuth: []
      parameters:
        - name: module_id
          in: query
          schema:
            type: string
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: page_size
          in: query
          schema:
            type: integer
            default: 20
      responses:
        "200":
          description: List of muted resources
          content:
            application/json:
              schema:
                type: object
                properties:
                  mutes:
                    type: array
                    items:
                      $ref: "#/components/schemas/NotificationMute"
                  total:
                    type: integer
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/notifications/mutes/{muteId}:
    delete:
      summary: Unmute a previously muted resource
      tags: [notifications]
      operationId: unmuteResource
      security:
        - bearerAuth: []
      parameters:
        - name: muteId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Resource unmuted
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/notifications/quiet-hours:
    get:
      summary: Get quiet hours configuration
      tags: [notifications]
      operationId: getQuietHours
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Quiet hours configuration
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuietHours"
        "401":
          $ref: "#/components/responses/Unauthorized"
    put:
      summary: Update quiet hours configuration
      tags: [notifications]
      operationId: updateQuietHours
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateQuietHoursRequest"
      responses:
        "200":
          description: Quiet hours updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuietHours"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/notifications/dnd:
    post:
      summary: Toggle manual Do Not Disturb mode
      tags: [notifications]
      operationId: toggleDND
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [enabled]
              properties:
                enabled:
                  type: boolean
                until:
                  type: string
                  format: date-time
                  description: Optional end time for DND (RFC3339)
      responses:
        "200":
          description: DND toggled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuietHours"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  # ========================================================================
  # Dashboard Endpoints
  # ========================================================================

  /api/v1/dashboard/layout:
    get:
      summary: Get effective dashboard layout for authenticated user
      tags: [dashboard]
      operationId: getDashboardLayout
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Dashboard layout (user override or role default)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DashboardLayoutResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
    put:
      summary: Save personal dashboard layout
      tags: [dashboard]
      operationId: saveDashboardLayout
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SaveDashboardLayoutRequest"
      responses:
        "200":
          description: Layout saved
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DashboardLayoutResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
    delete:
      summary: Reset dashboard to role defaults
      tags: [dashboard]
      operationId: resetDashboardLayout
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Layout reset to defaults
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/dashboard/defaults/{role}:
    get:
      summary: Get default dashboard layout for a role (admin only)
      tags: [dashboard]
      operationId: getDashboardDefaults
      security:
        - bearerAuth: []
      parameters:
        - name: role
          in: path
          required: true
          schema:
            type: string
            enum: [admin, manager, member]
      responses:
        "200":
          description: Role default layout
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DashboardDefault"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    put:
      summary: Save default dashboard layout for a role (admin only)
      tags: [dashboard]
      operationId: saveDashboardDefaults
      security:
        - bearerAuth: []
      parameters:
        - name: role
          in: path
          required: true
          schema:
            type: string
            enum: [admin, manager, member]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SaveDashboardDefaultsRequest"
      responses:
        "200":
          description: Default layout saved
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DashboardDefault"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  # ===========================================================================
  # Work / Project Management
  # ===========================================================================

  /api/v1/projects:
    get:
      summary: List projects
      tags: [projects]
      operationId: listProjects
      security:
        - bearerAuth: []
      parameters:
        - name: include_archived
          in: query
          schema:
            type: boolean
            default: false
        - name: templates_only
          in: query
          schema:
            type: boolean
            default: false
        - name: search
          in: query
          schema:
            type: string
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: page_size
          in: query
          schema:
            type: integer
            default: 20
      responses:
        "200":
          description: Project list
          content:
            application/json:
              schema:
                type: object
                properties:
                  projects:
                    type: array
                    items:
                      $ref: "#/components/schemas/ProjectResponse"
                  total:
                    type: integer
        "401":
          $ref: "#/components/responses/Unauthorized"
    post:
      summary: Create project
      tags: [projects]
      operationId: createProject
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateProjectRequest"
      responses:
        "201":
          description: Project created
          content:
            application/json:
              schema:
                type: object
                properties:
                  project:
                    $ref: "#/components/schemas/ProjectResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"

  /api/v1/projects/{id}:
    get:
      summary: Get project by ID
      tags: [projects]
      operationId: getProject
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Project details
          content:
            application/json:
              schema:
                type: object
                properties:
                  project:
                    $ref: "#/components/schemas/ProjectResponse"
        "404":
          $ref: "#/components/responses/NotFound"
    put:
      summary: Update project
      tags: [projects]
      operationId: updateProject
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateProjectRequest"
      responses:
        "200":
          description: Project updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  project:
                    $ref: "#/components/schemas/ProjectResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/projects/{id}/archive:
    post:
      summary: Archive project
      tags: [projects]
      operationId: archiveProject
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Project archived
          content:
            application/json:
              schema:
                type: object
                properties:
                  project:
                    $ref: "#/components/schemas/ProjectResponse"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/projects/{id}/members:
    get:
      summary: List project members
      tags: [projects]
      operationId: listProjectMembers
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Member list
          content:
            application/json:
              schema:
                type: object
                properties:
                  members:
                    type: array
                    items:
                      $ref: "#/components/schemas/ProjectMemberResponse"
    post:
      summary: Add project member
      tags: [projects]
      operationId: addProjectMember
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [user_id, role]
              properties:
                user_id:
                  type: string
                  format: uuid
                role:
                  type: string
                  enum: [owner, member, viewer]
      responses:
        "201":
          description: Member added
          content:
            application/json:
              schema:
                type: object
                properties:
                  member:
                    $ref: "#/components/schemas/ProjectMemberResponse"
        "400":
          $ref: "#/components/responses/BadRequest"

  /api/v1/projects/{id}/members/{userId}:
    put:
      summary: Update project member role
      tags: [projects]
      operationId: updateProjectMemberRole
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: userId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [role]
              properties:
                role:
                  type: string
                  enum: [owner, member, viewer]
      responses:
        "200":
          description: Role updated
        "400":
          $ref: "#/components/responses/BadRequest"
    delete:
      summary: Remove project member
      tags: [projects]
      operationId: removeProjectMember
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: userId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Member removed

  /api/v1/projects/{id}/template:
    post:
      summary: Save project as template
      tags: [projects]
      operationId: saveProjectAsTemplate
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [template_name]
              properties:
                template_name:
                  type: string
      responses:
        "201":
          description: Template created
          content:
            application/json:
              schema:
                type: object
                properties:
                  template:
                    $ref: "#/components/schemas/ProjectResponse"

  /api/v1/projects/from-template:
    post:
      summary: Create project from template
      tags: [projects]
      operationId: createProjectFromTemplate
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [template_id, name, project_key]
              properties:
                template_id:
                  type: string
                  format: uuid
                name:
                  type: string
                project_key:
                  type: string
      responses:
        "201":
          description: Project created from template
          content:
            application/json:
              schema:
                type: object
                properties:
                  project:
                    $ref: "#/components/schemas/ProjectResponse"

  /api/v1/projects/{id}/statuses:
    get:
      summary: List project statuses
      tags: [projects]
      operationId: listProjectStatuses
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Status list
          content:
            application/json:
              schema:
                type: object
                properties:
                  statuses:
                    type: array
                    items:
                      $ref: "#/components/schemas/ProjectStatusResponse"
    post:
      summary: Create project status
      tags: [projects]
      operationId: createProjectStatus
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateProjectStatusRequest"
      responses:
        "201":
          description: Status created
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    $ref: "#/components/schemas/ProjectStatusResponse"

  /api/v1/projects/{id}/statuses/reorder:
    post:
      summary: Reorder project statuses
      tags: [projects]
      operationId: reorderProjectStatuses
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [status_ids]
              properties:
                status_ids:
                  type: array
                  items:
                    type: string
                    format: uuid
      responses:
        "200":
          description: Statuses reordered

  /api/v1/project-statuses/{id}:
    put:
      summary: Update project status
      tags: [projects]
      operationId: updateProjectStatus
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateProjectStatusRequest"
      responses:
        "200":
          description: Status updated
    delete:
      summary: Delete project status
      tags: [projects]
      operationId: deleteProjectStatus
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Status deleted

  /api/v1/projects/{id}/preferences:
    get:
      summary: Get user project preference
      tags: [projects]
      operationId: getUserProjectPreference
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: User preference
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserProjectPreferenceResponse"
    put:
      summary: Set user project preference
      tags: [projects]
      operationId: setUserProjectPreference
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SetUserProjectPreferenceRequest"
      responses:
        "200":
          description: Preference saved

  /api/v1/projects/{id}/time-entries:
    get:
      summary: List a project's completed time entries (Stunden abrechnen)
      description: >
        Completed time entries whose task belongs to this project, joined
        with task and contributor names, for the project's hours-to-invoice
        roll-up.
      tags: [projects]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: billed
          in: query
          description: "false (default) returns all completed entries; true always returns none -- nothing yet persists which entries an invoice covered."
          schema:
            type: string
            enum: ["true", "false"]
      responses:
        "200":
          description: Time entries of the project
          content:
            application/json:
              schema:
                type: object
                properties:
                  entries:
                    type: array
                    items:
                      $ref: "#/components/schemas/ProjectTimeEntry"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "503":
          description: Work service unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /api/v1/projects/{id}/team-utilization:
    get:
      summary: Project team utilization roll-up (Auslastung)
      description: >
        Every project member's tracked hours per week (last 6 ISO weeks) and
        per month (last 3 months), including members with zero tracked
        hours. Carries no cost or salary figure -- this route sits behind
        the project-read permission, not team:salary:view.
      tags: [projects]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Team utilization roll-up
          content:
            application/json:
              schema:
                type: object
                properties:
                  team:
                    type: array
                    items:
                      $ref: "#/components/schemas/MemberUtilization"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "503":
          description: Work service unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /api/v1/tasks:
    get:
      summary: List tasks with filters
      tags: [tasks]
      operationId: listTasks
      security:
        - bearerAuth: []
      parameters:
        - name: project_id
          in: query
          schema:
            type: string
            format: uuid
        - name: assignee_id
          in: query
          schema:
            type: string
            format: uuid
        - name: status_id
          in: query
          schema:
            type: string
            format: uuid
        - name: priority
          in: query
          schema:
            type: string
            enum: [urgent, high, normal, low]
        - name: parent_task_id
          in: query
          schema:
            type: string
            format: uuid
        - name: search
          in: query
          schema:
            type: string
        - name: sort_by
          in: query
          schema:
            type: string
        - name: sort_desc
          in: query
          schema:
            type: boolean
        - name: include_completed
          in: query
          schema:
            type: boolean
            default: false
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: page_size
          in: query
          schema:
            type: integer
            default: 20
      responses:
        "200":
          description: Task list
          content:
            application/json:
              schema:
                type: object
                properties:
                  tasks:
                    type: array
                    items:
                      $ref: "#/components/schemas/TaskResponse"
                  total:
                    type: integer
        "401":
          $ref: "#/components/responses/Unauthorized"
    post:
      summary: Create task
      tags: [tasks]
      operationId: createTask
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateTaskRequest"
      responses:
        "201":
          description: Task created
          content:
            application/json:
              schema:
                type: object
                properties:
                  task:
                    $ref: "#/components/schemas/TaskResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/tasks/{id}:
    get:
      summary: Get task by ID
      tags: [tasks]
      operationId: getTask
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Task details
          content:
            application/json:
              schema:
                type: object
                properties:
                  task:
                    $ref: "#/components/schemas/TaskResponse"
        "404":
          $ref: "#/components/responses/NotFound"
    put:
      summary: Update task
      tags: [tasks]
      operationId: updateTask
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateTaskRequest"
      responses:
        "200":
          description: Task updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  task:
                    $ref: "#/components/schemas/TaskResponse"
    delete:
      summary: Delete task
      tags: [tasks]
      operationId: deleteTask
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Task deleted

  /api/v1/tasks/{id}/move:
    post:
      summary: Move task (Kanban status change)
      tags: [tasks]
      operationId: moveTask
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [status_id]
              properties:
                status_id:
                  type: string
                  format: uuid
                sort_order:
                  type: number
                  format: double
      responses:
        "200":
          description: Task moved
          content:
            application/json:
              schema:
                type: object
                properties:
                  task:
                    $ref: "#/components/schemas/TaskResponse"

  /api/v1/tasks/{id}/subtasks:
    get:
      summary: List subtasks
      tags: [tasks]
      operationId: listSubtasks
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: recursive
          in: query
          schema:
            type: boolean
            default: false
      responses:
        "200":
          description: Subtask list
          content:
            application/json:
              schema:
                type: object
                properties:
                  tasks:
                    type: array
                    items:
                      $ref: "#/components/schemas/TaskResponse"

  /api/v1/tasks/{id}/dependencies:
    get:
      summary: List task dependencies
      tags: [tasks]
      operationId: listTaskDependencies
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Dependency list
          content:
            application/json:
              schema:
                type: object
                properties:
                  dependencies:
                    type: array
                    items:
                      $ref: "#/components/schemas/TaskDependencyResponse"
    post:
      summary: Create task dependency
      tags: [tasks]
      operationId: createTaskDependency
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [target_task_id, dependency_type]
              properties:
                target_task_id:
                  type: string
                  format: uuid
                dependency_type:
                  type: string
                  enum: [blocks, blocked_by, relates_to, duplicates]
      responses:
        "201":
          description: Dependency created
          content:
            application/json:
              schema:
                type: object
                properties:
                  dependency:
                    $ref: "#/components/schemas/TaskDependencyResponse"

  /api/v1/task-dependencies/{id}:
    delete:
      summary: Delete task dependency
      tags: [tasks]
      operationId: deleteTaskDependency
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Dependency deleted

  /api/v1/tasks/{id}/comments:
    get:
      summary: List task comments
      tags: [task-comments]
      operationId: listTaskComments
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: page_size
          in: query
          schema:
            type: integer
            default: 20
      responses:
        "200":
          description: Comment list
          content:
            application/json:
              schema:
                type: object
                properties:
                  comments:
                    type: array
                    items:
                      $ref: "#/components/schemas/TaskCommentResponse"
                  total:
                    type: integer
    post:
      summary: Create task comment
      tags: [task-comments]
      operationId: createTaskComment
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [content]
              properties:
                content:
                  type: string
                  maxLength: 10000
                quoted_comment_id:
                  type: string
                  format: uuid
      responses:
        "201":
          description: Comment created
          content:
            application/json:
              schema:
                type: object
                properties:
                  comment:
                    $ref: "#/components/schemas/TaskCommentResponse"

  /api/v1/task-comments/{id}:
    put:
      summary: Update task comment
      tags: [task-comments]
      operationId: updateTaskComment
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [content]
              properties:
                content:
                  type: string
                  maxLength: 10000
      responses:
        "200":
          description: Comment updated
    delete:
      summary: Delete task comment
      tags: [task-comments]
      operationId: deleteTaskComment
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Comment deleted

  /api/v1/tasks/{id}/links:
    get:
      summary: List task entity links
      tags: [task-links]
      operationId: listTaskEntityLinks
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Entity link list
          content:
            application/json:
              schema:
                type: object
                properties:
                  links:
                    type: array
                    items:
                      $ref: "#/components/schemas/TaskEntityLinkResponse"
    post:
      summary: Link CRM entity to task
      tags: [task-links]
      operationId: linkEntityToTask
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [entity_type, entity_id]
              properties:
                entity_type:
                  type: string
                  enum: [contact, company, deal, activity, project]
                entity_id:
                  type: string
                  format: uuid
      responses:
        "201":
          description: Entity linked
          content:
            application/json:
              schema:
                type: object
                properties:
                  link:
                    $ref: "#/components/schemas/TaskEntityLinkResponse"

  /api/v1/task-links/{id}:
    delete:
      summary: Unlink entity from task
      tags: [task-links]
      operationId: unlinkEntityFromTask
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Entity unlinked

  /api/v1/entity-tasks:
    get:
      summary: List tasks linked to a CRM entity
      tags: [task-links]
      operationId: listEntityTasks
      security:
        - bearerAuth: []
      parameters:
        - name: entity_type
          in: query
          required: true
          schema:
            type: string
            enum: [contact, company, deal, activity, project]
        - name: entity_id
          in: query
          required: true
          schema:
            type: string
            format: uuid
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: page_size
          in: query
          schema:
            type: integer
            default: 20
      responses:
        "200":
          description: Tasks linked to entity
          content:
            application/json:
              schema:
                type: object
                properties:
                  tasks:
                    type: array
                    items:
                      $ref: "#/components/schemas/TaskResponse"
                  total:
                    type: integer

  /api/v1/tasks/{id}/activities:
    get:
      summary: List task activity log
      tags: [tasks]
      operationId: listTaskActivities
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: page_size
          in: query
          schema:
            type: integer
            default: 20
      responses:
        "200":
          description: Activity log
          content:
            application/json:
              schema:
                type: object
                properties:
                  activities:
                    type: array
                    items:
                      $ref: "#/components/schemas/TaskActivityResponse"
                  total:
                    type: integer

  /api/v1/tasks/{id}/files:
    get:
      summary: List task files
      tags: [task-files]
      operationId: listTaskFiles
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: File list
          content:
            application/json:
              schema:
                type: object
                properties:
                  files:
                    type: array
                    items:
                      $ref: "#/components/schemas/TaskFileResponse"
    post:
      summary: Attach file to task
      tags: [task-files]
      operationId: attachFileToTask
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [filename, mime_type, file_size, storage_key]
              properties:
                filename:
                  type: string
                mime_type:
                  type: string
                file_size:
                  type: integer
                  format: int64
                storage_key:
                  type: string
                thumbnail_key:
                  type: string
      responses:
        "201":
          description: File attached
          content:
            application/json:
              schema:
                type: object
                properties:
                  file:
                    $ref: "#/components/schemas/TaskFileResponse"

  /api/v1/task-files/{id}:
    delete:
      summary: Remove file from task
      tags: [task-files]
      operationId: removeTaskFile
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: File removed

  /api/v1/tasks/{id}/custom-fields:
    get:
      summary: Get task custom field values
      tags: [tasks]
      operationId: getTaskCustomFieldValues
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Custom field values
          content:
            application/json:
              schema:
                type: object
                properties:
                  custom_fields:
                    type: object
                    additionalProperties:
                      type: string
    put:
      summary: Set task custom field values
      tags: [tasks]
      operationId: setTaskCustomFieldValues
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [values]
              properties:
                values:
                  type: array
                  items:
                    type: object
                    required: [field_id, value]
                    properties:
                      field_id:
                        type: string
                        format: uuid
                      value:
                        type: string
      responses:
        "200":
          description: Custom fields updated

  /api/v1/work/search:
    get:
      summary: Search tasks
      tags: [work-search]
      operationId: searchTasks
      security:
        - bearerAuth: []
      parameters:
        - name: q
          in: query
          required: true
          schema:
            type: string
        - name: project_ids
          in: query
          schema:
            type: array
            items:
              type: string
              format: uuid
        - name: assignee_ids
          in: query
          schema:
            type: array
            items:
              type: string
              format: uuid
        - name: include_completed
          in: query
          schema:
            type: boolean
            default: false
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: page_size
          in: query
          schema:
            type: integer
            default: 20
      responses:
        "200":
          description: Search results
          content:
            application/json:
              schema:
                type: object
                properties:
                  tasks:
                    type: array
                    items:
                      $ref: "#/components/schemas/TaskResponse"
                  total:
                    type: integer
        "400":
          $ref: "#/components/responses/BadRequest"

  /api/v1/tasks/{id}/timer/start:
    post:
      summary: Start timer on a task
      tags: [time-tracking]
      operationId: startTimer
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "201":
          description: Timer started
          content:
            application/json:
              schema:
                type: object
                properties:
                  entry:
                    $ref: "#/components/schemas/TimeEntryResponse"
                  stopped_entry:
                    $ref: "#/components/schemas/TimeEntryResponse"
                    description: Previously running timer that was auto-stopped

  /api/v1/timer/stop:
    post:
      summary: Stop the active timer for the current user
      tags: [time-tracking]
      operationId: stopTimer
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Timer stopped
          content:
            application/json:
              schema:
                type: object
                properties:
                  entry:
                    $ref: "#/components/schemas/TimeEntryResponse"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/timer/active:
    get:
      summary: Get the active timer for the current user
      tags: [time-tracking]
      operationId: getActiveTimer
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Active timer (null if none)
          content:
            application/json:
              schema:
                type: object
                properties:
                  timer:
                    $ref: "#/components/schemas/ActiveTimerResponse"

  /api/v1/tasks/{id}/time-entries:
    get:
      summary: List time entries for a task
      tags: [time-tracking]
      operationId: listTimeEntries
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: page_size
          in: query
          schema:
            type: integer
            default: 20
      responses:
        "200":
          description: Time entries
          content:
            application/json:
              schema:
                type: object
                properties:
                  entries:
                    type: array
                    items:
                      $ref: "#/components/schemas/TimeEntryResponse"
                  total:
                    type: integer
    post:
      summary: Add a manual time entry
      tags: [time-tracking]
      operationId: addManualTimeEntry
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [started_at, duration_seconds]
              properties:
                started_at:
                  type: string
                  format: date-time
                duration_seconds:
                  type: integer
                  minimum: 1
                description:
                  type: string
                  maxLength: 2000
      responses:
        "201":
          description: Time entry created
          content:
            application/json:
              schema:
                type: object
                properties:
                  entry:
                    $ref: "#/components/schemas/TimeEntryResponse"

  /api/v1/tasks/{id}/time-summary:
    get:
      summary: Get aggregated time summary for a task
      tags: [time-tracking]
      operationId: getTaskTimeSummary
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Time summary
          content:
            application/json:
              schema:
                type: object
                properties:
                  summary:
                    $ref: "#/components/schemas/TimeSummaryResponse"

  /api/v1/time-entries/{id}:
    put:
      summary: Update a time entry
      tags: [time-tracking]
      operationId: updateTimeEntry
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                started_at:
                  type: string
                  format: date-time
                duration_seconds:
                  type: integer
                  minimum: 1
                description:
                  type: string
                  maxLength: 2000
      responses:
        "200":
          description: Time entry updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  entry:
                    $ref: "#/components/schemas/TimeEntryResponse"
    delete:
      summary: Delete a time entry
      tags: [time-tracking]
      operationId: deleteTimeEntry
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Time entry deleted

  # ===========================================================================
  # Calendar – Calendar CRUD
  # ===========================================================================

  /api/v1/calendar/calendars:
    post:
      summary: Create a new calendar
      tags: [calendars]
      operationId: createCalendar
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateCalendarRequest"
      responses:
        "201":
          description: Calendar created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CalendarResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

    get:
      summary: List calendars for current user
      tags: [calendars]
      operationId: listCalendars
      security:
        - bearerAuth: []
      parameters:
        - name: include_hidden
          in: query
          description: Include calendars the user has hidden
          schema:
            type: boolean
            default: false
      responses:
        "200":
          description: List of calendars with member info
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CalendarListResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/calendar/calendars/{id}:
    get:
      summary: Get calendar by ID
      tags: [calendars]
      operationId: getCalendar
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Calendar details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CalendarResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

    put:
      summary: Update a calendar
      tags: [calendars]
      operationId: updateCalendar
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateCalendarRequest"
      responses:
        "200":
          description: Calendar updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CalendarResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

    delete:
      summary: Delete a calendar
      tags: [calendars]
      operationId: deleteCalendar
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Calendar deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StatusResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  # ===========================================================================
  # Calendar – Members
  # ===========================================================================

  /api/v1/calendar/calendars/{id}/members:
    get:
      summary: List calendar members
      tags: [calendars]
      operationId: listCalendarMembers
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: List of calendar members
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CalendarMembersResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

    post:
      summary: Add a member to a calendar
      tags: [calendars]
      operationId: addCalendarMember
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AddCalendarMemberRequest"
      responses:
        "201":
          description: Member added
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CalendarMemberSchema"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/calendar/calendars/{id}/members/{userId}:
    delete:
      summary: Remove a member from a calendar
      tags: [calendars]
      operationId: removeCalendarMember
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
        - name: userId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Member removed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StatusResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/calendar/calendars/{id}/members/{userId}/permission:
    put:
      summary: Update a calendar member's permission level
      tags: [calendars]
      operationId: updateCalendarMemberPermission
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
        - name: userId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateCalendarMemberPermissionRequest"
      responses:
        "200":
          description: Permission updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CalendarMemberSchema"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  # ===========================================================================
  # Calendar – Discovery & Subscriptions
  # ===========================================================================

  /api/v1/calendar/browse:
    get:
      summary: Browse subscribable calendars
      tags: [calendars]
      operationId: listBrowsableCalendars
      security:
        - bearerAuth: []
      parameters:
        - name: search
          in: query
          description: Filter calendars by name
          schema:
            type: string
      responses:
        "200":
          description: List of browsable calendars
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CalendarListResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/calendar/calendars/{id}/subscribe:
    post:
      summary: Subscribe to a calendar
      tags: [calendars]
      operationId: subscribeToCalendar
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Subscribed successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CalendarMemberSchema"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

    delete:
      summary: Unsubscribe from a calendar
      tags: [calendars]
      operationId: unsubscribeFromCalendar
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Unsubscribed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StatusResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  # ===========================================================================
  # Calendar – Preferences
  # ===========================================================================

  /api/v1/calendar/preferences:
    get:
      summary: Get calendar preferences for current user
      tags: [calendars]
      operationId: getCalendarPreferences
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Calendar preferences
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CalendarPreferencesResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

    put:
      summary: Update calendar preferences for current user
      tags: [calendars]
      operationId: updateCalendarPreferences
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateCalendarPreferencesRequest"
      responses:
        "200":
          description: Preferences updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CalendarPreferencesResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  # ===========================================================================
  # Calendar – Event Categories
  # ===========================================================================

  /api/v1/calendar/categories:
    get:
      summary: List event categories for current user
      tags: [events]
      operationId: listEventCategories
      security:
        - bearerAuth: []
      responses:
        "200":
          description: List of event categories
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EventCategoriesResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

    post:
      summary: Create an event category
      tags: [events]
      operationId: createEventCategory
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateEventCategoryRequest"
      responses:
        "201":
          description: Category created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EventCategorySchema"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/calendar/categories/{id}:
    delete:
      summary: Delete an event category
      tags: [events]
      operationId: deleteEventCategory
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Category deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StatusResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  # ===========================================================================
  # Calendar – Events
  # ===========================================================================

  /api/v1/calendar/events:
    post:
      summary: Create a calendar event
      tags: [events]
      operationId: createEvent
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateEventRequest"
      responses:
        "201":
          description: Event created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CalendarEventResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

    get:
      summary: List events in a date range
      description: Returns expanded events (including recurring instances) within the given time range.
      tags: [events]
      operationId: listEventsInRange
      security:
        - bearerAuth: []
      parameters:
        - name: start
          in: query
          required: true
          description: Range start (ISO 8601 / RFC 3339)
          schema:
            type: string
            format: date-time
        - name: end
          in: query
          required: true
          description: Range end (ISO 8601 / RFC 3339)
          schema:
            type: string
            format: date-time
        - name: calendar_ids
          in: query
          description: Filter by specific calendar IDs
          schema:
            type: array
            items:
              type: string
              format: uuid
      responses:
        "200":
          description: List of expanded events
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ExpandedEventListResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/calendar/events/{id}:
    get:
      summary: Get event by ID
      tags: [events]
      operationId: getEvent
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Event details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CalendarEventResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

    put:
      summary: Update a calendar event
      tags: [events]
      operationId: updateEvent
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateEventRequest"
      responses:
        "200":
          description: Event updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CalendarEventResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

    delete:
      summary: Delete a calendar event
      tags: [events]
      operationId: deleteEvent
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Event deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StatusResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  # ===========================================================================
  # Calendar – Recurring Events
  # ===========================================================================

  /api/v1/calendar/events/{id}/recurring:
    put:
      summary: Update a recurring event with scope control
      description: |
        Edit a recurring event with three scope options:
        - `this`: Creates an exception for the specific occurrence
        - `this_and_future`: Splits the series, applying changes from the given date forward
        - `all`: Updates the master recurring event
      tags: [events]
      operationId: updateRecurringEvent
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateRecurringEventRequest"
      responses:
        "200":
          description: Recurring event updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CalendarEventResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  # ===========================================================================
  # Calendar – Event Attendees
  # ===========================================================================

  /api/v1/calendar/events/{id}/attendees:
    get:
      summary: List event attendees
      tags: [events]
      operationId: listEventAttendees
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: List of attendees
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EventAttendeesResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  # ===========================================================================
  # Calendar – RSVP
  # ===========================================================================

  /api/v1/calendar/events/{id}/rsvp:
    post:
      summary: RSVP to an event
      tags: [events]
      operationId: rsvpToEvent
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RSVPRequest"
      responses:
        "200":
          description: RSVP recorded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EventAttendeeSchema"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  # ===========================================================================
  # Calendar – Reminders
  # ===========================================================================

  /api/v1/calendar/events/{id}/reminders:
    get:
      summary: List reminders for an event
      tags: [events]
      operationId: listEventReminders
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: List of reminders
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EventRemindersResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

    put:
      summary: Set reminders for an event (replaces existing)
      tags: [events]
      operationId: setEventReminders
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SetEventRemindersRequest"
      responses:
        "200":
          description: Reminders updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EventRemindersResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  # ===========================================================================
  # Calendar – Resources
  # ===========================================================================

  /api/v1/calendar/resources:
    post:
      summary: Create a resource (room, equipment, vehicle)
      tags: [resources]
      operationId: createResource
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateResourceRequest"
      responses:
        "201":
          description: Resource created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ResourceResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

    get:
      summary: List resources
      tags: [resources]
      operationId: listResources
      security:
        - bearerAuth: []
      parameters:
        - name: type
          in: query
          description: Filter by resource type
          schema:
            type: string
            enum: [room, equipment, vehicle]
        - name: min_capacity
          in: query
          description: Minimum capacity filter
          schema:
            type: integer
        - name: floor
          in: query
          description: Filter by floor
          schema:
            type: string
        - name: tags
          in: query
          description: Comma-separated tag filter
          schema:
            type: string
        - name: include_inactive
          in: query
          description: Include soft-deleted resources
          schema:
            type: boolean
            default: false
      responses:
        "200":
          description: List of resources
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ResourceListResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/calendar/resources/{id}:
    get:
      summary: Get resource by ID
      tags: [resources]
      operationId: getResource
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Resource details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ResourceResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

    put:
      summary: Update a resource
      tags: [resources]
      operationId: updateResource
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateResourceRequest"
      responses:
        "200":
          description: Resource updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ResourceResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

    delete:
      summary: Delete a resource (soft-delete)
      tags: [resources]
      operationId: deleteResource
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Resource deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StatusResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  # ===========================================================================
  # Calendar – Resource Availability
  # ===========================================================================

  /api/v1/calendar/resources/{id}/availability:
    get:
      summary: Get resource availability in a date range
      tags: [resources]
      operationId: getResourceAvailability
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
        - name: start
          in: query
          required: true
          description: Range start (ISO 8601 / RFC 3339)
          schema:
            type: string
            format: date-time
        - name: end
          in: query
          required: true
          description: Range end (ISO 8601 / RFC 3339)
          schema:
            type: string
            format: date-time
      responses:
        "200":
          description: Availability information with existing bookings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ResourceBookingsResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  # ===========================================================================
  # Calendar – Resource Bookings
  # ===========================================================================

  /api/v1/calendar/bookings:
    post:
      summary: Book a resource for an event
      tags: [resources]
      operationId: bookResource
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BookResourceRequest"
      responses:
        "201":
          description: Booking created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ResourceBookingSchema"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"

  /api/v1/calendar/bookings/{id}:
    delete:
      summary: Cancel a resource booking
      tags: [resources]
      operationId: cancelBooking
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Booking cancelled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StatusResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  # ===========================================================================
  # Calendar – Holidays
  # ===========================================================================

  /api/v1/calendar/holidays:
    get:
      summary: List public holidays
      tags: [holidays]
      operationId: listHolidays
      security:
        - bearerAuth: []
      parameters:
        - name: country_code
          in: query
          required: true
          description: ISO 3166-1 alpha-2 country code (e.g. DE, AT, CH)
          schema:
            type: string
        - name: subdivision_code
          in: query
          description: Subdivision code (e.g. DE-BY for Bavaria)
          schema:
            type: string
        - name: start
          in: query
          description: Filter start date (ISO 8601 / RFC 3339)
          schema:
            type: string
            format: date-time
        - name: end
          in: query
          description: Filter end date (ISO 8601 / RFC 3339)
          schema:
            type: string
            format: date-time
      responses:
        "200":
          description: List of public holidays
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HolidayListResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/calendar/holidays/seed:
    post:
      summary: Seed holidays for a country and year from Nager.Date API
      tags: [holidays]
      operationId: seedHolidays
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SeedHolidaysRequest"
      responses:
        "201":
          description: Holidays seeded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HolidayListResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  # ===========================================================================
  # Calendar – Task Deadlines
  # ===========================================================================

  /api/v1/calendar/task-deadlines:
    get:
      summary: List task deadlines in a date range (calendar overlay)
      tags: [events]
      operationId: listTaskDeadlines
      security:
        - bearerAuth: []
      parameters:
        - name: start
          in: query
          required: true
          description: Range start (ISO 8601 / RFC 3339)
          schema:
            type: string
            format: date-time
        - name: end
          in: query
          required: true
          description: Range end (ISO 8601 / RFC 3339)
          schema:
            type: string
            format: date-time
      responses:
        "200":
          description: List of task deadline stubs
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TaskDeadlinesResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  # ===========================================================================
  # Calendar – LiveKit Video
  # ===========================================================================

  /api/v1/calendar/events/{id}/join-token:
    post:
      summary: Generate a LiveKit join token for a video call event
      tags: [events]
      operationId: generateJoinToken
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GenerateJoinTokenRequest"
      responses:
        "200":
          description: Join token generated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JoinTokenResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/calendar/booking-pages:
    get:
      summary: List booking pages (admin)
      tags: [calendar]
      operationId: listBookingPages
      security: [{bearerAuth: []}]
      parameters:
        - name: include_inactive
          in: query
          schema: {type: boolean, default: false}
      responses:
        "200": {description: List of booking pages}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
    post:
      summary: Create booking page (admin)
      tags: [calendar]
      operationId: createBookingPage
      security: [{bearerAuth: []}]
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: "#/components/schemas/BookingPageInput"}
      responses:
        "201": {description: Created booking page}
        "400": {$ref: "#/components/responses/BadRequest"}
        "403": {$ref: "#/components/responses/Forbidden"}
  /api/v1/calendar/booking-pages/{id}:
    get:
      summary: Get booking page (admin)
      tags: [calendar]
      operationId: getBookingPage
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
      responses:
        "200": {description: Booking page}
        "404": {$ref: "#/components/responses/NotFound"}
    put:
      summary: Update booking page (admin)
      tags: [calendar]
      operationId: updateBookingPage
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: "#/components/schemas/BookingPageUpdateInput"}
      responses:
        "200": {description: Updated booking page}
        "404": {$ref: "#/components/responses/NotFound"}
    delete:
      summary: Delete booking page (admin)
      tags: [calendar]
      operationId: deleteBookingPage
      security: [{bearerAuth: []}]
      parameters:
        - {name: id, in: path, required: true, schema: {type: string, format: uuid}}
      responses:
        "200": {description: Deleted}
        "404": {$ref: "#/components/responses/NotFound"}
  /api/v1/public/booking-pages/{slug}:
    get:
      summary: Get public booking page by slug (no auth)
      tags: [public-booking]
      operationId: getPublicBookingPage
      security: []
      parameters:
        - {name: slug, in: path, required: true, schema: {type: string}}
      responses:
        "200": {description: Public booking page}
        "404": {$ref: "#/components/responses/NotFound"}
  /api/v1/public/booking-pages/{slug}/availability:
    get:
      summary: Get available slots for a booking page (no auth)
      tags: [public-booking]
      operationId: getPublicAvailability
      security: []
      parameters:
        - {name: slug, in: path, required: true, schema: {type: string}}
        - {name: date_from, in: query, required: true, schema: {type: string, format: date}}
        - {name: date_to, in: query, required: true, schema: {type: string, format: date}}
        - {name: service_id, in: query, schema: {type: string}}
      responses:
        "200": {description: Available slots per day}
        "400": {$ref: "#/components/responses/BadRequest"}
        "404": {$ref: "#/components/responses/NotFound"}
  /api/v1/public/bookings:
    post:
      summary: Create a public booking (no auth)
      tags: [public-booking]
      operationId: createPublicBooking
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: "#/components/schemas/PublicBookingInput"}
      responses:
        "201": {description: Booking confirmed}
        "400": {$ref: "#/components/responses/BadRequest"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "410": {description: Slot in the past or within lead time}
        "429": {description: Rate limited}

  # ===========================================================================
  # Video Calls
  # ===========================================================================

  /api/v1/video/calls:
    post:
      summary: Create a new video call
      tags: [video-calls]
      operationId: createCall
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateCallRequest"
      responses:
        "201":
          description: Call created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CallSessionResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
    get:
      summary: List active calls for the authenticated user
      tags: [video-calls]
      operationId: listActiveCalls
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Active calls listed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListActiveCallsResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/video/calls/{id}:
    get:
      summary: Get a specific call by ID
      tags: [video-calls]
      operationId: getCall
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Call details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CallSessionResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/video/calls/{id}/join:
    post:
      summary: Join an active call
      tags: [video-calls]
      operationId: joinCall
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Joined call with LiveKit token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JoinCallResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/video/calls/{id}/end:
    post:
      summary: End an active call
      tags: [video-calls]
      operationId: endCall
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Call ended
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: call ended
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  # ===========================================================================
  # Recording
  # ===========================================================================

  /api/v1/video/recordings/start:
    post:
      summary: Start recording a call or meeting
      tags: [recordings]
      operationId: startRecording
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StartRecordingRequest"
      responses:
        "201":
          description: Recording started
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecordingResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/video/recordings/{id}/stop:
    post:
      summary: Stop an active recording
      tags: [recordings]
      operationId: stopRecording
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Recording stopped
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecordingResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/video/recordings/{id}/consent:
    post:
      summary: Set recording consent for authenticated user
      tags: [recordings]
      operationId: setRecordingConsent
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SetRecordingConsentRequest"
      responses:
        "200":
          description: Consent recorded
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: consent recorded
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
    get:
      summary: Get recording consent status
      tags: [recordings]
      operationId: getRecordingConsent
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Consent status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecordingConsentResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/video/recordings:
    get:
      summary: List recordings with optional filters
      tags: [recordings]
      operationId: listRecordings
      security:
        - bearerAuth: []
      parameters:
        - name: call_id
          in: query
          schema:
            type: string
            format: uuid
        - name: meeting_id
          in: query
          schema:
            type: string
            format: uuid
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: page_size
          in: query
          schema:
            type: integer
            default: 20
      responses:
        "200":
          description: Recordings listed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListRecordingsResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/video/recordings/{id}:
    delete:
      summary: Delete a recording (DSGVO compliance)
      tags: [recordings]
      operationId: deleteRecording
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Recording deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: recording deleted
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  # ===========================================================================
  # Presence
  # ===========================================================================

  /api/v1/video/presence/{userId}:
    get:
      summary: Get presence status for a specific user
      tags: [presence]
      operationId: getPresence
      security:
        - bearerAuth: []
      parameters:
        - name: userId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: User presence status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PresenceResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/video/presence/bulk:
    post:
      summary: Get presence status for multiple users
      tags: [presence]
      operationId: getBulkPresence
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BulkPresenceRequest"
      responses:
        "200":
          description: Bulk presence status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BulkPresenceResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/video/presence/status:
    post:
      summary: Set manual presence status for the authenticated user
      tags: [presence]
      operationId: setPresenceStatus
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SetPresenceStatusRequest"
      responses:
        "200":
          description: Presence status updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: presence updated
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/video/presence/config:
    get:
      summary: Get presence configuration
      tags: [presence]
      operationId: getPresenceConfig
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Presence configuration
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PresenceConfigResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
    put:
      summary: Update presence configuration (requires settings:write permission)
      tags: [presence]
      operationId: updatePresenceConfig
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdatePresenceConfigRequest"
      responses:
        "200":
          description: Presence config updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: presence config updated
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  # ===========================================================================
  # Reactions (via chat service — /api/v1/messages)
  # ===========================================================================

  /api/v1/messages/{id}/reactions:
    post:
      summary: Toggle a reaction on a message (add or remove)
      tags: [reactions]
      operationId: toggleReaction
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [emoji]
              properties:
                emoji:
                  type: string
                  maxLength: 32
      responses:
        "200":
          description: Reaction toggled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReactionResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
    get:
      summary: List individual reactions for a message
      tags: [reactions]
      operationId: listReactions
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Reactions listed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListReactionsResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/messages/reactions/summary:
    post:
      summary: Get reaction summary for multiple messages (batch)
      tags: [reactions]
      operationId: getReactionSummary
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReactionSummaryRequest"
      responses:
        "200":
          description: Reaction summaries
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReactionSummaryResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/messages/bookmarks:
    get:
      summary: List the current user's bookmarked messages, newest bookmark first
      tags: [chat-messages]
      operationId: listBookmarks
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Bookmarked messages
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListBookmarksResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/messages/{id}/bookmark:
    post:
      summary: Toggle the bookmark state of a message for the current user
      tags: [chat-messages]
      operationId: toggleBookmark
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Bookmark toggled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ToggleBookmarkResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: Caller is not a member of the message's channel
        "404":
          $ref: "#/components/responses/NotFound"

  # ===========================================================================
  # Meetings
  # ===========================================================================

  /api/v1/meetings:
    post:
      summary: Create a new meeting
      tags: [meetings]
      operationId: createMeeting
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateMeetingRequest"
      responses:
        "201":
          description: Meeting created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeetingResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
    get:
      summary: List meetings for the authenticated user
      tags: [meetings]
      operationId: listMeetings
      security:
        - bearerAuth: []
      parameters:
        - name: status
          in: query
          schema:
            type: string
            enum: [scheduled, in_progress, completed, cancelled]
        - name: start_after
          in: query
          schema:
            type: string
            format: date-time
        - name: start_before
          in: query
          schema:
            type: string
            format: date-time
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: page_size
          in: query
          schema:
            type: integer
            default: 20
      responses:
        "200":
          description: Meetings listed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListMeetingsResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/meetings/{id}:
    get:
      summary: Get a specific meeting
      tags: [meetings]
      operationId: getMeeting
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Meeting details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeetingResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    put:
      summary: Update a meeting
      tags: [meetings]
      operationId: updateMeeting
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateMeetingRequest"
      responses:
        "200":
          description: Meeting updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeetingResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      summary: Delete a meeting
      tags: [meetings]
      operationId: deleteMeeting
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Meeting deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: meeting deleted
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/meetings/{id}/start:
    post:
      summary: Start a scheduled meeting (creates LiveKit room)
      tags: [meetings]
      operationId: startMeeting
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Meeting started with LiveKit token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StartMeetingResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/meetings/{id}/join:
    post:
      summary: Join a meeting (idempotent; organizer auto-starts, attendees get a token)
      tags: [meetings]
      operationId: joinMeeting
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: LiveKit token, ws_url and per-session TURN ice_servers for the room
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JoinMeetingResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/meetings/{id}/end:
    post:
      summary: End an active meeting
      tags: [meetings]
      operationId: endMeeting
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Meeting ended with summary
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EndMeetingResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  # ===========================================================================
  # Meeting Notes
  # ===========================================================================

  /api/v1/meetings/{id}/notes:
    put:
      summary: Save or update meeting notes
      tags: [meeting-notes]
      operationId: saveMeetingNotes
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SaveMeetingNotesRequest"
      responses:
        "200":
          description: Notes saved
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeetingNotesResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    get:
      summary: Get meeting notes
      tags: [meeting-notes]
      operationId: getMeetingNotes
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Meeting notes
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeetingNotesListResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/meetings/{id}/previous-notes:
    get:
      summary: Get notes from the previous recurring meeting
      tags: [meeting-notes]
      operationId: getPreviousMeetingNotes
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Previous meeting notes
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeetingNotesListResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  # ===========================================================================
  # Action Items
  # ===========================================================================

  /api/v1/meetings/{id}/action-items:
    post:
      summary: Create an action item for a meeting
      tags: [action-items]
      operationId: createActionItem
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateActionItemRequest"
      responses:
        "201":
          description: Action item created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ActionItemResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    get:
      summary: List action items for a meeting
      tags: [action-items]
      operationId: listActionItems
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Action items listed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListActionItemsResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/meetings/action-items/{itemId}:
    put:
      summary: Update an action item
      tags: [action-items]
      operationId: updateActionItem
      security:
        - bearerAuth: []
      parameters:
        - name: itemId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateActionItemRequest"
      responses:
        "200":
          description: Action item updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ActionItemResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      summary: Delete an action item
      tags: [action-items]
      operationId: deleteActionItem
      security:
        - bearerAuth: []
      parameters:
        - name: itemId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Action item deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: action item deleted
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/meetings/{id}/action-items/convert:
    post:
      summary: Convert action items to project tasks
      tags: [action-items]
      operationId: convertActionItemsToTasks
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ConvertActionItemsRequest"
      responses:
        "200":
          description: Action items converted to tasks
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConvertActionItemsResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  # ===========================================================================
  # LiveKit Webhook
  # ===========================================================================

  /api/v1/webhooks/livekit:
    post:
      summary: LiveKit webhook endpoint (validated via JWT signature, no bearer auth)
      tags: [webhooks]
      operationId: handleLiveKitWebhook
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: LiveKit webhook event payload
              properties:
                event:
                  type: string
                  description: "Event type (participant_joined, participant_left, room_finished, egress_ended)"
                room:
                  type: object
                  properties:
                    name:
                      type: string
                participant:
                  type: object
                  properties:
                    identity:
                      type: string
                egress_info:
                  type: object
                  properties:
                    egress_id:
                      type: string
                    status:
                      type: integer
      responses:
        "200":
          description: Webhook acknowledged
        "400":
          $ref: "#/components/responses/BadRequest"

  # ===========================================================================
  # Finance: Company Settings
  # ===========================================================================
  /api/v1/finance/settings:
    get:
      summary: Get company settings
      tags: [finance-settings]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Company settings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CompanySettings"
        "401":
          $ref: "#/components/responses/Unauthorized"
    put:
      summary: Update company settings
      tags: [finance-settings]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CompanySettingsInput"
      responses:
        "200":
          description: Updated company settings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CompanySettings"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  # ===========================================================================
  # Finance: Quotes
  # ===========================================================================
  /api/v1/finance/quotes:
    post:
      summary: Create a new quote
      tags: [quotes]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateQuoteRequest"
      responses:
        "201":
          description: Quote created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Quote"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
    get:
      summary: List quotes
      tags: [quotes]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: status
          in: query
          schema:
            type: string
            enum: [draft, sent, accepted, rejected, expired]
        - name: deal_id
          in: query
          schema:
            type: string
        - $ref: "#/components/parameters/page"
        - $ref: "#/components/parameters/page_size"
      responses:
        "200":
          description: List of quotes
          content:
            application/json:
              schema:
                type: object
                properties:
                  quotes:
                    type: array
                    items:
                      $ref: "#/components/schemas/Quote"
                  total:
                    type: integer
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/finance/quotes/{id}:
    get:
      summary: Get a quote by ID
      tags: [quotes]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Quote details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Quote"
        "404":
          $ref: "#/components/responses/NotFound"
    put:
      summary: Update a quote
      tags: [quotes]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateQuoteRequest"
      responses:
        "200":
          description: Updated quote
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Quote"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      summary: Delete a quote
      tags: [quotes]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Quote deleted
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/finance/quotes/{id}/send:
    post:
      summary: Send a quote to the customer
      tags: [quotes]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Quote sent
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Quote"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/finance/quotes/{id}/accept:
    post:
      summary: Accept a quote
      tags: [quotes]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Quote accepted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Quote"

  /api/v1/finance/quotes/{id}/reject:
    post:
      summary: Reject a quote
      tags: [quotes]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Quote rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Quote"

  /api/v1/finance/quotes/{id}/convert:
    post:
      summary: Convert a quote to an invoice
      tags: [quotes]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                invoice_date:
                  type: string
                  format: date
                due_date:
                  type: string
                  format: date
                delivery_date:
                  type: string
                  format: date
                payment_terms:
                  type: string
      responses:
        "201":
          description: Invoice created from quote
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Invoice"

  /api/v1/finance/quotes/{id}/pdf:
    get:
      summary: Generate PDF for a quote
      tags: [quotes]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: PDF binary
          content:
            application/pdf:
              schema:
                type: string
                format: binary
        "501":
          description: PDF generation not yet available via gateway

  # ===========================================================================
  # Finance: Invoices
  # ===========================================================================
  /api/v1/finance/invoices:
    post:
      summary: Create a new invoice
      tags: [invoices]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateInvoiceRequest"
      responses:
        "201":
          description: Invoice created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Invoice"
    get:
      summary: List invoices
      tags: [invoices]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: status
          in: query
          schema:
            type: string
            enum: [draft, sent, paid, overdue, cancelled]
        - $ref: "#/components/parameters/page"
        - $ref: "#/components/parameters/page_size"
        - name: contact_id
          in: query
          description: Filter to invoices linked to this CRM contact (Contact-360 view)
          schema:
            type: string
            format: uuid
        - name: recurring_id
          in: query
          description: Filter to invoices emitted by this recurring schedule (Migration 000246)
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: List of invoices
          content:
            application/json:
              schema:
                type: object
                properties:
                  invoices:
                    type: array
                    items:
                      $ref: "#/components/schemas/Invoice"
                  total:
                    type: integer

  /api/v1/finance/invoices/{id}:
    get:
      summary: Get an invoice by ID
      tags: [invoices]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Invoice details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Invoice"
    put:
      summary: Update a draft invoice
      tags: [invoices]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateInvoiceRequest"
      responses:
        "200":
          description: Updated invoice
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Invoice"
        "410":
          description: Invoice is immutable (already sent)

  /api/v1/finance/invoices/{id}/send:
    post:
      summary: Send an invoice (freezes snapshot, GoBD immutable)
      tags: [invoices]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Invoice sent
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Invoice"

  /api/v1/finance/invoices/{id}/mark-paid:
    post:
      summary: Mark an invoice as fully paid
      tags: [invoices]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Invoice marked as paid
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Invoice"

  /api/v1/finance/invoices/{id}/cancel:
    post:
      summary: Cancel an invoice (creates reversal entry)
      tags: [invoices]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Invoice cancelled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Invoice"

  /api/v1/finance/invoices/{id}/pdf:
    get:
      summary: Generate PDF for an invoice
      description: >
        With `format=zugferd` the PDF carries the Factur-X/ZUGFeRD XML as an
        embedded attachment named `factur-x.xml`, declared as the machine-readable
        alternative of the document. An invoice that does not meet EN 16931 is
        rejected with 409 listing every unmet requirement — no plain PDF is
        returned in its place, because the caller could not tell it apart from
        an e-invoice.
      tags: [invoices]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
        - name: format
          in: query
          required: false
          schema:
            type: string
            enum: [zugferd]
          description: Embed the Factur-X/ZUGFeRD XML into the PDF.
      responses:
        "200":
          description: PDF binary
          content:
            application/pdf:
              schema:
                type: string
                format: binary
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Invoice does not meet the e-invoice requirements
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /api/v1/finance/invoices/{id}/erechnung:
    post:
      summary: Generate an outbound e-invoice (XRechnung or ZUGFeRD)
      description: >
        `format=xrechnung` returns a bare XRechnung UBL 2.1 XML document.
        `format=zugferd` returns a PDF with the Factur-X/ZUGFeRD XML embedded and
        declared, identical to `GET .../pdf?format=zugferd`. `buyer_reference`
        is BT-10 (Leitweg-ID); it only applies to xrechnung, and supplying it
        also enforces the stricter German CIUS for public-sector buyers on top
        of the EN 16931 core. An invoice that does not meet the applicable
        requirements is rejected with 409 listing every unmet one — no partial
        document is returned in its place.
      tags: [invoices]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
        - name: format
          in: query
          required: true
          schema:
            type: string
            enum: [xrechnung, zugferd]
        - name: buyer_reference
          in: query
          required: false
          schema:
            type: string
          description: Leitweg-ID (BT-10), xrechnung only.
      responses:
        "200":
          description: XRechnung XML or ZUGFeRD PDF, depending on format
          content:
            application/xml:
              schema:
                type: string
                format: binary
            application/pdf:
              schema:
                type: string
                format: binary
        "400":
          description: format missing or not one of xrechnung, zugferd
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Invoice does not meet the e-invoice requirements
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /api/v1/finance/invoices/{id}/payments:
    post:
      summary: Record a payment against an invoice
      tags: [payments]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RecordPaymentRequest"
      responses:
        "201":
          description: Payment recorded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Payment"
    get:
      summary: List payments for an invoice
      tags: [payments]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
        - $ref: "#/components/parameters/page"
        - $ref: "#/components/parameters/page_size"
      responses:
        "200":
          description: List of payments
          content:
            application/json:
              schema:
                type: object
                properties:
                  payments:
                    type: array
                    items:
                      $ref: "#/components/schemas/Payment"
                  total:
                    type: integer

  # ===========================================================================
  # Finance: Credit Notes
  # ===========================================================================
  /api/v1/finance/recurring:
    get:
      summary: List recurring invoice schedules
      tags: [recurring-invoices]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: status
          in: query
          schema:
            type: string
            enum: [active, paused, ended]
        - $ref: "#/components/parameters/page"
        - $ref: "#/components/parameters/page_size"
      responses:
        "200":
          description: List of schedules
          content:
            application/json:
              schema:
                type: object
                properties:
                  recurring:
                    type: array
                    items:
                      $ref: "#/components/schemas/RecurringInvoice"
                  total:
                    type: integer
    post:
      summary: Create a recurring invoice schedule
      tags: [recurring-invoices]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateRecurringInvoiceRequest"
      responses:
        "201":
          description: Schedule created
          content:
            application/json:
              schema:
                type: object
                properties:
                  recurring:
                    $ref: "#/components/schemas/RecurringInvoice"
        "400":
          description: Invalid interval, date range or empty line items

  /api/v1/finance/recurring/{id}:
    get:
      summary: Get a recurring invoice schedule
      tags: [recurring-invoices]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Schedule details
          content:
            application/json:
              schema:
                type: object
                properties:
                  recurring:
                    $ref: "#/components/schemas/RecurringInvoice"
        "404":
          description: Schedule not found
    put:
      summary: Update a recurring invoice schedule (partial)
      tags: [recurring-invoices]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateRecurringInvoiceRequest"
      responses:
        "200":
          description: Updated schedule
          content:
            application/json:
              schema:
                type: object
                properties:
                  recurring:
                    $ref: "#/components/schemas/RecurringInvoice"
        "400":
          description: Invalid interval or date range
        "404":
          description: Schedule not found
    delete:
      summary: Delete a recurring invoice schedule
      tags: [recurring-invoices]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Schedule deleted
          content:
            application/json:
              schema:
                type: object
        "404":
          description: Schedule not found

  /api/v1/finance/recurring/{id}/pause:
    post:
      summary: Pause a recurring invoice schedule
      tags: [recurring-invoices]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Paused schedule
          content:
            application/json:
              schema:
                type: object
                properties:
                  recurring:
                    $ref: "#/components/schemas/RecurringInvoice"
        "404":
          description: Schedule not found

  /api/v1/finance/recurring/{id}/resume:
    post:
      summary: Resume a paused recurring invoice schedule
      description: A schedule whose end date has passed stays ended.
      tags: [recurring-invoices]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Resumed schedule
          content:
            application/json:
              schema:
                type: object
                properties:
                  recurring:
                    $ref: "#/components/schemas/RecurringInvoice"
        "404":
          description: Schedule not found

  /api/v1/finance/recurring/{id}/generate:
    post:
      summary: Emit the invoice for the current period of the schedule
      description: >
        Idempotent per period. A repeated call for the same period returns the
        invoice that was already emitted instead of creating a second one.
      tags: [recurring-invoices]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "201":
          description: Emitted invoice plus the advanced schedule
          content:
            application/json:
              schema:
                type: object
                properties:
                  invoice:
                    $ref: "#/components/schemas/Invoice"
                  recurring:
                    $ref: "#/components/schemas/RecurringInvoice"
        "404":
          description: Schedule not found
        "412":
          description: Schedule is paused, ended or past its end date

  /api/v1/finance/credit-notes:
    post:
      summary: Create a credit note
      tags: [credit-notes]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateCreditNoteRequest"
      responses:
        "201":
          description: Credit note created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreditNote"
    get:
      summary: List credit notes
      tags: [credit-notes]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: invoice_id
          in: query
          schema:
            type: string
        - $ref: "#/components/parameters/page"
        - $ref: "#/components/parameters/page_size"
      responses:
        "200":
          description: List of credit notes
          content:
            application/json:
              schema:
                type: object
                properties:
                  credit_notes:
                    type: array
                    items:
                      $ref: "#/components/schemas/CreditNote"
                  total:
                    type: integer

  /api/v1/finance/credit-notes/{id}:
    get:
      summary: Get a credit note by ID
      tags: [credit-notes]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Credit note details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreditNote"

  /api/v1/finance/credit-notes/{id}/send:
    post:
      summary: Send a credit note
      tags: [credit-notes]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Credit note sent
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreditNote"

  /api/v1/finance/credit-notes/{id}/pdf:
    get:
      summary: Generate PDF for a credit note
      tags: [credit-notes]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: PDF binary
          content:
            application/pdf:
              schema:
                type: string
                format: binary

  # ===========================================================================
  # Finance: Payments (top-level)
  # ===========================================================================
  /api/v1/finance/payments/{id}:
    delete:
      summary: Delete a payment
      tags: [payments]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Payment deleted

  # ===========================================================================
  # Finance: Dunning
  # ===========================================================================
  /api/v1/finance/bank-statements:
    get:
      summary: List imported bank statements
      description: Imported account statements, newest first.
      tags: [finance]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/page"
        - $ref: "#/components/parameters/page_size"
      responses:
        "200":
          description: Imported statements
          content:
            application/json:
              schema:
                type: object
                properties:
                  statements:
                    type: array
                    items:
                      $ref: "#/components/schemas/BankStatement"
                  total:
                    type: integer
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "503":
          description: Finance service unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /api/v1/finance/bank-statements/import:
    post:
      summary: Import a bank statement (CAMT.053 or MT940)
      description: >
        Uploads an account statement. The format is detected from the content,
        not from the filename or MIME type. Every credit is matched against the
        open items of the tenant and carries a suggestion; nothing is booked
        here — a payment is only recorded when a transaction is reconciled.

        The import is idempotent on the file content: uploading the identical
        file again returns the original statement with already_imported=true and
        200 instead of 201, and creates no second set of transactions.
      tags: [finance]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
                  description: CAMT.053 XML or MT940 text file, at most 10 MiB
      responses:
        "201":
          description: Statement imported
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BankStatementImportResult"
        "200":
          description: The identical file had already been imported
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BankStatementImportResult"
        "400":
          description: Missing file, unrecognised format or malformed statement
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "413":
          description: File exceeds the 10 MiB limit
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "503":
          description: Finance service unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /api/v1/finance/bank-statements/{id}:
    get:
      summary: Get one bank statement with its transactions
      tags: [finance]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Statement with its transactions
          content:
            application/json:
              schema:
                type: object
                properties:
                  statement:
                    $ref: "#/components/schemas/BankStatement"
                  transactions:
                    type: array
                    items:
                      $ref: "#/components/schemas/BankTransaction"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "503":
          description: Finance service unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /api/v1/finance/bank-transactions:
    get:
      summary: List imported bank transactions
      description: >
        The reconciliation queue. Without a status this is what still needs a
        decision and leaves out entries that were set aside; status=ignored asks
        for exactly those.
      tags: [finance]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: statement_id
          in: query
          description: Restrict to one import
          schema:
            type: string
            format: uuid
        - name: status
          in: query
          description: >
            Restrict to one reconciliation state. Absent or "all" returns the
            queue, which excludes ignored entries.
          schema:
            type: string
            enum: [all, unmatched, suggested, matched, ignored]
        - $ref: "#/components/parameters/page"
        - $ref: "#/components/parameters/page_size"
      responses:
        "200":
          description: Imported transactions
          content:
            application/json:
              schema:
                type: object
                properties:
                  transactions:
                    type: array
                    items:
                      $ref: "#/components/schemas/BankTransactionQueueEntry"
                  total:
                    type: integer
        "400":
          description: Unknown status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "503":
          description: Finance service unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /api/v1/finance/bank-transactions/{id}/match:
    post:
      summary: Book a bank credit as a payment against an invoice
      description: >
        Records a payment through the payment service, so invoice status,
        numbering and the GoBD trail keep running through the one path that owns
        them. An empty body confirms the suggestion the import attached; an
        invoice_id or invoice_number assigns a different one, and invoice_id
        wins where both are given. The payment carries an idempotency key
        derived from the transaction, so a retried confirmation records exactly
        one payment.
      tags: [finance]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                invoice_id:
                  type: string
                  format: uuid
                  description: Overrides the suggested invoice
                invoice_number:
                  type: string
                  description: >
                    The same override as the operator sees it on the document.
                    Read only when invoice_id is absent.
      responses:
        "200":
          description: The matched transaction
          content:
            application/json:
              schema:
                type: object
                properties:
                  transaction:
                    $ref: "#/components/schemas/BankTransactionQueueEntry"
        "400":
          description: Invalid id, invoice_id or invoice_number
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          description: >
            No such transaction, or no invoice carries the given number within
            the tenant.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          description: >
            Already reconciled, or the entry is a debit — money leaving the
            account cannot settle a receivable.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "503":
          description: Finance service unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /api/v1/finance/bank-transactions/{id}/reject-match:
    post:
      summary: Discard the suggested match of a bank transaction
      description: >
        Says "not this invoice" and puts the entry back in front of the operator
        as unmatched — distinct from ignore, which says it is not a customer
        payment at all. An already unmatched entry is returned unchanged rather
        than refused: the outcome the caller asked for holds.
      tags: [finance]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: The transaction, now unmatched
          content:
            application/json:
              schema:
                type: object
                properties:
                  transaction:
                    $ref: "#/components/schemas/BankTransactionQueueEntry"
        "400":
          description: Invalid id
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: >
            The entry is booked or was set aside — both are reversals, not
            rejections.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "503":
          description: Finance service unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /api/v1/finance/bank-transactions/{id}/ignore:
    post:
      summary: Take a bank transaction out of the reconciliation queue
      description: >
        For entries that are not customer payments — bank fees, own transfers.
        A booked transaction cannot be ignored: undoing it means deleting its
        payment.
      tags: [finance]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: The ignored transaction
          content:
            application/json:
              schema:
                type: object
                properties:
                  transaction:
                    $ref: "#/components/schemas/BankTransactionQueueEntry"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The transaction is already booked
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "503":
          description: Finance service unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /api/v1/finance/bank-accounts:
    get:
      summary: List bank accounts (Bankkonten)
      description: >
        The accounts the caller's tenant holds, ordered by bank name. There is
        no pagination: a KMU holds a handful of accounts and the client renders
        all of them. Balance and lastSync are derived from the newest imported
        statement of the account's IBAN, so an account without an import
        reports a zero balance and a null lastSync.
      tags: [finance]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Bank accounts of the tenant
          content:
            application/json:
              schema:
                type: object
                properties:
                  accounts:
                    type: array
                    items:
                      $ref: "#/components/schemas/BankAccount"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "503":
          description: Finance service unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    post:
      summary: Add a bank account
      description: >
        The IBAN may be sent grouped or compact; it is normalised and its ISO
        7064 check digits are verified before it is stored. An account starts
        disconnected.
      tags: [finance]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BankAccountInput"
      responses:
        "201":
          description: The created account
          content:
            application/json:
              schema:
                type: object
                properties:
                  account:
                    $ref: "#/components/schemas/BankAccount"
        "400":
          description: Missing bank name, malformed IBAN, BIC or currency
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: The tenant already holds an account with this IBAN
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "503":
          description: Finance service unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /api/v1/finance/bank-accounts/{id}:
    patch:
      summary: Edit a bank account
      description: >
        Every field is optional; an omitted one is left untouched. Correcting
        the IBAN also changes which imported statements the balance is read
        from — that is the point, an account entered with a typo would
        otherwise never find its own imports.
      tags: [finance]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BankAccountUpdateInput"
      responses:
        "200":
          description: The updated account
          content:
            application/json:
              schema:
                type: object
                properties:
                  account:
                    $ref: "#/components/schemas/BankAccount"
        "400":
          description: Invalid id, IBAN, BIC or currency
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Another account of the tenant already holds this IBAN
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "503":
          description: Finance service unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    delete:
      summary: Remove a bank account
      description: >
        Statements already imported for this IBAN are unaffected — they record
        what a bank reported and outlive the master-data row.
      tags: [finance]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: The account was removed
          content:
            application/json:
              schema:
                type: object
        "400":
          description: Invalid id
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "503":
          description: Finance service unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /api/v1/finance/bank-accounts/{id}/connect:
    post:
      summary: Mark a bank account as connected
      description: >
        Records the outcome of the bank login flow. There is no bank session
        behind this: the PSD2 handshake is simulated in the desktop client and
        the real FinAPI integration is a later phase. Connecting an already
        connected account is a no-op that keeps the original timestamp.
      tags: [finance]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: The connected account
          content:
            application/json:
              schema:
                type: object
                properties:
                  account:
                    $ref: "#/components/schemas/BankAccount"
        "400":
          description: Invalid id
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "503":
          description: Finance service unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /api/v1/finance/expenses:
    get:
      summary: List expenses (Ausgaben)
      description: >
        Expenses of the caller's tenant, newest first. A caller whose read grant
        is narrowed to the "own" data scope only sees what they submitted, and
        the total reflects the same restriction.
      tags: [finance]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: status
          in: query
          description: Restrict to one approval state
          schema:
            type: string
            enum: [pending, approved, rejected]
        - $ref: "#/components/parameters/page"
        - $ref: "#/components/parameters/page_size"
      responses:
        "200":
          description: Expenses of the tenant
          content:
            application/json:
              schema:
                type: object
                properties:
                  expenses:
                    type: array
                    items:
                      $ref: "#/components/schemas/Expense"
                  total:
                    type: integer
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "503":
          description: Finance service unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    post:
      summary: Record a new expense
      description: >
        The expense always starts pending; it cannot be created already
        approved. The submitter is taken from the token, not from the body.
      tags: [finance]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ExpenseInput"
      responses:
        "201":
          description: The created expense
          content:
            application/json:
              schema:
                type: object
                properties:
                  expense:
                    $ref: "#/components/schemas/Expense"
        "400":
          description: Missing description, non-positive amount or invalid date
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "503":
          description: Finance service unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /api/v1/finance/expenses/{id}:
    put:
      summary: Edit an expense
      description: >
        Every field is optional; an omitted one is left untouched. Once the
        expense has been approved or rejected only the SKR03/04 account and the
        receipt filename stay editable — Kontierung and a late receipt are
        normal after-the-fact work, while changing the amount would make the
        decision describe something else.
      tags: [finance]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ExpenseUpdateInput"
      responses:
        "200":
          description: The updated expense
          content:
            application/json:
              schema:
                type: object
                properties:
                  expense:
                    $ref: "#/components/schemas/Expense"
        "400":
          description: Invalid id, amount or date
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The expense was already decided and the edit reaches past account and receipt
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "503":
          description: Finance service unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    delete:
      summary: Delete a pending expense
      description: >
        Only a pending expense can be removed. A decided one is the record of a
        decision somebody made and stays.
      tags: [finance]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Deleted
          content:
            application/json:
              schema:
                type: object
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The expense was already decided
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "503":
          description: Finance service unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /api/v1/finance/expenses/{id}/approve:
    post:
      summary: Approve a pending expense
      description: >
        Only a pending expense can be decided, and never by the person who
        submitted it — enforced server-side, so hiding the button is a courtesy
        rather than the control.
      tags: [finance]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: The approved expense
          content:
            application/json:
              schema:
                type: object
                properties:
                  expense:
                    $ref: "#/components/schemas/Expense"
        "400":
          description: Invalid id
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Already decided, or the caller is the submitter
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "503":
          description: Finance service unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /api/v1/finance/expenses/{id}/reject:
    post:
      summary: Reject a pending expense
      description: >
        Same guards as approve: pending only, and never by the submitter.
      tags: [finance]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: The rejected expense
          content:
            application/json:
              schema:
                type: object
                properties:
                  expense:
                    $ref: "#/components/schemas/Expense"
        "400":
          description: Invalid id
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Already decided, or the caller is the submitter
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "503":
          description: Finance service unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /api/v1/finance/expenses/{id}/receipt:
    post:
      summary: Attach a receipt filename to an expense
      description: >
        Records the name of the document handed in. This carries metadata, not
        bytes — there is no upload here. It stays open after a decision, because
        a receipt arriving late is the normal case.
      tags: [finance]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [receiptName]
              properties:
                receiptName:
                  type: string
                  maxLength: 255
                  example: hetzner-2026-05.pdf
      responses:
        "200":
          description: The expense with the receipt recorded
          content:
            application/json:
              schema:
                type: object
                properties:
                  expense:
                    $ref: "#/components/schemas/Expense"
        "400":
          description: Invalid id or missing receiptName
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "503":
          description: Finance service unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /api/v1/finance/transactions:
    get:
      summary: List the consolidated payment ledger (Transaktionen)
      description: >
        Every recorded customer payment (income) and every approved expense
        (expense) of the tenant, merged and sorted by date, newest first.
      tags: [finance]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Transactions of the tenant
          content:
            application/json:
              schema:
                type: object
                properties:
                  transactions:
                    type: array
                    items:
                      $ref: "#/components/schemas/Transaction"
                  total:
                    type: integer
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "503":
          description: Finance service unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /api/v1/finance/transactions/{id}:
    delete:
      summary: Remove a ledger entry
      description: >
        A payment delete also reverts the invoice's paid status if it no
        longer covers the total. An approved expense is refused with 409 — it
        is the record of a decision somebody made, not a draft.
      tags: [finance]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          description: Opaque, prefixed ("pay-"/"exp-") — not a plain UUID
          schema:
            type: string
      responses:
        "200":
          description: Deleted
          content:
            application/json:
              schema:
                type: object
        "400":
          description: Unrecognized id prefix
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The entry names an approved expense, which cannot be withdrawn
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "503":
          description: Finance service unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /api/v1/finance/document-chains:
    get:
      summary: List document chains (Belegkette)
      description: >
        Every customer document traced from quote through invoice to payment,
        dunning, or credit note. One entry per invoice (prefixed with its
        source quote, if one converted into it), plus one entry per quote that
        has not become an invoice yet. Tenant-wide overview, not filtered by
        any single document — the desktop client paginates and searches
        client-side.
      tags: [finance]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Document chains of the tenant
          content:
            application/json:
              schema:
                type: object
                properties:
                  chains:
                    type: array
                    items:
                      $ref: "#/components/schemas/DocumentChain"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "503":
          description: Finance service unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /api/v1/finance/time-entries:
    get:
      summary: List billable time entries (Stunden -> Rechnung)
      description: >
        Completed time entries across every project of the tenant, joined
        with task, project, and employee names, for building invoice line
        items from tracked hours.
      tags: [finance]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: billed
          in: query
          description: "false returns only unbilled entries (the only value the desktop client sends)."
          schema:
            type: string
            enum: ["true", "false"]
      responses:
        "200":
          description: Billable time entries of the tenant
          content:
            application/json:
              schema:
                type: object
                properties:
                  entries:
                    type: array
                    items:
                      $ref: "#/components/schemas/FinanceTimeEntry"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "503":
          description: Work service unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /api/v1/finance/open-items:
    get:
      summary: List open items (Offene Posten) with receivables aging
      description: >
        Unpaid receivables — sent or overdue invoices whose gross total is not
        yet covered by recorded payments. open_amount is the residual, so a
        partially paid invoice appears for the remainder only. The summary always
        covers every open item of the tenant, not just the returned page, and is
        reported per currency: without a stored exchange rate the server cannot
        fold CHF mirrors and EUR invoices into a single figure.
      tags: [dunning]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: bucket
          in: query
          description: Restrict to one aging bucket
          schema:
            type: string
            enum: [current, d30, d60, d60plus]
        - name: overdue_only
          in: query
          description: Drop items that are not past due yet
          schema:
            type: boolean
        - $ref: "#/components/parameters/page"
        - $ref: "#/components/parameters/page_size"
      responses:
        "200":
          description: Open items with the tenant-wide aging summary
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      $ref: "#/components/schemas/OpenItem"
                  total:
                    type: integer
                  summary:
                    $ref: "#/components/schemas/OpenItemsSummary"
        "400":
          description: Unknown aging bucket
        "401":
          description: Missing or invalid tenant
        "403":
          description: Missing finance read permission
        "503":
          description: Finance service unavailable

  /api/v1/finance/dunning:
    get:
      summary: List dunning records
      tags: [dunning]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: invoice_id
          in: query
          schema:
            type: string
        - name: status
          in: query
          schema:
            type: string
            enum: [draft, sent, paid]
        - $ref: "#/components/parameters/page"
        - $ref: "#/components/parameters/page_size"
      responses:
        "200":
          description: List of dunning records
          content:
            application/json:
              schema:
                type: object
                properties:
                  dunnings:
                    type: array
                    items:
                      $ref: "#/components/schemas/DunningRecord"
                  total:
                    type: integer

  /api/v1/finance/dunning/detect:
    post:
      summary: Detect overdue invoices and create dunning records
      tags: [dunning]
      security: [{ bearerAuth: [] }]
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                invoice_id:
                  type: string
                  description: Optional - target a specific invoice
      responses:
        "201":
          description: Dunning record created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DunningRecord"

  /api/v1/finance/dunning/{id}/send:
    post:
      summary: Send a dunning letter
      tags: [dunning]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Dunning record sent
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DunningRecord"

  /api/v1/finance/dunning/{id}/escalate:
    post:
      summary: Escalate a dunning to the next level
      tags: [dunning]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [invoice_id]
              properties:
                invoice_id:
                  type: string
      responses:
        "200":
          description: Dunning escalated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DunningRecord"

  /api/v1/finance/dunning/{id}/pdf:
    get:
      summary: Generate PDF for a dunning letter
      tags: [dunning]
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: PDF binary
          content:
            application/pdf:
              schema:
                type: string
                format: binary

  /api/v1/finance/dunning/config:
    get:
      summary: Get dunning configuration
      tags: [dunning]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Dunning configuration
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DunningConfig"
    put:
      summary: Update dunning configuration (admin only)
      tags: [dunning]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DunningConfigInput"
      responses:
        "200":
          description: Updated dunning config
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DunningConfig"

  # ===========================================================================
  # Finance: Dashboard
  # ===========================================================================
  /api/v1/finance/dashboard:
    get:
      summary: Get finance dashboard data
      tags: [finance-dashboard]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Dashboard metrics
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinanceDashboard"

  # ===========================================================================
  # Finance: DATEV Export
  # ===========================================================================
  /api/v1/finance/export/datev:
    post:
      summary: Export DATEV Buchungsstapel CSV
      tags: [datev]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [start_date, end_date, fiscal_year_start]
              properties:
                start_date:
                  type: string
                  format: date
                end_date:
                  type: string
                  format: date
                fiscal_year_start:
                  type: string
                  format: date
      responses:
        "200":
          description: DATEV CSV file download
          content:
            text/csv:
              schema:
                type: string
                format: binary
          headers:
            Content-Disposition:
              schema:
                type: string
                example: 'attachment; filename="EXTF_Buchungsstapel_20260101_20261231.csv"'

  # ===========================================================================
  # HR: Leave Management
  # ===========================================================================
  /api/v1/hr/leave/requests:
    post:
      summary: Create a new leave request
      tags: [hr-leave]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateLeaveRequestInput"
      responses:
        "201":
          description: Leave request created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LeaveRequest"
    get:
      summary: List leave requests (filtered)
      tags: [hr-leave]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: employee_id
          in: query
          schema: { type: string, format: uuid }
        - name: status
          in: query
          schema: { type: string, enum: [pending, approved, rejected, cancelled] }
        - name: start_date
          in: query
          schema: { type: string, format: date }
        - name: end_date
          in: query
          schema: { type: string, format: date }
        - { name: page, in: query, schema: { type: integer } }
        - { name: page_size, in: query, schema: { type: integer } }
      responses:
        "200":
          description: List of leave requests
          content:
            application/json:
              schema:
                type: object
                properties:
                  leave_requests:
                    type: array
                    items:
                      $ref: "#/components/schemas/LeaveRequest"
                  total: { type: integer }

  /api/v1/hr/leave/requests/{id}:
    get:
      summary: Get a single leave request
      tags: [hr-leave]
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Leave request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LeaveRequest"

  /api/v1/hr/leave/requests/{id}/approve:
    post:
      summary: Approve a leave request
      tags: [hr-leave]
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                comment: { type: string }
      responses:
        "200":
          description: Leave request approved
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LeaveRequest"

  /api/v1/hr/leave/requests/{id}/reject:
    post:
      summary: Reject a leave request
      tags: [hr-leave]
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                comment: { type: string }
      responses:
        "200":
          description: Leave request rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LeaveRequest"

  /api/v1/hr/leave/requests/{id}/cancel:
    post:
      summary: Cancel a leave request
      tags: [hr-leave]
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Leave request cancelled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LeaveRequest"

  /api/v1/hr/leave/balance:
    get:
      summary: Get current user leave balance
      tags: [hr-leave]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Leave balance
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LeaveBalance"

  /api/v1/hr/leave/balance/{userId}:
    get:
      summary: Get leave balance for a specific employee (manager view)
      tags: [hr-leave]
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: userId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Employee leave balance
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LeaveBalance"

  /api/v1/hr/leave/types:
    get:
      summary: List available leave types
      tags: [hr-leave]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Leave types
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/LeaveType"

  /api/v1/hr/leave/sick:
    post:
      summary: Record sick leave
      tags: [hr-leave]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [start_date, end_date]
              properties:
                start_date: { type: string, format: date }
                end_date: { type: string, format: date }
                notes: { type: string }
      responses:
        "201":
          description: Sick leave recorded
          content:
            application/json:
              schema:
                type: object
                properties:
                  leave_request:
                    $ref: "#/components/schemas/LeaveRequest"
                  au_document_required: { type: boolean }

  # ===========================================================================
  # HR: Time Tracking
  # ===========================================================================
  /api/v1/hr/time/clock-in:
    post:
      summary: Clock in to start a shift
      tags: [hr-time]
      security: [{ bearerAuth: [] }]
      responses:
        "201":
          description: Work time entry created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkTimeEntry"

  /api/v1/hr/time/clock-out:
    post:
      summary: Clock out to end a shift
      tags: [hr-time]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Work time entry completed with ArbZG compliance info
          content:
            application/json:
              schema:
                type: object
                properties:
                  entry:
                    $ref: "#/components/schemas/WorkTimeEntry"
                  severity: { type: string, enum: [none, info, warning, error] }
                  arbzg_message: { type: string }

  /api/v1/hr/time/break/start:
    post:
      summary: Start a break
      tags: [hr-time]
      security: [{ bearerAuth: [] }]
      responses:
        "201":
          description: Break started
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BreakEntry"

  /api/v1/hr/time/break/end:
    post:
      summary: End a break
      tags: [hr-time]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Break ended
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BreakEntry"

  /api/v1/hr/time/active:
    get:
      summary: Get active shift and breaks
      tags: [hr-time]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Active shift info
          content:
            application/json:
              schema:
                type: object
                properties:
                  entry:
                    $ref: "#/components/schemas/WorkTimeEntry"
                  is_on_break: { type: boolean }
                  active_break:
                    $ref: "#/components/schemas/BreakEntry"

  /api/v1/hr/time/status:
    get:
      summary: Get work time status for header quick-toggle
      tags: [hr-time]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Current work time status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkTimeStatus"

  /api/v1/hr/time/entries:
    get:
      summary: List work time entries
      tags: [hr-time]
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: employee_id, in: query, schema: { type: string, format: uuid } }
        - { name: start_date, in: query, schema: { type: string, format: date } }
        - { name: end_date, in: query, schema: { type: string, format: date } }
        - { name: page, in: query, schema: { type: integer } }
        - { name: page_size, in: query, schema: { type: integer } }
      responses:
        "200":
          description: Work time entries
          content:
            application/json:
              schema:
                type: object
                properties:
                  entries:
                    type: array
                    items:
                      $ref: "#/components/schemas/WorkTimeEntry"
                  total: { type: integer }

  /api/v1/hr/time/summary/daily:
    get:
      summary: Get daily work time summary
      tags: [hr-time]
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: date, in: query, schema: { type: string, format: date } }
      responses:
        "200":
          description: Daily summary
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DailySummary"

  /api/v1/hr/time/summary/weekly:
    get:
      summary: Get weekly work time summary
      tags: [hr-time]
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: week_start, in: query, schema: { type: string, format: date } }
      responses:
        "200":
          description: Weekly summary
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WeeklySummary"

  /api/v1/hr/time/corrections:
    post:
      summary: Submit a time correction
      tags: [hr-time]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SubmitCorrectionInput"
      responses:
        "201":
          description: Correction submitted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkTimeEntry"

  /api/v1/hr/time/corrections/{id}/approve:
    post:
      summary: Approve a time correction
      tags: [hr-time]
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Correction approved
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WorkTimeEntry"

  # ===========================================================================
  # HR: Absence Calendar
  # ===========================================================================
  /api/v1/hr/absences/calendar:
    get:
      summary: Get absence calendar for team view
      tags: [hr-absence]
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: start_date, in: query, required: true, schema: { type: string, format: date } }
        - { name: end_date, in: query, required: true, schema: { type: string, format: date } }
        - { name: department, in: query, schema: { type: string } }
      responses:
        "200":
          description: Absence calendar entries
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/AbsenceEntry"

  # ===========================================================================
  # HR: Employee Profiles
  # ===========================================================================
  /api/v1/hr/employees:
    get:
      summary: List employees
      tags: [hr-employee]
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: department, in: query, schema: { type: string } }
        - { name: page, in: query, schema: { type: integer } }
        - { name: page_size, in: query, schema: { type: integer } }
      responses:
        "200":
          description: Employee list
          content:
            application/json:
              schema:
                type: object
                properties:
                  employees:
                    type: array
                    items:
                      $ref: "#/components/schemas/EmployeeProfile"
                  total: { type: integer }
    post:
      summary: Create employee profile
      description: Creates a new HR employee profile for an existing auth user. Auth user creation is a separate step.
      operationId: createEmployee
      tags: [hr-employee]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [user_id, start_date]
              properties:
                user_id:
                  type: string
                  format: uuid
                department:
                  type: string
                position_title:
                  type: string
                contract_type:
                  type: string
                  enum: [full_time, part_time, mini_job, intern, temporary]
                work_days_per_week:
                  type: integer
                  minimum: 1
                  maximum: 7
                annual_leave_days:
                  type: integer
                  minimum: 0
                manager_user_id:
                  type: string
                  format: uuid
                start_date:
                  type: string
                  format: date
                emergency_contact_name:
                  type: string
                emergency_contact_phone:
                  type: string
                address_street:
                  type: string
                address_city:
                  type: string
                address_postal_code:
                  type: string
                address_country:
                  type: string
      responses:
        "201":
          description: Employee profile created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmployeeProfile"
        "409":
          description: Employee profile already exists for this user

  /api/v1/hr/employees/me:
    get:
      summary: Get own employee profile
      tags: [hr-employee]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Own profile
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmployeeProfile"
    put:
      summary: Update own profile (self-service fields only)
      tags: [hr-employee]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateSelfProfileInput"
      responses:
        "200":
          description: Profile updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmployeeProfile"

  /api/v1/hr/employees/{id}:
    get:
      summary: Get employee by ID
      tags: [hr-employee]
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Employee profile
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmployeeProfile"
    put:
      summary: Update employee (admin/manager fields)
      tags: [hr-employee]
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateEmployeeInput"
      responses:
        "200":
          description: Employee updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmployeeProfile"

  /api/v1/hr/employees/{id}/documents:
    get:
      summary: List employee documents
      tags: [hr-employee]
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Documents list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/EmployeeDocument"
    post:
      summary: Upload a document for an employee
      tags: [hr-employee]
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [category_id, file_id]
              properties:
                category_id: { type: string, format: uuid }
                file_id: { type: string, format: uuid }
                notes: { type: string }
      responses:
        "201":
          description: Document uploaded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmployeeDocument"

  # ===========================================================================
  # HR: Document categories (tenant master data)
  # ===========================================================================
  /api/v1/hr/employees/{id}/documents/categories:
    get:
      summary: List HR document categories visible for an employee's Akte
      description: >
        Returns the tenant's document categories plus the system seed categories
        (migration 000046), filtered to the visibility tiers (hr_only/manager/
        employee) the caller's team:documents:view permission scope permits —
        the same tiers RLS migration 000127 enforces on the documents
        themselves, so a caller who cannot see hr_only documents for this
        employee does not learn hr_only categories exist either. These are
        tenant master data, not an employee attribute — the ids returned here
        are the ones POST /api/v1/hr/employees/{id}/documents accepts.
      tags: [hr-employee]
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Category list
          content:
            application/json:
              schema:
                type: object
                properties:
                  categories:
                    type: array
                    items:
                      $ref: "#/components/schemas/HRDocumentCategory"
        "401": { description: Missing or invalid tenant }
        "403": { description: Missing hr:read or team:documents:view permission }
        "503": { description: HR service unavailable }

  # ===========================================================================
  # HR: Settings (admin only)
  # ===========================================================================
  /api/v1/hr/settings:
    get:
      summary: Get HR company settings
      tags: [hr-settings]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: HR settings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HRSettings"
    put:
      summary: Update HR company settings
      tags: [hr-settings]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateHRSettingsInput"
      responses:
        "200":
          description: HR settings updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HRSettings"

  # =========================================================================
  # Inbox
  # =========================================================================

  /api/v1/inbox/messages:
    get:
      summary: List inbox messages
      tags: [inbox-messages]
      operationId: listInboxMessages
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: channel, in: query, schema: { type: string, enum: [email, chat, notification] } }
        - { name: is_read, in: query, schema: { type: boolean } }
        - { name: is_starred, in: query, schema: { type: boolean } }
        - { name: status, in: query, schema: { type: string, enum: [open, pending, resolved, closed] } }
        - { name: team_inbox_id, in: query, schema: { type: string, format: uuid } }
        - { name: search, in: query, schema: { type: string } }
        - { name: page_size, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 50 } }
        - { name: page_token, in: query, schema: { type: string } }
      responses:
        "200":
          description: Inbox messages list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InboxMessageList"

  /api/v1/inbox/messages/{id}:
    get:
      summary: Get inbox message by ID
      tags: [inbox-messages]
      operationId: getInboxMessage
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Inbox message
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InboxMessage"

  /api/v1/inbox/messages/{id}/read:
    post:
      summary: Mark message as read
      tags: [inbox-messages]
      operationId: markInboxMessageRead
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Message marked as read
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InboxMessageWrapper"

  /api/v1/inbox/messages/{id}/unread:
    post:
      summary: Mark message as unread
      tags: [inbox-messages]
      operationId: markInboxMessageUnread
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Message marked as unread
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InboxMessageWrapper"

  /api/v1/inbox/messages/{id}/star:
    post:
      summary: Toggle star on message
      tags: [inbox-messages]
      operationId: toggleInboxMessageStar
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Star toggled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InboxMessageWrapper"

  /api/v1/inbox/messages/{id}/status:
    post:
      summary: Set conversation-level status on a message
      tags: [inbox-messages]
      operationId: setInboxMessageStatus
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [status]
              properties:
                status: { type: string, enum: [open, pending, resolved, closed] }
      responses:
        "200":
          description: Status updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InboxMessageWrapper"
        "400":
          $ref: "#/components/responses/BadRequest"

  /api/v1/inbox/messages/{id}/tags:
    post:
      summary: Add a tag to a message
      tags: [inbox-messages]
      operationId: addInboxMessageTag
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [tag]
              properties:
                tag: { type: string }
      responses:
        "200":
          description: Tag added
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InboxMessage"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      summary: Remove a tag from a message
      tags: [inbox-messages]
      operationId: removeInboxMessageTag
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [tag]
              properties:
                tag: { type: string }
      responses:
        "200":
          description: Tag removed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InboxMessage"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/inbox/messages/{id}/forward:
    post:
      summary: Forward message to a new recipient through the originating channel
      tags: [inbox-messages]
      operationId: forwardInboxMessage
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [to]
              properties:
                to: { type: string, description: "Recipient — email address or free-text name/handle" }
                note: { type: string }
      responses:
        "200":
          description: Message forwarded
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean }
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
        "501":
          description: Forwarding is not supported for this message's channel

  /api/v1/inbox/messages/{id}/archive:
    post:
      summary: Archive message
      tags: [inbox-messages]
      operationId: archiveInboxMessage
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Message archived
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InboxMessageWrapper"

  /api/v1/inbox/messages/{id}/unarchive:
    post:
      summary: Unarchive message
      tags: [inbox-messages]
      operationId: unarchiveInboxMessage
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Message unarchived
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InboxMessageWrapper"

  /api/v1/inbox/messages/{id}/snooze:
    post:
      summary: Snooze message until a future time
      tags: [inbox-messages]
      operationId: snoozeInboxMessage
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [snooze_until]
              properties:
                snooze_until: { type: string, format: date-time }
      responses:
        "200":
          description: Message snoozed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InboxMessageWrapper"

  /api/v1/inbox/messages/{id}/unsnooze:
    post:
      summary: Unsnooze message
      tags: [inbox-messages]
      operationId: unsnoozeInboxMessage
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Message unsnoozed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InboxMessageWrapper"

  /api/v1/inbox/messages/{id}/reply:
    post:
      summary: Reply to message through originating channel
      tags: [inbox-messages]
      operationId: replyToInboxMessage
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [body]
              properties:
                body: { type: string }
      responses:
        "200":
          description: Reply sent
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean }

  /api/v1/inbox/messages/{id}/assign:
    post:
      summary: Assign message to a user
      tags: [inbox-messages]
      operationId: assignInboxMessage
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [assignee_id]
              properties:
                assignee_id: { type: string, format: uuid }
      responses:
        "200":
          description: Message assigned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InboxMessageWrapper"

  /api/v1/inbox/unread-count:
    get:
      summary: Get unread message count by channel
      tags: [inbox-messages]
      operationId: getInboxUnreadCount
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Unread counts
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InboxUnreadCount"

  /api/v1/inbox/messages/bulk-read:
    post:
      summary: Mark multiple messages as read
      tags: [inbox-messages]
      operationId: bulkMarkInboxRead
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [ids]
              properties:
                ids:
                  type: array
                  items: { type: string, format: uuid }
      responses:
        "200":
          description: Messages marked as read
          content:
            application/json:
              schema:
                type: object
                properties:
                  updated_count: { type: integer }

  /api/v1/inbox/messages/bulk-archive:
    post:
      summary: Archive multiple messages
      tags: [inbox-messages]
      operationId: bulkArchiveInbox
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [ids]
              properties:
                ids:
                  type: array
                  items: { type: string, format: uuid }
      responses:
        "200":
          description: Messages archived
          content:
            application/json:
              schema:
                type: object
                properties:
                  updated_count: { type: integer }

  /api/v1/inbox/teams:
    post:
      summary: Create a team inbox (admin/manager only)
      tags: [inbox-teams]
      operationId: createTeamInbox
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateTeamInboxInput"
      responses:
        "201":
          description: Team inbox created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TeamInbox"
    get:
      summary: List team inboxes for current user
      tags: [inbox-teams]
      operationId: listTeamInboxes
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Team inboxes list
          content:
            application/json:
              schema:
                type: object
                properties:
                  team_inboxes:
                    type: array
                    items:
                      $ref: "#/components/schemas/TeamInbox"

  /api/v1/inbox/teams/{id}:
    put:
      summary: Update a team inbox
      tags: [inbox-teams]
      operationId: updateTeamInbox
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateTeamInboxInput"
      responses:
        "200":
          description: Team inbox updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TeamInbox"
    delete:
      summary: Delete a team inbox (admin/manager only)
      tags: [inbox-teams]
      operationId: deleteTeamInbox
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Team inbox deleted

  /api/v1/inbox/teams/{id}/members:
    post:
      summary: Add member to team inbox
      tags: [inbox-teams]
      operationId: addTeamInboxMember
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [member_user_id]
              properties:
                member_user_id: { type: string, format: uuid }
                role: { type: string, enum: [admin, member], default: member }
      responses:
        "201":
          description: Member added
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TeamInboxMember"
    get:
      summary: List team inbox members
      tags: [inbox-teams]
      operationId: listTeamInboxMembers
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Team inbox members
          content:
            application/json:
              schema:
                type: object
                properties:
                  members:
                    type: array
                    items:
                      $ref: "#/components/schemas/TeamInboxMember"

  /api/v1/inbox/teams/{id}/members/{user_id}:
    delete:
      summary: Remove member from team inbox
      tags: [inbox-teams]
      operationId: removeTeamInboxMember
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: user_id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Member removed

  /api/v1/inbox/messages/{id}/claim:
    post:
      summary: Claim an unassigned team inbox message
      tags: [inbox-teams]
      operationId: claimInboxMessage
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Message claimed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InboxMessage"

  /api/v1/inbox/rules:
    post:
      summary: Create a routing rule (admin/manager only)
      tags: [inbox-routing]
      operationId: createInboxRoutingRule
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateRoutingRuleInput"
      responses:
        "201":
          description: Routing rule created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RoutingRule"
    get:
      summary: List routing rules
      tags: [inbox-routing]
      operationId: listInboxRoutingRules
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Routing rules list
          content:
            application/json:
              schema:
                type: object
                properties:
                  rules:
                    type: array
                    items:
                      $ref: "#/components/schemas/RoutingRule"

  /api/v1/inbox/rules/{id}:
    put:
      summary: Update a routing rule (admin/manager only)
      tags: [inbox-routing]
      operationId: updateInboxRoutingRule
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateRoutingRuleInput"
      responses:
        "200":
          description: Routing rule updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RoutingRule"
    delete:
      summary: Delete a routing rule (admin/manager only)
      tags: [inbox-routing]
      operationId: deleteInboxRoutingRule
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Routing rule deleted

  /api/v1/inbox/rules/test:
    post:
      summary: Test routing rule conditions against a sample message
      tags: [inbox-routing]
      operationId: testInboxRoutingRule
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [conditions, test_message]
              properties:
                conditions: { type: object }
                test_message:
                  $ref: "#/components/schemas/InboxMessage"
      responses:
        "200":
          description: Test result
          content:
            application/json:
              schema:
                type: object
                properties:
                  matches: { type: boolean }

  # =========================================================================
  # Automations
  # =========================================================================

  /api/v1/automations:
    post:
      summary: Create a new automation
      tags: [automations]
      operationId: createAutomation
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateAutomationRequest"
      responses:
        "201":
          description: Automation created
          content:
            application/json:
              schema:
                type: object
                properties:
                  automation:
                    $ref: "#/components/schemas/Automation"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
    get:
      summary: List automations
      tags: [automations]
      operationId: listAutomations
      security: [{ bearerAuth: [] }]
      parameters:
        - name: owner_id
          in: query
          schema: { type: string, format: uuid }
        - name: scope
          in: query
          schema: { type: string, enum: [personal, team, organization] }
        - name: trigger_type
          in: query
          schema: { type: string }
        - name: is_active
          in: query
          schema: { type: boolean }
        - name: limit
          in: query
          schema: { type: integer, default: 50 }
        - name: offset
          in: query
          schema: { type: integer, default: 0 }
      responses:
        "200":
          description: List of automations
          content:
            application/json:
              schema:
                type: object
                properties:
                  automations:
                    type: array
                    items:
                      $ref: "#/components/schemas/Automation"
                  total_count: { type: integer }
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/automations/{id}:
    get:
      summary: Get automation by ID
      tags: [automations]
      operationId: getAutomation
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: Automation details
          content:
            application/json:
              schema:
                type: object
                properties:
                  automation:
                    $ref: "#/components/schemas/Automation"
        "404":
          $ref: "#/components/responses/NotFound"
    put:
      summary: Update automation
      tags: [automations]
      operationId: updateAutomation
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateAutomationRequest"
      responses:
        "200":
          description: Automation updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  automation:
                    $ref: "#/components/schemas/Automation"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      summary: Delete automation
      tags: [automations]
      operationId: deleteAutomation
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: Automation deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  status: { type: string }
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/automations/{id}/enable:
    post:
      summary: Enable automation
      tags: [automations]
      operationId: enableAutomation
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: Automation enabled
          content:
            application/json:
              schema:
                type: object
                properties:
                  automation:
                    $ref: "#/components/schemas/Automation"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/automations/{id}/disable:
    post:
      summary: Disable automation
      tags: [automations]
      operationId: disableAutomation
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: Automation disabled
          content:
            application/json:
              schema:
                type: object
                properties:
                  automation:
                    $ref: "#/components/schemas/Automation"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/automations/{id}/executions:
    get:
      summary: List execution logs for an automation
      tags: [automations]
      operationId: listAutomationExecutions
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
        - name: status
          in: query
          schema: { type: string, enum: [running, completed, failed, skipped, aborted] }
        - name: limit
          in: query
          schema: { type: integer, default: 50 }
        - name: offset
          in: query
          schema: { type: integer, default: 0 }
      responses:
        "200":
          description: List of executions
          content:
            application/json:
              schema:
                type: object
                properties:
                  executions:
                    type: array
                    items:
                      $ref: "#/components/schemas/AutomationExecution"
                  total_count: { type: integer }
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/automations/executions/{executionId}:
    get:
      summary: Get a single execution log
      tags: [automations]
      operationId: getAutomationExecution
      security: [{ bearerAuth: [] }]
      parameters:
        - name: executionId
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: Execution details
          content:
            application/json:
              schema:
                type: object
                properties:
                  execution:
                    $ref: "#/components/schemas/AutomationExecution"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/automations/triggers:
    get:
      summary: List all available trigger definitions
      tags: [automations]
      operationId: listTriggerDefinitions
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Trigger catalog
          content:
            application/json:
              schema:
                type: object
                properties:
                  triggers:
                    type: array
                    items:
                      $ref: "#/components/schemas/TriggerDefinition"

  /api/v1/automations/actions:
    get:
      summary: List all available action definitions
      tags: [automations]
      operationId: listActionDefinitions
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Action catalog
          content:
            application/json:
              schema:
                type: object
                properties:
                  actions:
                    type: array
                    items:
                      $ref: "#/components/schemas/ActionDefinition"

  /api/v1/automations/templates:
    get:
      summary: List automation templates
      tags: [automations]
      operationId: listAutomationTemplates
      security: [{ bearerAuth: [] }]
      parameters:
        - name: category
          in: query
          schema: { type: string }
      responses:
        "200":
          description: List of templates
          content:
            application/json:
              schema:
                type: object
                properties:
                  templates:
                    type: array
                    items:
                      $ref: "#/components/schemas/AutomationTemplate"

  /api/v1/automations/templates/{templateId}/create:
    post:
      summary: Create automation from template
      tags: [automations]
      operationId: createAutomationFromTemplate
      security: [{ bearerAuth: [] }]
      parameters:
        - name: templateId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name: { type: string }
      responses:
        "201":
          description: Automation created from template
          content:
            application/json:
              schema:
                type: object
                properties:
                  automation:
                    $ref: "#/components/schemas/Automation"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/automations/test-condition:
    post:
      summary: Test a condition against sample data
      tags: [automations]
      operationId: testAutomationCondition
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                condition: { type: object }
                sample_env: { type: object }
      responses:
        "200":
          description: Condition test result
          content:
            application/json:
              schema:
                type: object
                properties:
                  result: { type: boolean }

  /api/v1/automations/dry-run:
    post:
      summary: Dry-run an automation without executing real actions
      tags: [automations]
      operationId: dryRunAutomation
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [automation_id]
              properties:
                automation_id: { type: string, format: uuid }
                sample_env: { type: object }
      responses:
        "200":
          description: Dry-run result
          content:
            application/json:
              schema:
                type: object
                properties:
                  condition_result: { type: boolean }
                  steps:
                    type: array
                    items:
                      $ref: "#/components/schemas/DryRunStep"

  /api/v1/automations/stats:
    get:
      summary: Get automation statistics (admin only)
      tags: [automations]
      operationId: getAutomationStats
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Automation statistics
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AutomationStats"
        "403":
          $ref: "#/components/responses/Forbidden"

  /api/v1/contacts/{contactId}/advisory-protocols:
    get:
      summary: List advisory protocols for a contact
      operationId: listAdvisoryProtocols
      tags: [advisory-protocols]
      security:
        - bearerAuth: []
      parameters:
        - name: contactId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: List of protocols
          content:
            application/json:
              schema:
                type: object
                properties:
                  protocols:
                    type: array
                    items:
                      $ref: '#/components/schemas/AdvisoryProtocol'
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
    post:
      summary: Create a new advisory protocol draft for a contact
      operationId: createAdvisoryProtocol
      tags: [advisory-protocols]
      security:
        - bearerAuth: []
      parameters:
        - name: contactId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '201':
          description: Protocol created
          content:
            application/json:
              schema:
                type: object
                properties:
                  protocol:
                    $ref: '#/components/schemas/AdvisoryProtocol'
        '403': { $ref: '#/components/responses/Forbidden' }
        '404':
          description: Contact not found

  /api/v1/advisory-protocols/{id}:
    get:
      summary: Get an advisory protocol by ID
      operationId: getAdvisoryProtocol
      tags: [advisory-protocols]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Protocol
          content:
            application/json:
              schema:
                type: object
                properties:
                  protocol:
                    $ref: '#/components/schemas/AdvisoryProtocol'
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
    put:
      summary: Update an advisory protocol draft (draft-only)
      operationId: updateAdvisoryProtocol
      tags: [advisory-protocols]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdvisoryProtocolUpdate'
      responses:
        '200':
          description: Updated protocol
          content:
            application/json:
              schema:
                type: object
                properties:
                  protocol:
                    $ref: '#/components/schemas/AdvisoryProtocol'
        '400': { $ref: '#/components/responses/BadRequest' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '410':
          description: Protocol is finalized and immutable
    delete:
      summary: Delete an advisory protocol draft (draft-only)
      operationId: deleteAdvisoryProtocol
      tags: [advisory-protocols]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: Deleted
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }
        '410':
          description: Protocol is finalized and cannot be deleted

  /api/v1/advisory-protocols/{id}/hand-over:
    post:
      summary: >
        Finalize and mark the protocol as handed over to the customer.
        After this call the protocol is immutable (legal document).
        Idempotent — safe to call twice.
      operationId: handOverAdvisoryProtocol
      tags: [advisory-protocols]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Finalized protocol
          content:
            application/json:
              schema:
                type: object
                properties:
                  protocol:
                    $ref: '#/components/schemas/AdvisoryProtocol'
        '403': { $ref: '#/components/responses/Forbidden' }
        '404': { $ref: '#/components/responses/NotFound' }

  /api/v1/advisory-protocols/{id}/pdf:
    get:
      summary: >
        Server-side PDF of the advisory protocol (Geeignetheitserklärung).
        Currently returns 501 — use FE window.print() path for now.
        Will be wired to GeneratePDF once a dedicated streaming RPC is added.
      operationId: getAdvisoryProtocolPDF
      tags: [advisory-protocols]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: PDF binary
          content:
            application/pdf:
              schema:
                type: string
                format: binary
        '501':
          description: Not yet implemented on server side

  /api/v1/contacts/referral-report:
    get:
      summary: Aggregated referral count per referrer (all contacts in the tenant)
      operationId: getReferralReport
      tags: [advisory-protocols]
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Referral report
          content:
            application/json:
              schema:
                type: object
                properties:
                  entries:
                    type: array
                    items:
                      $ref: '#/components/schemas/ReferralReportEntry'
        '403': { $ref: '#/components/responses/Forbidden' }

  /api/v1/tenant/module-leads:
    get:
      summary: List module-lead assignments for the tenant
      operationId: listModuleLeads
      tags: [Settings]
      security:
        - bearerAuth: []
      parameters:
        - name: user_id
          in: query
          schema: { type: string, format: uuid }
          description: Filter by user ID
        - name: module_id
          in: query
          schema: { type: string }
          description: Filter by module ID
      responses:
        "200":
          description: List of module leads
          content:
            application/json:
              schema:
                type: object
                properties:
                  leads:
                    type: array
                    items: { $ref: "#/components/schemas/ModuleLead" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/tenant/module-leads/me:
    get:
      summary: Get module IDs the current user is a lead for
      operationId: getMyModuleLeads
      tags: [Settings]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Lead modules for the caller
          content:
            application/json:
              schema:
                type: object
                properties:
                  module_ids:
                    type: array
                    items: { type: string }
                    description: Module IDs where the caller is lead (empty for non-leads)
                  is_admin:
                    type: boolean
                    description: True when the caller is an admin (implicitly leads all modules)
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/tenant/module-leads/{user_id}/{module_id}:
    put:
      summary: Grant module-lead rights to a user (admin only)
      operationId: grantModuleLead
      tags: [Settings]
      security:
        - bearerAuth: []
      parameters:
        - name: user_id
          in: path
          required: true
          schema: { type: string, format: uuid }
        - name: module_id
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Module lead granted
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ModuleLead" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    delete:
      summary: Revoke module-lead rights from a user (admin only)
      operationId: revokeModuleLead
      tags: [Settings]
      security:
        - bearerAuth: []
      parameters:
        - name: user_id
          in: path
          required: true
          schema: { type: string, format: uuid }
        - name: module_id
          in: path
          required: true
          schema: { type: string }
      responses:
        "204":
          description: Module lead revoked
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/settings/{module_id}:
    get:
      summary: Get resolved settings (user > tenant, server-side merge)
      operationId: getResolvedSettings
      tags: [Settings]
      security:
        - bearerAuth: []
      parameters:
        - name: module_id
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Resolved settings map
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SettingsResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/settings/{module_id}/tenant:
    get:
      summary: Get raw tenant-scope settings for a module
      operationId: getTenantSettings
      tags: [Settings]
      security:
        - bearerAuth: []
      parameters:
        - name: module_id
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Tenant-scope settings
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SettingsResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
    put:
      summary: Update tenant-scope settings (module-lead or admin only)
      operationId: putTenantSettings
      tags: [Settings]
      security:
        - bearerAuth: []
      parameters:
        - name: module_id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/PutSettingsRequest" }
      responses:
        "200":
          description: Updated tenant settings
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SettingsResponse" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: Forbidden — caller is not a module-lead or admin for this module

  /api/v1/settings/{module_id}/user:
    get:
      summary: Get the caller's user-scope settings for a module
      operationId: getUserSettings
      tags: [Settings]
      security:
        - bearerAuth: []
      parameters:
        - name: module_id
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: User-scope settings
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SettingsResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
    put:
      summary: Update the caller's user-scope settings for a module
      operationId: putUserSettings
      tags: [Settings]
      security:
        - bearerAuth: []
      parameters:
        - name: module_id
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/PutSettingsRequest" }
      responses:
        "200":
          description: Updated user settings
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SettingsResponse" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/customization/labels:
    get:
      summary: Get resolved tenant label overrides for a locale
      description: >
        Every whitelisted i18n key, each with the effective value and its
        provenance ("tenant" or "default"). Available to any authenticated
        user — the labels drive UI text for everyone, not just editors.
      operationId: getCustomizationLabels
      tags: [Customization]
      security:
        - bearerAuth: []
      parameters:
        - name: locale
          in: query
          required: false
          schema: { type: string, default: de }
        - name: base
          in: query
          required: false
          schema: { type: string, enum: ["1"] }
          description: When "1", returns the code-default baseline (every key provenance "default") instead of resolving tenant overrides.
      responses:
        "200":
          description: Resolved label map
          content:
            application/json:
              schema: { $ref: "#/components/schemas/LabelOverridesResponse" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
    put:
      summary: Update tenant-scope label overrides for a locale
      operationId: putCustomizationLabels
      tags: [Customization]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpdateLabelOverridesRequest" }
      responses:
        "200":
          description: Updated, freshly resolved label map
          content:
            application/json:
              schema: { $ref: "#/components/schemas/LabelOverridesResponse" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403":
          description: Forbidden — caller lacks admin:customization:manage

  /api/v1/finance/gobd-archive:
    get:
      summary: List GoBD archived documents
      operationId: listGobdDocuments
      tags: [GoBD Archive]
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: doc_type
          schema:
            type: string
            enum: [invoice, credit_note, receipt, contract, correspondence, other]
          description: Filter by document type
        - in: query
          name: source_invoice_id
          schema:
            type: string
            format: uuid
          description: Filter by linked invoice ID
        - in: query
          name: date_from
          schema:
            type: string
            format: date
          description: Lower bound for archived_at (YYYY-MM-DD)
        - in: query
          name: date_to
          schema:
            type: string
            format: date
          description: Upper bound for archived_at (YYYY-MM-DD)
        - in: query
          name: page
          schema:
            type: integer
            minimum: 1
            default: 1
        - in: query
          name: per_page
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 50
      responses:
        "200":
          description: Paginated list of archived documents
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GobdDocumentList"
        "401":
          description: Unauthorized
    post:
      summary: Archive a new document (Beleg archivieren)
      operationId: archiveDocument
      tags: [GoBD Archive]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file, doc_type]
              properties:
                file:
                  type: string
                  format: binary
                  description: Document file (max 50 MiB)
                doc_type:
                  type: string
                  enum: [invoice, credit_note, receipt, contract, correspondence, other]
                source_invoice_id:
                  type: string
                  format: uuid
                  description: Optional — links the document to a finance_invoices record
      responses:
        "201":
          description: Document archived successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GobdDocument"
        "400":
          description: Validation error (empty file, unknown doc_type, file too large)
        "401":
          description: Unauthorized

  /api/v1/finance/gobd-archive/from-invoice/{invoiceId}:
    post:
      summary: Archive the PDF of a locked invoice
      description: |
        Generates the invoice PDF and archives it. The invoice must be locked
        (LockedAt set) — GoBD requires finalizing documents before archiving.
      operationId: archiveInvoiceDocument
      tags: [GoBD Archive]
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: invoiceId
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "201":
          description: Invoice document archived
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GobdDocument"
        "400":
          description: Invoice not locked
        "404":
          description: Invoice not found

  /api/v1/finance/gobd-archive/{id}:
    get:
      summary: Get a single GoBD document with its audit trail
      operationId: getGobdDocument
      tags: [GoBD Archive]
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Document with events
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GobdDocumentWithEvents"
        "404":
          description: Document not found

  /api/v1/finance/gobd-archive/{id}/download:
    get:
      summary: Get a presigned download URL for a GoBD document
      operationId: downloadGobdDocument
      tags: [GoBD Archive]
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Presigned URL valid for 1 hour
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GobdDownloadResponse"
        "404":
          description: Document not found

  /api/v1/finance/gobd-archive/{id}/annotations:
    post:
      summary: Add a text annotation to a GoBD document
      description: |
        Appends an annotation event to the immutable audit trail.
        The document itself is not modified (GoBD immutability).
      operationId: addDocumentAnnotation
      tags: [GoBD Archive]
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [note]
              properties:
                note:
                  type: string
                  minLength: 1
                  maxLength: 2000
      responses:
        "201":
          description: Annotation added
        "404":
          description: Document not found

  /api/v1/finance/invoices/import:
    post:
      summary: Import an inbound e-invoice (E-Rechnung Eingang)
      description: |
        Accepts a PDF (with embedded ZUGFeRD/Factur-X XML) or a raw XML file
        (XRechnung UBL or ZUGFeRD CII). Parses and persists the invoice.
      operationId: importIncomingInvoice
      tags:
        - Finance
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: PDF with embedded ZUGFeRD XML, or raw XML (CII/UBL)
      responses:
        "201":
          description: Invoice imported successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IncomingInvoice"
        "400":
          $ref: "#/components/responses/BadRequest"
        "409":
          description: Duplicate invoice (same supplier + invoice number already imported)
        "415":
          description: Unsupported media type
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/finance/incoming-invoices:
    get:
      summary: List incoming invoices
      operationId: listIncomingInvoices
      tags:
        - Finance
      security:
        - bearerAuth: []
      parameters:
        - name: status
          in: query
          schema:
            type: string
            enum: [received, reviewed, booked, rejected]
        - name: date_from
          in: query
          schema:
            type: string
            format: date
        - name: date_to
          in: query
          schema:
            type: string
            format: date
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: per_page
          in: query
          schema:
            type: integer
            default: 50
      responses:
        "200":
          description: Paginated list of incoming invoices
          content:
            application/json:
              schema:
                type: object
                properties:
                  invoices:
                    type: array
                    items:
                      $ref: "#/components/schemas/IncomingInvoice"
                  total:
                    type: integer
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/finance/incoming-invoices/{id}:
    get:
      summary: Get a single incoming invoice
      operationId: getIncomingInvoice
      tags:
        - Finance
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Incoming invoice
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IncomingInvoice"
        "404":
          $ref: "#/components/responses/NotFound"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/finance/incoming-invoices/{id}/status:
    patch:
      summary: Update incoming invoice status
      operationId: updateIncomingInvoiceStatus
      tags:
        - Finance
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - status
              properties:
                status:
                  type: string
                  enum: [reviewed, booked, rejected]
      responses:
        "200":
          description: Updated incoming invoice
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IncomingInvoice"
        "422":
          description: Invalid status transition
        "404":
          $ref: "#/components/responses/NotFound"
        "401":
          $ref: "#/components/responses/Unauthorized"

  # ============================================================================
  # Work Labels
  # ============================================================================

  /api/v1/work/labels:
    get:
      summary: List all labels for the tenant
      tags: [work-labels]
      operationId: listWorkLabels
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Label list
          content:
            application/json:
              schema:
                type: object
                properties:
                  labels:
                    type: array
                    items:
                      $ref: "#/components/schemas/WorkLabel"
        "401":
          $ref: "#/components/responses/Unauthorized"
    post:
      summary: Create a new label
      tags: [work-labels]
      operationId: createWorkLabel
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name:
                  type: string
                  maxLength: 100
                color:
                  type: string
                  pattern: '^#[0-9A-Fa-f]{6}$'
                  default: "#6b7280"
      responses:
        "201":
          description: Created label
          content:
            application/json:
              schema:
                type: object
                properties:
                  label:
                    $ref: "#/components/schemas/WorkLabel"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          description: Label name already exists

  /api/v1/work/labels/{id}:
    parameters:
      - $ref: "#/components/parameters/ResourceId"
    get:
      summary: Get a label by ID
      tags: [work-labels]
      operationId: getWorkLabel
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Label
          content:
            application/json:
              schema:
                type: object
                properties:
                  label:
                    $ref: "#/components/schemas/WorkLabel"
        "404":
          $ref: "#/components/responses/NotFound"
        "401":
          $ref: "#/components/responses/Unauthorized"
    put:
      summary: Update a label
      tags: [work-labels]
      operationId: updateWorkLabel
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name:
                  type: string
                  maxLength: 100
                color:
                  type: string
                  pattern: '^#[0-9A-Fa-f]{6}$'
      responses:
        "200":
          description: Updated label
          content:
            application/json:
              schema:
                type: object
                properties:
                  label:
                    $ref: "#/components/schemas/WorkLabel"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
        "401":
          $ref: "#/components/responses/Unauthorized"
    delete:
      summary: Delete a label
      tags: [work-labels]
      operationId: deleteWorkLabel
      security:
        - bearerAuth: []
      responses:
        "204":
          description: Deleted
        "404":
          $ref: "#/components/responses/NotFound"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/tasks/{id}/labels:
    parameters:
      - $ref: "#/components/parameters/ResourceId"
    put:
      summary: Replace all labels assigned to a task
      tags: [work-labels]
      operationId: setTaskLabels
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                label_ids:
                  type: array
                  items:
                    type: string
                    format: uuid
      responses:
        "204":
          description: Labels updated
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  # ============================================================================
  # Work Custom Field Definitions
  # ============================================================================

  /api/v1/work/custom-fields:
    get:
      summary: List all custom field definitions for the tenant
      tags: [work-custom-fields]
      operationId: listWorkCustomFieldDefinitions
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Custom field definition list
          content:
            application/json:
              schema:
                type: object
                properties:
                  definitions:
                    type: array
                    items:
                      $ref: "#/components/schemas/WorkCustomFieldDefinition"
        "401":
          $ref: "#/components/responses/Unauthorized"
    post:
      summary: Create a new custom field definition
      tags: [work-custom-fields]
      operationId: createWorkCustomFieldDefinition
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name, field_type]
              properties:
                name:
                  type: string
                  maxLength: 100
                field_type:
                  type: string
                  enum: [text, number, date, boolean, select, multi_select, url, email, phone]
                options:
                  type: array
                  items:
                    type: string
                position:
                  type: integer
                  default: 0
      responses:
        "201":
          description: Created definition
          content:
            application/json:
              schema:
                type: object
                properties:
                  definition:
                    $ref: "#/components/schemas/WorkCustomFieldDefinition"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          description: Field name already exists

  /api/v1/work/custom-fields/{id}:
    parameters:
      - $ref: "#/components/parameters/ResourceId"
    get:
      summary: Get a custom field definition by ID
      tags: [work-custom-fields]
      operationId: getWorkCustomFieldDefinition
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Custom field definition
          content:
            application/json:
              schema:
                type: object
                properties:
                  definition:
                    $ref: "#/components/schemas/WorkCustomFieldDefinition"
        "404":
          $ref: "#/components/responses/NotFound"
        "401":
          $ref: "#/components/responses/Unauthorized"
    put:
      summary: Update a custom field definition
      tags: [work-custom-fields]
      operationId: updateWorkCustomFieldDefinition
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name, field_type]
              properties:
                name:
                  type: string
                  maxLength: 100
                field_type:
                  type: string
                  enum: [text, number, date, boolean, select, multi_select, url, email, phone]
                options:
                  type: array
                  items:
                    type: string
                position:
                  type: integer
      responses:
        "200":
          description: Updated definition
          content:
            application/json:
              schema:
                type: object
                properties:
                  definition:
                    $ref: "#/components/schemas/WorkCustomFieldDefinition"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
        "401":
          $ref: "#/components/responses/Unauthorized"
    delete:
      summary: Delete a custom field definition
      tags: [work-custom-fields]
      operationId: deleteWorkCustomFieldDefinition
      security:
        - bearerAuth: []
      responses:
        "204":
          description: Deleted
        "404":
          $ref: "#/components/responses/NotFound"
        "401":
          $ref: "#/components/responses/Unauthorized"

  # ===========================================================================
  # Helpdesk
  # ===========================================================================

  /api/v1/helpdesk/tickets:
    get:
      summary: List helpdesk tickets
      tags: [helpdesk-tickets]
      operationId: listHelpdeskTickets
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: status, in: query, schema: { type: string, enum: [open, pending, closed, resolved] } }
        - { name: page, in: query, schema: { type: integer, minimum: 1, default: 1 } }
        - { name: page_size, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 20 } }
        - { name: contact_id, in: query, schema: { type: string, format: uuid } }
        - { name: org_id, in: query, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Paginated ticket list
          content:
            application/json:
              schema: { type: object }
        "401": { $ref: "#/components/responses/Unauthorized" }
    post:
      summary: Create a helpdesk ticket
      tags: [helpdesk-tickets]
      operationId: createHelpdeskTicket
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [subject]
              properties:
                subject: { type: string, maxLength: 200 }
                priority: { type: string, enum: [low, normal, high, urgent] }
                assignee_id: { type: string, format: uuid }
                queue_id: { type: string, format: uuid }
                contact_id: { type: string, format: uuid }
                org_id: { type: string, format: uuid }
      responses:
        "201":
          description: Ticket created
          content:
            application/json:
              schema: { type: object }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/helpdesk/tickets/from-message:
    post:
      summary: Convert an inbox message into a ticket
      description: >-
        Links back to the source inbox message via source_channel/source_message_id
        instead of copying its content a second time. Converting the same
        message_id twice is idempotent: the second call returns the
        already-existing ticket with 200 instead of creating a duplicate.
      tags: [helpdesk-tickets]
      operationId: createHelpdeskTicketFromMessage
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [message_id]
              properties:
                message_id: { type: string, format: uuid }
      responses:
        "201":
          description: Ticket created from the inbox message
          content:
            application/json:
              schema: { type: object }
        "200":
          description: message_id was already converted; the existing ticket is returned
          content:
            application/json:
              schema: { type: object }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
        "503": { $ref: "#/components/responses/ServiceUnavailable" }

  /api/v1/helpdesk/tickets/{id}:
    get:
      summary: Get a helpdesk ticket by ID
      tags: [helpdesk-tickets]
      operationId: getHelpdeskTicket
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Ticket
          content:
            application/json:
              schema: { type: object }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    put:
      summary: Update a helpdesk ticket
      tags: [helpdesk-tickets]
      operationId: updateHelpdeskTicket
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                subject: { type: string, maxLength: 200 }
                priority: { type: string, enum: [low, normal, high, urgent] }
                assignee_id: { type: string, format: uuid }
                queue_id: { type: string, format: uuid }
                contact_id: { type: string, format: uuid }
                org_id: { type: string, format: uuid }
      responses:
        "200":
          description: Ticket updated
          content:
            application/json:
              schema: { type: object }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/helpdesk/tickets/{id}/close:
    post:
      summary: Close a helpdesk ticket
      tags: [helpdesk-tickets]
      operationId: closeHelpdeskTicket
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Ticket closed
          content:
            application/json:
              schema: { type: object }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/helpdesk/tickets/{id}/reopen:
    post:
      summary: Reopen a closed helpdesk ticket
      tags: [helpdesk-tickets]
      operationId: reopenHelpdeskTicket
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Ticket reopened
          content:
            application/json:
              schema: { type: object }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/helpdesk/tickets/{id}/assign:
    post:
      summary: Assign a helpdesk ticket to an agent
      tags: [helpdesk-tickets]
      operationId: assignHelpdeskTicket
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [assignee_id]
              properties:
                assignee_id: { type: string, format: uuid }
      responses:
        "200":
          description: Ticket assigned
          content:
            application/json:
              schema: { type: object }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/helpdesk/tickets/{id}/merge:
    post:
      summary: Merge a ticket into another ticket
      tags: [helpdesk-tickets]
      operationId: mergeHelpdeskTickets
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid }, description: Source ticket ID }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [target_ticket_id]
              properties:
                target_ticket_id: { type: string, format: uuid }
      responses:
        "200":
          description: Tickets merged
          content:
            application/json:
              schema: { type: object, properties: { status: { type: string } } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/helpdesk/tickets/{id}/messages:
    get:
      summary: List messages on a helpdesk ticket
      tags: [helpdesk-messages]
      operationId: listHelpdeskMessages
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Message list
          content:
            application/json:
              schema: { type: object }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      summary: Add a message to a helpdesk ticket
      tags: [helpdesk-messages]
      operationId: addHelpdeskMessage
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [body]
              properties:
                body: { type: string }
                internal: { type: boolean, description: Internal note (not visible to requester) }
                attachments: { type: array, items: { type: string } }
      responses:
        "201":
          description: Message added
          content:
            application/json:
              schema: { type: object }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/helpdesk/queues:
    get:
      summary: List helpdesk queues
      tags: [helpdesk-queues]
      operationId: listHelpdeskQueues
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Queue list
          content:
            application/json:
              schema: { type: object }
        "401": { $ref: "#/components/responses/Unauthorized" }
    post:
      summary: Create a helpdesk queue
      tags: [helpdesk-queues]
      operationId: createHelpdeskQueue
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name: { type: string }
                default_assignee_id: { type: string, format: uuid }
                sla_policy_id: { type: string, format: uuid }
      responses:
        "201":
          description: Queue created
          content:
            application/json:
              schema: { type: object }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/helpdesk/queues/{id}:
    put:
      summary: Update a helpdesk queue
      tags: [helpdesk-queues]
      operationId: updateHelpdeskQueue
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name: { type: string }
                default_assignee_id: { type: string, format: uuid }
                sla_policy_id: { type: string, format: uuid }
      responses:
        "200":
          description: Queue updated
          content:
            application/json:
              schema: { type: object }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      summary: Delete a helpdesk queue
      tags: [helpdesk-queues]
      operationId: deleteHelpdeskQueue
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Queue deleted
          content:
            application/json:
              schema: { type: object, properties: { status: { type: string } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/helpdesk/canned-responses:
    get:
      summary: List canned (template) responses
      tags: [helpdesk-canned-responses]
      operationId: listCannedResponses
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Canned response list
          content:
            application/json:
              schema: { type: object }
        "401": { $ref: "#/components/responses/Unauthorized" }
    post:
      summary: Create a canned response
      tags: [helpdesk-canned-responses]
      operationId: createCannedResponse
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name, body]
              properties:
                name: { type: string }
                body: { type: string }
      responses:
        "201":
          description: Canned response created
          content:
            application/json:
              schema: { type: object }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/helpdesk/canned-responses/{id}:
    put:
      summary: Update a canned response
      tags: [helpdesk-canned-responses]
      operationId: updateCannedResponse
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name: { type: string }
                body: { type: string }
      responses:
        "200":
          description: Canned response updated
          content:
            application/json:
              schema: { type: object }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      summary: Delete a canned response
      tags: [helpdesk-canned-responses]
      operationId: deleteCannedResponse
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Canned response deleted
          content:
            application/json:
              schema: { type: object, properties: { status: { type: string } } }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/helpdesk/sla-policies:
    get:
      summary: List SLA policies
      tags: [helpdesk-sla]
      operationId: listSLAPolicies
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: SLA policy list
          content:
            application/json:
              schema: { type: object }
        "401": { $ref: "#/components/responses/Unauthorized" }
    post:
      summary: Create an SLA policy
      tags: [helpdesk-sla]
      operationId: createSLAPolicy
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name: { type: string }
                first_response_mins: { type: integer, description: Minutes until first response }
                resolution_mins: { type: integer, description: Minutes until resolution }
                business_hours: { type: string }
      responses:
        "201":
          description: SLA policy created
          content:
            application/json:
              schema: { type: object }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/helpdesk/sla-policies/{id}:
    put:
      summary: Update an SLA policy
      tags: [helpdesk-sla]
      operationId: updateSLAPolicy
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name: { type: string }
                first_response_mins: { type: integer }
                resolution_mins: { type: integer }
                business_hours: { type: string }
      responses:
        "200":
          description: SLA policy updated
          content:
            application/json:
              schema: { type: object }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      summary: Delete an SLA policy
      tags: [helpdesk-sla]
      operationId: deleteSLAPolicy
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: SLA policy deleted
          content:
            application/json:
              schema: { type: object }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/helpdesk/tickets/{id}/sla:
    post:
      summary: Apply an SLA policy to a ticket
      tags: [helpdesk-sla]
      operationId: applySLAPolicy
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [sla_policy_id]
              properties:
                sla_policy_id: { type: string, format: uuid }
      responses:
        "200":
          description: SLA applied
          content:
            application/json:
              schema: { type: object }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/helpdesk/tickets/{id}/sla-status:
    get:
      summary: Get SLA status for a ticket
      tags: [helpdesk-sla]
      operationId: getHelpdeskSLAStatus
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: sla_policy_id, in: query, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: SLA status
          content:
            application/json:
              schema: { type: object }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/helpdesk/kb-articles:
    get:
      summary: List helpdesk knowledge-base articles
      tags: [helpdesk-kb]
      operationId: listHelpdeskKBArticles
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: category, in: query, schema: { type: string } }
        - { name: status, in: query, schema: { type: string, enum: [draft, published] } }
      responses:
        "200":
          description: KB article list
          content:
            application/json:
              schema: { type: object }
        "401": { $ref: "#/components/responses/Unauthorized" }
    post:
      summary: Create a helpdesk knowledge-base article
      tags: [helpdesk-kb]
      operationId: createHelpdeskKBArticle
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [title]
              properties:
                title: { type: string, maxLength: 300 }
                content: { type: string }
                category: { type: string, maxLength: 100 }
                status: { type: string, enum: [draft, published] }
      responses:
        "201":
          description: KB article created
          content:
            application/json:
              schema: { type: object }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/helpdesk/kb-articles/{id}:
    put:
      summary: Update a helpdesk knowledge-base article
      tags: [helpdesk-kb]
      operationId: updateHelpdeskKBArticle
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                title: { type: string, maxLength: 300 }
                content: { type: string }
                category: { type: string, maxLength: 100 }
                status: { type: string, enum: [draft, published] }
      responses:
        "200":
          description: KB article updated
          content:
            application/json:
              schema: { type: object }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      summary: Delete a helpdesk knowledge-base article
      tags: [helpdesk-kb]
      operationId: deleteHelpdeskKBArticle
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: KB article deleted
          content:
            application/json:
              schema: { type: object }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/helpdesk/routing-rules:
    get:
      summary: List helpdesk ticket-routing rules
      tags: [helpdesk-routing]
      operationId: listHelpdeskRoutingRules
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Routing rule list
          content:
            application/json:
              schema: { type: object }
        "401": { $ref: "#/components/responses/Unauthorized" }
    post:
      summary: Create a helpdesk ticket-routing rule
      tags: [helpdesk-routing]
      operationId: createHelpdeskRoutingRule
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name: { type: string }
                conditions: { type: string, description: "JSON-encoded condition object" }
                target_queue_id: { type: string, format: uuid }
                priority: { type: integer }
                enabled: { type: boolean }
      responses:
        "201":
          description: Routing rule created
          content:
            application/json:
              schema: { type: object }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/helpdesk/routing-rules/{id}:
    put:
      summary: Update a helpdesk ticket-routing rule
      tags: [helpdesk-routing]
      operationId: updateHelpdeskRoutingRule
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name: { type: string }
                conditions: { type: string }
                target_queue_id: { type: string, format: uuid }
                priority: { type: integer }
                enabled: { type: boolean }
      responses:
        "200":
          description: Routing rule updated
          content:
            application/json:
              schema: { type: object }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      summary: Delete a helpdesk ticket-routing rule
      tags: [helpdesk-routing]
      operationId: deleteHelpdeskRoutingRule
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Routing rule deleted
          content:
            application/json:
              schema: { type: object }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/helpdesk/stats:
    get:
      summary: Get helpdesk dashboard statistics
      tags: [helpdesk-stats]
      operationId: getHelpdeskStats
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Helpdesk statistics
          content:
            application/json:
              schema: { type: object }
        "401": { $ref: "#/components/responses/Unauthorized" }

  # ===========================================================================
  # Wiki
  # ===========================================================================

  /api/v1/wiki/articles:
    get:
      summary: List wiki articles
      tags: [wiki-articles]
      operationId: listWikiArticles
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: search, in: query, schema: { type: string } }
        - { name: category_id, in: query, schema: { type: string, format: uuid } }
        - { name: author_id, in: query, schema: { type: string, format: uuid } }
        - { name: published, in: query, schema: { type: boolean } }
        - { name: sort_by, in: query, schema: { type: string } }
        - { name: sort_desc, in: query, schema: { type: boolean } }
        - { name: page, in: query, schema: { type: integer, minimum: 1, default: 1 } }
        - { name: page_size, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 20 } }
      responses:
        "200":
          description: Paginated article list
          content:
            application/json:
              schema: { type: object }
        "401": { $ref: "#/components/responses/Unauthorized" }
    post:
      summary: Create a wiki article
      tags: [wiki-articles]
      operationId: createWikiArticle
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [title]
              properties:
                title: { type: string }
                slug: { type: string }
                content: { type: string, format: byte }
                category_id: { type: string, format: uuid }
                published: { type: boolean }
      responses:
        "201":
          description: Article created
          content:
            application/json:
              schema: { type: object }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/wiki/articles/{id}:
    get:
      summary: Get a wiki article
      tags: [wiki-articles]
      operationId: getWikiArticle
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Article
          content:
            application/json:
              schema: { type: object }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      summary: Update a wiki article
      tags: [wiki-articles]
      operationId: updateWikiArticle
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                title: { type: string }
                slug: { type: string }
                content: { type: string, format: byte }
                category_id: { type: string, format: uuid }
                published: { type: boolean }
      responses:
        "200":
          description: Article updated
          content:
            application/json:
              schema: { type: object }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      summary: Delete a wiki article
      tags: [wiki-articles]
      operationId: deleteWikiArticle
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "204":
          description: Deleted
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/wiki/articles/{id}/versions:
    get:
      summary: List versions of a wiki article
      tags: [wiki-articles]
      operationId: listWikiArticleVersions
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Version list
          content:
            application/json:
              schema: { type: object }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/wiki/articles/{id}/versions/{versionId}/restore:
    post:
      summary: Restore a previous version of a wiki article
      tags: [wiki-articles]
      operationId: restoreWikiArticleVersion
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: versionId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Article restored to version
          content:
            application/json:
              schema: { type: object }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/wiki/articles/{id}/attachments:
    get:
      summary: List attachments of a wiki article
      tags: [wiki-articles]
      operationId: listWikiArticleAttachments
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Attachment list
          content:
            application/json:
              schema: { type: object }
        "401": { $ref: "#/components/responses/Unauthorized" }
    post:
      summary: Upload an attachment to a wiki article
      tags: [wiki-articles]
      operationId: uploadWikiArticleAttachment
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [file_ref]
              properties:
                file_ref: { type: string }
                mime: { type: string }
                size: { type: integer, format: int64 }
      responses:
        "201":
          description: Attachment uploaded
          content:
            application/json:
              schema: { type: object }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/wiki/articles/{id}/attachments/{attachmentId}:
    delete:
      summary: Delete a wiki article attachment
      tags: [wiki-articles]
      operationId: deleteWikiArticleAttachment
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: attachmentId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "204":
          description: Attachment deleted
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/wiki/search:
    get:
      summary: Full-text search across wiki articles
      tags: [wiki-articles]
      operationId: searchWikiArticles
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: q, in: query, required: true, schema: { type: string } }
        - { name: limit, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 20 } }
      responses:
        "200":
          description: Search results
          content:
            application/json:
              schema: { type: object }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/wiki/categories:
    get:
      summary: List wiki categories
      tags: [wiki-categories]
      operationId: listWikiCategories
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Category list
          content:
            application/json:
              schema: { type: object }
        "401": { $ref: "#/components/responses/Unauthorized" }
    post:
      summary: Create a wiki category
      tags: [wiki-categories]
      operationId: createWikiCategory
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name: { type: string }
                parent_id: { type: string, format: uuid }
                position: { type: integer }
      responses:
        "201":
          description: Category created
          content:
            application/json:
              schema: { type: object }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/wiki/categories/{id}:
    patch:
      summary: Update a wiki category
      tags: [wiki-categories]
      operationId: updateWikiCategory
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name: { type: string }
                parent_id: { type: string }
                position: { type: integer }
      responses:
        "200":
          description: Category updated
          content:
            application/json:
              schema: { type: object }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      summary: Delete a wiki category
      tags: [wiki-categories]
      operationId: deleteWikiCategory
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "204":
          description: Category deleted
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/wiki/articles/{id}/share:
    get:
      summary: List share tokens for a wiki article
      tags: [wiki-articles]
      operationId: listWikiShareTokens
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Share token list
          content:
            application/json:
              schema: { type: object }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      summary: Create a share token for a wiki article
      tags: [wiki-articles]
      operationId: createWikiShareToken
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                expires_at: { type: string, format: date-time }
                permissions: { type: array, items: { type: string } }
      responses:
        "201":
          description: Share token created
          content:
            application/json:
              schema: { type: object }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/wiki/share/{tokenId}:
    delete:
      summary: Revoke a wiki article share token
      tags: [wiki-articles]
      operationId: revokeWikiShareToken
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: tokenId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "204":
          description: Share token revoked
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/wiki/versions/{id}:
    get:
      summary: Get a single wiki article version
      tags: [wiki-articles]
      operationId: getWikiVersion
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Version
          content:
            application/json:
              schema: { type: object }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  # ===========================================================================
  # Schichten (Shift Planning)
  # ===========================================================================

  /api/v1/schichten/shifts:
    get:
      summary: List shifts
      tags: [schichten]
      operationId: listShifts
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: status, in: query, schema: { type: string } }
        - { name: page, in: query, schema: { type: integer, minimum: 1, default: 1 } }
        - { name: page_size, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 50 } }
      responses:
        "200":
          description: Paginated shift list
          content:
            application/json:
              schema:
                type: object
                properties:
                  shifts:
                    type: array
                    items:
                      $ref: '#/components/schemas/SchichtenShift'
                  total:
                    type: integer
        "401": { $ref: "#/components/responses/Unauthorized" }
    post:
      summary: Create a shift
      tags: [schichten]
      operationId: createShift
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [title, start_time, end_time]
              properties:
                title: { type: string }
                description: { type: string }
                start_time: { type: string, format: date-time }
                end_time: { type: string, format: date-time }
                location: { type: string }
                created_by: { type: string, format: uuid }
      responses:
        "201":
          description: Shift created
          content:
            application/json:
              schema:
                type: object
                properties:
                  shift:
                    $ref: '#/components/schemas/SchichtenShift'
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/schichten/shifts/publish:
    post:
      summary: Publish shifts for a time range
      tags: [schichten]
      operationId: publishShifts
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [from, to]
              properties:
                from: { type: string, format: date-time }
                to: { type: string, format: date-time }
      responses:
        "200":
          description: Shifts published
          content:
            application/json:
              schema:
                type: object
                properties:
                  published_count:
                    type: integer
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/schichten/shifts/{id}:
    get:
      summary: Get a shift by ID
      tags: [schichten]
      operationId: getShift
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Shift
          content:
            application/json:
              schema:
                type: object
                properties:
                  shift:
                    $ref: '#/components/schemas/SchichtenShift'
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      summary: Update a shift
      tags: [schichten]
      operationId: updateShift
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                title: { type: string }
                description: { type: string }
                start_time: { type: string, format: date-time }
                end_time: { type: string, format: date-time }
                location: { type: string }
      responses:
        "200":
          description: Shift updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  shift:
                    $ref: '#/components/schemas/SchichtenShift'
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      summary: Delete a shift
      tags: [schichten]
      operationId: deleteShift
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "204":
          description: Deleted
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/schichten/shifts/{id}/assignments:
    get:
      summary: List employee assignments for a shift
      tags: [schichten]
      operationId: listShiftAssignments
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Assignment list
          content:
            application/json:
              schema:
                type: object
                properties:
                  assignments:
                    type: array
                    items:
                      $ref: '#/components/schemas/SchichtenAssignment'
        "401": { $ref: "#/components/responses/Unauthorized" }
    post:
      summary: Assign an employee to a shift
      tags: [schichten]
      operationId: assignEmployeeToShift
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [employee_id]
              properties:
                employee_id: { type: string, format: uuid }
                assigned_by: { type: string, format: uuid }
      responses:
        "201":
          description: Employee assigned
          content:
            application/json:
              schema:
                type: object
                properties:
                  assignment:
                    $ref: '#/components/schemas/SchichtenAssignment'
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/schichten/shifts/{id}/assignments/{employee_id}:
    delete:
      summary: Unassign an employee from a shift
      tags: [schichten]
      operationId: unassignEmployeeFromShift
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: employee_id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "204":
          description: Unassigned
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/schichten/templates:
    get:
      summary: List shift templates
      tags: [schichten]
      operationId: listShiftTemplates
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: page, in: query, schema: { type: integer, minimum: 1, default: 1 } }
        - { name: page_size, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 50 } }
      responses:
        "200":
          description: Template list
          content:
            application/json:
              schema:
                type: object
                properties:
                  templates:
                    type: array
                    items:
                      $ref: '#/components/schemas/SchichtenTemplate'
                  total:
                    type: integer
        "401": { $ref: "#/components/responses/Unauthorized" }
    post:
      summary: Create a shift template
      tags: [schichten]
      operationId: createShiftTemplate
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name, day_of_week, duration_minutes]
              properties:
                name: { type: string }
                description: { type: string }
                day_of_week: { type: integer, minimum: 0, maximum: 6 }
                start_hour: { type: integer, minimum: 0, maximum: 23 }
                start_minute: { type: integer, minimum: 0, maximum: 59 }
                duration_minutes: { type: integer, minimum: 1 }
                location: { type: string }
      responses:
        "201":
          description: Template created
          content:
            application/json:
              schema:
                type: object
                properties:
                  template:
                    $ref: '#/components/schemas/SchichtenTemplate'
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/schichten/templates/{id}:
    patch:
      summary: Update a shift template
      tags: [schichten]
      operationId: updateShiftTemplate
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name: { type: string }
                description: { type: string }
                day_of_week: { type: integer }
                start_hour: { type: integer }
                start_minute: { type: integer }
                duration_minutes: { type: integer }
                location: { type: string }
      responses:
        "200":
          description: Template updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  template:
                    $ref: '#/components/schemas/SchichtenTemplate'
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      summary: Delete a shift template
      tags: [schichten]
      operationId: deleteShiftTemplate
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "204":
          description: Deleted
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/schichten/templates/{id}/apply:
    post:
      summary: Apply a shift template to a date range
      tags: [schichten]
      operationId: applyShiftTemplate
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [range_start, range_end]
              properties:
                range_start: { type: string, format: date-time }
                range_end: { type: string, format: date-time }
                created_by: { type: string, format: uuid }
      responses:
        "201":
          description: Shifts created from template
          content:
            application/json:
              schema:
                type: object
                properties:
                  shifts:
                    type: array
                    items:
                      $ref: '#/components/schemas/SchichtenShift'
                  created_count:
                    type: integer
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/schichten/compliance:
    get:
      summary: Check ArbZG compliance for a planned shift
      tags: [schichten]
      operationId: checkArbzgCompliance
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: employee_id, in: query, required: true, schema: { type: string, format: uuid } }
        - { name: new_shift_start, in: query, required: true, schema: { type: string, format: date-time } }
        - { name: new_shift_end, in: query, required: true, schema: { type: string, format: date-time } }
      responses:
        "200":
          description: Compliance check result
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SchichtenComplianceReport'
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/schichten/stats:
    get:
      summary: Get shift planning statistics
      tags: [schichten]
      operationId: getShiftStats
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Shift statistics
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SchichtenStats'
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/schichten/swap-requests:
    get:
      summary: List shift swap requests
      tags: [schichten]
      operationId: listSwapRequests
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: shift_id, in: query, schema: { type: string, format: uuid } }
        - { name: status, in: query, schema: { type: string } }
        - { name: page, in: query, schema: { type: integer, minimum: 1, default: 1 } }
        - { name: page_size, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 50 } }
      responses:
        "200":
          description: Swap request list
          content:
            application/json:
              schema:
                type: object
                properties:
                  swap_requests:
                    type: array
                    items:
                      $ref: '#/components/schemas/SchichtenSwapRequest'
                  total:
                    type: integer
        "401": { $ref: "#/components/responses/Unauthorized" }
    post:
      summary: Create a shift swap request
      tags: [schichten]
      operationId: createSwapRequest
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: Idempotency-Key, in: header, required: true, schema: { type: string } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [assignment_id, shift_id, requested_by_employee_id, swap_with_employee_id]
              properties:
                assignment_id: { type: string, format: uuid }
                shift_id: { type: string, format: uuid }
                requested_by_employee_id: { type: string, format: uuid }
                swap_with_employee_id: { type: string, format: uuid }
                reason: { type: string }
      responses:
        "201":
          description: Swap request created
          content:
            application/json:
              schema:
                type: object
                properties:
                  swap_request:
                    $ref: '#/components/schemas/SchichtenSwapRequest'
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/schichten/swap-requests/{id}/approve:
    post:
      summary: Approve a shift swap request
      tags: [schichten]
      operationId: approveSwapRequest
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Swap request approved
          content:
            application/json:
              schema:
                type: object
                properties:
                  swap_request:
                    $ref: '#/components/schemas/SchichtenSwapRequest'
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/schichten/swap-requests/{id}/reject:
    post:
      summary: Reject a shift swap request
      tags: [schichten]
      operationId: rejectSwapRequest
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Swap request rejected
          content:
            application/json:
              schema:
                type: object
                properties:
                  swap_request:
                    $ref: '#/components/schemas/SchichtenSwapRequest'
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  # ===========================================================================
  # Produktion (Production)
  # ===========================================================================

  /api/v1/produktion/orders:
    get:
      summary: List production orders
      tags: [produktion]
      operationId: listProductionOrders
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: status, in: query, schema: { type: string } }
        - { name: priority, in: query, schema: { type: integer } }
        - { name: date_from, in: query, schema: { type: string, format: date-time } }
        - { name: date_to, in: query, schema: { type: string, format: date-time } }
        - { name: page, in: query, schema: { type: integer, minimum: 1, default: 1 } }
        - { name: page_size, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 20 } }
      responses:
        "200":
          description: Paginated order list
          content:
            application/json:
              schema: { type: object }
        "401": { $ref: "#/components/responses/Unauthorized" }
    post:
      summary: Create a production order
      tags: [produktion]
      operationId: createProductionOrder
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [order_number, product_name, quantity, planned_start, planned_end]
              properties:
                order_number: { type: string }
                product_name: { type: string }
                quantity: { type: integer, minimum: 1 }
                planned_start: { type: string, format: date-time }
                planned_end: { type: string, format: date-time }
                priority: { type: integer }
                notes: { type: string }
      responses:
        "201":
          description: Order created
          content:
            application/json:
              schema: { type: object }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/produktion/orders/{id}:
    get:
      summary: Get a production order
      tags: [produktion]
      operationId: getProductionOrder
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Production order
          content:
            application/json:
              schema: { type: object }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      summary: Update a production order
      tags: [produktion]
      operationId: updateProductionOrder
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                product_name: { type: string }
                quantity: { type: integer }
                planned_start: { type: string, format: date-time }
                planned_end: { type: string, format: date-time }
                priority: { type: integer }
                notes: { type: string }
      responses:
        "200":
          description: Order updated
          content:
            application/json:
              schema: { type: object }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      summary: Delete a production order
      tags: [produktion]
      operationId: deleteProductionOrder
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "204":
          description: Deleted
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/produktion/orders/{id}/start:
    post:
      summary: Start production on an order
      tags: [produktion]
      operationId: startProductionOrder
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Order started
          content:
            application/json:
              schema: { type: object }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/produktion/orders/{id}/complete:
    post:
      summary: Complete a production order
      tags: [produktion]
      operationId: completeProductionOrder
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Order completed
          content:
            application/json:
              schema: { type: object }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/produktion/orders/{id}/cancel:
    post:
      summary: Cancel a production order
      tags: [produktion]
      operationId: cancelProductionOrder
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Order cancelled
          content:
            application/json:
              schema: { type: object }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/produktion/bookings:
    get:
      summary: List machine bookings
      tags: [produktion]
      operationId: listMachineBookings
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: machine_id, in: query, schema: { type: string, format: uuid } }
        - { name: production_order_id, in: query, schema: { type: string, format: uuid } }
        - { name: date_from, in: query, schema: { type: string, format: date-time } }
        - { name: date_to, in: query, schema: { type: string, format: date-time } }
        - { name: page, in: query, schema: { type: integer, minimum: 1, default: 1 } }
        - { name: page_size, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 20 } }
      responses:
        "200":
          description: Machine booking list
          content:
            application/json:
              schema: { type: object }
        "401": { $ref: "#/components/responses/Unauthorized" }
    post:
      summary: Create a machine booking
      tags: [produktion]
      operationId: createMachineBooking
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [machine_id, production_order_id, starts_at, ends_at]
              properties:
                machine_id: { type: string, format: uuid }
                production_order_id: { type: string, format: uuid }
                starts_at: { type: string, format: date-time }
                ends_at: { type: string, format: date-time }
                notes: { type: string }
      responses:
        "201":
          description: Booking created
          content:
            application/json:
              schema: { type: object }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/produktion/bookings/{id}:
    patch:
      summary: Update a machine booking
      tags: [produktion]
      operationId: updateMachineBooking
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                machine_id: { type: string, format: uuid }
                production_order_id: { type: string, format: uuid }
                starts_at: { type: string, format: date-time }
                ends_at: { type: string, format: date-time }
                notes: { type: string }
      responses:
        "200":
          description: Booking updated
          content:
            application/json:
              schema: { type: object }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      summary: Delete a machine booking
      tags: [produktion]
      operationId: deleteMachineBooking
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "204":
          description: Deleted
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/produktion/plans:
    post:
      summary: Create a production plan
      tags: [produktion]
      operationId: createProductionPlan
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name, total_capacity_hours]
              properties:
                name: { type: string }
                week_number: { type: integer }
                year: { type: integer }
                total_capacity_hours: { type: number, format: float }
                planned_capacity_hours: { type: number, format: float }
                notes: { type: string }
      responses:
        "201":
          description: Plan created
          content:
            application/json:
              schema: { type: object }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/produktion/plans/{id}:
    get:
      summary: Get a production plan
      tags: [produktion]
      operationId: getProductionPlan
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Production plan
          content:
            application/json:
              schema: { type: object }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      summary: Update a production plan
      tags: [produktion]
      operationId: updateProductionPlan
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name: { type: string }
                week_number: { type: integer }
                year: { type: integer }
                total_capacity_hours: { type: number, format: float }
                planned_capacity_hours: { type: number, format: float }
                status: { type: string }
                notes: { type: string }
      responses:
        "200":
          description: Plan updated
          content:
            application/json:
              schema: { type: object }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/produktion/plans/{id}/capacity:
    get:
      summary: Get capacity overview for a production plan and machine
      tags: [produktion]
      operationId: getProductionCapacityOverview
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: machine_id, in: query, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Capacity overview
          content:
            application/json:
              schema: { type: object }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  # ===========================================================================
  # Fuhrpark (Fleet Management)
  # ===========================================================================

  /api/v1/fuhrpark/vehicles:
    get:
      summary: List fleet vehicles
      tags: [fuhrpark]
      operationId: listVehicles
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: search, in: query, schema: { type: string } }
        - { name: status, in: query, schema: { type: string } }
        - { name: fuel_type, in: query, schema: { type: string, enum: [petrol, diesel, electric, hybrid, other] } }
        - { name: page, in: query, schema: { type: integer, minimum: 1, default: 1 } }
        - { name: page_size, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 50 } }
      responses:
        "200":
          description: Paginated vehicle list
          content:
            application/json:
              schema:
                type: object
                properties:
                  vehicles:
                    type: array
                    items:
                      $ref: '#/components/schemas/FuhrparkVehicle'
                  total:
                    type: integer
        "401": { $ref: "#/components/responses/Unauthorized" }
    post:
      summary: Create (register) a vehicle
      tags: [fuhrpark]
      operationId: createVehicle
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [license_plate, make, model]
              properties:
                license_plate: { type: string }
                make: { type: string }
                model: { type: string }
                year: { type: integer }
                vin: { type: string }
                color: { type: string }
                fuel_type: { type: string, enum: [petrol, diesel, electric, hybrid, other] }
                mileage_km: { type: integer, format: int64 }
                tuev_due_date: { type: string }
                assigned_driver_id: { type: string, format: uuid }
                notes: { type: string }
      responses:
        "201":
          description: Vehicle created
          content:
            application/json:
              schema:
                type: object
                properties:
                  vehicle:
                    $ref: '#/components/schemas/FuhrparkVehicle'
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/fuhrpark/vehicles/{id}:
    get:
      summary: Get a vehicle by ID
      tags: [fuhrpark]
      operationId: getVehicle
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Vehicle
          content:
            application/json:
              schema:
                type: object
                properties:
                  vehicle:
                    $ref: '#/components/schemas/FuhrparkVehicle'
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      summary: Update a vehicle
      tags: [fuhrpark]
      operationId: updateVehicle
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                license_plate: { type: string }
                make: { type: string }
                model: { type: string }
                year: { type: integer }
                vin: { type: string }
                color: { type: string }
                fuel_type: { type: string }
                status: { type: string }
                mileage_km: { type: integer, format: int64 }
                tuev_due_date: { type: string }
                assigned_driver_id: { type: string, format: uuid }
                notes: { type: string }
      responses:
        "200":
          description: Vehicle updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  vehicle:
                    $ref: '#/components/schemas/FuhrparkVehicle'
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      summary: Delete a vehicle
      tags: [fuhrpark]
      operationId: deleteVehicle
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "204":
          description: Deleted
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/fuhrpark/vehicles/{id}/history:
    get:
      summary: Get event history for a vehicle
      tags: [fuhrpark]
      operationId: getVehicleHistory
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: page, in: query, schema: { type: integer, minimum: 1, default: 1 } }
        - { name: page_size, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 50 } }
      responses:
        "200":
          description: Vehicle history
          content:
            application/json:
              schema: { type: object }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/fuhrpark/vehicles/{id}/services:
    get:
      summary: List services for a vehicle
      tags: [fuhrpark]
      operationId: listVehicleServices
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: page, in: query, schema: { type: integer, minimum: 1, default: 1 } }
        - { name: page_size, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 50 } }
      responses:
        "200":
          description: Service list
          content:
            application/json:
              schema:
                type: object
                properties:
                  services:
                    type: array
                    items:
                      $ref: '#/components/schemas/FuhrparkService'
                  total:
                    type: integer
        "401": { $ref: "#/components/responses/Unauthorized" }
    post:
      summary: Schedule a service for a vehicle
      tags: [fuhrpark]
      operationId: scheduleVehicleService
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [service_type, scheduled_at]
              properties:
                service_type: { type: string }
                description: { type: string }
                scheduled_at: { type: string }
                cost_cents: { type: integer, format: int64 }
                workshop: { type: string }
                mileage_km: { type: integer, format: int64 }
                notes: { type: string }
      responses:
        "201":
          description: Service scheduled
          content:
            application/json:
              schema:
                type: object
                properties:
                  service:
                    $ref: '#/components/schemas/FuhrparkService'
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/fuhrpark/vehicles/{id}/damages:
    get:
      summary: List damage reports for a vehicle
      tags: [fuhrpark]
      operationId: listVehicleDamages
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: page, in: query, schema: { type: integer, minimum: 1, default: 1 } }
        - { name: page_size, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 50 } }
      responses:
        "200":
          description: Damage list
          content:
            application/json:
              schema:
                type: object
                properties:
                  damages:
                    type: array
                    items:
                      $ref: '#/components/schemas/FuhrparkDamage'
                  total:
                    type: integer
        "401": { $ref: "#/components/responses/Unauthorized" }
    post:
      summary: Report damage for a vehicle
      tags: [fuhrpark]
      operationId: reportVehicleDamage
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [description]
              properties:
                description: { type: string }
                severity: { type: string, enum: [minor, moderate, major, totalled] }
                reported_by: { type: string, format: uuid }
                photo_keys: { type: array, items: { type: string } }
                cost_cents: { type: integer, format: int64 }
                notes: { type: string }
      responses:
        "201":
          description: Damage reported
          content:
            application/json:
              schema:
                type: object
                properties:
                  damage:
                    $ref: '#/components/schemas/FuhrparkDamage'
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/fuhrpark/vehicles/{id}/fuel-logs:
    get:
      summary: List fuel logs for a vehicle
      tags: [fuhrpark]
      operationId: listVehicleFuelLogs
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: page, in: query, schema: { type: integer, minimum: 1, default: 1 } }
        - { name: page_size, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 50 } }
      responses:
        "200":
          description: Fuel log list
          content:
            application/json:
              schema:
                type: object
                properties:
                  fuel_logs:
                    type: array
                    items:
                      $ref: '#/components/schemas/FuhrparkFuelLog'
                  total:
                    type: integer
        "401": { $ref: "#/components/responses/Unauthorized" }
    post:
      summary: Add a fuel log entry for a vehicle
      tags: [fuhrpark]
      operationId: createVehicleFuelLog
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [date, liters]
              properties:
                date: { type: string, format: date }
                liters: { type: number, format: float }
                cost_cents: { type: integer, format: int64 }
                mileage_km: { type: integer, format: int64 }
                fuel_type: { type: string, enum: [diesel, petrol, electric, hybrid, other] }
                notes: { type: string }
      responses:
        "201":
          description: Fuel log created
          content:
            application/json:
              schema:
                type: object
                properties:
                  fuel_log:
                    $ref: '#/components/schemas/FuhrparkFuelLog'
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/fuhrpark/vehicles/{id}/trip-logs:
    get:
      summary: List trip logs for a vehicle
      tags: [fuhrpark]
      operationId: listVehicleTripLogs
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: page, in: query, schema: { type: integer, minimum: 1, default: 1 } }
        - { name: page_size, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 50 } }
      responses:
        "200":
          description: Trip log list
          content:
            application/json:
              schema:
                type: object
                properties:
                  trip_logs:
                    type: array
                    items:
                      $ref: '#/components/schemas/FuhrparkTripLog'
                  total:
                    type: integer
        "401": { $ref: "#/components/responses/Unauthorized" }
    post:
      summary: Add a trip log entry for a vehicle
      tags: [fuhrpark]
      operationId: createVehicleTripLog
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [date, start_location, end_location, purpose, driver_name]
              properties:
                date: { type: string, format: date }
                start_location: { type: string }
                end_location: { type: string }
                purpose: { type: string }
                start_km: { type: integer, format: int64 }
                end_km: { type: integer, format: int64 }
                is_private: { type: boolean }
                driver_name: { type: string }
                notes: { type: string }
      responses:
        "201":
          description: Trip log created
          content:
            application/json:
              schema:
                type: object
                properties:
                  trip_log:
                    $ref: '#/components/schemas/FuhrparkTripLog'
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/fuhrpark/vehicles/{id}/documents:
    get:
      summary: List documents for a vehicle
      tags: [fuhrpark]
      operationId: listVehicleDocuments
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: page, in: query, schema: { type: integer, minimum: 1, default: 1 } }
        - { name: page_size, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 50 } }
      responses:
        "200":
          description: Document list
          content:
            application/json:
              schema:
                type: object
                properties:
                  documents:
                    type: array
                    items:
                      $ref: '#/components/schemas/FuhrparkDocument'
                  total:
                    type: integer
        "401": { $ref: "#/components/responses/Unauthorized" }
    post:
      summary: Attach a document to a vehicle
      tags: [fuhrpark]
      operationId: createVehicleDocument
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [doc_type, name, object_key]
              properties:
                doc_type: { type: string, enum: [registration, insurance, tuev, other] }
                name: { type: string }
                object_key: { type: string }
                expiry_date: { type: string, format: date }
      responses:
        "201":
          description: Document attached
          content:
            application/json:
              schema:
                type: object
                properties:
                  document:
                    $ref: '#/components/schemas/FuhrparkDocument'
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/fuhrpark/services:
    get:
      summary: List all vehicle services (tenant-wide)
      tags: [fuhrpark]
      operationId: listAllVehicleServices
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: status, in: query, schema: { type: string } }
        - { name: vehicle_id, in: query, schema: { type: string, format: uuid } }
        - { name: page, in: query, schema: { type: integer, minimum: 1, default: 1 } }
        - { name: page_size, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 50 } }
      responses:
        "200":
          description: Service list
          content:
            application/json:
              schema:
                type: object
                properties:
                  services:
                    type: array
                    items:
                      $ref: '#/components/schemas/FuhrparkService'
                  total:
                    type: integer
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/fuhrpark/services/upcoming:
    get:
      summary: List upcoming vehicle services
      tags: [fuhrpark]
      operationId: listUpcomingVehicleServices
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: page, in: query, schema: { type: integer, minimum: 1, default: 1 } }
        - { name: page_size, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 50 } }
      responses:
        "200":
          description: Upcoming service list
          content:
            application/json:
              schema:
                type: object
                properties:
                  services:
                    type: array
                    items:
                      $ref: '#/components/schemas/FuhrparkService'
                  total:
                    type: integer
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/fuhrpark/services/{id}:
    patch:
      summary: Update a vehicle service record
      tags: [fuhrpark]
      operationId: updateVehicleService
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                service_type: { type: string }
                description: { type: string }
                scheduled_at: { type: string }
                cost_cents: { type: integer, format: int64 }
                workshop: { type: string }
                mileage_km: { type: integer, format: int64 }
                status: { type: string }
                notes: { type: string }
      responses:
        "200":
          description: Service updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  service:
                    $ref: '#/components/schemas/FuhrparkService'
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      summary: Delete a vehicle service record
      tags: [fuhrpark]
      operationId: deleteVehicleService
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "204":
          description: Deleted
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/fuhrpark/services/{id}/complete:
    post:
      summary: Mark a vehicle service as completed
      tags: [fuhrpark]
      operationId: completeVehicleService
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                cost_cents: { type: integer, format: int64 }
                mileage_km: { type: integer, format: int64 }
                notes: { type: string }
      responses:
        "200":
          description: Service completed
          content:
            application/json:
              schema:
                type: object
                properties:
                  service:
                    $ref: '#/components/schemas/FuhrparkService'
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/fuhrpark/damages:
    get:
      summary: List all damage reports (tenant-wide)
      tags: [fuhrpark]
      operationId: listAllVehicleDamages
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: status, in: query, schema: { type: string } }
        - { name: vehicle_id, in: query, schema: { type: string, format: uuid } }
        - { name: page, in: query, schema: { type: integer, minimum: 1, default: 1 } }
        - { name: page_size, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 50 } }
      responses:
        "200":
          description: Damage list
          content:
            application/json:
              schema:
                type: object
                properties:
                  damages:
                    type: array
                    items:
                      $ref: '#/components/schemas/FuhrparkDamage'
                  total:
                    type: integer
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/fuhrpark/damages/{id}:
    patch:
      summary: Update a damage report
      tags: [fuhrpark]
      operationId: updateVehicleDamage
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                description: { type: string }
                severity: { type: string, enum: [minor, moderate, major, totalled] }
                status: { type: string }
                photo_keys: { type: array, items: { type: string } }
                cost_cents: { type: integer, format: int64 }
                notes: { type: string }
      responses:
        "200":
          description: Damage updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  damage:
                    $ref: '#/components/schemas/FuhrparkDamage'
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/fuhrpark/damages/{id}/resolve:
    post:
      summary: Mark a damage report as resolved
      tags: [fuhrpark]
      operationId: resolveVehicleDamage
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                resolved_by: { type: string, format: uuid }
                cost_cents: { type: integer, format: int64 }
                notes: { type: string }
      responses:
        "200":
          description: Damage resolved
          content:
            application/json:
              schema:
                type: object
                properties:
                  damage:
                    $ref: '#/components/schemas/FuhrparkDamage'
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/fuhrpark/fuel-logs:
    get:
      summary: List fuel logs (tenant-wide)
      tags: [fuhrpark]
      operationId: listAllFuelLogs
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: vehicle_id, in: query, schema: { type: string, format: uuid } }
        - { name: page, in: query, schema: { type: integer, minimum: 1, default: 1 } }
        - { name: page_size, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 50 } }
      responses:
        "200":
          description: Fuel log list
          content:
            application/json:
              schema:
                type: object
                properties:
                  fuel_logs:
                    type: array
                    items:
                      $ref: '#/components/schemas/FuhrparkFuelLog'
                  total:
                    type: integer
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/fuhrpark/fuel-logs/{id}:
    patch:
      summary: Update a fuel log entry
      tags: [fuhrpark]
      operationId: updateFuelLog
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                date: { type: string, format: date }
                liters: { type: number, format: float }
                cost_cents: { type: integer, format: int64 }
                mileage_km: { type: integer, format: int64 }
                fuel_type: { type: string }
                notes: { type: string }
      responses:
        "200":
          description: Fuel log updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  fuel_log:
                    $ref: '#/components/schemas/FuhrparkFuelLog'
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      summary: Delete a fuel log entry
      tags: [fuhrpark]
      operationId: deleteFuelLog
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "204":
          description: Deleted
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/fuhrpark/trip-logs:
    get:
      summary: List trip logs (tenant-wide)
      tags: [fuhrpark]
      operationId: listAllTripLogs
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: vehicle_id, in: query, schema: { type: string, format: uuid } }
        - { name: page, in: query, schema: { type: integer, minimum: 1, default: 1 } }
        - { name: page_size, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 50 } }
      responses:
        "200":
          description: Trip log list
          content:
            application/json:
              schema:
                type: object
                properties:
                  trip_logs:
                    type: array
                    items:
                      $ref: '#/components/schemas/FuhrparkTripLog'
                  total:
                    type: integer
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/fuhrpark/trip-logs/{id}:
    patch:
      summary: Update a trip log entry
      tags: [fuhrpark]
      operationId: updateTripLog
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                date: { type: string, format: date }
                start_location: { type: string }
                end_location: { type: string }
                purpose: { type: string }
                start_km: { type: integer, format: int64 }
                end_km: { type: integer, format: int64 }
                is_private: { type: boolean }
                driver_name: { type: string }
                notes: { type: string }
      responses:
        "200":
          description: Trip log updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  trip_log:
                    $ref: '#/components/schemas/FuhrparkTripLog'
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      summary: Delete a trip log entry
      tags: [fuhrpark]
      operationId: deleteTripLog
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "204":
          description: Deleted
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/fuhrpark/documents/{id}:
    delete:
      summary: Delete a vehicle document
      tags: [fuhrpark]
      operationId: deleteVehicleDocument
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "204":
          description: Deleted
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/fuhrpark/driver-licenses:
    get:
      summary: List driver license checks (tenant-wide, optionally filtered by driver)
      tags: [fuhrpark]
      operationId: listDriverLicenses
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: driver_id, in: query, schema: { type: string, format: uuid } }
        - { name: page, in: query, schema: { type: integer, minimum: 1, default: 1 } }
        - { name: page_size, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 50 } }
      responses:
        "200":
          description: Driver license check list
          content:
            application/json:
              schema:
                type: object
                properties:
                  licenses:
                    type: array
                    items:
                      $ref: '#/components/schemas/FuhrparkDriverLicense'
                  total:
                    type: integer
        "401": { $ref: "#/components/responses/Unauthorized" }
    post:
      summary: Record a new driver license check
      tags: [fuhrpark]
      operationId: createDriverLicense
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [driver_id, license_classes, next_check_due_date]
              properties:
                driver_id: { type: string, format: uuid }
                license_classes: { type: array, items: { type: string } }
                expiry_date: { type: string, format: date }
                checked_at: { type: string, format: date }
                next_check_due_date: { type: string, format: date }
                notes: { type: string }
      responses:
        "201":
          description: Driver license check recorded
          content:
            application/json:
              schema:
                type: object
                properties:
                  license:
                    $ref: '#/components/schemas/FuhrparkDriverLicense'
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/fuhrpark/driver-licenses/{id}:
    patch:
      summary: Correct a driver license check entry
      tags: [fuhrpark]
      operationId: updateDriverLicense
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [license_classes, next_check_due_date]
              properties:
                license_classes: { type: array, items: { type: string } }
                expiry_date: { type: string, format: date }
                checked_at: { type: string, format: date }
                next_check_due_date: { type: string, format: date }
                notes: { type: string }
      responses:
        "200":
          description: Driver license check updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  license:
                    $ref: '#/components/schemas/FuhrparkDriverLicense'
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      summary: Delete a driver license check entry
      tags: [fuhrpark]
      operationId: deleteDriverLicense
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "204":
          description: Deleted
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/fuhrpark/gps/ingest:
    post:
      summary: Ingest GPS positions for a vehicle
      tags: [fuhrpark]
      operationId: ingestGpsPositions
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: vehicle_id, in: query, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [positions]
              properties:
                positions:
                  type: array
                  items:
                    type: object
                    required: [lat, lng, recorded_at]
                    properties:
                      lat: { type: number, format: double }
                      lng: { type: number, format: double }
                      speed_kmh: { type: number, format: double }
                      recorded_at: { type: string, format: date-time }
      responses:
        "200":
          description: Positions ingested
          content:
            application/json:
              schema:
                type: object
                properties:
                  ingested:
                    type: integer
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/fuhrpark/gps/routes:
    get:
      summary: Get vehicle routes from GPS data
      tags: [fuhrpark]
      operationId: getVehicleRoutes
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: vehicle_id, in: query, schema: { type: string, format: uuid } }
        - { name: date_from, in: query, schema: { type: string, format: date } }
        - { name: date_to, in: query, schema: { type: string, format: date } }
      responses:
        "200":
          description: Vehicle routes
          content:
            application/json:
              schema:
                type: object
                properties:
                  routes:
                    type: array
                    items:
                      $ref: '#/components/schemas/FuhrparkRoute'
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/fuhrpark/gps/positions:
    get:
      summary: Get GPS positions for a vehicle
      tags: [fuhrpark]
      operationId: getGpsPositions
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: vehicle_id, in: query, required: true, schema: { type: string, format: uuid } }
        - { name: from, in: query, schema: { type: string, format: date-time } }
        - { name: to, in: query, schema: { type: string, format: date-time } }
      responses:
        "200":
          description: GPS positions
          content:
            application/json:
              schema:
                type: object
                properties:
                  positions:
                    type: array
                    items:
                      $ref: '#/components/schemas/FuhrparkGpsPosition'
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/fuhrpark/tuev-due:
    get:
      summary: List vehicles with upcoming or overdue TUV inspections
      tags: [fuhrpark]
      operationId: checkVehiclesTuevDue
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: TUV-due vehicle list
          content:
            application/json:
              schema:
                type: object
                properties:
                  vehicles:
                    type: array
                    items:
                      $ref: '#/components/schemas/FuhrparkVehicle'
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/fuhrpark/export:
    get:
      summary: Export vehicle fleet report (CSV or PDF)
      tags: [fuhrpark]
      operationId: exportVehicleReport
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: format, in: query, schema: { type: string, enum: [csv, pdf], default: csv } }
      responses:
        "200":
          description: Report file
          content:
            application/octet-stream:
              schema: { type: string, format: binary }
        "401": { $ref: "#/components/responses/Unauthorized" }

  # ===========================================================================
  # Einkauf (Purchasing)
  # ===========================================================================

  /api/v1/einkauf/suppliers:
    get:
      summary: List suppliers
      tags: [einkauf]
      operationId: listSuppliers
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: search, in: query, schema: { type: string } }
        - { name: active_only, in: query, schema: { type: boolean } }
        - { name: page, in: query, schema: { type: integer, minimum: 1, default: 1 } }
        - { name: page_size, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 20 } }
      responses:
        "200":
          description: Supplier list
          content:
            application/json:
              schema: { type: object }
        "401": { $ref: "#/components/responses/Unauthorized" }
    post:
      summary: Create a supplier
      tags: [einkauf]
      operationId: createSupplier
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name: { type: string }
                contact_id: { type: string, format: uuid }
                email: { type: string, format: email }
                phone: { type: string }
                address: { type: string }
                payment_terms: { type: string }
                notes: { type: string }
      responses:
        "201":
          description: Supplier created
          content:
            application/json:
              schema: { type: object }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/einkauf/suppliers/{id}:
    get:
      summary: Get a supplier
      tags: [einkauf]
      operationId: getSupplier
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Supplier
          content:
            application/json:
              schema: { type: object }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      summary: Update a supplier
      tags: [einkauf]
      operationId: updateSupplier
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name: { type: string }
                contact_id: { type: string, format: uuid }
                email: { type: string, format: email }
                phone: { type: string }
                address: { type: string }
                payment_terms: { type: string }
                notes: { type: string }
      responses:
        "200":
          description: Supplier updated
          content:
            application/json:
              schema: { type: object }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      summary: Delete a supplier
      tags: [einkauf]
      operationId: deleteSupplier
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "204":
          description: Deleted
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/einkauf/pos:
    get:
      summary: List purchase orders
      tags: [einkauf]
      operationId: listPurchaseOrders
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: supplier_id, in: query, schema: { type: string, format: uuid } }
        - { name: status, in: query, schema: { type: string } }
        - { name: page, in: query, schema: { type: integer, minimum: 1, default: 1 } }
        - { name: page_size, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 20 } }
      responses:
        "200":
          description: Purchase order list
          content:
            application/json:
              schema: { type: object }
        "401": { $ref: "#/components/responses/Unauthorized" }
    post:
      summary: Create a purchase order
      tags: [einkauf]
      operationId: createPurchaseOrder
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [supplier_id, po_number]
              properties:
                supplier_id: { type: string, format: uuid }
                po_number: { type: string }
                order_date: { type: string, format: date }
                expected_delivery_date: { type: string, format: date }
                currency: { type: string, example: EUR }
                notes: { type: string }
      responses:
        "201":
          description: Purchase order created
          content:
            application/json:
              schema: { type: object }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/einkauf/pos/{id}:
    get:
      summary: Get a purchase order
      tags: [einkauf]
      operationId: getPurchaseOrder
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Purchase order
          content:
            application/json:
              schema: { type: object }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      summary: Update a purchase order
      tags: [einkauf]
      operationId: updatePurchaseOrder
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                supplier_id: { type: string, format: uuid }
                po_number: { type: string }
                order_date: { type: string, format: date }
                expected_delivery_date: { type: string, format: date }
                currency: { type: string }
                notes: { type: string }
      responses:
        "200":
          description: Purchase order updated
          content:
            application/json:
              schema: { type: object }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      summary: Delete a purchase order
      tags: [einkauf]
      operationId: deletePurchaseOrder
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "204":
          description: Deleted
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/einkauf/pos/{id}/submit:
    post:
      summary: Submit a purchase order to the supplier
      tags: [einkauf]
      operationId: submitPurchaseOrder
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Purchase order submitted
          content:
            application/json:
              schema: { type: object }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/einkauf/pos/{id}/cancel:
    post:
      summary: Cancel a purchase order
      description: >
        Cancels a purchase order that has not been received yet. Only orders in
        draft, submitted or sent state can be cancelled; any other state is
        rejected with 409.
      tags: [einkauf]
      operationId: cancelPurchaseOrder
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Purchase order cancelled
          content:
            application/json:
              schema: { type: object }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: Purchase order is not in a cancellable state
          content:
            application/json:
              schema: { type: object }

  /api/v1/einkauf/pos/{id}/receive:
    post:
      summary: Mark a purchase order as fully received
      tags: [einkauf]
      operationId: receivePurchaseOrderGoods
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Goods received
          content:
            application/json:
              schema: { type: object }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/einkauf/pos/{id}/partial-receive:
    post:
      summary: Record partial goods receipt for specific order lines
      tags: [einkauf]
      operationId: partialReceivePurchaseOrder
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [items]
              properties:
                items:
                  type: array
                  items:
                    type: object
                    properties:
                      line_id: { type: string, format: uuid }
                      received_quantity: { type: string }
      responses:
        "200":
          description: Partial receipt recorded
          content:
            application/json:
              schema: { type: object }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/einkauf/pos/{id}/lines:
    get:
      summary: List lines of a purchase order
      tags: [einkauf]
      operationId: listPurchaseOrderLines
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Line list
          content:
            application/json:
              schema: { type: object }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      summary: Add a line item to a purchase order
      tags: [einkauf]
      operationId: addPurchaseOrderLine
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [product_name, quantity, unit_price]
              properties:
                product_name: { type: string }
                sku: { type: string }
                quantity: { type: string }
                unit_price: { type: string }
                tax_rate: { type: string }
                line_position: { type: integer }
      responses:
        "201":
          description: Line added
          content:
            application/json:
              schema: { type: object }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/einkauf/pos/{id}/lines/{lineId}:
    patch:
      summary: Update a purchase order line
      tags: [einkauf]
      operationId: updatePurchaseOrderLine
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: lineId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                product_name: { type: string }
                sku: { type: string }
                quantity: { type: string }
                unit_price: { type: string }
                tax_rate: { type: string }
                line_position: { type: integer }
      responses:
        "200":
          description: Line updated
          content:
            application/json:
              schema: { type: object }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      summary: Delete a purchase order line
      tags: [einkauf]
      operationId: deletePurchaseOrderLine
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: lineId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "204":
          description: Deleted
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  # ===========================================================================
  # Einkauf - Extended (catalog, supplier ratings, framework contracts)
  # ===========================================================================

  /api/v1/einkauf/catalog:
    get:
      summary: List catalog items
      tags: [einkauf]
      operationId: listEinkaufCatalogItems
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: supplier_id, in: query, schema: { type: string, format: uuid } }
        - { name: category, in: query, schema: { type: string } }
        - { name: search, in: query, schema: { type: string } }
        - { name: available, in: query, schema: { type: boolean }, description: "Filter by availability" }
        - { name: page, in: query, schema: { type: integer, minimum: 1, default: 1 } }
        - { name: page_size, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 20 } }
      responses:
        "200":
          description: Catalog item list
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items: { $ref: "#/components/schemas/EinkaufCatalogItem" }
                  total: { type: integer }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      summary: Create a catalog item
      tags: [einkauf]
      operationId: createEinkaufCatalogItem
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [supplier_id, name, price]
              properties:
                supplier_id: { type: string, format: uuid }
                name: { type: string }
                sku: { type: string }
                category: { type: string }
                price: { type: string, description: "Numeric string (e.g. \"12.50\")" }
                currency: { type: string, example: EUR }
                unit: { type: string }
                available: { type: boolean }
                min_order_qty: { type: string, description: "Minimum order quantity as numeric string" }
      responses:
        "201":
          description: Catalog item created
          content:
            application/json:
              schema:
                type: object
                properties:
                  item: { $ref: "#/components/schemas/EinkaufCatalogItem" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/einkauf/catalog/{id}:
    get:
      summary: Get a catalog item
      tags: [einkauf]
      operationId: getEinkaufCatalogItem
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Catalog item
          content:
            application/json:
              schema:
                type: object
                properties:
                  item: { $ref: "#/components/schemas/EinkaufCatalogItem" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      summary: Update a catalog item
      tags: [einkauf]
      operationId: updateEinkaufCatalogItem
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name: { type: string }
                sku: { type: string }
                category: { type: string }
                price: { type: string, description: "Numeric string" }
                currency: { type: string }
                unit: { type: string }
                available: { type: boolean }
                min_order_qty: { type: string, description: "Numeric string" }
      responses:
        "200":
          description: Catalog item updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  item: { $ref: "#/components/schemas/EinkaufCatalogItem" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      summary: Delete a catalog item
      tags: [einkauf]
      operationId: deleteEinkaufCatalogItem
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "204":
          description: Deleted
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/einkauf/suppliers/{supplierId}/ratings:
    get:
      summary: List ratings for a supplier
      tags: [einkauf]
      operationId: listEinkaufSupplierRatings
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: supplierId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Supplier rating list
          content:
            application/json:
              schema:
                type: object
                properties:
                  ratings:
                    type: array
                    items: { $ref: "#/components/schemas/EinkaufSupplierRating" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      summary: Create a supplier rating
      tags: [einkauf]
      operationId: createEinkaufSupplierRating
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: supplierId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [category, rating]
              properties:
                category: { type: string, enum: [quality, delivery, price] }
                rating: { type: integer, minimum: 1, maximum: 5 }
                comment: { type: string }
      responses:
        "201":
          description: Rating created
          content:
            application/json:
              schema:
                type: object
                properties:
                  rating: { $ref: "#/components/schemas/EinkaufSupplierRating" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/einkauf/suppliers/{supplierId}/ratings/{ratingId}:
    delete:
      summary: Delete a supplier rating
      tags: [einkauf]
      operationId: deleteEinkaufSupplierRating
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: supplierId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: ratingId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "204":
          description: Deleted
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/einkauf/contracts:
    get:
      summary: List framework contracts
      tags: [einkauf]
      operationId: listEinkaufFrameworkContracts
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: supplier_id, in: query, schema: { type: string, format: uuid } }
        - { name: status, in: query, schema: { type: string, enum: [draft, active, expired] } }
        - { name: page, in: query, schema: { type: integer, minimum: 1, default: 1 } }
        - { name: page_size, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 20 } }
      responses:
        "200":
          description: Framework contract list
          content:
            application/json:
              schema:
                type: object
                properties:
                  contracts:
                    type: array
                    items: { $ref: "#/components/schemas/EinkaufFrameworkContract" }
                  total: { type: integer }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      summary: Create a framework contract
      tags: [einkauf]
      operationId: createEinkaufFrameworkContract
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [supplier_id, title]
              properties:
                supplier_id: { type: string, format: uuid }
                title: { type: string }
                contract_nr: { type: string }
                start_date: { type: string, format: date, description: "ISO 8601 date (YYYY-MM-DD)" }
                end_date: { type: string, format: date, description: "ISO 8601 date (YYYY-MM-DD)" }
                total_value: { type: string, description: "Budgeted value as numeric string (e.g. \"10000.00\")" }
                currency: { type: string, example: EUR }
                status: { type: string, enum: [draft, active, expired], default: draft }
      responses:
        "201":
          description: Framework contract created
          content:
            application/json:
              schema:
                type: object
                properties:
                  contract: { $ref: "#/components/schemas/EinkaufFrameworkContract" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/einkauf/contracts/{id}:
    get:
      summary: Get a framework contract (includes line items)
      tags: [einkauf]
      operationId: getEinkaufFrameworkContract
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Framework contract with line items populated
          content:
            application/json:
              schema:
                type: object
                properties:
                  contract: { $ref: "#/components/schemas/EinkaufFrameworkContract" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      summary: Update a framework contract
      tags: [einkauf]
      operationId: updateEinkaufFrameworkContract
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                supplier_id: { type: string, format: uuid }
                title: { type: string }
                contract_nr: { type: string }
                start_date: { type: string, format: date }
                end_date: { type: string, format: date }
                total_value: { type: string, description: "Numeric string" }
                currency: { type: string }
                status: { type: string, enum: [draft, active, expired] }
      responses:
        "200":
          description: Framework contract updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  contract: { $ref: "#/components/schemas/EinkaufFrameworkContract" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      summary: Delete a framework contract
      tags: [einkauf]
      operationId: deleteEinkaufFrameworkContract
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "204":
          description: Deleted
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/einkauf/contracts/{id}/items:
    post:
      summary: Add a line item to a framework contract
      tags: [einkauf]
      operationId: createEinkaufContractItem
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name: { type: string }
                unit_price: { type: string, description: "Numeric string" }
                unit: { type: string }
                agreed_qty: { type: string, description: "Agreed total quantity as numeric string" }
      responses:
        "201":
          description: Contract item added
          content:
            application/json:
              schema:
                type: object
                properties:
                  item: { $ref: "#/components/schemas/EinkaufContractItem" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/einkauf/contracts/{id}/items/{itemId}:
    patch:
      summary: Update a framework contract item
      tags: [einkauf]
      operationId: updateEinkaufContractItem
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: itemId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name: { type: string }
                unit_price: { type: string, description: "Numeric string" }
                unit: { type: string }
                agreed_qty: { type: string, description: "Numeric string" }
      responses:
        "200":
          description: Contract item updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  item: { $ref: "#/components/schemas/EinkaufContractItem" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      summary: Delete a framework contract item
      tags: [einkauf]
      operationId: deleteEinkaufContractItem
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: itemId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "204":
          description: Deleted
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/einkauf/contracts/{id}/calls:
    get:
      summary: List call-offs for a framework contract
      tags: [einkauf]
      operationId: listEinkaufContractCalls
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Contract call-off list
          content:
            application/json:
              schema:
                type: object
                properties:
                  calls:
                    type: array
                    items: { $ref: "#/components/schemas/EinkaufContractCall" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      summary: Create a call-off against a framework contract
      tags: [einkauf]
      operationId: createEinkaufContractCall
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [amount]
              properties:
                po_id: { type: string, format: uuid, description: "Optional linked purchase order" }
                amount: { type: string, description: "Called-off value as numeric string" }
                currency: { type: string, example: EUR }
                notes: { type: string }
      responses:
        "201":
          description: Contract call-off created
          content:
            application/json:
              schema:
                type: object
                properties:
                  call: { $ref: "#/components/schemas/EinkaufContractCall" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # ===========================================================================
  # Rapporte (Field Reports)
  # ===========================================================================

  /api/v1/rapporte/reports:
    get:
      summary: List work reports
      tags: [rapporte]
      operationId: listRapporteReports
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: search, in: query, schema: { type: string } }
        - { name: status, in: query, schema: { type: string, enum: [draft, submitted, approved, rejected] }, description: "Filter by report status" }
        - { name: author_id, in: query, schema: { type: string, format: uuid }, description: "Filter by author UUID" }
        - { name: page, in: query, schema: { type: integer, minimum: 1, default: 1 } }
        - { name: page_size, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 50 } }
      responses:
        "200":
          description: Paginated report list
          content:
            application/json:
              schema:
                type: object
                properties:
                  reports:
                    type: array
                    items: { $ref: "#/components/schemas/RapporteReport" }
                  total: { type: integer }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      summary: Create a work report
      tags: [rapporte]
      operationId: createRapporteReport
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [title, author_id]
              properties:
                title: { type: string }
                description: { type: string }
                author_id: { type: string, format: uuid }
                lat: { type: number, format: double, nullable: true }
                lon: { type: number, format: double, nullable: true }
                report_date: { type: string, description: "Date in YYYY-MM-DD format; defaults to today if omitted" }
      responses:
        "201":
          description: Report created
          content:
            application/json:
              schema:
                type: object
                properties:
                  report: { $ref: "#/components/schemas/RapporteReport" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/rapporte/reports/{id}:
    get:
      summary: Get a work report by ID
      tags: [rapporte]
      operationId: getRapporteReport
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Work report
          content:
            application/json:
              schema:
                type: object
                properties:
                  report: { $ref: "#/components/schemas/RapporteReport" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      summary: Update a work report
      tags: [rapporte]
      operationId: updateRapporteReport
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                title: { type: string }
                description: { type: string }
                lat: { type: number, format: double, nullable: true }
                lon: { type: number, format: double, nullable: true }
                report_date: { type: string, description: "YYYY-MM-DD" }
      responses:
        "200":
          description: Updated report
          content:
            application/json:
              schema:
                type: object
                properties:
                  report: { $ref: "#/components/schemas/RapporteReport" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      summary: Delete a work report
      tags: [rapporte]
      operationId: deleteRapporteReport
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "204":
          description: Report deleted
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/rapporte/reports/{id}/signature:
    put:
      summary: Save digital signature for a report
      tags: [rapporte]
      operationId: saveRapporteReportSignature
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [signature_data, signed_by]
              properties:
                signature_data:
                  type: string
                  description: "Data URI — data:image/png;base64,… or data:image/svg+xml;base64,…"
                signed_by:
                  type: string
                  description: "Name or identifier of the signer"
      responses:
        "200":
          description: Report with signature saved
          content:
            application/json:
              schema:
                type: object
                properties:
                  report: { $ref: "#/components/schemas/RapporteReport" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/rapporte/reports/{id}/submit:
    post:
      summary: Submit a draft report for approval
      tags: [rapporte]
      operationId: submitRapporteReport
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Report transitioned to submitted
          content:
            application/json:
              schema:
                type: object
                properties:
                  report: { $ref: "#/components/schemas/RapporteReport" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/rapporte/reports/{id}/approve:
    post:
      summary: Approve a submitted report
      description: "Requires rapporte:report/approve permission."
      tags: [rapporte]
      operationId: approveRapporteReport
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [reviewer_id]
              properties:
                reviewer_id: { type: string, format: uuid }
                review_note: { type: string }
      responses:
        "200":
          description: Report approved
          content:
            application/json:
              schema:
                type: object
                properties:
                  report: { $ref: "#/components/schemas/RapporteReport" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/rapporte/reports/{id}/reject:
    post:
      summary: Reject a submitted report
      description: "Requires rapporte:report/approve permission."
      tags: [rapporte]
      operationId: rejectRapporteReport
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [reviewer_id]
              properties:
                reviewer_id: { type: string, format: uuid }
                review_note: { type: string }
      responses:
        "200":
          description: Report rejected
          content:
            application/json:
              schema:
                type: object
                properties:
                  report: { $ref: "#/components/schemas/RapporteReport" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/rapporte/reports/{id}/lines:
    get:
      summary: List lines for a report
      tags: [rapporte]
      operationId: listRapporteLines
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid }, description: "Report ID" }
      responses:
        "200":
          description: Report lines (no pagination; all lines returned)
          content:
            application/json:
              schema:
                type: object
                properties:
                  lines:
                    type: array
                    items: { $ref: "#/components/schemas/RapporteLine" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      summary: Add a line to a report
      tags: [rapporte]
      operationId: addRapporteLine
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid }, description: "Report ID" }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [description]
              properties:
                description: { type: string }
                quantity: { type: number, format: double }
                unit: { type: string }
                notes: { type: string }
                position: { type: integer, format: int32 }
      responses:
        "201":
          description: Line added
          content:
            application/json:
              schema:
                type: object
                properties:
                  line: { $ref: "#/components/schemas/RapporteLine" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/rapporte/reports/{id}/attachments:
    get:
      summary: List attachments for a report
      tags: [rapporte]
      operationId: listRapporteAttachments
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid }, description: "Report ID" }
        - { name: line_id, in: query, schema: { type: string, format: uuid }, description: "Filter by line UUID" }
      responses:
        "200":
          description: Attachment list
          content:
            application/json:
              schema:
                type: object
                properties:
                  attachments:
                    type: array
                    items: { $ref: "#/components/schemas/RapporteAttachment" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      summary: Register an attachment for a report
      description: >-
        The caller must pre-upload the file to object storage and supply the
        resulting object_key. This endpoint only registers the metadata; it does
        not accept multipart uploads.
      tags: [rapporte]
      operationId: uploadRapporteAttachment
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid }, description: "Report ID" }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [filename, size_bytes, object_key, uploaded_by]
              properties:
                line_id: { type: string, format: uuid, nullable: true }
                filename: { type: string }
                content_type: { type: string }
                size_bytes: { type: integer, format: int64 }
                object_key: { type: string, description: "Pre-computed object storage key supplied by the caller" }
                uploaded_by: { type: string, format: uuid }
      responses:
        "201":
          description: Attachment registered
          content:
            application/json:
              schema:
                type: object
                properties:
                  attachment: { $ref: "#/components/schemas/RapporteAttachment" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/rapporte/reports/{id}/export/pdf:
    get:
      summary: Export a report as PDF
      tags: [rapporte]
      operationId: exportRapportePDF
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: PDF binary payload
          headers:
            Content-Disposition:
              schema: { type: string }
              description: "attachment; filename=\"<report-title>.pdf\""
          content:
            application/pdf:
              schema:
                type: string
                format: binary
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/rapporte/lines/{id}:
    patch:
      summary: Update a report line
      tags: [rapporte]
      operationId: updateRapporteLine
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid }, description: "Line ID" }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                description: { type: string }
                quantity: { type: number, format: double }
                unit: { type: string }
                notes: { type: string }
                position: { type: integer, format: int32 }
      responses:
        "200":
          description: Updated line
          content:
            application/json:
              schema:
                type: object
                properties:
                  line: { $ref: "#/components/schemas/RapporteLine" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      summary: Delete a report line
      tags: [rapporte]
      operationId: deleteRapporteLine
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid }, description: "Line ID" }
      responses:
        "204":
          description: Line deleted
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/rapporte/attachments/{id}:
    delete:
      summary: Delete an attachment
      tags: [rapporte]
      operationId: deleteRapporteAttachment
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid }, description: "Attachment ID" }
      responses:
        "204":
          description: Attachment deleted
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/rapporte/stats:
    get:
      summary: Get report statistics for the tenant
      tags: [rapporte]
      operationId: getRapporteReportStats
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Aggregate report counts
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RapporteReportStats"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/rapporte/pending:
    get:
      summary: List reports pending approval
      tags: [rapporte]
      operationId: listRapportePendingApprovals
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: page, in: query, schema: { type: integer, minimum: 1, default: 1 } }
        - { name: page_size, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 50 } }
      responses:
        "200":
          description: Paginated list of submitted reports awaiting review
          content:
            application/json:
              schema:
                type: object
                properties:
                  reports:
                    type: array
                    items: { $ref: "#/components/schemas/RapporteReport" }
                  total: { type: integer }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/rapporte/measurements:
    get:
      summary: List measurements
      tags: [rapporte]
      operationId: listRapporteMeasurements
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: report_id, in: query, schema: { type: string, format: uuid }, description: "Filter by report UUID" }
        - { name: page, in: query, schema: { type: integer, minimum: 1, default: 1 } }
        - { name: page_size, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 50 } }
      responses:
        "200":
          description: Paginated measurement list
          content:
            application/json:
              schema:
                type: object
                properties:
                  measurements:
                    type: array
                    items: { $ref: "#/components/schemas/RapporteMeasurement" }
                  total: { type: integer }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      summary: Create a measurement
      tags: [rapporte]
      operationId: createRapporteMeasurement
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [title]
              properties:
                report_id: { type: string, format: uuid, nullable: true }
                title: { type: string }
                location: { type: string }
                measured_by: { type: string }
                measured_at: { type: string }
                notes: { type: string }
      responses:
        "201":
          description: Measurement created
          content:
            application/json:
              schema:
                type: object
                properties:
                  measurement: { $ref: "#/components/schemas/RapporteMeasurement" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/rapporte/measurements/{id}:
    get:
      summary: Get a measurement by ID
      tags: [rapporte]
      operationId: getRapporteMeasurement
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Measurement with positions
          content:
            application/json:
              schema:
                type: object
                properties:
                  measurement: { $ref: "#/components/schemas/RapporteMeasurement" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    put:
      summary: Update a measurement
      tags: [rapporte]
      operationId: updateRapporteMeasurement
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [title]
              properties:
                title: { type: string }
                location: { type: string }
                measured_by: { type: string }
                measured_at: { type: string }
                notes: { type: string }
      responses:
        "200":
          description: Updated measurement
          content:
            application/json:
              schema:
                type: object
                properties:
                  measurement: { $ref: "#/components/schemas/RapporteMeasurement" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      summary: Delete a measurement
      tags: [rapporte]
      operationId: deleteRapporteMeasurement
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "204":
          description: Measurement deleted
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/rapporte/measurements/{id}/positions:
    post:
      summary: Add a position to a measurement
      tags: [rapporte]
      operationId: addRapporteMeasurementPosition
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid }, description: "Measurement ID" }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [description]
              properties:
                position_number: { type: integer, format: int32 }
                description: { type: string }
                unit: { type: string }
                quantity: { type: number, format: double }
                unit_price: { type: number, format: double }
      responses:
        "201":
          description: Position added
          content:
            application/json:
              schema:
                type: object
                properties:
                  position: { $ref: "#/components/schemas/RapporteMeasurementPosition" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/rapporte/measurements/positions/{pos_id}:
    delete:
      summary: Delete a measurement position
      tags: [rapporte]
      operationId: deleteRapporteMeasurementPosition
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: pos_id, in: path, required: true, schema: { type: string, format: uuid }, description: "Position ID" }
      responses:
        "204":
          description: Position deleted
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/rapporte/templates:
    get:
      summary: List report templates
      tags: [rapporte]
      operationId: listRapporteTemplates
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: active_only, in: query, schema: { type: boolean, default: false }, description: "Return only active templates" }
        - { name: page, in: query, schema: { type: integer, minimum: 1, default: 1 } }
        - { name: page_size, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 50 } }
      responses:
        "200":
          description: Paginated template list
          content:
            application/json:
              schema:
                type: object
                properties:
                  templates:
                    type: array
                    items: { $ref: "#/components/schemas/RapporteTemplate" }
                  total: { type: integer }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      summary: Create a report template
      tags: [rapporte]
      operationId: createRapporteTemplate
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name: { type: string }
                description: { type: string }
                category: { type: string }
                default_lines_json: { type: string, description: "JSON string describing default lines to pre-populate on report creation" }
      responses:
        "201":
          description: Template created
          content:
            application/json:
              schema:
                type: object
                properties:
                  template: { $ref: "#/components/schemas/RapporteTemplate" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/rapporte/templates/{id}:
    get:
      summary: Get a report template by ID
      tags: [rapporte]
      operationId: getRapporteTemplate
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Report template
          content:
            application/json:
              schema:
                type: object
                properties:
                  template: { $ref: "#/components/schemas/RapporteTemplate" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    put:
      summary: Update a report template
      tags: [rapporte]
      operationId: updateRapporteTemplate
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name: { type: string }
                description: { type: string }
                category: { type: string }
                default_lines_json: { type: string }
                is_active: { type: boolean }
      responses:
        "200":
          description: Updated template
          content:
            application/json:
              schema:
                type: object
                properties:
                  template: { $ref: "#/components/schemas/RapporteTemplate" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      summary: Delete a report template
      tags: [rapporte]
      operationId: deleteRapporteTemplate
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "204":
          description: Template deleted
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }


  # --------------------------------------------------------------------------
  # Sessions
  # --------------------------------------------------------------------------

  /api/v1/auth/sessions:
    get:
      summary: List own active sessions
      tags: [auth]
      operationId: listMySessions
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Session list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListSessionsResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
    delete:
      summary: Terminate all own sessions
      tags: [auth]
      operationId: terminateAllSessions
      security:
        - bearerAuth: []
      parameters:
        - name: current_session_id
          in: query
          description: Keep this session alive (pass current session UUID to avoid self-logout)
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: All sessions terminated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StatusResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/auth/sessions/{id}:
    delete:
      summary: Terminate a specific session
      description: >
        Signs out one of the caller's own devices and revokes the refresh token
        that device holds. A session belonging to another user answers 404 —
        the same as one that does not exist, so an id cannot be probed.
      tags: [auth]
      operationId: terminateSession
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Session terminated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StatusResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          description: No such session, or it belongs to another user

  /api/v1/auth/sessions/all:
    get:
      summary: List all sessions for a specific user (admin only)
      tags: [auth]
      operationId: listAllSessions
      security:
        - bearerAuth: []
      parameters:
        - name: user_id
          in: query
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Session list for the given user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListSessionsResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  # --------------------------------------------------------------------------
  # 2FA — user-facing
  # --------------------------------------------------------------------------

  /api/v1/auth/2fa/setup:
    post:
      summary: Initiate TOTP 2FA setup (returns QR secret)
      tags: [2fa]
      operationId: setup2FA
      security:
        - bearerAuth: []
      responses:
        "200":
          description: TOTP setup data
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TwoFactorSetupResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/auth/2fa/verify:
    post:
      summary: Confirm TOTP setup with a valid code
      tags: [2fa]
      operationId: verify2FA
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Verify2FARequest"
      responses:
        "200":
          description: 2FA enabled; returns recovery codes
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecoveryCodesResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/auth/2fa/disable:
    post:
      summary: Disable 2FA for the current user
      tags: [2fa]
      operationId: disable2FA
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Disable2FARequest"
      responses:
        "200":
          description: 2FA disabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StatusResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/auth/2fa/regenerate-codes:
    post:
      summary: Regenerate 2FA recovery codes
      tags: [2fa]
      operationId: regenerateRecoveryCodes
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RegenerateCodesRequest"
      responses:
        "200":
          description: New recovery codes
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecoveryCodesResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  # --------------------------------------------------------------------------
  # 2FA — admin policy
  # --------------------------------------------------------------------------

  /api/v1/auth/2fa/policy:
    get:
      summary: Get 2FA enforcement policy (admin only)
      tags: [2fa]
      operationId: getTwoFactorPolicy
      security:
        - bearerAuth: []
      parameters:
        - name: role_name
          in: query
          description: Filter by role (omit to get all roles)
          schema:
            type: string
            enum: [admin, manager, member]
      responses:
        "200":
          description: 2FA policy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TwoFactorPolicyResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    put:
      summary: Update 2FA enforcement policy for a role (admin only)
      tags: [2fa]
      operationId: updateTwoFactorPolicy
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateTwoFactorPolicyRequest"
      responses:
        "200":
          description: Updated policy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TwoFactorPolicyResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /api/v1/auth/2fa/admin-reset:
    post:
      summary: Admin reset of 2FA for a specific user
      tags: [2fa]
      operationId: adminReset2FA
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdminReset2FARequest"
      responses:
        "200":
          description: 2FA reset
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StatusResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  # --------------------------------------------------------------------------
  # Audit log
  # --------------------------------------------------------------------------

  /api/v1/security/audit:
    get:
      summary: List audit log entries (admin only)
      tags: [security]
      operationId: listAuditEntries
      security:
        - bearerAuth: []
      parameters:
        - name: action
          in: query
          schema:
            type: string
        - name: result
          in: query
          schema:
            type: string
        - name: user_id
          in: query
          schema:
            type: string
            format: uuid
        - name: page
          in: query
          schema:
            type: integer
            minimum: 1
            default: 1
        - name: page_size
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 50
      responses:
        "200":
          description: Paginated audit entries
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListAuditEntriesResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /api/v1/security/audit/export:
    get:
      summary: Export audit log as CSV or JSON (admin only)
      tags: [security]
      operationId: exportAuditLog
      security:
        - bearerAuth: []
      parameters:
        - name: format
          in: query
          description: Export format (csv or json)
          schema:
            type: string
            enum: [csv, json]
            default: csv
        - name: action
          in: query
          schema:
            type: string
        - name: result
          in: query
          schema:
            type: string
        - name: user_id
          in: query
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Audit log file download
          content:
            text/csv:
              schema:
                type: string
                format: binary
            application/json:
              schema:
                type: string
                format: binary
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /api/v1/security/audit/verify:
    post:
      summary: Verify tamper-evidence chain of audit log (admin only)
      tags: [security]
      operationId: verifyAuditChain
      security:
        - bearerAuth: []
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              description: Optional range; omit to verify the full log
              properties:
                from_sequence:
                  type: integer
                  format: int64
                to_sequence:
                  type: integer
                  format: int64
      responses:
        "200":
          description: Chain verification result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VerifyAuditChainResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  # --------------------------------------------------------------------------
  # Vault
  # --------------------------------------------------------------------------

  /api/v1/security/vault:
    get:
      summary: List vault secrets (metadata only, no plaintext) (admin only)
      tags: [security]
      operationId: listVaultSecrets
      security:
        - bearerAuth: []
      responses:
        "200":
          description: List of vault secret metadata
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListVaultSecretsResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    put:
      summary: Create or update a vault secret (admin only)
      tags: [security]
      operationId: setVaultSecret
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SetVaultSecretRequest"
      responses:
        "200":
          description: Secret stored; metadata returned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VaultSecretResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /api/v1/security/vault/{keyName}:
    get:
      summary: Get a vault secret (incl. decrypted value) (admin only)
      tags: [security]
      operationId: getVaultSecret
      security:
        - bearerAuth: []
      parameters:
        - name: keyName
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Secret metadata and decrypted value
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VaultSecretValueResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      summary: Delete a vault secret (admin only)
      tags: [security]
      operationId: deleteVaultSecret
      security:
        - bearerAuth: []
      parameters:
        - name: keyName
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Secret deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StatusResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  # --------------------------------------------------------------------------
  # GDPR — data export
  # --------------------------------------------------------------------------

  /api/v1/security/gdpr/export/request:
    post:
      summary: Request a personal data export (GDPR Art. 20)
      tags: [security]
      operationId: requestGDPRExport
      security:
        - bearerAuth: []
      responses:
        "201":
          description: Export request created; status pending
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GDPRExportRequestResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/security/gdpr/exports:
    get:
      summary: List GDPR export requests (own or all for admin)
      tags: [security]
      operationId: listGDPRExports
      security:
        - bearerAuth: []
      parameters:
        - name: status
          in: query
          description: Filter by status (pending, approved, denied, ready, expired)
          schema:
            type: string
        - name: user_id
          in: query
          description: Filter by user (admin only; defaults to caller's own)
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: List of export requests
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListGDPRExportsResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/security/gdpr/export/{id}/approve:
    post:
      summary: Approve a GDPR export request (admin only)
      tags: [security]
      operationId: approveGDPRExport
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReviewExportRequest"
      responses:
        "200":
          description: Export request approved
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GDPRExportRequestResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/security/gdpr/export/{id}/deny:
    post:
      summary: Deny a GDPR export request (admin only)
      tags: [security]
      operationId: denyGDPRExport
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReviewExportRequest"
      responses:
        "200":
          description: Export request denied
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GDPRExportRequestResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/security/gdpr/export/{id}/download:
    get:
      summary: Download approved GDPR export archive by one-time token
      tags: [security]
      operationId: downloadGDPRExport
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          description: One-time download token (not the export request ID)
          schema:
            type: string
      responses:
        "200":
          description: ZIP archive of personal data
          content:
            application/zip:
              schema:
                type: string
                format: binary
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  # --------------------------------------------------------------------------
  # GDPR — right to erasure
  # --------------------------------------------------------------------------

  /api/v1/security/gdpr/erasure/preview:
    post:
      summary: Preview what would be erased for a user (admin only)
      tags: [security]
      operationId: previewErasure
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ErasureUserRequest"
      responses:
        "200":
          description: Per-module erasure preview
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErasurePreviewResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /api/v1/security/gdpr/erasure/execute:
    post:
      summary: Execute GDPR erasure / anonymisation (admin only)
      tags: [security]
      operationId: executeErasure
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ExecuteErasureRequest"
      responses:
        "200":
          description: Erasure result with confirmation hash
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ExecuteErasureResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  # --------------------------------------------------------------------------
  # DSAR
  # --------------------------------------------------------------------------

  /api/v1/security/dsar/search:
    get:
      summary: Cross-module GDPR Art. 15 person search (admin only)
      tags: [security]
      operationId: dsarSearch
      security:
        - bearerAuth: []
      parameters:
        - name: q
          in: query
          required: true
          description: Search query — name, email, or company (min 2 characters)
          schema:
            type: string
            minLength: 2
      responses:
        "200":
          description: Persons with all their data across modules
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DSARSearchResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  # --------------------------------------------------------------------------
  # Password policy
  # --------------------------------------------------------------------------

  /api/v1/security/password/policy:
    get:
      summary: Get the tenant password policy
      tags: [security]
      operationId: getPasswordPolicy
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Current password policy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PasswordPolicyResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
    put:
      summary: Update the tenant password policy (admin only)
      tags: [security]
      operationId: updatePasswordPolicy
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdatePasswordPolicyRequest"
      responses:
        "200":
          description: Updated password policy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PasswordPolicyResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /api/v1/security/password/validate:
    post:
      summary: Validate a candidate password against the policy
      tags: [security]
      operationId: validatePassword
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ValidatePasswordRequest"
      responses:
        "200":
          description: Validation result with violations list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidatePasswordResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  # --------------------------------------------------------------------------
  # IP access rules
  # --------------------------------------------------------------------------

  /api/v1/security/ip-rules:
    get:
      summary: List IP access rules (admin only)
      tags: [security]
      operationId: listIPRules
      security:
        - bearerAuth: []
      parameters:
        - name: rule_type
          in: query
          description: Filter by rule type (allow or block)
          schema:
            type: string
            enum: [allow, block]
      responses:
        "200":
          description: List of IP rules
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListIPRulesResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      summary: Create an IP access rule (admin only)
      tags: [security]
      operationId: createIPRule
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateIPRuleRequest"
      responses:
        "201":
          description: IP rule created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IPRuleResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /api/v1/security/ip-rules/{id}:
    delete:
      summary: Delete an IP access rule (admin only)
      tags: [security]
      operationId: deleteIPRule
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: IP rule deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StatusResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"


  # --------------------------------------------------------------------------
  # Retention policies (DSGVO Art. 5(1)(e))
  # --------------------------------------------------------------------------

  /api/v1/security/retention-policies:
    get:
      summary: List retention policies (admin only)
      tags: [security]
      operationId: listRetentionPolicies
      security:
        - bearerAuth: []
      responses:
        "200":
          description: List of retention policies
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListRetentionPoliciesResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      summary: Create a retention policy (admin only)
      tags: [security]
      operationId: createRetentionPolicy
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateRetentionPolicyRequest"
      responses:
        "201":
          description: Retention policy created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RetentionPolicyResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /api/v1/security/retention-policies/{id}:
    put:
      summary: Update a retention policy (admin only)
      tags: [security]
      operationId: updateRetentionPolicy
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateRetentionPolicyRequest"
      responses:
        "200":
          description: Retention policy updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RetentionPolicyResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      summary: Delete a retention policy (admin only)
      tags: [security]
      operationId: deleteRetentionPolicy
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Retention policy deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StatusResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  # --------------------------------------------------------------------------
  # Vendor Access (RBAC R-5 B, GDAP-light v3). Own top-level prefix, not under
  # /api/v1/security -- matches the path the frontend already calls
  # (desktop/src/renderer/src/api/vendor-access.ts).
  # --------------------------------------------------------------------------

  /api/v1/vendor-access:
    get:
      summary: List vendor access requests for the caller's tenant
      tags: [security]
      operationId: listVendorAccessRequests
      security:
        - bearerAuth: []
      responses:
        "200":
          description: List of vendor access requests
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VendorAccessListResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /api/v1/vendor-access/{id}/approve:
    post:
      summary: Approve a vendor access request
      tags: [security]
      operationId: approveVendorAccessRequest
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ApproveVendorAccessRequest"
      responses:
        "200":
          description: Vendor access request approved
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VendorAccessRequestResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Request is not in a valid state for this operation
        "422":
          description: sensitive_ack required for a sensitive scope

  /api/v1/vendor-access/{id}/decline:
    post:
      summary: Decline a vendor access request
      tags: [security]
      operationId: declineVendorAccessRequest
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Vendor access request declined
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VendorAccessRequestResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Request is not in a valid state for this operation

  /api/v1/vendor-access/{id}/counter-propose:
    post:
      summary: Propose an alternative start date for a pending vendor access request
      tags: [security]
      operationId: counterProposeVendorAccessRequest
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CounterProposeVendorAccessRequest"
      responses:
        "200":
          description: Vendor access request counter-proposed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VendorAccessRequestResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Request is not in a valid state for this operation

  /api/v1/vendor-access/{id}/revoke:
    post:
      summary: Revoke an active vendor access request
      tags: [security]
      operationId: revokeVendorAccessRequest
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Vendor access request revoked
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VendorAccessRequestResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Request is not in a valid state for this operation

  # ---------------------------------------------------------------------------
  # Modul: formulare
  # ---------------------------------------------------------------------------

  /api/v1/formulare/schemas:
    get:
      summary: List form schemas
      tags: [formulare]
      operationId: listFormSchemas
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: search, in: query, schema: { type: string } }
        - { name: page, in: query, schema: { type: integer, minimum: 1, default: 1 } }
        - { name: page_size, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 20 } }
      responses:
        "200":
          description: Paginated form schema list
          content:
            application/json:
              schema:
                type: object
                properties:
                  schemas:
                    type: array
                    items: { $ref: "#/components/schemas/FormSchema" }
                  total: { type: integer }
        "401": { $ref: "#/components/responses/Unauthorized" }
    post:
      summary: Create a form schema
      tags: [formulare]
      operationId: createFormSchema
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [title]
              properties:
                title: { type: string }
                description: { type: string }
                fields: { type: string, format: byte, description: "JSON-array of FormField (base64-encoded bytes)" }
                is_template: { type: boolean }
                is_public: { type: boolean }
                page_count: { type: integer, format: int32 }
      responses:
        "201":
          description: Form schema created
          content:
            application/json:
              schema:
                type: object
                properties:
                  schema: { $ref: "#/components/schemas/FormSchema" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/formulare/schemas/{id}:
    get:
      summary: Get a form schema by ID
      tags: [formulare]
      operationId: getFormSchema
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Form schema
          content:
            application/json:
              schema:
                type: object
                properties:
                  schema: { $ref: "#/components/schemas/FormSchema" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      summary: Update a form schema
      tags: [formulare]
      operationId: updateFormSchema
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                title: { type: string }
                description: { type: string }
                fields: { type: string, format: byte, description: "JSON-array of FormField (base64-encoded bytes)" }
                is_template: { type: boolean }
                is_public: { type: boolean }
                page_count: { type: integer, format: int32 }
      responses:
        "200":
          description: Form schema updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  schema: { $ref: "#/components/schemas/FormSchema" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      summary: Delete a form schema
      tags: [formulare]
      operationId: deleteFormSchema
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "204":
          description: Form schema deleted
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/formulare/schemas/{id}/duplicate:
    post:
      summary: Duplicate a form schema
      tags: [formulare]
      operationId: duplicateFormSchema
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                new_title: { type: string }
      responses:
        "201":
          description: Duplicated form schema
          content:
            application/json:
              schema:
                type: object
                properties:
                  schema: { $ref: "#/components/schemas/FormSchema" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/formulare/schemas/{id}/stats:
    get:
      summary: Get submission statistics for a form schema
      tags: [formulare]
      operationId: getFormStats
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Form submission statistics
          content:
            application/json:
              schema:
                type: object
                properties:
                  stats: { $ref: "#/components/schemas/FormStats" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/formulare/schemas/{id}/submissions:
    get:
      summary: List submissions for a form schema
      tags: [formulare]
      operationId: listFormSubmissions
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: page, in: query, schema: { type: integer, minimum: 1, default: 1 } }
        - { name: page_size, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 20 } }
      responses:
        "200":
          description: Paginated submission list
          content:
            application/json:
              schema:
                type: object
                properties:
                  submissions:
                    type: array
                    items: { $ref: "#/components/schemas/FormSubmission" }
                  total: { type: integer }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      summary: Create a submission for a form schema
      tags: [formulare]
      operationId: createFormSubmission
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                answers: { type: string, format: byte, description: "JSON-object of field answers (base64-encoded bytes)" }
                submitted_by: { type: string }
                ip_address: { type: string }
      responses:
        "201":
          description: Submission created
          content:
            application/json:
              schema:
                type: object
                properties:
                  submission: { $ref: "#/components/schemas/FormSubmission" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/formulare/schemas/{id}/submissions/export:
    get:
      summary: Export submissions for a form schema as CSV or XLSX
      tags: [formulare]
      operationId: exportFormSubmissions
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: format, in: query, schema: { type: string, enum: [csv, xlsx], default: csv } }
      responses:
        "200":
          description: Submission export file
          content:
            text/csv:
              schema: { type: string, format: binary }
            application/vnd.openxmlformats-officedocument.spreadsheetml.sheet:
              schema: { type: string, format: binary }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/formulare/schemas/{id}/webhooks:
    get:
      summary: List webhooks for a form schema
      tags: [formulare]
      operationId: listFormWebhooks
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Webhook list
          content:
            application/json:
              schema:
                type: object
                properties:
                  webhooks:
                    type: array
                    items: { $ref: "#/components/schemas/FormWebhook" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      summary: Create a webhook for a form schema
      tags: [formulare]
      operationId: createFormWebhook
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [url]
              properties:
                url: { type: string, format: uri }
                secret: { type: string }
                events:
                  type: array
                  items: { type: string }
                active: { type: boolean }
      responses:
        "201":
          description: Webhook created
          content:
            application/json:
              schema:
                type: object
                properties:
                  webhook: { $ref: "#/components/schemas/FormWebhook" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/formulare/submissions/{id}:
    get:
      summary: Get a submission by ID
      tags: [formulare]
      operationId: getFormSubmission
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Form submission
          content:
            application/json:
              schema:
                type: object
                properties:
                  submission: { $ref: "#/components/schemas/FormSubmission" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      summary: Update submission status
      tags: [formulare]
      operationId: updateFormSubmissionStatus
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [status]
              properties:
                status: { type: string, enum: [new, read, archived] }
      responses:
        "200":
          description: Submission status updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  submission: { $ref: "#/components/schemas/FormSubmission" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/formulare/webhooks/{id}:
    get:
      summary: Get a webhook by ID
      tags: [formulare]
      operationId: getFormWebhook
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Webhook
          content:
            application/json:
              schema:
                type: object
                properties:
                  webhook: { $ref: "#/components/schemas/FormWebhook" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      summary: Update a webhook
      tags: [formulare]
      operationId: updateFormWebhook
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                url: { type: string, format: uri }
                secret: { type: string }
                events:
                  type: array
                  items: { type: string }
                events_set: { type: boolean, description: "true = replace the full events list" }
                active: { type: boolean }
      responses:
        "200":
          description: Webhook updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  webhook: { $ref: "#/components/schemas/FormWebhook" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      summary: Delete a webhook
      tags: [formulare]
      operationId: deleteFormWebhook
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "204":
          description: Webhook deleted
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/formulare/webhooks/{id}/deliveries:
    get:
      summary: List deliveries for a specific webhook
      tags: [formulare]
      operationId: listFormWebhookDeliveriesForWebhook
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: page, in: query, schema: { type: integer, minimum: 1, default: 1 } }
        - { name: page_size, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 20 } }
      responses:
        "200":
          description: Delivery list for webhook
          content:
            application/json:
              schema:
                type: object
                properties:
                  deliveries:
                    type: array
                    items: { $ref: "#/components/schemas/FormWebhookDelivery" }
                  total: { type: integer }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/formulare/deliveries:
    get:
      summary: List webhook deliveries (global, filterable)
      tags: [formulare]
      operationId: listFormWebhookDeliveries
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: webhook_id, in: query, schema: { type: string, format: uuid } }
        - { name: submission_id, in: query, schema: { type: string, format: uuid } }
        - { name: status, in: query, schema: { type: integer, description: "WebhookDeliveryStatus enum int (1=pending, 2=delivered, 3=failed, 4=dead)" } }
        - { name: page, in: query, schema: { type: integer, minimum: 1, default: 1 } }
        - { name: page_size, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 20 } }
      responses:
        "200":
          description: Paginated delivery list
          content:
            application/json:
              schema:
                type: object
                properties:
                  deliveries:
                    type: array
                    items: { $ref: "#/components/schemas/FormWebhookDelivery" }
                  total: { type: integer }
        "401": { $ref: "#/components/responses/Unauthorized" }

  # ---------------------------------------------------------------------------
  # Modul: vertraege
  # ---------------------------------------------------------------------------

  /api/v1/vertraege/contracts:
    get:
      summary: List contracts
      tags: [vertraege]
      operationId: listContracts
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: status, in: query, schema: { type: string, enum: [draft, active, expired, terminated] } }
        - { name: contract_type, in: query, schema: { type: string, enum: [rental, service, employment, nda, other] } }
        - { name: contact_id, in: query, schema: { type: string, format: uuid }, description: "Filter by party contact_id (Contact-360 view)" }
        - { name: page, in: query, schema: { type: integer, minimum: 1, default: 1 } }
        - { name: page_size, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 20 } }
      responses:
        "200":
          description: Paginated contract list
          content:
            application/json:
              schema:
                type: object
                properties:
                  contracts:
                    type: array
                    items: { $ref: "#/components/schemas/VertraegeContract" }
                  total: { type: integer }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
    post:
      summary: Create a contract
      tags: [vertraege]
      operationId: createContract
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [contract_number, title, contract_type]
              properties:
                contract_number: { type: string }
                title: { type: string }
                contract_type: { type: string, enum: [rental, service, employment, nda, other] }
                starts_on: { type: string, description: "RFC3339 date string" }
                ends_on: { type: string, description: "RFC3339 date string (optional)" }
                document_url: { type: string, format: uri }
                notes: { type: string }
      responses:
        "201":
          description: Contract created
          content:
            application/json:
              schema:
                type: object
                properties:
                  contract: { $ref: "#/components/schemas/VertraegeContract" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/vertraege/contracts/{id}:
    get:
      summary: Get a contract by ID
      description: Returns the contract including nested parties and reminders.
      tags: [vertraege]
      operationId: getContract
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Contract (with parties and reminders)
          content:
            application/json:
              schema:
                type: object
                properties:
                  contract: { $ref: "#/components/schemas/VertraegeContract" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      summary: Update a contract
      tags: [vertraege]
      operationId: updateContract
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                title: { type: string }
                contract_type: { type: string, enum: [rental, service, employment, nda, other] }
                status: { type: string, enum: [draft, active, expired, terminated] }
                starts_on: { type: string, description: "RFC3339 date string" }
                ends_on: { type: string, description: "RFC3339 date string" }
                clear_ends_on: { type: boolean, description: "Set true to remove the ends_on date" }
                document_url: { type: string, format: uri }
                notes: { type: string }
      responses:
        "200":
          description: Contract updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  contract: { $ref: "#/components/schemas/VertraegeContract" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      summary: Delete a contract
      tags: [vertraege]
      operationId: deleteContract
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "204":
          description: Contract deleted
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/vertraege/contracts/{id}/export:
    get:
      summary: Export a contract (plain text; PDF in Sprint 3)
      tags: [vertraege]
      operationId: exportContract
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Contract export file
          content:
            text/plain:
              schema: { type: string, format: binary }
            application/pdf:
              schema: { type: string, format: binary }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/vertraege/contracts/{id}/signature:
    put:
      summary: Save a digital signature on a contract
      tags: [vertraege]
      operationId: saveContractSignature
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [signature_data, signed_by]
              properties:
                signature_data:
                  type: string
                  description: "data:image/png;base64,... or data:image/svg+xml;base64,..."
                signed_by:
                  type: string
                  description: Display name or user ID of signer
      responses:
        "200":
          description: Contract with signature saved
          content:
            application/json:
              schema:
                type: object
                properties:
                  contract: { $ref: "#/components/schemas/VertraegeContract" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/vertraege/contracts/{id}/parties:
    get:
      summary: List parties for a contract
      tags: [vertraege]
      operationId: listContractParties
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Party list
          content:
            application/json:
              schema:
                type: object
                properties:
                  parties:
                    type: array
                    items: { $ref: "#/components/schemas/VertraegeContractParty" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      summary: Add a party to a contract
      tags: [vertraege]
      operationId: addContractParty
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [party_type, role_in_contract]
              properties:
                party_type: { type: string, enum: [contact, company, external] }
                contact_id: { type: string, format: uuid }
                company_id: { type: string, format: uuid }
                external_name: { type: string }
                role_in_contract: { type: string }
                signed_on: { type: string, description: "RFC3339 datetime string" }
      responses:
        "201":
          description: Party added
          content:
            application/json:
              schema:
                type: object
                properties:
                  party: { $ref: "#/components/schemas/VertraegeContractParty" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/vertraege/contracts/{id}/parties/{partyId}:
    delete:
      summary: Remove a party from a contract
      tags: [vertraege]
      operationId: removeContractParty
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: partyId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "204":
          description: Party removed
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/vertraege/contracts/{id}/reminders:
    get:
      summary: List reminders for a contract
      tags: [vertraege]
      operationId: listContractReminders
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: only_pending, in: query, schema: { type: boolean, default: false } }
      responses:
        "200":
          description: Reminder list
          content:
            application/json:
              schema:
                type: object
                properties:
                  reminders:
                    type: array
                    items: { $ref: "#/components/schemas/VertraegeContractReminder" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      summary: Create a reminder for a contract
      tags: [vertraege]
      operationId: createContractReminder
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [subject]
              properties:
                remind_at: { type: string, description: "RFC3339 datetime string" }
                reminder_type: { type: string, enum: [renewal, expiry, payment, custom] }
                subject: { type: string }
                message: { type: string }
      responses:
        "201":
          description: Reminder created
          content:
            application/json:
              schema:
                type: object
                properties:
                  reminder: { $ref: "#/components/schemas/VertraegeContractReminder" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/vertraege/contracts/{id}/reminders/{reminderId}:
    patch:
      summary: Update a contract reminder
      tags: [vertraege]
      operationId: updateContractReminder
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: reminderId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                remind_at: { type: string, description: "RFC3339 datetime string" }
                reminder_type: { type: string, enum: [renewal, expiry, payment, custom] }
                subject: { type: string }
                message: { type: string }
                status: { type: string, enum: [pending, sent, cancelled] }
      responses:
        "200":
          description: Reminder updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  reminder: { $ref: "#/components/schemas/VertraegeContractReminder" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      summary: Delete a contract reminder
      tags: [vertraege]
      operationId: deleteContractReminder
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: reminderId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "204":
          description: Reminder deleted
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  # ---------------------------------------------------------------------------
  # Modul: mails
  # ---------------------------------------------------------------------------

  /api/v1/email/accounts:
    post:
      summary: Create an email account (IMAP/SMTP)
      tags: [mails]
      operationId: createEmailAccount
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateEmailAccountRequest"
      responses:
        "201":
          description: Account created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateEmailAccountResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
    get:
      summary: Get email account for a user
      tags: [mails]
      operationId: getEmailAccount
      security:
        - bearerAuth: []
      parameters:
        - name: user_id
          in: query
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Email account
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetEmailAccountResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/email/accounts/{id}:
    put:
      summary: Update an email account
      tags: [mails]
      operationId: updateEmailAccount
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateEmailAccountRequest"
      responses:
        "200":
          description: Account updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UpdateEmailAccountResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      summary: Delete an email account
      tags: [mails]
      operationId: deleteEmailAccount
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Account deleted
          content:
            application/json:
              schema:
                type: object
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/email/accounts/test:
    post:
      summary: Test IMAP/SMTP connection credentials
      tags: [mails]
      operationId: testEmailConnection
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TestEmailConnectionRequest"
      responses:
        "200":
          description: Connection test result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TestEmailConnectionResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/email/folders:
    get:
      summary: List IMAP folders for an account
      tags: [mails]
      operationId: listEmailFolders
      security:
        - bearerAuth: []
      parameters:
        - name: account_id
          in: query
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Folder list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListFoldersResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/email/folders/{id}:
    get:
      summary: Get a single folder
      tags: [mails]
      operationId: getEmailFolder
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Folder details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetFolderResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/email/folders/sync:
    post:
      summary: Sync folders from IMAP server
      tags: [mails]
      operationId: syncEmailFolders
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SyncFoldersRequest"
      responses:
        "200":
          description: Synced folder list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListFoldersResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/email/messages:
    get:
      summary: List messages in a folder (paginated)
      tags: [mails]
      operationId: listEmailMessages
      security:
        - bearerAuth: []
      parameters:
        - name: folder_id
          in: query
          required: true
          schema:
            type: string
            format: uuid
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: per_page
          in: query
          schema:
            type: integer
            default: 20
        - name: search
          in: query
          schema:
            type: string
        - name: sort_by
          in: query
          schema:
            type: string
            enum: [date, subject, from, size]
        - name: sort_desc
          in: query
          schema:
            type: boolean
            default: true
      responses:
        "200":
          description: Message list with total count
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmailListMessagesResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/email/messages/{id}:
    get:
      summary: Get a single message (with body and attachments)
      tags: [mails]
      operationId: getEmailMessage
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Message detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetMessageResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      summary: Delete a message
      tags: [mails]
      operationId: deleteEmailMessage
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Message deleted
          content:
            application/json:
              schema:
                type: object
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/email/messages/thread/{threadId}:
    get:
      summary: Get all messages in a thread
      tags: [mails]
      operationId: getThreadMessages
      security:
        - bearerAuth: []
      parameters:
        - name: threadId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Thread messages
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetThreadMessagesResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/email/messages/{id}/read:
    post:
      summary: Mark a message as read
      tags: [mails]
      operationId: markEmailRead
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Marked as read
          content:
            application/json:
              schema:
                type: object
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/email/messages/{id}/unread:
    post:
      summary: Mark a message as unread
      tags: [mails]
      operationId: markEmailUnread
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Marked as unread
          content:
            application/json:
              schema:
                type: object
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/email/messages/{id}/star:
    post:
      summary: Toggle star on a message
      tags: [mails]
      operationId: toggleEmailStar
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Star toggled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ToggleStarResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/email/messages/{id}/move:
    post:
      summary: Move a message to a different folder
      tags: [mails]
      operationId: moveEmailToFolder
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MoveToFolderRequest"
      responses:
        "200":
          description: Message moved
          content:
            application/json:
              schema:
                type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/email/messages/{id}/labels:
    post:
      summary: Replace the label set of a message
      description: >
        Full replace, not an add/remove pair: the given label ids become the
        message's entire label set. Every id must already be a label of the
        tenant.
      tags: [mails]
      operationId: assignMessageLabels
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssignMessageLabelsRequest"
      responses:
        "200":
          description: Message with its updated label set
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetMessageResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          description: Message not found, or one of the label ids does not belong to the tenant

  /api/v1/email/messages/bulk:
    post:
      summary: Apply an action to many messages at once
      description: >-
        Requires email:write; action=delete additionally requires
        email:delete, matching the single-message DELETE route. Ids that
        don't exist or belong to another tenant are silently excluded from
        the result, not errored. archive/spam resolve the destination folder
        per message from that message's own account.
      tags: [mails]
      operationId: bulkEmailMessageAction
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BulkMessageActionRequest"
      responses:
        "200":
          description: Bulk action applied
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BulkMessageActionResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          description: Unknown action

  /api/v1/email/send:
    post:
      summary: Send an email (with optional UWG §7 consent check when contact_id is set)
      tags: [mails]
      operationId: sendEmail
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SendEmailRequest"
      responses:
        "201":
          description: Email sent; message record returned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SendEmailResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /api/v1/email/send/draft:
    post:
      summary: Save or update a draft
      tags: [mails]
      operationId: saveEmailDraft
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SaveDraftRequest"
      responses:
        "201":
          description: Draft saved
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SaveDraftResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/email/send/reply:
    post:
      summary: Reply to a message
      tags: [mails]
      operationId: replyEmail
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReplyEmailRequest"
      responses:
        "201":
          description: Reply sent
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReplyEmailResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/email/send/forward:
    post:
      summary: Forward a message
      tags: [mails]
      operationId: forwardEmail
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ForwardEmailRequest"
      responses:
        "201":
          description: Forward sent
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ForwardEmailResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/email/signatures:
    get:
      summary: List signatures for a user
      tags: [mails]
      operationId: listEmailSignatures
      security:
        - bearerAuth: []
      parameters:
        - name: user_id
          in: query
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Signature list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListSignaturesResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
    post:
      summary: Create a signature
      tags: [mails]
      operationId: createEmailSignature
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateSignatureRequest"
      responses:
        "201":
          description: Signature created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateSignatureResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/email/signatures/{id}:
    get:
      summary: Get a signature
      tags: [mails]
      operationId: getEmailSignature
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Signature detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetSignatureResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    put:
      summary: Update a signature
      tags: [mails]
      operationId: updateEmailSignature
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateSignatureRequest"
      responses:
        "200":
          description: Signature updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UpdateSignatureResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      summary: Delete a signature
      tags: [mails]
      operationId: deleteEmailSignature
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Signature deleted
          content:
            application/json:
              schema:
                type: object
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/email/signatures/{id}/default:
    post:
      summary: Set a signature as the default for a user
      tags: [mails]
      operationId: setDefaultEmailSignature
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SetDefaultSignatureRequest"
      responses:
        "200":
          description: Default signature set
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SetDefaultSignatureResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/email/rules:
    get:
      summary: List the tenant's email rules
      tags: [mails]
      operationId: listEmailRules
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Rule list in application order (oldest first)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListEmailRulesResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      summary: Create an email rule
      tags: [mails]
      operationId: createEmailRule
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateEmailRuleRequest"
      responses:
        "201":
          description: Rule created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmailRuleEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          description: Move action target is not a folder of this tenant

  /api/v1/email/rules/{id}:
    patch:
      summary: Update an email rule
      description: >
        Partial update: omitted fields keep their stored value. The merged rule
        is validated as a whole, so a patch cannot produce a combination a
        create would have rejected.
      tags: [mails]
      operationId: updateEmailRule
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateEmailRuleRequest"
      responses:
        "200":
          description: Rule updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmailRuleEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      summary: Delete an email rule
      tags: [mails]
      operationId: deleteEmailRule
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Rule deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/email/rules/apply:
    post:
      summary: Apply all rules to the tenant's stored messages
      description: >
        Runs every rule over the tenant's non-trashed messages, newest first,
        capped at 2000 messages per run. Rules apply in creation order and
        accumulate: labels add up, a later move overrides an earlier one.
      tags: [mails]
      operationId: applyEmailRules
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Apply result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApplyEmailRulesResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /api/v1/email/labels:
    get:
      summary: List the tenant's email labels
      tags: [mails]
      operationId: listEmailLabels
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Label list, name-ordered
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListEmailLabelsResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      summary: Create an email label
      tags: [mails]
      operationId: createEmailLabel
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateEmailLabelRequest"
      responses:
        "201":
          description: Label created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmailLabelEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: A label with this name already exists for the tenant

  /api/v1/email/labels/{id}:
    patch:
      summary: Update an email label
      description: "Partial update: omitted fields keep their stored value."
      tags: [mails]
      operationId: updateEmailLabel
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateEmailLabelRequest"
      responses:
        "200":
          description: Label updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmailLabelEnvelope"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: A label with this name already exists for the tenant
    delete:
      summary: Delete an email label
      description: >
        Also strips the label id from every message of the tenant that still
        carries it (label_ids has no foreign key to do that on its own).
      tags: [mails]
      operationId: deleteEmailLabel
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Label deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/email/links/message/{messageId}:
    get:
      summary: Get CRM contact links for a message
      tags: [mails]
      operationId: getEmailContactLinks
      security:
        - bearerAuth: []
      parameters:
        - name: messageId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Contact links for the message
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetEmailContactLinksResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/email/links:
    post:
      summary: Link a message to a CRM contact
      tags: [mails]
      operationId: linkEmailToContact
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LinkEmailToContactRequest"
      responses:
        "201":
          description: Link created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LinkEmailToContactResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
    delete:
      summary: Unlink a message from a CRM contact
      tags: [mails]
      operationId: unlinkEmailFromContact
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UnlinkEmailFromContactRequest"
      responses:
        "200":
          description: Link removed
          content:
            application/json:
              schema:
                type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/email/links/contact/{contactId}:
    get:
      summary: Get emails linked to a CRM contact (paginated)
      tags: [mails]
      operationId: getContactEmails
      security:
        - bearerAuth: []
      parameters:
        - name: contactId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: per_page
          in: query
          schema:
            type: integer
            default: 20
      responses:
        "200":
          description: Emails linked to the contact
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetContactEmailsResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/email/sync/trigger:
    post:
      summary: Trigger IMAP sync for an account
      tags: [mails]
      operationId: triggerEmailSync
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TriggerSyncRequest"
      responses:
        "200":
          description: Sync triggered or already running
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TriggerSyncResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/email/sync/status:
    get:
      summary: Get sync status for an account
      tags: [mails]
      operationId: getEmailSyncStatus
      security:
        - bearerAuth: []
      parameters:
        - name: account_id
          in: query
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Sync status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetSyncStatusResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/email/sync/read-flag:
    post:
      summary: Propagate read/unread flag back to IMAP server
      tags: [mails]
      operationId: setEmailReadFlag
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SetReadFlagRequest"
      responses:
        "200":
          description: Flag set
          content:
            application/json:
              schema:
                type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/email/attachments/upload:
    post:
      summary: Upload an attachment (multipart/form-data, max 50 MB)
      tags: [mails]
      operationId: uploadEmailAttachment
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file, account_id]
              properties:
                file:
                  type: string
                  format: binary
                account_id:
                  type: string
                  format: uuid
      responses:
        "201":
          description: Attachment uploaded; ID usable in send/draft requests
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UploadAttachmentResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/email/attachments/{id}/download:
    get:
      summary: Get a pre-signed download URL for an attachment
      tags: [mails]
      operationId: getEmailAttachmentDownloadURL
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Download URL and metadata
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetAttachmentDownloadURLResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/email/contacts/import/csv:
    post:
      summary: Import CRM contacts from a CSV file
      tags: [mails]
      operationId: importContactsCSV
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ImportContactsCSVRequest"
      responses:
        "200":
          description: Import result with counts and errors
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmailImportContactsResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/email/contacts/import/vcard:
    post:
      summary: Import CRM contacts from a vCard file
      tags: [mails]
      operationId: importContactsVCard
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ImportContactsVCardRequest"
      responses:
        "200":
          description: Import result with counts and errors
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmailImportContactsResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/email/contacts/export/csv:
    post:
      summary: Export CRM contacts as CSV (file_content is base64-encoded bytes)
      tags: [mails]
      operationId: exportContactsCSV
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EmailExportContactsRequest"
      responses:
        "200":
          description: CSV file content (base64) and filename
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmailExportContactsResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/email/contacts/export/vcard:
    post:
      summary: Export CRM contacts as vCard (file_content is base64-encoded bytes)
      tags: [mails]
      operationId: exportContactsVCard
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EmailExportContactsRequest"
      responses:
        "200":
          description: vCard file content (base64) and filename
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmailExportContactsResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  # --- Dialer ---
  # All endpoints below use response.Proto (protojson, UseProtoNames → snake_case,
  # UseEnumNumbers → integer enums, EmitUnpopulated=false → optional fields omitted).
  # Timestamps are RFC3339 strings. GET /api/v1/dialer/supervisor and
  # GET /api/v1/dialer/contacts/{id}/calls are already specced above.

  /api/v1/dialer/campaigns:
    get:
      summary: List dialer campaigns
      tags: [dialer]
      operationId: listDialerCampaigns
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: page, in: query, schema: { type: integer, minimum: 1, default: 1 } }
        - { name: page_size, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 20 } }
        - name: status_filter
          in: query
          description: "Filter by CampaignStatus integer: 0=UNSPECIFIED 1=DRAFT 2=ACTIVE 3=PAUSED 4=COMPLETED 5=ARCHIVED"
          schema: { type: integer }
      responses:
        "200":
          description: Paginated campaign list
          content:
            application/json:
              schema:
                type: object
                properties:
                  campaigns:
                    type: array
                    items: { $ref: "#/components/schemas/DialerCampaign" }
                  total: { type: integer }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      summary: Create a campaign
      tags: [dialer]
      operationId: createDialerCampaign
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name, mode]
              properties:
                name: { type: string }
                description: { type: string }
                mode:
                  type: integer
                  description: "CampaignMode integer: 0=UNSPECIFIED 1=PREVIEW 2=POWER 3=PREDICTIVE"
                assigned_agent_ids:
                  type: array
                  items: { type: string, format: uuid }
                settings: { type: string, description: "JSON-encoded campaign settings (wrap-up timeout, retry rules)" }
      responses:
        "201":
          description: Campaign created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DialerCampaign" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/dialer/campaigns/{id}:
    get:
      summary: Get a campaign by ID
      tags: [dialer]
      operationId: getDialerCampaign
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Campaign
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DialerCampaign" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    put:
      summary: Update a campaign
      tags: [dialer]
      operationId: updateDialerCampaign
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name: { type: string }
                description: { type: string }
                assigned_agent_ids:
                  type: array
                  items: { type: string, format: uuid }
                settings: { type: string, description: "JSON-encoded campaign settings" }
      responses:
        "200":
          description: Campaign updated
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DialerCampaign" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      summary: Archive a campaign
      tags: [dialer]
      operationId: archiveDialerCampaign
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Campaign archived
          content:
            application/json:
              schema:
                type: object
                properties:
                  status: { type: string, example: campaign archived }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/dialer/campaigns/{id}/start:
    post:
      summary: Start a campaign
      tags: [dialer]
      operationId: startDialerCampaign
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Campaign started
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DialerCampaign" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/dialer/campaigns/{id}/pause:
    post:
      summary: Pause a campaign
      tags: [dialer]
      operationId: pauseDialerCampaign
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Campaign paused
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DialerCampaign" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/dialer/campaigns/{id}/contacts:
    post:
      summary: Add contacts to a campaign
      tags: [dialer]
      operationId: addContactsToDialerCampaign
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                contact_ids:
                  type: array
                  items: { type: string, format: uuid }
                  description: Explicit contact UUIDs to add
                filter_id:
                  type: string
                  format: uuid
                  description: Saved filter ID (alternative to contact_ids)
      responses:
        "200":
          description: Contacts added
          content:
            application/json:
              schema:
                type: object
                properties:
                  added_count: { type: integer }
                  skipped_count: { type: integer, description: "Contacts already in this campaign" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    get:
      summary: List contacts in a campaign
      tags: [dialer]
      operationId: listDialerCampaignContacts
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: page, in: query, schema: { type: integer, minimum: 1, default: 1 } }
        - { name: page_size, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 20 } }
        - name: status_filter
          in: query
          description: "CampaignContactStatus integer: 0=UNSPECIFIED 1=PENDING 2=IN_PROGRESS 3=COMPLETED 4=SKIPPED 5=CALLBACK"
          schema: { type: integer }
      responses:
        "200":
          description: Paginated campaign contacts
          content:
            application/json:
              schema:
                type: object
                properties:
                  contacts:
                    type: array
                    items: { $ref: "#/components/schemas/DialerCampaignContact" }
                  total: { type: integer }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/dialer/campaigns/{id}/next:
    get:
      summary: Get next contact for agent
      description: Returns the next pending campaign contact for the calling agent (or an explicit agent_id). Used to drive the preview/power dialer queue.
      tags: [dialer]
      operationId: getDialerNextContact
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - name: agent_id
          in: query
          description: Agent user UUID; defaults to the authenticated user
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: Next campaign contact
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DialerCampaignContact" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/dialer/campaigns/{id}/contacts/{cid}/skip:
    post:
      summary: Skip a campaign contact
      tags: [dialer]
      operationId: skipDialerCampaignContact
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: cid, in: path, required: true, description: "Campaign contact UUID", schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Contact skipped
          content:
            application/json:
              schema:
                type: object
                properties:
                  status: { type: string, example: contact skipped }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/dialer/campaigns/{id}/contacts/{cid}/requeue:
    post:
      summary: Requeue a skipped campaign contact
      tags: [dialer]
      operationId: requeueDialerCampaignContact
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: cid, in: path, required: true, description: "Campaign contact UUID", schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Contact requeued
          content:
            application/json:
              schema:
                type: object
                properties:
                  status: { type: string, example: contact requeued }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/dialer/campaigns/{id}/dashboard:
    get:
      summary: Campaign dashboard statistics
      tags: [dialer]
      operationId: getDialerCampaignDashboard
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Campaign dashboard
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DialerCampaignDashboard" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/dialer/calls:
    post:
      summary: Initiate a dialer call session
      tags: [dialer]
      operationId: initiateDialerCall
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [campaign_contact_id]
              properties:
                campaign_contact_id: { type: string, format: uuid }
                agent_id: { type: string, format: uuid, description: "Defaults to authenticated user" }
                call_session_id: { type: string, format: uuid, description: "LiveKit call session UUID, if pre-created via VideoService" }
      responses:
        "201":
          description: Dialer call session created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DialerCallSession" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/dialer/calls/{id}/outcome:
    post:
      summary: Log call outcome
      tags: [dialer]
      operationId: logDialerCallOutcome
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, description: "Dialer call session UUID", schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [outcome_id]
              properties:
                outcome_id: { type: string, format: uuid }
                notes: { type: string }
                next_action: { type: string }
                callback_at: { type: string, format: date-time, description: "RFC3339 timestamp for scheduled callback" }
                appointment_id: { type: string, format: uuid }
      responses:
        "200":
          description: Outcome logged; updated call session returned
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DialerCallSession" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/dialer/calls/{id}/notes:
    put:
      summary: Save call notes
      tags: [dialer]
      operationId: saveDialerCallNotes
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, description: "Dialer call session UUID", schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [notes]
              properties:
                notes: { type: string }
      responses:
        "200":
          description: Notes saved
          content:
            application/json:
              schema:
                type: object
                properties:
                  status: { type: string, example: notes saved }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/dialer/calls/{id}/complete:
    post:
      summary: Complete wrap-up for a call session
      tags: [dialer]
      operationId: completeDialerCallWrapUp
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, description: "Dialer call session UUID", schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                agent_id: { type: string, format: uuid, description: "Defaults to authenticated user" }
      responses:
        "200":
          description: Wrap-up completed
          content:
            application/json:
              schema:
                type: object
                properties:
                  status: { type: string, example: wrap-up completed }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/dialer/agent-status:
    get:
      summary: Get agent dialer status
      tags: [dialer]
      operationId: getDialerAgentStatus
      security: [{ bearerAuth: [] }]
      parameters:
        - name: user_id
          in: query
          description: User UUID; defaults to the authenticated user
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: Agent status
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DialerAgentStatus" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    put:
      summary: Set agent dialer status
      tags: [dialer]
      operationId: setDialerAgentStatus
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [status]
              properties:
                user_id: { type: string, format: uuid, description: "Defaults to authenticated user" }
                status:
                  type: integer
                  description: "AgentDialerStatus integer: 0=UNSPECIFIED 1=AVAILABLE 2=ON_CALL 3=WRAP_UP 4=BREAK 5=OFFLINE"
                campaign_id: { type: string, format: uuid }
      responses:
        "200":
          description: Agent status updated
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DialerAgentStatus" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/dialer/agent-status/campaign/{campaignId}:
    get:
      summary: Get all agent statuses for a campaign
      tags: [dialer]
      operationId: getDialerCampaignAgents
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: campaignId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Agent statuses for the campaign
          content:
            application/json:
              schema:
                type: object
                properties:
                  agents:
                    type: array
                    items: { $ref: "#/components/schemas/DialerAgentStatus" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/dialer/outcomes:
    get:
      summary: List call outcomes
      tags: [dialer]
      operationId: listDialerCallOutcomes
      security: [{ bearerAuth: [] }]
      parameters:
        - name: include_inactive
          in: query
          description: When true, include inactive outcomes (default active-only)
          schema: { type: boolean }
      responses:
        "200":
          description: List of call outcomes
          content:
            application/json:
              schema:
                type: object
                properties:
                  outcomes:
                    type: array
                    items: { $ref: "#/components/schemas/DialerCallOutcome" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      summary: Create a call outcome
      tags: [dialer]
      operationId: createDialerCallOutcome
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [label]
              properties:
                label: { type: string }
                color: { type: string, description: "Hex color string, e.g. #4CAF50" }
                is_positive: { type: boolean }
                is_callback: { type: boolean }
                is_appointment: { type: boolean }
                sort_order: { type: integer }
      responses:
        "201":
          description: Call outcome created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DialerCallOutcome" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/dialer/outcomes/{id}:
    put:
      summary: Update a call outcome
      tags: [dialer]
      operationId: updateDialerCallOutcome
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                label: { type: string }
                color: { type: string }
                is_positive: { type: boolean }
                is_callback: { type: boolean }
                is_appointment: { type: boolean }
                sort_order: { type: integer }
                is_active: { type: boolean }
      responses:
        "200":
          description: Call outcome updated
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DialerCallOutcome" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      summary: Delete a call outcome
      tags: [dialer]
      operationId: deleteDialerCallOutcome
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Outcome deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  status: { type: string, example: outcome deleted }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/dialer/dashboard/agent:
    get:
      summary: Agent dashboard statistics
      tags: [dialer]
      operationId: getDialerAgentDashboard
      security: [{ bearerAuth: [] }]
      parameters:
        - name: agent_id
          in: query
          description: Agent user UUID; defaults to the authenticated user
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: Agent dashboard
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DialerAgentDashboard" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  # --- Inventar ---

  /api/v1/inventar/items:
    get:
      summary: List inventory items
      tags: [inventar]
      operationId: listInventarItems
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: search, in: query, schema: { type: string } }
        - { name: location, in: query, schema: { type: string } }
        - { name: low_stock, in: query, schema: { type: boolean }, description: "Filter to items below min_quantity" }
        - { name: page, in: query, schema: { type: integer, minimum: 1, default: 1 } }
        - { name: page_size, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 50 } }
      responses:
        "200":
          description: Paginated item list
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items: { $ref: "#/components/schemas/InventarItem" }
                  total: { type: integer }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      summary: Create an inventory item
      tags: [inventar]
      operationId: createInventarItem
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name, sku]
              properties:
                name: { type: string }
                sku: { type: string }
                barcode: { type: string }
                quantity: { type: integer, format: int64, minimum: 0 }
                min_quantity: { type: integer, format: int64, minimum: 0 }
                unit: { type: string }
                location: { type: string }
      responses:
        "201":
          description: Item created
          content:
            application/json:
              schema:
                type: object
                properties:
                  item: { $ref: "#/components/schemas/InventarItem" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/inventar/items/{id}:
    get:
      summary: Get an inventory item by ID
      tags: [inventar]
      operationId: getInventarItem
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Inventory item
          content:
            application/json:
              schema:
                type: object
                properties:
                  item: { $ref: "#/components/schemas/InventarItem" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      summary: Update an inventory item
      tags: [inventar]
      operationId: updateInventarItem
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name: { type: string }
                sku: { type: string }
                barcode: { type: string }
                min_quantity: { type: integer, format: int64, minimum: 0 }
                unit: { type: string }
                location: { type: string }
      responses:
        "200":
          description: Updated item
          content:
            application/json:
              schema:
                type: object
                properties:
                  item: { $ref: "#/components/schemas/InventarItem" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      summary: Delete an inventory item
      tags: [inventar]
      operationId: deleteInventarItem
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "204":
          description: Item deleted
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/inventar/items/{id}/adjust:
    post:
      summary: Adjust stock quantity for an item
      tags: [inventar]
      operationId: adjustInventarStock
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [delta, reason]
              properties:
                delta:
                  type: integer
                  format: int64
                  description: "Positive = stock in, negative = stock out"
                performed_by: { type: string, format: uuid }
                reason: { type: string }
      responses:
        "200":
          description: Updated item after stock adjustment
          content:
            application/json:
              schema:
                type: object
                properties:
                  item: { $ref: "#/components/schemas/InventarItem" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/inventar/items/{id}/movements:
    get:
      summary: List movements for an item
      tags: [inventar]
      operationId: listInventarItemMovements
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: page, in: query, schema: { type: integer, minimum: 1, default: 1 } }
        - { name: page_size, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 50 } }
      responses:
        "200":
          description: Paginated movement list
          content:
            application/json:
              schema:
                type: object
                properties:
                  movements:
                    type: array
                    items: { $ref: "#/components/schemas/InventarMovement" }
                  total: { type: integer }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      summary: Record a stock movement for an item
      tags: [inventar]
      operationId: recordInventarMovement
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [movement_type, quantity, reason]
              properties:
                movement_type:
                  type: string
                  enum: [in, out, adjustment]
                  description: "Allowed values: in, out, adjustment (transfer via POST /transfer)"
                quantity: { type: integer, format: int64, minimum: 1 }
                performed_by: { type: string, format: uuid }
                reason: { type: string }
      responses:
        "201":
          description: Movement recorded
          content:
            application/json:
              schema:
                type: object
                properties:
                  movement: { $ref: "#/components/schemas/InventarMovement" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/inventar/items/{id}/history:
    get:
      summary: Get stock history for an item
      tags: [inventar]
      operationId: getInventarStockHistory
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: page, in: query, schema: { type: integer, minimum: 1, default: 1 } }
        - { name: page_size, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 50 } }
      responses:
        "200":
          description: Paginated stock history (movements)
          content:
            application/json:
              schema:
                type: object
                properties:
                  movements:
                    type: array
                    items: { $ref: "#/components/schemas/InventarMovement" }
                  total: { type: integer }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/inventar/transfer:
    post:
      summary: Transfer stock between two items
      tags: [inventar]
      operationId: transferInventarStock
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [from_item_id, to_item_id, quantity]
              properties:
                from_item_id: { type: string, format: uuid }
                to_item_id: { type: string, format: uuid }
                quantity: { type: integer, format: int64, minimum: 1 }
                performed_by: { type: string, format: uuid }
                reason: { type: string }
      responses:
        "204":
          description: Transfer completed
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/inventar/warnings:
    get:
      summary: List stock warnings
      tags: [inventar]
      operationId: listInventarWarnings
      security: [{ bearerAuth: [] }]
      parameters:
        - name: status
          in: query
          schema:
            type: string
            enum: [active, acknowledged, resolved]
          description: "Filter by warning status"
        - { name: page, in: query, schema: { type: integer, minimum: 1, default: 1 } }
        - { name: page_size, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 50 } }
      responses:
        "200":
          description: Paginated warning list
          content:
            application/json:
              schema:
                type: object
                properties:
                  warnings:
                    type: array
                    items: { $ref: "#/components/schemas/InventarWarning" }
                  total: { type: integer }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      summary: Create a stock warning
      tags: [inventar]
      operationId: createInventarWarning
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [item_id, threshold, current_quantity]
              properties:
                item_id: { type: string, format: uuid }
                threshold: { type: integer, format: int64, minimum: 1 }
                current_quantity: { type: integer, format: int64, minimum: 0 }
      responses:
        "201":
          description: Warning created
          content:
            application/json:
              schema:
                type: object
                properties:
                  warning: { $ref: "#/components/schemas/InventarWarning" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/inventar/warnings/{id}:
    patch:
      summary: Update a stock warning
      tags: [inventar]
      operationId: updateInventarWarning
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [status]
              properties:
                status:
                  type: string
                  enum: [active, acknowledged, resolved]
      responses:
        "200":
          description: Updated warning
          content:
            application/json:
              schema:
                type: object
                properties:
                  warning: { $ref: "#/components/schemas/InventarWarning" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/inventar/warnings/{id}/acknowledge:
    post:
      summary: Acknowledge a stock warning
      tags: [inventar]
      operationId: acknowledgeInventarWarning
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                acknowledged_by:
                  type: string
                  format: uuid
                  description: "UUID of acknowledging user; falls back to authenticated user if omitted"
      responses:
        "200":
          description: Acknowledged warning
          content:
            application/json:
              schema:
                type: object
                properties:
                  warning: { $ref: "#/components/schemas/InventarWarning" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/inventar/report:
    get:
      summary: Get aggregated stock report
      tags: [inventar]
      operationId: getInventarStockReport
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Stock report summary
          content:
            application/json:
              schema:
                type: object
                properties:
                  total_items:
                    type: integer
                    description: "Total number of distinct items"
                  total_quantity:
                    type: integer
                    format: int64
                    description: "Sum of all item quantities"
                  low_stock_count:
                    type: integer
                    description: "Number of items below min_quantity"
                  active_warnings:
                    type: integer
                    description: "Number of warnings with status=active"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/inventar/export:
    get:
      summary: Export inventory as file
      tags: [inventar]
      operationId: exportInventar
      security: [{ bearerAuth: [] }]
      parameters:
        - name: format
          in: query
          schema:
            type: string
            default: csv
          description: "Export format; currently only csv is supported"
      responses:
        "200":
          description: Binary file download
          headers:
            Content-Disposition:
              schema: { type: string }
              description: "attachment; filename=inventar.csv"
          content:
            application/octet-stream:
              schema:
                type: string
                format: binary
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/inventar/locations:
    get:
      summary: List inventory locations
      tags: [inventar]
      operationId: listInventarLocations
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: page, in: query, schema: { type: integer, minimum: 1, default: 1 } }
        - { name: page_size, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 50 } }
      responses:
        "200":
          description: Paginated location list
          content:
            application/json:
              schema:
                type: object
                properties:
                  locations:
                    type: array
                    items: { $ref: "#/components/schemas/InventarLocation" }
                  total: { type: integer }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      summary: Create an inventory location
      tags: [inventar]
      operationId: createInventarLocation
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name: { type: string }
                address: { type: string }
                type:
                  type: string
                  enum: [warehouse, store, vehicle]
      responses:
        "201":
          description: Location created
          content:
            application/json:
              schema:
                type: object
                properties:
                  location: { $ref: "#/components/schemas/InventarLocation" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/inventar/locations/{id}:
    get:
      summary: Get an inventory location by ID
      tags: [inventar]
      operationId: getInventarLocation
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Inventory location
          content:
            application/json:
              schema:
                type: object
                properties:
                  location: { $ref: "#/components/schemas/InventarLocation" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      summary: Update an inventory location
      tags: [inventar]
      operationId: updateInventarLocation
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name: { type: string }
                address: { type: string }
                type:
                  type: string
                  enum: [warehouse, store, vehicle]
      responses:
        "200":
          description: Updated location
          content:
            application/json:
              schema:
                type: object
                properties:
                  location: { $ref: "#/components/schemas/InventarLocation" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      summary: Delete an inventory location
      tags: [inventar]
      operationId: deleteInventarLocation
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "204":
          description: Location deleted
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/inventar/inventur:
    get:
      summary: List Inventur sessions
      tags: [inventar]
      operationId: listInventurSessions
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: page, in: query, schema: { type: integer, minimum: 1, default: 1 } }
        - { name: page_size, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 50 } }
      responses:
        "200":
          description: Paginated Inventur session list
          content:
            application/json:
              schema:
                type: object
                properties:
                  sessions:
                    type: array
                    items: { $ref: "#/components/schemas/InventarInventurSession" }
                  total: { type: integer }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      summary: Create an Inventur session
      tags: [inventar]
      operationId: createInventurSession
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name: { type: string }
                date:
                  type: string
                  description: "Date string in YYYY-MM-DD format"
                location_id: { type: string, format: uuid }
                item_ids:
                  type: array
                  items: { type: string, format: uuid }
      responses:
        "201":
          description: Inventur session created
          content:
            application/json:
              schema:
                type: object
                properties:
                  session: { $ref: "#/components/schemas/InventarInventurSession" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/inventar/inventur/{id}:
    get:
      summary: Get an Inventur session by ID
      tags: [inventar]
      operationId: getInventurSession
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Inventur session
          content:
            application/json:
              schema:
                type: object
                properties:
                  session: { $ref: "#/components/schemas/InventarInventurSession" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      summary: Delete an Inventur session
      tags: [inventar]
      operationId: deleteInventurSession
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "204":
          description: Inventur session deleted
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/inventar/inventur/{id}/status:
    patch:
      summary: Update Inventur session status
      tags: [inventar]
      operationId: updateInventurSessionStatus
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [status]
              properties:
                status:
                  type: string
                  enum: [open, counting, review, completed]
      responses:
        "200":
          description: Updated Inventur session
          content:
            application/json:
              schema:
                type: object
                properties:
                  session: { $ref: "#/components/schemas/InventarInventurSession" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/inventar/inventur/{id}/counts:
    post:
      summary: Upsert an Inventur count entry
      tags: [inventar]
      operationId: upsertInventurCount
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [item_id, counted]
              properties:
                item_id: { type: string, format: uuid }
                counted: { type: integer, format: int64, minimum: 0 }
      responses:
        "200":
          description: Count upserted
          content:
            application/json:
              schema:
                type: object
                properties:
                  count: { $ref: "#/components/schemas/InventarInventurCount" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/inventar/inventur/{id}/book:
    post:
      summary: Book Inventur differences to stock
      tags: [inventar]
      operationId: bookInventurDifferences
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                booked_by:
                  type: string
                  format: uuid
                  description: "UUID of user booking the differences; optional"
      responses:
        "200":
          description: Inventur session after booking
          content:
            application/json:
              schema:
                type: object
                properties:
                  session: { $ref: "#/components/schemas/InventarInventurSession" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # --- Vermietung ---

  /api/v1/vermietung/objects:
    get:
      summary: List rental objects
      tags: [vermietung]
      operationId: listVermietungObjects
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: search, in: query, schema: { type: string } }
        - { name: category, in: query, schema: { type: string } }
        - { name: active_only, in: query, schema: { type: boolean } }
        - { name: page, in: query, schema: { type: integer, minimum: 1, default: 1 } }
        - { name: page_size, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 50 } }
      responses:
        "200":
          description: Paginated rental object list
          content:
            application/json:
              schema:
                type: object
                properties:
                  objects:
                    type: array
                    items: { $ref: "#/components/schemas/VermietungRentalObject" }
                  total: { type: integer }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      summary: Create a rental object
      tags: [vermietung]
      operationId: createVermietungObject
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name, daily_rate, deposit]
              properties:
                name: { type: string }
                description: { type: string }
                category: { type: string }
                daily_rate: { type: number, format: double, minimum: 0, exclusiveMinimum: true }
                deposit: { type: number, format: double, minimum: 0 }
                location: { type: string }
                notes: { type: string }
      responses:
        "201":
          description: Rental object created
          content:
            application/json:
              schema:
                type: object
                properties:
                  object: { $ref: "#/components/schemas/VermietungRentalObject" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/vermietung/objects/{id}:
    get:
      summary: Get a rental object by ID
      tags: [vermietung]
      operationId: getVermietungObject
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Rental object
          content:
            application/json:
              schema:
                type: object
                properties:
                  object: { $ref: "#/components/schemas/VermietungRentalObject" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      summary: Update a rental object
      tags: [vermietung]
      operationId: updateVermietungObject
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name: { type: string }
                description: { type: string }
                category: { type: string }
                daily_rate: { type: number, format: double }
                deposit: { type: number, format: double }
                location: { type: string }
                notes: { type: string }
                active: { type: boolean }
      responses:
        "200":
          description: Rental object updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  object: { $ref: "#/components/schemas/VermietungRentalObject" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      summary: Delete a rental object
      tags: [vermietung]
      operationId: deleteVermietungObject
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "204":
          description: Rental object deleted
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/vermietung/objects/{id}/availability:
    get:
      summary: Check availability of a rental object
      tags: [vermietung]
      operationId: checkVermietungAvailability
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: start_date, in: query, required: true, schema: { type: string }, description: "RFC3339 timestamp" }
        - { name: end_date, in: query, required: true, schema: { type: string }, description: "RFC3339 timestamp" }
        - { name: exclude_rental_id, in: query, schema: { type: string, format: uuid }, description: "Exclude this rental ID from conflict detection (use when editing an existing rental)" }
      responses:
        "200":
          description: Availability result
          content:
            application/json:
              schema:
                type: object
                properties:
                  available: { type: boolean }
                  conflicting_rentals:
                    type: array
                    items: { $ref: "#/components/schemas/VermietungRental" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/vermietung/rentals:
    get:
      summary: List rentals
      tags: [vermietung]
      operationId: listVermietungRentals
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: object_id, in: query, schema: { type: string, format: uuid } }
        - { name: status, in: query, schema: { type: string, enum: [reserved, active, completed, cancelled] } }
        - { name: from, in: query, schema: { type: string }, description: "RFC3339 timestamp — filter start_date >= from" }
        - { name: to, in: query, schema: { type: string }, description: "RFC3339 timestamp — filter start_date <= to" }
        - { name: page, in: query, schema: { type: integer, minimum: 1, default: 1 } }
        - { name: page_size, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 50 } }
      responses:
        "200":
          description: Paginated rental list
          content:
            application/json:
              schema:
                type: object
                properties:
                  rentals:
                    type: array
                    items: { $ref: "#/components/schemas/VermietungRental" }
                  total: { type: integer }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      summary: Create a rental
      tags: [vermietung]
      operationId: createVermietungRental
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [object_id, renter_name, start_date, end_date]
              properties:
                object_id: { type: string, format: uuid }
                contact_id: { type: string, format: uuid }
                renter_name: { type: string }
                start_date: { type: string, description: "RFC3339 timestamp" }
                end_date: { type: string, description: "RFC3339 timestamp" }
                total_price: { type: number, format: double, minimum: 0 }
                deposit_paid: { type: boolean }
                notes: { type: string }
      responses:
        "201":
          description: Rental created
          content:
            application/json:
              schema:
                type: object
                properties:
                  rental: { $ref: "#/components/schemas/VermietungRental" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/vermietung/rentals/{id}:
    get:
      summary: Get a rental by ID
      tags: [vermietung]
      operationId: getVermietungRental
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Rental
          content:
            application/json:
              schema:
                type: object
                properties:
                  rental: { $ref: "#/components/schemas/VermietungRental" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      summary: Update a rental
      tags: [vermietung]
      operationId: updateVermietungRental
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                renter_name: { type: string }
                start_date: { type: string, description: "RFC3339 timestamp" }
                end_date: { type: string, description: "RFC3339 timestamp" }
                total_price: { type: number, format: double }
                deposit_paid: { type: boolean }
                notes: { type: string }
      responses:
        "200":
          description: Rental updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  rental: { $ref: "#/components/schemas/VermietungRental" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      summary: Delete a rental
      tags: [vermietung]
      operationId: deleteVermietungRental
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "204":
          description: Rental deleted
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/vermietung/rentals/{id}/start:
    post:
      summary: Start a rental (transition to active)
      tags: [vermietung]
      operationId: startVermietungRental
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Rental started
          content:
            application/json:
              schema:
                type: object
                properties:
                  rental: { $ref: "#/components/schemas/VermietungRental" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/vermietung/rentals/{id}/end:
    post:
      summary: End a rental (transition to completed)
      tags: [vermietung]
      operationId: endVermietungRental
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Rental ended
          content:
            application/json:
              schema:
                type: object
                properties:
                  rental: { $ref: "#/components/schemas/VermietungRental" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/vermietung/rentals/{id}/signature:
    put:
      summary: Save rental signature
      tags: [vermietung]
      operationId: saveVermietungRentalSignature
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [signature_data, signed_by]
              properties:
                signature_data: { type: string, description: "data:image/png;base64,... or data:image/svg+xml;base64,..." }
                signed_by: { type: string }
      responses:
        "200":
          description: Signature saved
          content:
            application/json:
              schema:
                type: object
                properties:
                  rental: { $ref: "#/components/schemas/VermietungRental" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/vermietung/rentals/{id}/inspections:
    get:
      summary: List inspections for a rental
      tags: [vermietung]
      operationId: listVermietungInspections
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: page, in: query, schema: { type: integer, minimum: 1, default: 1 } }
        - { name: page_size, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 50 } }
      responses:
        "200":
          description: Paginated inspection list
          content:
            application/json:
              schema:
                type: object
                properties:
                  inspections:
                    type: array
                    items: { $ref: "#/components/schemas/VermietungRentalInspection" }
                  total: { type: integer }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      summary: Create an inspection for a rental
      tags: [vermietung]
      operationId: createVermietungInspection
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [kind]
              properties:
                kind: { type: string, enum: [handover, return] }
                notes: { type: string }
                photo_urls:
                  type: array
                  items: { type: string, format: uri }
                performed_by: { type: string, format: uuid }
      responses:
        "201":
          description: Inspection created
          content:
            application/json:
              schema:
                type: object
                properties:
                  inspection: { $ref: "#/components/schemas/VermietungRentalInspection" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/vermietung/inspections/{id}:
    get:
      summary: Get an inspection by ID
      tags: [vermietung]
      operationId: getVermietungInspection
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Inspection
          content:
            application/json:
              schema:
                type: object
                properties:
                  inspection: { $ref: "#/components/schemas/VermietungRentalInspection" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      summary: Update an inspection
      tags: [vermietung]
      operationId: updateVermietungInspection
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                notes: { type: string }
                photo_urls:
                  type: array
                  items: { type: string, format: uri }
      responses:
        "200":
          description: Inspection updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  inspection: { $ref: "#/components/schemas/VermietungRentalInspection" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/vermietung/calendar:
    get:
      summary: Get rental calendar for a month
      tags: [vermietung]
      operationId: getVermietungCalendar
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: year, in: query, schema: { type: integer }, description: "Calendar year (e.g. 2026)" }
        - { name: month, in: query, schema: { type: integer, minimum: 1, maximum: 12 }, description: "Calendar month (1-12)" }
      responses:
        "200":
          description: Calendar entries for the requested month
          content:
            application/json:
              schema:
                type: object
                properties:
                  entries:
                    type: array
                    items: { $ref: "#/components/schemas/VermietungCalendarEntry" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/vermietung/export:
    get:
      summary: Export rental report
      tags: [vermietung]
      operationId: exportVermietungRentalReport
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: format, in: query, schema: { type: string, enum: [csv], default: csv }, description: "Export format (default: csv)" }
      responses:
        "200":
          description: Binary export file
          content:
            application/octet-stream:
              schema:
                type: string
                format: binary
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  # === openapi-drift sweep: berichte ===
  # --- Berichte (custom report builder) ---
  # route_berichte.go, mounted at /api/v1/berichte/*, gated behind the
  # modules.berichte feature flag. All handlers call response.JSON(w, status,
  # resp) directly on the raw gRPC response struct (encoding/json, not
  # protojson): snake_case field names come from the pb.go json tags, and
  # google.protobuf.Timestamp fields serialize as {seconds,nanos} ->
  # $ref ProtoTimestamp, NOT RFC3339 strings. bytes fields (query_config,
  # params, payload) serialize as base64 strings (Go encoding/json default
  # for []byte), NOT nested JSON objects. Error codes are limited to what
  # backend/internal/server/berichte_grpc.go's mapBerichteError actually
  # produces: NotFound (definition/schedule/cache-miss), InvalidArgument
  # (bad query_config/cron/format/module/kind), Unavailable (executor down,
  # mapped generically to 500 by grpcStatusToHTTP's default branch since
  # Unavailable is reserved for a down gRPC connection at the gateway level),
  # Internal fallback. No AlreadyExists/FailedPrecondition is ever returned,
  # so these endpoints never document 409.

  /api/v1/berichte/definitions:
    get:
      summary: List report definitions
      description: Requires berichte:reports:read permission.
      tags: [berichte]
      operationId: listBerichteDefinitions
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: search, in: query, schema: { type: string } }
        - $ref: "#/components/parameters/page"
        - $ref: "#/components/parameters/page_size"
        - { name: sort_by, in: query, schema: { type: string } }
        - { name: sort_desc, in: query, schema: { type: boolean } }
        - name: module
          in: query
          description: "Filter by module: finanzen|crm|helpdesk|inventar|produktion|cross"
          schema: { type: string }
        - name: kind
          in: query
          description: "Filter by kind: system|custom"
          schema: { type: string }
        - { name: is_published, in: query, schema: { type: boolean } }
      responses:
        "200":
          description: Paginated report definition list
          content:
            application/json:
              schema:
                type: object
                properties:
                  definitions:
                    type: array
                    items: { $ref: "#/components/schemas/BerichtDefinition" }
                  total: { type: integer }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      summary: Create a report definition
      description: Requires berichte:reports:write permission.
      tags: [berichte]
      operationId: createBerichteDefinition
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name, module]
              properties:
                name: { type: string }
                description: { type: string }
                module:
                  type: string
                  description: "finanzen|crm|helpdesk|inventar|produktion|cross"
                kind:
                  type: string
                  description: "system|custom (defaults to custom)"
                query_config:
                  type: string
                  format: byte
                  description: "Raw JSONB aggregation spec (base64-encoded bytes on the wire)"
                default_format:
                  type: string
                  description: "pdf|csv|xlsx"
                is_published: { type: boolean }
      responses:
        "201":
          description: Report definition created
          content:
            application/json:
              schema:
                type: object
                properties:
                  definition: { $ref: "#/components/schemas/BerichtDefinition" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/berichte/definitions/{id}:
    get:
      summary: Get a report definition by ID
      description: Requires berichte:reports:read permission.
      tags: [berichte]
      operationId: getBerichteDefinition
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Report definition
          content:
            application/json:
              schema:
                type: object
                properties:
                  definition: { $ref: "#/components/schemas/BerichtDefinition" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      summary: Update a report definition
      description: Requires berichte:reports:write permission. Only present fields are changed.
      tags: [berichte]
      operationId: updateBerichteDefinition
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name: { type: string }
                description: { type: string }
                module:
                  type: string
                  description: "finanzen|crm|helpdesk|inventar|produktion|cross"
                query_config:
                  type: string
                  format: byte
                  description: "Raw JSONB aggregation spec (base64-encoded bytes on the wire); empty means no change"
                default_format:
                  type: string
                  description: "pdf|csv|xlsx"
                is_published: { type: boolean }
      responses:
        "200":
          description: Report definition updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  definition: { $ref: "#/components/schemas/BerichtDefinition" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      summary: Delete a report definition
      description: Requires berichte:reports:write permission.
      tags: [berichte]
      operationId: deleteBerichteDefinition
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "204":
          description: Report definition deleted
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/berichte/definitions/{id}/run:
    post:
      summary: Run a report definition on demand
      description: >-
        Requires berichte:reports:read permission. Executes the report
        immediately (trigger=manual) and returns both the result payload and
        the run record. Serves from cache unless force_refresh is set.
      tags: [berichte]
      operationId: runBerichteReport
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                params:
                  type: string
                  format: byte
                  description: "JSON-encoded report filter params (base64-encoded bytes on the wire)"
                force_refresh:
                  type: boolean
                  description: Bypass any cached result and recompute
      responses:
        "200":
          description: Report result and run record
          content:
            application/json:
              schema:
                type: object
                properties:
                  result: { $ref: "#/components/schemas/BerichtReportResult" }
                  run: { $ref: "#/components/schemas/BerichtRun" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/berichte/definitions/{id}/export:
    post:
      summary: Run and export a report as a binary file
      description: >-
        Requires berichte:reports:read permission. Streams the rendered
        report as a binary download. Content-Disposition is set from the
        server-generated filename (safely quoted; path separators and
        control characters are stripped).
      tags: [berichte]
      operationId: exportBerichteReport
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
        - name: format
          in: query
          description: "Export format; defaults to pdf"
          schema: { type: string, enum: [pdf, csv, xlsx], default: pdf }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                params:
                  type: string
                  format: byte
                  description: "JSON-encoded report filter params (base64-encoded bytes on the wire)"
      responses:
        "200":
          description: Exported report file
          content:
            application/pdf:
              schema: { type: string, format: binary }
            text/csv:
              schema: { type: string, format: binary }
            application/vnd.openxmlformats-officedocument.spreadsheetml.sheet:
              schema: { type: string, format: binary }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/berichte/definitions/{id}/cache:
    delete:
      summary: Invalidate the cached result(s) for a report definition
      description: Requires berichte:reports:write permission.
      tags: [berichte]
      operationId: invalidateBerichteCache
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Number of cache entries evicted
          content:
            application/json:
              schema:
                type: object
                properties:
                  evicted: { type: integer, format: int32 }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/berichte/schedules:
    get:
      summary: List report delivery schedules
      description: Requires berichte:reports:read permission.
      tags: [berichte]
      operationId: listBerichteSchedules
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/page"
        - $ref: "#/components/parameters/page_size"
        - { name: definition_id, in: query, schema: { type: string, format: uuid } }
        - { name: active, in: query, schema: { type: boolean } }
      responses:
        "200":
          description: Paginated schedule list
          content:
            application/json:
              schema:
                type: object
                properties:
                  schedules:
                    type: array
                    items: { $ref: "#/components/schemas/BerichtSchedule" }
                  total: { type: integer }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      summary: Create a report delivery schedule
      description: Requires berichte:reports:write permission.
      tags: [berichte]
      operationId: createBerichteSchedule
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name, cron_expression]
              properties:
                definition_id: { type: string, format: uuid }
                name: { type: string }
                cron_expression: { type: string, description: "Standard cron expression" }
                recipients:
                  type: array
                  items: { type: string, format: email }
                format: { type: string, enum: [pdf, csv, xlsx] }
                params:
                  type: string
                  format: byte
                  description: "JSON-encoded report params (base64-encoded bytes on the wire)"
                active: { type: boolean }
      responses:
        "201":
          description: Schedule created
          content:
            application/json:
              schema:
                type: object
                properties:
                  schedule: { $ref: "#/components/schemas/BerichtSchedule" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/berichte/schedules/{id}:
    patch:
      summary: Update a report delivery schedule
      description: >-
        Requires berichte:reports:write permission. Only present fields are
        changed; recipients is only replaced when recipients_set is true
        (this allows clearing the list to empty, distinct from "unchanged").
      tags: [berichte]
      operationId: updateBerichteSchedule
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name: { type: string }
                cron_expression: { type: string }
                recipients:
                  type: array
                  items: { type: string, format: email }
                recipients_set:
                  type: boolean
                  description: "Set true to replace recipients with the (possibly empty) recipients array above"
                format: { type: string, enum: [pdf, csv, xlsx] }
                params:
                  type: string
                  format: byte
                  description: "JSON-encoded report params (base64-encoded bytes on the wire)"
                active: { type: boolean }
      responses:
        "200":
          description: Schedule updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  schedule: { $ref: "#/components/schemas/BerichtSchedule" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      summary: Delete a report delivery schedule
      description: Requires berichte:reports:write permission.
      tags: [berichte]
      operationId: deleteBerichteSchedule
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "204":
          description: Schedule deleted
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/berichte/schedules/{id}/toggle:
    post:
      summary: Toggle a report delivery schedule active/inactive
      description: Requires berichte:reports:write permission.
      tags: [berichte]
      operationId: toggleBerichteSchedule
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [active]
              properties:
                active: { type: boolean }
      responses:
        "200":
          description: Schedule updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  schedule: { $ref: "#/components/schemas/BerichtSchedule" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/berichte/documents:
    get:
      summary: List report documents
      description: >
        Requires berichte:reports:read permission. Returns the tenant's report
        documents, newest first. The library view has no pager, so page_size
        defaults to 100 (capped at 100 server-side).
      tags: [berichte]
      operationId: listBerichteDocuments
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: search, in: query, description: Case-insensitive title match, schema: { type: string } }
        - name: module
          in: query
          description: "Filter by module: finanzen|crm|helpdesk|inventar|produktion|cross"
          schema: { type: string }
        - name: status
          in: query
          description: "Filter by status: draft|final|released|archived"
          schema: { type: string }
        - $ref: "#/components/parameters/page"
        - $ref: "#/components/parameters/page_size"
      responses:
        "200":
          description: Report document list
          content:
            application/json:
              schema:
                type: object
                properties:
                  documents:
                    type: array
                    items: { $ref: "#/components/schemas/BerichtDocument" }
                  total: { type: integer }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      summary: Create a report document
      description: >
        Requires berichte:reports:write permission. Every field is optional:
        the title falls back to "Neuer Bericht", the module to "cross", rows to
        an empty array and settings to an empty object. New documents always
        start in status "draft".
      tags: [berichte]
      operationId: createBerichteDocument
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                title: { type: string }
                description: { type: string }
                module:
                  type: string
                  description: "finanzen|crm|helpdesk|inventar|produktion|cross (defaults to cross)"
                template_id:
                  type: string
                  nullable: true
                rows:
                  type: array
                  items:
                    type: object
                    additionalProperties: true
                settings:
                  type: object
                  additionalProperties: true
      responses:
        "201":
          description: Report document created
          content:
            application/json:
              schema:
                type: object
                properties:
                  document: { $ref: "#/components/schemas/BerichtDocument" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/berichte/documents/{id}:
    get:
      summary: Get a report document by ID
      description: Requires berichte:reports:read permission.
      tags: [berichte]
      operationId: getBerichteDocument
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Report document
          content:
            application/json:
              schema:
                type: object
                properties:
                  document: { $ref: "#/components/schemas/BerichtDocument" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      summary: Update a report document
      description: >
        Requires berichte:reports:write permission. Only present fields are
        changed; omitted rows/settings keep their stored value. The first
        transition to status "released" stamps released_at, later edits leave
        it untouched.
      tags: [berichte]
      operationId: updateBerichteDocument
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                title: { type: string }
                description: { type: string }
                module:
                  type: string
                  description: "finanzen|crm|helpdesk|inventar|produktion|cross"
                status:
                  type: string
                  description: "draft|final|released|archived"
                rows:
                  type: array
                  items:
                    type: object
                    additionalProperties: true
                settings:
                  type: object
                  additionalProperties: true
      responses:
        "200":
          description: Report document updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  document: { $ref: "#/components/schemas/BerichtDocument" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      summary: Delete a report document
      description: Requires berichte:reports:write permission.
      tags: [berichte]
      operationId: deleteBerichteDocument
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "204":
          description: Report document deleted
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/berichte/documents/{id}/export/pdf:
    get:
      summary: Server-rendered PDF of a report document
      description: >
        Requires berichte:reports:read permission. Renders the document's
        block tree (rows -> columns -> blocks) via the maroto/v2 renderer —
        chart and table blocks are rendered as data tables, not charts (no
        chart-rendering dependency added). Blocks backed by an inline query
        instead of a saved definition render as unavailable, since the
        executor only runs saved definitions.
      tags: [berichte]
      operationId: exportBerichteDocumentPdf
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: PDF binary
          content:
            application/pdf:
              schema:
                type: string
                format: binary
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/berichte/documents/{id}/shares:
    get:
      summary: List the live share links of a report document
      description: >
        Requires berichte:reports:read permission. Returns only links that are
        still live: revoked ones stay in the database for the audit trail but
        are not listed. The token is returned in clear because the frontend
        renders it as a copyable URL.
      tags: [berichte]
      operationId: listBerichteDocumentShares
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Share links of the document
          content:
            application/json:
              schema:
                type: object
                properties:
                  shares:
                    type: array
                    items: { $ref: "#/components/schemas/BerichtShareToken" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      summary: Create an external share link for a report document
      description: >
        Requires berichte:reports:write permission — minting a link hands out
        an unauthenticated read path to the document, so it sits behind the
        same permission as changing the document itself.


        The returned token is the whole credential for
        POST /api/v1/public/berichte/reports/{token}. An optional password is
        bcrypt-hashed and never returned; the response only reports whether one
        is set. expires_in_days is capped at 365; omitting it or passing 0
        creates a link that never expires.
      tags: [berichte]
      operationId: createBerichteDocumentShare
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                expires_in_days:
                  type: integer
                  nullable: true
                  minimum: 0
                  maximum: 365
                  description: "null or 0 = never expires"
                password:
                  type: string
                  maxLength: 72
                  description: >
                    Optional. Capped at 72 bytes because bcrypt ignores input
                    past that length, which would make two different passwords
                    interchangeable.
      responses:
        "201":
          description: Share link created
          content:
            application/json:
              schema:
                type: object
                properties:
                  share: { $ref: "#/components/schemas/BerichtShareToken" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/berichte/shares/{shareId}:
    delete:
      summary: Revoke an external share link
      description: >
        Requires berichte:reports:write permission. The link stops working
        immediately; the row is kept with a revocation timestamp so the view
        count survives. Revoking an already-revoked link answers 404.
      tags: [berichte]
      operationId: revokeBerichteDocumentShare
      security: [{ bearerAuth: [] }]
      parameters:
        - name: shareId
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        "204":
          description: Share link revoked
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/public/berichte/reports/{token}:
    post:
      summary: Read a shared report document (public, unauthenticated)
      description: >
        No authentication. The share token in the path is the entire
        credential; the tenant is resolved from it server-side and the response
        can only ever be the one document the link names.


        It is a POST rather than a GET because the password must not travel in
        a URL — where it would land in access logs, browser history and Referer
        headers — and because the call increments the link's view counter,
        which no prefetching GET should be free to do. The request body is
        optional: a link without a password is read with no body at all.


        Unknown, expired and revoked links all answer 404, deliberately
        indistinguishable. A missing or wrong password answers 401. This route
        is behind the strict per-IP public rate limit
        (PUBLIC_RATE_LIMIT_RPS), not the authenticated one.


        The response is a reduced document: tenant_id, created_by and the
        editing status are withheld from anonymous callers.
      tags: [berichte]
      operationId: getPublicBerichteSharedReport
      security: []
      parameters:
        - name: token
          in: path
          required: true
          schema: { type: string }
          description: The share link secret (43-char base64url).
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                password:
                  type: string
                  description: Required only if the link is password-protected.
      responses:
        "200":
          description: The shared report document
          content:
            application/json:
              schema:
                type: object
                properties:
                  document: { $ref: "#/components/schemas/BerichtPublicDocument" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401":
          description: The link is password-protected and no valid password was given
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorResponse" }
        "404":
          description: No such link, or it expired or was revoked
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorResponse" }
        "429": { $ref: "#/components/responses/TooManyRequests" }
        "503": { $ref: "#/components/responses/ServiceUnavailable" }

  /api/v1/berichte/templates:
    get:
      summary: List report starter templates
      description: >
        Requires berichte:reports:read permission. Returns the static set of
        starter block structures for "Neuer Bericht aus Vorlage" — not tenant
        data, so the response is identical for every tenant.
      tags: [berichte]
      operationId: listBerichteTemplates
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Report template list
          content:
            application/json:
              schema:
                type: object
                properties:
                  templates:
                    type: array
                    items: { $ref: "#/components/schemas/BerichtTemplate" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/berichte/kpis:
    get:
      summary: Get dashboard KPI tiles
      description: Requires berichte:reports:read permission.
      tags: [berichte]
      operationId: getBerichteDashboardKPIs
      security: [{ bearerAuth: [] }]
      parameters:
        - name: modules
          in: query
          description: "Comma-separated module list (e.g. finanzen,crm,helpdesk). The list is intersected server-side with the caller's <module>:module:view capabilities; empty means every module the caller may see, and a module they may not see is dropped rather than rejected."
          schema: { type: string }
      responses:
        "200":
          description: Dashboard KPI tiles
          content:
            application/json:
              schema:
                type: object
                properties:
                  kpis:
                    type: array
                    items: { $ref: "#/components/schemas/BerichtKPI" }
                  generated_at:
                    $ref: "#/components/schemas/ProtoTimestamp"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  # === openapi-drift sweep: documents ===
  # ==========================================================================
  # Folders
  # ==========================================================================
  /api/v1/documents/folders:
    get:
      summary: List document folders
      description: Requires documents:read permission.
      tags: [documents]
      operationId: listDocumentFolders
      security: [{ bearerAuth: [] }]
      parameters:
        - name: parent_id
          in: query
          description: Filter by parent folder ID
          schema: { type: string, format: uuid }
        - name: space_type
          in: query
          description: Unrecognized values are treated as unspecified (no filter).
          schema: { type: string, enum: [personal, team, project] }
        - name: space_id
          in: query
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: Folder list (protojson — created_at/updated_at as RFC3339 strings)
          content:
            application/json:
              schema:
                type: object
                properties:
                  folders:
                    type: array
                    items:
                      allOf:
                        - $ref: "#/components/schemas/DocumentFolder"
                        - type: object
                          properties:
                            created_at: { type: string, format: date-time }
                            updated_at: { type: string, format: date-time }
                  total: { type: integer }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      summary: Create a document folder
      description: Requires documents:write permission.
      tags: [documents]
      operationId: createDocumentFolder
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name:
                  type: string
                parent_id:
                  type: string
                  format: uuid
                  description: Omit or empty for a root folder
                space_type:
                  type: string
                  enum: [personal, team, project]
                space_id:
                  type: string
                  format: uuid
                icon:
                  type: string
      responses:
        "201":
          description: Folder created
          content:
            application/json:
              schema:
                type: object
                properties:
                  folder: { $ref: "#/components/schemas/DocumentFolder" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/documents/folders/{id}:
    get:
      summary: Get a document folder by ID
      description: Requires documents:read permission.
      tags: [documents]
      operationId: getDocumentFolder
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Folder
          content:
            application/json:
              schema:
                type: object
                properties:
                  folder: { $ref: "#/components/schemas/DocumentFolder" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    put:
      summary: Update a document folder
      description: Requires documents:write permission. Setting parent_id moves the folder.
      tags: [documents]
      operationId: updateDocumentFolder
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                parent_id:
                  type: string
                  format: uuid
                  description: Move the folder under a new parent
                icon:
                  type: string
      responses:
        "200":
          description: Folder updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  folder: { $ref: "#/components/schemas/DocumentFolder" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      summary: Delete a document folder
      description: Requires documents:delete permission.
      tags: [documents]
      operationId: deleteDocumentFolder
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Folder deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  deleted: { type: boolean, example: true }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/documents/folders/{id}/path:
    get:
      summary: Get breadcrumb path segments for a folder
      description: Requires documents:read permission. Returns root-to-leaf breadcrumb segments.
      tags: [documents]
      operationId: getDocumentFolderPath
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Breadcrumb segments
          content:
            application/json:
              schema:
                type: object
                properties:
                  segments:
                    type: array
                    items:
                      $ref: "#/components/schemas/DocumentFolderPathSegment"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/documents/folders/initialize-user:
    post:
      summary: Initialize a user's personal document space
      description: >-
        Requires documents:write permission. Idempotent — safe to call repeatedly. Body is
        optional; when omitted (or user_id absent), defaults to the authenticated user's ID
        from the JWT. The created root folder is not returned in the response.
      tags: [documents]
      operationId: initializeUserDocumentSpace
      security: [{ bearerAuth: [] }]
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                user_id:
                  type: string
                  format: uuid
      responses:
        "200":
          description: User space initialized
          content:
            application/json:
              schema:
                type: object
                properties:
                  initialized: { type: boolean, example: true }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/documents/folders/initialize-team:
    post:
      summary: Initialize a team's document space
      description: >-
        Requires documents:write permission. A JSON body is required (an empty object is
        valid); team_id itself is optional. The created root folder is not returned in the
        response.
      tags: [documents]
      operationId: initializeTeamDocumentSpace
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                team_id:
                  type: string
                  format: uuid
      responses:
        "200":
          description: Team space initialized
          content:
            application/json:
              schema:
                type: object
                properties:
                  initialized: { type: boolean, example: true }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  # ==========================================================================
  # Files
  # ==========================================================================
  /api/v1/documents/files:
    get:
      summary: List document files
      description: Requires documents:read permission.
      tags: [documents]
      operationId: listDocumentFiles
      security: [{ bearerAuth: [] }]
      parameters:
        - name: folder_id
          in: query
          schema: { type: string, format: uuid }
        - name: owner_id
          in: query
          schema: { type: string, format: uuid }
        - name: sort
          in: query
          description: Unrecognized values fall back to unsorted.
          schema: { type: string, enum: [name, size, date, type] }
        - name: sort_dir
          in: query
          schema: { type: string, enum: [asc, desc] }
        - name: favorites
          in: query
          description: Only the exact string "true" activates the is_favorite filter.
          schema: { type: string, example: "true" }
        - name: page
          in: query
          schema: { type: integer, minimum: 1, default: 1 }
        - name: page_size
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 50 }
      responses:
        "200":
          description: Paginated file list
          content:
            application/json:
              schema:
                type: object
                properties:
                  files:
                    type: array
                    items: { $ref: "#/components/schemas/DocumentFile" }
                  total: { type: integer }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      summary: Register metadata for a browser-uploaded file
      description: >-
        Requires documents:write permission. Registers metadata for a file already uploaded
        directly to object storage via a presigned PUT URL (POST /api/v1/files/presign-upload
        → PUT to MinIO → register here). Does not touch object storage itself. Owner is taken
        from the JWT.
      tags: [documents]
      operationId: registerUploadedDocumentFile
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [folder_id, filename, file_size, storage_key]
              properties:
                folder_id:
                  type: string
                  format: uuid
                filename:
                  type: string
                mime_type:
                  type: string
                file_size:
                  type: integer
                  format: int64
                  minimum: 1
                storage_key:
                  type: string
                  description: Object key returned by the presigned upload
      responses:
        "201":
          description: File registered
          content:
            application/json:
              schema:
                type: object
                properties:
                  file: { $ref: "#/components/schemas/DocumentFile" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/documents/files/upload:
    post:
      summary: Upload a file directly into the document store
      description: >-
        Requires documents:write permission. Unlike POST /api/v1/documents/files
        (which only registers metadata for bytes already in MinIO via a
        presigned PUT), this route streams the file itself through the
        gateway. Intended for small server-generated files (e.g. a report
        PDF) where a presign round-trip is unnecessary overhead. folder_id
        and, if given, tag_id must belong to the caller's tenant. Owner is
        taken from the JWT.
      tags: [documents]
      operationId: uploadDocumentFile
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file, folder_id]
              properties:
                file:
                  type: string
                  format: binary
                  description: Document file (max 50 MiB, allowlisted content types only)
                folder_id:
                  type: string
                  format: uuid
                tag_id:
                  type: string
                  format: uuid
                  description: Optional — tag applied to the file after upload
      responses:
        "201":
          description: File uploaded
          content:
            application/json:
              schema:
                type: object
                properties:
                  file: { $ref: "#/components/schemas/DocumentFile" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "413":
          description: File exceeds the 50 MiB limit
        "415":
          description: Content type not in the document upload allowlist

  /api/v1/documents/files/{id}:
    get:
      summary: Get a document file by ID
      description: Requires documents:read permission.
      tags: [documents]
      operationId: getDocumentFile
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: File
          content:
            application/json:
              schema:
                type: object
                properties:
                  file: { $ref: "#/components/schemas/DocumentFile" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    put:
      summary: Update a document file
      description: Requires documents:write permission. filename renames; folder_id moves; is_favorite toggles the favorite flag.
      tags: [documents]
      operationId: updateDocumentFile
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filename:
                  type: string
                folder_id:
                  type: string
                  format: uuid
                is_favorite:
                  type: boolean
      responses:
        "200":
          description: File updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  file: { $ref: "#/components/schemas/DocumentFile" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      summary: Delete a document file
      description: Requires documents:delete permission.
      tags: [documents]
      operationId: deleteDocumentFile
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: File deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  deleted: { type: boolean, example: true }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/documents/files/{id}/copy:
    post:
      summary: Copy a document file to another folder
      description: Requires documents:write permission.
      tags: [documents]
      operationId: copyDocumentFile
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [target_folder_id]
              properties:
                target_folder_id:
                  type: string
                  format: uuid
      responses:
        "201":
          description: File copied
          content:
            application/json:
              schema:
                type: object
                properties:
                  file: { $ref: "#/components/schemas/DocumentFile" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/documents/files/{id}/move:
    post:
      summary: Move a document file to another folder
      description: Requires documents:write permission.
      tags: [documents]
      operationId: moveDocumentFile
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [target_folder_id]
              properties:
                target_folder_id:
                  type: string
                  format: uuid
      responses:
        "200":
          description: File moved
          content:
            application/json:
              schema:
                type: object
                properties:
                  file: { $ref: "#/components/schemas/DocumentFile" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/documents/files/{id}/download-url:
    get:
      summary: Get a download URL for a document file
      description: Requires documents:read permission.
      tags: [documents]
      operationId: getDocumentFileDownloadUrl
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Presigned download URL and file metadata
          content:
            application/json:
              schema:
                type: object
                properties:
                  download_url: { type: string, format: uri }
                  filename: { type: string }
                  content_type: { type: string }
                  file_size: { type: integer, format: int64 }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/documents/files/{id}/activity:
    get:
      summary: Get the activity history of a document file
      description: >-
        Requires documents:read permission. Append-only audit trail: uploaded, renamed, moved,
        copied, downloaded, shared, version_created, reverted — newest first.
      tags: [documents]
      operationId: listDocumentFileActivity
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Activity history (newest first)
          content:
            application/json:
              schema:
                type: object
                properties:
                  activities:
                    type: array
                    items:
                      $ref: "#/components/schemas/DocumentFileActivity"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/documents/files/{id}/comments:
    get:
      summary: List comments on a document file
      description: Requires documents:read permission. Oldest first.
      tags: [documents]
      operationId: listDocumentFileComments
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Comment list (oldest first)
          content:
            application/json:
              schema:
                type: object
                properties:
                  comments:
                    type: array
                    items:
                      $ref: "#/components/schemas/DocumentFileComment"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      summary: Comment on a document file
      description: Requires documents:write permission. The author is taken from the auth context.
      tags: [documents]
      operationId: createDocumentFileComment
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [content]
              properties:
                content:
                  type: string
                  maxLength: 10000
      responses:
        "201":
          description: Comment created
          content:
            application/json:
              schema:
                type: object
                properties:
                  comment: { $ref: "#/components/schemas/DocumentFileComment" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/documents/files/{id}/share-links:
    get:
      summary: List external share links for a document file
      description: Requires documents:read permission. Newest first.
      tags: [documents]
      operationId: listDocumentShareLinks
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Share link list (newest first)
          content:
            application/json:
              schema:
                type: object
                properties:
                  share_links:
                    type: array
                    items:
                      $ref: "#/components/schemas/DocumentShareLink"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      summary: Create an external share link for a document file
      description: >-
        Requires documents:write permission plus documents:share_link:create. The password, if
        given, is bcrypt-hashed and never stored or returned in clear.
      tags: [documents]
      operationId: createDocumentShareLink
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                expires_in_days:
                  type: integer
                  nullable: true
                  description: Absent, null or 0 means the link never expires. Max 365.
                password:
                  type: string
                  maxLength: 72
                  description: Absent or empty means no password.
      responses:
        "201":
          description: Share link created
          content:
            application/json:
              schema:
                type: object
                properties:
                  share_link: { $ref: "#/components/schemas/DocumentShareLink" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/documents/share-links/{id}:
    delete:
      summary: Revoke an external document share link
      description: >-
        Requires documents:write permission plus documents:share:manage. Revocation is immediate;
        the next redemption attempt gets the same generic "invalid" answer an unknown token would.
      tags: [documents]
      operationId: revokeDocumentShareLink
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Share link revoked
          content:
            application/json:
              schema:
                type: object
                properties:
                  revoked: { type: boolean, example: true }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/public/documents/share/{token}:
    post:
      summary: Read/download a shared document file (public, unauthenticated)
      description: >
        No authentication. The share token in the path is the entire credential; the tenant is
        resolved from it server-side and the response can only ever be the one file the link
        names.


        It is a POST rather than a GET because the password must not travel in a URL — where it
        would land in access logs, browser history and Referer headers — and because the call
        increments the link's view counter, which no prefetching GET should be free to do. The
        request body is optional: a link without a password is read with no body at all.


        Unknown token, revoked link, expired link, missing password and wrong password all answer
        404 with the identical message — deliberately indistinguishable, stricter than the
        berichte share-token route, which tells "not found" and "wrong password" apart by status
        code. This route is behind the strict per-IP public rate limit (PUBLIC_RATE_LIMIT_RPS),
        not the authenticated one.
      tags: [documents]
      operationId: getPublicDocumentSharedFile
      security: []
      parameters:
        - name: token
          in: path
          required: true
          schema: { type: string }
          description: The share link secret (43-char base64url).
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                password:
                  type: string
                  description: Required only if the link is password-protected.
      responses:
        "200":
          description: A short-lived presigned download for the shared file
          content:
            application/json:
              schema:
                type: object
                properties:
                  download_url: { type: string }
                  filename: { type: string }
                  content_type: { type: string }
                  file_size: { type: integer }
        "400": { $ref: "#/components/responses/BadRequest" }
        "404":
          description: No such link, or it is expired, revoked, or password-rejected
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorResponse" }
        "429": { $ref: "#/components/responses/TooManyRequests" }
        "503": { $ref: "#/components/responses/ServiceUnavailable" }

  /api/v1/documents/comments/{id}:
    put:
      summary: Update a document file comment
      description: Requires documents:write permission. Only the comment's author may edit it.
      tags: [documents]
      operationId: updateDocumentFileComment
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [content]
              properties:
                content:
                  type: string
                  maxLength: 10000
      responses:
        "200":
          description: Comment updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  comment: { $ref: "#/components/schemas/DocumentFileComment" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      summary: Delete a document file comment
      description: Requires documents:write permission. Only the comment's author or an admin may delete it.
      tags: [documents]
      operationId: deleteDocumentFileComment
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Comment deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  deleted: { type: boolean, example: true }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/documents/files/{id}/versions:
    get:
      summary: List versions of a document file
      description: Requires documents:read permission.
      tags: [documents]
      operationId: listDocumentFileVersions
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Version history (oldest→newest as stored)
          content:
            application/json:
              schema:
                type: object
                properties:
                  versions:
                    type: array
                    items:
                      $ref: "#/components/schemas/DocumentFileVersion"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/documents/files/{id}/versions/revert:
    post:
      summary: Revert a document file to a previous version
      description: >-
        Requires documents:write permission. Despite the "version" response key, the value is
        the file's now-current DocumentFile object after the revert (not a DocumentFileVersion) —
        see the HandleRevertFileVersion comment in route_document.go.
      tags: [documents]
      operationId: revertDocumentFileVersion
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [version_number]
              properties:
                version_number:
                  type: integer
                  minimum: 1
      responses:
        "200":
          description: File reverted
          content:
            application/json:
              schema:
                type: object
                properties:
                  version: { $ref: "#/components/schemas/DocumentFile" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/documents/files/{id}/links:
    get:
      summary: List CRM entity links for a document file
      description: Requires documents:read permission.
      tags: [documents]
      operationId: listDocumentFileEntityLinks
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Entity links (protojson — created_at as RFC3339 string)
          content:
            application/json:
              schema:
                type: object
                properties:
                  links:
                    type: array
                    items:
                      allOf:
                        - $ref: "#/components/schemas/DocumentEntityLink"
                        - type: object
                          properties:
                            created_at: { type: string, format: date-time }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      summary: Link a document file to a CRM entity
      description: Requires documents:write permission.
      tags: [documents]
      operationId: linkDocumentFileToEntity
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [entity_type, entity_id]
              properties:
                entity_type:
                  type: string
                  description: "Typically contact, company, deal, project, or task (not server-validated against an enum)"
                entity_id:
                  type: string
                  format: uuid
      responses:
        "201":
          description: Link created
          content:
            application/json:
              schema:
                type: object
                properties:
                  link: { $ref: "#/components/schemas/DocumentEntityLink" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      summary: Unlink a document file from a CRM entity
      description: Requires documents:write permission.
      tags: [documents]
      operationId: unlinkDocumentFileFromEntity
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [entity_type, entity_id]
              properties:
                entity_type:
                  type: string
                entity_id:
                  type: string
                  format: uuid
      responses:
        "200":
          description: Link removed
          content:
            application/json:
              schema:
                type: object
                properties:
                  unlinked: { type: boolean, example: true }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/documents/links/{id}:
    delete:
      summary: Revoke a document entity link by its own ID
      description: >-
        Requires documents:write permission. Unlike DELETE
        /documents/files/{id}/links, this deletes by the link's own ID
        without requiring the caller to know which file or entity it
        connects — the desktop entity-link panel only holds the link ID.
      tags: [documents]
      operationId: deleteDocumentEntityLink
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Link removed
          content:
            application/json:
              schema:
                type: object
                properties:
                  unlinked: { type: boolean, example: true }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # ==========================================================================
  # Shares
  # ==========================================================================
  /api/v1/documents/shares:
    post:
      summary: Share a file or folder with another user
      description: Requires documents:write permission.
      tags: [documents]
      operationId: shareDocumentEntity
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [entity_type, entity_id, shared_with_user_id, permission]
              properties:
                entity_type:
                  type: string
                  description: "\"file\" or \"folder\" (not server-validated against an enum)"
                entity_id:
                  type: string
                  format: uuid
                shared_with_user_id:
                  type: string
                  format: uuid
                permission:
                  type: string
                  enum: [read, write]
      responses:
        "201":
          description: Share created
          content:
            application/json:
              schema:
                type: object
                properties:
                  share: { $ref: "#/components/schemas/DocumentShare" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      summary: Revoke a file or folder share
      description: Requires documents:write permission.
      tags: [documents]
      operationId: unshareDocumentEntity
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [entity_type, entity_id, shared_with_user_id]
              properties:
                entity_type:
                  type: string
                entity_id:
                  type: string
                  format: uuid
                shared_with_user_id:
                  type: string
                  format: uuid
      responses:
        "200":
          description: Share revoked
          content:
            application/json:
              schema:
                type: object
                properties:
                  unshared: { type: boolean, example: true }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/documents/shares/entity:
    get:
      summary: List shares for a file or folder
      description: Requires documents:read permission. Both query parameters are required.
      tags: [documents]
      operationId: listDocumentShares
      security: [{ bearerAuth: [] }]
      parameters:
        - name: entity_type
          in: query
          required: true
          schema: { type: string }
        - name: entity_id
          in: query
          required: true
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: Share list (protojson — created_at as RFC3339 string)
          content:
            application/json:
              schema:
                type: object
                properties:
                  shares:
                    type: array
                    items:
                      allOf:
                        - $ref: "#/components/schemas/DocumentShare"
                        - type: object
                          properties:
                            created_at: { type: string, format: date-time }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/documents/shares/shared-with-me:
    get:
      summary: List files and folders shared with the authenticated user
      description: Requires documents:read permission.
      tags: [documents]
      operationId: listDocumentsSharedWithMe
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Files and folders shared with the caller
          content:
            application/json:
              schema:
                type: object
                properties:
                  files:
                    type: array
                    items: { $ref: "#/components/schemas/DocumentFile" }
                  folders:
                    type: array
                    items: { $ref: "#/components/schemas/DocumentFolder" }
                  total: { type: integer }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  # ==========================================================================
  # Tags
  # ==========================================================================
  /api/v1/documents/tags:
    get:
      summary: List document tags
      description: Requires documents:read permission.
      tags: [documents]
      operationId: listDocumentTags
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Tag list (protojson — created_at as RFC3339 string)
          content:
            application/json:
              schema:
                type: object
                properties:
                  tags:
                    type: array
                    items:
                      allOf:
                        - $ref: "#/components/schemas/DocumentTag"
                        - type: object
                          properties:
                            created_at: { type: string, format: date-time }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      summary: Create a document tag
      description: Requires documents:write permission.
      tags: [documents]
      operationId: createDocumentTag
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name:
                  type: string
                color:
                  type: string
      responses:
        "201":
          description: Tag created
          content:
            application/json:
              schema:
                type: object
                properties:
                  tag: { $ref: "#/components/schemas/DocumentTag" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/documents/tags/{id}:
    delete:
      summary: Delete a document tag
      description: Requires documents:delete permission.
      tags: [documents]
      operationId: deleteDocumentTag
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Tag deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  deleted: { type: boolean, example: true }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/documents/tags/file:
    post:
      summary: Apply a tag to a document file
      description: Requires documents:write permission.
      tags: [documents]
      operationId: tagDocumentFile
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [file_id, tag_id]
              properties:
                file_id:
                  type: string
                  format: uuid
                tag_id:
                  type: string
                  format: uuid
      responses:
        "200":
          description: File tagged
          content:
            application/json:
              schema:
                type: object
                properties:
                  tagged: { type: boolean, example: true }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      summary: Remove a tag from a document file
      description: Requires documents:write permission.
      tags: [documents]
      operationId: untagDocumentFile
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [file_id, tag_id]
              properties:
                file_id:
                  type: string
                  format: uuid
                tag_id:
                  type: string
                  format: uuid
      responses:
        "200":
          description: Tag removed from file
          content:
            application/json:
              schema:
                type: object
                properties:
                  untagged: { type: boolean, example: true }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # ==========================================================================
  # Search
  # ==========================================================================
  /api/v1/documents/search:
    get:
      summary: Full-text search document files
      description: Requires documents:read permission. The q query parameter is required.
      tags: [documents]
      operationId: searchDocumentFiles
      security: [{ bearerAuth: [] }]
      parameters:
        - name: q
          in: query
          required: true
          schema: { type: string }
        - name: folder_id
          in: query
          description: Scope the search to a folder
          schema: { type: string, format: uuid }
        - name: tag_ids
          in: query
          style: form
          explode: true
          schema:
            type: array
            items: { type: string, format: uuid }
        - $ref: "#/components/parameters/page"
        - $ref: "#/components/parameters/page_size"
      responses:
        "200":
          description: Search results
          content:
            application/json:
              schema:
                type: object
                properties:
                  results:
                    type: array
                    items: { $ref: "#/components/schemas/DocumentFileSearchResult" }
                  total: { type: integer }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  # ==========================================================================
  # Virtual Files
  # ==========================================================================
  /api/v1/documents/virtual:
    get:
      summary: List virtual files (attachments from chat, email, task)
      description: Requires documents:read permission.
      tags: [documents]
      operationId: listVirtualDocumentFiles
      security: [{ bearerAuth: [] }]
      parameters:
        - name: source
          in: query
          schema: { type: string, enum: [chat, email, task] }
        - name: page
          in: query
          schema: { type: integer, minimum: 1, default: 1 }
        - name: page_size
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 50 }
      responses:
        "200":
          description: Paginated virtual file list
          content:
            application/json:
              schema:
                type: object
                properties:
                  files:
                    type: array
                    items: { $ref: "#/components/schemas/DocumentVirtualFile" }
                  total: { type: integer }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  # ==========================================================================
  # WOPI (OnlyOffice) — token generation, not the bare WOPI protocol endpoints
  # ==========================================================================
  /api/v1/documents/wopi/token:
    post:
      summary: Generate a WOPI access token for a document file
      description: >-
        Requires documents:write permission. This is an authenticated token-issuance endpoint,
        not part of the bare WOPI protocol surface used by the OnlyOffice server (those live
        under /wopi/* and are out of scope here).
      tags: [documents]
      operationId: generateDocumentWopiToken
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [file_id]
              properties:
                file_id:
                  type: string
                  format: uuid
      responses:
        "200":
          description: WOPI access token
          content:
            application/json:
              schema:
                type: object
                properties:
                  access_token:
                    type: string
                  access_token_ttl:
                    type: integer
                    format: int64
                    description: Token lifetime in seconds
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/documents/wopi/discovery:
    get:
      summary: Get the OnlyOffice WOPI discovery actions
      description: Requires documents:read permission.
      tags: [documents]
      operationId: getDocumentWopiDiscovery
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: WOPI actions (protojson bare array)
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/DocumentWopiAction"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  # === openapi-drift sweep: plugins ===
  /api/v1/plugins/manifests:
    get:
      summary: List plugin manifests
      tags: [plugins]
      operationId: listPluginManifests
      security: [{ bearerAuth: [] }]
      parameters:
        - name: type
          in: query
          description: Optional filter on plugin_type (config or wasm).
          schema: { type: string, enum: [config, wasm] }
      responses:
        "200":
          description: Manifest list
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/PluginManifest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
    post:
      summary: Create a plugin manifest (admin-only)
      tags: [plugins]
      operationId: createPluginManifest
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [slug, name]
              properties:
                slug: { type: string, description: Unique plugin slug. }
                name: { type: string }
                description: { type: string }
                version: { type: string, default: "0.1.0" }
                author: { type: string }
                settings_schema: { type: string, description: "JSON Schema for the plugin's settings, encoded as a string." }
                permissions:
                  type: array
                  items: { type: string }
                hook_registrations:
                  type: array
                  items: { $ref: "#/components/schemas/PluginHookRegistration" }
                wasm_binary:
                  type: string
                  format: byte
                  description: Base64-encoded WASM binary. Ignored unless plugin_type is wasm.
                plugin_type:
                  type: string
                  enum: [config, wasm]
                  default: config
                  description: >-
                    wasm manifests are rejected with 400 while the plugins.wasm feature flag
                    is off (default in production — WASM plugin execution is compiled out via
                    the no_wasm build tag).
      responses:
        "201":
          description: Manifest created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/PluginManifest" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/plugins/manifests/{manifest_id}:
    get:
      summary: Get a plugin manifest by ID (admin-only)
      tags: [plugins]
      operationId: getPluginManifest
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: manifest_id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Manifest
          content:
            application/json:
              schema: { $ref: "#/components/schemas/PluginManifest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      summary: Delete a plugin manifest (admin-only)
      description: >-
        Only manifests the calling tenant created itself can be deleted.
        Manifests shipped with the product are readable by every tenant and
        owned by none; deleting one answers 403.
      tags: [plugins]
      operationId: deletePluginManifest
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: manifest_id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Manifest deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/plugins/installations:
    get:
      summary: List plugin installations for the current tenant
      tags: [plugins]
      operationId: listPluginInstallations
      security: [{ bearerAuth: [] }]
      parameters:
        - name: status
          in: query
          description: Optional filter on installation status.
          schema: { type: string, enum: [pending_approval, active, disabled, error, uninstalled] }
      responses:
        "200":
          description: Installation list
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/PluginInstallation" }
        "401": { $ref: "#/components/responses/Unauthorized" }
    post:
      summary: Install a plugin for a tenant (admin-only)
      tags: [plugins]
      operationId: installPlugin
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [tenant_id, manifest_id, installed_by]
              properties:
                tenant_id: { type: string, format: uuid }
                manifest_id: { type: string, format: uuid }
                installed_by: { type: string, format: uuid }
      responses:
        "201":
          description: Installation created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/PluginInstallation" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /api/v1/plugins/installations/{installation_id}:
    get:
      summary: Get a plugin installation by ID (admin-only)
      tags: [plugins]
      operationId: getPluginInstallation
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: installation_id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Installation
          content:
            application/json:
              schema: { $ref: "#/components/schemas/PluginInstallation" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      summary: Uninstall a plugin (admin-only)
      tags: [plugins]
      operationId: uninstallPlugin
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: installation_id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Plugin uninstalled
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/plugins/installations/{installation_id}/enable:
    post:
      summary: Enable a plugin installation (admin-only)
      tags: [plugins]
      operationId: enablePluginInstallation
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: installation_id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Installation enabled
          content:
            application/json:
              schema: { $ref: "#/components/schemas/PluginInstallation" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/plugins/installations/{installation_id}/disable:
    post:
      summary: Disable a plugin installation (admin-only)
      tags: [plugins]
      operationId: disablePluginInstallation
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: installation_id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Installation disabled
          content:
            application/json:
              schema: { $ref: "#/components/schemas/PluginInstallation" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/plugins/installations/{installation_id}/permissions:
    get:
      summary: List permissions granted to a plugin installation (admin-only)
      tags: [plugins]
      operationId: listGrantedPluginPermissions
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: installation_id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Granted permission keys
          content:
            application/json:
              schema:
                type: array
                items: { type: string }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      summary: Approve requested permissions for a plugin installation (admin-only)
      tags: [plugins]
      operationId: approvePluginPermissions
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: installation_id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [permissions, granted_by]
              properties:
                permissions:
                  type: array
                  minItems: 1
                  items: { type: string }
                granted_by: { type: string, format: uuid }
      responses:
        "200":
          description: Permissions approved
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/plugins/installations/{installation_id}/settings:
    get:
      summary: Get a plugin installation's settings (admin-only)
      tags: [plugins]
      operationId: getPluginSettings
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: installation_id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: >-
            Settings JSON object. The handler writes the stored settings
            column through unchanged (Content-Type: application/json), so the
            body shape is whatever JSON the plugin's settings_schema defines.
          content:
            application/json:
              schema: { type: object, additionalProperties: true }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    put:
      summary: Update a plugin installation's settings (admin-only)
      tags: [plugins]
      operationId: updatePluginSettings
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: installation_id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        description: Arbitrary JSON object; accepted as-is and stored as the installation's settings.
        content:
          application/json:
            schema: { type: object, additionalProperties: true }
      responses:
        "200":
          description: Settings updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/plugins/installations/{installation_id}/settings/schema:
    get:
      summary: Get the JSON Schema for a plugin installation's settings (admin-only)
      tags: [plugins]
      operationId: getPluginSettingsSchema
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: installation_id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: >-
            JSON Schema document, written through unchanged from the stored
            manifest settings_schema (Content-Type: application/json).
          content:
            application/json:
              schema: { type: object, additionalProperties: true }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/plugins/validation-rules:
    get:
      summary: List validation rules for the current tenant (admin-only)
      tags: [plugins]
      operationId: listPluginValidationRules
      security: [{ bearerAuth: [] }]
      parameters:
        - name: entity_type
          in: query
          description: Optional filter, e.g. contact, deal, invoice.
          schema: { type: string }
      responses:
        "200":
          description: Validation rule list
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/PluginValidationRule" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      summary: Create a validation rule (admin-only)
      tags: [plugins]
      operationId: createPluginValidationRule
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [tenant_id, name, entity_type, field_name, rule_type]
              properties:
                tenant_id: { type: string, format: uuid }
                installation_id: { type: string, format: uuid, description: Optional owning plugin installation. }
                name: { type: string }
                description: { type: string }
                entity_type: { type: string }
                field_name: { type: string }
                rule_type: { type: string, enum: [regex, range, required_if, format, custom, enum] }
                rule_config: { type: string, description: "Rule-type-specific configuration, encoded as a JSON string." }
                error_message: { type: string }
                priority: { type: integer, format: int32 }
      responses:
        "201":
          description: Validation rule created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/PluginValidationRule" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/plugins/validation-rules/{rule_id}:
    put:
      summary: Update a validation rule (admin-only)
      tags: [plugins]
      operationId: updatePluginValidationRule
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: rule_id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name: { type: string }
                description: { type: string }
                rule_config: { type: string, description: "Rule-type-specific configuration, encoded as a JSON string." }
                error_message: { type: string }
                priority: { type: integer, format: int32 }
                enabled: { type: boolean }
      responses:
        "200":
          description: Validation rule updated
          content:
            application/json:
              schema: { $ref: "#/components/schemas/PluginValidationRule" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      summary: Delete a validation rule (admin-only)
      tags: [plugins]
      operationId: deletePluginValidationRule
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: rule_id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Validation rule deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/plugins/workflow-rules:
    get:
      summary: List workflow rules for the current tenant (admin-only)
      tags: [plugins]
      operationId: listPluginWorkflowRules
      security: [{ bearerAuth: [] }]
      parameters:
        - name: trigger_event
          in: query
          description: "Optional filter, e.g. contact.created, deal.stage_changed."
          schema: { type: string }
      responses:
        "200":
          description: Workflow rule list
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/PluginWorkflowRule" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      summary: Create a workflow rule (admin-only)
      tags: [plugins]
      operationId: createPluginWorkflowRule
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [tenant_id, name, trigger_event]
              properties:
                tenant_id: { type: string, format: uuid }
                installation_id: { type: string, format: uuid, description: Optional owning plugin installation. }
                name: { type: string }
                description: { type: string }
                trigger_event: { type: string }
                conditions: { type: string, description: "Trigger conditions, encoded as a JSON array string." }
                actions: { type: string, description: "Actions to run when conditions match, encoded as a JSON array string." }
                priority: { type: integer, format: int32 }
      responses:
        "201":
          description: Workflow rule created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/PluginWorkflowRule" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/plugins/workflow-rules/{rule_id}:
    put:
      summary: Update a workflow rule (admin-only)
      tags: [plugins]
      operationId: updatePluginWorkflowRule
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: rule_id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name: { type: string }
                description: { type: string }
                conditions: { type: string, description: "Trigger conditions, encoded as a JSON array string." }
                actions: { type: string, description: "Actions to run when conditions match, encoded as a JSON array string." }
                priority: { type: integer, format: int32 }
                enabled: { type: boolean }
      responses:
        "200":
          description: Workflow rule updated
          content:
            application/json:
              schema: { $ref: "#/components/schemas/PluginWorkflowRule" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      summary: Delete a workflow rule (admin-only)
      tags: [plugins]
      operationId: deletePluginWorkflowRule
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: rule_id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Workflow rule deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/plugins/templates:
    get:
      summary: List industry templates
      tags: [plugins]
      operationId: listPluginIndustryTemplates
      security: [{ bearerAuth: [] }]
      parameters:
        - name: industry
          in: query
          description: Optional filter, e.g. handwerk, dienstleister.
          schema: { type: string }
      responses:
        "200":
          description: Industry template list
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/PluginIndustryTemplate" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/plugins/templates/{template_id}/apply:
    post:
      summary: Apply an industry template to the current tenant (admin-only)
      tags: [plugins]
      operationId: applyPluginIndustryTemplate
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: template_id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [applied_by]
              properties:
                applied_by: { type: string, format: uuid }
      responses:
        "200":
          description: Template applied
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean }
                  installation_id:
                    type: string
                    format: uuid
                    description: Set if the template created a plugin installation.
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/plugins/execution-logs:
    get:
      summary: List plugin hook execution logs for the current tenant (admin-only)
      tags: [plugins]
      operationId: listPluginExecutionLogs
      security: [{ bearerAuth: [] }]
      parameters:
        - name: installation_id
          in: query
          description: Optional filter by plugin installation.
          schema: { type: string, format: uuid }
        - name: limit
          in: query
          description: Max rows to return. Defaults to 100 server-side; non-numeric values fall back to the default.
          schema: { type: integer, default: 100 }
      responses:
        "200":
          description: Execution log list
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/PluginExecutionLog" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  # === openapi-drift sweep: produktion-ext ===
  # --- Produktion: BOMs, Work Steps, Machines, Quality Checks ---
  # route_produktion_ext.go. All handlers call response.JSON(w, status, resp)
  # on the raw gRPC response struct (encoding/json, not protojson): snake_case
  # field names (from the pb.go json tags), and google.protobuf.Timestamp
  # fields serialize as {seconds,nanos} -> $ref ProtoTimestamp, NOT RFC3339.

  /api/v1/produktion/boms:
    get:
      summary: List BOMs (Stücklisten)
      tags: [produktion]
      operationId: listProductionBOMs
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/page"
        - $ref: "#/components/parameters/page_size"
      responses:
        "200":
          description: Paginated BOM list
          content:
            application/json:
              schema:
                type: object
                properties:
                  boms:
                    type: array
                    items: { $ref: "#/components/schemas/ProduktionBom" }
                  total: { type: integer }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      summary: Create a BOM
      tags: [produktion]
      operationId: createProductionBOM
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [product_name, sku]
              properties:
                product_name: { type: string }
                sku: { type: string }
                version: { type: string }
                active: { type: boolean }
                notes: { type: string }
                items:
                  type: array
                  items:
                    type: object
                    properties:
                      material_name: { type: string }
                      quantity: { type: number }
                      unit: { type: string }
                      sort_order: { type: integer }
      responses:
        "201":
          description: BOM created
          content:
            application/json:
              schema:
                type: object
                properties:
                  bom: { $ref: "#/components/schemas/ProduktionBom" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /api/v1/produktion/boms/{bomId}:
    get:
      summary: Get a BOM by ID
      tags: [produktion]
      operationId: getProductionBOM
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: bomId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: BOM
          content:
            application/json:
              schema:
                type: object
                properties:
                  bom: { $ref: "#/components/schemas/ProduktionBom" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      summary: Update a BOM
      tags: [produktion]
      operationId: updateProductionBOM
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: bomId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                product_name: { type: string }
                sku: { type: string }
                version: { type: string }
                active: { type: boolean }
                notes: { type: string }
      responses:
        "200":
          description: BOM updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  bom: { $ref: "#/components/schemas/ProduktionBom" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    delete:
      summary: Delete a BOM
      tags: [produktion]
      operationId: deleteProductionBOM
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: bomId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "204":
          description: Deleted
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/produktion/orders/{orderId}/steps:
    get:
      summary: List work steps for a production order
      tags: [produktion]
      operationId: listProductionOrderSteps
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: orderId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Work step list
          content:
            application/json:
              schema:
                type: object
                properties:
                  steps:
                    type: array
                    items: { $ref: "#/components/schemas/ProduktionOrderStep" }
                  total: { type: integer }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      summary: Create a work step on a production order
      tags: [produktion]
      operationId: createProductionOrderStep
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: orderId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                step_nr: { type: integer }
                name: { type: string }
                description: { type: string }
                duration_minutes: { type: integer }
                assignee: { type: string }
      responses:
        "201":
          description: Work step created
          content:
            application/json:
              schema:
                type: object
                properties:
                  step: { $ref: "#/components/schemas/ProduktionOrderStep" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/produktion/orders/{orderId}/steps/{stepId}:
    patch:
      summary: Update a work step
      tags: [produktion]
      operationId: updateProductionOrderStep
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: orderId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: stepId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name: { type: string }
                description: { type: string }
                duration_minutes: { type: integer }
                status:
                  type: string
                  description: "pending, in_progress, completed, or skipped"
                assignee: { type: string }
      responses:
        "200":
          description: Work step updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  step: { $ref: "#/components/schemas/ProduktionOrderStep" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      summary: Delete a work step
      tags: [produktion]
      operationId: deleteProductionOrderStep
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: orderId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: stepId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "204":
          description: Deleted
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/produktion/machines:
    get:
      summary: List machines
      tags: [produktion]
      operationId: listProductionMachines
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/page"
        - $ref: "#/components/parameters/page_size"
        - name: status
          in: query
          description: "Filter by machine status: available, in_use, or maintenance"
          schema: { type: string }
      responses:
        "200":
          description: Paginated machine list
          content:
            application/json:
              schema:
                type: object
                properties:
                  machines:
                    type: array
                    items: { $ref: "#/components/schemas/ProduktionMachine" }
                  total: { type: integer }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      summary: Create a machine
      tags: [produktion]
      operationId: createProductionMachine
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name: { type: string }
                type: { type: string }
                notes: { type: string }
      responses:
        "201":
          description: Machine created
          content:
            application/json:
              schema:
                type: object
                properties:
                  machine: { $ref: "#/components/schemas/ProduktionMachine" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/produktion/machines/{machineId}:
    get:
      summary: Get a machine by ID
      tags: [produktion]
      operationId: getProductionMachine
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: machineId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Machine
          content:
            application/json:
              schema:
                type: object
                properties:
                  machine: { $ref: "#/components/schemas/ProduktionMachine" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      summary: Update a machine
      tags: [produktion]
      operationId: updateProductionMachine
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: machineId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name: { type: string }
                type: { type: string }
                status:
                  type: string
                  description: "available, in_use, or maintenance"
                notes: { type: string }
      responses:
        "200":
          description: Machine updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  machine: { $ref: "#/components/schemas/ProduktionMachine" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      summary: Delete a machine
      tags: [produktion]
      operationId: deleteProductionMachine
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: machineId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "204":
          description: Deleted
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/produktion/quality:
    get:
      summary: List quality checks
      tags: [produktion]
      operationId: listProductionQualityChecks
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/page"
        - $ref: "#/components/parameters/page_size"
        - name: order_id
          in: query
          description: Filter by production order ID
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: Paginated quality check list
          content:
            application/json:
              schema:
                type: object
                properties:
                  checks:
                    type: array
                    items: { $ref: "#/components/schemas/ProduktionQualityCheck" }
                  total: { type: integer }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      summary: Create a quality check
      tags: [produktion]
      operationId: createProductionQualityCheck
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [order_id, inspector]
              properties:
                order_id: { type: string, format: uuid }
                inspector: { type: string }
                checked_at:
                  type: string
                  format: date-time
                  description: "RFC3339; optional, defaults to the server's current time"
                passed: { type: boolean }
                defects_found: { type: integer }
                notes: { type: string }
      responses:
        "201":
          description: Quality check created
          content:
            application/json:
              schema:
                type: object
                properties:
                  check: { $ref: "#/components/schemas/ProduktionQualityCheck" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/produktion/quality/{checkId}:
    get:
      summary: Get a quality check by ID
      tags: [produktion]
      operationId: getProductionQualityCheck
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: checkId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Quality check
          content:
            application/json:
              schema:
                type: object
                properties:
                  check: { $ref: "#/components/schemas/ProduktionQualityCheck" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # === openapi-drift sweep: wp3-integration-datev ===
  # ==========================================================================
  # route_integration.go -- generic Teams/Slack integration admin config,
  # channel mappings, account linking, inbound webhooks.
  # Handler: internal/gateway/route_integration.go (IntegrationRoutes)
  # Responses use response.JSON(w, status, resp) with the RAW proto response
  # struct (NOT response.Proto/protojson) -- google.protobuf.Timestamp fields
  # therefore serialize via encoding/json as the wire shape {seconds, nanos},
  # not RFC3339. See components/schemas/ProtoTimestamp (already defined in the
  # main spec) which is reused below.
  # ==========================================================================

  /api/v1/integrations/configs:
    get:
      summary: List integration configs
      description: Requires admin role.
      tags: [integrations]
      operationId: listIntegrationConfigs
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: All configured integration platforms
          content:
            application/json:
              schema:
                type: object
                properties:
                  configs:
                    type: array
                    items: { $ref: "#/components/schemas/IntegrationConfig" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      summary: Create an integration config
      description: >-
        Requires admin role. platform must be a supported value (currently
        slack or teams) or the service returns 400. The config is created
        inactive (is_active=false) until verified via the test endpoint.
      tags: [integrations]
      operationId: createIntegrationConfig
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [platform, credentials_vault_key]
              properties:
                platform: { type: string, description: "e.g. slack, teams" }
                credentials_vault_key: { type: string, description: "Vault key referencing the platform credentials; the raw secret is never returned" }
                metadata: { type: string, description: "JSON-encoded string with platform-specific metadata; defaults to \"{}\" when omitted" }
      responses:
        "201":
          description: Config created
          content:
            application/json:
              schema:
                type: object
                properties:
                  config: { $ref: "#/components/schemas/IntegrationConfig" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /api/v1/integrations/configs/{platform}:
    get:
      summary: Get an integration config by platform
      description: Requires admin role.
      tags: [integrations]
      operationId: getIntegrationConfig
      security: [{ bearerAuth: [] }]
      parameters:
        - name: platform
          in: path
          required: true
          schema: { type: string }
          description: "Integration platform identifier, e.g. slack or teams"
      responses:
        "200":
          description: Integration config
          content:
            application/json:
              schema:
                type: object
                properties:
                  config: { $ref: "#/components/schemas/IntegrationConfig" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    put:
      summary: Update an integration config
      description: Requires admin role.
      tags: [integrations]
      operationId: updateIntegrationConfig
      security: [{ bearerAuth: [] }]
      parameters:
        - name: platform
          in: path
          required: true
          schema: { type: string }
          description: "Integration platform identifier, e.g. slack or teams"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                is_active: { type: boolean }
                credentials_vault_key: { type: string, nullable: true, description: "Omit/null to keep the existing vault key" }
                metadata: { type: string, description: "JSON-encoded string; defaults to \"{}\" when omitted" }
      responses:
        "200":
          description: Config updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  config: { $ref: "#/components/schemas/IntegrationConfig" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      summary: Delete an integration config
      description: Requires admin role.
      tags: [integrations]
      operationId: deleteIntegrationConfig
      security: [{ bearerAuth: [] }]
      parameters:
        - name: platform
          in: path
          required: true
          schema: { type: string }
          description: "Integration platform identifier, e.g. slack or teams"
      responses:
        "200":
          description: Config deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  status: { type: string, example: config deleted }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/integrations/configs/{platform}/test:
    post:
      summary: Test an integration config
      description: >-
        Requires admin role. Probes the platform with the credentials the
        notification service holds: Slack via auth.test, Teams via a Bot
        Framework client_credentials token request. Neither writes anything into
        a channel. A 200 therefore means the platform actually answered -- there
        is no success=false response; a config that does not exist for this
        tenant is 404, and a platform that refuses or that the server has no
        credentials for is 409 with the reason in the error message.
      tags: [integrations]
      operationId: testIntegrationConfig
      security: [{ bearerAuth: [] }]
      parameters:
        - name: platform
          in: path
          required: true
          schema: { type: string }
          description: "Integration platform identifier, e.g. slack or teams"
      responses:
        "200":
          description: Platform answered the probe
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean, example: true }
                  message:
                    type: string
                    description: Identity the credentials resolved to
                    example: authenticated as cosmi-bot in workspace Zentria
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: >-
            Platform refused the credentials, or the server holds no client for
            this platform
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "503":
          description: Notification service or integration storage unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /api/v1/integrations/configs/{platform}/mappings:
    get:
      summary: List channel mappings for an integration config
      description: Requires admin role.
      tags: [integrations]
      operationId: listIntegrationChannelMappings
      security: [{ bearerAuth: [] }]
      parameters:
        - name: platform
          in: path
          required: true
          schema: { type: string }
          description: "Integration platform identifier, e.g. slack or teams"
      responses:
        "200":
          description: Channel mappings for the platform's config
          content:
            application/json:
              schema:
                type: object
                properties:
                  mappings:
                    type: array
                    items: { $ref: "#/components/schemas/IntegrationChannelMapping" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      summary: Create a channel mapping for an integration config
      description: Requires admin role.
      tags: [integrations]
      operationId: createIntegrationChannelMapping
      security: [{ bearerAuth: [] }]
      parameters:
        - name: platform
          in: path
          required: true
          schema: { type: string }
          description: "Integration platform identifier, e.g. slack or teams"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [channel_id, channel_name, modules]
              properties:
                channel_id: { type: string }
                channel_name: { type: string }
                modules:
                  type: array
                  minItems: 1
                  items: { type: string }
                  description: "KMU Hub module keys routed to this channel (e.g. deals, tasks)"
      responses:
        "201":
          description: Mapping created
          content:
            application/json:
              schema:
                type: object
                properties:
                  mapping: { $ref: "#/components/schemas/IntegrationChannelMapping" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/integrations/mappings/{id}:
    put:
      summary: Update a channel mapping
      description: Requires admin role.
      tags: [integrations]
      operationId: updateIntegrationChannelMapping
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [channel_id, channel_name, modules]
              properties:
                channel_id: { type: string }
                channel_name: { type: string }
                modules:
                  type: array
                  minItems: 1
                  items: { type: string }
                is_active: { type: boolean }
      responses:
        "200":
          description: Mapping updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  mapping: { $ref: "#/components/schemas/IntegrationChannelMapping" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      summary: Delete a channel mapping
      description: Requires admin role.
      tags: [integrations]
      operationId: deleteIntegrationChannelMapping
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Mapping deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  status: { type: string, example: mapping deleted }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/integrations/link:
    post:
      summary: Link the caller's KMU Hub account to an external platform account
      description: >-
        Any authenticated user (no admin role required). token is the raw,
        single-use link token surfaced by the platform bot (Teams `/kmuhub
        link` message or Slack `/kmuhub link` slash command); it is hashed
        server-side for lookup. Fails with 404 if the token is unknown, 409 if
        already used, 504 if expired (DeadlineExceeded mapping).
      tags: [integrations]
      operationId: linkIntegrationAccount
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [token]
              properties:
                token: { type: string, description: "Raw single-use link token from the platform bot" }
      responses:
        "200":
          description: Account linked
          content:
            application/json:
              schema:
                type: object
                properties:
                  platform: { type: string }
                  external_display_name: { type: string }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
        "504":
          description: Link token expired (gRPC DeadlineExceeded mapping)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /api/v1/integrations/link/{platform}:
    delete:
      summary: Unlink the caller's account from an external platform
      description: Any authenticated user (no admin role required).
      tags: [integrations]
      operationId: unlinkIntegrationAccount
      security: [{ bearerAuth: [] }]
      parameters:
        - name: platform
          in: path
          required: true
          schema: { type: string }
          description: "Integration platform identifier, e.g. slack or teams"
      responses:
        "200":
          description: Account unlinked
          content:
            application/json:
              schema:
                type: object
                properties:
                  status: { type: string, example: account unlinked }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/integrations/link/{platform}/status:
    get:
      summary: Get the caller's account link status for a platform
      description: >-
        Any authenticated user (no admin role required). GetAccountLinkStatus is
        called with only user_id (it returns every platform the caller has
        linked); the gateway handler then picks the link matching the platform
        path segment and flattens it into the single-platform shape the frontend
        types as AccountLinkStatus. No link for that platform is a 200 with
        linked=false, not a 404. linked_at is hand-formatted RFC3339 in the
        handler, so it is a genuine timestamp string here (unlike the
        ProtoTimestamp-shaped IntegrationAccountLink schema).
      tags: [integrations]
      operationId: getIntegrationAccountLinkStatus
      security: [{ bearerAuth: [] }]
      parameters:
        - name: platform
          in: path
          required: true
          schema: { type: string }
          description: "Integration platform identifier, e.g. slack or teams"
      responses:
        "200":
          description: Link status of the caller for the requested platform
          content:
            application/json:
              schema:
                type: object
                required: [linked, platform]
                properties:
                  linked: { type: boolean }
                  platform: { type: string, description: "Echo of the path parameter" }
                  external_display_name:
                    type: string
                    description: "Only present when linked is true"
                  linked_at:
                    type: string
                    format: date-time
                    description: "RFC3339. Only present when linked is true and the link carries a timestamp"
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/integrations/teams/webhook:
    post:
      summary: Inbound Microsoft Teams Bot Framework webhook
      description: >-
        Public endpoint (no bearerAuth) called by the Microsoft Bot Framework
        for all bot interactions (messages, Action.Execute invokes). The
        gateway is a pure proxy here: it forwards the raw body and the
        Authorization header to the notification service, which validates the
        Bot Framework JWT and only then reads any data.

        Tenant resolution: the request is unauthenticated, so the tenant comes
        from channelData.tenant.id (the Azure AD tenant of the Teams
        organisation), matched against the `tenant_id` key in
        integration_configs.metadata. An admin sets that key via
        POST /api/v1/integrations/configs (metadata is free-form JSON). Until
        it is set the organisation resolves to no tenant and the activity is
        answered with an explanatory message instead of being processed --
        no tenant is ever guessed.

        Effect: only the acknowledge card action runs (it marks the
        notification read). approve, reject and reply have no counterpart in
        Cosmi yet and are answered as not-yet-executed rather than confirmed.

        Returns 501 if Teams webhooks are not configured on this deployment
        (TEAMS_APP_ID / TEAMS_APP_PASSWORD unset). On JWT verification failure
        the body is PLAIN TEXT "unauthorized" (not the JSON ErrorResponse
        shape).
      tags: [integrations]
      operationId: handleTeamsWebhook
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: "Bot Framework Activity payload (schema.Activity); shape is platform-defined and not modeled here."
      responses:
        "200":
          description: >-
            Activity acknowledged. Body shape varies by activity type: Invoke
            responses return {statusCode, type, value}; Message responses
            return {type, text}; other activity types return an empty 200.
          content:
            application/json:
              schema:
                type: object
        "401":
          description: Bot Framework JWT verification failed (plain text body, not JSON)
          content:
            text/plain:
              schema: { type: string, example: "unauthorized" }
        "501":
          description: Teams webhooks are not configured on this deployment
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorResponse" }

  /api/v1/integrations/slack/interact:
    post:
      summary: Inbound Slack interactive message callback (Block Kit button clicks)
      description: >-
        Public endpoint (no bearerAuth) called by Slack for block_actions
        interaction payloads. The gateway is a pure proxy here: it forwards the
        raw body and the two X-Slack-* headers to the notification service,
        which verifies the signature over the untouched bytes before anything
        is parsed or read. Body is application/x-www-form-urlencoded with a
        single `payload` field containing the JSON-encoded Slack
        InteractionCallback.

        Tenant resolution: the request is unauthenticated, so the tenant comes
        from the Slack workspace id (payload team.id), matched against the
        `team_id` key in integration_configs.metadata. An admin sets that key
        via POST /api/v1/integrations/configs (metadata is free-form JSON).
        Until it is set the workspace resolves to no tenant and the user gets
        an explanatory ephemeral message -- no tenant is ever guessed.

        Effect: only the acknowledge action runs (it marks the notification
        read) and only then is the card rewritten with a resolved banner.
        approve, reject and reply have no counterpart in Cosmi yet; they leave
        the card untouched and answer with an ephemeral note.

        Returns 501 if Slack webhooks are not configured on this deployment
        (SLACK_BOT_TOKEN / SLACK_SIGNING_SECRET unset). Signature and parse
        failures return a PLAIN TEXT body (not the JSON ErrorResponse shape).
      tags: [integrations]
      operationId: handleSlackInteraction
      security: []
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              required: [payload]
              properties:
                payload: { type: string, description: "JSON-encoded Slack InteractionCallback" }
      responses:
        "200":
          description: Interaction acknowledged (empty body)
        "400":
          description: Missing payload or malformed JSON (plain text body, not JSON)
          content:
            text/plain:
              schema: { type: string }
        "401":
          description: Slack signature verification failed (plain text body, not JSON)
          content:
            text/plain:
              schema: { type: string, example: "unauthorized" }
        "501":
          description: Slack webhooks are not configured on this deployment
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorResponse" }

  /api/v1/integrations/slack/commands:
    post:
      summary: Inbound Slack slash command (/kmuhub)
      description: >-
        Public endpoint (no bearerAuth) called by Slack for the /kmuhub slash
        command. The gateway is a pure proxy here: it forwards the raw body and
        the two X-Slack-* headers to the notification service, which verifies
        the signature over the untouched bytes before anything is parsed or
        read. Body is application/x-www-form-urlencoded (Slack SlashCommand
        payload: command, text, user_id, team_id, channel_id, ...). Supported
        text subcommands: `link`, `unlink`; anything else returns a help
        message.

        Tenant resolution: the request is unauthenticated, so the tenant comes
        from the `team_id` form field, matched against the `team_id` key in
        integration_configs.metadata. An admin sets that key via
        POST /api/v1/integrations/configs (metadata is free-form JSON). Until
        it is set the workspace resolves to no tenant and the command answers
        with an explanatory ephemeral message -- no tenant is ever guessed, and
        no link token is issued without one.

        Returns 501 if Slack webhooks are not configured on this deployment
        (SLACK_BOT_TOKEN / SLACK_SIGNING_SECRET unset). Signature and parse
        failures return a PLAIN TEXT body (not the JSON ErrorResponse shape).
      tags: [integrations]
      operationId: handleSlackSlashCommand
      security: []
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              description: "Slack SlashCommand payload (command, text, user_id, channel_id, ...)"
      responses:
        "200":
          description: Slack response_type=ephemeral message
          content:
            application/json:
              schema:
                type: object
                properties:
                  response_type: { type: string, example: ephemeral }
                  text: { type: string }
        "400":
          description: Malformed request (plain text body, not JSON)
          content:
            text/plain:
              schema: { type: string }
        "401":
          description: Slack signature verification failed (plain text body, not JSON)
          content:
            text/plain:
              schema: { type: string, example: "unauthorized" }
        "501":
          description: Slack webhooks are not configured on this deployment
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorResponse" }

  /api/v1/integrations/slack/oauth/install:
    get:
      summary: Start the Slack app install (OAuth authorize) redirect -- NOT IMPLEMENTED
      description: >-
        Public endpoint (no bearerAuth), reserved for the Slack app install
        flow. It always answers 501 today, and that is a decision rather than
        missing wiring: the install hands back a per-workspace bot token and
        there is nowhere to keep it -- integration_configs.credentials_vault_key
        is a bare string with no resolver behind it. Until a credential store
        exists, Slack is configured with a single deployment-wide bot token
        from the environment.
      tags: [integrations]
      operationId: slackOAuthInstall
      security: []
      responses:
        "501":
          description: Slack OAuth install is not available on this deployment
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorResponse" }

  /api/v1/integrations/slack/oauth/callback:
    get:
      summary: Slack OAuth install callback -- NOT IMPLEMENTED
      description: >-
        Public endpoint (no bearerAuth), reserved for the Slack app install
        callback. It always answers 501 today, for two reasons that both need
        their own work: the exchanged bot token has nowhere to live (see
        /oauth/install), and the callback arrives unauthenticated, so it would
        additionally need a signed `state` carrying the tenant from a prior
        authenticated install step. Answering 501 is the honest state until
        both exist.
      tags: [integrations]
      operationId: slackOAuthCallback
      security: []
      parameters:
        - name: code
          in: query
          required: false
          schema: { type: string }
          description: "OAuth authorization code issued by Slack (ignored while the flow is disabled)"
      responses:
        "501":
          description: Slack OAuth install is not available on this deployment
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorResponse" }

  # ==========================================================================
  # route_datev_upload.go -- DATEV OAuth connection + Buchungsstapel/Beleg
  # upload + upload config + upload logs.
  # Handler: internal/gateway/route_datev_upload.go (DatevUploadRoutes)
  # Responses are hand-built maps via response.JSON (NOT raw proto structs),
  # so no {seconds,nanos} timestamp quirk here except in
  # ListDatevUploadLogs, where started_at/completed_at go through
  # timestamppb.Timestamp.AsTime() before being encoded -- those ARE genuine
  # RFC3339 strings (type: string, format: date-time).
  # tenant_id is derived server-side from the JWT via getTenantID(r), not a
  # request parameter, for every endpoint below.
  # ==========================================================================

  /api/v1/finance/datev/oauth/callback:
    get:
      summary: DATEV OAuth redirect callback
      description: >-
        Public endpoint (no bearerAuth) -- DATEV redirects here after the
        tenant admin approves the connection. The tenant is recovered from the
        HMAC-signed `state` token (not from the caller's JWT, since this is an
        unauthenticated redirect). Always responds with a 302 redirect back
        into the app; there is no JSON error body on failure, only a
        `datev_error` query parameter on the redirect target.
      tags: [datev]
      operationId: datevOAuthCallback
      security: []
      parameters:
        - name: code
          in: query
          required: false
          schema: { type: string }
          description: "OAuth authorization code issued by DATEV; absent on a denied/failed authorization"
        - name: state
          in: query
          required: false
          schema: { type: string }
          description: "HMAC-signed CSRF state token issued by GET /oauth/authorize, encodes the tenant_id"
        - name: error
          in: query
          required: false
          schema: { type: string }
          description: "Error code from DATEV when the user denies authorization"
        - name: redirect_url
          in: query
          required: false
          schema: { type: string, format: uri }
      responses:
        "302":
          description: >-
            Redirect to /settings/integrations. On success appends
            ?datev_connected=true; on failure appends
            ?datev_error=<missing_authorization_code|state_not_configured|invalid_state|connection_failed|{service error message}>.
          headers:
            Location:
              schema: { type: string, format: uri }

  /api/v1/finance/datev/oauth/authorize:
    get:
      summary: Get the DATEV OAuth authorization URL
      description: >-
        Requires admin role. Issues an HMAC-signed, expiring CSRF state token
        (mirrors the Bexio OAuth flow) embedding the tenant_id, then asks the
        biz service for the DATEV authorization URL. Returns 409 if DATEV
        OAuth is not configured for this deployment (FailedPrecondition
        mapping), 500 if the state secret is not configured.
      tags: [datev]
      operationId: getDatevAuthUrl
      security: [{ bearerAuth: [] }]
      parameters:
        - name: redirect_url
          in: query
          required: false
          schema: { type: string, format: uri }
      responses:
        "200":
          description: Authorization URL
          content:
            application/json:
              schema:
                type: object
                properties:
                  authorization_url: { type: string, format: uri }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /api/v1/finance/datev/disconnect:
    post:
      summary: Disconnect the DATEV integration
      description: Requires admin role. Revokes stored tokens and deactivates the connection.
      tags: [datev]
      operationId: disconnectDatev
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Disconnected
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/finance/datev/status:
    get:
      summary: Get the DATEV connection status
      description: Requires admin role.
      tags: [datev]
      operationId: getDatevConnectionStatus
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Connection status
          content:
            application/json:
              schema:
                type: object
                properties:
                  connected: { type: boolean }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/finance/datev/upload:
    post:
      summary: Trigger a DATEV Buchungsstapel export and upload
      description: >-
        Requires admin role. Renders the tenant's booking batch for the requested
        period (the same rows and EXTF header the GoBD export produces) and
        transfers it to DATEV. Success means the batch reached DATEV; every
        precondition that would produce a meaningless transfer answers 409
        instead: no active DATEV connection, no client number in the upload
        config, no Berater-/Mandantennummer in the company settings, or a period
        without a single bookable document. The period is mandatory.
      tags: [datev]
      operationId: uploadDatevBuchungsstapel
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [start_date, end_date]
              properties:
                start_date: { type: string, description: "YYYY-MM-DD" }
                end_date: { type: string, description: "YYYY-MM-DD" }
                fiscal_year_start:
                  type: string
                  description: "YYYY-MM-DD; defaults to start_date"
      responses:
        "200":
          description: Batch transferred to DATEV
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean }
                  document_count:
                    type: integer
                    description: Invoices and credit notes that produced booking lines (drafts are skipped)
                  file_size: { type: integer, description: CSV size in bytes }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409":
          description: >-
            Upload preconditions unmet (not connected, client number or advisor
            numbers missing, or no bookable document in the period)
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorResponse" }
        "503":
          description: DATEV upload service not available
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorResponse" }

  /api/v1/finance/datev/upload/beleg/{invoice_id}:
    post:
      summary: Upload a single invoice PDF as a DATEV Belegbild
      description: >-
        Requires admin role. Renders the invoice PDF with the same generator the
        finance module serves for download and transfers it as a Belegbild.
        Success means the document reached DATEV. Answers 409 when a
        precondition is unmet (no active DATEV connection, no client number, or
        company settings missing a Pflichtangabe of section 14 UStG, which would
        render a document the tax advisor may not book) and 404 when the invoice
        does not exist in this tenant.
      tags: [datev]
      operationId: uploadDatevBeleg
      security: [{ bearerAuth: [] }]
      parameters:
        - name: invoice_id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: Belegbild transferred to DATEV
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: Upload preconditions unmet (not connected, client number or company settings incomplete)
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorResponse" }
        "503":
          description: DATEV upload service not available
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ErrorResponse" }

  /api/v1/finance/datev/config:
    get:
      summary: Get the DATEV upload configuration
      description: Requires admin role. Returns 404 if no upload config has been saved yet for this tenant.
      tags: [datev]
      operationId: getDatevUploadConfig
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Upload configuration
          content:
            application/json:
              schema:
                type: object
                properties:
                  client_number: { type: string }
                  auto_upload_enabled: { type: boolean }
                  upload_after_export: { type: boolean }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    put:
      summary: Update the DATEV upload configuration
      description: Requires admin role.
      tags: [datev]
      operationId: updateDatevUploadConfig
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                client_number: { type: string }
                auto_upload_enabled: { type: boolean }
                upload_after_export: { type: boolean }
      responses:
        "200":
          description: Configuration updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/finance/datev/upload/logs:
    get:
      summary: List recent DATEV upload log entries
      description: Requires admin role.
      tags: [datev]
      operationId: listDatevUploadLogs
      security: [{ bearerAuth: [] }]
      parameters:
        - name: limit
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 20 }
      responses:
        "200":
          description: Upload log entries, most recent first
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/DatevUploadLogEntry" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  # === openapi-drift sweep: wp4-bexio-lexware ===
  # ==========================================================================
  # route_bexio.go -- Bexio accounting integration (OAuth connect, sync
  # trigger/status/config/logs, field mappings, manual invoice/quote push).
  # Handler: internal/gateway/route_bexio.go (BexioRoutes)
  # Every handler uses response.JSON(w, status, data) on a hand-built map or
  # raw proto struct (NEVER response.Proto/protojson). Where the handler
  # calls .AsTime() on a google.protobuf.Timestamp before putting it in the
  # response map (connected_at, last_*_at, started_at, completed_at), the
  # field IS a genuine RFC3339 string. No Bexio/Lexware handler below returns
  # a raw, un-.AsTime()'d Timestamp, so ProtoTimestamp is not needed here.
  # tenant_id is derived server-side from the JWT via getTenantID(r), not a
  # request parameter, for every admin endpoint below.
  # ==========================================================================

  /api/v1/integrations/bexio/oauth/callback:
    get:
      summary: Bexio OAuth redirect callback
      description: >-
        Public endpoint (no bearerAuth) -- Bexio redirects here after the
        tenant admin approves the connection. The tenant is recovered from
        the HMAC-signed `state` token (not from the caller's JWT, since this
        is an unauthenticated redirect). Responds with a 302 redirect back
        into the app for the missing-code, callback-RPC-error and
        callback-RPC-unsuccessful paths (appends a `bexio_error` query
        param), and for the success path (appends `bexio_connected=true`).
        Two failure paths return a JSON ErrorResponse instead of redirecting:
        an invalid/expired `state` token (400) and a misconfigured
        BEXIO_STATE_SECRET (500, not formally listed below since it signals
        server misconfiguration, not a client error).
      tags: [bexio]
      operationId: bexioOAuthCallback
      security: []
      parameters:
        - name: code
          in: query
          required: false
          schema: { type: string }
          description: "OAuth authorization code issued by Bexio; absent on a denied/failed authorization"
        - name: state
          in: query
          required: false
          schema: { type: string }
          description: "HMAC-signed CSRF state token issued by GET /oauth/authorize, encodes the tenant_id"
        - name: error
          in: query
          required: false
          schema: { type: string }
          description: "Error code from Bexio when the user denies authorization; used as the bexio_error redirect value when code is absent"
      responses:
        "302":
          description: >-
            Redirect to /settings/integrations. On success appends
            ?bexio_connected=true; on failure appends
            ?bexio_error=<missing_authorization_code|{error query param}|connection_failed|{callback RPC error_message}>.
          headers:
            Location:
              schema: { type: string, format: uri }
        "400": { $ref: "#/components/responses/BadRequest" }

  /api/v1/integrations/bexio/oauth/authorize:
    get:
      summary: Get the Bexio OAuth authorization URL
      description: >-
        Requires admin role. Issues an HMAC-signed, expiring CSRF state token
        embedding the tenant_id (mirrors the DATEV OAuth flow), then asks the
        biz service for the Bexio authorization URL. Returns 500 if
        BEXIO_STATE_SECRET is not configured for this deployment.
      tags: [bexio]
      operationId: getBexioAuthUrl
      security: [{ bearerAuth: [] }]
      parameters:
        - name: redirect_url
          in: query
          required: false
          schema: { type: string, format: uri }
      responses:
        "200":
          description: Authorization URL
          content:
            application/json:
              schema:
                type: object
                properties:
                  authorization_url: { type: string, format: uri }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/integrations/bexio/disconnect:
    post:
      summary: Disconnect the Bexio integration
      description: Requires admin role.
      tags: [bexio]
      operationId: disconnectBexio
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Disconnected
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/integrations/bexio/status:
    get:
      summary: Get the Bexio connection status
      description: Requires admin role.
      tags: [bexio]
      operationId: getBexioConnectionStatus
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Connection status
          content:
            application/json:
              schema:
                type: object
                properties:
                  connected: { type: boolean }
                  org_name: { type: string }
                  connected_at:
                    type: string
                    format: date-time
                    description: >-
                      Present only when connected=true. Genuine RFC3339
                      string (handler converts via .AsTime() before
                      JSON-encoding), not the {seconds,nanos} ProtoTimestamp
                      wire shape.
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/integrations/bexio/sync/trigger:
    post:
      summary: Trigger a manual Bexio sync
      description: Requires admin role.
      tags: [bexio]
      operationId: triggerBexioSync
      security: [{ bearerAuth: [] }]
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                sync_type:
                  type: string
                  description: "\"contacts\", \"payments\", \"invoices\", or omitted/empty for all"
      responses:
        "202":
          description: Sync accepted
          content:
            application/json:
              schema:
                type: object
                properties:
                  sync_id: { type: string }
                  status: { type: string }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/integrations/bexio/sync/status:
    get:
      summary: Get the Bexio sync status overview
      description: Requires admin role.
      tags: [bexio]
      operationId: getBexioSyncStatus
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: >-
            Sync status. last_contact_sync_at, last_payment_poll_at and
            last_sync_error_at are genuine RFC3339 strings (handler converts
            via .AsTime() before JSON-encoding), not the {seconds,nanos}
            ProtoTimestamp wire shape.
          content:
            application/json:
              schema:
                type: object
                properties:
                  contact_sync_enabled: { type: boolean }
                  invoice_push_enabled: { type: boolean }
                  quote_push_enabled: { type: boolean }
                  payment_poll_enabled: { type: boolean }
                  invoice_pull_enabled: { type: boolean, description: "Read-only invoice mirror (Bexio -> Cosmi) enabled" }
                  total_contacts_mapped: { type: integer, format: int32 }
                  total_invoices_mapped: { type: integer, format: int32 }
                  total_quotes_mapped: { type: integer, format: int32 }
                  last_contact_sync_at: { type: string, format: date-time, description: "Present only once a contact sync has run" }
                  last_payment_poll_at: { type: string, format: date-time, description: "Present only once a payment poll has run" }
                  last_invoice_pull_at: { type: string, format: date-time, description: "Present only once an invoice pull has run" }
                  last_sync_error: { type: string, description: "Present only when the last sync attempt failed" }
                  last_sync_error_at: { type: string, format: date-time, description: "Present only when the last sync attempt failed" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/integrations/bexio/sync/config:
    put:
      summary: Update Bexio sync flags and polling intervals
      description: Requires admin role.
      tags: [bexio]
      operationId: updateBexioSyncConfig
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                contact_sync_enabled: { type: boolean }
                contact_sync_interval_minutes: { type: integer, format: int32 }
                invoice_push_enabled: { type: boolean }
                quote_push_enabled: { type: boolean }
                payment_poll_enabled: { type: boolean }
                payment_poll_interval_minutes: { type: integer, format: int32 }
                invoice_pull_enabled: { type: boolean }
                invoice_pull_interval_minutes: { type: integer, format: int32 }
      responses:
        "200":
          description: Sync config updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/integrations/bexio/sync/logs:
    get:
      summary: List recent Bexio sync log entries
      description: Requires admin role.
      tags: [bexio]
      operationId: listBexioSyncLogs
      security: [{ bearerAuth: [] }]
      parameters:
        - name: limit
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 20 }
      responses:
        "200":
          description: Sync log entries, most recent first
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/BexioSyncLogEntry" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/integrations/bexio/mappings/{entity_type}:
    get:
      summary: Get Bexio field mappings for an entity type
      description: Requires admin role.
      tags: [bexio]
      operationId: getBexioFieldMappings
      security: [{ bearerAuth: [] }]
      parameters:
        - name: entity_type
          in: path
          required: true
          schema: { type: string }
          description: "e.g. contact, invoice, quote"
      responses:
        "200":
          description: Field mappings for the entity type (bare array, not wrapped in an object)
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/BexioFieldMappingEntry" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    put:
      summary: Save Bexio field mappings for an entity type
      description: Requires admin role.
      tags: [bexio]
      operationId: updateBexioFieldMappings
      security: [{ bearerAuth: [] }]
      parameters:
        - name: entity_type
          in: path
          required: true
          schema: { type: string }
          description: "e.g. contact, invoice, quote"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [mappings]
              properties:
                mappings:
                  type: array
                  minItems: 1
                  items: { $ref: "#/components/schemas/BexioFieldMappingEntry" }
      responses:
        "200":
          description: Field mappings saved
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/integrations/bexio/push/invoice/{invoice_id}:
    post:
      summary: Push an invoice to Bexio
      description: >-
        Requires admin role. Business-level push failures (e.g. invoice not
        found, Bexio API error) do NOT surface as an HTTP error -- the RPC
        returns success=false with a 200 response and error_message set. Only
        a malformed invoice_id (not a valid UUID) produces a 400.
      tags: [bexio]
      operationId: pushBexioInvoice
      security: [{ bearerAuth: [] }]
      parameters:
        - name: invoice_id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: Push result
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean }
                  bexio_id: { type: string, description: "Present only on success" }
                  error_message: { type: string, description: "Present only on failure" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/integrations/bexio/push/quote/{quote_id}:
    post:
      summary: Push a quote to Bexio
      description: >-
        Requires admin role. Business-level push failures (e.g. quote not
        found, Bexio API error) do NOT surface as an HTTP error -- the RPC
        returns success=false with a 200 response and error_message set. Only
        a malformed quote_id (not a valid UUID) produces a 400.
      tags: [bexio]
      operationId: pushBexioQuote
      security: [{ bearerAuth: [] }]
      parameters:
        - name: quote_id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: Push result
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean }
                  bexio_id: { type: string, description: "Present only on success" }
                  error_message: { type: string, description: "Present only on failure" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  # ==========================================================================
  # route_lexware.go -- Lexware accounting integration (API-key connect,
  # inbound webhook events, sync trigger/status/logs, field mappings, manual
  # invoice/quote push).
  # Handler: internal/gateway/route_lexware.go (LexwareRoutes)
  # Same response.JSON-on-map/raw-proto-struct pattern as route_bexio.go
  # above -- see that block's header comment for the .AsTime() timestamp rule.
  # tenant_id is derived server-side from the JWT via getTenantID(r), not a
  # request parameter, for every admin endpoint below.
  # ==========================================================================

  /api/v1/integrations/lexware/webhooks:
    post:
      summary: Inbound Lexware webhook event
      description: >-
        Public endpoint (no bearerAuth) -- called by Lexware (or a
        self-hosted relay) for invoice/quote/payment events. HMAC-SHA256
        signature validation runs when LEXWARE_WEBHOOK_SECRET is configured:
        the `X-Signature` header is checked against the raw request body,
        rejecting with 401 when missing or invalid. When the secret is unset
        in production the request is rejected with 500 (webhook secret not
        configured, not formally listed below since it signals server
        misconfiguration); in dev/staging the check is skipped with a
        warning log instead.
      tags: [lexware]
      operationId: handleLexwareWebhookEvent
      security: []
      parameters:
        - name: X-Signature
          in: header
          required: false
          schema: { type: string }
          description: "Hex-encoded HMAC-SHA256 signature of the raw request body; required when LEXWARE_WEBHOOK_SECRET is configured"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                event_type: { type: string }
                resource_id: { type: string }
                organization_id: { type: string }
      responses:
        "200":
          description: Event processed
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/integrations/lexware/connect:
    post:
      summary: Connect the Lexware integration via API key
      description: >-
        Requires admin role. Stores the API key and activates the
        integration. Returns 400 (JSON ErrorResponse carrying the RPC's
        error_message) when the underlying ConnectLexware RPC reports
        success=false, e.g. an invalid API key.
      tags: [lexware]
      operationId: connectLexware
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [api_key]
              properties:
                api_key: { type: string }
      responses:
        "200":
          description: Connected
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/integrations/lexware/disconnect:
    post:
      summary: Disconnect the Lexware integration
      description: Requires admin role.
      tags: [lexware]
      operationId: disconnectLexware
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Disconnected
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/integrations/lexware/status:
    get:
      summary: Get the Lexware connection status
      description: Requires admin role.
      tags: [lexware]
      operationId: getLexwareConnectionStatus
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Connection status
          content:
            application/json:
              schema:
                type: object
                properties:
                  connected: { type: boolean }
                  connected_at:
                    type: string
                    format: date-time
                    description: >-
                      Present only when connected=true. Genuine RFC3339
                      string (handler converts via .AsTime() before
                      JSON-encoding), not the {seconds,nanos} ProtoTimestamp
                      wire shape.
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/integrations/lexware/test:
    post:
      summary: Test the Lexware API connection
      description: Requires admin role.
      tags: [lexware]
      operationId: testLexwareConnection
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Test result
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean }
                  error_message: { type: string, description: "Present only when success=false" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/integrations/lexware/sync/trigger:
    post:
      summary: Trigger a manual Lexware sync
      description: Requires admin role.
      tags: [lexware]
      operationId: triggerLexwareSync
      security: [{ bearerAuth: [] }]
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                sync_type:
                  type: string
                  description: "\"contacts\", or omitted/empty for all"
      responses:
        "202":
          description: Sync accepted
          content:
            application/json:
              schema:
                type: object
                properties:
                  sync_id: { type: string }
                  status: { type: string }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/integrations/lexware/sync/status:
    get:
      summary: Get the Lexware sync status overview
      description: Requires admin role.
      tags: [lexware]
      operationId: getLexwareSyncStatus
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: >-
            Sync status. last_contact_sync_at and last_sync_error_at are
            genuine RFC3339 strings (handler converts via .AsTime() before
            JSON-encoding), not the {seconds,nanos} ProtoTimestamp wire
            shape.
          content:
            application/json:
              schema:
                type: object
                properties:
                  contact_sync_enabled: { type: boolean }
                  invoice_push_enabled: { type: boolean }
                  quote_push_enabled: { type: boolean }
                  webhook_enabled: { type: boolean }
                  total_contacts_mapped: { type: integer, format: int32 }
                  total_invoices_mapped: { type: integer, format: int32 }
                  total_quotes_mapped: { type: integer, format: int32 }
                  last_contact_sync_at: { type: string, format: date-time, description: "Present only once a contact sync has run" }
                  last_sync_error: { type: string, description: "Present only when the last sync attempt failed" }
                  last_sync_error_at: { type: string, format: date-time, description: "Present only when the last sync attempt failed" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/integrations/lexware/sync/logs:
    get:
      summary: List recent Lexware sync log entries
      description: Requires admin role.
      tags: [lexware]
      operationId: listLexwareSyncLogs
      security: [{ bearerAuth: [] }]
      parameters:
        - name: limit
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 20 }
      responses:
        "200":
          description: Sync log entries, most recent first
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/LexwareSyncLogEntry" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/integrations/lexware/mappings/{entity_type}:
    get:
      summary: Get Lexware field mappings for an entity type
      description: Requires admin role.
      tags: [lexware]
      operationId: getLexwareFieldMappings
      security: [{ bearerAuth: [] }]
      parameters:
        - name: entity_type
          in: path
          required: true
          schema: { type: string }
          description: "e.g. contact, invoice, quote"
      responses:
        "200":
          description: Field mappings for the entity type (bare array, not wrapped in an object)
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/LexwareFieldMappingEntry" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    put:
      summary: Save Lexware field mappings for an entity type
      description: Requires admin role.
      tags: [lexware]
      operationId: updateLexwareFieldMappings
      security: [{ bearerAuth: [] }]
      parameters:
        - name: entity_type
          in: path
          required: true
          schema: { type: string }
          description: "e.g. contact, invoice, quote"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [mappings]
              properties:
                mappings:
                  type: array
                  minItems: 1
                  items: { $ref: "#/components/schemas/LexwareFieldMappingEntry" }
      responses:
        "200":
          description: Field mappings saved
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/integrations/lexware/push/invoice/{id}:
    post:
      summary: Push an invoice to Lexware
      description: >-
        Requires admin role. Business-level push failures (e.g. invoice not
        found, Lexware API error) do NOT surface as an HTTP error -- the RPC
        returns success=false with a 200 response and error_message set. Only
        a malformed id (not a valid UUID) produces a 400.
      tags: [lexware]
      operationId: pushLexwareInvoice
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
          description: Invoice ID
      responses:
        "200":
          description: Push result
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean }
                  lexware_id: { type: string, description: "Present only on success" }
                  error_message: { type: string, description: "Present only on failure" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/integrations/lexware/push/quote/{id}:
    post:
      summary: Push a quote to Lexware
      description: >-
        Requires admin role. Business-level push failures (e.g. quote not
        found, Lexware API error) do NOT surface as an HTTP error -- the RPC
        returns success=false with a 200 response and error_message set. Only
        a malformed id (not a valid UUID) produces a 400.
      tags: [lexware]
      operationId: pushLexwareQuote
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
          description: Quote ID
      responses:
        "200":
          description: Push result
          content:
            application/json:
              schema:
                type: object
                properties:
                  success: { type: boolean }
                  lexware_id: { type: string, description: "Present only on success" }
                  error_message: { type: string, description: "Present only on failure" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  # === openapi-drift sweep: wp5-caldav-guest-misc ===
  # ============================================================================
  # CalDAV / CardDAV — app-specific passwords (JWT auth, route_caldav.go)
  # response.JSON on hand-written structs (CalDAVPasswordInfo etc.) — snake_case
  # json tags, RFC3339 timestamps. Protocol routes /caldav/*, /carddav/* (HTTP
  # Basic Auth, WebDAV PROPFIND/REPORT/... verbs) and the /.well-known/{caldav,
  # carddav} redirects are intentionally out of scope for this document.
  # ============================================================================

  /api/v1/caldav/passwords:
    get:
      summary: List the current user's CalDAV app-specific passwords
      tags: [caldav]
      operationId: listCaldavPasswords
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: App-specific passwords
          content:
            application/json:
              schema:
                type: object
                properties:
                  passwords:
                    type: array
                    items: { $ref: "#/components/schemas/CaldavPasswordInfo" }
        "401": { $ref: "#/components/responses/Unauthorized" }
    post:
      summary: Create a new CalDAV app-specific password
      tags: [caldav]
      operationId: createCaldavPassword
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CaldavCreatePasswordRequest"
      responses:
        "201":
          description: Password created — the plaintext value is shown once and is never retrievable again
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CaldavCreatePasswordResponse"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/caldav/passwords/{id}:
    delete:
      summary: Revoke a CalDAV app-specific password
      tags: [caldav]
      operationId: revokeCaldavPassword
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Password revoked
          content:
            application/json:
              schema:
                type: object
                properties:
                  status: { type: string, example: revoked }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/caldav/status:
    get:
      summary: Get CalDAV/CardDAV status for the current user
      tags: [caldav]
      operationId: getCaldavStatus
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: CalDAV status (org-wide + per-user)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CaldavStatusResponse"
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/caldav/test:
    post:
      summary: Test the CalDAV/CardDAV connection for the current user
      description: >-
        Makes real, timeout-bounded HTTP requests against this server's own
        /caldav/ and /carddav/ endpoints using a freshly minted, immediately
        revoked app-specific password -- the same path a real CalDAV/CardDAV
        client would use. Distinguishes network failure, auth failure, and
        unexpected responses instead of only checking that the enabled flags
        are set.
      tags: [caldav]
      operationId: testCaldavConnection
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Connection test result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CaldavTestResult"
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/caldav/enable:
    put:
      summary: Enable CalDAV/CardDAV for the current user
      tags: [caldav]
      operationId: enableCaldav
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: CalDAV enabled
          content:
            application/json:
              schema:
                type: object
                properties:
                  status: { type: string, example: enabled }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/caldav/disable:
    put:
      summary: Disable CalDAV/CardDAV for the current user
      tags: [caldav]
      operationId: disableCaldav
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: CalDAV disabled
          content:
            application/json:
              schema:
                type: object
                properties:
                  status: { type: string, example: disabled }
        "401": { $ref: "#/components/responses/Unauthorized" }

  # --- CalDAV admin (JWT auth + admin role) ---

  /api/v1/admin/caldav/settings:
    get:
      summary: Get the org-wide CalDAV/CardDAV enabled setting
      description: Requires admin role.
      tags: [caldav]
      operationId: getAdminCaldavSettings
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Org-wide CalDAV setting
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CaldavAdminSettings"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    put:
      summary: Set the org-wide CalDAV/CardDAV enabled setting
      description: Requires admin role.
      tags: [caldav]
      operationId: updateAdminCaldavSettings
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CaldavAdminSettings"
      responses:
        "200":
          description: Setting updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CaldavAdminSettings"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/admin/caldav/users:
    get:
      summary: List users with CalDAV app-specific passwords (audit)
      description: Requires admin role.
      tags: [caldav]
      operationId: listAdminCaldavUsers
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: CalDAV user list
          content:
            application/json:
              schema:
                type: object
                properties:
                  users:
                    type: array
                    items: { $ref: "#/components/schemas/CaldavUserInfo" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/admin/caldav/users/{userId}/passwords:
    delete:
      summary: Revoke all CalDAV app-specific passwords for a user
      description: Requires admin role.
      tags: [caldav]
      operationId: revokeAdminCaldavUserPasswords
      security: [{ bearerAuth: [] }]
      parameters:
        - name: userId
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: Passwords revoked
          content:
            application/json:
              schema:
                type: object
                properties:
                  revoked_count: { type: integer, format: int64 }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  # ============================================================================
  # Guest chat — public API (route_guest.go). Session endpoints are fully
  # unauthenticated; message/config endpoints require a guest session token in
  # the X-Guest-Token header (issued by POST /sessions) instead of a JWT
  # bearer token, so security is [] throughout and the header is documented
  # as an explicit parameter. The two chat-proxy endpoints forward
  # chatv1.GetMessagesResponse / chatv1.SendMessageResponse via response.JSON
  # on the raw proto struct; these reuse the existing ListMessagesResponse /
  # MessageResponse schemas (chat-messages tag) — MessageInfo.created_at is a
  # plain RFC3339 string field on the proto message, not a google.protobuf.Timestamp,
  # so no {seconds,nanos} shape applies here.
  # ============================================================================

  /api/v1/guest/sessions:
    post:
      summary: Create a guest chat session
      tags: [guest]
      operationId: createGuestSession
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GuestCreateSessionRequest"
      responses:
        "201":
          description: Guest session created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GuestCreateSessionResponse"
        "400": { $ref: "#/components/responses/BadRequest" }

  /api/v1/guest/sessions/validate:
    post:
      summary: Validate a guest session token
      tags: [guest]
      operationId: validateGuestSessionToken
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GuestValidateTokenRequest"
      responses:
        "200":
          description: Token is valid — returns the session and (if configured) the channel's guest config
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GuestValidateTokenResponse"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401":
          description: Invalid or expired guest token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /api/v1/guest/channels/{channelID}/messages:
    get:
      summary: List messages in a guest-accessible channel
      description: Requires a valid guest session (X-Guest-Token header); the session's channel_id must match {channelID}.
      tags: [guest]
      operationId: listGuestChannelMessages
      security: []
      parameters:
        - name: channelID
          in: path
          required: true
          schema: { type: string, format: uuid }
        - name: X-Guest-Token
          in: header
          required: true
          description: Guest session token issued by POST /api/v1/guest/sessions
          schema: { type: string }
        - name: limit
          in: query
          schema: { type: integer, default: 50, maximum: 100 }
        - name: before
          in: query
          description: Cursor — return messages created before this message ID
          schema: { type: string }
      responses:
        "200":
          description: Messages (chatv1.GetMessagesResponse passthrough)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListMessagesResponse"
        "401":
          description: Guest session required, or the token is invalid/expired
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Guest session does not grant access to this channel
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "503":
          description: Chat service unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    post:
      summary: Send a message to a guest-accessible channel
      description: Requires a valid guest session (X-Guest-Token header); the session's channel_id must match {channelID}. Rate-limited per guest session.
      tags: [guest]
      operationId: sendGuestChannelMessage
      security: []
      parameters:
        - name: channelID
          in: path
          required: true
          schema: { type: string, format: uuid }
        - name: X-Guest-Token
          in: header
          required: true
          description: Guest session token issued by POST /api/v1/guest/sessions
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GuestSendMessageRequest"
      responses:
        "201":
          description: Message sent (chatv1.SendMessageResponse passthrough)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageResponse"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401":
          description: Guest session required, or the token is invalid/expired
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "403":
          description: Guest session does not grant access to this channel
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "429": { description: Guest message rate limit exceeded }
        "503":
          description: Chat service unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /api/v1/guest/config:
    get:
      summary: Get the public guest-chat channel config
      description: Requires a valid guest session (X-Guest-Token header). Returns only public-safe config fields (no internal IDs).
      tags: [guest]
      operationId: getGuestChannelConfig
      security: []
      parameters:
        - name: X-Guest-Token
          in: header
          required: true
          description: Guest session token issued by POST /api/v1/guest/sessions
          schema: { type: string }
      responses:
        "200":
          description: Public channel config
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GuestPublicConfig"
        "401":
          description: Guest session required, or the token is invalid/expired
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404": { $ref: "#/components/responses/NotFound" }

  # ============================================================================
  # Feature flags (route_feature_flags.go)
  # ============================================================================

  /api/v1/feature-flags:
    get:
      summary: Get resolved feature flag values for the authenticated user
      tags: [feature-flags]
      operationId: getFeatureFlags
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Resolved feature flags
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FeatureFlagsResponse"
        "401": { $ref: "#/components/responses/Unauthorized" }

  # ============================================================================
  # Global search (route_search_global.go) — reuses the existing `search` tag.
  # Fans out to CRM, Documents, and Email in parallel with a 500ms timeout;
  # each module's failure is isolated and reported per-module.
  # ============================================================================

  /api/v1/search/global:
    get:
      summary: Global search across CRM, documents, and email
      description: >-
        Requires search:read permission. Fans out to the CRM, Documents, and
        Email services in parallel with a 500ms budget; a slow or failing
        module is reported via its own `error` field instead of failing the
        whole request. Email search is not yet implemented and always
        returns an empty result set.
      tags: [search]
      operationId: globalSearch
      security: [{ bearerAuth: [] }]
      parameters:
        - name: q
          in: query
          required: true
          description: Search query
          schema: { type: string }
        - name: limit
          in: query
          description: Max results per module (default 5, max 20)
          schema: { type: integer, default: 5, maximum: 20 }
      responses:
        "200":
          description: Per-module search results
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GlobalSearchResponse"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  # === openapi-drift sweep: wp8-partial-gaps ===
  # ==========================================================================
  # route_crm_ext.go -- CRM extension routes: duplicate detection, contact
  # timeline, GDPR consent CRUD. Registered inside the EXISTING /contacts and
  # /companies subrouters (route_crm.go), so base paths are /api/v1/contacts
  # and /api/v1/companies -- NOT /api/v1/crm/*. All handlers use
  # response.JSON(w, status, resp) on RAW proto structs/fields (not
  # response.Proto) -- the generated pb.go json tags are snake_case and enum
  # int32 values serialize as plain numbers via encoding/json, matching
  # protojson's UseProtoNames/UseEnumNumbers output byte-for-byte. None of the
  # fields involved are google.protobuf.Timestamp (crmv1 uses plain RFC3339
  # strings here), so no ProtoTimestamp refs are needed in this section.
  # Handler: internal/gateway/route_crm_ext.go (CRMExtRoutes)
  # ==========================================================================

  /api/v1/contacts/{id}/duplicates:
    get:
      summary: Find potential duplicate contacts for a contact
      tags: [contacts]
      operationId: findContactDuplicates
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Duplicate candidates ranked by similarity
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CrmContactDuplicatesResponse"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/contacts/merge:
    post:
      summary: Merge a duplicate contact into a primary contact
      description: The duplicate contact's activities/deals are reassigned to the primary contact; the duplicate contact is then deleted.
      tags: [contacts]
      operationId: mergeContacts
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CrmMergeRequest"
      responses:
        "200":
          description: Merged contact
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CrmContactSnapshot"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/contacts/{id}/timeline:
    get:
      summary: Get the activity/event timeline for a contact
      tags: [contacts]
      operationId: getContactTimeline
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
        - name: offset
          in: query
          description: Page number (1-based -- despite the query param name this is not a row offset, it maps directly to the proto page field)
          schema: { type: integer, minimum: 1, default: 1 }
        - name: limit
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 20 }
      responses:
        "200":
          description: Timeline events for the contact
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CrmContactTimelineResponse"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/contacts/{id}/consents:
    get:
      summary: Get the current consent summary for a contact (GDPR)
      tags: [security]
      operationId: getContactConsents
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Latest consent record per consent type
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GdprConsentSummary"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      summary: Grant a consent for a contact (GDPR)
      tags: [security]
      operationId: grantContactConsent
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GdprGrantConsentRequest"
      responses:
        "201":
          description: Consent record created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GdprConsentRecord"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/contacts/{id}/consents/{type}:
    delete:
      summary: Revoke a previously granted consent for a contact (GDPR)
      tags: [security]
      operationId: revokeContactConsent
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
        - name: type
          in: path
          required: true
          schema: { type: string }
          description: Consent type identifier (e.g. marketing_email, call_recording)
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                notes:
                  type: string
      responses:
        "200":
          description: Revoked consent record
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GdprConsentRecord"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/contacts/{id}/consents/{type}/history:
    get:
      summary: Get the full consent history for a contact and consent type (GDPR audit trail)
      tags: [security]
      operationId: getContactConsentHistory
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
        - name: type
          in: path
          required: true
          schema: { type: string }
          description: Consent type identifier (e.g. marketing_email, call_recording)
      responses:
        "200":
          description: Chronological consent record history, newest first
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GdprConsentHistoryResponse"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/contacts/{id}/gdpr/deletion-request:
    post:
      summary: Request GDPR right-to-erasure deletion for a contact
      description: Creates a pending deletion request. Actual erasure happens separately via POST /api/v1/gdpr/deletion-requests/{id}/process (admin only).
      tags: [security]
      operationId: requestContactDeletion
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                reason:
                  type: string
      responses:
        "201":
          description: Deletion request created (status "pending")
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GdprDeletionRequest"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/companies/{id}/duplicates:
    get:
      summary: Find potential duplicate companies for a company
      tags: [companies]
      operationId: findCompanyDuplicates
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Duplicate candidates ranked by similarity
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CrmCompanyDuplicatesResponse"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/companies/merge:
    post:
      summary: Merge a duplicate company into a primary company
      description: The duplicate company's contacts/deals are reassigned to the primary company; the duplicate company is then deleted.
      tags: [companies]
      operationId: mergeCompanies
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CrmMergeRequest"
      responses:
        "200":
          description: Merged company
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CrmCompanySnapshot"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/gdpr/deletion-requests/{id}/process:
    post:
      summary: Process a pending GDPR deletion request (admin only, performs the actual erasure)
      tags: [security]
      operationId: processGdprDeletionRequest
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
          description: Deletion request ID
      responses:
        "200":
          description: Deletion processed
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: completed
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # ==========================================================================
  # route_biz_billing.go / route_biz_quotes.go -- GoBD journal/compliance gap
  # endpoints, invoice number validation & locking, payment stats, dunning
  # status override, deal-to-quote conversion. All handlers use
  # response.JSON(w, status, resp) on RAW proto response structs (not
  # response.Proto) -- snake_case json tags match protojson's UseProtoNames
  # output and enum fields serialize as plain ints, matching UseEnumNumbers.
  # None of the new response fields introduced here are Timestamp-typed
  # (LockInvoiceResponse.locked_at is a plain string field; GetJournalSummary
  # Response/GetPaymentStatsResponse/ValidateInvoiceNumberResponse are plain
  # ints/strings/bools), so no ProtoTimestamp refs are needed for those.
  # Where a handler instead returns an EXISTING Quote/DunningRecord proto
  # message (CreateQuoteFromDeal, UpdateDunningStatus, SendDunningNotice),
  # the already-defined Quote/DunningRecord schemas are reused as-is for
  # consistency with their already-documented sibling endpoints, which
  # serialize identically via response.JSON on the very same proto types.
  # Handlers: internal/gateway/route_biz_billing.go, route_biz_quotes.go
  # ==========================================================================

  /api/v1/finance/journal/summary:
    get:
      summary: Get the GoBD gap-detection journal summary for a fiscal year
      description: gaps_detected should always be 0 for GoBD-compliant invoice numbering; a nonzero value indicates a numbering gap requiring investigation.
      tags: [GoBD Archive]
      operationId: getFinanceJournalSummary
      security: [{ bearerAuth: [] }]
      parameters:
        - name: year
          in: query
          required: true
          schema: { type: integer, minimum: 2000, maximum: 2100 }
      responses:
        "200":
          description: Journal summary for the fiscal year
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinanceJournalSummary"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/finance/invoices/validate-number:
    get:
      summary: Validate the format and uniqueness of an invoice number
      tags: [invoices]
      operationId: validateInvoiceNumber
      security: [{ bearerAuth: [] }]
      parameters:
        - name: number
          in: query
          required: true
          schema: { type: string }
          description: "e.g. RE-2026-0042"
      responses:
        "200":
          description: Validation result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinanceInvoiceNumberValidation"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/finance/invoices/{id}/lock:
    post:
      summary: Administratively lock an invoice (GoBD immutability after lock)
      tags: [invoices]
      operationId: lockInvoice
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Invoice locked
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinanceInvoiceLockResult"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/finance/stats/payments:
    get:
      summary: Get aggregated payment statistics for a date range
      tags: [finance-dashboard]
      operationId: getPaymentStats
      security: [{ bearerAuth: [] }]
      parameters:
        - name: from
          in: query
          required: true
          schema: { type: string, format: date }
        - name: to
          in: query
          required: true
          schema: { type: string, format: date }
      responses:
        "200":
          description: Aggregated payment statistics for the date range
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinancePaymentStats"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/finance/dunning/{id}/status:
    put:
      summary: Directly set a dunning record's status (admin override)
      tags: [dunning]
      operationId: updateDunningStatus
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [status]
              properties:
                status:
                  type: string
                  enum: [draft, sent, paid]
      responses:
        "200":
          description: Updated dunning record
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DunningRecord"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/finance/dunning/{id}/notice:
    post:
      summary: Mark a dunning record as sent (notice dispatch)
      tags: [dunning]
      operationId: sendDunningNotice
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Dunning marked as sent
          content:
            application/json:
              schema:
                type: object
                properties:
                  dunning:
                    $ref: "#/components/schemas/DunningRecord"
                  email_queued:
                    type: boolean
                    description: False until the dunning-notice email integration is wired up (Sprint 3 TODO)
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/finance/export/gobd:
    post:
      summary: Generate a GoBD-compliant CSV export (Paragraph 147 AO)
      tags: [GoBD Archive]
      operationId: generateGobdExport
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [from_date, to_date]
              properties:
                from_date:
                  type: string
                  format: date
                to_date:
                  type: string
                  format: date
      responses:
        "200":
          description: GoBD CSV file download
          content:
            text/csv:
              schema:
                type: string
                format: binary
          headers:
            Content-Disposition:
              schema:
                type: string
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/finance/deals/{dealId}/quote:
    post:
      summary: Create a quote from a CRM deal
      description: Cross-service orchestration performed entirely on the biz gRPC service -- it fetches the deal plus its linked contact and company, and assembles the customer snapshot server-side.
      tags: [quotes]
      operationId: createQuoteFromDeal
      security: [{ bearerAuth: [] }]
      parameters:
        - name: dealId
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        "201":
          description: Quote created from the deal
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Quote"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # --------------------------------------------------------------------------
  # route_biz_ext.go -- time-tracking-to-invoice conversion, registered inside
  # the EXISTING /api/v1/hr/time subrouter (route_hr.go). The biz service
  # returns a legacy JSON-encoded models.Invoice; the gateway proxies those
  # bytes straight through as the literal HTTP response body (NOT base64-
  # wrapped -- unlike the []byte field on FinanceInvoiceLockResult, this
  # handler writes the bytes directly via w.Write, so the wire body is the
  # JSON object itself), hence the free-form response schema below.
  # Handler: internal/gateway/route_biz_ext.go (BizExtRoutes)
  # --------------------------------------------------------------------------

  /api/v1/hr/time/create-invoice:
    post:
      summary: Create an invoice from an employee's time entries in a date range
      tags: [hr-time]
      operationId: createInvoiceFromTime
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/HrTimeCreateInvoiceRequest"
      responses:
        "201":
          description: >-
            Invoice created from the time entries. Legacy JSON-encoded
            models.Invoice shape proxied through unchanged from the biz
            service -- does not share the field set of the Invoice schema.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  # ==========================================================================
  # route_settings.go -- module-access grant CRUD (tenant admin grants/revokes
  # a user's access to a paid module). Sibling of the already-documented
  # /api/v1/tenant/module-leads branch, reusing the same undeclared [Settings]
  # tag. Handlers return a flattened camelCase Go struct (userModuleGrantJSON)
  # via response.JSON, NOT the raw settingsv1.UserModuleGrant proto --
  # timestamps are pre-formatted RFC3339 strings via time.Time.Format(), so no
  # ProtoTimestamp refs are needed here.
  # Handler: internal/gateway/route_settings.go (SettingsRoutes)
  # ==========================================================================

  /api/v1/admin/license:
    get:
      summary: List every Cosmi module with its tenant-wide activation state
      description: >
        Returns the full module catalogue, not only the activated ones. A module
        the deployment does not run (modules.* feature flag off) always reports
        active=false regardless of the stored activation.
      tags: [Settings]
      operationId: getTenantLicense
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Module activation state for the tenant
          content:
            application/json:
              schema:
                type: object
                properties:
                  modules:
                    type: array
                    items:
                      $ref: "#/components/schemas/TenantModule"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    patch:
      summary: Activate or deactivate one module tenant-wide (admin only)
      tags: [Settings]
      operationId: setTenantModuleActive
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [moduleId, active]
              properties:
                moduleId:
                  type: string
                  description: Catalogue module id (see GET for the list)
                active:
                  type: boolean
      responses:
        "200":
          description: The updated module
          content:
            application/json:
              schema:
                type: object
                properties:
                  module:
                    $ref: "#/components/schemas/TenantModule"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: Unknown module id
        "409":
          description: Module is not available in this deployment (feature flag off)

  /api/v1/admin/subscription:
    get:
      summary: Booked plan, support tier and seats of the tenant
      tags: [Settings]
      operationId: getTenantSubscription
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: The tenant's subscription
          content:
            application/json:
              schema:
                type: object
                properties:
                  subscription:
                    $ref: "#/components/schemas/TenantSubscription"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: Tenant not found

  /api/v1/admin/branding:
    get:
      summary: Get the tenant's workspace branding (name, logo, icon, accent color)
      description: >
        logo_object_key/icon_object_key are MinIO object keys, not URLs —
        resolve them via POST /api/v1/files/presign-download (same pattern as
        User.avatar_url). accent_color defaults to #10B981 until the first
        write.
      tags: [Settings]
      operationId: getBranding
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: The tenant's branding
          content:
            application/json:
              schema:
                type: object
                properties:
                  branding:
                    $ref: "#/components/schemas/Branding"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    put:
      summary: Replace the tenant's workspace branding (admin only)
      description: >
        Full replace, not a patch — omitting logo_object_key/icon_object_key
        clears them. accent_color must be one of the Cosmi swatch colors
        (server-validated, no free hex). logo/icon object keys must belong to
        this tenant's branding presign scope (obtained from
        POST /api/v1/files/presign-upload with scope=branding), or the write
        is rejected.
      tags: [Settings]
      operationId: putBranding
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name, accent_color]
              properties:
                name:
                  type: string
                  maxLength: 200
                logo_object_key:
                  type: string
                icon_object_key:
                  type: string
                accent_color:
                  type: string
                  enum:
                    [
                      "#6B7280",
                      "#3B82F6",
                      "#06B6D4",
                      "#10B981",
                      "#F59E0B",
                      "#F97316",
                      "#EF4444",
                      "#8B5CF6",
                      "#EC4899",
                      "#6366F1",
                    ]
      responses:
        "200":
          description: The updated branding
          content:
            application/json:
              schema:
                type: object
                properties:
                  branding:
                    $ref: "#/components/schemas/Branding"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/tenant/module-grants:
    get:
      summary: List module-access grants for the tenant
      tags: [Settings]
      operationId: listModuleGrants
      security: [{ bearerAuth: [] }]
      parameters:
        - name: user_id
          in: query
          schema: { type: string, format: uuid }
          description: Filter by user ID
        - name: module_id
          in: query
          schema: { type: string }
          description: Filter by module ID
      responses:
        "200":
          description: List of module-access grants
          content:
            application/json:
              schema:
                type: object
                properties:
                  grants:
                    type: array
                    items:
                      $ref: "#/components/schemas/SettingsModuleGrant"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/tenant/module-grants/bulk-revoke:
    post:
      summary: Revoke multiple module-access grants in one call (admin only)
      tags: [Settings]
      operationId: bulkRevokeModuleAccess
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [pairs]
              properties:
                pairs:
                  type: array
                  items:
                    type: object
                    required: [userId, moduleId]
                    properties:
                      userId:
                        type: string
                        format: uuid
                      moduleId:
                        type: string
      responses:
        "200":
          description: Number of grants revoked
          content:
            application/json:
              schema:
                type: object
                properties:
                  revokedCount:
                    type: integer
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/tenant/module-grants/{user_id}/{module_id}:
    put:
      summary: Grant a user access to a module (admin only)
      tags: [Settings]
      operationId: grantModuleAccess
      security: [{ bearerAuth: [] }]
      parameters:
        - name: user_id
          in: path
          required: true
          schema: { type: string, format: uuid }
        - name: module_id
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Module access granted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SettingsModuleGrant"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    delete:
      summary: Revoke a user's access to a module (admin only)
      tags: [Settings]
      operationId: revokeModuleAccess
      security: [{ bearerAuth: [] }]
      parameters:
        - name: user_id
          in: path
          required: true
          schema: { type: string, format: uuid }
        - name: module_id
          in: path
          required: true
          schema: { type: string }
      responses:
        "204":
          description: Module access revoked
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  # ==========================================================================
  # route_files.go -- generic presigned upload/download URL endpoints, backed
  # by the Document gRPC service. Distinct from the chat-file routes also
  # mounted under /api/v1/files/* (route_chat.go, tag chat-files) -- these two
  # are the generic, non-chat presign endpoints referenced by the already-
  # documented POST /api/v1/files upload-registration flow (tag documents).
  # Handler: internal/gateway/route_files.go (FileRoutes)
  # ==========================================================================

  /api/v1/files/presign-upload:
    post:
      summary: Get a short-lived presigned PUT URL for browser-direct file upload
      description: URL expires after 15 minutes. Does not touch object storage itself -- upload the file with a PUT to the returned URL, then register its metadata via POST /api/v1/files.
      tags: [documents]
      operationId: presignFileUpload
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FilePresignUploadRequest"
      responses:
        "200":
          description: Presigned upload URL
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FilePresignUploadResponse"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/files/presign-download:
    get:
      summary: Get a short-lived presigned GET URL for a stored object
      description: URL expires after 1 hour. The document service enforces tenant ownership of the object key.
      tags: [documents]
      operationId: presignFileDownload
      security: [{ bearerAuth: [] }]
      parameters:
        - name: object_key
          in: query
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Presigned download URL
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FilePresignDownloadResponse"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  # === openapi-drift sweep: r2a-video-meetings ===
  # ===========================================================================
  # Meeting Breakout Rooms (Wave 6A — host/co-host authz enforced in the
  # service; no RBAC guard except /join which requires meetings:write)
  # ===========================================================================

  /api/v1/meetings/{id}/breakout-rooms:
    post:
      summary: Create breakout rooms for a meeting (host/co-host only, enforced in the service)
      tags: [meetings]
      operationId: createBreakoutRooms
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateBreakoutRoomsRequest"
      responses:
        "201":
          description: Breakout rooms created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateBreakoutRoomsResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    get:
      summary: List breakout rooms and current participant assignments for a meeting
      tags: [meetings]
      operationId: listBreakoutRooms
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Breakout rooms and assignments listed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListBreakoutRoomsResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/meetings/{id}/breakout-rooms/assign:
    post:
      summary: Assign a participant to a breakout room (or back to main room when omitted)
      tags: [meetings]
      operationId: assignBreakoutParticipant
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssignBreakoutParticipantRequest"
      responses:
        "200":
          description: Participant assigned
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: assigned
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/meetings/{id}/breakout-rooms/join:
    post:
      summary: Join the caller's assigned breakout room (requires meetings:write)
      tags: [meetings]
      operationId: joinBreakoutRoom
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: LiveKit token, ws_url and per-session TURN ice_servers for the breakout room
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JoinBreakoutRoomResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/meetings/{id}/breakout-rooms/assignment:
    get:
      summary: Get the caller's current breakout room assignment (poll target; unset means main room)
      tags: [meetings]
      operationId: getBreakoutAssignment
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Current breakout room assignment
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetBreakoutAssignmentResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/meetings/{id}/breakout-rooms/return:
    post:
      summary: Return a participant (self, or another when host/co-host) to the main room
      tags: [meetings]
      operationId: returnToMainRoom
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReturnToMainRoomRequest"
      responses:
        "200":
          description: Participant returned to the main room
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: returned
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/meetings/{id}/breakout-rooms/close:
    post:
      summary: Close all breakout rooms and return every participant to the main room (host/co-host only)
      tags: [meetings]
      operationId: closeBreakoutRooms
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Breakout rooms closed
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: breakout rooms closed
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  # ===========================================================================
  # Meeting AI Summary (Wave 7C — host/co-host authz enforced in the service)
  # ===========================================================================

  /api/v1/meetings/{id}/ai-summary:
    post:
      summary: Generate an LLM summary of the meeting's public notes (host/co-host only, enforced in the service)
      tags: [meetings]
      operationId: generateMeetingSummary
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Meeting updated with the generated ai_summary
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeetingSchema"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  # ===========================================================================
  # Meeting Chat (persisted in-call messages)
  # ===========================================================================

  /api/v1/meetings/{meetingId}/chat:
    post:
      summary: Save a persisted in-call chat message
      tags: [meetings]
      operationId: saveMeetingChatMessage
      security:
        - bearerAuth: []
      parameters:
        - name: meetingId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SaveMeetingChatMessageRequest"
      responses:
        "201":
          description: Chat message saved
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeetingChatMessage"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    get:
      summary: List persisted chat messages for a meeting
      tags: [meetings]
      operationId: listMeetingChatMessages
      security:
        - bearerAuth: []
      parameters:
        - name: meetingId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: limit
          in: query
          description: Capped at 500 server-side; defaults to 100 when omitted.
          schema:
            type: integer
            default: 100
      responses:
        "200":
          description: Chat messages listed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListMeetingChatMessagesResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  # ===========================================================================
  # Meeting Host Controls — Co-Hosts (Wave 3 — organizer-only, enforced in the service)
  # ===========================================================================

  /api/v1/meetings/{meetingId}/cohosts:
    post:
      summary: Grant co-host rights to a user (organizer only, enforced in the service)
      tags: [meetings]
      operationId: promoteCoHost
      security:
        - bearerAuth: []
      parameters:
        - name: meetingId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PromoteCoHostRequest"
      responses:
        "200":
          description: Co-host promoted
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: co-host promoted
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    get:
      summary: List co-hosts for a meeting
      tags: [meetings]
      operationId: listCoHosts
      security:
        - bearerAuth: []
      parameters:
        - name: meetingId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Co-hosts listed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListCoHostsResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/meetings/{meetingId}/cohosts/{userId}:
    delete:
      summary: Revoke co-host rights from a user (organizer only, enforced in the service)
      tags: [meetings]
      operationId: demoteCoHost
      security:
        - bearerAuth: []
      parameters:
        - name: meetingId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: userId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Co-host demoted
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: co-host demoted
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  # ===========================================================================
  # Meeting Host Controls — Lock and Moderation (Wave 3 — host/co-host authz
  # enforced in the service, no RBAC guard)
  # ===========================================================================

  /api/v1/meetings/{meetingId}/lock:
    post:
      summary: Lock or unlock a meeting to new joiners (host/co-host only, enforced in the service)
      tags: [meetings]
      operationId: setMeetingLock
      security:
        - bearerAuth: []
      parameters:
        - name: meetingId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SetMeetingLockRequest"
      responses:
        "200":
          description: Meeting lock updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: meeting lock updated
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/meetings/{meetingId}/moderation/mute:
    post:
      summary: Server-mute a single participant (host/co-host only, enforced in the service)
      tags: [meetings]
      operationId: muteMeetingParticipant
      security:
        - bearerAuth: []
      parameters:
        - name: meetingId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MeetingModerationTargetRequest"
      responses:
        "200":
          description: Participant muted
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: participant muted
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/meetings/{meetingId}/moderation/mute-all:
    post:
      summary: Server-mute all non-host participants (host/co-host only, enforced in the service)
      tags: [meetings]
      operationId: muteAllMeetingParticipants
      security:
        - bearerAuth: []
      parameters:
        - name: meetingId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: All participants muted
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: all participants muted
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/meetings/{meetingId}/moderation/kick:
    post:
      summary: Remove a participant from the meeting room (host/co-host only, enforced in the service)
      tags: [meetings]
      operationId: removeMeetingParticipant
      security:
        - bearerAuth: []
      parameters:
        - name: meetingId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MeetingModerationTargetRequest"
      responses:
        "200":
          description: Participant removed
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: participant removed
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  # ===========================================================================
  # Recordings by Meeting
  # ===========================================================================

  /api/v1/video/meetings/{meetingId}/recordings:
    get:
      summary: List recordings for a specific meeting with pagination
      tags: [recordings]
      operationId: listRecordingsByMeeting
      security:
        - bearerAuth: []
      parameters:
        - name: meetingId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/page"
        - $ref: "#/components/parameters/page_size"
      responses:
        "200":
          description: Recordings for the meeting (bare array, not wrapped)
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/RecordingSchema"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  # ===========================================================================
  # Recording Sub-Operations (S1.7 / Welle 1.B — recording-tag gRPC extension)
  # ===========================================================================

  /api/v1/video/recordings/{id}/consents:
    get:
      summary: Get the consent snapshot and live consent rows for a recording
      tags: [recordings]
      operationId: getRecordingConsents
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Consent snapshot and live consent status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecordingConsentsResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/video/recordings/{id}/tag-consents:
    post:
      summary: Overwrite the consent snapshot on a recording (requires recordings:admin)
      tags: [recordings]
      operationId: tagRecordingWithConsents
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TagRecordingWithConsentsRequest"
      responses:
        "200":
          description: Consent snapshot updated
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: consent snapshot updated
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/video/recordings/{id}/metadata:
    patch:
      summary: Update mutable fields on a recording (requires recordings:admin)
      tags: [recordings]
      operationId: updateRecordingMetadata
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateRecordingMetadataRequest"
      responses:
        "200":
          description: Recording metadata updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecordingSchema"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/video/recordings/{id}/status:
    get:
      summary: Get the current status of a single recording (polling target)
      tags: [recordings]
      operationId: getRecordingStatus
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Current recording status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecordingSchema"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/video/recordings/{id}/download:
    get:
      summary: Generate a presigned MinIO GET URL for a completed recording
      tags: [recordings]
      operationId: getRecordingDownloadURL
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Presigned download URL
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecordingDownloadURLResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/video/recordings/{id}/cleanup:
    post:
      summary: Delete a single recording past its retention period (requires recordings:admin; admin/cron endpoint)
      tags: [recordings]
      operationId: cleanupExpiredRecording
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Recording deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: recording deleted
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/v1/video/recordings/{id}/initiator-consent:
    post:
      summary: Stamp initiator pre-recording consent before StartRecording (requires recording:write)
      tags: [recordings]
      operationId: confirmInitiatorConsent
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: Pre-recording consent stamped
          content:
            application/json:
              schema:
                type: object
                properties:
                  stamped:
                    type: boolean
                    example: true
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "412":
          description: >-
            Pre-recording consent could not be confirmed: the recording does
            not exist or is not owned by the calling user.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  # === openapi-drift sweep: r2b-hr-time ===
  # --- HR time tracking: analytics, balance, categories, templates, ---
  # --- projects, team, week approvals (route_hr.go, gap-fill sweep) ---
  # All handlers below use response.JSON on a raw proto message (not
  # response.Proto), so google.protobuf.Timestamp fields serialize as
  # ProtoTimestamp ({seconds,nanos}), not RFC3339 strings.

  /api/v1/hr/time/analytics:
    get:
      summary: Get time-tracking analytics (totals, trend, project breakdown) for the caller
      tags: [hr-time]
      operationId: getHrTimeAnalytics
      security: [{ bearerAuth: [] }]
      parameters:
        - name: range
          in: query
          description: "Aggregation range. Only 'month' is recognized explicitly; any other value (including omitted) defaults to the current week."
          schema: { type: string, enum: [week, month], default: week }
      responses:
        "200":
          description: Time analytics for the authenticated user
          content:
            application/json:
              schema: { $ref: "#/components/schemas/HrTimeAnalytics" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/hr/time/balance:
    get:
      summary: Get the caller's running time balance (overtime/undertime)
      tags: [hr-time]
      operationId: getHrTimeBalance
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Time balance for the authenticated user
          content:
            application/json:
              schema: { $ref: "#/components/schemas/HrTimeBalance" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/hr/time/team:
    get:
      summary: Get the current week's time overview for the whole team
      tags: [hr-time]
      operationId: getHrTimeTeam
      security: [{ bearerAuth: [] }]
      parameters:
        - name: week_start
          in: query
          description: "Week start date (YYYY-MM-DD, Monday). Defaults to the current week server-side when omitted."
          schema: { type: string, format: date }
      responses:
        "200":
          description: Per-employee time overview for the requested week
          content:
            application/json:
              schema:
                type: object
                properties:
                  team:
                    type: array
                    items: { $ref: "#/components/schemas/HrTimeTeamEntry" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/hr/time/categories:
    get:
      summary: List time categories for the tenant
      tags: [hr-time]
      operationId: listHrTimeCategories
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Time categories
          content:
            application/json:
              schema:
                type: object
                properties:
                  categories:
                    type: array
                    items: { $ref: "#/components/schemas/HrTimeCategory" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      summary: Create a time category
      tags: [hr-time]
      operationId: createHrTimeCategory
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/HrTimeCategoryInput" }
      responses:
        "201":
          description: Time category created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/HrTimeCategory" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/hr/time/categories/{id}:
    put:
      summary: Update a time category
      tags: [hr-time]
      operationId: updateHrTimeCategory
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/HrTimeCategoryInput" }
      responses:
        "200":
          description: Time category updated
          content:
            application/json:
              schema: { $ref: "#/components/schemas/HrTimeCategory" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      summary: Delete a time category
      tags: [hr-time]
      operationId: deleteHrTimeCategory
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "204":
          description: Time category deleted
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/hr/time/templates:
    get:
      summary: List time templates for the tenant
      tags: [hr-time]
      operationId: listHrTimeTemplates
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Time templates
          content:
            application/json:
              schema:
                type: object
                properties:
                  templates:
                    type: array
                    items: { $ref: "#/components/schemas/HrTimeTemplate" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      summary: Create a time template
      tags: [hr-time]
      operationId: createHrTimeTemplate
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/HrTimeCreateTemplateRequest" }
      responses:
        "201":
          description: Time template created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/HrTimeTemplate" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/hr/time/templates/{id}:
    delete:
      summary: Delete a time template
      tags: [hr-time]
      operationId: deleteHrTimeTemplate
      security: [{ bearerAuth: [] }]
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "204":
          description: Time template deleted
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/hr/time/projects:
    get:
      summary: List time projects for the tenant
      tags: [hr-time]
      operationId: listHrTimeProjects
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Time projects
          content:
            application/json:
              schema:
                type: object
                properties:
                  projects:
                    type: array
                    items: { $ref: "#/components/schemas/HrTimeProject" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      summary: Create a time project
      tags: [hr-time]
      operationId: createHrTimeProject
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/HrTimeCreateProjectRequest" }
      responses:
        "201":
          description: Time project created
          content:
            application/json:
              schema: { $ref: "#/components/schemas/HrTimeProject" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/hr/time/weeks/status:
    get:
      summary: Get the caller's week-approval status for a given week
      tags: [hr-time]
      operationId: getHrTimeWeekStatus
      security: [{ bearerAuth: [] }]
      parameters:
        - name: week_start
          in: query
          description: "Week start date (YYYY-MM-DD, Monday)."
          schema: { type: string, format: date }
      responses:
        "200":
          description: Week approval status and minute totals for the authenticated user
          content:
            application/json:
              schema:
                type: object
                properties:
                  week_approval: { $ref: "#/components/schemas/HrTimeWeekApproval" }
                  total_minutes: { type: integer }
                  target_minutes: { type: integer }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/hr/time/weeks/submit:
    post:
      summary: Submit the caller's own week for approval
      tags: [hr-time]
      operationId: submitHrTimeWeek
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/HrTimeSubmitWeekRequest" }
      responses:
        "200":
          description: Week submitted for approval
          content:
            application/json:
              schema: { $ref: "#/components/schemas/HrTimeWeekApproval" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /api/v1/hr/time/weeks/approve:
    post:
      summary: Approve an employee's submitted week (manager/admin)
      tags: [hr-time]
      operationId: approveHrTimeWeek
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/HrTimeApproveWeekRequest" }
      responses:
        "200":
          description: Week approved
          content:
            application/json:
              schema: { $ref: "#/components/schemas/HrTimeWeekApproval" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /api/v1/hr/time/weeks/reject:
    post:
      summary: Reject an employee's submitted week (manager/admin)
      tags: [hr-time]
      operationId: rejectHrTimeWeek
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/HrTimeRejectWeekRequest" }
      responses:
        "200":
          description: Week rejected
          content:
            application/json:
              schema: { $ref: "#/components/schemas/HrTimeWeekApproval" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /api/v1/hr/time/weeks/reopen:
    post:
      summary: Reopen a submitted or approved week for corrections (manager/admin)
      description: >
        A submitted or approved week rejects every write of work time — clock-in,
        manual entry and time corrections all return 409. Reopening is the only
        way back to an editable week. A week that is open or rejected is already
        editable and returns 409.
      tags: [hr-time]
      operationId: reopenHrTimeWeek
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/HrTimeReopenWeekRequest" }
      responses:
        "200":
          description: Week reopened
          content:
            application/json:
              schema: { $ref: "#/components/schemas/HrTimeWeekApproval" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  # === openapi-drift sweep: r2c-misc-gaps ===
  # --- 2FA: pending-login validation (backend/internal/gateway/route_auth.go) ---
  # Public route (no bearer token) — called after POST /auth/login returns a
  # pending_token because the account has 2FA enabled.

  /api/v1/auth/2fa/validate:
    post:
      summary: Validate a 2FA code (or recovery code) to complete a pending login
      description: >-
        Public endpoint — no bearer token required. Uses the short-lived
        pending_token returned by POST /auth/login instead of JWT auth.
      tags: [2fa]
      operationId: validate2FALogin
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Validate2FALoginRequest"
      responses:
        "200":
          description: 2FA validated; login completed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  # --- Contacts: import/export/visibility (backend/internal/gateway/route_crm_contacts.go) ---
  # Distinct from the /api/v1/email/contacts/* endpoints (tag `mails`), which take
  # base64 JSON bodies. These CRM-native endpoints take real multipart uploads and
  # stream binary files back directly (no base64 wrapping).

  /api/v1/contacts/import/csv:
    post:
      summary: Import contacts from a CSV file (multipart upload)
      tags: [contacts]
      operationId: importCRMContactsCSV
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/ContactsImportCSVForm"
      responses:
        "200":
          description: Import result with counts and per-row errors
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ContactsImportResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /api/v1/contacts/import/vcard:
    post:
      summary: Import contacts from a vCard file (multipart upload)
      tags: [contacts]
      operationId: importCRMContactsVCard
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/ContactsImportVCardForm"
      responses:
        "200":
          description: Import result with counts and per-row errors
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ContactsImportResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /api/v1/contacts/import/preview:
    post:
      summary: Preview a CSV file's columns and sample rows before import
      tags: [contacts]
      operationId: previewCRMContactsImportCSV
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              $ref: "#/components/schemas/ContactsPreviewImportForm"
      responses:
        "200":
          description: Detected columns, sample rows, and a heuristic field mapping
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ContactsPreviewImportResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /api/v1/contacts/export/csv:
    post:
      summary: Export contacts as a CSV file
      description: >-
        Returns a raw binary stream (not base64/JSON) — Content-Type and
        Content-Disposition are set from the service response.
      tags: [contacts]
      operationId: exportCRMContactsCSV
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ContactsExportCSVRequest"
      responses:
        "200":
          description: CSV file download
          headers:
            Content-Disposition:
              schema:
                type: string
              description: "attachment; filename=<generated>.csv"
          content:
            text/csv:
              schema:
                type: string
                format: binary
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/contacts/export/vcard:
    post:
      summary: Export contacts as a vCard file
      description: >-
        Returns a raw binary stream (not base64/JSON) — Content-Type and
        Content-Disposition are set from the service response.
      tags: [contacts]
      operationId: exportCRMContactsVCard
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ContactsExportVCardRequest"
      responses:
        "200":
          description: vCard file download
          headers:
            Content-Disposition:
              schema:
                type: string
              description: "attachment; filename=<generated>.vcf"
          content:
            text/vcard:
              schema:
                type: string
                format: binary
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/contacts/{id}/visibility:
    put:
      summary: Update a contact's visibility (shared vs personal)
      tags: [contacts]
      operationId: updateContactVisibility
      security:
        - bearerAuth: []
      parameters:
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateContactVisibilityRequest"
      responses:
        "200":
          description: Contact visibility updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ContactResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  # --- Helpdesk: business hours (backend/internal/gateway/route_helpdesk.go) ---
  # response.JSON on the raw *helpdeskv1.BusinessHoursResponse proto — updated_at
  # serializes as {seconds,nanos}, not RFC3339 (see ProtoTimestamp).

  /api/v1/helpdesk/business-hours:
    get:
      summary: Get the tenant's helpdesk business hours and holiday schedule
      tags: [helpdesk-sla]
      operationId: getHelpdeskBusinessHours
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Business hours schedule
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HelpdeskBusinessHours"
        "401": { $ref: "#/components/responses/Unauthorized" }
    put:
      summary: Update the tenant's helpdesk business hours and holiday schedule
      tags: [helpdesk-sla]
      operationId: updateHelpdeskBusinessHours
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateHelpdeskBusinessHoursRequest"
      responses:
        "200":
          description: Business hours schedule updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HelpdeskBusinessHours"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  # --- Inbox: canned responses, thread messages, unread-count alias
  # (backend/internal/gateway/route_inbox.go) ---
  # response.JSON on raw inbox proto messages — timestamps serialize as
  # {seconds,nanos}, not RFC3339 (see ProtoTimestamp). Follows this module's
  # existing minimal-responses convention (mostly 200/201 only, see neighboring
  # /api/v1/inbox/teams* and /api/v1/inbox/rules* paths already documented above).

  /api/v1/inbox/canned-responses:
    get:
      summary: List saved canned (template) replies for the inbox
      tags: [inbox-messages]
      operationId: listInboxCannedResponses
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Canned response list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InboxCannedResponseList"
    post:
      summary: Create an inbox canned (template) reply
      tags: [inbox-messages]
      operationId: createInboxCannedResponse
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateInboxCannedResponseRequest"
      responses:
        "201":
          description: Canned response created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InboxCannedResponseWrapper"

  /api/v1/inbox/canned-responses/{id}:
    put:
      summary: Update an inbox canned (template) reply
      tags: [inbox-messages]
      operationId: updateInboxCannedResponse
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateInboxCannedResponseRequest"
      responses:
        "200":
          description: Canned response updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InboxCannedResponseWrapper"
    delete:
      summary: Delete an inbox canned (template) reply
      tags: [inbox-messages]
      operationId: deleteInboxCannedResponse
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Canned response deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StatusResponse"

  /api/v1/inbox/messages/{id}/thread:
    get:
      summary: List thread messages (replies) for an inbox message
      tags: [inbox-messages]
      operationId: listInboxMessageThread
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Thread messages for the given inbox message
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InboxThreadMessagesResponse"

  /api/v1/inbox/messages/unread-count:
    get:
      summary: Get unread message count by channel (alias of GET /api/v1/inbox/unread-count)
      description: >-
        Registered as a second route to the same handler as GET
        /api/v1/inbox/unread-count (HandleGetUnreadCount) — kept for
        frontend routing/back-compat, not a distinct implementation.
      tags: [inbox-messages]
      operationId: getInboxUnreadCountAlias
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Unread counts
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InboxUnreadCount"

  # --- Inventar: item attachments (backend/internal/gateway/route_inventar.go) ---
  # response.JSON on raw inventar proto messages — timestamps serialize as
  # {seconds,nanos}, not RFC3339 (see ProtoTimestamp). POST is metadata-only:
  # it registers an object_key produced by a prior presigned-upload step, it
  # does not accept the file body directly.

  /api/v1/inventar/items/{id}/attachments:
    get:
      summary: List attachments for an inventory item
      tags: [inventar]
      operationId: listInventarItemAttachments
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Item attachments
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InventarItemAttachmentList"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      summary: Register an attachment for an inventory item
      description: >-
        Metadata-only — expects object_key from a prior presigned-upload
        step, does not accept the file body directly.
      tags: [inventar]
      operationId: createInventarItemAttachment
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateInventarItemAttachmentRequest"
      responses:
        "201":
          description: Attachment registered
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InventarItemAttachmentResponse"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/inventar/attachments/{id}:
    delete:
      summary: Delete an inventory item attachment
      description: Path {id} is the attachment ID, not the item ID.
      tags: [inventar]
      operationId: deleteInventarItemAttachment
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Attachment deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StatusResponse"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: JWT access token (15 min expiry)

  parameters:
    UserId:
      name: id
      in: path
      required: true
      schema:
        type: string
        format: uuid

    ResourceId:
      name: id
      in: path
      required: true
      schema:
        type: string
        format: uuid

    RoleId:
      name: roleId
      in: path
      required: true
      schema:
        type: string
        format: uuid

    page:
      name: page
      in: query
      schema:
        type: integer
        minimum: 1
        default: 1

    page_size:
      name: page_size
      in: query
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 20

  schemas:
    UserInfo:
      type: object
      description: Go encoding/json serialization uses snake_case field names.
      properties:
        id:
          type: string
          format: uuid
        email:
          type: string
          format: email
        first_name:
          type: string
        last_name:
          type: string
        is_active:
          type: boolean
        roles:
          type: array
          items:
            type: string
        created_at:
          type: string
          format: date-time
        two_factor_enabled:
          type: boolean
        locale:
          type: string
        avatar_url:
          type: string
          description: >
            Presigned-upload object key for the user's avatar
            ({tenant_id}/avatar/{uuid}{ext}). Resolve to a viewable URL via
            /api/v1/files/presign-download. Empty string when unset.

    UpdateProfileRequest:
      type: object
      description: >
        Self-service profile patch. All fields optional; the user is taken from
        the JWT, never the body. Cannot change is_active (admin-only).
      properties:
        first_name:
          type: string
          maxLength: 100
        last_name:
          type: string
          maxLength: 100
        avatar_url:
          type: string
          maxLength: 512

    RegisterRequest:
      type: object
      required: [email, password]
      properties:
        email:
          type: string
          format: email
        password:
          type: string
          format: password
          minLength: 8
        first_name:
          type: string
        last_name:
          type: string

    LoginRequest:
      type: object
      required: [email, password]
      properties:
        email:
          type: string
          format: email
        password:
          type: string
          format: password

    RefreshRequest:
      type: object
      required: [refresh_token]
      properties:
        refresh_token:
          type: string

    LogoutRequest:
      type: object
      required: [refresh_token]
      properties:
        refresh_token:
          type: string

    UpdateUserRequest:
      type: object
      properties:
        first_name:
          type: string
        last_name:
          type: string
        is_active:
          type: boolean

    AssignRoleRequest:
      type: object
      required: [roleId]
      properties:
        roleId:
          type: string
          format: uuid
          description: >
            Id of a system preset or a custom role of the caller's tenant.
            Validated against the roles table, not against a fixed list — a
            tenant's own roles cannot be enumerated in the spec.

    UserRolesResponse:
      type: object
      required: [roles]
      properties:
        roles:
          type: array
          description: Role ids the account holds after the mutation, oldest assignment first.
          items:
            type: string
            format: uuid

    ChangePasswordRequest:
      type: object
      required: [old_password, new_password]
      properties:
        old_password:
          type: string
          format: password
        new_password:
          type: string
          format: password
          minLength: 8

    ForgotPasswordRequest:
      type: object
      required: [email]
      properties:
        email:
          type: string
          format: email

    ResetPasswordRequest:
      type: object
      required: [token, new_password]
      properties:
        token:
          type: string
        new_password:
          type: string
          minLength: 8

    ProvisionTenantRequest:
      type: object
      required: [name, admin_email]
      properties:
        name:
          type: string
          maxLength: 200
        plan_type:
          type: string
          enum: [cosmi, orbit]
          default: cosmi
        support_tier:
          type: string
          enum: [standard, priority, enterprise]
          default: standard
        seat_limit:
          type: integer
          minimum: 1
          nullable: true
          description: >-
            Booked seats. Omit for an uncapped tenant. The administrator's
            invitation takes the first seat, so 0 is rejected.
        billing_period_end:
          type: string
          format: date
          description: End of the current billing period (YYYY-MM-DD).
        modules:
          type: array
          items:
            type: string
          description: >-
            Catalogue module ids to activate. Omit or leave empty to activate
            the whole catalogue. An unknown id is rejected rather than skipped.
        admin_email:
          type: string
          format: email
          description: >-
            Receives the invitation that creates the first account. Mandatory:
            a tenant without a way in is stranded, not provisioned.

    TenantInfo:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        planType:
          type: string
          enum: [cosmi, orbit]
        supportTier:
          type: string
          enum: [standard, priority, enterprise]
        subscriptionStatus:
          type: string
          enum: [active, past_due, paused]
        billingPeriodEnd:
          type: string
          description: YYYY-MM-DD, empty when no period has been agreed.
        seatLimit:
          type: integer
        hasSeatLimit:
          type: boolean
          description: False means the tenant is uncapped; seatLimit is then meaningless.
        createdAt:
          type: string
          format: date-time
        modules:
          type: array
          items:
            type: string
          description: The module ids activated at provisioning time.

    ProvisionTenantResponse:
      type: object
      properties:
        tenant:
          $ref: "#/components/schemas/TenantInfo"
        invitation:
          $ref: "#/components/schemas/InvitationInfo"
        invitationToken:
          type: string
          description: >-
            Plaintext invitation token. Returned once, never stored and never
            retrievable again.

    CreateInvitationRequest:
      type: object
      required: [email, role]
      properties:
        email:
          type: string
          format: email
        role:
          type: string
          enum: [admin, manager, member]

    AcceptInvitationRequest:
      type: object
      required: [password, first_name, last_name]
      properties:
        password:
          type: string
          format: password
          minLength: 8
        first_name:
          type: string
        last_name:
          type: string

    InvitationInfo:
      type: object
      properties:
        id:
          type: string
          format: uuid
        email:
          type: string
          format: email
        role:
          type: string
        expiresAt:
          type: string
          format: date-time
        createdBy:
          type: string
          format: uuid
        createdAt:
          type: string
          format: date-time

    InvitationResponse:
      type: object
      properties:
        invitation:
          $ref: "#/components/schemas/InvitationInfo"
        token:
          type: string
          description: Plain token, only returned on creation

    ListInvitationsResponse:
      type: object
      properties:
        invitations:
          type: array
          items:
            $ref: "#/components/schemas/InvitationInfo"

    AuthResponse:
      type: object
      description: Returned by register and login. Go encoding/json uses snake_case.
      properties:
        user:
          $ref: "#/components/schemas/UserInfo"
        access_token:
          type: string
        refresh_token:
          type: string

    RefreshResponse:
      type: object
      properties:
        access_token:
          type: string
        refresh_token:
          type: string

    GetUserResponse:
      type: object
      properties:
        user:
          $ref: "#/components/schemas/UserInfo"

    ListUsersResponse:
      type: object
      properties:
        users:
          type: array
          items:
            $ref: "#/components/schemas/UserInfo"
        total:
          type: integer

    # RBAC — effective (multi-role union) permissions. camelCase in
    # EffectiveRole is intentional: this block is app-owned API contract
    # mirroring the frontend type, not a proto passthrough.
    EffectiveRole:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
          description: Technical role name, unique per tenant.
        isSystem:
          type: boolean
          description: True for a Zentria-maintained preset (tenant_id IS NULL).
        color:
          type: string
          description: HSL accent used for role dots in the admin surfaces.
          example: hsl(217 91% 60%)

    EffectiveCapability:
      type: object
      properties:
        scope:
          type: string
          enum: [own, team, all]
          description: >
            Widest data scope any of the account's roles grants for this key.
        sources:
          type: array
          description: >
            IDs of every role contributing this grant, including those that lost
            the scope comparison. Resolvable against the roles list of the same
            response.
          items:
            type: string
            format: uuid
      required: [scope, sources]

    EffectivePermissions:
      type: object
      properties:
        roles:
          type: array
          items:
            $ref: "#/components/schemas/EffectiveRole"
        capabilities:
          type: object
          description: >
            Capability key (module:subject:action) to resolved grant. An absent
            key means denied — default-deny, so no entry ever carries an empty
            scope. Coarse legacy permission names are filtered out.
          additionalProperties:
            $ref: "#/components/schemas/EffectiveCapability"
      required: [roles, capabilities]

    EffectivePermissionsResponse:
      type: object
      properties:
        permissions:
          $ref: "#/components/schemas/EffectivePermissions"
      required: [permissions]

    # Account roster (phase 3 admin surface). app-owned contract mirroring
    # admin-types.ts, not a proto passthrough.
    AdminUser:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: >
            The account id for a real user, or the invitation id while
            status is "invited" — no user row exists yet in that case.
        firstName:
          type: string
          description: Empty for an invited row — invitations do not collect a name.
        lastName:
          type: string
          description: Empty for an invited row — invitations do not collect a name.
        email:
          type: string
        jobTitle:
          type: string
          description: >
            Always "" — the backend has no job-title data on an account
            (that lives in the HR employee record, a different service).
        roles:
          type: array
          items:
            type: string
          description: >
            Role ids (preset or custom). An invited row carries at most the
            one preset id its invitations.role legacy name resolves to.
        hasOverrides:
          type: boolean
          description: Per-user permission overrides (R-6) do not exist yet — always omitted.
        status:
          type: string
          enum: [active, invited, deactivated]
        lastLoginAt:
          type: string
          format: date-time
          nullable: true
          description: >
            Most recent user_sessions row for the account. Null if the
            account never logged in, or every session since has been removed
            by logout/termination.
        invitedAt:
          type: string
          format: date-time
          nullable: true
          description: Null unless status is "invited".
      required: [id, firstName, lastName, email, jobTitle, roles, status, lastLoginAt, invitedAt]

    AdminUsersResponse:
      type: object
      properties:
        users:
          type: array
          items:
            $ref: "#/components/schemas/AdminUser"
      required: [users]

    # RBAC — role administration (Phase 1 wave 1b). camelCase and the null
    # sentinels on tenantId/basedOn are intentional: app-owned contract
    # mirroring the frontend type, not a proto passthrough.
    Role:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
          description: Technical name, unique per tenant.
        description:
          type: string
        tenantId:
          type: string
          format: uuid
          nullable: true
          description: Null for a Zentria-maintained system preset.
        basedOn:
          type: string
          format: uuid
          nullable: true
          description: Id of the preset (or role) a custom role was cloned from. Null on presets.
        isSystem:
          type: boolean
        color:
          type: string
          example: hsl(217 91% 60%)
        memberCount:
          type: integer
          description: Accounts in the caller's tenant currently holding this role.
        capabilityCount:
          type: integer
          description: Number of capability grants the role carries.
      required:
        [id, name, description, tenantId, basedOn, isSystem, color, memberCount, capabilityCount]

    RolesResponse:
      type: object
      properties:
        roles:
          type: array
          items:
            $ref: "#/components/schemas/Role"
      required: [roles]

    RoleResponse:
      type: object
      properties:
        role:
          $ref: "#/components/schemas/Role"
      required: [role]

    CreateRoleRequest:
      type: object
      properties:
        name:
          type: string
          maxLength: 50
        description:
          type: string
          maxLength: 1000
        color:
          type: string
          maxLength: 40
          example: hsl(217 91% 60%)
        basedOn:
          type: string
          format: uuid
          description: >
            Role the new one is cloned from — usually a preset. Every grant of
            that role is copied over, scope included.
      required: [name, basedOn]

    UpdateRoleRequest:
      type: object
      properties:
        name:
          type: string
          maxLength: 50
        description:
          type: string
          maxLength: 1000
        color:
          type: string
          maxLength: 40
          example: hsl(217 91% 60%)

    RoleGrant:
      type: object
      properties:
        scope:
          type: string
          enum: [own, team, all]
      required: [scope]

    RolePermissionsResponse:
      type: object
      properties:
        roleId:
          type: string
          format: uuid
        grants:
          type: object
          description: Capability key (resource:action) → grant. Missing key = not granted.
          additionalProperties:
            $ref: "#/components/schemas/RoleGrant"
      required: [roleId, grants]

    UpdateRolePermissionsRequest:
      type: object
      properties:
        grants:
          type: object
          description: Full replacement grant map — keys absent here are revoked.
          additionalProperties:
            $ref: "#/components/schemas/RoleGrant"
      required: [grants]

    StatusResponse:
      type: object
      properties:
        status:
          type: string

    ErrorResponse:
      type: object
      properties:
        error:
          type: string

    # Custom Fields
    CustomFieldInfo:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        entityType:
          type: string
          enum: [contact, company, deal, activity]
        fieldType:
          type: string
          enum: [text, number, date, boolean, select, multiselect]
        options:
          type: array
          items:
            type: string
        isRequired:
          type: boolean
        sortOrder:
          type: integer
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    CreateCustomFieldRequest:
      type: object
      required: [name, entity_type, field_type]
      properties:
        name:
          type: string
        entity_type:
          type: string
          enum: [contact, company, deal, activity]
        field_type:
          type: string
          enum: [text, number, date, boolean, select, multiselect]
        options:
          type: array
          items:
            type: string
        is_required:
          type: boolean
          default: false

    UpdateCustomFieldRequest:
      type: object
      properties:
        name:
          type: string
        options:
          type: array
          items:
            type: string
        is_required:
          type: boolean

    CustomFieldResponse:
      type: object
      properties:
        customField:
          $ref: "#/components/schemas/CustomFieldInfo"

    ListCustomFieldsResponse:
      type: object
      properties:
        customFields:
          type: array
          items:
            $ref: "#/components/schemas/CustomFieldInfo"

    # Tags
    TagInfo:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        color:
          type: string
          description: Hex color code (#rrggbb)
        entityType:
          type: string
          enum: [contact, company, deal, activity]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    CreateTagRequest:
      type: object
      required: [name, entity_type]
      properties:
        name:
          type: string
        color:
          type: string
          description: Hex color code (#rrggbb)
          default: "#3b82f6"
        entity_type:
          type: string
          enum: [contact, company, deal, activity]

    UpdateTagRequest:
      type: object
      properties:
        name:
          type: string
        color:
          type: string

    TagResponse:
      type: object
      properties:
        tag:
          $ref: "#/components/schemas/TagInfo"

    ListTagsResponse:
      type: object
      properties:
        tags:
          type: array
          items:
            $ref: "#/components/schemas/TagInfo"

    TagIdsRequest:
      type: object
      required: [tag_ids]
      properties:
        tag_ids:
          type: array
          items:
            type: string
            format: uuid

    # Contacts
    ContactInfo:
      type: object
      properties:
        id:
          type: string
          format: uuid
        firstName:
          type: string
        lastName:
          type: string
        email:
          type: string
          format: email
        phone:
          type: string
        title:
          type: string
        companyId:
          type: string
          format: uuid
        companyName:
          type: string
        notes:
          type: string
        tags:
          type: array
          items:
            $ref: "#/components/schemas/TagInfo"
        customFields:
          type: object
          additionalProperties: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    CreateContactRequest:
      type: object
      required: [first_name, last_name]
      properties:
        first_name:
          type: string
        last_name:
          type: string
        email:
          type: string
          format: email
        phone:
          type: string
        title:
          type: string
        company_id:
          type: string
          format: uuid
        notes:
          type: string
        tag_ids:
          type: array
          items:
            type: string
            format: uuid
        custom_fields:
          type: object
          additionalProperties: true

    UpdateContactRequest:
      type: object
      properties:
        first_name:
          type: string
        last_name:
          type: string
        email:
          type: string
          format: email
        phone:
          type: string
        title:
          type: string
        company_id:
          type: string
          format: uuid
        notes:
          type: string
        custom_fields:
          type: object
          additionalProperties: true

    ContactResponse:
      type: object
      properties:
        contact:
          $ref: "#/components/schemas/ContactInfo"

    ListContactsResponse:
      type: object
      properties:
        contacts:
          type: array
          items:
            $ref: "#/components/schemas/ContactInfo"
        total:
          type: integer

    # Leads (contact lifecycle stage — same rows as contacts)
    Lead:
      type: object
      description: >
        A contact at the lead or qualified lifecycle stage. camelCase because
        this is hand-mapped onto the desktop inbox's Lead type rather than
        emitted straight from the proto.
      properties:
        id:
          type: string
          format: uuid
        firstName:
          type: string
        lastName:
          type: string
        company:
          type: string
          description: Linked company name, else the free-text employer from intake.
        email:
          type: string
        phone:
          type: string
        source:
          type: string
          enum: [manual, csv, dialer]
        score:
          type: integer
          description: 0-100, computed server-side from source and data completeness.
        temperature:
          type: string
          enum: [hot, warm, cold]
          description: Effective value — the manual override if pinned, else derived from score.
        temperatureOverride:
          type: string
          enum: [hot, warm, cold]
          description: Present only when a user pinned the temperature by hand.
        status:
          type: string
          enum: [new, contacted, qualified, disqualified]
        lifecycleStage:
          type: string
          enum: [lead, qualified]
        notes:
          type: string
        createdAt:
          type: string
          format: date-time

    LeadResponse:
      type: object
      properties:
        lead:
          $ref: "#/components/schemas/Lead"

    ListLeadsResponse:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/Lead"
        total:
          type: integer

    CreateLeadRequest:
      type: object
      required: [firstName, lastName, source]
      properties:
        firstName:
          type: string
        lastName:
          type: string
        company:
          type: string
        email:
          type: string
          format: email
        phone:
          type: string
        notes:
          type: string
        source:
          type: string
          enum: [manual, csv, dialer]

    UpdateLeadRequest:
      type: object
      description: At least one of status or temperature must be present.
      properties:
        status:
          type: string
          enum: [new, contacted, qualified, disqualified]
        temperature:
          type: string
          enum: [hot, warm, cold, ""]
          description: Empty string clears the manual override.

    # Companies
    CompanyInfo:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        website:
          type: string
        industry:
          type: string
        address:
          type: string
        notes:
          type: string
        tags:
          type: array
          items:
            $ref: "#/components/schemas/TagInfo"
        customFields:
          type: object
          additionalProperties: true
        contactCount:
          type: integer
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    CreateCompanyRequest:
      type: object
      required: [name]
      properties:
        name:
          type: string
        website:
          type: string
        industry:
          type: string
        address:
          type: string
        notes:
          type: string
        tag_ids:
          type: array
          items:
            type: string
            format: uuid
        custom_fields:
          type: object
          additionalProperties: true

    UpdateCompanyRequest:
      type: object
      properties:
        name:
          type: string
        website:
          type: string
        industry:
          type: string
        address:
          type: string
        notes:
          type: string
        custom_fields:
          type: object
          additionalProperties: true

    CompanyResponse:
      type: object
      properties:
        company:
          $ref: "#/components/schemas/CompanyInfo"

    ListCompaniesResponse:
      type: object
      properties:
        companies:
          type: array
          items:
            $ref: "#/components/schemas/CompanyInfo"
        total:
          type: integer

    # Pipeline Stages
    PipelineStageInfo:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        color:
          type: string
          description: Hex color code (#rrggbb)
        sortOrder:
          type: integer
        isWon:
          type: boolean
        isLost:
          type: boolean
        probability:
          type: number
          format: double
          minimum: 0
          maximum: 100
        dealCount:
          type: integer
        totalValue:
          type: number
          format: double
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    CreatePipelineStageRequest:
      type: object
      required: [name]
      properties:
        name:
          type: string
        color:
          type: string
          description: Hex color code (#rrggbb)
          default: "#3b82f6"
        is_won:
          type: boolean
          default: false
        is_lost:
          type: boolean
          default: false
        probability:
          type: number
          format: double
          minimum: 0
          maximum: 100
          default: 0

    UpdatePipelineStageRequest:
      type: object
      properties:
        name:
          type: string
        color:
          type: string
        is_won:
          type: boolean
        is_lost:
          type: boolean
        probability:
          type: number
          format: double
          minimum: 0
          maximum: 100

    ReorderPipelineStagesRequest:
      type: object
      required: [stage_ids]
      properties:
        stage_ids:
          type: array
          items:
            type: string
            format: uuid

    PipelineStageResponse:
      type: object
      properties:
        stage:
          $ref: "#/components/schemas/PipelineStageInfo"

    ListPipelineStagesResponse:
      type: object
      properties:
        stages:
          type: array
          items:
            $ref: "#/components/schemas/PipelineStageInfo"

    # Deals
    DealInfo:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        value:
          type: number
          format: double
        currency:
          type: string
          description: ISO 4217 currency code
        stageId:
          type: string
          format: uuid
        stageName:
          type: string
        contactId:
          type: string
          format: uuid
        contactName:
          type: string
        companyId:
          type: string
          format: uuid
        companyName:
          type: string
        ownerId:
          type: string
          format: uuid
        ownerName:
          type: string
        expectedCloseDate:
          type: string
          format: date
        notes:
          type: string
        tags:
          type: array
          items:
            $ref: "#/components/schemas/TagInfo"
        customFields:
          type: object
          additionalProperties: true
        createdBy:
          type: string
          format: uuid
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        closedAt:
          type: string
          format: date-time

    CreateDealRequest:
      type: object
      required: [name, stage_id]
      properties:
        name:
          type: string
        value:
          type: number
          format: double
          default: 0
        currency:
          type: string
          description: ISO 4217 currency code
          default: EUR
        stage_id:
          type: string
          format: uuid
        contact_id:
          type: string
          format: uuid
        company_id:
          type: string
          format: uuid
        owner_id:
          type: string
          format: uuid
        expected_close_date:
          type: string
          format: date
        notes:
          type: string
        tag_ids:
          type: array
          items:
            type: string
            format: uuid
        custom_fields:
          type: object
          additionalProperties: true

    UpdateDealRequest:
      type: object
      properties:
        name:
          type: string
        value:
          type: number
          format: double
        currency:
          type: string
        contact_id:
          type: string
          format: uuid
        company_id:
          type: string
          format: uuid
        owner_id:
          type: string
          format: uuid
        expected_close_date:
          type: string
          format: date
        notes:
          type: string
        custom_fields:
          type: object
          additionalProperties: true

    MoveDealToStageRequest:
      type: object
      required: [stage_id]
      properties:
        stage_id:
          type: string
          format: uuid

    DealResponse:
      type: object
      properties:
        deal:
          $ref: "#/components/schemas/DealInfo"

    ListDealsResponse:
      type: object
      properties:
        deals:
          type: array
          items:
            $ref: "#/components/schemas/DealInfo"
        total:
          type: integer

    # ============================================================================
    # Activity Schemas
    # ============================================================================

    ActivityInfo:
      type: object
      properties:
        id:
          type: string
          format: uuid
        activity_type:
          type: string
          enum: [call, meeting, note, email, task]
        subject:
          type: string
        description:
          type: string
        contact_id:
          type: string
          format: uuid
        contact_name:
          type: string
        company_id:
          type: string
          format: uuid
        company_name:
          type: string
        deal_id:
          type: string
          format: uuid
        deal_name:
          type: string
        assigned_to:
          type: string
          format: uuid
        assigned_to_name:
          type: string
        due_date:
          type: string
          format: date-time
        is_completed:
          type: boolean
        completed_at:
          type: string
          format: date-time
        created_by:
          type: string
          format: uuid
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time

    CreateActivityRequest:
      type: object
      required: [activity_type, subject]
      properties:
        activity_type:
          type: string
          enum: [call, meeting, note, email, task]
        subject:
          type: string
        description:
          type: string
        contact_id:
          type: string
          format: uuid
        company_id:
          type: string
          format: uuid
        deal_id:
          type: string
          format: uuid
        assigned_to:
          type: string
          format: uuid
        due_date:
          type: string
          format: date-time

    UpdateActivityRequest:
      type: object
      properties:
        subject:
          type: string
        description:
          type: string
        contact_id:
          type: string
          format: uuid
        company_id:
          type: string
          format: uuid
        deal_id:
          type: string
          format: uuid
        assigned_to:
          type: string
          format: uuid
        due_date:
          type: string
          format: date-time

    ActivityResponse:
      type: object
      properties:
        activity:
          $ref: "#/components/schemas/ActivityInfo"

    ListActivitiesResponse:
      type: object
      properties:
        activities:
          type: array
          items:
            $ref: "#/components/schemas/ActivityInfo"
        total:
          type: integer

    # ============================================================================
    # Search Schemas
    # ============================================================================

    SearchResult:
      type: object
      properties:
        id:
          type: string
          format: uuid
        entity_type:
          type: string
          enum: [contact, company, deal, activity]
        title:
          type: string
          description: Primary display text (name, subject)
        subtitle:
          type: string
          description: Secondary display text (email, value)
        score:
          type: number
          format: double
          description: Relevance score (higher is better)

    SearchResponse:
      type: object
      properties:
        results:
          type: array
          items:
            $ref: "#/components/schemas/SearchResult"

    # Saved Filters Schemas

    SavedFilter:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        entity_type:
          type: string
          enum: [contact, company, deal, activity]
        filter_json:
          type: string
          description: JSON-encoded filter criteria
        is_default:
          type: boolean
        created_by:
          type: string
          format: uuid
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time

    CreateSavedFilterRequest:
      type: object
      required: [name, entity_type, filter_json]
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 255
        entity_type:
          type: string
          enum: [contact, company, deal, activity]
        filter_json:
          type: string
          description: JSON-encoded filter criteria
        is_default:
          type: boolean
          default: false

    UpdateSavedFilterRequest:
      type: object
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 255
        filter_json:
          type: string
          description: JSON-encoded filter criteria
        is_default:
          type: boolean

    SavedFilterResponse:
      type: object
      properties:
        filter:
          $ref: "#/components/schemas/SavedFilter"

    ListSavedFiltersResponse:
      type: object
      properties:
        filters:
          type: array
          items:
            $ref: "#/components/schemas/SavedFilter"

    # Report Schemas

    PipelineStageValue:
      type: object
      properties:
        stage_id:
          type: string
          format: uuid
        stage_name:
          type: string
        deal_count:
          type: integer
        total_value:
          type: number
          format: double
        weighted_value:
          type: number
          format: double
          description: value × (probability / 100)

    PipelineReportResponse:
      type: object
      properties:
        stages:
          type: array
          items:
            $ref: "#/components/schemas/PipelineStageValue"
        total_deals:
          type: integer
        total_value:
          type: number
          format: double
        total_weighted_value:
          type: number
          format: double

    ConversionMetric:
      type: object
      properties:
        from_stage:
          type: string
        to_stage:
          type: string
        converted_count:
          type: integer
        conversion_rate:
          type: number
          format: double
          description: Percentage of total transitions
        average_days:
          type: number
          format: double
          description: Average days between stages

    ConversionReportResponse:
      type: object
      properties:
        metrics:
          type: array
          items:
            $ref: "#/components/schemas/ConversionMetric"
        overall_win_rate:
          type: number
          format: double
          description: Percentage of deals that reached Won stage
        average_deal_cycle:
          type: number
          format: double
          description: Average days from creation to close

    ActivityMetric:
      type: object
      properties:
        activity_type:
          type: string
          enum: [call, meeting, note, email, task]
        total_count:
          type: integer
        completed_count:
          type: integer
        overdue_count:
          type: integer

    ActivityReportResponse:
      type: object
      properties:
        metrics:
          type: array
          items:
            $ref: "#/components/schemas/ActivityMetric"
        total_activities:
          type: integer
        completion_rate:
          type: number
          format: double
          description: Percentage of completed activities

    # ========================================================================
    # Chat Schemas
    # ========================================================================

    ChannelInfo:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        description:
          type: string
        is_private:
          type: boolean
        is_dm:
          type: boolean
        is_archived:
          type: boolean
        created_by:
          type: string
          format: uuid
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        member_count:
          type: integer
        my_role:
          type: string
          enum: [owner, admin, member]
        last_message:
          $ref: "#/components/schemas/MessageInfo"
        unread_count:
          type: integer
          description: Number of unread messages in the channel for the current user

    MessageInfo:
      type: object
      properties:
        id:
          type: string
          format: uuid
        channel_id:
          type: string
          format: uuid
        content:
          type: string
        is_deleted:
          type: boolean
        edited_at:
          type: string
          format: date-time
        created_by:
          type: string
          format: uuid
        created_at:
          type: string
          format: date-time
        sender_first_name:
          type: string
        sender_last_name:
          type: string
        parent_message_id:
          type: string
          format: uuid
          description: ID of parent message (for thread replies)
        reply_count:
          type: integer
          description: Number of thread replies
        mentions:
          type: array
          items:
            $ref: "#/components/schemas/MentionInfo"
          description: Users mentioned in this message
        files:
          type: array
          items:
            $ref: "#/components/schemas/FileInfo"
          description: Files attached to this message

    MemberInfo:
      type: object
      properties:
        user_id:
          type: string
          format: uuid
        role:
          type: string
          enum: [owner, admin, member]
        joined_at:
          type: string
          format: date-time
        last_read_at:
          type: string
          format: date-time
        first_name:
          type: string
        last_name:
          type: string
        email:
          type: string

    CreateChannelRequest:
      type: object
      required: [name]
      properties:
        name:
          type: string
        description:
          type: string
        is_private:
          type: boolean
          default: false
        initial_member_ids:
          type: array
          items:
            type: string
            format: uuid

    UpdateChannelRequest:
      type: object
      properties:
        name:
          type: string
        description:
          type: string

    ChannelResponse:
      type: object
      properties:
        channel:
          $ref: "#/components/schemas/ChannelInfo"

    ListChannelsResponse:
      type: object
      properties:
        channels:
          type: array
          items:
            $ref: "#/components/schemas/ChannelInfo"
        total:
          type: integer

    MemberResponse:
      type: object
      properties:
        member:
          $ref: "#/components/schemas/MemberInfo"

    ListMembersResponse:
      type: object
      properties:
        members:
          type: array
          items:
            $ref: "#/components/schemas/MemberInfo"
        total:
          type: integer

    MessageResponse:
      type: object
      properties:
        message:
          $ref: "#/components/schemas/MessageInfo"

    ListMessagesResponse:
      type: object
      properties:
        messages:
          type: array
          items:
            $ref: "#/components/schemas/MessageInfo"
        has_more:
          type: boolean

    GetOrCreateDMResponse:
      type: object
      properties:
        channel:
          $ref: "#/components/schemas/ChannelInfo"
        created:
          type: boolean
          description: true if a new DM was created, false if existing

    ThreadRepliesResponse:
      type: object
      properties:
        parent:
          $ref: "#/components/schemas/MessageInfo"
        replies:
          type: array
          items:
            $ref: "#/components/schemas/MessageInfo"
        has_more:
          type: boolean

    MentionInfo:
      type: object
      properties:
        user_id:
          type: string
          format: uuid
        mention_type:
          type: string
          enum: [user, channel, everyone]
        first_name:
          type: string
        last_name:
          type: string

    UnreadCountsResponse:
      type: object
      properties:
        unread_counts:
          type: object
          additionalProperties:
            type: integer
          description: Map of channel_id to unread message count

    UserMentionsResponse:
      type: object
      properties:
        mentions:
          type: array
          items:
            type: object
            properties:
              message_id:
                type: string
                format: uuid
              channel_id:
                type: string
                format: uuid
              content:
                type: string
              mention_type:
                type: string
                enum: [user, channel, everyone]
              sender_first_name:
                type: string
              sender_last_name:
                type: string
              created_at:
                type: string
                format: date-time
        total:
          type: integer

    FileInfo:
      type: object
      properties:
        id:
          type: string
          format: uuid
        message_id:
          type: string
          format: uuid
        channel_id:
          type: string
          format: uuid
        filename:
          type: string
        mime_type:
          type: string
        file_size:
          type: integer
          format: int64
          description: File size in bytes
        uploaded_by:
          type: string
          format: uuid
        created_at:
          type: string
          format: date-time
        uploader_first_name:
          type: string
        uploader_last_name:
          type: string
        has_thumbnail:
          type: boolean
          description: Whether a thumbnail is available (images only)

    ChatSearchResult:
      type: object
      properties:
        type:
          type: string
          enum: [message, file]
          description: Result type
        id:
          type: string
          format: uuid
        channel_id:
          type: string
          format: uuid
        channel_name:
          type: string
        score:
          type: number
          format: double
          description: Relevance score
        snippet:
          type: string
          description: Highlighted text snippet with <mark> tags
        created_at:
          type: string
          format: date-time
        first_name:
          type: string
        last_name:
          type: string
        message_id:
          type: string
          format: uuid
          description: Present for message results
        parent_message_id:
          type: string
          format: uuid
          description: Present if message is a thread reply
        file_id:
          type: string
          format: uuid
          description: Present for file results
        filename:
          type: string
          description: Present for file results
        mime_type:
          type: string
          description: Present for file results
        file_size:
          type: integer
          format: int64
          description: Present for file results

    # =========================================================================
    # Dialer Schemas (supervisor overview + contact call history)
    # Wire shape: response.Proto / protojson — snake_case fields, integer enums
    # (UseEnumNumbers), RFC3339 timestamps, optional fields omitted when unset.
    # =========================================================================

    SupervisorOverview:
      type: object
      properties:
        agents:
          type: array
          items:
            $ref: "#/components/schemas/SupervisorAgent"
        recent_calls:
          type: array
          items:
            $ref: "#/components/schemas/RecentCall"
        totals:
          $ref: "#/components/schemas/SupervisorTotals"

    SupervisorAgent:
      type: object
      properties:
        user_id:
          type: string
          format: uuid
        first_name:
          type: string
        last_name:
          type: string
        status:
          type: integer
          description: AgentDialerStatus enum serialized as integer (UseEnumNumbers).
        campaign_id:
          type: string
          format: uuid
          description: Present only while the agent is assigned to a campaign.
        since:
          type: string
          format: date-time
        calls_today:
          type: integer
        avg_duration_seconds:
          type: number
          format: float
        active_campaign_name:
          type: string

    RecentCall:
      type: object
      properties:
        id:
          type: string
          format: uuid
        contact_name:
          type: string
        contact_company:
          type: string
        outcome_label:
          type: string
        outcome_color:
          type: string
        is_positive:
          type: boolean
        duration_seconds:
          type: integer
        agent_name:
          type: string
        campaign_name:
          type: string
        created_at:
          type: string
          format: date-time

    SupervisorTotals:
      type: object
      properties:
        active_agents:
          type: integer
        on_call:
          type: integer
        calls_today:
          type: integer
        appointments_today:
          type: integer

    ContactCall:
      type: object
      properties:
        id:
          type: string
          format: uuid
        outcome_label:
          type: string
        outcome_color:
          type: string
        is_positive:
          type: boolean
        duration_seconds:
          type: integer
        notes:
          type: string
        agent_name:
          type: string
        created_at:
          type: string
          format: date-time

    # =========================================================================
    # Notification Schemas
    # =========================================================================

    Notification:
      type: object
      properties:
        id:
          type: string
          format: uuid
        user_id:
          type: string
          format: uuid
        event_type_key:
          type: string
        module_id:
          type: string
        priority:
          type: string
          enum: [low, normal, high, urgent]
          description: >-
            Normalized priority string. NOTE on wire drift: the notification
            list/action endpoints serialize the proto via response.JSON, so the
            raw wire value is an integer enum (0-4). The desktop client
            normalizes it to this string (0,2 -> normal, 1 -> low, 3 -> urgent,
            4 -> high) in normalizeNotification. Demo/mock mode already emits the
            string form.
        actor_id:
          type: string
          format: uuid
        actor_name:
          type: string
          nullable: true
          description: >-
            Resolved display name of the actor that triggered the notification.
            Null until the backend resolves the actor.
        resource_id:
          type: string
        title:
          type: string
        body:
          type: string
        deep_link:
          type: string
        group_key:
          type: string
        group_count:
          type: integer
        is_read:
          type: boolean
        is_pinned:
          type: boolean
          description: Whether the current user pinned this notification (pinned items sort first).
        is_dismissed:
          type: boolean
          description: Whether the current user dismissed it (excluded from default lists and the unread count).
        read_at:
          type: string
          format: date-time
          description: >-
            ISO-8601 timestamp. Raw wire shape is a proto Timestamp object
            ({seconds,nanos}); the desktop client normalizes it via
            normalizeWireTimestamps.
        delivered_desktop:
          type: boolean
        created_at:
          type: string
          format: date-time
          description: >-
            ISO-8601 timestamp. Raw wire shape is a proto Timestamp object
            ({seconds,nanos}); normalized client-side.

    SnoozeNotificationResponse:
      type: object
      description: >-
        Flat response for the snooze endpoint (not the wrapped `{notification}`
        shape used by pin/dismiss) — matches the desktop client's SnoozeResponse
        type exactly.
      properties:
        id:
          type: string
          format: uuid
        snoozed_until:
          type: string
          format: date-time

    NotificationPreference:
      type: object
      properties:
        id:
          type: string
          format: uuid
        user_id:
          type: string
          format: uuid
        event_type_key:
          type: string
        module_id:
          type: string
        in_app:
          type: boolean
        desktop_push:
          type: boolean
        sound:
          type: string

    UpdateNotificationPreferenceRequest:
      type: object
      properties:
        event_type_key:
          type: string
          description: Specific event type key (mutually exclusive with module_id for module-level default)
        module_id:
          type: string
          description: Module ID for module-level default preference
        in_app:
          type: boolean
        desktop_push:
          type: boolean
        sound:
          type: string

    EventType:
      type: object
      properties:
        id:
          type: string
          format: uuid
        module_id:
          type: string
        event_key:
          type: string
        display_name:
          type: string
        description:
          type: string
        default_priority:
          type: string
          enum: [low, normal, high, urgent]
        default_in_app:
          type: boolean
        default_desktop_push:
          type: boolean
        default_sound:
          type: string

    NotificationMute:
      type: object
      properties:
        id:
          type: string
          format: uuid
        user_id:
          type: string
          format: uuid
        module_id:
          type: string
        resource_id:
          type: string
        created_at:
          type: string
          format: date-time

    QuietHours:
      type: object
      properties:
        user_id:
          type: string
          format: uuid
        start_time:
          type: string
          description: Start time in HH:MM format
        end_time:
          type: string
          description: End time in HH:MM format
        timezone:
          type: string
          description: IANA timezone (e.g. Europe/Berlin)
        days_of_week:
          type: array
          items:
            type: integer
          description: Days of week (0=Sunday, 6=Saturday)
        enabled:
          type: boolean
        manual_dnd:
          type: boolean
        manual_dnd_until:
          type: string
          format: date-time

    UpdateQuietHoursRequest:
      type: object
      required: [start_time, end_time, timezone, enabled]
      properties:
        start_time:
          type: string
        end_time:
          type: string
        timezone:
          type: string
        days_of_week:
          type: array
          items:
            type: integer
        enabled:
          type: boolean

    # ========================================================================
    # Dashboard Schemas
    # ========================================================================

    DashboardLayoutResponse:
      type: object
      properties:
        layout:
          type: array
          items:
            type: object
            additionalProperties: true
          description: Grid layout items (react-grid-layout format)
        active_widgets:
          type: array
          items:
            type: string
          description: List of active widget IDs
        is_custom:
          type: boolean
          description: Whether this is a user override (true) or role default (false)
        updated_at:
          type: string
          format: date-time

    SaveDashboardLayoutRequest:
      type: object
      required: [layout, active_widgets]
      properties:
        layout:
          type: array
          items:
            type: object
            additionalProperties: true
          description: Grid layout items
        active_widgets:
          type: array
          items:
            type: string
          description: List of active widget IDs

    DashboardDefault:
      type: object
      properties:
        id:
          type: string
          format: uuid
        role:
          type: string
          enum: [admin, manager, member]
        layout:
          type: array
          items:
            type: object
            additionalProperties: true
        active_widgets:
          type: array
          items:
            type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time

    SaveDashboardDefaultsRequest:
      type: object
      required: [layout, active_widgets]
      properties:
        layout:
          type: array
          items:
            type: object
            additionalProperties: true
        active_widgets:
          type: array
          items:
            type: string

    # ===========================================================================
    # Work / Project Management Schemas
    # ===========================================================================

    CreateProjectRequest:
      type: object
      required: [name, project_key]
      properties:
        name:
          type: string
          minLength: 3
          maxLength: 100
        description:
          type: string
        project_key:
          type: string
          minLength: 2
          maxLength: 10
          pattern: "^[A-Z0-9]+$"

    UpdateProjectRequest:
      type: object
      properties:
        name:
          type: string
          minLength: 3
          maxLength: 100
        description:
          type: string

    ProjectResponse:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        description:
          type: string
        project_key:
          type: string
        next_task_number:
          type: integer
        is_template:
          type: boolean
        template_source_id:
          type: string
          format: uuid
        created_by:
          type: string
          format: uuid
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        archived_at:
          type: string
          format: date-time
        member_count:
          type: integer
        task_count:
          type: integer
        owner_name:
          type: string

    ProjectMemberResponse:
      type: object
      properties:
        project_id:
          type: string
          format: uuid
        user_id:
          type: string
          format: uuid
        role:
          type: string
          enum: [owner, member, viewer]
        added_at:
          type: string
          format: date-time
        first_name:
          type: string
        last_name:
          type: string

    CreateProjectStatusRequest:
      type: object
      required: [name]
      properties:
        name:
          type: string
          maxLength: 50
        color:
          type: string
        is_default:
          type: boolean
          default: false
        is_closed:
          type: boolean
          default: false

    UpdateProjectStatusRequest:
      type: object
      properties:
        name:
          type: string
          maxLength: 50
        color:
          type: string
        is_default:
          type: boolean
        is_closed:
          type: boolean

    ProjectStatusResponse:
      type: object
      properties:
        id:
          type: string
          format: uuid
        project_id:
          type: string
          format: uuid
        name:
          type: string
        color:
          type: string
        sort_order:
          type: integer
        is_default:
          type: boolean
        is_closed:
          type: boolean
        created_at:
          type: string
          format: date-time

    UserProjectPreferenceResponse:
      type: object
      properties:
        user_id:
          type: string
          format: uuid
        project_id:
          type: string
          format: uuid
        view_type:
          type: string
          enum: [list, kanban, gantt]
        list_group_by:
          type: string
        list_sort_by:
          type: string
        list_sort_desc:
          type: boolean

    SetUserProjectPreferenceRequest:
      type: object
      properties:
        view_type:
          type: string
          enum: [list, kanban, gantt]
        list_group_by:
          type: string
        list_sort_by:
          type: string
        list_sort_desc:
          type: boolean

    CreateTaskRequest:
      type: object
      required: [title, priority]
      properties:
        project_id:
          type: string
          format: uuid
        title:
          type: string
          maxLength: 500
        description:
          type: string
        status_id:
          type: string
          format: uuid
        priority:
          type: string
          enum: [urgent, high, normal, low]
        assignee_id:
          type: string
          format: uuid
        parent_task_id:
          type: string
          format: uuid
        due_date:
          type: string
          format: date-time
        custom_fields:
          type: array
          items:
            type: object
            required: [field_id, value]
            properties:
              field_id:
                type: string
                format: uuid
              value:
                type: string

    UpdateTaskRequest:
      type: object
      properties:
        title:
          type: string
          maxLength: 500
        description:
          type: string
        status_id:
          type: string
          format: uuid
        project_id:
          type: string
          format: uuid
          description: Reassign the task to another project (or to none for a standalone task).
        priority:
          type: string
          enum: [urgent, high, normal, low]
        assignee_id:
          type: string
          format: uuid
        due_date:
          type: string
          format: date-time
        custom_fields:
          type: array
          items:
            type: object
            required: [field_id, value]
            properties:
              field_id:
                type: string
                format: uuid
              value:
                type: string

    TaskResponse:
      type: object
      properties:
        id:
          type: string
          format: uuid
        project_id:
          type: string
          format: uuid
        task_number:
          type: integer
        title:
          type: string
        description:
          type: string
        status_id:
          type: string
          format: uuid
        status_name:
          type: string
        status_color:
          type: string
        priority:
          type: string
          enum: [urgent, high, normal, low]
        assignee_id:
          type: string
          format: uuid
        assignee_name:
          type: string
        parent_task_id:
          type: string
          format: uuid
        depth:
          type: integer
        sort_order:
          type: number
          format: double
        due_date:
          type: string
          format: date-time
        created_by:
          type: string
          format: uuid
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        completed_at:
          type: string
          format: date-time
        subtask_count:
          type: integer
        has_blocked_deps:
          type: boolean
        project_key:
          type: string
        custom_fields:
          type: object
          additionalProperties:
            type: string
        label_ids:
          type: array
          items:
            type: string
            format: uuid

    WorkLabel:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
          maxLength: 100
        color:
          type: string
          pattern: '^#[0-9A-Fa-f]{6}$'

    WorkCustomFieldDefinition:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
          maxLength: 100
        field_type:
          type: string
          enum: [text, number, date, boolean, select, multi_select, url, email, phone]
        options:
          type: array
          items:
            type: string
        position:
          type: integer

    TaskDependencyResponse:
      type: object
      properties:
        id:
          type: string
          format: uuid
        source_task_id:
          type: string
          format: uuid
        target_task_id:
          type: string
          format: uuid
        dependency_type:
          type: string
          enum: [blocks, blocked_by, relates_to, duplicates]
        created_by:
          type: string
          format: uuid
        created_at:
          type: string
          format: date-time

    TaskCommentResponse:
      type: object
      properties:
        id:
          type: string
          format: uuid
        task_id:
          type: string
          format: uuid
        author_id:
          type: string
          format: uuid
        author_name:
          type: string
        content:
          type: string
        quoted_comment_id:
          type: string
          format: uuid
        quoted_content_preview:
          type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time

    TaskEntityLinkResponse:
      type: object
      properties:
        id:
          type: string
          format: uuid
        task_id:
          type: string
          format: uuid
        entity_type:
          type: string
          enum: [contact, company, deal, activity, project]
        entity_id:
          type: string
          format: uuid
        entity_display_name:
          type: string
        created_by:
          type: string
          format: uuid
        created_at:
          type: string
          format: date-time

    TaskActivityResponse:
      type: object
      properties:
        id:
          type: string
          format: uuid
        task_id:
          type: string
          format: uuid
        actor_id:
          type: string
          format: uuid
        actor_name:
          type: string
        action:
          type: string
        field_name:
          type: string
        old_value:
          type: string
        new_value:
          type: string
        created_at:
          type: string
          format: date-time

    TaskFileResponse:
      type: object
      properties:
        id:
          type: string
          format: uuid
        task_id:
          type: string
          format: uuid
        filename:
          type: string
        mime_type:
          type: string
        file_size:
          type: integer
          format: int64
        storage_key:
          type: string
        thumbnail_key:
          type: string
        uploaded_by:
          type: string
          format: uuid
        uploaded_by_name:
          type: string
        created_at:
          type: string
          format: date-time

    ContactFileResponse:
      type: object
      properties:
        id:
          type: string
          format: uuid
        contact_id:
          type: string
          format: uuid
        filename:
          type: string
        mime_type:
          type: string
        storage_key:
          type: string
        created_at:
          type: string
          format: date-time

    TimeEntryResponse:
      type: object
      properties:
        id:
          type: string
          format: uuid
        task_id:
          type: string
          format: uuid
        user_id:
          type: string
          format: uuid
        user_name:
          type: string
        started_at:
          type: string
          format: date-time
        ended_at:
          type: string
          format: date-time
        duration_seconds:
          type: integer
        description:
          type: string
        is_manual:
          type: boolean
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time

    ActiveTimerResponse:
      type: object
      properties:
        id:
          type: string
          format: uuid
        task_id:
          type: string
          format: uuid
        task_title:
          type: string
        user_id:
          type: string
          format: uuid
        started_at:
          type: string
          format: date-time

    TimeSummaryResponse:
      type: object
      properties:
        task_id:
          type: string
          format: uuid
        total_duration_seconds:
          type: integer
        entry_count:
          type: integer

    # =========================================================================
    # Calendar schemas
    # =========================================================================

    CalendarSchema:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        description:
          type: string
        calendar_type:
          type: string
          enum: [personal, shared, resource]
        color:
          type: string
        owner_id:
          type: string
          format: uuid
        is_default:
          type: boolean
        timezone:
          type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time

    CalendarWithMemberInfoSchema:
      allOf:
        - $ref: "#/components/schemas/CalendarSchema"
        - type: object
          properties:
            permission:
              type: string
              enum: [view, edit, admin]
            color_override:
              type: string
              nullable: true
            is_visible:
              type: boolean

    CalendarMemberSchema:
      type: object
      properties:
        calendar_id:
          type: string
          format: uuid
        user_id:
          type: string
          format: uuid
        permission:
          type: string
          enum: [view, edit, admin]
        color_override:
          type: string
          nullable: true
        is_visible:
          type: boolean
        created_at:
          type: string
          format: date-time
        user_first_name:
          type: string
        user_last_name:
          type: string

    CalendarEventSchema:
      type: object
      properties:
        id:
          type: string
          format: uuid
        calendar_id:
          type: string
          format: uuid
        title:
          type: string
        description:
          type: string
        location:
          type: string
        resource_id:
          type: string
          format: uuid
          nullable: true
        start_time:
          type: string
          format: date-time
        end_time:
          type: string
          format: date-time
        is_all_day:
          type: boolean
        timezone:
          type: string
        rrule:
          type: string
          nullable: true
        recurrence_end:
          type: string
          format: date-time
          nullable: true
        has_video_call:
          type: boolean
        livekit_room_name:
          type: string
          nullable: true
        category_id:
          type: string
          format: uuid
          nullable: true
        created_by:
          type: string
          format: uuid
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time

    ExpandedEventSchema:
      allOf:
        - $ref: "#/components/schemas/CalendarEventSchema"
        - type: object
          properties:
            original_event_id:
              type: string
              format: uuid
              nullable: true
            original_date:
              type: string
              nullable: true
            calendar_name:
              type: string
            calendar_color:
              type: string
            display_color:
              type: string
            category_name:
              type: string
              nullable: true
            category_color:
              type: string
              nullable: true
            resource_name:
              type: string
              nullable: true
            attendee_count:
              type: integer
            my_rsvp:
              type: string
              enum: [pending, accepted, declined, tentative]
              nullable: true

    EventAttendeeSchema:
      type: object
      properties:
        event_id:
          type: string
          format: uuid
        user_id:
          type: string
          format: uuid
        rsvp_status:
          type: string
          enum: [pending, accepted, declined, tentative]
        responded_at:
          type: string
          format: date-time
          nullable: true
        created_at:
          type: string
          format: date-time
        user_first_name:
          type: string
        user_last_name:
          type: string

    EventExceptionSchema:
      type: object
      properties:
        id:
          type: string
          format: uuid
        event_id:
          type: string
          format: uuid
        original_date:
          type: string
        is_cancelled:
          type: boolean
        title:
          type: string
          nullable: true
        description:
          type: string
          nullable: true
        location:
          type: string
          nullable: true
        start_time:
          type: string
          format: date-time
          nullable: true
        end_time:
          type: string
          format: date-time
          nullable: true
        resource_id:
          type: string
          format: uuid
          nullable: true
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time

    EventReminderSchema:
      type: object
      properties:
        id:
          type: string
          format: uuid
        event_id:
          type: string
          format: uuid
        minutes_before:
          type: integer
        created_at:
          type: string
          format: date-time

    EventCategorySchema:
      type: object
      properties:
        id:
          type: string
          format: uuid
        user_id:
          type: string
          format: uuid
        name:
          type: string
        color:
          type: string
        sort_order:
          type: integer
        created_at:
          type: string
          format: date-time

    ResourceSchema:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        resource_type:
          type: string
          enum: [room, equipment, vehicle]
        capacity:
          type: integer
          nullable: true
        floor:
          type: string
          nullable: true
        location:
          type: string
          nullable: true
        description:
          type: string
          nullable: true
        is_active:
          type: boolean
        tags:
          type: array
          items:
            type: string
        created_by:
          type: string
          format: uuid
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time

    ResourceBookingSchema:
      type: object
      properties:
        id:
          type: string
          format: uuid
        resource_id:
          type: string
          format: uuid
        event_id:
          type: string
          format: uuid
        booked_by:
          type: string
          format: uuid
        start_time:
          type: string
          format: date-time
        end_time:
          type: string
          format: date-time
        cancelled_at:
          type: string
          format: date-time
          nullable: true
        created_at:
          type: string
          format: date-time
        resource_name:
          type: string
        event_title:
          type: string

    PublicHolidaySchema:
      type: object
      properties:
        id:
          type: string
          format: uuid
        date:
          type: string
          format: date
        name:
          type: string
        local_name:
          type: string
        country_code:
          type: string
        is_global:
          type: boolean
        subdivision_codes:
          type: array
          items:
            type: string
        holiday_type:
          type: string
        year:
          type: integer

    CalendarPreferencesSchema:
      type: object
      properties:
        user_id:
          type: string
          format: uuid
        default_view:
          type: string
          enum: [day, week, month]
        week_days:
          type: integer
          enum: [5, 7]
        default_reminder_minutes:
          type: integer
        default_allday_reminder_minutes:
          type: integer
        subdivision_code:
          type: string
          nullable: true
        show_task_deadlines:
          type: boolean

    TaskDeadlineStubSchema:
      type: object
      properties:
        task_id:
          type: string
          format: uuid
        title:
          type: string
        due_date:
          type: string
          format: date-time
        project_key:
          type: string
          nullable: true
        priority:
          type: string

    # =========================================================================
    # Calendar request schemas
    # =========================================================================

    CreateCalendarRequest:
      type: object
      required: [name, calendar_type]
      properties:
        name:
          type: string
        description:
          type: string
        calendar_type:
          type: string
          enum: [personal, shared, resource]
        color:
          type: string
        timezone:
          type: string

    UpdateCalendarRequest:
      type: object
      properties:
        name:
          type: string
        description:
          type: string
        color:
          type: string
        timezone:
          type: string

    AddCalendarMemberRequest:
      type: object
      required: [user_id, permission]
      properties:
        user_id:
          type: string
          format: uuid
        permission:
          type: string
          enum: [view, edit, admin]

    UpdateCalendarMemberPermissionRequest:
      type: object
      required: [permission]
      properties:
        permission:
          type: string
          enum: [view, edit, admin]

    CreateEventRequest:
      type: object
      required: [calendar_id, title, start_time, end_time]
      properties:
        calendar_id:
          type: string
          format: uuid
        title:
          type: string
        description:
          type: string
        location:
          type: string
        resource_id:
          type: string
          format: uuid
        start_time:
          type: string
          format: date-time
        end_time:
          type: string
          format: date-time
        is_all_day:
          type: boolean
          default: false
        timezone:
          type: string
        rrule:
          type: string
          description: RFC 5545 RRULE string for recurring events
        has_video_call:
          type: boolean
          default: false
        category_id:
          type: string
          format: uuid
        attendee_ids:
          type: array
          items:
            type: string
            format: uuid

    UpdateEventRequest:
      type: object
      properties:
        title:
          type: string
        description:
          type: string
        location:
          type: string
        resource_id:
          type: string
          format: uuid
          nullable: true
        start_time:
          type: string
          format: date-time
        end_time:
          type: string
          format: date-time
        is_all_day:
          type: boolean
        category_id:
          type: string
          format: uuid
          nullable: true
        has_video_call:
          type: boolean

    UpdateRecurringEventRequest:
      type: object
      required: [scope, original_date, changes]
      properties:
        scope:
          type: string
          enum: [this, this_and_future, all]
          description: "Scope of the recurring update: this (single exception), this_and_future (split series), all (update master)"
        original_date:
          type: string
          description: The original occurrence date being edited (YYYY-MM-DD)
        changes:
          $ref: "#/components/schemas/UpdateEventRequest"

    RSVPRequest:
      type: object
      required: [status]
      properties:
        status:
          type: string
          enum: [accepted, declined, tentative]

    SetEventRemindersRequest:
      type: object
      required: [minutes_before]
      properties:
        minutes_before:
          type: array
          items:
            type: integer
          description: List of reminder offsets in minutes before event start

    CreateResourceRequest:
      type: object
      required: [name, resource_type]
      properties:
        name:
          type: string
        resource_type:
          type: string
          enum: [room, equipment, vehicle]
        capacity:
          type: integer
        floor:
          type: string
        location:
          type: string
        description:
          type: string
        tags:
          type: array
          items:
            type: string

    UpdateResourceRequest:
      type: object
      properties:
        name:
          type: string
        resource_type:
          type: string
          enum: [room, equipment, vehicle]
        capacity:
          type: integer
          nullable: true
        floor:
          type: string
          nullable: true
        location:
          type: string
          nullable: true
        description:
          type: string
          nullable: true

    BookResourceRequest:
      type: object
      required: [resource_id, event_id, start_time, end_time]
      properties:
        resource_id:
          type: string
          format: uuid
        event_id:
          type: string
          format: uuid
        start_time:
          type: string
          format: date-time
        end_time:
          type: string
          format: date-time

    SeedHolidaysRequest:
      type: object
      required: [year, country_code]
      properties:
        year:
          type: integer
        country_code:
          type: string
          description: ISO 3166-1 alpha-2 country code (DE, AT, CH)

    CreateEventCategoryRequest:
      type: object
      required: [name, color]
      properties:
        name:
          type: string
        color:
          type: string

    UpdateCalendarPreferencesRequest:
      type: object
      properties:
        default_view:
          type: string
          enum: [day, week, month]
        week_days:
          type: integer
          enum: [5, 7]
        default_reminder_minutes:
          type: integer
        default_allday_reminder_minutes:
          type: integer
        subdivision_code:
          type: string
        show_task_deadlines:
          type: boolean

    GenerateJoinTokenRequest:
      type: object
      required: [display_name]
      properties:
        display_name:
          type: string

    # =========================================================================
    # Calendar response schemas
    # =========================================================================

    CalendarResponse:
      type: object
      properties:
        calendar:
          $ref: "#/components/schemas/CalendarSchema"

    CalendarListResponse:
      type: object
      properties:
        calendars:
          type: array
          items:
            $ref: "#/components/schemas/CalendarWithMemberInfoSchema"

    CalendarMembersResponse:
      type: object
      properties:
        members:
          type: array
          items:
            $ref: "#/components/schemas/CalendarMemberSchema"

    CalendarEventResponse:
      type: object
      properties:
        event:
          $ref: "#/components/schemas/CalendarEventSchema"

    ExpandedEventListResponse:
      type: object
      properties:
        events:
          type: array
          items:
            $ref: "#/components/schemas/ExpandedEventSchema"

    EventAttendeesResponse:
      type: object
      properties:
        attendees:
          type: array
          items:
            $ref: "#/components/schemas/EventAttendeeSchema"

    EventRemindersResponse:
      type: object
      properties:
        reminders:
          type: array
          items:
            $ref: "#/components/schemas/EventReminderSchema"

    EventCategoriesResponse:
      type: object
      properties:
        categories:
          type: array
          items:
            $ref: "#/components/schemas/EventCategorySchema"

    ResourceResponse:
      type: object
      properties:
        resource:
          $ref: "#/components/schemas/ResourceSchema"

    ResourceListResponse:
      type: object
      properties:
        resources:
          type: array
          items:
            $ref: "#/components/schemas/ResourceSchema"

    ResourceBookingsResponse:
      type: object
      properties:
        bookings:
          type: array
          items:
            $ref: "#/components/schemas/ResourceBookingSchema"

    HolidayListResponse:
      type: object
      properties:
        holidays:
          type: array
          items:
            $ref: "#/components/schemas/PublicHolidaySchema"

    CalendarPreferencesResponse:
      type: object
      properties:
        preferences:
          $ref: "#/components/schemas/CalendarPreferencesSchema"

    TaskDeadlinesResponse:
      type: object
      properties:
        deadlines:
          type: array
          items:
            $ref: "#/components/schemas/TaskDeadlineStubSchema"

    JoinTokenResponse:
      type: object
      properties:
        token:
          type: string
          description: LiveKit JWT join token
        url:
          type: string
          description: LiveKit WebSocket URL

    # =========================================================================
    # Booking Page Schemas
    # =========================================================================

    BookingServiceInput:
      type: object
      required: [id, name, duration_min, price]
      properties:
        id: {type: string}
        name: {type: string}
        duration_min: {type: integer, minimum: 1}
        price: {type: string}
        description: {type: string, nullable: true}

    BookingPageInput:
      type: object
      required: [calendar_id, slug, company_name, availability_rules]
      properties:
        calendar_id: {type: string, format: uuid}
        slug: {type: string, pattern: "^[a-z0-9][a-z0-9\\-]{1,61}[a-z0-9]$"}
        company_name: {type: string}
        logo_url: {type: string, nullable: true}
        services: {type: array, items: {$ref: "#/components/schemas/BookingServiceInput"}}
        availability_rules: {type: string, description: "JSON blob (AvailabilityRules)"}

    BookingPageUpdateInput:
      type: object
      properties:
        company_name: {type: string}
        logo_url: {type: string, nullable: true}
        services: {type: array, items: {$ref: "#/components/schemas/BookingServiceInput"}}
        availability_rules: {type: string}
        active: {type: boolean}

    PublicBookingInput:
      type: object
      required: [slug, service_id, date, time_slot, customer]
      properties:
        slug: {type: string}
        service_id: {type: string}
        date: {type: string, format: date}
        time_slot: {type: string, example: "09:00"}
        customer:
          type: object
          required: [name, email]
          properties:
            name: {type: string}
            email: {type: string, format: email}
            phone: {type: string, nullable: true}
            notes: {type: string, nullable: true}

    # =========================================================================
    # Video Call Schemas
    # =========================================================================

    CreateCallRequest:
      type: object
      required: [call_type, participant_ids]
      properties:
        call_type:
          type: string
          enum: [one_to_one, group]
        participant_ids:
          type: array
          items:
            type: string
            format: uuid
        channel_id:
          type: string
          format: uuid

    CallSessionSchema:
      type: object
      properties:
        id:
          type: string
          format: uuid
        call_type:
          type: string
          enum: [CALL_TYPE_ONE_TO_ONE, CALL_TYPE_GROUP]
        status:
          type: string
          enum: [CALL_STATUS_RINGING, CALL_STATUS_ACTIVE, CALL_STATUS_ENDED]
        room_name:
          type: string
        initiator_id:
          type: string
          format: uuid
        channel_id:
          type: string
          format: uuid
        started_at:
          type: string
          format: date-time
        ended_at:
          type: string
          format: date-time
        participants:
          type: array
          items:
            $ref: "#/components/schemas/CallParticipantSchema"

    CallParticipantSchema:
      type: object
      properties:
        user_id:
          type: string
          format: uuid
        display_name:
          type: string
        joined_at:
          type: string
          format: date-time
        left_at:
          type: string
          format: date-time

    CallSessionResponse:
      type: object
      properties:
        call:
          $ref: "#/components/schemas/CallSessionSchema"

    JoinCallResponse:
      type: object
      properties:
        call:
          $ref: "#/components/schemas/CallSessionSchema"
        token:
          type: string
          description: LiveKit JWT join token
        livekit_url:
          type: string
          description: LiveKit WebSocket URL

    ListActiveCallsResponse:
      type: object
      properties:
        calls:
          type: array
          items:
            $ref: "#/components/schemas/CallSessionSchema"

    # =========================================================================
    # Recording Schemas
    # =========================================================================

    StartRecordingRequest:
      type: object
      properties:
        call_id:
          type: string
          format: uuid
        meeting_id:
          type: string
          format: uuid

    SetRecordingConsentRequest:
      type: object
      required: [consented]
      properties:
        consented:
          type: boolean

    RecordingSchema:
      type: object
      properties:
        id:
          type: string
          format: uuid
        call_id:
          type: string
          format: uuid
        meeting_id:
          type: string
          format: uuid
        status:
          type: string
          enum: [RECORDING_STATUS_RECORDING, RECORDING_STATUS_COMPLETED, RECORDING_STATUS_FAILED]
        file_path:
          type: string
        file_size_bytes:
          type: integer
          format: int64
        duration_seconds:
          type: integer
        started_by:
          type: string
          format: uuid
        started_at:
          type: string
          format: date-time
        stopped_at:
          type: string
          format: date-time

    RecordingResponse:
      type: object
      properties:
        recording:
          $ref: "#/components/schemas/RecordingSchema"

    RecordingConsentSchema:
      type: object
      properties:
        user_id:
          type: string
          format: uuid
        recording_id:
          type: string
          format: uuid
        status:
          type: string
          enum: [CONSENT_PENDING, CONSENT_GRANTED, CONSENT_DENIED]
        responded_at:
          type: string
          format: date-time

    RecordingConsentResponse:
      type: object
      properties:
        consents:
          type: array
          items:
            $ref: "#/components/schemas/RecordingConsentSchema"

    ListRecordingsResponse:
      type: object
      properties:
        recordings:
          type: array
          items:
            $ref: "#/components/schemas/RecordingSchema"
        total:
          type: integer

    # =========================================================================
    # Presence Schemas
    # =========================================================================

    SetPresenceStatusRequest:
      type: object
      required: [status]
      properties:
        status:
          type: string
          enum: [online, away, dnd, in_call, offline]

    BulkPresenceRequest:
      type: object
      required: [user_ids]
      properties:
        user_ids:
          type: array
          items:
            type: string
            format: uuid

    PresenceSchema:
      type: object
      properties:
        user_id:
          type: string
          format: uuid
        status:
          type: string
          enum: [PRESENCE_ONLINE, PRESENCE_AWAY, PRESENCE_DND, PRESENCE_IN_CALL, PRESENCE_OFFLINE]
        last_seen:
          type: string
          format: date-time

    PresenceResponse:
      type: object
      properties:
        presence:
          $ref: "#/components/schemas/PresenceSchema"

    BulkPresenceResponse:
      type: object
      properties:
        presences:
          type: array
          items:
            $ref: "#/components/schemas/PresenceSchema"

    PresenceConfigSchema:
      type: object
      properties:
        away_timeout_seconds:
          type: integer

    PresenceConfigResponse:
      type: object
      properties:
        config:
          $ref: "#/components/schemas/PresenceConfigSchema"

    UpdatePresenceConfigRequest:
      type: object
      required: [away_timeout_seconds]
      properties:
        away_timeout_seconds:
          type: integer

    # =========================================================================
    # Reaction Schemas
    # =========================================================================

    ToggleReactionRequest:
      type: object
      required: [message_id, emoji]
      properties:
        message_id:
          type: string
          format: uuid
        emoji:
          type: string
          description: "Emoji character (e.g., thumbsup, heart)"

    ReactionSchema:
      type: object
      properties:
        id:
          type: string
          format: uuid
        message_id:
          type: string
          format: uuid
        user_id:
          type: string
          format: uuid
        emoji:
          type: string
        created_at:
          type: string
          format: date-time

    ReactionResponse:
      type: object
      properties:
        reaction:
          $ref: "#/components/schemas/ReactionSchema"
        added:
          type: boolean
          description: true if reaction was added, false if removed

    ListReactionsResponse:
      type: object
      properties:
        reactions:
          type: array
          items:
            $ref: "#/components/schemas/ReactionSchema"

    ReactionSummaryRequest:
      type: object
      required: [message_ids]
      properties:
        message_ids:
          type: array
          items:
            type: string
            format: uuid

    ReactionSummaryResponse:
      type: object
      properties:
        summaries:
          type: array
          items:
            type: object
            properties:
              message_id:
                type: string
                format: uuid
              counts:
                type: object
                additionalProperties:
                  type: integer
                description: "Map of emoji to count"

    ToggleBookmarkResponse:
      type: object
      properties:
        bookmarked:
          type: boolean
          description: true if the message is now bookmarked, false if the bookmark was removed

    ListBookmarksResponse:
      type: object
      properties:
        messages:
          type: array
          items:
            $ref: "#/components/schemas/MessageInfo"

    # =========================================================================
    # Meeting Schemas
    # =========================================================================

    CreateMeetingRequest:
      type: object
      required: [title, scheduled_start, scheduled_end]
      properties:
        title:
          type: string
        description:
          type: string
        agenda:
          type: string
        scheduled_start:
          type: string
          format: date-time
        scheduled_end:
          type: string
          format: date-time
        calendar_event_id:
          type: string
          format: uuid
        recurring_meeting_id:
          type: string
          format: uuid
        attendee_user_ids:
          type: array
          items:
            type: string
            format: uuid

    UpdateMeetingRequest:
      type: object
      properties:
        title:
          type: string
        description:
          type: string
        agenda:
          type: string
        scheduled_start:
          type: string
          format: date-time
        scheduled_end:
          type: string
          format: date-time
        attendee_user_ids:
          type: array
          items:
            type: string
            format: uuid

    MeetingSchema:
      type: object
      properties:
        id:
          type: string
          format: uuid
        title:
          type: string
        description:
          type: string
        agenda:
          type: string
        status:
          type: string
          enum: [MEETING_STATUS_SCHEDULED, MEETING_STATUS_IN_PROGRESS, MEETING_STATUS_COMPLETED, MEETING_STATUS_CANCELLED]
        organizer_id:
          type: string
          format: uuid
        scheduled_start:
          type: string
          format: date-time
        scheduled_end:
          type: string
          format: date-time
        actual_start:
          type: string
          format: date-time
        actual_end:
          type: string
          format: date-time
        room_name:
          type: string
        calendar_event_id:
          type: string
          format: uuid
        recurring_meeting_id:
          type: string
          format: uuid
        attendees:
          type: array
          items:
            type: object
            properties:
              user_id:
                type: string
                format: uuid
              display_name:
                type: string
              rsvp_status:
                type: string
        created_at:
          type: string
          format: date-time

    MeetingResponse:
      type: object
      properties:
        meeting:
          $ref: "#/components/schemas/MeetingSchema"

    ListMeetingsResponse:
      type: object
      properties:
        meetings:
          type: array
          items:
            $ref: "#/components/schemas/MeetingSchema"
        total:
          type: integer

    StartMeetingResponse:
      type: object
      properties:
        meeting:
          $ref: "#/components/schemas/MeetingSchema"
        token:
          type: string
          description: LiveKit JWT join token
        livekit_url:
          type: string
          description: LiveKit WebSocket URL

    JoinMeetingResponse:
      type: object
      properties:
        meeting:
          $ref: "#/components/schemas/MeetingSchema"
        token:
          type: string
          description: LiveKit JWT join token
        ws_url:
          type: string
          description: LiveKit WebSocket signaling URL
        room_name:
          type: string
        ice_servers:
          type: array
          description: >-
            Per-session TURN credentials for the client RTCConfiguration;
            empty/absent when TURN is not configured.
          items:
            type: object
            properties:
              urls:
                type: array
                items:
                  type: string
              username:
                type: string
              credential:
                type: string

    EndMeetingResponse:
      type: object
      properties:
        meeting:
          $ref: "#/components/schemas/MeetingSchema"
        summary:
          $ref: "#/components/schemas/MeetingSummarySchema"

    MeetingSummarySchema:
      type: object
      properties:
        duration_minutes:
          type: integer
        participant_count:
          type: integer
        notes_count:
          type: integer
        action_item_count:
          type: integer
        recording_count:
          type: integer

    # =========================================================================
    # Meeting Notes Schemas
    # =========================================================================

    SaveMeetingNotesRequest:
      type: object
      required: [content]
      properties:
        content:
          type: string
        is_private:
          type: boolean

    MeetingNotesSchema:
      type: object
      properties:
        id:
          type: string
          format: uuid
        meeting_id:
          type: string
          format: uuid
        author_id:
          type: string
          format: uuid
        content:
          type: string
        is_private:
          type: boolean
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time

    MeetingNotesResponse:
      type: object
      properties:
        notes:
          $ref: "#/components/schemas/MeetingNotesSchema"

    MeetingNotesListResponse:
      type: object
      properties:
        notes:
          type: array
          items:
            $ref: "#/components/schemas/MeetingNotesSchema"

    # =========================================================================
    # Action Item Schemas
    # =========================================================================

    CreateActionItemRequest:
      type: object
      required: [description]
      properties:
        description:
          type: string
        assignee_id:
          type: string
          format: uuid
        sort_order:
          type: integer

    UpdateActionItemRequest:
      type: object
      properties:
        description:
          type: string
        assignee_id:
          type: string
          format: uuid
        is_completed:
          type: boolean
        sort_order:
          type: integer

    ActionItemSchema:
      type: object
      properties:
        id:
          type: string
          format: uuid
        meeting_id:
          type: string
          format: uuid
        description:
          type: string
        assignee_id:
          type: string
          format: uuid
        is_completed:
          type: boolean
        sort_order:
          type: integer
        linked_task_id:
          type: string
          format: uuid
        created_at:
          type: string
          format: date-time

    ActionItemResponse:
      type: object
      properties:
        action_item:
          $ref: "#/components/schemas/ActionItemSchema"

    ListActionItemsResponse:
      type: object
      properties:
        action_items:
          type: array
          items:
            $ref: "#/components/schemas/ActionItemSchema"

    ConvertActionItemsRequest:
      type: object
      required: [action_item_ids, project_id]
      properties:
        action_item_ids:
          type: array
          items:
            type: string
            format: uuid
        project_id:
          type: string
          format: uuid

    ConvertActionItemsResponse:
      type: object
      properties:
        converted_count:
          type: integer
        task_ids:
          type: array
          items:
            type: string
            format: uuid

    # =========================================================================
    # WebSocket Event Types (documentation reference)
    # =========================================================================
    # The following WebSocket message types are supported via /api/v1/ws:
    #
    # Presence Events:
    #   presence.update     (Server -> Client) - User presence status changed
    #   presence.heartbeat  (Client -> Server) - Client heartbeat to maintain online status
    #   presence.subscribe  (Client -> Server) - Subscribe to presence updates for user IDs
    #   presence.set_status (Client -> Server) - Manually set presence status (online/away/dnd/offline)
    #
    # Call Events:
    #   call.incoming  (Server -> Client) - Incoming call notification
    #   call.accepted  (Client -> Server) - User accepted incoming call
    #   call.declined  (Client -> Server) - User declined incoming call
    #   call.ended     (Server -> Client) - Call has ended
    #
    # Reaction Events:
    #   reaction.toggled (Server -> Client) - Reaction added/removed on a message

    # =========================================================================
    # Finance Schemas
    # =========================================================================

    LineItem:
      type: object
      properties:
        id:
          type: string
        position:
          type: integer
        description:
          type: string
        quantity:
          type: string
          description: Decimal as string
        unit_price:
          type: string
          description: Decimal as string
        tax_rate:
          type: string
          description: Decimal as string (e.g. "19.00", "7.00")
        line_total:
          type: string
          description: Decimal as string (quantity * unit_price)

    TaxBreakdown:
      type: object
      properties:
        subtotal:
          type: string
        tax_by_rate:
          type: object
          additionalProperties:
            type: string
        total_tax:
          type: string
        gross_total:
          type: string

    CustomerSnapshot:
      type: object
      properties:
        name:
          type: string
        address:
          type: string
        email:
          type: string
        ust_id_nr:
          type: string

    CompanySettings:
      type: object
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        name:
          type: string
        street:
          type: string
        plz:
          type: string
        city:
          type: string
        country:
          type: string
        steuernummer:
          type: string
        ust_id_nr:
          type: string
        handelsregister:
          type: string
        bank_name:
          type: string
        iban:
          type: string
        bic:
          type: string
        logo_url:
          type: string
        accent_color:
          type: string
        is_kleinunternehmer:
          type: boolean
        default_payment_terms_days:
          type: integer
        default_quote_validity_days:
          type: integer
        basiszinssatz:
          type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time

    CompanySettingsInput:
      type: object
      properties:
        name:
          type: string
        street:
          type: string
        plz:
          type: string
        city:
          type: string
        country:
          type: string
        steuernummer:
          type: string
        ust_id_nr:
          type: string
        handelsregister:
          type: string
        bank_name:
          type: string
        iban:
          type: string
        bic:
          type: string
        logo_url:
          type: string
        accent_color:
          type: string
        is_kleinunternehmer:
          type: boolean
        default_payment_terms_days:
          type: integer
        default_quote_validity_days:
          type: integer
        basiszinssatz:
          type: string

    Quote:
      type: object
      properties:
        id:
          type: string
          format: uuid
        quote_number:
          type: string
        status:
          type: string
          enum: [draft, sent, accepted, rejected, expired]
        customer:
          $ref: "#/components/schemas/CustomerSnapshot"
        line_items:
          type: array
          items:
            $ref: "#/components/schemas/LineItem"
        tax_mode:
          type: string
          enum: [standard, reverse_charge, kleinunternehmer]
        tax_breakdown:
          $ref: "#/components/schemas/TaxBreakdown"
        valid_until:
          type: string
          format: date
        notes:
          type: string
        deal_id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        created_by:
          type: string
          format: uuid
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        currency:
          type: string
          description: ISO 4217 code of the document currency. EUR for rows written before the column existed.
          example: EUR

    CreateQuoteRequest:
      type: object
      required: [customer, line_items, tax_mode]
      properties:
        customer:
          $ref: "#/components/schemas/CustomerSnapshot"
        line_items:
          type: array
          items:
            $ref: "#/components/schemas/LineItem"
        tax_mode:
          type: string
          enum: [standard, reverse_charge, kleinunternehmer]
        valid_until:
          type: string
          format: date
        notes:
          type: string
        deal_id:
          type: string
          format: uuid

    UpdateQuoteRequest:
      type: object
      properties:
        customer:
          $ref: "#/components/schemas/CustomerSnapshot"
        line_items:
          type: array
          items:
            $ref: "#/components/schemas/LineItem"
        tax_mode:
          type: string
          enum: [standard, reverse_charge, kleinunternehmer]
        valid_until:
          type: string
          format: date
        notes:
          type: string

    Invoice:
      type: object
      properties:
        id:
          type: string
          format: uuid
        invoice_number:
          type: string
        status:
          type: string
          enum: [draft, sent, paid, overdue, cancelled]
        customer:
          $ref: "#/components/schemas/CustomerSnapshot"
        line_items:
          type: array
          items:
            $ref: "#/components/schemas/LineItem"
        tax_mode:
          type: string
        tax_breakdown:
          $ref: "#/components/schemas/TaxBreakdown"
        invoice_date:
          type: string
          format: date
        delivery_date:
          type: string
          format: date
        due_date:
          type: string
          format: date
        payment_terms:
          type: string
        notes:
          type: string
        source_quote_id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        created_by:
          type: string
          format: uuid
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        contact_id:
          type: string
          format: uuid
          description: CRM contact linked to this invoice (Contact-360, Migration 000141)
        currency:
          type: string
          description: ISO 4217 code of the document currency. EUR for rows written before the column existed.
          example: EUR
        source:
          type: string
          enum: [cosmi, bexio]
          description: Provenance (Migration 000243). Bexio rows are read-only mirrors.
        external_id:
          type: string
          description: Identity in the source system. Absent for cosmi invoices.
        external_number:
          type: string
          description: Document number in the source system. Absent for cosmi invoices.
        recurring_id:
          type: string
          format: uuid
          description: Recurring schedule that emitted this invoice (Migration 000246). Absent for manually created invoices.

    RecurringInvoice:
      type: object
      description: A schedule that emits draft invoices at a fixed interval (Migration 000246).
      properties:
        id:
          type: string
          format: uuid
        title:
          type: string
        customer:
          $ref: "#/components/schemas/CustomerSnapshot"
        line_items:
          type: array
          items:
            $ref: "#/components/schemas/LineItem"
        tax_mode:
          type: string
          enum: [standard, reverse_charge, kleinunternehmer]
        tax_breakdown:
          $ref: "#/components/schemas/TaxBreakdown"
        currency:
          type: string
          example: EUR
        interval:
          type: string
          enum: [weekly, monthly, quarterly, yearly]
        status:
          type: string
          enum: [active, paused, ended]
        start_date:
          type: string
          format: date
        end_date:
          type: string
          format: date
          description: Empty when the schedule is open-ended.
        next_run:
          type: string
          format: date
        payment_terms_days:
          type: integer
        generated_count:
          type: integer
        last_generated_at:
          type: string
          format: date
          description: Empty before the first run.
        notes:
          type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time

    CreateRecurringInvoiceRequest:
      type: object
      required: [customer, line_items, tax_mode, interval, start_date]
      properties:
        title:
          type: string
        customer:
          $ref: "#/components/schemas/CustomerSnapshot"
        line_items:
          type: array
          minItems: 1
          items:
            $ref: "#/components/schemas/LineItem"
        tax_mode:
          type: string
          enum: [standard, reverse_charge, kleinunternehmer]
        currency:
          type: string
          example: EUR
        interval:
          type: string
          enum: [weekly, monthly, quarterly, yearly]
        start_date:
          type: string
          format: date
        end_date:
          type: string
          format: date
        payment_terms_days:
          type: integer
          minimum: 0
          maximum: 365
        notes:
          type: string

    UpdateRecurringInvoiceRequest:
      type: object
      description: Partial update; omitted fields stay unchanged, an empty end_date clears it.
      properties:
        title:
          type: string
        customer:
          $ref: "#/components/schemas/CustomerSnapshot"
        line_items:
          type: array
          items:
            $ref: "#/components/schemas/LineItem"
        tax_mode:
          type: string
          enum: [standard, reverse_charge, kleinunternehmer]
        currency:
          type: string
        interval:
          type: string
          enum: [weekly, monthly, quarterly, yearly]
        start_date:
          type: string
          format: date
        end_date:
          type: string
          format: date
        payment_terms_days:
          type: integer
          minimum: 0
          maximum: 365
        notes:
          type: string

    CreateInvoiceRequest:
      type: object
      required: [customer, line_items, tax_mode, invoice_date, due_date]
      properties:
        customer:
          $ref: "#/components/schemas/CustomerSnapshot"
        line_items:
          type: array
          items:
            $ref: "#/components/schemas/LineItem"
        tax_mode:
          type: string
          enum: [standard, reverse_charge, kleinunternehmer]
        invoice_date:
          type: string
          format: date
        delivery_date:
          type: string
          format: date
        due_date:
          type: string
          format: date
        payment_terms:
          type: string
        notes:
          type: string
        source_quote_id:
          type: string
          format: uuid
        contact_id:
          type: string
          format: uuid
          description: CRM contact to link to this invoice (Contact-360, Migration 000141)

    UpdateInvoiceRequest:
      type: object
      properties:
        customer:
          $ref: "#/components/schemas/CustomerSnapshot"
        line_items:
          type: array
          items:
            $ref: "#/components/schemas/LineItem"
        tax_mode:
          type: string
        invoice_date:
          type: string
          format: date
        delivery_date:
          type: string
          format: date
        due_date:
          type: string
          format: date
        payment_terms:
          type: string
        notes:
          type: string

    CreditNote:
      type: object
      properties:
        id:
          type: string
          format: uuid
        credit_note_number:
          type: string
        status:
          type: string
          enum: [draft, sent]
        original_invoice_id:
          type: string
          format: uuid
        customer:
          $ref: "#/components/schemas/CustomerSnapshot"
        line_items:
          type: array
          items:
            $ref: "#/components/schemas/LineItem"
        tax_mode:
          type: string
        tax_breakdown:
          $ref: "#/components/schemas/TaxBreakdown"
        reason:
          type: string
        tenant_id:
          type: string
          format: uuid
        created_by:
          type: string
          format: uuid
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        currency:
          type: string
          description: ISO 4217 code of the document currency. EUR for rows written before the column existed.
          example: EUR

    CreateCreditNoteRequest:
      type: object
      required: [original_invoice_id, customer, line_items, tax_mode]
      properties:
        original_invoice_id:
          type: string
          format: uuid
        customer:
          $ref: "#/components/schemas/CustomerSnapshot"
        line_items:
          type: array
          items:
            $ref: "#/components/schemas/LineItem"
        tax_mode:
          type: string
          enum: [standard, reverse_charge, kleinunternehmer]
        reason:
          type: string

    Payment:
      type: object
      properties:
        id:
          type: string
          format: uuid
        invoice_id:
          type: string
          format: uuid
        amount:
          type: string
        payment_date:
          type: string
          format: date
        method:
          type: string
          enum: [bank_transfer, cash, credit_card, other]
        reference:
          type: string
        notes:
          type: string
        tenant_id:
          type: string
          format: uuid
        created_by:
          type: string
          format: uuid
        created_at:
          type: string
          format: date-time

    RecordPaymentRequest:
      type: object
      required: [amount, payment_date, method]
      properties:
        amount:
          type: string
          description: Decimal as string
        payment_date:
          type: string
          format: date
        method:
          type: string
          enum: [bank_transfer, cash, credit_card, other]
        reference:
          type: string
        notes:
          type: string

    BankStatement:
      type: object
      description: One imported account statement file (Migration 000247).
      properties:
        id:
          type: string
          format: uuid
        format:
          type: string
          enum: [camt053, mt940]
        filename:
          type: string
        content_hash:
          type: string
          description: >
            SHA-256 of the uploaded bytes. Importing the same file twice
            resolves to this same statement instead of creating a second one.
        account_iban:
          type: string
        statement_ref:
          type: string
        currency:
          type: string
          description: ISO 4217
        opening_balance:
          type: string
          description: Decimal as string
        closing_balance:
          type: string
          description: Decimal as string
        statement_date:
          type: string
          format: date
        transaction_count:
          type: integer
        imported_by:
          type: string
          format: uuid
        created_at:
          type: string
          format: date-time

    BankAccount:
      type: object
      description: >
        One account a tenant holds. balance and lastSync are read from the
        newest imported statement of this IBAN and are never stored on the
        account itself, so a figure shown here is always one this system
        actually imported.
      properties:
        id:
          type: string
          format: uuid
        bankName:
          type: string
        iban:
          type: string
          description: Grouped in blocks of four for display; stored canonically.
          example: DE89 3704 0044 0532 0130 00
        bic:
          type: string
        balance:
          type: number
          format: double
          description: Closing balance of the newest imported statement, 0 when none.
        currency:
          type: string
          example: EUR
        connected:
          type: boolean
          description: Master-data flag, not a live bank session.
        lastSync:
          type: string
          format: date
          nullable: true
          description: Date of the newest imported statement, null when none.
    BankAccountInput:
      type: object
      required: [bankName, iban]
      properties:
        bankName:
          type: string
          maxLength: 160
        iban:
          type: string
          description: Grouped or compact; validated with the ISO 7064 mod-97 check.
        bic:
          type: string
        currency:
          type: string
          description: ISO 4217; defaults to EUR when omitted.
          example: EUR
    BankAccountUpdateInput:
      type: object
      description: Partial edit — an omitted field is left untouched.
      properties:
        bankName:
          type: string
          maxLength: 160
        iban:
          type: string
        bic:
          type: string
        currency:
          type: string
        connected:
          type: boolean
    Expense:
      type: object
      description: >
        An expense (Ausgabe) as the desktop client reads it. Two fields
        deliberately break the snake_case convention used elsewhere in this
        spec, because this shape is the existing frontend contract: receiptName
        is camelCase, and amount is a JSON number rather than a decimal string.
      properties:
        id:
          type: string
          format: uuid
        description:
          type: string
        amount:
          type: number
          format: double
          description: Gross amount, always positive
          example: 234.5
        date:
          type: string
          format: date
          description: The day the money was spent
          example: "2026-05-29"
        category:
          type: string
          example: Büromaterial
        supplier:
          type: string
          example: Hetzner Online GmbH
        project:
          type: string
          description: Absent when the expense is not booked on a project
        account:
          type: string
          description: SKR03/04 Sachkonto, absent until Kontierung
          example: "4930"
        receipt:
          type: boolean
          description: Derived from receiptName, never stored separately
        receiptName:
          type: string
          description: Filename of the attached document, absent when none
          example: hetzner-2026-05.pdf
        status:
          type: string
          enum: [pending, approved, rejected]
      required: [id, description, amount, date, category, supplier, status]

    ExpenseInput:
      type: object
      description: A new expense. The submitter and the pending status come from the server.
      required: [description, amount, date]
      properties:
        description:
          type: string
          maxLength: 500
        amount:
          type: number
          format: double
          description: Must be greater than zero
        date:
          type: string
          format: date
          example: "2026-05-29"
        category:
          type: string
          maxLength: 120
        supplier:
          type: string
          maxLength: 255
        project:
          type: string
          maxLength: 255
        account:
          type: string
          maxLength: 20
        receiptName:
          type: string
          maxLength: 255

    ExpenseUpdateInput:
      type: object
      description: >
        A partial edit — every field is optional and an omitted one is left
        untouched, so the Kontierung dialog can send account alone.
      properties:
        description:
          type: string
          maxLength: 500
        amount:
          type: number
          format: double
        date:
          type: string
          format: date
        category:
          type: string
          maxLength: 120
        supplier:
          type: string
          maxLength: 255
        project:
          type: string
          maxLength: 255
        account:
          type: string
          maxLength: 20
        receiptName:
          type: string
          maxLength: 255

    Transaction:
      type: object
      description: >
        One entry in the consolidated payment ledger (Transaktionen): either a
        recorded customer payment (income) or an approved expense (expense).
        id is opaque and prefixed ("pay-"/"exp-") to name the underlying
        record for delete; do not parse it as a UUID.
      properties:
        id:
          type: string
          example: pay-3fa85f64-5717-4562-b3fc-2c963f66afa6
        type:
          type: string
          enum: [income, expense]
        description:
          type: string
        amount:
          type: number
          format: double
          description: Always positive; sign is implied by type
          example: 4280.0
        date:
          type: string
          format: date
          example: "2026-05-29"
        category:
          type: string
          example: Umsatzerlöse
        status:
          type: string
          enum: [completed, pending]
          description: Always "completed" today — only settled money movements are listed
        reference:
          type: string
          description: Invoice number; present for income entries only
          example: RE-2026-042
        invoiceId:
          type: string
          format: uuid
          description: Present for income entries only
      required: [id, type, description, amount, date, category, status]

    BankTransaction:
      type: object
      description: >
        One entry of a statement plus its reconciliation state. Everything up to
        remittance_info is what the bank reported and is never rewritten after
        import; the match_ fields carry what this system made of it.
      properties:
        id:
          type: string
          format: uuid
        statement_id:
          type: string
          format: uuid
        entry_ref:
          type: string
        end_to_end_id:
          type: string
        value_date:
          type: string
          format: date
        booking_date:
          type: string
          format: date
        amount:
          type: string
          description: >
            Decimal as string, signed: a credit (money in) is positive, a debit
            negative.
        currency:
          type: string
        counterparty_name:
          type: string
        counterparty_iban:
          type: string
        remittance_info:
          type: string
        match_status:
          type: string
          enum: [unmatched, suggested, matched, ignored]
        match_reason:
          type: string
          description: >
            Why the matcher suggested what it did.
          enum: ["", invoice_number+amount, invoice_number, amount, manual, ignored]
        matched_invoice_id:
          type: string
          format: uuid
        payment_id:
          type: string
          format: uuid
        reconciled_at:
          type: string
          format: date-time
        reconciled_by:
          type: string
          format: uuid
        created_at:
          type: string
          format: date-time
        matched_invoice_number:
          type: string
          description: >
            Number of the matched invoice, joined on read. Absent while nothing
            is matched.

    BankTransactionQueueEntry:
      type: object
      description: >
        One entry of the reconciliation queue, in the shape the desktop client
        reads. Narrower than BankTransaction on purpose: the queue shows what an
        operator decides on, not the full bank record, which the statement
        endpoints carry.
      properties:
        id:
          type: string
          format: uuid
        date:
          type: string
          format: date
          description: Value date of the entry.
        description:
          type: string
          description: >
            Remittance information, falling back to the bank's entry reference
            where the payer left it empty.
        amount:
          type: number
          description: "Signed: a credit (money in) is positive, a debit negative."
        type:
          type: string
          enum: [credit, debit]
          description: Restates the sign of amount.
        counterpart:
          type: string
        matchStatus:
          type: string
          enum: [unmatched, suggested, matched, ignored]
        matchedInvoice:
          type: string
          description: >
            Number of the matched invoice — not its id. Absent while nothing is
            matched.

    ChainNode:
      type: object
      description: >
        One step in a document's lifecycle. number and date read "—" for a
        step that has not happened yet — the pending-payment placeholder on an
        invoice that is not fully settled.
      properties:
        type:
          type: string
          enum: [quote, invoice, payment, dunning, credit-note]
        number:
          type: string
        date:
          type: string
          description: "YYYY-MM-DD, or — if the step has not happened yet."
        amount:
          type: string
          description: >
            Pre-formatted for display, e.g. "CHF 12.450,00" — the client
            renders it verbatim.
        status:
          type: string
          enum: [completed, active, pending, cancelled, overdue]
          description: >
            Presentational, independent of the underlying document's own
            status field.

    DocumentChain:
      type: object
      description: One customer document traced from quote to its current step.
      properties:
        id:
          type: string
          format: uuid
        customer:
          type: string
        totalValue:
          type: string
          description: Pre-formatted for display, e.g. "CHF 12.450,00".
        isComplete:
          type: boolean
        nodes:
          type: array
          items:
            $ref: "#/components/schemas/ChainNode"

    FinanceTimeEntry:
      type: object
      description: >
        A completed time entry available for invoicing (Stunden -> Rechnung).
        billed is always false today -- invoice creation from this list does
        not yet persist which entries it covered.
      properties:
        id:
          type: string
          format: uuid
        date:
          type: string
          description: "YYYY-MM-DD"
        project:
          type: string
        task:
          type: string
        employee:
          type: string
        hours:
          type: number
        description:
          type: string
        billed:
          type: boolean

    ProjectTimeEntry:
      type: object
      description: >
        A completed time entry scoped to one project's tasks, for that
        project's "Stunden abrechnen" roll-up.
      properties:
        id:
          type: string
          format: uuid
        date:
          type: string
          description: "YYYY-MM-DD"
        task:
          type: string
        person:
          type: string
        hours:
          type: number
        description:
          type: string

    UtilizationPoint:
      type: object
      description: One labeled period ("KW 6", "Aug 2026") in a member's tracked-hours trend.
      properties:
        label:
          type: string
        hours:
          type: number

    MemberUtilization:
      type: object
      description: >
        One project member's tracked-hours roll-up for the Auslastung view.
        Deliberately carries no cost or salary figure.
      properties:
        member:
          type: object
          properties:
            id:
              type: string
              format: uuid
            name:
              type: string
            role:
              type: string
            avatarInitial:
              type: string
            weeklyTarget:
              type: integer
              description: Fixed full-time-week default (40h), not the member's real contracted hours.
        weeklyData:
          type: array
          items:
            $ref: "#/components/schemas/UtilizationPoint"
        monthlyData:
          type: array
          items:
            $ref: "#/components/schemas/UtilizationPoint"

    BankStatementImportResult:
      type: object
      properties:
        statement:
          $ref: "#/components/schemas/BankStatement"
        transactions:
          type: array
          items:
            $ref: "#/components/schemas/BankTransaction"
        already_imported:
          type: boolean
          description: >
            True when the identical file had been imported before. The response
            then carries the original statement, not a second copy.

    OpenItem:
      type: object
      description: One unpaid receivable with its aging and dunning state.
      properties:
        invoice_id:
          type: string
          format: uuid
        invoice_number:
          type: string
        status:
          type: string
          enum: [sent, overdue]
        customer_name:
          type: string
        customer_email:
          type: string
        currency:
          type: string
          description: ISO 4217 code
        gross_total:
          type: string
          description: Decimal as string
        paid_amount:
          type: string
          description: Decimal as string — everything recorded in finance_payments
        open_amount:
          type: string
          description: Decimal as string — gross_total minus paid_amount
        invoice_date:
          type: string
          format: date
        due_date:
          type: string
          format: date
        days_overdue:
          type: integer
          description: Negative when the item is not due yet
        aging_bucket:
          type: string
          enum: [current, d30, d60, d60plus]
        dunning_level:
          type: integer
          description: Highest dunning level reached, 0 when never dunned
        dunning_status:
          type: string
          description: Empty when never dunned
        last_dunned_at:
          type: string
          format: date-time
        contact_id:
          type: string
          format: uuid

    OpenItemBucketTotal:
      type: object
      description: One aging bucket in one currency, over all open items.
      properties:
        currency:
          type: string
        bucket:
          type: string
          enum: [current, d30, d60, d60plus]
        count:
          type: integer
        amount:
          type: string
          description: Decimal as string

    OpenItemCurrencyTotal:
      type: object
      description: Receivables totals for one currency.
      properties:
        currency:
          type: string
        open_amount:
          type: string
          description: Decimal as string
        open_count:
          type: integer
        overdue_amount:
          type: string
          description: Decimal as string
        overdue_count:
          type: integer
        avg_days_overdue:
          type: integer

    OpenItemsSummary:
      type: object
      description: Tenant-wide receivables aggregates, never page-scoped.
      properties:
        totals:
          type: array
          items:
            $ref: "#/components/schemas/OpenItemCurrencyTotal"
        buckets:
          type: array
          items:
            $ref: "#/components/schemas/OpenItemBucketTotal"

    DunningRecord:
      type: object
      properties:
        id:
          type: string
          format: uuid
        invoice_id:
          type: string
          format: uuid
        level:
          type: integer
          description: "1, 2, or 3"
        status:
          type: string
          enum: [draft, sent, paid]
        fee:
          type: string
        interest:
          type: string
        sent_at:
          type: string
          format: date-time
        tenant_id:
          type: string
          format: uuid
        created_by:
          type: string
          format: uuid
        created_at:
          type: string
          format: date-time

    DunningConfig:
      type: object
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        level1_days_after_due:
          type: integer
        level2_days_after_level1:
          type: integer
        level3_days_after_level2:
          type: integer
        level1_fee:
          type: string
        level2_fee:
          type: string
        level3_fee:
          type: string
        updated_at:
          type: string
          format: date-time

    DunningConfigInput:
      type: object
      properties:
        level1_days_after_due:
          type: integer
        level2_days_after_level1:
          type: integer
        level3_days_after_level2:
          type: integer
        level1_fee:
          type: string
        level2_fee:
          type: string
        level3_fee:
          type: string

    FinanceDashboard:
      type: object
      properties:
        total_invoiced:
          type: string
        total_paid:
          type: string
        total_outstanding:
          type: string
        overdue_amount:
          type: string
        quotes_pending:
          type: integer
        conversion_rate:
          type: string
        average_deal_size:
          type: string
        revenue_forecast:
          type: string
        status_breakdown:
          type: object
          additionalProperties:
            type: integer
        recent_invoices:
          type: array
          items:
            $ref: "#/components/schemas/Invoice"
        expiring_quotes:
          type: array
          items:
            $ref: "#/components/schemas/Quote"
        pending_dunnings:
          type: array
          items:
            $ref: "#/components/schemas/DunningRecord"

    # =========================================================================
    # HR Schemas
    # =========================================================================
    CreateLeaveRequestInput:
      type: object
      required: [leave_type_id, start_date, end_date]
      properties:
        leave_type_id: { type: string, format: uuid }
        start_date: { type: string, format: date }
        end_date: { type: string, format: date }
        is_half_day_start: { type: boolean }
        is_half_day_end: { type: boolean }
        half_day_period_start: { type: string, enum: [morning, afternoon] }
        half_day_period_end: { type: string, enum: [morning, afternoon] }
        reason: { type: string }

    LeaveRequest:
      type: object
      properties:
        id: { type: string, format: uuid }
        tenant_id: { type: string, format: uuid }
        employee_id: { type: string, format: uuid }
        leave_type_id: { type: string, format: uuid }
        start_date: { type: string, format: date }
        end_date: { type: string, format: date }
        is_half_day_start: { type: boolean }
        is_half_day_end: { type: boolean }
        half_day_period_start: { type: string, enum: [morning, afternoon] }
        half_day_period_end: { type: string, enum: [morning, afternoon] }
        total_days: { type: string }
        reason: { type: string }
        status: { type: string, enum: [pending, approved, rejected, cancelled] }
        approved_by: { type: string, format: uuid }
        approved_at: { type: string, format: date-time }
        approval_comment: { type: string }
        au_document_required: { type: boolean }
        au_document_file_id: { type: string, format: uuid }
        employee_name: { type: string }
        leave_type_name: { type: string }
        leave_type_color: { type: string }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }

    LeaveBalance:
      type: object
      properties:
        id: { type: string, format: uuid }
        tenant_id: { type: string, format: uuid }
        employee_id: { type: string, format: uuid }
        year: { type: integer }
        entitlement: { type: string }
        carried_over: { type: string }
        used: { type: string }
        remaining: { type: string }
        carryover_expires_at: { type: string, format: date }
        carryover_notified: { type: boolean }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }

    LeaveType:
      type: object
      properties:
        id: { type: string, format: uuid }
        tenant_id: { type: string, format: uuid }
        name: { type: string }
        key: { type: string }
        color: { type: string }
        deducts_from_balance: { type: boolean }
        requires_approval: { type: boolean }
        requires_au_document: { type: boolean }
        is_system: { type: boolean }
        sort_order: { type: integer }
        created_at: { type: string, format: date-time }

    WorkTimeEntry:
      type: object
      properties:
        id: { type: string, format: uuid }
        tenant_id: { type: string, format: uuid }
        employee_id: { type: string, format: uuid }
        clock_in: { type: string, format: date-time }
        clock_out: { type: string, format: date-time }
        break_minutes: { type: integer }
        auto_break_deducted: { type: integer }
        net_work_minutes: { type: integer }
        status: { type: string, enum: [active, completed, correction_pending, correction_approved, superseded] }
        is_correction: { type: boolean }
        original_entry_id: { type: string, format: uuid }
        correction_reason: { type: string }
        correction_approved_by: { type: string, format: uuid }
        correction_approved_at: { type: string, format: date-time }
        employee_name: { type: string }
        breaks:
          type: array
          items:
            $ref: "#/components/schemas/BreakEntry"
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }

    BreakEntry:
      type: object
      properties:
        id: { type: string, format: uuid }
        work_time_entry_id: { type: string, format: uuid }
        start_time: { type: string, format: date-time }
        end_time: { type: string, format: date-time }
        duration_minutes: { type: integer }

    WorkTimeStatus:
      type: object
      properties:
        is_clocked_in: { type: boolean }
        is_on_break: { type: boolean }
        current_shift_start: { type: string, format: date-time }
        current_break_start: { type: string, format: date-time }
        today_total_minutes: { type: integer }
        arbzg_severity: { type: string, enum: [none, info, warning, error] }

    DailySummary:
      type: object
      properties:
        date: { type: string, format: date }
        total_work_minutes: { type: integer }
        total_break_minutes: { type: integer }
        net_work_minutes: { type: integer }
        overtime_minutes: { type: integer }

    WeeklySummary:
      type: object
      properties:
        week_start: { type: string, format: date }
        week_end: { type: string, format: date }
        total_work_minutes: { type: integer }
        total_break_minutes: { type: integer }
        net_work_minutes: { type: integer }
        overtime_minutes: { type: integer }
        work_days: { type: integer }
        daily_summaries:
          type: array
          items:
            $ref: "#/components/schemas/DailySummary"

    SubmitCorrectionInput:
      type: object
      required: [original_entry_id, corrected_clock_in, corrected_clock_out, reason]
      properties:
        original_entry_id: { type: string, format: uuid }
        corrected_clock_in: { type: string, format: date-time }
        corrected_clock_out: { type: string, format: date-time }
        corrected_break_minutes: { type: integer }
        reason: { type: string }

    AbsenceEntry:
      type: object
      properties:
        employee_id: { type: string, format: uuid }
        employee_name: { type: string }
        department: { type: string }
        start_date: { type: string, format: date }
        end_date: { type: string, format: date }
        leave_type_name: { type: string }
        leave_type_color: { type: string }

    EmployeeProfile:
      type: object
      properties:
        id: { type: string, format: uuid }
        user_id: { type: string, format: uuid }
        department: { type: string }
        position_title: { type: string }
        contract_type: { type: string, enum: [full_time, part_time, mini_job, intern, temporary] }
        work_days_per_week: { type: integer }
        annual_leave_days: { type: integer }
        start_date: { type: string, format: date }
        manager_user_id: { type: string, format: uuid }
        emergency_contact_name: { type: string }
        emergency_contact_phone: { type: string }
        address_street: { type: string }
        address_city: { type: string }
        address_postal_code: { type: string }
        address_country: { type: string }
        user_name: { type: string }
        user_email: { type: string }
        manager_name: { type: string }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }

    UpdateEmployeeInput:
      type: object
      properties:
        department: { type: string }
        position_title: { type: string }
        contract_type: { type: string, enum: [full_time, part_time, mini_job, intern, temporary] }
        work_days_per_week: { type: integer }
        annual_leave_days: { type: integer }
        manager_user_id: { type: string, format: uuid }
        start_date: { type: string, format: date }

    UpdateSelfProfileInput:
      type: object
      properties:
        emergency_contact_name: { type: string }
        emergency_contact_phone: { type: string }
        address_street: { type: string }
        address_city: { type: string }
        address_postal_code: { type: string }
        address_country: { type: string }

    EmployeeDocument:
      type: object
      properties:
        id: { type: string, format: uuid }
        tenant_id: { type: string, format: uuid }
        employee_id: { type: string, format: uuid }
        category_id: { type: string, format: uuid }
        file_id: { type: string, format: uuid }
        uploaded_by: { type: string, format: uuid }
        notes: { type: string }
        category_name: { type: string }
        file_name: { type: string }
        file_size: { type: integer }
        uploaded_by_name: { type: string }
        created_at: { type: string, format: date-time }

    HRDocumentCategory:
      type: object
      properties:
        id: { type: string, format: uuid }
        tenant_id: { type: string, format: uuid }
        name: { type: string }
        key: { type: string }
        visibility: { type: string, enum: [hr_only, manager, employee] }
        is_system: { type: boolean }
        sort_order: { type: integer }
        created_at: { type: string, format: date-time }

    HRSettings:
      type: object
      properties:
        id: { type: string, format: uuid }
        tenant_id: { type: string, format: uuid }
        au_threshold_days: { type: integer }
        show_absence_reason: { type: boolean }
        default_annual_leave_days: { type: integer }
        timezone: { type: string }
        work_hours_per_day: { type: number, format: float, description: Standard work hours per day (e.g. 8.0) }
        max_daily_hours: { type: number, format: float, description: Maximum allowed working hours per day (ArbZG) }
        break_after_hours: { type: number, format: float, description: Hours worked after which a mandatory break is required }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }

    UpdateHRSettingsInput:
      type: object
      properties:
        au_threshold_days: { type: integer }
        show_absence_reason: { type: boolean }
        default_annual_leave_days: { type: integer }
        timezone: { type: string }
        work_hours_per_day: { type: number, format: float }
        max_daily_hours: { type: number, format: float }
        break_after_hours: { type: number, format: float }

    # =========================================================================
    # Inbox Schemas
    # =========================================================================

    InboxMessage:
      type: object
      properties:
        id: { type: string, format: uuid }
        user_id: { type: string, format: uuid }
        channel: { type: string, enum: [email, chat, notification] }
        source_id: { type: string }
        sender_name: { type: string }
        sender_id: { type: string, format: uuid }
        sender_email: { type: string }
        subject: { type: string }
        preview: { type: string }
        is_read: { type: boolean }
        is_starred: { type: boolean }
        is_archived: { type: boolean }
        status: { type: string, enum: [open, pending, resolved, closed] }
        snoozed_until: { type: string, format: date-time }
        assigned_to: { type: string, format: uuid }
        team_inbox_id: { type: string, format: uuid }
        tags: { type: array, items: { type: string } }
        deep_link: { type: string }
        crm_contact_id: { type: string, format: uuid }
        metadata: { type: object }
        received_at: { type: string, format: date-time }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }

    InboxMessageWrapper:
      type: object
      properties:
        message:
          $ref: "#/components/schemas/InboxMessage"

    InboxMessageList:
      type: object
      properties:
        messages:
          type: array
          items:
            $ref: "#/components/schemas/InboxMessage"
        next_page_token: { type: string }
        total_count: { type: integer }

    InboxUnreadCount:
      type: object
      properties:
        total: { type: integer }
        by_channel:
          type: array
          items:
            type: object
            properties:
              channel: { type: string, enum: [email, chat, notification] }
              count: { type: integer }

    TeamInbox:
      type: object
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        description: { type: string }
        assignment_mode: { type: string, enum: [manual, round_robin] }
        visibility: { type: string, enum: [open, private] }
        created_by: { type: string, format: uuid }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }

    CreateTeamInboxInput:
      type: object
      required: [name]
      properties:
        name: { type: string }
        description: { type: string }
        assignment_mode: { type: string, enum: [manual, round_robin], default: manual }
        visibility: { type: string, enum: [open, private], default: open }

    UpdateTeamInboxInput:
      type: object
      properties:
        name: { type: string }
        description: { type: string }
        assignment_mode: { type: string, enum: [manual, round_robin] }
        visibility: { type: string, enum: [open, private] }

    TeamInboxMember:
      type: object
      properties:
        team_inbox_id: { type: string, format: uuid }
        user_id: { type: string, format: uuid }
        role: { type: string, enum: [admin, member] }
        created_at: { type: string, format: date-time }

    RoutingRule:
      type: object
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        channel: { type: string, enum: [email, chat, notification] }
        conditions: { type: object, description: "AND/OR condition tree with field/operator/value leaves" }
        actions: { type: object, description: "Array of actions: route_to_team, assign_to, add_tags, auto_reply" }
        priority: { type: integer }
        is_active: { type: boolean }
        created_by: { type: string, format: uuid }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }

    CreateRoutingRuleInput:
      type: object
      required: [name, conditions, actions]
      properties:
        name: { type: string }
        channel: { type: string, enum: [email, chat, notification] }
        conditions: { type: object }
        actions: { type: object }
        priority: { type: integer, default: 100 }

    UpdateRoutingRuleInput:
      type: object
      properties:
        name: { type: string }
        channel: { type: string, enum: [email, chat, notification] }
        conditions: { type: object }
        actions: { type: object }
        priority: { type: integer }
        is_active: { type: boolean }

    # =======================================================================
    # Automation schemas
    # =======================================================================

    Automation:
      type: object
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        description: { type: string }
        scope: { type: string, enum: [personal, team, organization] }
        owner_id: { type: string, format: uuid }
        trigger_type: { type: string }
        trigger_config: { type: object }
        conditions: { type: object }
        actions: { type: object }
        is_active: { type: boolean }
        max_steps: { type: integer }
        template_id: { type: string, nullable: true }
        last_triggered_at: { type: string, format: date-time, nullable: true }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }

    CreateAutomationRequest:
      type: object
      required: [name, trigger_type]
      properties:
        name: { type: string }
        description: { type: string }
        scope: { type: string, enum: [personal, team, organization], default: personal }
        trigger_type: { type: string }
        trigger_config: { type: object }
        conditions: { type: object }
        actions: { type: object }
        max_steps: { type: integer, default: 10 }

    UpdateAutomationRequest:
      type: object
      properties:
        name: { type: string }
        description: { type: string }
        scope: { type: string, enum: [personal, team, organization] }
        trigger_type: { type: string }
        trigger_config: { type: object }
        conditions: { type: object }
        actions: { type: object }
        max_steps: { type: integer }

    AutomationExecution:
      type: object
      properties:
        id: { type: string, format: uuid }
        automation_id: { type: string, format: uuid }
        chain_id: { type: string, format: uuid }
        trigger_event: { type: object }
        condition_result: { type: boolean }
        status: { type: string, enum: [running, completed, failed, skipped, aborted] }
        steps:
          type: array
          items:
            $ref: "#/components/schemas/ExecutionStep"
        error_message: { type: string, nullable: true }
        started_at: { type: string, format: date-time }
        completed_at: { type: string, format: date-time, nullable: true }
        duration_ms: { type: integer, nullable: true }

    ExecutionStep:
      type: object
      properties:
        action_type: { type: string }
        input: { type: object }
        output: { type: object }
        error: { type: string, nullable: true }
        duration_ms: { type: integer }

    TriggerDefinition:
      type: object
      properties:
        type: { type: string }
        module: { type: string }
        name: { type: string }
        description: { type: string }

    ActionDefinition:
      type: object
      properties:
        type: { type: string }
        module: { type: string }
        name: { type: string }
        description: { type: string }

    AutomationTemplate:
      type: object
      properties:
        id: { type: string }
        name: { type: string }
        description: { type: string }
        category: { type: string }
        complexity: { type: string, enum: [simple, medium, advanced] }
        trigger_type: { type: string }
        trigger_config: { type: object }
        conditions: { type: object }
        actions: { type: object }
        created_at: { type: string, format: date-time }

    DryRunStep:
      type: object
      properties:
        action_type: { type: string }
        action_name: { type: string }
        would_run: { type: boolean }
        error: { type: string, nullable: true }

    AutomationStats:
      type: object
      properties:
        total_automations: { type: integer }
        active_automations: { type: integer }
        total_executions: { type: integer }
        success_rate: { type: number, format: float }

    AdvisoryProduct:
      type: object
      required: [id, name, risk_class, risks]
      properties:
        id:         { type: string }
        name:       { type: string }
        isin:       { type: string, nullable: true }
        category:   { type: string, nullable: true }
        risk_class: { type: integer, minimum: 1, maximum: 7, description: 'PRIIP SRI 1-7' }
        opportunities: { type: string, nullable: true }
        risks:      { type: string }
        costs_one_time: { type: number, format: double, nullable: true }
        costs_running:  { type: number, format: double, nullable: true }
        recommended: { type: boolean }

    AdvisoryProtocol:
      type: object
      required: [id, tenant_id, contact_id, created_by, status, risk_class]
      properties:
        id:          { type: string, format: uuid }
        tenant_id:   { type: string, format: uuid }
        contact_id:  { type: string, format: uuid }
        created_by:  { type: string, format: uuid }
        status:      { type: string, enum: [draft, finalized] }
        handed_over_at: { type: string, format: date-time, nullable: true }
        # § 1
        date:             { type: string, format: date, nullable: true }
        time_from:        { type: string }
        time_to:          { type: string }
        location:         { type: string, enum: [office, phone, video, onsite, ''] }
        advisor:          { type: string }
        occasion:         { type: string, enum: [initial, followup, event, ''] }
        occasion_note:    { type: string }
        customer_category: { type: string, enum: [private, professional, ''] }
        # § 2
        birth_date:       { type: string, format: date, nullable: true }
        marital_status:   { type: string }
        tax_status:       { type: string }
        # § 3
        known_asset_classes: { type: array, items: { type: string } }
        past_transactions:   { type: string }
        financial_education: { type: string }
        professional_experience: { type: string }
        self_assessment:     { type: integer, minimum: 1, maximum: 5, nullable: true }
        # § 4
        monthly_net_income:    { type: number, format: double, nullable: true }
        recurring_liabilities: { type: number, format: double, nullable: true }
        liquid_assets:         { type: number, format: double, nullable: true }
        current_investments:   { type: number, format: double, nullable: true }
        real_estate:           { type: string }
        existing_insurance:    { type: string }
        max_loss_capacity_abs: { type: number, format: double, nullable: true }
        max_loss_capacity_pct: { type: number, format: double, nullable: true }
        # § 5
        investment_purpose: { type: array, items: { type: string } }
        horizon:            { type: string }
        risk_tolerance:     { type: string }
        risk_capacity:      { type: string }
        risk_class:         { type: integer, minimum: 1, maximum: 7 }
        esg_preference:     { type: boolean }
        esg_details:        { type: string }
        one_time_amount:    { type: number, format: double, nullable: true }
        monthly_savings:    { type: number, format: double, nullable: true }
        # § 6
        products: { type: array, items: { $ref: '#/components/schemas/AdvisoryProduct' } }
        # § 7
        recommendation_summary: { type: string }
        suitability_reasoning:  { type: string }
        goal_reference:         { type: string }
        alternatives:           { type: string }
        not_recommended:        { type: string }
        # § 8
        main_concerns:          { type: string }
        warnings_given:         { type: array, items: { type: string } }
        document_delivered:     { type: boolean }
        document_delivered_date: { type: string, format: date, nullable: true }
        delivery_form:          { type: string, enum: [paper, email, portal] }
        advisor_signature:      { type: string }
        customer_confirmation:  { type: boolean }
        document_waiver:        { type: boolean }
        followup_date:          { type: string, format: date, nullable: true }
        internal_notes:         { type: string }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }

    AdvisoryProtocolUpdate:
      # Same shape as AdvisoryProtocol minus read-only server fields.
      # The FE sends a full snapshot; the server ignores id/tenant_id/status/created_by.
      allOf:
        - $ref: '#/components/schemas/AdvisoryProtocol'

    ReferralReportEntry:
      type: object
      properties:
        referrer_id:         { type: string, format: uuid }
        referrer_first_name: { type: string }
        referrer_last_name:  { type: string }
        referred_count:      { type: integer }
    ModuleLead:
      type: object
      properties:
        tenant_id:
          type: string
          format: uuid
        user_id:
          type: string
          format: uuid
        module_id:
          type: string
          description: One of the ModuleId values (see lib/pricing.ts)
        granted_by:
          type: string
          format: uuid
        granted_at:
          type: string
          format: date-time
      required: [tenant_id, user_id, module_id, granted_at]

    SettingEntry:
      type: object
      properties:
        key:
          type: string
          description: Setting key (e.g. "defaultView", "payroll.beraterNr")
        value:
          description: Any JSON value (string, number, boolean, array, object)
      required: [key, value]

    SettingsResponse:
      type: object
      properties:
        entries:
          type: array
          items: { $ref: "#/components/schemas/SettingEntry" }

    PutSettingsRequest:
      type: object
      description: >
        Flat map of key → value. Patch semantics — only supplied keys are written;
        others are untouched. Example:
          {"defaultView": "week", "workStartHour": 8, "payroll.beraterNr": "12345"}
      properties:
        settings:
          type: object
          additionalProperties: true
          description: >
            Key-value pairs where values can be any JSON type.
            Keys should follow the payroll.* namespacing convention for payroll settings.
      required: [settings]
      example:
        settings:
          defaultView: "week"
          workStartHour: 8
          payroll.beraterNr: "12345"
          payroll.mandantNr: "67890"
    ResolvedLabel:
      type: object
      properties:
        value:
          type: string
          description: Effective text — the tenant override, or "" when unset (the FE i18n bundle already holds the code default).
        provenance:
          type: string
          enum: [default, tenant]
          description: Where the effective value came from. Vendor/draft provenance is not produced by this endpoint yet.
        key:
          type: string
          description: The i18n key from the label whitelist.
      required: [value, provenance, key]

    LabelOverridesResponse:
      type: object
      properties:
        locale:
          type: string
        labels:
          type: object
          additionalProperties: { $ref: "#/components/schemas/ResolvedLabel" }
          description: One entry per whitelisted i18n key.
      required: [locale, labels]

    UpdateLabelOverridesRequest:
      type: object
      description: >
        Upserts tenant-scope label overrides for a locale. Only whitelisted keys are
        honoured — others are silently dropped. An empty-string value clears that key's
        override back to the code default.
      properties:
        locale:
          type: string
        layer:
          type: string
          enum: [tenant, vendor]
          description: Only "tenant" (the default) is currently writable; "vendor" is rejected with 400.
        overrides:
          type: object
          additionalProperties: { type: string }
      required: [locale, overrides]
      example:
        locale: de
        overrides:
          crm.deals.title: "Aufträge"

    GobdDocument:
      type: object
      description: An immutable archived GoBD document (§147 AO)
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        doc_type:
          type: string
          enum: [invoice, credit_note, receipt, contract, correspondence, other]
        source_invoice_id:
          type: string
          format: uuid
          nullable: true
        storage_key:
          type: string
        sha256:
          type: string
          description: Hex-encoded SHA-256 checksum of the file contents (64 chars)
        original_filename:
          type: string
        mime_type:
          type: string
        file_size_bytes:
          type: integer
          format: int64
        archived_at:
          type: string
          format: date-time
        archived_by:
          type: string
          format: uuid
        retention_until:
          type: string
          format: date
          description: "31.12 of (archive_year + 8) — minimum §147 AO retention deadline"

    GobdDocumentEvent:
      type: object
      description: An audit trail entry for a GoBD document
      properties:
        id:
          type: string
          format: uuid
        document_id:
          type: string
          format: uuid
        event_type:
          type: string
          enum: [archived, annotation, access, integrity_check]
        created_by:
          type: string
          format: uuid
        created_at:
          type: string
          format: date-time
        note:
          type: string
        metadata_json:
          type: string
          description: JSON-encoded event metadata

    GobdDocumentWithEvents:
      type: object
      properties:
        document:
          $ref: "#/components/schemas/GobdDocument"
        events:
          type: array
          items:
            $ref: "#/components/schemas/GobdDocumentEvent"

    GobdDocumentList:
      type: object
      properties:
        documents:
          type: array
          items:
            $ref: "#/components/schemas/GobdDocument"
        total:
          type: integer
        page:
          type: integer
        per_page:
          type: integer

    GobdDownloadResponse:
      type: object
      properties:
        presigned_url:
          type: string
          format: uri
          description: Presigned MinIO/S3 URL valid for 1 hour
        original_filename:
          type: string
        mime_type:
          type: string

    IncomingInvoiceLineItem:
      type: object
      properties:
        position:
          type: integer
        description:
          type: string
        quantity:
          type: string
        unit_price:
          type: string
        tax_rate:
          type: string
        line_total:
          type: string

    IncomingInvoiceTaxEntry:
      type: object
      properties:
        tax_rate:
          type: string
        taxable_net:
          type: string
        tax_amount:
          type: string

    IncomingInvoice:
      type: object
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        supplier_name:
          type: string
        supplier_address:
          type: string
        supplier_vat_id:
          type: string
        invoice_number:
          type: string
        invoice_date:
          type: string
          format: date
        due_date:
          type: string
          format: date
          nullable: true
        currency:
          type: string
          example: EUR
        line_items:
          type: array
          items:
            $ref: "#/components/schemas/IncomingInvoiceLineItem"
        tax_breakdown:
          type: array
          items:
            $ref: "#/components/schemas/IncomingInvoiceTaxEntry"
        subtotal:
          type: string
        total_tax:
          type: string
        gross_total:
          type: string
        source_format:
          type: string
          enum: [zugferd_cii, xrechnung_ubl, xml_only]
        original_filename:
          type: string
        status:
          type: string
          enum: [received, reviewed, booked, rejected]
        created_by:
          type: string
          format: uuid
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time


    # -----------------------------------------------------------------------
    # Shared
    # -----------------------------------------------------------------------

    ProtoTimestamp:
      type: object
      description: >-
        Wire shape of google.protobuf.Timestamp as serialised by Go's
        encoding/json over protobuf. NOT an RFC-3339 string. Normalise
        client-side with normalizeWireTimestamps().
      properties:
        seconds:
          type: integer
          format: int64
        nanos:
          type: integer
          format: int32

    # -----------------------------------------------------------------------
    # Sessions
    # -----------------------------------------------------------------------

    UserSession:
      type: object
      properties:
        id:
          type: string
          format: uuid
        user_id:
          type: string
          format: uuid
        device:
          type: string
          description: Device/user-agent description
        ip_address:
          type: string
        created_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        last_active_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        expires_at:
          $ref: "#/components/schemas/ProtoTimestamp"

    ListSessionsResponse:
      type: object
      properties:
        sessions:
          type: array
          items:
            $ref: "#/components/schemas/UserSession"

    # -----------------------------------------------------------------------
    # 2FA — user
    # -----------------------------------------------------------------------

    TwoFactorSetupResponse:
      type: object
      description: Returned by POST /auth/2fa/setup
      properties:
        secret:
          type: string
          description: Base32 TOTP secret
        qr_uri:
          type: string
          description: otpauth:// URI for QR code generation

    Verify2FARequest:
      type: object
      required: [code]
      properties:
        code:
          type: string
          minLength: 6
          maxLength: 6
          description: 6-digit TOTP code

    RecoveryCodesResponse:
      type: object
      properties:
        recovery_codes:
          type: array
          items:
            type: string
          description: One-time recovery codes (show once, then hashed)

    Disable2FARequest:
      type: object
      required: [code]
      properties:
        code:
          type: string
          description: Current TOTP code to confirm intent

    RegenerateCodesRequest:
      type: object
      required: [code]
      properties:
        code:
          type: string
          description: Current TOTP code to confirm intent

    # -----------------------------------------------------------------------
    # 2FA — admin policy
    # -----------------------------------------------------------------------

    TwoFactorPolicy:
      type: object
      properties:
        id:
          type: string
          format: uuid
        role_name:
          type: string
          enum: [admin, manager, member]
        enforced:
          type: boolean
        grace_period_days:
          type: integer
          minimum: 0
          maximum: 365
        updated_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        updated_by:
          type: string
          format: uuid

    TwoFactorPolicyResponse:
      type: object
      description: Single policy (when filtered by role) or all policies
      properties:
        policy:
          $ref: "#/components/schemas/TwoFactorPolicy"
        policies:
          type: array
          description: Present when no role_name filter is applied
          items:
            $ref: "#/components/schemas/TwoFactorPolicy"

    UpdateTwoFactorPolicyRequest:
      type: object
      required: [role_name, enforced]
      properties:
        role_name:
          type: string
          enum: [admin, manager, member]
        enforced:
          type: boolean
        grace_period_days:
          type: integer
          minimum: 0
          maximum: 365

    AdminReset2FARequest:
      type: object
      required: [user_id, reason]
      properties:
        user_id:
          type: string
          format: uuid
        reason:
          type: string

    # -----------------------------------------------------------------------
    # Audit log
    # -----------------------------------------------------------------------

    AuditEntry:
      type: object
      properties:
        id:
          type: string
          format: uuid
        sequence_num:
          type: integer
          format: int64
        timestamp:
          $ref: "#/components/schemas/ProtoTimestamp"
        user_id:
          type: string
          format: uuid
        action:
          type: string
        target:
          type: string
        target_type:
          type: string
        details:
          type: string
          description: JSON string with action-specific details
        ip_address:
          type: string
        user_agent:
          type: string
        result:
          type: string
          description: success or failure
        previous_hash:
          type: string
        entry_hash:
          type: string

    ListAuditEntriesResponse:
      type: object
      properties:
        entries:
          type: array
          items:
            $ref: "#/components/schemas/AuditEntry"
        total:
          type: integer

    VerifyAuditChainResponse:
      type: object
      properties:
        valid:
          type: boolean
        entries_checked:
          type: integer
          format: int64
        first_invalid_sequence:
          type: integer
          format: int64
          description: 0 when all entries are valid
        error_message:
          type: string

    # -----------------------------------------------------------------------
    # Vault
    # -----------------------------------------------------------------------

    VaultSecretMeta:
      type: object
      properties:
        id:
          type: string
          format: uuid
        key_name:
          type: string
        key_version:
          type: integer
        description:
          type: string
        created_by:
          type: string
          format: uuid
        created_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        updated_at:
          $ref: "#/components/schemas/ProtoTimestamp"

    ListVaultSecretsResponse:
      type: object
      properties:
        secrets:
          type: array
          items:
            $ref: "#/components/schemas/VaultSecretMeta"

    SetVaultSecretRequest:
      type: object
      required: [key_name, plaintext_value]
      properties:
        key_name:
          type: string
        plaintext_value:
          type: string
          format: password
        description:
          type: string

    VaultSecretResponse:
      type: object
      description: Returned by PUT /vault — metadata only, no plaintext
      properties:
        secret:
          $ref: "#/components/schemas/VaultSecretMeta"

    VaultSecretValueResponse:
      type: object
      description: Returned by GET /vault/{keyName} — includes decrypted value
      properties:
        secret:
          $ref: "#/components/schemas/VaultSecretMeta"
        decrypted_value:
          type: string

    # -----------------------------------------------------------------------
    # GDPR — export
    # -----------------------------------------------------------------------

    GDPRExportEntry:
      type: object
      description: Single GDPR export request record
      properties:
        id:
          type: string
          format: uuid
        user_id:
          type: string
          format: uuid
        status:
          type: string
          description: pending | approved | denied | ready | expired
        requested_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        reviewed_by:
          type: string
          format: uuid
        reviewed_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        review_note:
          type: string
        download_token:
          type: string
        download_expires_at:
          $ref: "#/components/schemas/ProtoTimestamp"

    GDPRExportRequestResponse:
      type: object
      properties:
        export_request:
          $ref: "#/components/schemas/GDPRExportEntry"

    ListGDPRExportsResponse:
      type: object
      properties:
        export_requests:
          type: array
          items:
            $ref: "#/components/schemas/GDPRExportEntry"

    ReviewExportRequest:
      type: object
      properties:
        review_note:
          type: string

    # -----------------------------------------------------------------------
    # GDPR — erasure
    # -----------------------------------------------------------------------

    ErasureUserRequest:
      type: object
      required: [user_id]
      properties:
        user_id:
          type: string
          format: uuid

    ModuleErasurePreview:
      type: object
      properties:
        module_name:
          type: string
        record_count:
          type: integer
        action:
          type: string
          description: anonymize or delete

    ErasurePreviewResponse:
      type: object
      properties:
        modules:
          type: array
          items:
            $ref: "#/components/schemas/ModuleErasurePreview"
        total_records:
          type: integer

    ExecuteErasureRequest:
      type: object
      required: [user_id, admin_password]
      properties:
        user_id:
          type: string
          format: uuid
        admin_password:
          type: string
          format: password
          description: Admin password confirmation to prevent accidental erasure

    ExecuteErasureResponse:
      type: object
      properties:
        anonymized_label:
          type: string
          description: Pseudonym replacing the erased user's name (e.g. deleted_user_<hash>)
        confirmation_hash:
          type: string
          description: SHA-256 hash of the erasure audit record
        modules_affected:
          type: array
          items:
            $ref: "#/components/schemas/ModuleErasurePreview"

    # -----------------------------------------------------------------------
    # DSAR
    # -----------------------------------------------------------------------

    DSARModule:
      type: object
      properties:
        module:
          type: string
        columns:
          type: array
          items:
            type: string
        records:
          type: array
          description: >-
            Each record is a flat key→value object. Column order is preserved
            per the columns list.
          items:
            type: object
            additionalProperties:
              type: string

    DSARPerson:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        email:
          type: string
          format: email
        company:
          type: string
        avatar:
          type: string
        modules:
          type: array
          items:
            $ref: "#/components/schemas/DSARModule"

    DSARSearchResponse:
      type: object
      properties:
        results:
          type: array
          items:
            $ref: "#/components/schemas/DSARPerson"

    # -----------------------------------------------------------------------
    # Password policy
    # -----------------------------------------------------------------------

    PasswordPolicy:
      type: object
      properties:
        id:
          type: string
          format: uuid
        min_length:
          type: integer
          minimum: 1
        require_uppercase:
          type: boolean
        require_lowercase:
          type: boolean
        require_digit:
          type: boolean
        require_special:
          type: boolean
        min_entropy:
          type: number
          format: double
          description: Minimum Shannon entropy (bits)
        max_age_days:
          type: integer
          description: 0 = no expiration
        prevent_reuse_count:
          type: integer
          description: Number of previous passwords that cannot be reused
        updated_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        updated_by:
          type: string
          format: uuid

    PasswordPolicyResponse:
      type: object
      properties:
        policy:
          $ref: "#/components/schemas/PasswordPolicy"

    UpdatePasswordPolicyRequest:
      type: object
      properties:
        min_length:
          type: integer
          minimum: 1
        require_uppercase:
          type: boolean
        require_lowercase:
          type: boolean
        require_digit:
          type: boolean
        require_special:
          type: boolean
        min_entropy:
          type: number
          format: double
        max_age_days:
          type: integer
        prevent_reuse_count:
          type: integer

    ValidatePasswordRequest:
      type: object
      required: [password]
      properties:
        password:
          type: string
          format: password

    ValidatePasswordResponse:
      type: object
      properties:
        valid:
          type: boolean
        violations:
          type: array
          items:
            type: string
          description: Human-readable rule violations (empty when valid)
        entropy:
          type: number
          format: double

    # -----------------------------------------------------------------------
    # IP access rules
    # -----------------------------------------------------------------------

    IPAccessRule:
      type: object
      properties:
        id:
          type: string
          format: uuid
        ip_cidr:
          type: string
          description: CIDR notation, e.g. 192.168.1.0/24 or 10.0.0.1/32
        rule_type:
          type: string
          enum: [allow, block]
        description:
          type: string
        created_by:
          type: string
          format: uuid
        created_at:
          $ref: "#/components/schemas/ProtoTimestamp"

    ListIPRulesResponse:
      type: object
      properties:
        rules:
          type: array
          items:
            $ref: "#/components/schemas/IPAccessRule"

    CreateIPRuleRequest:
      type: object
      required: [ip_cidr, rule_type]
      properties:
        ip_cidr:
          type: string
          description: CIDR notation
        rule_type:
          type: string
          enum: [allow, block]
        description:
          type: string

    IPRuleResponse:
      type: object
      properties:
        rule:
          $ref: "#/components/schemas/IPAccessRule"
    RetentionPolicy:
      type: object
      properties:
        id:
          type: string
          format: uuid
        resource_type:
          type: string
          description: Resource type this policy applies to (e.g. contacts, audit_log)
        retention_days:
          type: integer
          minimum: 1
          description: Number of days to retain data before action is applied
        action:
          type: string
          enum: [delete, anonymize]
          description: Action to perform when retention_days is exceeded
        enabled:
          type: boolean
        description:
          type: string
        created_by:
          type: string
          format: uuid
        created_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        updated_at:
          $ref: "#/components/schemas/ProtoTimestamp"

    ListRetentionPoliciesResponse:
      type: object
      properties:
        policies:
          type: array
          items:
            $ref: "#/components/schemas/RetentionPolicy"

    RetentionPolicyResponse:
      type: object
      properties:
        policy:
          $ref: "#/components/schemas/RetentionPolicy"

    CreateRetentionPolicyRequest:
      type: object
      required: [resource_type, retention_days, action]
      properties:
        resource_type:
          type: string
        retention_days:
          type: integer
          minimum: 1
        action:
          type: string
          enum: [delete, anonymize]
        enabled:
          type: boolean
          default: true
        description:
          type: string

    UpdateRetentionPolicyRequest:
      type: object
      required: [retention_days, action]
      properties:
        retention_days:
          type: integer
          minimum: 1
        action:
          type: string
          enum: [delete, anonymize]
        enabled:
          type: boolean
        description:
          type: string

    # -----------------------------------------------------------------------
    # Vendor Access (RBAC R-5 B, GDAP-light v3)
    # -----------------------------------------------------------------------

    VendorAccessAgent:
      type: object
      properties:
        name:
          type: string

    VendorAccessRequest:
      type: object
      description: A time-boxed Zentria support access request into a tenant's data.
      properties:
        id:
          type: string
          format: uuid
        reason:
          type: string
        description:
          type: string
        ticket_ref:
          type: string
        agents:
          type: array
          items:
            $ref: "#/components/schemas/VendorAccessAgent"
        scope:
          type: array
          items:
            type: string
          description: Area IDs from the frontend's VENDOR_ACCESS_AREAS catalogue.
        requested_start:
          type: string
          format: date
        duration_days:
          type: integer
        expires_at:
          type: string
          format: date
        status:
          type: string
          enum: [pending, counter_proposed, active, declined, expired, revoked, completed]
        counter_proposed_start:
          type: string
          format: date
        approved_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        approved_by:
          type: string
          description: Reviewer display name, resolved server-side.
        sensitive_ack:
          type: boolean
        revoked_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        revoked_by:
          type: string
          description: Reviewer display name, resolved server-side.
        completed_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        created_at:
          $ref: "#/components/schemas/ProtoTimestamp"

    VendorAccessListResponse:
      type: object
      properties:
        requests:
          type: array
          items:
            $ref: "#/components/schemas/VendorAccessRequest"

    VendorAccessRequestResponse:
      type: object
      properties:
        request:
          $ref: "#/components/schemas/VendorAccessRequest"

    ApproveVendorAccessRequest:
      type: object
      properties:
        sensitive_ack:
          type: boolean

    CounterProposeVendorAccessRequest:
      type: object
      required: [proposed_start]
      properties:
        proposed_start:
          type: string
          format: date

    # -----------------------------------------------------------------------
    # Modul: formulare
    # -----------------------------------------------------------------------

    FormSchema:
      type: object
      description: A form schema definition (fields layout + metadata).
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        title:
          type: string
        description:
          type: string
        fields:
          type: string
          format: byte
          description: JSON-array of FormField (base64-encoded bytes on the wire)
        status:
          type: string
          enum: [draft, active, archived]
          description: "Proto enum: 1=draft, 2=active, 3=archived"
        is_template:
          type: boolean
        is_public:
          type: boolean
        page_count:
          type: integer
          format: int32
        submission_count:
          type: integer
          format: int32
        created_by:
          type: string
          format: uuid
        created_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        updated_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        deleted_at:
          $ref: "#/components/schemas/ProtoTimestamp"

    FormSubmission:
      type: object
      description: A single form submission (answers from one respondent).
      properties:
        id:
          type: string
          format: uuid
        form_schema_id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        answers:
          type: string
          format: byte
          description: JSON-object of field answers (base64-encoded bytes on the wire)
        status:
          type: string
          enum: [new, read, archived]
          description: "Proto enum: 1=new, 2=read, 3=archived"
        submitted_by:
          type: string
        ip_address:
          type: string
        submitted_at:
          $ref: "#/components/schemas/ProtoTimestamp"

    FormWebhook:
      type: object
      description: A webhook subscription attached to a form schema.
      properties:
        id:
          type: string
          format: uuid
        form_schema_id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        url:
          type: string
          format: uri
        secret:
          type: string
          description: HMAC secret (write-only; returned masked or omitted)
        events:
          type: array
          items: { type: string }
        active:
          type: boolean
        last_triggered_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        last_status:
          type: string
        created_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        updated_at:
          $ref: "#/components/schemas/ProtoTimestamp"

    FormWebhookDelivery:
      type: object
      description: A single delivery attempt for a webhook event.
      properties:
        id:
          type: string
          format: uuid
        webhook_id:
          type: string
          format: uuid
        submission_id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        payload:
          type: string
          format: byte
          description: Raw delivery payload (base64-encoded bytes on the wire)
        status:
          type: string
          enum: [pending, delivered, failed, dead]
          description: "Proto enum: 1=pending, 2=delivered, 3=failed, 4=dead"
        attempt_count:
          type: integer
          format: int32
        max_attempts:
          type: integer
          format: int32
        next_attempt_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        last_error:
          type: string
        last_response_code:
          type: integer
          format: int32
        created_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        delivered_at:
          $ref: "#/components/schemas/ProtoTimestamp"

    FormStats:
      type: object
      description: Aggregated submission statistics for a form schema.
      properties:
        form_schema_id:
          type: string
          format: uuid
        total_count:
          type: integer
          format: int32
        new_count:
          type: integer
          format: int32
        last_7d_count:
          type: integer
          format: int32
        last_30d_count:
          type: integer
          format: int32

    # -----------------------------------------------------------------------
    # Modul: vertraege
    # -----------------------------------------------------------------------

    VertraegeContract:
      type: object
      description: A contract managed in the Vertraege module.
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        contract_number:
          type: string
        title:
          type: string
        contract_type:
          type: string
          enum: [rental, service, employment, nda, other]
        status:
          type: string
          enum: [draft, active, expired, terminated]
        starts_on:
          $ref: "#/components/schemas/ProtoTimestamp"
        ends_on:
          $ref: "#/components/schemas/ProtoTimestamp"
        document_url:
          type: string
          format: uri
        notes:
          type: string
        created_by:
          type: string
          format: uuid
        signature_provider:
          type: string
          description: "Phase D placeholder (Skribble); null until then"
        signature_data:
          type: string
          description: "data:image/png;base64,... or data:image/svg+xml;base64,..."
        signed_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        signed_by:
          type: string
        created_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        updated_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        parties:
          type: array
          description: "Populated only by GET /contracts/{id}"
          items: { $ref: "#/components/schemas/VertraegeContractParty" }
        reminders:
          type: array
          description: "Populated only by GET /contracts/{id}"
          items: { $ref: "#/components/schemas/VertraegeContractReminder" }

    VertraegeContractParty:
      type: object
      description: A party (contact, company, or external) linked to a contract.
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        contract_id:
          type: string
          format: uuid
        party_type:
          type: string
          enum: [contact, company, external]
        contact_id:
          type: string
          format: uuid
        company_id:
          type: string
          format: uuid
        external_name:
          type: string
        role_in_contract:
          type: string
        signed_on:
          $ref: "#/components/schemas/ProtoTimestamp"
        created_at:
          $ref: "#/components/schemas/ProtoTimestamp"

    VertraegeContractReminder:
      type: object
      description: A scheduled reminder attached to a contract.
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        contract_id:
          type: string
          format: uuid
        remind_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        reminder_type:
          type: string
          enum: [renewal, expiry, payment, custom]
        subject:
          type: string
        message:
          type: string
        status:
          type: string
          enum: [pending, sent, cancelled]
        created_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        sent_at:
          $ref: "#/components/schemas/ProtoTimestamp"

    # -----------------------------------------------------------------------
    # Modul: mails
    # -----------------------------------------------------------------------

    EmailAddressInfo:
      type: object
      properties:
        name:
          type: string
        email:
          type: string
          format: email

    EmailAttachmentInfo:
      type: object
      properties:
        id:
          type: string
          format: uuid
        filename:
          type: string
        content_type:
          type: string
        size_bytes:
          type: integer
          format: int64
        minio_key:
          type: string
          description: Internal MinIO object key (not exposed to end users)
        content_id:
          type: string
          description: CID reference for inline images
        is_inline:
          type: boolean

    EmailAccountInfo:
      type: object
      properties:
        id:
          type: string
          format: uuid
        user_id:
          type: string
          format: uuid
        email_address:
          type: string
          format: email
        display_name:
          type: string
        imap_host:
          type: string
        imap_port:
          type: integer
        smtp_host:
          type: string
        smtp_port:
          type: integer
        username:
          type: string
        use_ssl:
          type: boolean
        sync_enabled:
          type: boolean
        last_sync_at:
          type: string
          format: date-time
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time

    CreateEmailAccountRequest:
      type: object
      required: [user_id, email_address, imap_host, imap_port, smtp_host, smtp_port, username, password]
      properties:
        user_id:
          type: string
          format: uuid
        email_address:
          type: string
          format: email
        display_name:
          type: string
        imap_host:
          type: string
        imap_port:
          type: integer
        smtp_host:
          type: string
        smtp_port:
          type: integer
        username:
          type: string
        password:
          type: string
          description: Plaintext; encrypted at service layer
        use_ssl:
          type: boolean
        sync_enabled:
          type: boolean

    CreateEmailAccountResponse:
      type: object
      properties:
        account:
          $ref: "#/components/schemas/EmailAccountInfo"

    GetEmailAccountResponse:
      type: object
      properties:
        account:
          $ref: "#/components/schemas/EmailAccountInfo"

    UpdateEmailAccountRequest:
      type: object
      properties:
        display_name:
          type: string
          nullable: true
        imap_host:
          type: string
          nullable: true
        imap_port:
          type: integer
          nullable: true
        smtp_host:
          type: string
          nullable: true
        smtp_port:
          type: integer
          nullable: true
        username:
          type: string
          nullable: true
        password:
          type: string
          nullable: true
        use_ssl:
          type: boolean
          nullable: true
        sync_enabled:
          type: boolean
          nullable: true

    UpdateEmailAccountResponse:
      type: object
      properties:
        account:
          $ref: "#/components/schemas/EmailAccountInfo"

    TestEmailConnectionRequest:
      type: object
      required: [imap_host, imap_port, smtp_host, smtp_port, username, password]
      properties:
        imap_host:
          type: string
        imap_port:
          type: integer
        smtp_host:
          type: string
        smtp_port:
          type: integer
        username:
          type: string
        password:
          type: string
        use_ssl:
          type: boolean

    TestEmailConnectionResponse:
      type: object
      properties:
        imap_ok:
          type: boolean
        smtp_ok:
          type: boolean
        imap_error:
          type: string
        smtp_error:
          type: string

    EmailFolderInfo:
      type: object
      properties:
        id:
          type: string
          format: uuid
        account_id:
          type: string
          format: uuid
        name:
          type: string
        imap_name:
          type: string
        folder_type:
          type: string
          enum: [inbox, sent, drafts, trash, spam, archive, custom]
        uid_validity:
          type: integer
          format: int64
        message_count:
          type: integer
        unread_count:
          type: integer
        sort_order:
          type: integer
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time

    ListFoldersResponse:
      type: object
      properties:
        folders:
          type: array
          items:
            $ref: "#/components/schemas/EmailFolderInfo"

    GetFolderResponse:
      type: object
      properties:
        folder:
          $ref: "#/components/schemas/EmailFolderInfo"

    SyncFoldersRequest:
      type: object
      required: [account_id]
      properties:
        account_id:
          type: string
          format: uuid

    EmailMessageInfo:
      type: object
      properties:
        id:
          type: string
          format: uuid
        account_id:
          type: string
          format: uuid
        folder_id:
          type: string
          format: uuid
        uid:
          type: integer
          format: int64
          description: IMAP UID
        message_id_header:
          type: string
          description: RFC 2822 Message-ID header value
        in_reply_to:
          type: string
        references:
          type: array
          items:
            type: string
        thread_id:
          type: string
        from:
          $ref: "#/components/schemas/EmailAddressInfo"
        to:
          type: array
          items:
            $ref: "#/components/schemas/EmailAddressInfo"
        cc:
          type: array
          items:
            $ref: "#/components/schemas/EmailAddressInfo"
        bcc:
          type: array
          items:
            $ref: "#/components/schemas/EmailAddressInfo"
        subject:
          type: string
        preview:
          type: string
          description: Short plain-text excerpt (~100 chars)
        body_text:
          type: string
        body_html:
          type: string
        is_read:
          type: boolean
        is_starred:
          type: boolean
        is_draft:
          type: boolean
        has_attachments:
          type: boolean
        date:
          type: string
          format: date-time
        size_bytes:
          type: integer
          format: int64
        attachments:
          type: array
          items:
            $ref: "#/components/schemas/EmailAttachmentInfo"
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        label_ids:
          type: array
          items:
            type: string
            format: uuid

    EmailListMessagesResponse:
      type: object
      properties:
        messages:
          type: array
          items:
            $ref: "#/components/schemas/EmailMessageInfo"
        total:
          type: integer

    GetMessageResponse:
      type: object
      properties:
        message:
          $ref: "#/components/schemas/EmailMessageInfo"

    GetThreadMessagesResponse:
      type: object
      properties:
        messages:
          type: array
          items:
            $ref: "#/components/schemas/EmailMessageInfo"

    ToggleStarResponse:
      type: object
      properties:
        is_starred:
          type: boolean

    MoveToFolderRequest:
      type: object
      required: [target_folder_id]
      properties:
        target_folder_id:
          type: string
          format: uuid

    BulkMessageActionRequest:
      type: object
      required: [ids, action]
      properties:
        ids:
          type: array
          minItems: 1
          maxItems: 500
          items:
            type: string
            format: uuid
        action:
          type: string
          enum: [read, unread, star, unstar, archive, spam, move, delete]
        target:
          type: string
          format: uuid
          description: Destination folder id. Required for "move"; ignored otherwise.

    BulkMessageActionResponse:
      type: object
      properties:
        affected:
          type: integer
          description: Number of messages actually changed. Ids outside the caller's tenant are silently excluded, not errored.

    SendEmailRequest:
      type: object
      required: [account_id, to, subject]
      properties:
        account_id:
          type: string
          format: uuid
        to:
          type: array
          minItems: 1
          items:
            type: string
            format: email
        cc:
          type: array
          items:
            type: string
            format: email
        bcc:
          type: array
          items:
            type: string
            format: email
        subject:
          type: string
        body_html:
          type: string
        body_text:
          type: string
        attachment_ids:
          type: array
          items:
            type: string
            format: uuid
        in_reply_to_message_id:
          type: string
          nullable: true
          description: For in-thread sending
        signature_id:
          type: string
          format: uuid
          nullable: true
        contact_id:
          type: string
          format: uuid
          nullable: true
          description: When set, triggers UWG §7 marketing-consent pre-check

    SendEmailResponse:
      type: object
      properties:
        message:
          $ref: "#/components/schemas/EmailMessageInfo"

    SaveDraftRequest:
      type: object
      required: [account_id]
      properties:
        account_id:
          type: string
          format: uuid
        draft_id:
          type: string
          format: uuid
          nullable: true
          description: If set, updates an existing draft
        to:
          type: array
          items:
            type: string
            format: email
        cc:
          type: array
          items:
            type: string
            format: email
        bcc:
          type: array
          items:
            type: string
            format: email
        subject:
          type: string
        body_html:
          type: string
        body_text:
          type: string
        attachment_ids:
          type: array
          items:
            type: string
            format: uuid
        in_reply_to_message_id:
          type: string
          nullable: true

    SaveDraftResponse:
      type: object
      properties:
        message:
          $ref: "#/components/schemas/EmailMessageInfo"

    ReplyEmailRequest:
      type: object
      required: [account_id, original_message_id]
      properties:
        account_id:
          type: string
          format: uuid
        original_message_id:
          type: string
          format: uuid
        body_html:
          type: string
        body_text:
          type: string
        reply_all:
          type: boolean
          default: false
        attachment_ids:
          type: array
          items:
            type: string
            format: uuid

    ReplyEmailResponse:
      type: object
      properties:
        message:
          $ref: "#/components/schemas/EmailMessageInfo"

    ForwardEmailRequest:
      type: object
      required: [account_id, original_message_id, to]
      properties:
        account_id:
          type: string
          format: uuid
        original_message_id:
          type: string
          format: uuid
        to:
          type: array
          minItems: 1
          items:
            type: string
            format: email
        body_html:
          type: string
        body_text:
          type: string
        attachment_ids:
          type: array
          items:
            type: string
            format: uuid

    ForwardEmailResponse:
      type: object
      properties:
        message:
          $ref: "#/components/schemas/EmailMessageInfo"

    EmailRuleInfo:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        field:
          type: string
          enum: [from, subject]
        op:
          type: string
          enum: [contains]
        value:
          type: string
        action_type:
          type: string
          enum: [label, move]
        action_target:
          type: string
          format: uuid
          description: >
            Label id when action_type is "label", folder id when it is "move".

    ListEmailRulesResponse:
      type: object
      properties:
        rules:
          type: array
          items:
            $ref: "#/components/schemas/EmailRuleInfo"

    EmailRuleEnvelope:
      type: object
      properties:
        rule:
          $ref: "#/components/schemas/EmailRuleInfo"

    CreateEmailRuleRequest:
      type: object
      required: [name, field, value, action_type, action_target]
      properties:
        name:
          type: string
        field:
          type: string
          enum: [from, subject]
        op:
          type: string
          enum: [contains]
          default: contains
        value:
          type: string
        action_type:
          type: string
          enum: [label, move]
        action_target:
          type: string
          format: uuid

    UpdateEmailRuleRequest:
      type: object
      properties:
        name:
          type: string
          nullable: true
        field:
          type: string
          enum: [from, subject]
          nullable: true
        op:
          type: string
          enum: [contains]
          nullable: true
        value:
          type: string
          nullable: true
        action_type:
          type: string
          enum: [label, move]
          nullable: true
        action_target:
          type: string
          format: uuid
          nullable: true

    ApplyEmailRulesResponse:
      type: object
      properties:
        affected:
          type: integer
          description: Messages that actually changed, not the number of rule matches.
        scanned:
          type: integer
          description: Messages inspected, capped by the per-run scan limit.

    EmailLabelInfo:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        color:
          type: string
          description: Hex swatch colour, e.g. "#6B7280".

    ListEmailLabelsResponse:
      type: object
      properties:
        labels:
          type: array
          items:
            $ref: "#/components/schemas/EmailLabelInfo"

    EmailLabelEnvelope:
      type: object
      properties:
        label:
          $ref: "#/components/schemas/EmailLabelInfo"

    CreateEmailLabelRequest:
      type: object
      required: [name]
      properties:
        name:
          type: string
        color:
          type: string
          description: Hex swatch colour. Defaults to a neutral grey if omitted.

    UpdateEmailLabelRequest:
      type: object
      properties:
        name:
          type: string
          nullable: true
        color:
          type: string
          nullable: true

    AssignMessageLabelsRequest:
      type: object
      required: [label_ids]
      properties:
        label_ids:
          type: array
          items:
            type: string
            format: uuid
          description: >
            Full replacement set. Every id must already be a label of the
            tenant.

    EmailSignatureInfo:
      type: object
      properties:
        id:
          type: string
          format: uuid
        user_id:
          type: string
          format: uuid
        name:
          type: string
        html_content:
          type: string
        is_default:
          type: boolean
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time

    CreateSignatureRequest:
      type: object
      required: [user_id, name, html_content]
      properties:
        user_id:
          type: string
          format: uuid
        name:
          type: string
        html_content:
          type: string
        is_default:
          type: boolean

    CreateSignatureResponse:
      type: object
      properties:
        signature:
          $ref: "#/components/schemas/EmailSignatureInfo"

    GetSignatureResponse:
      type: object
      properties:
        signature:
          $ref: "#/components/schemas/EmailSignatureInfo"

    ListSignaturesResponse:
      type: object
      properties:
        signatures:
          type: array
          items:
            $ref: "#/components/schemas/EmailSignatureInfo"

    UpdateSignatureRequest:
      type: object
      properties:
        name:
          type: string
          nullable: true
        html_content:
          type: string
          nullable: true
        is_default:
          type: boolean
          nullable: true

    UpdateSignatureResponse:
      type: object
      properties:
        signature:
          $ref: "#/components/schemas/EmailSignatureInfo"

    SetDefaultSignatureRequest:
      type: object
      required: [user_id]
      properties:
        user_id:
          type: string
          format: uuid

    SetDefaultSignatureResponse:
      type: object
      properties:
        signature:
          $ref: "#/components/schemas/EmailSignatureInfo"

    EmailContactLinkInfo:
      type: object
      properties:
        id:
          type: string
          format: uuid
        message_id:
          type: string
          format: uuid
        contact_id:
          type: string
          format: uuid
        link_type:
          type: string
          enum: [auto, manual]
        created_at:
          type: string
          format: date-time

    GetEmailContactLinksResponse:
      type: object
      properties:
        links:
          type: array
          items:
            $ref: "#/components/schemas/EmailContactLinkInfo"

    LinkEmailToContactRequest:
      type: object
      required: [message_id, contact_id]
      properties:
        message_id:
          type: string
          format: uuid
        contact_id:
          type: string
          format: uuid
        link_type:
          type: string
          enum: [auto, manual]
          default: manual

    LinkEmailToContactResponse:
      type: object
      properties:
        link:
          $ref: "#/components/schemas/EmailContactLinkInfo"

    UnlinkEmailFromContactRequest:
      type: object
      required: [message_id, contact_id]
      properties:
        message_id:
          type: string
          format: uuid
        contact_id:
          type: string
          format: uuid

    GetContactEmailsResponse:
      type: object
      properties:
        messages:
          type: array
          items:
            $ref: "#/components/schemas/EmailMessageInfo"
        total:
          type: integer

    TriggerSyncRequest:
      type: object
      required: [account_id]
      properties:
        account_id:
          type: string
          format: uuid

    TriggerSyncResponse:
      type: object
      properties:
        status:
          type: string
          enum: [started, already_syncing]

    GetSyncStatusResponse:
      type: object
      properties:
        status:
          type: string
          enum: [idle, syncing, error]
        last_sync_at:
          type: string
          format: date-time
        error_message:
          type: string

    SetReadFlagRequest:
      type: object
      required: [message_id, is_read]
      properties:
        message_id:
          type: string
          format: uuid
        is_read:
          type: boolean

    UploadAttachmentResponse:
      type: object
      properties:
        id:
          type: string
          format: uuid
        minio_key:
          type: string
        size_bytes:
          type: integer
          format: int64

    GetAttachmentDownloadURLResponse:
      type: object
      properties:
        download_url:
          type: string
          format: uri
        filename:
          type: string
        content_type:
          type: string
        size_bytes:
          type: integer
          format: int64

    ImportContactsCSVRequest:
      type: object
      required: [file_content]
      properties:
        file_content:
          type: string
          format: byte
          description: Base64-encoded CSV file content
        field_mapping:
          type: object
          additionalProperties:
            type: string
          description: Map of CSV column names to CRM field names
        visibility:
          type: string
          enum: [shared, personal]
        merge_by_email:
          type: boolean

    ImportContactsVCardRequest:
      type: object
      required: [file_content]
      properties:
        file_content:
          type: string
          format: byte
          description: Base64-encoded vCard file content
        visibility:
          type: string
          enum: [shared, personal]
        merge_by_email:
          type: boolean

    EmailImportError:
      type: object
      properties:
        row:
          type: integer
        field:
          type: string
        error:
          type: string

    EmailImportContactsResponse:
      type: object
      properties:
        imported_count:
          type: integer
        merged_count:
          type: integer
        skipped_count:
          type: integer
        errors:
          type: array
          items:
            $ref: "#/components/schemas/EmailImportError"

    EmailExportContactsRequest:
      type: object
      properties:
        contact_ids:
          type: array
          items:
            type: string
            format: uuid
        fields:
          type: array
          items:
            type: string
          description: Restrict exported fields; omit for all fields

    EmailExportContactsResponse:
      type: object
      properties:
        file_content:
          type: string
          format: byte
          description: Base64-encoded file content (CSV or vCard depending on endpoint)
        filename:
          type: string

    # --- Dialer Schemas ---
    # Wire shape: response.Proto / protojson (UseProtoNames → snake_case,
    # UseEnumNumbers → integer enums, EmitUnpopulated=false → optional fields omitted).
    # Timestamps are RFC3339 strings (protojson well-known type handling).
    # Existing schemas (SupervisorOverview, SupervisorAgent, RecentCall,
    # SupervisorTotals, ContactCall) are already defined above — referenced via $ref.

    DialerCampaign:
      type: object
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        name:
          type: string
        description:
          type: string
          description: Present only when set
        status:
          type: integer
          description: "CampaignStatus integer (UseEnumNumbers): 0=UNSPECIFIED 1=DRAFT 2=ACTIVE 3=PAUSED 4=COMPLETED 5=ARCHIVED"
        mode:
          type: integer
          description: "CampaignMode integer: 0=UNSPECIFIED 1=PREVIEW 2=POWER 3=PREDICTIVE"
        settings:
          type: string
          description: "JSON-encoded campaign settings (wrap-up timeout, retry rules). Omitted when unset."
        created_by:
          type: string
          format: uuid
        assigned_agent_ids:
          type: array
          items: { type: string, format: uuid }
        contact_count:
          type: integer
        completed_count:
          type: integer
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        started_at:
          type: string
          format: date-time
          description: Omitted when not yet started
        completed_at:
          type: string
          format: date-time
          description: Omitted when not yet completed

    DialerCampaignContact:
      type: object
      properties:
        id:
          type: string
          format: uuid
        campaign_id:
          type: string
          format: uuid
        contact_id:
          type: string
          format: uuid
        position:
          type: integer
          description: Queue position within the campaign
        status:
          type: integer
          description: "CampaignContactStatus integer: 0=UNSPECIFIED 1=PENDING 2=IN_PROGRESS 3=COMPLETED 4=SKIPPED 5=CALLBACK"
        outcome_id:
          type: string
          format: uuid
          description: Omitted when no outcome logged yet
        notes:
          type: string
          description: Omitted when empty
        callback_at:
          type: string
          format: date-time
          description: Omitted when no callback scheduled
        last_called_at:
          type: string
          format: date-time
          description: Omitted when never called
        call_count:
          type: integer
        contact_name:
          type: string
          description: Denormalized from contact record
        contact_phone:
          type: string
        contact_company:
          type: string
          description: Omitted when contact has no company

    DialerCallSession:
      type: object
      properties:
        id:
          type: string
          format: uuid
        campaign_contact_id:
          type: string
          format: uuid
        call_session_id:
          type: string
          format: uuid
          description: "FK to LiveKit video call_sessions. Present only when call was routed via LiveKit."
        agent_id:
          type: string
          format: uuid
        outcome_id:
          type: string
          format: uuid
          description: Omitted until outcome is logged
        duration_seconds:
          type: integer
          description: Omitted until call completes
        notes:
          type: string
          description: Omitted when empty
        next_action:
          type: string
          description: Free-text next-action note; omitted when empty
        appointment_id:
          type: string
          format: uuid
          description: Omitted when no appointment was created
        created_at:
          type: string
          format: date-time
        wrap_up_completed_at:
          type: string
          format: date-time
          description: Omitted until wrap-up is completed

    DialerAgentStatus:
      type: object
      properties:
        user_id:
          type: string
          format: uuid
        status:
          type: integer
          description: "AgentDialerStatus integer: 0=UNSPECIFIED 1=AVAILABLE 2=ON_CALL 3=WRAP_UP 4=BREAK 5=OFFLINE"
        campaign_id:
          type: string
          format: uuid
          description: Present only while assigned to a campaign
        since:
          type: string
          format: date-time
          description: Timestamp when the agent entered the current status
        first_name:
          type: string
          description: Denormalized from user record
        last_name:
          type: string

    DialerCallOutcome:
      type: object
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        label:
          type: string
        color:
          type: string
          description: "Hex color string, e.g. #4CAF50"
        is_positive:
          type: boolean
        is_callback:
          type: boolean
        is_appointment:
          type: boolean
        sort_order:
          type: integer
        is_active:
          type: boolean

    DialerOutcomeCount:
      type: object
      description: Per-outcome call count aggregation, used in dashboard breakdowns
      properties:
        outcome_id:
          type: string
          format: uuid
        outcome_label:
          type: string
        outcome_color:
          type: string
        count:
          type: integer

    DialerCampaignDashboard:
      type: object
      properties:
        campaign_id:
          type: string
          format: uuid
        total_contacts:
          type: integer
        completed_contacts:
          type: integer
        pending_contacts:
          type: integer
        skipped_contacts:
          type: integer
        callback_contacts:
          type: integer
        total_calls:
          type: integer
        positive_calls:
          type: integer
        average_duration_seconds:
          type: number
          format: float
        calls_per_hour:
          type: number
          format: float
        outcome_breakdown:
          type: array
          items: { $ref: "#/components/schemas/DialerOutcomeCount" }

    DialerAgentDashboard:
      type: object
      properties:
        agent_id:
          type: string
          format: uuid
        total_calls_today:
          type: integer
        average_duration_seconds:
          type: number
          format: float
        calls_per_hour:
          type: number
          format: float
        outcome_breakdown:
          type: array
          items: { $ref: "#/components/schemas/DialerOutcomeCount" }
        active_campaign_id:
          type: string
          format: uuid
          description: Omitted when agent has no active campaign
        active_campaign_name:
          type: string
          description: Omitted when agent has no active campaign

    # --- Inventar Schemas ---

    InventarItem:
      type: object
      description: An inventory item tracked in the Inventar module.
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        name:
          type: string
        sku:
          type: string
        barcode:
          type: string
          nullable: true
        quantity:
          type: integer
          format: int64
          description: "Current stock quantity"
        min_quantity:
          type: integer
          format: int64
          description: "Minimum stock threshold; below this triggers a warning"
        unit:
          type: string
          description: "Unit of measure (e.g. Stk, kg, m)"
        location:
          type: string
          nullable: true
          description: "Free-text location label"
        location_id:
          type: string
          format: uuid
          nullable: true
          description: "Reference to an InventarLocation"
        category:
          type: string
          nullable: true
        price:
          type: number
          format: double
          nullable: true
        currency:
          type: string
          nullable: true
          description: "ISO 4217 currency code (e.g. EUR)"
        batch_number:
          type: string
          nullable: true
        serial_numbers:
          type: array
          items: { type: string }
        linked_purchase_order:
          type: string
          nullable: true
        created_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        updated_at:
          $ref: "#/components/schemas/ProtoTimestamp"

    InventarMovement:
      type: object
      description: A stock movement record (in, out, adjustment, or transfer).
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        item_id:
          type: string
          format: uuid
        movement_type:
          type: string
          enum: [in, out, adjustment, transfer]
          description: "Type of movement: in=stock receipt, out=stock issue, adjustment=manual correction, transfer=inter-item transfer"
        quantity:
          type: integer
          format: int64
        performed_by:
          type: string
          format: uuid
          nullable: true
        reason:
          type: string
        reference:
          type: string
          nullable: true
        location_from:
          type: string
          nullable: true
        location_to:
          type: string
          nullable: true
        notes:
          type: string
          nullable: true
        created_at:
          $ref: "#/components/schemas/ProtoTimestamp"

    InventarWarning:
      type: object
      description: A low-stock warning for an inventory item.
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        item_id:
          type: string
          format: uuid
        threshold:
          type: integer
          format: int64
          description: "Quantity threshold that triggered the warning"
        current_quantity:
          type: integer
          format: int64
          description: "Quantity at the time the warning was created"
        status:
          type: string
          enum: [active, acknowledged, resolved]
          description: "Warning lifecycle status"
        acknowledged_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        acknowledged_by:
          type: string
          format: uuid
          nullable: true
        created_at:
          $ref: "#/components/schemas/ProtoTimestamp"

    InventarLocation:
      type: object
      description: A physical or logical storage location in the Inventar module.
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        name:
          type: string
        address:
          type: string
        type:
          type: string
          enum: [warehouse, store, vehicle]
          description: "Location type: warehouse, store, or vehicle"
        created_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        updated_at:
          $ref: "#/components/schemas/ProtoTimestamp"

    InventarInventurSession:
      type: object
      description: An Inventur (physical stocktake) session.
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        name:
          type: string
        date:
          $ref: "#/components/schemas/ProtoTimestamp"
          description: "Planned date of the stocktake"
        status:
          type: string
          enum: [open, counting, review, completed]
          description: "Session lifecycle status"
        location_id:
          type: string
          format: uuid
          nullable: true
        created_by:
          type: string
          format: uuid
          nullable: true
        counts:
          type: array
          description: "Count entries for this session"
          items: { $ref: "#/components/schemas/InventarInventurCount" }
        created_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        updated_at:
          $ref: "#/components/schemas/ProtoTimestamp"

    InventarInventurCount:
      type: object
      description: A single item count entry within an Inventur session.
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        session_id:
          type: string
          format: uuid
        item_id:
          type: string
          format: uuid
        expected:
          type: integer
          format: int64
          description: "Expected quantity (from current stock record)"
        counted:
          type: integer
          format: int64
          nullable: true
          description: "Actual counted quantity; null until counted"
        counted_at:
          $ref: "#/components/schemas/ProtoTimestamp"

    # --- Vermietung Schemas ---

    VermietungRentalObject:
      type: object
      description: A physical object available for rental.
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        name:
          type: string
        description:
          type: string
        category:
          type: string
        daily_rate:
          type: number
          format: double
        deposit:
          type: number
          format: double
        location:
          type: string
        notes:
          type: string
        active:
          type: boolean
        created_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        updated_at:
          $ref: "#/components/schemas/ProtoTimestamp"

    VermietungRental:
      type: object
      description: A rental booking linking a renter to a rental object for a date range.
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        object_id:
          type: string
          format: uuid
        contact_id:
          type: string
          format: uuid
        renter_name:
          type: string
        start_date:
          $ref: "#/components/schemas/ProtoTimestamp"
        end_date:
          $ref: "#/components/schemas/ProtoTimestamp"
        status:
          type: string
          description: "reserved | active | completed | cancelled"
          enum: [reserved, active, completed, cancelled]
        total_price:
          type: number
          format: double
        deposit_paid:
          type: boolean
        notes:
          type: string
        signature_data:
          type: string
          description: "data:image/png;base64,... or data:image/svg+xml;base64,..."
        signed_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        signed_by:
          type: string
        created_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        updated_at:
          $ref: "#/components/schemas/ProtoTimestamp"

    VermietungRentalInspection:
      type: object
      description: A handover or return inspection record for a rental.
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        rental_id:
          type: string
          format: uuid
        kind:
          type: string
          description: "handover | return"
          enum: [handover, return]
        notes:
          type: string
        photo_urls:
          type: array
          items:
            type: string
            format: uri
        performed_by:
          type: string
          format: uuid
        created_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        updated_at:
          $ref: "#/components/schemas/ProtoTimestamp"

    VermietungCalendarEntry:
      type: object
      description: Aggregated calendar view — all rentals for one object within the requested month.
      properties:
        object_id:
          type: string
          format: uuid
        object_name:
          type: string
        rentals:
          type: array
          items: { $ref: "#/components/schemas/VermietungRental" }

    RapporteReport:
      type: object
      description: >-
        A work report (Rapporte) in the field-report module. Status follows the
        state machine: draft → submitted → approved | rejected.
        created_at/updated_at/reviewed_at/signed_at are Proto wire timestamps
        ({seconds, nanos}) because the service uses google.protobuf.Timestamp.
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        title:
          type: string
        description:
          type: string
        status:
          type: string
          enum: [draft, submitted, approved, rejected]
          description: "Report lifecycle state"
        author_id:
          type: string
          format: uuid
        reviewer_id:
          type: string
          format: uuid
          nullable: true
        reviewed_at:
          allOf:
            - $ref: "#/components/schemas/ProtoTimestamp"
          nullable: true
        review_note:
          type: string
        lat:
          type: number
          format: double
          nullable: true
        lon:
          type: number
          format: double
          nullable: true
        report_date:
          type: string
          description: "YYYY-MM-DD"
        signature_data:
          type: string
          description: "Data URI (data:image/png;base64,… or data:image/svg+xml;base64,…)"
        signed_at:
          allOf:
            - $ref: "#/components/schemas/ProtoTimestamp"
          nullable: true
        signed_by:
          type: string
        weather:
          type: string
        temperature:
          type: number
          format: double
        work_start:
          type: string
          description: "HH:MM"
        work_end:
          type: string
          description: "HH:MM"
        break_minutes:
          type: integer
          format: int32
        project_name:
          type: string
        workers:
          type: array
          items: { $ref: "#/components/schemas/RapporteWorker" }
        created_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        updated_at:
          $ref: "#/components/schemas/ProtoTimestamp"

    RapporteWorker:
      type: object
      description: A worker entry attached to a work report.
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        report_id:
          type: string
          format: uuid
        name:
          type: string
        role:
          type: string
        hours:
          type: number
          format: double
        created_at:
          type: string
          description: "ISO-8601 string as returned by the service (string field in proto)"

    RapporteLine:
      type: object
      description: >-
        A line item within a work report. created_at/updated_at are Proto wire
        timestamps ({seconds, nanos}).
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        report_id:
          type: string
          format: uuid
        position:
          type: integer
          format: int32
          description: "Display order within the report"
        description:
          type: string
        quantity:
          type: number
          format: double
        unit:
          type: string
        notes:
          type: string
        category:
          type: string
        article:
          type: string
        created_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        updated_at:
          $ref: "#/components/schemas/ProtoTimestamp"

    RapporteAttachment:
      type: object
      description: >-
        A file attachment linked to a work report (and optionally to a specific
        line). created_at is a Proto wire timestamp ({seconds, nanos}).
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        report_id:
          type: string
          format: uuid
        line_id:
          type: string
          format: uuid
          nullable: true
        filename:
          type: string
        content_type:
          type: string
        size_bytes:
          type: integer
          format: int64
        object_key:
          type: string
          description: "Object storage key"
        presigned_url:
          type: string
          description: "Short-lived presigned download URL generated by the service"
        uploaded_by:
          type: string
          format: uuid
        created_at:
          $ref: "#/components/schemas/ProtoTimestamp"

    RapporteMeasurement:
      type: object
      description: >-
        A measurement sheet that can be linked to a work report. Positions are
        embedded. created_at/updated_at are plain strings (not proto Timestamps)
        because the Measurement proto uses string fields for those.
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        report_id:
          type: string
          format: uuid
          nullable: true
        title:
          type: string
        location:
          type: string
        measured_by:
          type: string
        measured_at:
          type: string
          description: "Free-text date/time string as provided by the caller"
        notes:
          type: string
        positions:
          type: array
          items: { $ref: "#/components/schemas/RapporteMeasurementPosition" }
        created_at:
          type: string
          description: "String timestamp (not a Proto wire timestamp)"
        updated_at:
          type: string
          description: "String timestamp (not a Proto wire timestamp)"

    RapporteMeasurementPosition:
      type: object
      description: >-
        A single position within a measurement sheet. created_at is a plain
        string (not a Proto wire timestamp).
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        measurement_id:
          type: string
          format: uuid
        position_number:
          type: integer
          format: int32
        description:
          type: string
        unit:
          type: string
        quantity:
          type: number
          format: double
        unit_price:
          type: number
          format: double
        created_at:
          type: string
          description: "String timestamp (not a Proto wire timestamp)"

    RapporteTemplate:
      type: object
      description: >-
        A reusable report template. created_at/updated_at are plain strings (not
        Proto wire timestamps) because the ReportTemplate proto uses string fields.
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        name:
          type: string
        description:
          type: string
        category:
          type: string
        default_lines_json:
          type: string
          description: "JSON string describing the default line items to pre-populate"
        is_active:
          type: boolean
        created_at:
          type: string
          description: "String timestamp (not a Proto wire timestamp)"
        updated_at:
          type: string
          description: "String timestamp (not a Proto wire timestamp)"

    RapporteReportStats:
      type: object
      description: Aggregate report counts for the tenant. Returned flat (no envelope wrapper).
      properties:
        total_reports:
          type: integer
          format: int32
        draft_count:
          type: integer
          format: int32
        submitted_count:
          type: integer
          format: int32
        approved_count:
          type: integer
          format: int32
        rejected_count:
          type: integer
          format: int32

    EinkaufCatalogItem:
      type: object
      description: A supplier catalog item in the Einkauf module.
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        supplier_id:
          type: string
          format: uuid
        name:
          type: string
        sku:
          type: string
        category:
          type: string
        price:
          type: string
          description: "Agreed price as numeric string (e.g. \"12.50\")"
        currency:
          type: string
          description: "ISO 4217 currency code (e.g. EUR)"
        unit:
          type: string
          description: "Unit of measure (e.g. Stk, kg, m)"
        available:
          type: boolean
        min_order_qty:
          type: string
          description: "Minimum order quantity as numeric string"
        created_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        updated_at:
          $ref: "#/components/schemas/ProtoTimestamp"

    EinkaufSupplierRating:
      type: object
      description: >-
        A quality/delivery/price rating submitted for a supplier.
        `rated_by` is the user ID of the submitter (nullable if recorded
        without an authenticated user context).
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        supplier_id:
          type: string
          format: uuid
        category:
          type: string
          enum: [quality, delivery, price]
        rating:
          type: integer
          minimum: 1
          maximum: 5
          description: "Score 1 (worst) – 5 (best)"
        comment:
          type: string
        rated_by:
          type: string
          format: uuid
          nullable: true
          description: "User UUID of the rater"
        rated_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        created_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        updated_at:
          $ref: "#/components/schemas/ProtoTimestamp"

    EinkaufFrameworkContract:
      type: object
      description: >-
        A framework (blanket) contract with a supplier that defines budget,
        validity period, and agreed line items. The `items` array is populated
        only on single-entity GET (GetFrameworkContract); list responses return
        an empty array.
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        supplier_id:
          type: string
          format: uuid
        title:
          type: string
        contract_nr:
          type: string
          description: "Human-readable contract reference number"
        start_date:
          allOf:
            - $ref: "#/components/schemas/ProtoTimestamp"
          nullable: true
          description: "Contract start date as ProtoTimestamp (null if not set)"
        end_date:
          allOf:
            - $ref: "#/components/schemas/ProtoTimestamp"
          nullable: true
          description: "Contract end date as ProtoTimestamp (null if not set)"
        total_value:
          type: string
          description: "Budgeted contract value as numeric string"
        used_value:
          type: string
          description: "Cumulative sum of all call-offs as numeric string"
        currency:
          type: string
          description: "ISO 4217 currency code (e.g. EUR)"
        status:
          type: string
          enum: [draft, active, expired]
        created_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        updated_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        items:
          type: array
          description: "Line items — populated only on single-entity GET"
          items:
            $ref: "#/components/schemas/EinkaufContractItem"

    EinkaufContractItem:
      type: object
      description: >-
        A line item within a framework contract, defining an agreed product/
        service with its price, unit, and agreed quantity. `called_qty` tracks
        the cumulative quantity already called off.
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        contract_id:
          type: string
          format: uuid
        name:
          type: string
        unit_price:
          type: string
          description: "Agreed unit price as numeric string"
        unit:
          type: string
          description: "Unit of measure (e.g. Stk, kg, m)"
        agreed_qty:
          type: string
          description: "Agreed total quantity as numeric string"
        called_qty:
          type: string
          description: "Sum of quantity called off so far as numeric string"
        created_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        updated_at:
          $ref: "#/components/schemas/ProtoTimestamp"

    EinkaufContractCall:
      type: object
      description: >-
        A single call-off (Abruf) against a framework contract, recording the
        amount drawn and optionally linking to the resulting purchase order.
        `called_at` is the timestamp of the call-off event.
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        contract_id:
          type: string
          format: uuid
        po_id:
          type: string
          format: uuid
          nullable: true
          description: "UUID of the linked purchase order, if any"
        amount:
          type: string
          description: "Called-off value as numeric string"
        currency:
          type: string
          description: "ISO 4217 currency code"
        called_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        notes:
          type: string
        created_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        updated_at:
          $ref: "#/components/schemas/ProtoTimestamp"

    SchichtenAssignment:
      type: object
      description: A shift assignment linking an employee to a shift (proto SchichtAssignment).
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        shift_id:
          type: string
          format: uuid
        employee_id:
          type: string
          format: uuid
        assigned_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        assigned_by:
          type: string
          format: uuid
          nullable: true

    SchichtenComplianceReport:
      type: object
      description: Result of an ArbZG compliance check for a planned shift.
      properties:
        compliant:
          type: boolean
          description: "true if the planned shift is ArbZG-compliant"
        violation_reason:
          type: string
          description: "Human-readable reason; empty string when compliant"

    SchichtenShift:
      type: object
      description: A planned or published work shift (proto Shift).
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        title:
          type: string
        description:
          type: string
        start_time:
          $ref: "#/components/schemas/ProtoTimestamp"
        end_time:
          $ref: "#/components/schemas/ProtoTimestamp"
        status:
          type: string
          enum: [draft, published]
          description: "Lifecycle status of the shift"
        location:
          type: string
          nullable: true
        created_by:
          type: string
          format: uuid
          nullable: true
        created_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        updated_at:
          $ref: "#/components/schemas/ProtoTimestamp"

    SchichtenStats:
      type: object
      description: Aggregate shift planning statistics for the current tenant (proto ShiftStatsResponse).
      properties:
        total_shifts:
          type: integer
        published_shifts:
          type: integer
        draft_shifts:
          type: integer
        total_assignments:
          type: integer
        unique_employees:
          type: integer

    SchichtenSwapRequest:
      type: object
      description: A shift-swap request between two employees (proto SwapRequest).
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        assignment_id:
          type: string
          format: uuid
        shift_id:
          type: string
          format: uuid
        requested_by_employee_id:
          type: string
          format: uuid
        swap_with_employee_id:
          type: string
          format: uuid
        status:
          type: string
          enum: [pending, approved, rejected]
        reason:
          type: string
        idempotency_key:
          type: string
        created_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        updated_at:
          $ref: "#/components/schemas/ProtoTimestamp"

    SchichtenTemplate:
      type: object
      description: A reusable shift template (proto ShiftTemplate).
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        name:
          type: string
        description:
          type: string
        day_of_week:
          type: integer
          minimum: 0
          maximum: 6
          description: "0 = Sunday, 6 = Saturday"
        start_hour:
          type: integer
          minimum: 0
          maximum: 23
        start_minute:
          type: integer
          minimum: 0
          maximum: 59
        duration_minutes:
          type: integer
          minimum: 0
          exclusiveMinimum: true
          description: "Duration in minutes; must be > 0"
        location:
          type: string
          nullable: true
        created_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        updated_at:
          $ref: "#/components/schemas/ProtoTimestamp"


    FuhrparkDamage:
      type: object
      description: A vehicle damage report (proto Damage).
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        vehicle_id:
          type: string
          format: uuid
        description:
          type: string
        severity:
          type: string
          enum: [minor, moderate, major, totalled]
        status:
          type: string
          enum: [reported, in_repair, resolved]
        reported_by:
          type: string
          format: uuid
          nullable: true
        resolved_by:
          type: string
          format: uuid
          nullable: true
        resolved_at:
          $ref: "#/components/schemas/ProtoTimestamp"
          nullable: true
        photo_keys:
          type: array
          items:
            type: string
          description: "MinIO / S3 object keys of damage photos"
        cost_cents:
          type: integer
          format: int64
          nullable: true
        notes:
          type: string
          nullable: true
        created_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        updated_at:
          $ref: "#/components/schemas/ProtoTimestamp"

    FuhrparkDocument:
      type: object
      description: A document attached to a vehicle (proto VehicleDocument).
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        vehicle_id:
          type: string
          format: uuid
        doc_type:
          type: string
          enum: [registration, insurance, tuev, other]
        name:
          type: string
        object_key:
          type: string
          description: "MinIO / S3 object key"
        upload_date:
          type: string
          description: "ISO date YYYY-MM-DD"
        expiry_date:
          type: string
          nullable: true
          description: "ISO date YYYY-MM-DD; absent when not applicable"
        created_at:
          type: string
          description: "ISO-8601 string (proto string field, not Timestamp)"
        updated_at:
          type: string

    FuhrparkDriverLicense:
      type: object
      description: A driving-licence compliance check for a driver (proto DriverLicense). Rows are append-only history; the most recent row per driver_id is the current status.
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        driver_id:
          type: string
          format: uuid
        license_classes:
          type: array
          items: { type: string }
        expiry_date:
          type: string
          nullable: true
          description: "ISO date YYYY-MM-DD; absent when the licence has no expiry"
        checked_at:
          type: string
          description: "ISO date YYYY-MM-DD"
        next_check_due_date:
          type: string
          description: "ISO date YYYY-MM-DD"
        notes:
          type: string
        created_at:
          type: string
          description: "ISO-8601 string (proto string field, not Timestamp)"
        updated_at:
          type: string

    FuhrparkFuelLog:
      type: object
      description: A fuel refuelling log entry (proto FuelLog).
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        vehicle_id:
          type: string
          format: uuid
        date:
          type: string
          description: "ISO date YYYY-MM-DD"
        liters:
          type: number
          format: double
        cost_cents:
          type: integer
          format: int64
        mileage_km:
          type: integer
          format: int64
        fuel_type:
          type: string
          enum: [diesel, petrol, electric, hybrid, other]
        notes:
          type: string
        created_at:
          type: string
          description: "ISO-8601 string (proto string field, not Timestamp)"
        updated_at:
          type: string

    FuhrparkGpsPosition:
      type: object
      description: A raw GPS position sample (proto GpsPosition).
      properties:
        id:
          type: string
          format: uuid
        vehicle_id:
          type: string
          format: uuid
        lat:
          type: number
          format: double
        lng:
          type: number
          format: double
        speed_kmh:
          type: number
          format: double
        recorded_at:
          type: string
          description: "RFC-3339 string (proto string field)"

    FuhrparkRoute:
      type: object
      description: A reconstructed vehicle route from GPS data (proto VehicleRoute).
      properties:
        vehicle_id:
          type: string
          format: uuid
        vehicle_name:
          type: string
        date:
          type: string
          description: "ISO date YYYY-MM-DD"
        positions:
          type: array
          items:
            $ref: "#/components/schemas/FuhrparkGpsPosition"
        daily_km:
          type: number
          format: double
        status:
          type: string
          enum: [driving, parked, unknown]
        driver:
          type: string

    FuhrparkService:
      type: object
      description: A vehicle service/maintenance record (proto VehicleService).
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        vehicle_id:
          type: string
          format: uuid
        service_type:
          type: string
        description:
          type: string
          nullable: true
        scheduled_at:
          type: string
          description: "ISO date YYYY-MM-DD"
        completed_at:
          $ref: "#/components/schemas/ProtoTimestamp"
          nullable: true
        cost_cents:
          type: integer
          format: int64
          nullable: true
        workshop:
          type: string
          nullable: true
        mileage_km:
          type: integer
          format: int64
          nullable: true
        status:
          type: string
          enum: [scheduled, in_progress, completed, cancelled]
        notes:
          type: string
          nullable: true
        created_by:
          type: string
          format: uuid
          nullable: true
        created_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        updated_at:
          $ref: "#/components/schemas/ProtoTimestamp"

    FuhrparkTripLog:
      type: object
      description: A vehicle trip (Fahrtenbuch) entry (proto TripLog).
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        vehicle_id:
          type: string
          format: uuid
        date:
          type: string
          description: "ISO date YYYY-MM-DD"
        start_location:
          type: string
        end_location:
          type: string
        purpose:
          type: string
        start_km:
          type: integer
          format: int64
        end_km:
          type: integer
          format: int64
        km:
          type: integer
          format: int64
          description: "Computed distance: end_km − start_km"
        is_private:
          type: boolean
        driver_name:
          type: string
        notes:
          type: string
        created_at:
          type: string
          description: "ISO-8601 string (proto string field, not Timestamp)"
        updated_at:
          type: string

    FuhrparkVehicle:
      type: object
      description: A fleet vehicle (proto Vehicle).
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        license_plate:
          type: string
        make:
          type: string
        model:
          type: string
        year:
          type: integer
        vin:
          type: string
          nullable: true
        color:
          type: string
          nullable: true
        fuel_type:
          type: string
          enum: [petrol, diesel, electric, hybrid, other]
        status:
          type: string
          enum: [active, in_service, inactive, decommissioned]
        mileage_km:
          type: integer
          format: int64
        tuev_due_date:
          type: string
          nullable: true
          description: "ISO date YYYY-MM-DD; absent if not set"
        assigned_driver_id:
          type: string
          format: uuid
          nullable: true
          description: "Driver assignment (stub Sprint 3)"
        notes:
          type: string
          nullable: true
        created_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        updated_at:
          $ref: "#/components/schemas/ProtoTimestamp"

    # === openapi-drift sweep: berichte ===
    # -----------------------------------------------------------------------
    # Berichte (custom report builder)
    # All served via response.JSON() on the raw gRPC struct (encoding/json,
    # not protojson): timestamps are google.protobuf.Timestamp wire shape
    # ({seconds,nanos}), not RFC3339 strings. bytes fields (query_config,
    # params, payload) serialize as base64 strings, not nested JSON objects.
    # -----------------------------------------------------------------------

    BerichtDefinition:
      type: object
      description: A saved custom report definition (query spec + metadata).
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        name:
          type: string
        description:
          type: string
        module:
          type: string
          description: "finanzen|crm|helpdesk|inventar|produktion|cross"
        kind:
          type: string
          description: "system|custom"
        query_config:
          type: string
          format: byte
          description: Raw JSONB aggregation spec (base64-encoded bytes on the wire)
        default_format:
          type: string
          description: "pdf|csv|xlsx"
        created_by:
          type: string
          format: uuid
        is_published:
          type: boolean
        created_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        updated_at:
          $ref: "#/components/schemas/ProtoTimestamp"

    BerichtShareToken:
      type: object
      description: >
        An external, unauthenticated read link for one report document.
        Mirrors ReportShareToken in berichte-types.ts. The bcrypt password hash
        has no field here at all — it never leaves the service; has_password is
        all a client learns.
      properties:
        id:
          type: string
          format: uuid
        document_id:
          type: string
          format: uuid
        token:
          type: string
          description: >
            The link secret in clear (43-char base64url, 32 bytes of
            crypto/rand). Returned so the UI can render a copyable URL; it is
            the entire credential for the public read route.
        expires_at:
          type: string
          format: date-time
          nullable: true
          description: null = never expires
        has_password:
          type: boolean
        view_count:
          type: integer
        created_at:
          type: string
          format: date-time

    BerichtPublicDocument:
      type: object
      description: >
        The reduced report document an anonymous visitor sees through a share
        link. Deliberately not BerichtDocument: tenant_id, created_by and the
        editing status are internal facts withheld from unauthenticated
        callers.
      properties:
        id:
          type: string
          format: uuid
        title:
          type: string
        description:
          type: string
        module:
          type: string
          description: "finanzen|crm|helpdesk|inventar|produktion|cross"
        rows:
          type: array
          description: Block tree (rows -> columns -> blocks), opaque JSONB.
          items:
            type: object
            additionalProperties: true
        settings:
          type: object
          additionalProperties: true
        updated_at:
          type: string
          format: date-time
        released_at:
          type: string
          format: date-time
          nullable: true

    BerichtDocument:
      type: object
      description: >
        A multi-page report authoring document ("Schicht 4"): cover page,
        headings, rich text and embedded chart/table/KPI blocks. Distinct from
        BerichtDefinition, which is a single data widget. Unlike the other
        berichte endpoints, the document handlers serialize a hand-built wire
        struct (encoding/json), so timestamps are RFC3339 strings and
        rows/settings are nested JSON, not base64.
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        title:
          type: string
        description:
          type: string
        module:
          type: string
          description: "finanzen|crm|helpdesk|inventar|produktion|cross"
        status:
          type: string
          description: "draft|final|released|archived"
        rows:
          type: array
          description: >
            Block tree (rows -> columns -> blocks). Stored and returned as
            opaque JSONB; the block structure is owned by the editor and its
            keys are camelCase.
          items:
            type: object
            additionalProperties: true
        settings:
          type: object
          additionalProperties: true
          description: Document-level page setup (header/footer/page numbers, palette).
        template_id:
          type: string
          nullable: true
          description: Frontend-side starter template key; not a foreign key.
        created_by:
          type: string
          format: uuid
          nullable: true
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        released_at:
          type: string
          format: date-time
          nullable: true
          description: Set when the status first transitions to "released".

    BerichtTemplate:
      type: object
      description: >
        A static starter block structure for "Neuer Bericht aus Vorlage".
        Not tenant data — served from a server-side constant, identical for
        every tenant. Same wire rationale as BerichtDocument: served via
        response.JSON, not response.Proto.
      properties:
        id:
          type: string
        title:
          type: string
        description:
          type: string
        module:
          type: string
          description: "finanzen|crm|helpdesk|inventar|produktion|cross"
        icon:
          type: string
          nullable: true
          description: Lucide icon name resolved in the picker UI.
        rows:
          type: array
          description: Block tree (rows -> columns -> blocks), same shape as BerichtDocument.rows.
          items:
            type: object
            additionalProperties: true
        settings:
          type: object
          additionalProperties: true

    BerichtSchedule:
      type: object
      description: A recurring delivery schedule for a report definition.
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        definition_id:
          type: string
          format: uuid
        name:
          type: string
        cron_expression:
          type: string
        recipients:
          type: array
          items:
            type: string
            format: email
        format:
          type: string
          description: "pdf|csv|xlsx"
        params:
          type: string
          format: byte
          description: Raw JSONB report params (base64-encoded bytes on the wire)
        active:
          type: boolean
        last_run_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        last_run_status:
          type: string
          description: "success|failed|skipped"
        last_run_error:
          type: string
        created_by:
          type: string
          format: uuid
        created_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        updated_at:
          $ref: "#/components/schemas/ProtoTimestamp"

    BerichtRun:
      type: object
      description: >-
        A single execution record of a report definition (manual, scheduled,
        or API-triggered).
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        definition_id:
          type: string
          format: uuid
        schedule_id:
          type: string
          format: uuid
        trigger:
          type: string
          description: "manual|scheduled|api"
        params:
          type: string
          format: byte
          description: Raw JSONB params supplied for this run (base64-encoded bytes on the wire)
        duration_ms:
          type: integer
          format: int32
        row_count:
          type: integer
          format: int32
        status:
          type: string
          description: "success|failed"
        error:
          type: string
        started_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        completed_at:
          $ref: "#/components/schemas/ProtoTimestamp"

    BerichtReportResult:
      type: object
      description: >-
        The materialized output of a report run. payload is an opaque JSON
        document produced by the report engine ({columns, rows, series,
        totals, meta}); shape varies per report definition, so it travels as
        base64-encoded bytes rather than a typed schema.
      properties:
        payload:
          type: string
          format: byte
          description: "JSON-encoded {columns, rows, series, totals, meta} (base64-encoded bytes on the wire)"
        row_count:
          type: integer
          format: int32
        generated_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        from_cache:
          type: boolean

    BerichtKPI:
      type: object
      description: A single dashboard KPI tile.
      properties:
        id:
          type: string
        label:
          type: string
        value:
          type: string
          description: Rendered value (string for display flexibility)
        unit:
          type: string
        change_percent:
          type: number
          format: double
        module_id:
          type: string

    # === openapi-drift sweep: documents ===
    DocumentFolder:
      type: object
      description: >-
        Field names match document.v1.DocumentFolder. GET .../folders (list) serializes
        via protojson (created_at/updated_at as RFC3339 strings); GET/POST/PUT on a single
        folder serialize via Go encoding/json over the raw proto struct (created_at/updated_at
        as ProtoTimestamp {seconds,nanos}) — see the differing response schemas per operation.
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        parent_id:
          type: string
          format: uuid
          description: Empty string if this is a root folder
        space_type:
          type: integer
          description: "FolderSpaceType integer (UseEnumNumbers): 0=UNSPECIFIED 1=PERSONAL 2=TEAM 3=PROJECT"
        space_id:
          type: string
          format: uuid
          description: User ID, team ID, or project ID depending on space_type
        is_system:
          type: boolean
        icon:
          type: string
        created_by:
          type: string
          format: uuid
        created_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        updated_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        file_count:
          type: integer
          description: Computed, not stored

    DocumentFile:
      type: object
      description: >-
        Always serialized via Go encoding/json over the raw proto struct (never via
        protojson/ProtoList) — created_at/updated_at are ProtoTimestamp {seconds,nanos}.
      properties:
        id:
          type: string
          format: uuid
        folder_id:
          type: string
          format: uuid
        filename:
          type: string
        mime_type:
          type: string
        file_size:
          type: integer
          format: int64
        storage_key:
          type: string
        thumbnail_key:
          type: string
        current_version:
          type: integer
        owner_id:
          type: string
          format: uuid
        is_favorite:
          type: boolean
        is_deleted:
          type: boolean
        tags:
          type: array
          items:
            $ref: "#/components/schemas/DocumentTag"
        created_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        updated_at:
          $ref: "#/components/schemas/ProtoTimestamp"

    DocumentFileVersion:
      type: object
      description: >-
        Only ever returned via GET .../files/{id}/versions, which serializes via protojson
        (bare array) — unlike DocumentFile/DocumentFolder, created_at here is a real RFC3339
        string, not a ProtoTimestamp object.
      properties:
        id:
          type: string
          format: uuid
        file_id:
          type: string
          format: uuid
        version_number:
          type: integer
        version_label:
          type: string
        storage_key:
          type: string
        file_size:
          type: integer
          format: int64
        created_by:
          type: string
          format: uuid
        created_at:
          type: string
          format: date-time

    DocumentFileActivity:
      type: object
      description: >-
        Only ever returned via GET .../files/{id}/activity, which serializes via protojson (bare
        array) — created_at here is a real RFC3339 string, not a ProtoTimestamp object. Append-only
        audit trail entry (DB-level trigger blocks UPDATE/DELETE).
      properties:
        id:
          type: string
          format: uuid
        file_id:
          type: string
          format: uuid
        action:
          type: string
          enum: [uploaded, renamed, moved, copied, downloaded, shared, version_created, reverted]
        actor_id:
          type: string
          format: uuid
        actor_name:
          type: string
          description: Denormalized from JOIN
        detail:
          type: string
        created_at:
          type: string
          format: date-time

    DocumentFileComment:
      type: object
      description: >-
        List/create/update all serialize via protojson — created_at and updated_at are always
        real RFC3339 strings, not ProtoTimestamp objects.
      properties:
        id:
          type: string
          format: uuid
        file_id:
          type: string
          format: uuid
        author_id:
          type: string
          format: uuid
        author_name:
          type: string
          description: Denormalized from JOIN
        content:
          type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time

    DocumentShareLink:
      type: object
      description: >-
        An external, unauthenticated read/download link for one document file. List/create
        serialize via protojson — created_at and expires_at are always real RFC3339 strings.
        password_hash never leaves the service; has_password is the only signal a caller gets.
      properties:
        id:
          type: string
          format: uuid
        file_id:
          type: string
          format: uuid
        token:
          type: string
          description: The link secret in clear — this is the copyable URL segment.
        expires_at:
          type: string
          format: date-time
          nullable: true
          description: Absent or null means the link never expires.
        has_password:
          type: boolean
        view_count:
          type: integer
        created_at:
          type: string
          format: date-time

    DocumentShare:
      type: object
      description: >-
        POST /shares serializes via encoding/json over the raw proto struct (created_at as
        ProtoTimestamp); GET /shares/entity serializes the same entity via protojson (created_at
        as an RFC3339 string) — see the differing response schemas per operation.
      properties:
        id:
          type: string
          format: uuid
        entity_type:
          type: string
          description: "\"file\" or \"folder\" (not server-validated against an enum)"
        entity_id:
          type: string
          format: uuid
        shared_with_user_id:
          type: string
          format: uuid
        shared_with_user_name:
          type: string
          description: Denormalized from JOIN
        permission:
          type: integer
          description: "SharePermission integer (UseEnumNumbers): 0=UNSPECIFIED 1=READ 2=WRITE"
        shared_by:
          type: string
          format: uuid
        shared_by_name:
          type: string
          description: Denormalized from JOIN
        created_at:
          $ref: "#/components/schemas/ProtoTimestamp"

    DocumentTag:
      type: object
      description: >-
        POST /tags serializes via encoding/json over the raw proto struct (created_at as
        ProtoTimestamp); GET /tags serializes the same entity via protojson (created_at as an
        RFC3339 string) — see the differing response schemas per operation.
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        color:
          type: string
        file_count:
          type: integer
          description: Computed, not stored
        created_by:
          type: string
          format: uuid
        created_at:
          $ref: "#/components/schemas/ProtoTimestamp"

    DocumentEntityLink:
      type: object
      description: >-
        POST .../links serializes via encoding/json over the raw proto struct (created_at as
        ProtoTimestamp); GET .../links serializes the same entity via protojson (created_at as
        an RFC3339 string) — see the differing response schemas per operation.
      properties:
        id:
          type: string
          format: uuid
        file_id:
          type: string
          format: uuid
        entity_type:
          type: string
          description: "Typically contact, company, deal, project, or task (not server-validated against an enum)"
        entity_id:
          type: string
          format: uuid
        entity_name:
          type: string
          description: Denormalized display name
        linked_by:
          type: string
          format: uuid
        created_at:
          $ref: "#/components/schemas/ProtoTimestamp"

    DocumentVirtualFile:
      type: object
      description: >-
        Read-only reference to an attachment stored outside the document tree (chat, email,
        task) — not editable via the Files endpoints. Serialized via encoding/json over the
        raw proto struct (created_at as ProtoTimestamp).
      properties:
        id:
          type: string
        filename:
          type: string
        mime_type:
          type: string
        file_size:
          type: integer
          format: int64
        storage_key:
          type: string
        source_type:
          type: integer
          description: "VirtualFileSource integer (UseEnumNumbers): 0=UNSPECIFIED 1=CHAT 2=EMAIL 3=TASK"
        source_id:
          type: string
          description: ID of the chat message, email, or task the file originated from
        source_name:
          type: string
        uploaded_by:
          type: string
          format: uuid
        uploaded_by_name:
          type: string
        created_at:
          $ref: "#/components/schemas/ProtoTimestamp"

    DocumentFolderPathSegment:
      type: object
      description: One breadcrumb segment (root-to-leaf order) returned by GET /api/v1/documents/folders/{id}/path
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string

    DocumentFileSearchResult:
      type: object
      properties:
        file:
          $ref: "#/components/schemas/DocumentFile"
        rank:
          type: number
          format: float
        snippet:
          type: string

    DocumentWopiAction:
      type: object
      description: One WOPI app action (e.g. Word/view) advertised by the OnlyOffice WOPI discovery document.
      properties:
        app:
          type: string
          example: Word
        name:
          type: string
          example: view
        ext:
          type: string
          example: docx
        url_src:
          type: string
          description: WOPI action URL template

    # === openapi-drift sweep: plugins ===
    PluginHookRegistration:
      type: object
      description: A single hook binding declared by a plugin manifest.
      properties:
        hook_type:
          type: string
          description: "e.g. before_create, after_update"
        module:
          type: string
          description: "e.g. crm, work, biz"
        entity_type:
          type: string
          description: "e.g. contact, task, invoice"
        priority:
          type: integer
          format: int32

    PluginManifest:
      type: object
      description: >-
        A registered plugin definition. Returned via response.JSON (plain
        encoding/json over the generated proto struct, not protojson) — field
        names already match proto snake_case via struct tags, but
        created_at/updated_at serialize as the raw proto wire shape
        ({seconds,nanos}), not RFC-3339 strings.
      properties:
        id:
          type: string
          format: uuid
        slug:
          type: string
          description: >-
            Plugin slug, unique per tenant. A manifest shipped with the product
            and a tenant's own manifest never share one.
        name:
          type: string
        description:
          type: string
        version:
          type: string
          example: "0.1.0"
        author:
          type: string
        settings_schema:
          type: string
          description: JSON Schema for the plugin's settings, encoded as a string.
        permissions:
          type: array
          items: { type: string }
          description: Permission keys the plugin requires (subject to per-installation approval).
        hook_registrations:
          type: array
          items: { $ref: "#/components/schemas/PluginHookRegistration" }
        wasm_binary_hash:
          type: string
          description: SHA-256 hash of the uploaded WASM binary, if plugin_type=wasm.
        plugin_type:
          type: string
          enum: [config, wasm]
          default: config
        created_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        updated_at:
          $ref: "#/components/schemas/ProtoTimestamp"

    PluginInstallation:
      type: object
      description: >-
        A per-tenant plugin installation, enriched with denormalized manifest
        fields and the currently granted permission set. created_at/updated_at
        are the raw proto wire shape ({seconds,nanos}).
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        manifest_id:
          type: string
          format: uuid
        status:
          type: string
          enum: [pending_approval, active, disabled, error, uninstalled]
        settings:
          type: string
          description: Installation settings as a JSON string.
        error_message:
          type: string
        installed_by:
          type: string
          format: uuid
        created_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        updated_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        manifest_slug:
          type: string
        manifest_name:
          type: string
        manifest_version:
          type: string
        plugin_type:
          type: string
          enum: [config, wasm]
        required_permissions:
          type: array
          items: { type: string }
        granted_permissions:
          type: array
          items: { type: string }
        settings_schema:
          type: string
          description: JSON Schema for this installation's settings, encoded as a string.

    PluginValidationRule:
      type: object
      description: >-
        Config-based field validation rule, optionally scoped to one plugin
        installation. created_at/updated_at are the raw proto wire shape
        ({seconds,nanos}).
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        installation_id:
          type: string
          format: uuid
          nullable: true
          description: Owning plugin installation, if the rule was installed by a plugin rather than authored directly.
        name:
          type: string
        description:
          type: string
        entity_type:
          type: string
          description: "e.g. contact, deal, invoice"
        field_name:
          type: string
        rule_type:
          type: string
          enum: [regex, range, required_if, format, custom, enum]
        rule_config:
          type: string
          description: Rule-type-specific configuration, encoded as a JSON string.
        error_message:
          type: string
        priority:
          type: integer
          format: int32
        enabled:
          type: boolean
        created_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        updated_at:
          $ref: "#/components/schemas/ProtoTimestamp"

    PluginWorkflowRule:
      type: object
      description: >-
        Config-based automation trigger, optionally scoped to one plugin
        installation. created_at/updated_at are the raw proto wire shape
        ({seconds,nanos}).
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        installation_id:
          type: string
          format: uuid
          nullable: true
          description: Owning plugin installation, if the rule was installed by a plugin rather than authored directly.
        name:
          type: string
        description:
          type: string
        trigger_event:
          type: string
          description: "e.g. contact.created, deal.stage_changed"
        conditions:
          type: string
          description: Trigger conditions, encoded as a JSON array string.
        actions:
          type: string
          description: Actions to run when conditions match, encoded as a JSON array string.
        priority:
          type: integer
          format: int32
        enabled:
          type: boolean
        created_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        updated_at:
          $ref: "#/components/schemas/ProtoTimestamp"

    PluginIndustryTemplate:
      type: object
      description: >-
        A preconfigured industry template that can be applied to a tenant,
        bundling custom fields, validation rules, workflow rules, and default
        settings. Has no created_at/updated_at (not tracked in the proto message).
      properties:
        id:
          type: string
          format: uuid
        slug:
          type: string
        name:
          type: string
        description:
          type: string
        industry:
          type: string
        icon:
          type: string
          default: building
        custom_fields:
          type: string
          description: Custom field definitions, encoded as a JSON array string.
        validation_rules:
          type: string
          description: Validation rules to seed, encoded as a JSON array string.
        workflow_rules:
          type: string
          description: Workflow rules to seed, encoded as a JSON array string.
        default_settings:
          type: string
          description: Default plugin settings, encoded as a JSON object string.

    PluginExecutionLog:
      type: object
      description: >-
        Audit trail entry for one hook execution. created_at is the raw proto
        wire shape ({seconds,nanos}).
      properties:
        id:
          type: string
          format: uuid
        installation_id:
          type: string
          format: uuid
        hook_type:
          type: string
        module:
          type: string
        entity_type:
          type: string
        entity_id:
          type: string
          format: uuid
        duration_ms:
          type: integer
          format: int32
        status:
          type: string
          enum: [success, error, timeout, skipped]
        error_message:
          type: string
        created_at:
          $ref: "#/components/schemas/ProtoTimestamp"

    # === openapi-drift sweep: produktion-ext ===
    # -----------------------------------------------------------------------
    # Produktion — BOMs, Work Steps, Machines, Quality Checks
    # All served via response.JSON() on the raw gRPC struct (encoding/json,
    # not protojson): timestamps are google.protobuf.Timestamp wire shape
    # ({seconds,nanos}), not RFC3339 strings.
    # -----------------------------------------------------------------------

    ProduktionBomItem:
      type: object
      description: A single material line on a BOM.
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        bom_id:
          type: string
          format: uuid
        material_name:
          type: string
        quantity:
          type: number
        unit:
          type: string
        sort_order:
          type: integer

    ProduktionBom:
      type: object
      description: A bill of materials (Stückliste).
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        product_name:
          type: string
        sku:
          type: string
        version:
          type: string
        active:
          type: boolean
        notes:
          type: string
        items:
          type: array
          items:
            $ref: "#/components/schemas/ProduktionBomItem"
        created_by:
          type: string
          format: uuid
        created_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        updated_at:
          $ref: "#/components/schemas/ProtoTimestamp"

    ProduktionOrderStep:
      type: object
      description: A work step (Arbeitsschritt) on a production order.
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        order_id:
          type: string
          format: uuid
        step_nr:
          type: integer
        name:
          type: string
        description:
          type: string
        duration_minutes:
          type: integer
        status:
          type: string
          description: "pending, in_progress, completed, or skipped"
        assignee:
          type: string
        created_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        updated_at:
          $ref: "#/components/schemas/ProtoTimestamp"

    ProduktionMachine:
      type: object
      description: A production machine.
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        name:
          type: string
        type:
          type: string
        status:
          type: string
          description: "available, in_use, or maintenance"
        notes:
          type: string
        created_by:
          type: string
          format: uuid
        created_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        updated_at:
          $ref: "#/components/schemas/ProtoTimestamp"

    ProduktionQualityCheck:
      type: object
      description: A quality inspection record (Qualitätsprüfung) for a production order.
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        order_id:
          type: string
          format: uuid
        inspector:
          type: string
        checked_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        passed:
          type: boolean
        defects_found:
          type: integer
        notes:
          type: string
        created_by:
          type: string
          format: uuid
        created_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        updated_at:
          $ref: "#/components/schemas/ProtoTimestamp"

    # === openapi-drift sweep: wp3-integration-datev ===
    # -----------------------------------------------------------------------
    # route_integration.go -- notification.proto IntegrationConfigInfo /
    # ChannelMappingInfo / AccountLinkInfo, as serialized by response.JSON
    # (raw proto struct through encoding/json -- snake_case json tags match
    # protojson's UseProtoNames output, but Timestamp fields are the wire
    # shape {seconds, nanos}, NOT RFC3339 -- see ProtoTimestamp).
    # -----------------------------------------------------------------------

    IntegrationConfig:
      type: object
      description: >-
        Admin-configured platform integration (Teams/Slack). credentials_vault_key
        is intentionally never returned by the API -- secrets stay server-side.
      properties:
        id:
          type: string
          format: uuid
        platform:
          type: string
          description: "e.g. slack, teams"
        is_active:
          type: boolean
        metadata:
          type: string
          description: "JSON-encoded string with platform-specific metadata"
        created_by:
          type: string
          format: uuid
        created_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        updated_at:
          $ref: "#/components/schemas/ProtoTimestamp"

    IntegrationChannelMapping:
      type: object
      description: Routes a KMU Hub module's notifications to a specific platform channel.
      properties:
        id:
          type: string
          format: uuid
        config_id:
          type: string
          format: uuid
        channel_id:
          type: string
        channel_name:
          type: string
        modules:
          type: array
          items:
            type: string
          description: "KMU Hub module keys routed to this channel (e.g. deals, tasks)"
        is_active:
          type: boolean
        created_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        updated_at:
          $ref: "#/components/schemas/ProtoTimestamp"

    IntegrationAccountLink:
      type: object
      description: A KMU Hub user's linked account on an external platform (Teams/Slack).
      properties:
        platform:
          type: string
          description: "e.g. slack, teams"
        external_display_name:
          type: string
        linked_at:
          $ref: "#/components/schemas/ProtoTimestamp"

    # -----------------------------------------------------------------------
    # route_datev_upload.go -- ListDatevUploadLogs. Built by hand-mapping
    # DatevUploadLogEntry fields into a map in the gateway handler; started_at
    # /completed_at go through timestamppb.Timestamp.AsTime() before being
    # JSON-encoded, so they ARE genuine RFC3339 strings here (unlike the
    # IntegrationConfig/-ChannelMapping/-AccountLink timestamps above).
    # -----------------------------------------------------------------------

    DatevUploadLogEntry:
      type: object
      properties:
        id:
          type: string
          format: uuid
        upload_type:
          type: string
          description: "e.g. buchungsstapel, beleg"
        status:
          type: string
        file_size:
          type: integer
          format: int32
        document_count:
          type: integer
          format: int32
        error_message:
          type: string
          description: "Present only when the upload run failed"
        started_at:
          type: string
          format: date-time
        completed_at:
          type: string
          format: date-time
          description: "Present only once the upload run has finished"

    # === openapi-drift sweep: wp4-bexio-lexware ===
    # -----------------------------------------------------------------------
    # route_bexio.go / route_lexware.go -- ListBexioSyncLogs /
    # ListLexwareSyncLogs entries. Built by hand-mapping the proto
    # BexioSyncLogEntry/LexwareSyncLogEntry fields into a map in the gateway
    # handler; started_at is unconditionally set via e.GetStartedAt().AsTime()
    # and completed_at (when present) likewise, so both ARE genuine RFC3339
    # strings here -- unlike the raw proto structs below (BexioFieldMappingEntry
    # / LexwareFieldMappingEntry), which go through response.JSON /
    # decodeAndValidate unchanged (no Timestamp fields, so no {seconds,nanos}
    # concern there either).
    # -----------------------------------------------------------------------

    BexioSyncLogEntry:
      type: object
      properties:
        id:
          type: string
          format: uuid
        sync_type:
          type: string
          description: "e.g. contacts, payments"
        status:
          type: string
        items_processed:
          type: integer
          format: int32
        items_created:
          type: integer
          format: int32
        items_updated:
          type: integer
          format: int32
        items_failed:
          type: integer
          format: int32
        error_message:
          type: string
          description: "Present only when the sync run failed"
        started_at:
          type: string
          format: date-time
        completed_at:
          type: string
          format: date-time
          description: "Present only once the sync run has finished"

    BexioFieldMappingEntry:
      type: object
      description: >-
        Raw proto struct returned by GET (bare array) and accepted as-is by
        PUT via decodeAndValidate -- json tags match the proto field names
        below (response.JSON on the raw struct, not protojson).
      properties:
        kmuhub_field:
          type: string
        bexio_field:
          type: string
        direction:
          type: string
          enum: [inbound, outbound, both]
        required:
          type: boolean

    LexwareSyncLogEntry:
      type: object
      properties:
        id:
          type: string
          format: uuid
        sync_type:
          type: string
          description: "e.g. contacts, invoices, quotes"
        status:
          type: string
        items_processed:
          type: integer
          format: int32
        items_created:
          type: integer
          format: int32
        items_updated:
          type: integer
          format: int32
        items_failed:
          type: integer
          format: int32
        error_message:
          type: string
          description: "Present only when the sync run failed"
        started_at:
          type: string
          format: date-time
        completed_at:
          type: string
          format: date-time
          description: "Present only once the sync run has finished"

    LexwareFieldMappingEntry:
      type: object
      description: >-
        Raw proto struct returned by GET (bare array) and accepted as-is by
        PUT via decodeAndValidate -- json tags match the proto field names
        below (response.JSON on the raw struct, not protojson).
      properties:
        kmuhub_field:
          type: string
        lexware_field:
          type: string
        direction:
          type: string
          enum: [inbound, outbound, both]
        required:
          type: boolean

    # === openapi-drift sweep: wp5-caldav-guest-misc ===
    CaldavPasswordInfo:
      type: object
      description: An app-specific password used for CalDAV/CardDAV HTTP Basic Auth. The plaintext password is never returned by this endpoint.
      properties:
        id:
          type: string
          format: uuid
        label:
          type: string
        password_prefix:
          type: string
          description: First few characters of the password, shown for identification
        last_used_at:
          type: string
          format: date-time
          nullable: true
        created_at:
          type: string
          format: date-time
        revoked_at:
          type: string
          format: date-time
          nullable: true

    CaldavCreatePasswordRequest:
      type: object
      required: [label]
      properties:
        label:
          type: string

    CaldavCreatePasswordResponse:
      type: object
      description: Returned once at creation time — the plaintext password is never retrievable again. Field names differ from CaldavPasswordInfo (password/prefix vs. password_prefix).
      properties:
        password:
          type: string
          description: Plaintext app-specific password (shown once)
        id:
          type: string
          format: uuid
        label:
          type: string
        prefix:
          type: string
          description: First few characters of the password

    CaldavStatusResponse:
      type: object
      properties:
        org_enabled:
          type: boolean
          description: Whether CalDAV/CardDAV is enabled organization-wide
        user_enabled:
          type: boolean
          description: Whether the current user has CalDAV/CardDAV enabled
        password_count:
          type: integer
          description: Number of active (non-revoked) app-specific passwords for the current user
        user_id:
          type: string
          format: uuid
        caldav_url:
          type: string
        carddav_url:
          type: string

    CaldavTestResult:
      type: object
      properties:
        success:
          type: boolean
          description: True only if both the CalDAV and the CardDAV probe reached and authenticated successfully
        message:
          type: string
          description: "Human-readable detail for any failed probe (e.g. 'CalDAV: authentication failed'); empty on success"
        caldav_reachable:
          type: boolean
        carddav_reachable:
          type: boolean

    CaldavAdminSettings:
      type: object
      properties:
        enabled:
          type: boolean
          description: Whether CalDAV/CardDAV is enabled organization-wide

    CaldavUserInfo:
      type: object
      description: Admin-facing summary of a user's CalDAV enablement, for audit purposes.
      properties:
        id:
          type: string
          format: uuid
        email:
          type: string
          format: email
        first_name:
          type: string
        last_name:
          type: string
        caldav_enabled:
          type: boolean
        password_count:
          type: integer
        last_used:
          type: string
          format: date-time
          nullable: true

    GuestSession:
      type: object
      description: A validated guest chat session, as returned to the guest client (the token hash is never exposed).
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        channel_id:
          type: string
          format: uuid
        display_name:
          type: string
        email:
          type: string
          format: email
          nullable: true
        ip_address:
          type: string
          nullable: true
        user_agent:
          type: string
          nullable: true
        last_activity_at:
          type: string
          format: date-time
        is_active:
          type: boolean
        created_at:
          type: string
          format: date-time
        expires_at:
          type: string
          format: date-time

    GuestChannelConfig:
      type: object
      description: Full per-channel guest chat configuration (admin/internal shape). See GuestPublicConfig for the subset exposed to guests.
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        channel_id:
          type: string
          format: uuid
        welcome_message:
          type: string
        logo_url:
          type: string
          nullable: true
        primary_color:
          type: string
        token_expiry_hours:
          type: integer
        max_file_size_mb:
          type: integer
        allowed_file_types:
          type: string
        is_active:
          type: boolean
        created_by:
          type: string
          format: uuid
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time

    GuestPublicConfig:
      type: object
      description: Public-safe subset of GuestChannelConfig returned by GET /api/v1/guest/config (no internal IDs).
      properties:
        welcome_message:
          type: string
        logo_url:
          type: string
          nullable: true
        primary_color:
          type: string
        max_file_size_mb:
          type: integer
        allowed_file_types:
          type: string

    GuestCreateSessionRequest:
      type: object
      required: [channel_id, display_name]
      properties:
        channel_id:
          type: string
          format: uuid
        display_name:
          type: string
        email:
          type: string
          format: email
          nullable: true

    GuestCreateSessionResponse:
      type: object
      properties:
        token:
          type: string
          description: Guest session token — pass as the X-Guest-Token header on subsequent requests
        session:
          $ref: "#/components/schemas/GuestSession"

    GuestValidateTokenRequest:
      type: object
      required: [token]
      properties:
        token:
          type: string

    GuestValidateTokenResponse:
      type: object
      properties:
        session:
          $ref: "#/components/schemas/GuestSession"
        config:
          description: Present only if guest chat config exists for the session's channel
          allOf:
            - $ref: "#/components/schemas/GuestChannelConfig"

    GuestSendMessageRequest:
      type: object
      required: [content]
      properties:
        content:
          type: string

    FeatureFlagsResponse:
      type: object
      properties:
        flags:
          type: object
          additionalProperties:
            type: boolean
          description: Map of flag key (e.g. "modules.dialer", "plugins.wasm") to its resolved boolean value
        version:
          type: string
          example: v1

    GlobalSearchModuleResult:
      type: object
      description: Result set for one search backend. Item shape in `results` is module-specific (e.g. CRM SearchResult, document FileSearchResult) — see the search and documents tags for the underlying record shapes.
      properties:
        module:
          type: string
          enum: [crm, documents, email]
        results:
          type: array
          items:
            type: object
            additionalProperties: true
        total:
          type: integer
        error:
          type: string
          description: Present only if this module's search failed or timed out; other modules still return normally

    GlobalSearchResponse:
      type: object
      properties:
        query:
          type: string
        modules:
          type: array
          items:
            $ref: "#/components/schemas/GlobalSearchModuleResult"

    # === openapi-drift sweep: wp8-partial-gaps ===
    # -----------------------------------------------------------------------
    # route_crm_ext.go -- crmv1 duplicate-detection / timeline / consent
    # proto messages, as serialized by response.JSON (raw proto struct
    # through encoding/json -- snake_case json tags match protojson's
    # UseProtoNames output). No Timestamp-typed fields are involved here
    # (crmv1 uses plain RFC3339 strings for these messages), so no
    # ProtoTimestamp refs are used in this block.
    # -----------------------------------------------------------------------

    CrmContactSnapshot:
      type: object
      description: >-
        Raw crmv1.ContactInfo proto as serialized by response.JSON. Distinct
        from the ContactInfo schema used by the primary /api/v1/contacts
        endpoints (same underlying proto type, but that schema documents an
        unrelated, already-drifted camelCase projection -- this one reflects
        the actual snake_case wire shape).
      properties:
        id:
          type: string
          format: uuid
        first_name:
          type: string
        last_name:
          type: string
        email:
          type: string
        phone:
          type: string
        company_id:
          type: string
          format: uuid
          nullable: true
        company_name:
          type: string
          nullable: true
        position:
          type: string
        notes:
          type: string
        tags:
          type: array
          items:
            $ref: "#/components/schemas/CrmTagSnapshot"
        custom_fields:
          type: object
          additionalProperties:
            type: string
        created_by:
          type: string
          format: uuid
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
        visibility:
          type: string
          enum: [shared, personal]
        owner_id:
          type: string
          format: uuid
          nullable: true

    CrmCompanySnapshot:
      type: object
      description: >-
        Raw crmv1.CompanyInfo proto as serialized by response.JSON. Distinct
        from the CompanyInfo schema used by the primary /api/v1/companies
        endpoints (same underlying proto type, but that schema documents an
        unrelated, already-drifted camelCase projection -- this one reflects
        the actual snake_case wire shape).
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        domain:
          type: string
        industry:
          type: string
        employee_count:
          type: integer
        address:
          type: string
        city:
          type: string
        country:
          type: string
        notes:
          type: string
        tags:
          type: array
          items:
            $ref: "#/components/schemas/CrmTagSnapshot"
        custom_fields:
          type: object
          additionalProperties:
            type: string
        contact_count:
          type: integer
        created_by:
          type: string
          format: uuid
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time

    CrmTagSnapshot:
      type: object
      description: Raw crmv1.TagInfo proto, nested inside CrmContactSnapshot/CrmCompanySnapshot.
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        color:
          type: string
        entity_type:
          type: string
          enum: [contact, company, deal, activity]
        created_at:
          type: string
          format: date-time

    CrmDuplicateContactCandidate:
      type: object
      properties:
        contact:
          $ref: "#/components/schemas/CrmContactSnapshot"
        similarity:
          type: number
          format: double
          description: Similarity score, higher is more similar
        match_type:
          type: string
          enum: [email_exact, name_fuzzy, phone_exact]

    CrmContactDuplicatesResponse:
      type: object
      properties:
        duplicates:
          type: array
          items:
            $ref: "#/components/schemas/CrmDuplicateContactCandidate"
        total:
          type: integer

    CrmDuplicateCompanyCandidate:
      type: object
      properties:
        company:
          $ref: "#/components/schemas/CrmCompanySnapshot"
        similarity:
          type: number
          format: double
          description: Similarity score, higher is more similar
        match_type:
          type: string
          enum: [domain_exact, name_fuzzy]

    CrmCompanyDuplicatesResponse:
      type: object
      properties:
        duplicates:
          type: array
          items:
            $ref: "#/components/schemas/CrmDuplicateCompanyCandidate"
        total:
          type: integer

    CrmMergeRequest:
      type: object
      description: Shared request body for POST /api/v1/contacts/merge and POST /api/v1/companies/merge.
      required: [primary_id, duplicate_id]
      properties:
        primary_id:
          type: string
          format: uuid
          description: Record that survives the merge
        duplicate_id:
          type: string
          format: uuid
          description: Record that is merged into primary_id and then deleted

    CrmTimelineEvent:
      type: object
      properties:
        id:
          type: string
        event_type:
          type: string
          description: "e.g. activity, deal_linked"
        occurred_at:
          type: string
          format: date-time
        title:
          type: string
        description:
          type: string
          nullable: true
        created_by_name:
          type: string
        metadata:
          type: object
          nullable: true
          properties:
            data:
              type: object
              additionalProperties:
                type: string

    CrmContactTimelineResponse:
      type: object
      properties:
        events:
          type: array
          items:
            $ref: "#/components/schemas/CrmTimelineEvent"
        total:
          type: integer

    # -----------------------------------------------------------------------
    # route_crm_ext.go -- GDPR consent + deletion-request proto messages, as
    # serialized by response.JSON. Same snake_case / no-Timestamp-fields
    # notes as the block above apply.
    # -----------------------------------------------------------------------

    GdprConsentRecord:
      type: object
      description: Raw crmv1.ConsentRecord proto as serialized by response.JSON.
      properties:
        id:
          type: string
          format: uuid
        contact_id:
          type: string
          format: uuid
        consent_type:
          type: string
        granted:
          type: boolean
        legal_basis:
          type: string
          description: "e.g. consent, contract, legitimate_interest"
        source:
          type: string
          description: "e.g. web_form, phone, import"
        ip_address:
          type: string
          nullable: true
        notes:
          type: string
        granted_at:
          type: string
          format: date-time
          nullable: true
        revoked_at:
          type: string
          format: date-time
          nullable: true
        created_by:
          type: string
          format: uuid
          nullable: true
        created_at:
          type: string
          format: date-time

    GdprConsentSummary:
      type: object
      properties:
        contact_id:
          type: string
          format: uuid
        consents:
          type: object
          description: Map keyed by consent_type -> latest ConsentRecord for that type
          additionalProperties:
            $ref: "#/components/schemas/GdprConsentRecord"

    GdprGrantConsentRequest:
      type: object
      required: [consent_type, source, legal_basis]
      properties:
        consent_type:
          type: string
        source:
          type: string
          description: "e.g. web_form, phone, import"
        legal_basis:
          type: string
          description: "e.g. consent, contract, legitimate_interest"
        ip_address:
          type: string
          nullable: true

    GdprConsentHistoryResponse:
      type: object
      properties:
        history:
          type: array
          items:
            $ref: "#/components/schemas/GdprConsentRecord"
        total:
          type: integer

    GdprDeletionRequest:
      type: object
      description: Raw crmv1.GDPRDeletionRequest proto as serialized by response.JSON.
      properties:
        id:
          type: string
          format: uuid
        contact_id:
          type: string
          format: uuid
        requested_by:
          type: string
          format: uuid
          nullable: true
        reason:
          type: string
        status:
          type: string
          enum: [pending, processing, completed]
        completed_at:
          type: string
          format: date-time
          nullable: true
        created_at:
          type: string
          format: date-time

    # -----------------------------------------------------------------------
    # route_biz_billing.go / route_biz_quotes.go -- bizv1 GoBD journal,
    # invoice-number-validation, invoice-lock and payment-stats proto
    # messages, as serialized by response.JSON. None of these fields are
    # Timestamp-typed (LockInvoiceResponse.locked_at is a plain string
    # field), so no ProtoTimestamp refs are used in this block. The
    # CreateQuoteFromDeal / UpdateDunningStatus / SendDunningNotice handlers
    # instead reuse the already-defined Quote / DunningRecord schemas (see
    # paths fragment) since they return those exact proto types unchanged.
    # -----------------------------------------------------------------------

    FinanceJournalSummary:
      type: object
      description: Raw bizv1.GetJournalSummaryResponse proto as serialized by response.JSON.
      properties:
        year:
          type: integer
        total_invoices_issued:
          type: integer
          description: Count of invoice numbers assigned
        gaps_detected:
          type: integer
          description: Should always be 0 for GoBD-compliant invoice numbering
        first_number:
          type: string
          description: "e.g. RE-2026-0001"
        last_number:
          type: string
          description: "e.g. RE-2026-0042"
        highest_sequence:
          type: integer
          description: Numeric part of the last assigned invoice number

    FinanceInvoiceNumberValidation:
      type: object
      description: Raw bizv1.ValidateInvoiceNumberResponse proto as serialized by response.JSON.
      properties:
        valid_format:
          type: boolean
          description: Matches pattern RE-YYYY-NNNN
        already_used:
          type: boolean
          description: True if the number is already assigned to an invoice
        canonical:
          type: string
          description: Normalized form of the number, empty when the format is invalid

    FinanceInvoiceLockResult:
      type: object
      description: Raw bizv1.LockInvoiceResponse proto as serialized by response.JSON.
      properties:
        invoice_json:
          type: string
          format: byte
          description: Base64-encoded legacy JSON snapshot of the locked invoice (models.Invoice shape, not the Invoice schema)
        locked_at:
          type: string
          format: date-time
          description: RFC3339 timestamp of when the lock was applied

    FinancePaymentStats:
      type: object
      description: Raw bizv1.GetPaymentStatsResponse proto as serialized by response.JSON.
      properties:
        total_invoices:
          type: integer
        total_paid:
          type: integer
        total_outstanding:
          type: integer
        total_paid_amount:
          type: string
          description: Decimal as string
        total_outstanding_amount:
          type: string
          description: Decimal as string
        average_days_to_pay:
          type: string
          description: Decimal as string; "0" if no data

    # -----------------------------------------------------------------------
    # route_biz_ext.go -- time-tracking-to-invoice request body.
    # -----------------------------------------------------------------------

    HrTimeCreateInvoiceRequest:
      type: object
      required: [employee_id, customer_name, date_from, date_to, hourly_rate]
      properties:
        employee_id:
          type: string
          format: uuid
        customer_name:
          type: string
        customer_address:
          type: string
        customer_email:
          type: string
          format: email
        date_from:
          type: string
          format: date
        date_to:
          type: string
          format: date
        hourly_rate:
          type: string
          description: Decimal as string
        description:
          type: string
          description: 'Optional; defaults to "Arbeitszeit {from}-{to}"'
        tax_mode:
          type: string
          enum: [standard, reverse_charge, kleinunternehmer]
          default: standard

    # -----------------------------------------------------------------------
    # route_settings.go -- flattened camelCase projection of
    # settingsv1.UserModuleGrant (see toUserModuleGrantJSON in
    # route_settings.go) -- NOT the raw proto. Timestamps are pre-formatted
    # RFC3339 strings via time.Time.Format(), so no ProtoTimestamp ref here.
    # -----------------------------------------------------------------------

    SettingsModuleGrant:
      type: object
      properties:
        userId:
          type: string
          format: uuid
        userName:
          type: string
        moduleId:
          type: string
          description: One of the ModuleId values (see lib/pricing.ts)
        grantedAt:
          type: string
          format: date-time
        lastActiveAt:
          type: string
          format: date-time
          nullable: true

    TenantModule:
      type: object
      description: >
        One catalogue module with this tenant's activation state. assignedSeats
        counts the users holding a module grant and is reported as 0 while the
        module is inactive — the grants themselves survive a deactivation.
      properties:
        moduleId:
          type: string
          description: One of the ModuleId values (see lib/pricing.ts)
        group:
          type: string
          enum: [core, comm, team, industry, tools]
        active:
          type: boolean
        assignedSeats:
          type: integer

    TenantSubscription:
      type: object
      description: >
        The booked contract of the tenant. Seats in use are not part of it —
        they are counted where they are enforced (the invitation path).
      properties:
        planType:
          type: string
          enum: [cosmi, orbit]
        supportTier:
          type: string
          enum: [standard, priority, enterprise]
        status:
          type: string
          enum: [active, past_due, paused]
        billingPeriodEnd:
          type: string
          format: date
          nullable: true
        totalSeats:
          type: integer
          nullable: true

    Branding:
      type: object
      description: >
        Tenant-wide workspace branding, served via protojson (snake_case).
        logo_object_key/icon_object_key are MinIO object keys — resolve via
        POST /api/v1/files/presign-download — not URLs. accent_color is
        restricted to the Cosmi swatch palette.
      properties:
        name:
          type: string
        logo_object_key:
          type: string
        icon_object_key:
          type: string
        accent_color:
          type: string
          description: Booked seats; null means unlimited

    # -----------------------------------------------------------------------
    # route_files.go -- generic presigned upload/download request/response
    # shapes (Document service). Timestamps go through resp.ExpiresAt
    # .AsTime() before being handed to response.JSON, so they serialize as
    # proper RFC3339 strings (time.Time has its own MarshalJSON) -- not the
    # ProtoTimestamp wire shape.
    # -----------------------------------------------------------------------

    FilePresignUploadRequest:
      type: object
      required: [scope, file_name, content_type, size_bytes]
      properties:
        scope:
          type: string
          description: Logical storage scope/prefix for the upload
        file_name:
          type: string
        content_type:
          type: string
        size_bytes:
          type: integer
          format: int64
          minimum: 1

    FilePresignUploadResponse:
      type: object
      properties:
        upload_url:
          type: string
          format: uri
          description: Presigned PUT URL, valid for 15 minutes
        object_key:
          type: string
        expires_at:
          type: string
          format: date-time

    FilePresignDownloadResponse:
      type: object
      properties:
        download_url:
          type: string
          format: uri
          description: Presigned GET URL, valid for 1 hour
        object_key:
          type: string
        expires_at:
          type: string
          format: date-time

    # === openapi-drift sweep: r2a-video-meetings ===
    # =========================================================================
    # Meeting Breakout Room Schemas (Wave 6A)
    # =========================================================================

    CreateBreakoutRoomsRequest:
      type: object
      required: [count]
      properties:
        count:
          type: integer
          minimum: 1
          maximum: 20
        labels:
          type: array
          description: Optional room labels; auto-generated ("Gruppe N") when empty.
          items:
            type: string

    MeetingBreakoutRoom:
      type: object
      properties:
        id:
          type: string
          format: uuid
        meeting_id:
          type: string
          format: uuid
        room_name:
          type: string
          description: LiveKit room identifier ("breakout-{meetingId}-{index}").
        label:
          type: string
        sort_index:
          type: integer
        status:
          type: string
          enum: [open, closed]
        created_by:
          type: string
          format: uuid
        closed_at:
          type: string
          format: date-time
        created_at:
          type: string
          format: date-time

    CreateBreakoutRoomsResponse:
      type: object
      properties:
        rooms:
          type: array
          items:
            $ref: "#/components/schemas/MeetingBreakoutRoom"

    MeetingBreakoutAssignment:
      type: object
      properties:
        user_id:
          type: string
          format: uuid
        breakout_room_id:
          type: string
          format: uuid

    ListBreakoutRoomsResponse:
      type: object
      properties:
        rooms:
          type: array
          items:
            $ref: "#/components/schemas/MeetingBreakoutRoom"
        assignments:
          type: array
          items:
            $ref: "#/components/schemas/MeetingBreakoutAssignment"

    AssignBreakoutParticipantRequest:
      type: object
      required: [target_user_id]
      properties:
        target_user_id:
          type: string
          format: uuid
        breakout_room_id:
          type: string
          format: uuid
          description: Omit to move the participant back to the main room.

    JoinBreakoutRoomResponse:
      type: object
      properties:
        token:
          type: string
          description: LiveKit JWT join token
        room_name:
          type: string
        ws_url:
          type: string
          description: LiveKit WebSocket signaling URL
        ice_servers:
          type: array
          description: >-
            Per-session TURN credentials for the client RTCConfiguration;
            empty/absent when TURN is not configured.
          items:
            type: object
            properties:
              urls:
                type: array
                items:
                  type: string
              username:
                type: string
              credential:
                type: string
        breakout_room:
          $ref: "#/components/schemas/MeetingBreakoutRoom"

    GetBreakoutAssignmentResponse:
      type: object
      properties:
        room:
          description: Absent when the caller is currently in the main room.
          allOf:
            - $ref: "#/components/schemas/MeetingBreakoutRoom"

    ReturnToMainRoomRequest:
      type: object
      properties:
        target_user_id:
          type: string
          format: uuid
          description: Omit to self-return; a host/co-host may pass another participant's ID.

    # =========================================================================
    # Meeting Chat Schemas
    # =========================================================================

    SaveMeetingChatMessageRequest:
      type: object
      required: [message]
      properties:
        sender_name:
          type: string
          description: Display name of the authenticated sender (sender_id is resolved server-side from the JWT).
        message:
          type: string

    MeetingChatMessage:
      type: object
      properties:
        id:
          type: string
          format: uuid
        meeting_id:
          type: string
          format: uuid
        sender_id:
          type: string
          format: uuid
        sender_name:
          type: string
        message:
          type: string
        created_at:
          type: string
          format: date-time

    ListMeetingChatMessagesResponse:
      type: object
      properties:
        messages:
          type: array
          items:
            $ref: "#/components/schemas/MeetingChatMessage"

    # =========================================================================
    # Meeting Host Controls Schemas (Wave 3 — co-hosts, lock, moderation)
    # =========================================================================

    PromoteCoHostRequest:
      type: object
      required: [user_id]
      properties:
        user_id:
          type: string
          format: uuid

    MeetingCoHost:
      type: object
      properties:
        id:
          type: string
          format: uuid
        meeting_id:
          type: string
          format: uuid
        user_id:
          type: string
          format: uuid
        granted_by:
          type: string
          format: uuid
        created_at:
          type: string
          format: date-time

    ListCoHostsResponse:
      type: object
      properties:
        co_hosts:
          type: array
          items:
            $ref: "#/components/schemas/MeetingCoHost"

    SetMeetingLockRequest:
      type: object
      required: [locked]
      properties:
        locked:
          type: boolean

    MeetingModerationTargetRequest:
      type: object
      description: Shared body shape for the mute and kick moderation actions.
      required: [target_user_id]
      properties:
        target_user_id:
          type: string
          format: uuid

    # =========================================================================
    # Recording Sub-Operation Schemas (S1.7 / Welle 1.B)
    # =========================================================================

    RecordingConsentDetail:
      type: object
      properties:
        recording_id:
          type: string
          format: uuid
        user_id:
          type: string
          format: uuid
        consented:
          type: boolean
        responded_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        first_name:
          type: string
        last_name:
          type: string

    RecordingConsentsResponse:
      type: object
      description: >-
        Encoded via encoding/json (not protojson) — responded_at is the raw
        {seconds,nanos} wire shape, not an RFC3339 string.
      properties:
        recording_id:
          type: string
          format: uuid
        consents:
          type: array
          items:
            $ref: "#/components/schemas/RecordingConsentDetail"
        all_consented:
          type: boolean

    RecordingConsentSnapshotEntry:
      type: object
      required: [user_id, display_name]
      properties:
        user_id:
          type: string
          format: uuid
        display_name:
          type: string
        joined_at:
          type: string
          description: Opaque ISO-8601 string forwarded as-is (not parsed/validated).

    TagRecordingWithConsentsRequest:
      type: object
      required: [snapshot]
      properties:
        snapshot:
          type: array
          minItems: 1
          items:
            $ref: "#/components/schemas/RecordingConsentSnapshotEntry"

    UpdateRecordingMetadataRequest:
      type: object
      properties:
        file_url:
          type: string
        file_size_bytes:
          type: integer
          format: int64
        duration_seconds:
          type: integer
        status:
          type: string
          description: RecordingStatus name (e.g. RECORDING_STATUS_COMPLETED).

    RecordingDownloadURLResponse:
      type: object
      properties:
        url:
          type: string
        expires_at:
          type: string
          format: date-time

    # === openapi-drift sweep: r2b-hr-time ===
    HrTimeCategory:
      type: object
      description: >-
        Raw hr.v1.TimeCategory proto serialized via response.JSON (not
        response.Proto) — timestamp fields are ProtoTimestamp, not RFC3339.
      properties:
        id: { type: string, format: uuid }
        tenant_id: { type: string, format: uuid }
        name: { type: string }
        color: { type: string }
        icon: { type: string }
        is_default: { type: boolean }
        created_at: { $ref: "#/components/schemas/ProtoTimestamp" }
        updated_at: { $ref: "#/components/schemas/ProtoTimestamp" }

    HrTimeCategoryInput:
      type: object
      description: Shared body shape for create and update (PUT replaces all fields).
      required: [name]
      properties:
        name: { type: string }
        color: { type: string }
        icon: { type: string }
        is_default: { type: boolean }

    HrTimeTemplate:
      type: object
      description: >-
        Raw hr.v1.TimeTemplate proto serialized via response.JSON — timestamp
        fields are ProtoTimestamp, not RFC3339.
      properties:
        id: { type: string, format: uuid }
        tenant_id: { type: string, format: uuid }
        name: { type: string }
        category_id: { type: string, format: uuid }
        description: { type: string }
        estimated_minutes: { type: integer }
        created_at: { $ref: "#/components/schemas/ProtoTimestamp" }
        updated_at: { $ref: "#/components/schemas/ProtoTimestamp" }

    HrTimeCreateTemplateRequest:
      type: object
      required: [name]
      properties:
        name: { type: string }
        category_id: { type: string, format: uuid }
        description: { type: string }
        estimated_minutes: { type: integer, minimum: 0 }

    HrTimeProject:
      type: object
      description: >-
        Raw hr.v1.TimeProject proto serialized via response.JSON — timestamp
        fields are ProtoTimestamp, not RFC3339.
      properties:
        id: { type: string, format: uuid }
        tenant_id: { type: string, format: uuid }
        name: { type: string }
        customer_name: { type: string }
        color: { type: string }
        billable_default: { type: boolean }
        created_at: { $ref: "#/components/schemas/ProtoTimestamp" }
        updated_at: { $ref: "#/components/schemas/ProtoTimestamp" }

    HrTimeCreateProjectRequest:
      type: object
      required: [name]
      properties:
        name: { type: string }
        customer_name: { type: string }
        color: { type: string }
        billable_default: { type: boolean }

    HrTimeTeamEntry:
      type: object
      properties:
        employee_id: { type: string, format: uuid }
        name: { type: string }
        department: { type: string }
        week_minutes: { type: integer }
        target_minutes: { type: integer }
        overtime_minutes: { type: integer }
        clocked_in: { type: boolean }
        week_status: { type: string, enum: [open, submitted, approved, rejected] }

    HrTimeWeekApproval:
      type: object
      description: >-
        Raw hr.v1.WeekApproval proto serialized via response.JSON — timestamp
        fields are ProtoTimestamp, not RFC3339.
      properties:
        id: { type: string, format: uuid }
        tenant_id: { type: string, format: uuid }
        employee_id: { type: string, format: uuid }
        week_start: { type: string, format: date }
        status: { type: string, enum: [open, submitted, approved, rejected] }
        submitted_at: { $ref: "#/components/schemas/ProtoTimestamp" }
        approved_by: { type: string, format: uuid }
        approved_at: { $ref: "#/components/schemas/ProtoTimestamp" }
        rejection_reason: { type: string }
        created_at: { $ref: "#/components/schemas/ProtoTimestamp" }
        updated_at: { $ref: "#/components/schemas/ProtoTimestamp" }

    HrTimeSubmitWeekRequest:
      type: object
      required: [week_start]
      properties:
        week_start: { type: string, format: date }

    HrTimeApproveWeekRequest:
      type: object
      required: [employee_id, week_start]
      properties:
        employee_id: { type: string, format: uuid }
        week_start: { type: string, format: date }

    HrTimeRejectWeekRequest:
      type: object
      required: [employee_id, week_start]
      properties:
        employee_id: { type: string, format: uuid }
        week_start: { type: string, format: date }
        rejection_reason: { type: string }

    HrTimeReopenWeekRequest:
      type: object
      required: [employee_id, week_start]
      properties:
        employee_id: { type: string, format: uuid }
        week_start: { type: string, format: date }
        reason:
          type: string
          description: Logged with the reopen event, not stored on the approval record.

    HrTimeAnalytics:
      type: object
      description: hr.v1.GetTimeAnalyticsResp serialized via response.JSON (no timestamp fields).
      properties:
        total_minutes: { type: integer }
        target_minutes: { type: integer }
        overtime_minutes: { type: integer }
        avg_daily_minutes: { type: integer }
        day_trend:
          type: array
          items: { $ref: "#/components/schemas/HrTimeDayTrendEntry" }
        by_project:
          type: array
          items: { $ref: "#/components/schemas/HrTimeProjectBreakdown" }

    HrTimeDayTrendEntry:
      type: object
      properties:
        date: { type: string, format: date }
        net_minutes: { type: integer }
        target_minutes: { type: integer }

    HrTimeProjectBreakdown:
      type: object
      properties:
        project_id: { type: string, format: uuid }
        project_name: { type: string }
        minutes: { type: integer }

    HrTimeBalance:
      type: object
      description: hr.v1.GetTimeBalanceResp serialized via response.JSON — period_start/as_of are YYYY-MM-DD strings, not timestamps.
      properties:
        balance_minutes: { type: integer }
        target_weekly_minutes: { type: integer }
        period_start: { type: string, format: date }
        as_of: { type: string, format: date }

    # === openapi-drift sweep: r2c-misc-gaps ===
    # --- 2FA: pending-login validation ---

    Validate2FALoginRequest:
      type: object
      required: [pending_token, code]
      properties:
        pending_token:
          type: string
          description: Short-lived token returned by POST /auth/login when 2FA is required
        code:
          type: string
          description: 6-digit TOTP code, or a recovery code if is_recovery_code is true
        is_recovery_code:
          type: boolean
          default: false

    # --- Contacts: import/export/visibility (multipart forms; binary export) ---

    ContactsImportCSVForm:
      type: object
      required: [file]
      description: >-
        multipart/form-data. Additional dynamic fields named map_<csv_column>
        (form value = target CRM field name) configure the CSV-column-to-CRM-field
        mapping; not enumerable as a fixed schema property.
      properties:
        file:
          type: string
          format: binary
          description: CSV file to import
        visibility:
          type: string
          enum: [shared, personal]
          default: shared
        merge_by_email:
          type: string
          enum: ["true", "false"]
          description: Merge with existing contacts sharing the same email

    ContactsImportVCardForm:
      type: object
      required: [file]
      properties:
        file:
          type: string
          format: binary
          description: vCard (.vcf) file to import
        visibility:
          type: string
          enum: [shared, personal]
          default: shared
        merge_by_email:
          type: string
          enum: ["true", "false"]

    ContactsPreviewImportForm:
      type: object
      required: [file]
      properties:
        file:
          type: string
          format: binary
          description: CSV file to preview before import

    ContactsImportError:
      type: object
      properties:
        row:
          type: integer
        reason:
          type: string

    ContactsImportResult:
      type: object
      description: >-
        Raw proto response serialized via response.JSON (encoding/json on the
        generated Go struct) — field names are snake_case per the pb.go json
        tags.
      properties:
        imported_count:
          type: integer
        merged_count:
          type: integer
        skipped_count:
          type: integer
        errors:
          type: array
          items:
            $ref: "#/components/schemas/ContactsImportError"

    ContactsPreviewImportResponse:
      type: object
      properties:
        columns:
          type: array
          items:
            type: string
        sample_rows:
          type: array
          items:
            type: object
            properties:
              values:
                type: array
                items:
                  type: string
        detected_mapping:
          type: object
          additionalProperties:
            type: string
          description: Heuristic CSV-column -> CRM-field mapping

    ContactsExportCSVRequest:
      type: object
      properties:
        contact_ids:
          type: array
          items:
            type: string
            format: uuid
          description: Restrict export to these contacts; omit for all visible contacts
        fields:
          type: array
          items:
            type: string
          description: Restrict exported columns; omit for all fields

    ContactsExportVCardRequest:
      type: object
      properties:
        contact_ids:
          type: array
          items:
            type: string
            format: uuid
          description: Restrict export to these contacts; omit for all visible contacts

    UpdateContactVisibilityRequest:
      type: object
      required: [visibility]
      properties:
        visibility:
          type: string
          enum: [shared, personal]

    # --- Helpdesk: business hours ---

    HelpdeskBusinessHours:
      type: object
      description: >-
        Raw proto response serialized via response.JSON — updated_at is the
        {seconds,nanos} wire shape, not an RFC3339 string.
      properties:
        tenant_id:
          type: string
          format: uuid
        schedule_json:
          type: string
          description: 'JSON schedule map, e.g. {"Montag":{"active":true,"start":"08:00","end":"17:30"}, ...}'
        holidays_json:
          type: string
          description: 'JSON holiday list, e.g. [{"id":"h-1","date":"2026-01-01","name":"Neujahr"}, ...]'
        timezone:
          type: string
        updated_at:
          $ref: "#/components/schemas/ProtoTimestamp"

    UpdateHelpdeskBusinessHoursRequest:
      type: object
      required: [schedule_json, holidays_json, timezone]
      properties:
        schedule_json:
          type: string
        holidays_json:
          type: string
        timezone:
          type: string

    # --- Inbox: canned responses + thread messages ---

    InboxCannedResponse:
      type: object
      description: >-
        Raw proto response serialized via response.JSON — timestamps are the
        {seconds,nanos} wire shape, not RFC3339 strings.
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        body:
          type: string
        created_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        updated_at:
          $ref: "#/components/schemas/ProtoTimestamp"

    InboxCannedResponseList:
      type: object
      properties:
        canned_responses:
          type: array
          items:
            $ref: "#/components/schemas/InboxCannedResponse"

    InboxCannedResponseWrapper:
      type: object
      properties:
        canned_response:
          $ref: "#/components/schemas/InboxCannedResponse"

    CreateInboxCannedResponseRequest:
      type: object
      required: [name, body]
      properties:
        name:
          type: string
        body:
          type: string

    UpdateInboxCannedResponseRequest:
      type: object
      properties:
        name:
          type: string
        body:
          type: string

    InboxThreadMessage:
      type: object
      description: >-
        Raw proto response serialized via response.JSON — created_at is the
        {seconds,nanos} wire shape, not an RFC3339 string.
      properties:
        id:
          type: string
          format: uuid
        message_id:
          type: string
          format: uuid
        author_id:
          type: string
          format: uuid
          nullable: true
        author_name:
          type: string
          description: Resolved display name (user name, else external sender, else empty)
        direction:
          type: string
          enum: [inbound, outbound, internal]
        body:
          type: string
        created_at:
          $ref: "#/components/schemas/ProtoTimestamp"

    InboxThreadMessagesResponse:
      type: object
      properties:
        messages:
          type: array
          items:
            $ref: "#/components/schemas/InboxThreadMessage"

    # --- Inventar: item attachments ---

    InventarItemAttachment:
      type: object
      description: >-
        Raw proto response serialized via response.JSON — timestamps are the
        {seconds,nanos} wire shape, not RFC3339 strings.
      properties:
        id:
          type: string
          format: uuid
        tenant_id:
          type: string
          format: uuid
        item_id:
          type: string
          format: uuid
        name:
          type: string
        object_key:
          type: string
        file_type:
          type: string
        created_at:
          $ref: "#/components/schemas/ProtoTimestamp"
        updated_at:
          $ref: "#/components/schemas/ProtoTimestamp"

    InventarItemAttachmentList:
      type: object
      properties:
        attachments:
          type: array
          items:
            $ref: "#/components/schemas/InventarItemAttachment"

    InventarItemAttachmentResponse:
      type: object
      properties:
        attachment:
          $ref: "#/components/schemas/InventarItemAttachment"

    CreateInventarItemAttachmentRequest:
      type: object
      required: [name, object_key]
      properties:
        name:
          type: string
        object_key:
          type: string
          description: Object storage key from a prior presigned-upload step
        file_type:
          type: string

  responses:
    BadRequest:
      description: Bad request
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    Unauthorized:
      description: Unauthorized
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    Forbidden:
      description: Forbidden
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    NotFound:
      description: Not found
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    Conflict:
      description: Conflict (e.g. duplicate email)
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    TooManyRequests:
      description: Rate limit exceeded
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    ServiceUnavailable:
      description: Upstream service unavailable
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
