/**
 * ExpenseFormDialog stub -- expenses are not in Phase 12 scope.
 * Kept as a minimal stub to avoid broken imports if referenced elsewhere.
 */

interface ExpenseFormDialogProps {
  open: boolean
  onOpenChange: (open: boolean) => void
}

export function ExpenseFormDialog({ open: _open, onOpenChange: _onOpenChange }: ExpenseFormDialogProps) {
  return null
}
