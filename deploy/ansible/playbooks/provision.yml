---
- name: Provision pilot hosts (foundation + secrets + app-deploy)
  hosts: pilots
  become: yes
  roles:
    - foundation
    - secrets
    - app-deploy

- name: Provision TURN host (foundation + turn)
  hosts: turn
  become: yes
  roles:
    - foundation
    - turn
