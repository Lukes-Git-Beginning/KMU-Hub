all:
  children:
    pilots:
      hosts:
        pilot-0-zfa:
          ansible_host: <PLATZHALTER_IP>
          pilot_name: zfa
          pilot_domain: zfa.zentria.tech
          pilot_segment: dienstleister
          # Pilot-0 (ZFA) — Juli-Provisioning.
          # Naming: pilot-N-<short-name>, N=Onboarding-Reihenfolge.
    turn:
      hosts:
        turn-shared:
          ansible_host: <PLATZHALTER_IP>
          turn_realm: turn.zentria.tech
