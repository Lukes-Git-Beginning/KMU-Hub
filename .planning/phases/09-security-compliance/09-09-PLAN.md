---
phase: 09-security-compliance
plan: 09
type: execute
wave: 4
depends_on: ["09-06", "09-07"]
files_modified:
  - desktop/src/renderer/src/modules/security/PasswordPolicyPage.tsx
  - desktop/src/renderer/src/modules/security/IPAccessPage.tsx
  - desktop/src/renderer/src/modules/security/GDPRExportPage.tsx
  - desktop/src/renderer/src/modules/security/GDPRErasurePage.tsx
  - desktop/src/renderer/src/modules/settings/SettingsPage.tsx
  - desktop/src/renderer/src/modules/settings/SecuritySettingsTab.tsx
  - desktop/src/renderer/src/modules/settings/LanguageSettingsTab.tsx
  - desktop/src/renderer/src/modules/settings/PrivacySettingsTab.tsx
  - desktop/src/renderer/src/components/layout/Sidebar.tsx
  - desktop/src/renderer/src/routes.tsx
autonomous: true

must_haves:
  truths:
    - "Admin can configure password policies"
    - "Admin can manage IP allowlist/blocklist"
    - "User can request DSGVO data export from settings"
    - "Admin can preview and execute data erasure"
    - "User can switch language from settings"
    - "Security pages accessible via sidebar navigation"
  artifacts:
    - path: "desktop/src/renderer/src/modules/settings/SettingsPage.tsx"
      provides: "Settings page with Security, Language, Privacy tabs"
      min_lines: 40
    - path: "desktop/src/renderer/src/modules/settings/LanguageSettingsTab.tsx"
      provides: "Language picker with DE/FR/IT/EN options"
      min_lines: 30
    - path: "desktop/src/renderer/src/modules/security/GDPRExportPage.tsx"
      provides: "DSGVO export request/download page"
      min_lines: 60
    - path: "desktop/src/renderer/src/modules/security/GDPRErasurePage.tsx"
      provides: "Admin erasure preview + execution page"
      min_lines: 80
  key_links:
    - from: "desktop/src/renderer/src/modules/settings/LanguageSettingsTab.tsx"
      to: "desktop/src/renderer/src/hooks/useLocale.ts"
      via: "setLocale on language selection"
      pattern: "setLocale"
    - from: "desktop/src/renderer/src/modules/settings/PrivacySettingsTab.tsx"
      to: "desktop/src/renderer/src/hooks/useSecurity.ts"
      via: "useRequestExport, useGDPRExports"
      pattern: "useRequestExport|useGDPRExports"
    - from: "desktop/src/renderer/src/routes.tsx"
      to: "desktop/src/renderer/src/modules/security"
      via: "Route definitions for security pages"
      pattern: "AuditLogPage|SessionsPage|VaultPage"
---

<objective>
Build DSGVO user settings, password policy admin, IP access control, language switcher, and integrate all security pages into navigation and routing. Cherry-pick and wire design branch components.

Purpose: Completes the frontend for SEC-05, SEC-06, SEC-09, SEC-10, SEC-11 and integrates all Phase 9 UI into the application shell.
Output: All security features accessible from the app, settings page with language/security/privacy tabs.
</objective>

<execution_context>
@C:\Users\Luke\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Luke\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-security-compliance/09-RESEARCH.md
@.planning/phases/09-security-compliance/09-07-SUMMARY.md
@.planning/phases/09-security-compliance/09-08-SUMMARY.md
@desktop/src/renderer/src/routes.tsx
@desktop/src/renderer/src/components/layout/Sidebar.tsx
@desktop/src/renderer/src/modules/settings/DashboardSettings.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Settings page with security, language, privacy tabs</name>
  <files>
    desktop/src/renderer/src/modules/settings/SettingsPage.tsx
    desktop/src/renderer/src/modules/settings/SecuritySettingsTab.tsx
    desktop/src/renderer/src/modules/settings/LanguageSettingsTab.tsx
    desktop/src/renderer/src/modules/settings/PrivacySettingsTab.tsx
  </files>
  <action>
    1. Check if design branch has SettingsPage.tsx and PrivacySettingsTab.tsx to cherry-pick:
       ```bash
       git show design/brainstorm:desktop/src/renderer/src/modules/settings/SettingsPage.tsx 2>/dev/null | head -5
       git show design/brainstorm:desktop/design-reference/ 2>/dev/null
       ```
       If design files exist, cherry-pick and adapt. If not, create from scratch.

    2. Create or update `modules/settings/SettingsPage.tsx`:
       Tab-based settings page with tabs: Allgemein (General/Dashboard), Sicherheit (Security), Sprache (Language), Datenschutz (Privacy).
       Use Radix Tabs component. Each tab renders its sub-component.
       Include existing DashboardSettings.tsx content in the General tab.
       Use react-intl FormattedMessage for all tab labels.

    3. Create `modules/settings/SecuritySettingsTab.tsx`:
       - 2FA section: shows current 2FA status (enabled/disabled), "Enable 2FA" button that opens TwoFactorSetupWizard, "Disable 2FA" button if enabled, "Regenerate Recovery Codes" button if enabled
       - Active sessions section: mini version of sessions list (user's own sessions only), link to full SessionsPage
       - Password section: change password form (current password, new password, confirm), validates against password policy in real-time using useValidatePassword hook. Shows entropy meter.
       - Uses react-intl FormattedMessage throughout

    4. Create `modules/settings/LanguageSettingsTab.tsx`:
       - Language picker with 4 options: Deutsch, English, Francais, Italiano
       - Radio buttons or select dropdown. Current language highlighted.
       - "Auto-detect (Browser)" option that clears locale store
       - On selection: calls useLocale().setLocale(locale)
       - Shows current detected browser language for reference
       - Date format preview: shows current date formatted in selected locale
       - Number format preview: shows "1234.56" formatted in selected locale
       - Uses react-intl FormattedMessage, FormattedDate, FormattedNumber for previews

    5. Create `modules/settings/PrivacySettingsTab.tsx`:
       - Data Export section:
         - "Request Data Export" button (calls useRequestExport)
         - Shows list of user's export requests with status badges (pending/approved/ready/downloaded)
         - "Download" button when status is 'ready' (download ZIP via blob URL)
       - Data Deletion section (info only for regular users):
         - Text explaining that data deletion must be requested through admin
         - If admin: link to GDPRErasurePage
       - Uses react-intl FormattedMessage
  </action>
  <verify>
    - `cd desktop && npx tsc --noEmit` compiles
    - SettingsPage.tsx has 4 tabs
    - LanguageSettingsTab.tsx calls setLocale from useLocale hook
    - SecuritySettingsTab.tsx renders TwoFactorSetupWizard trigger
    - PrivacySettingsTab.tsx uses useRequestExport and useGDPRExports hooks
  </verify>
  <done>Settings page with 4 tabs: General (existing dashboard settings), Security (2FA + sessions + password change), Language (DE/FR/IT/EN picker with format preview), Privacy (data export request + status).</done>
</task>

<task type="auto">
  <name>Task 2: Admin security pages, routing, and navigation</name>
  <files>
    desktop/src/renderer/src/modules/security/PasswordPolicyPage.tsx
    desktop/src/renderer/src/modules/security/IPAccessPage.tsx
    desktop/src/renderer/src/modules/security/GDPRExportPage.tsx
    desktop/src/renderer/src/modules/security/GDPRErasurePage.tsx
    desktop/src/renderer/src/components/layout/Sidebar.tsx
    desktop/src/renderer/src/routes.tsx
  </files>
  <action>
    1. Create `modules/security/PasswordPolicyPage.tsx`:
       Admin-only page to configure password policy:
       - Form with fields: min length (number input), min entropy (number input with explanation), max age days (optional number), reuse prevention count (number)
       - Toggle switches for: require uppercase, require lowercase, require digit, require special character
       - "Save" button (calls useUpdatePasswordPolicy)
       - Info box explaining entropy-based validation vs complexity rules (NIST recommendation)
       - Live preview: test a password against current policy (input + useValidatePassword)
       - Uses react-intl FormattedMessage

    2. Create `modules/security/IPAccessPage.tsx`:
       Admin-only page:
       - Table of existing IP rules: CIDR, type (allow badge green / block badge red), description, created date, delete button
       - "Add Rule" dialog: CIDR input, type dropdown (allow/block), description text input
       - Explanation text: how allowlist vs blocklist works
       - Uses react-intl, useIPRules, useCreateIPRule, useDeleteIPRule hooks

    3. Create `modules/security/GDPRExportPage.tsx`:
       Admin page showing all export requests across all users:
       - Table: user name, status badge, requested date, reviewed by, actions
       - Actions: "Approve" and "Deny" buttons for pending requests. Deny requires a note.
       - Filter by status: all/pending/approved/ready
       - Uses react-intl, useGDPRExports('admin'), useApproveExport, useDenyExport hooks

    4. Create `modules/security/GDPRErasurePage.tsx`:
       Admin page for right-to-erasure:
       - User search/picker (select which user to erase)
       - "Preview Erasure" button (calls usePreviewErasure? or inline via useMutation)
       - Preview display: table of modules with record count and action picker (anonymize/delete dropdown per module) per user decision
       - "Execute Erasure" button with two-step confirmation:
         a. First click shows confirmation dialog with summary
         b. Confirmation dialog requires admin password input (per user decision: confirm with password)
         c. On confirm: calls executeErasure with module_actions map
       - After execution: show success message with anonymized label
       - Uses react-intl, relevant security hooks

    5. Update `components/layout/Sidebar.tsx`:
       Add new navigation items in appropriate sections:
       - Under existing "Einstellungen" or a new "Administration" section:
         - "Sicherheit" (links to security admin overview or directly to audit log)
         - Or add as sub-items: Audit-Log, Sitzungen, Tresor, Passwort-Richtlinie, IP-Zugang, Datenexporte, Datenloeschung
       - Use react-intl FormattedMessage for labels
       - Admin-only items should check role from auth store (show only for admin role)
       - Settings link should point to the new SettingsPage

    6. Update `routes.tsx`:
       Add routes for all new pages:
       - `/settings` -> SettingsPage (with tabs)
       - `/admin/security/audit` -> AuditLogPage
       - `/admin/security/sessions` -> SessionsPage
       - `/admin/security/vault` -> VaultPage
       - `/admin/security/password-policy` -> PasswordPolicyPage
       - `/admin/security/ip-access` -> IPAccessPage
       - `/admin/security/gdpr/exports` -> GDPRExportPage
       - `/admin/security/gdpr/erasure` -> GDPRErasurePage

       Admin routes should be wrapped in a role check (redirect non-admins to dashboard).
       Use lazy loading (React.lazy) for all security pages.

    Also check if design branch has InfrastrukturPage.tsx (security tab) or VaultSettings.tsx to cherry-pick:
    ```bash
    git show design/brainstorm:desktop/src/renderer/src/modules/infrastruktur/InfrastrukturPage.tsx 2>/dev/null | head -10
    git show design/brainstorm:desktop/design-reference/VaultSettings.tsx 2>/dev/null | head -10
    ```
    If design files exist, integrate their visual design into the pages above (keep layout/styling, replace mock data with real hooks).
  </action>
  <verify>
    - `cd desktop && npx tsc --noEmit` compiles
    - `cd desktop && npm run build` succeeds
    - routes.tsx has routes for all 8 new pages (settings + 7 admin security pages)
    - Sidebar.tsx has admin-only security navigation items
    - All pages use react-intl FormattedMessage for visible text
    - GDPRErasurePage has two-step confirmation with password
  </verify>
  <done>Complete admin security pages (password policy, IP access, GDPR exports, GDPR erasure), settings page integrated, sidebar navigation updated, all routes defined with lazy loading.</done>
</task>

</tasks>

<verification>
- `cd desktop && npm run build` succeeds
- Settings page has Security, Language, Privacy tabs
- Language picker switches locale and formatting updates
- Admin pages are role-gated in navigation and routing
- GDPR erasure has two-step confirmation with password
- All visible text uses react-intl
</verification>

<success_criteria>
- User can request DSGVO data export from settings (SEC-05)
- Admin can approve/deny exports and manage erasure (SEC-06)
- Admin can configure password policies (SEC-09)
- Admin can manage IP allowlist/blocklist (SEC-10)
- User can switch language in settings (SEC-11)
- All security pages accessible via sidebar navigation
- Admin pages restricted to admin role
</success_criteria>

<output>
After completion, create `.planning/phases/09-security-compliance/09-09-SUMMARY.md`
</output>
