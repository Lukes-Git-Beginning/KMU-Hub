---
phase: 05-desktop-app-shell
plan: 04
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - desktop/src/renderer/src/modules/chat/ChatLayout.tsx
  - desktop/src/renderer/src/modules/chat/channels/ChannelList.tsx
  - desktop/src/renderer/src/modules/chat/channels/ChannelHeader.tsx
  - desktop/src/renderer/src/modules/chat/channels/CreateChannelDialog.tsx
  - desktop/src/renderer/src/modules/chat/messages/MessageList.tsx
  - desktop/src/renderer/src/modules/chat/messages/MessageInput.tsx
  - desktop/src/renderer/src/modules/chat/messages/MessageBubble.tsx
  - desktop/src/renderer/src/modules/chat/threads/ThreadPanel.tsx
  - desktop/src/renderer/src/modules/notifications/NotificationCenter.tsx
  - desktop/src/renderer/src/modules/notifications/NotificationBell.tsx
  - desktop/src/renderer/src/api/hooks/useChannels.ts
  - desktop/src/renderer/src/api/hooks/useMessages.ts
  - desktop/src/renderer/src/api/hooks/useNotifications.ts
  - desktop/src/renderer/src/components/layout/Header.tsx
autonomous: true

must_haves:
  truths:
    - "User can see a list of chat channels and DMs in the chat module sidebar"
    - "User can select a channel and see its message history"
    - "User can type and send a message that appears in the channel in real time"
    - "User sees typing indicators when another user is typing"
    - "Notification bell in the header shows unread notification count"
    - "Clicking the notification bell opens a dropdown listing recent notifications"
    - "Desktop push notifications appear for new messages and events when app is in background"
  artifacts:
    - path: "desktop/src/renderer/src/modules/chat/ChatLayout.tsx"
      provides: "Chat module with channel list and message area"
      contains: "ChannelList"
    - path: "desktop/src/renderer/src/modules/chat/messages/MessageList.tsx"
      provides: "Scrollable message history"
      contains: "useMessages"
    - path: "desktop/src/renderer/src/modules/chat/messages/MessageInput.tsx"
      provides: "Message composition with send"
      contains: "wsManager.send"
    - path: "desktop/src/renderer/src/modules/notifications/NotificationBell.tsx"
      provides: "Header notification bell with unread count"
      contains: "useNotifications"
    - path: "desktop/src/renderer/src/api/hooks/useChannels.ts"
      provides: "Channel query hooks"
      contains: "useQuery"
    - path: "desktop/src/renderer/src/api/hooks/useMessages.ts"
      provides: "Message query hooks with WebSocket integration"
      contains: "queryClient.setQueryData"
  key_links:
    - from: "desktop/src/renderer/src/modules/chat/messages/MessageInput.tsx"
      to: "desktop/src/renderer/src/api/websocket.ts"
      via: "wsManager.send for message sending"
      pattern: "wsManager\\.send"
    - from: "desktop/src/renderer/src/api/hooks/useMessages.ts"
      to: "desktop/src/renderer/src/api/websocket.ts"
      via: "WebSocket subscription for real-time messages"
      pattern: "wsManager\\.on|useWSSubscription"
    - from: "desktop/src/renderer/src/modules/notifications/NotificationBell.tsx"
      to: "desktop/src/renderer/src/api/hooks/useNotifications.ts"
      via: "unread count query"
      pattern: "useNotificationCount"
---

<objective>
Build the Chat module with real-time messaging and the Notification system with bell indicator and native push notifications. These are the two real-time features that exercise the WebSocket infrastructure.

Purpose: Chat is the second core daily-use module. Together with notifications, this proves the WebSocket integration works end-to-end from backend through the Electron app. Desktop push notifications fulfill DESK-01's real-time requirement and leverage the Phase 4 notification infrastructure.

Output: Working chat with channels, DMs, real-time messages, typing indicators, and thread panel. Notification bell with unread count, notification list, and native desktop push notifications.
</objective>

<execution_context>
@C:\Users\Luke\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Luke\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-desktop-app-shell/05-RESEARCH.md
@.planning/phases/05-desktop-app-shell/05-02-SUMMARY.md
@.planning/phases/04-notifications-gateway/04-03-SUMMARY.md
@desktop/src/renderer/src/api/types.ts
@desktop/src/renderer/src/api/websocket.ts
@backend/api/openapi.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Chat API hooks, channel list, and message area</name>
  <files>
    desktop/src/renderer/src/api/hooks/useChannels.ts
    desktop/src/renderer/src/api/hooks/useMessages.ts
    desktop/src/renderer/src/modules/chat/ChatLayout.tsx
    desktop/src/renderer/src/modules/chat/channels/ChannelList.tsx
    desktop/src/renderer/src/modules/chat/channels/ChannelHeader.tsx
    desktop/src/renderer/src/modules/chat/channels/CreateChannelDialog.tsx
    desktop/src/renderer/src/modules/chat/messages/MessageList.tsx
    desktop/src/renderer/src/modules/chat/messages/MessageInput.tsx
    desktop/src/renderer/src/modules/chat/messages/MessageBubble.tsx
    desktop/src/renderer/src/modules/chat/threads/ThreadPanel.tsx
  </files>
  <action>
    FIRST: Read the OpenAPI spec and generated types.ts to understand exact chat endpoint paths, WebSocket message types, and response shapes. The backend WebSocket supports message types: message.new, message.updated, message.deleted, typing.start, typing.stop, channel.subscribe, channel.unsubscribe, read.receipt, thread.reply.new, file.uploaded.

    1. Create `api/hooks/useChannels.ts`:
       - useChannels(): GET /api/v1/channels (list all channels user is member of)
       - useChannel(id): GET /api/v1/channels/{id}
       - useCreateChannel(): POST /api/v1/channels
       - useJoinChannel(): POST /api/v1/channels/{id}/join
       - useLeaveChannel(): POST /api/v1/channels/{id}/leave
       - useChannelMembers(channelId): GET /api/v1/channels/{id}/members
       - useDMs(): GET /api/v1/channels/dm (list DM conversations)
       - useGetOrCreateDM(): POST /api/v1/channels/dm (create/get DM with specific user)

    2. Create `api/hooks/useMessages.ts`:
       - useMessages(channelId, params?): GET /api/v1/channels/{id}/messages (paginated, newest first)
       - useSendMessage(): POST /api/v1/channels/{id}/messages -- but ALSO send via WebSocket for real-time
       - useEditMessage(): PUT /api/v1/messages/{id}
       - useDeleteMessage(): DELETE /api/v1/messages/{id}
       - useThreadReplies(messageId): GET /api/v1/messages/{id}/thread
       - useMessageWebSocket(channelId): custom hook that:
         a) Subscribes to 'message.new' via wsManager.on
         b) On new message for current channel: append to query cache via queryClient.setQueryData
         c) Subscribes to 'message.updated', 'message.deleted' for cache updates
         d) Sends 'channel.subscribe' on mount, 'channel.unsubscribe' on unmount
         e) Handles 'typing.start' and 'typing.stop' to track who is typing
         f) Handles 'read.receipt' for read status
         g) Returns { typingUsers: string[] }

    3. Create `modules/chat/ChatLayout.tsx`:
       - Three-panel layout: channel list (left, 280px), message area (center, flex-1), thread panel (right, conditional 350px)
       - State: selectedChannelId, selectedThreadMessageId (for thread panel)
       - If no channel selected, show "Select a channel to start chatting"
       - Use flex layout with resizable-like borders

    4. Create `modules/chat/channels/ChannelList.tsx`:
       - Sections: "Channels" header with + button, "Direct Messages" header with + button
       - Channel items: # icon + channel name + unread badge
       - DM items: avatar + user name + unread badge + online dot (if available)
       - Active channel highlighted with bg-slate-100
       - Search/filter input at top
       - "Create Channel" button opens CreateChannelDialog
       - Click channel -> setSelectedChannelId
       - Use useChannels() and useDMs() hooks

    5. Create `modules/chat/channels/ChannelHeader.tsx`:
       - Channel name (# prefix for channels, no prefix for DMs)
       - Member count
       - Channel description (if exists)
       - Actions: members list button, search messages button
       - Typing indicator text at bottom: "User is typing..." or "User1, User2 are typing..."

    6. Create `modules/chat/channels/CreateChannelDialog.tsx`:
       - Dialog with name input, description textarea, is_private toggle
       - Submit creates channel via useCreateChannel mutation
       - On success, close dialog and select new channel

    7. Create `modules/chat/messages/MessageList.tsx`:
       - Reverse-chronological message display (newest at bottom)
       - Infinite scroll: load older messages when scrolling to top (useMessages with cursor-based pagination if supported, else page-based)
       - Group messages by date with date separator
       - Use useMessages(selectedChannelId) hook
       - Use useMessageWebSocket(selectedChannelId) for real-time updates
       - Auto-scroll to bottom on new messages (only if user was already at bottom)
       - Loading skeleton during initial fetch
       - ScrollArea component from shadcn/ui

    8. Create `modules/chat/messages/MessageBubble.tsx`:
       - Props: message object, isOwn boolean
       - Layout: avatar (left), content (right): sender name, timestamp, message text
       - Own messages: slightly different bg (bg-blue-50 vs bg-white)
       - Hover actions: reply (thread), edit (if own), delete (if own)
       - Thread indicator: "N replies" link that opens thread panel
       - File attachments: if message has files, show file cards with name + size + download link
       - Mention highlights: @username rendered with bg-blue-100 text-blue-700
       - Edited indicator: "(edited)" text if message was updated

    9. Create `modules/chat/messages/MessageInput.tsx`:
       - Textarea (auto-growing, max 6 lines) at bottom of message area
       - Send button (disabled when empty)
       - Enter to send, Shift+Enter for newline
       - Send typing indicator via WebSocket: wsManager.send({ type: 'typing.start', channel_id })
       - Debounce typing indicator (send stop after 3 seconds of no input)
       - On submit: call useSendMessage mutation AND/OR wsManager.send message
       - File upload button (for future -- just the button, handler deferred)
       - @mention autocomplete (if time permits: on typing @, show member dropdown. Otherwise, just allow raw @username text)

    10. Create `modules/chat/threads/ThreadPanel.tsx`:
        - Slide-in panel on the right (350px wide, border-left)
        - Header: "Thread" title, parent message preview, close button
        - Thread replies list (similar to MessageList but for thread)
        - Reply input at bottom
        - Use useThreadReplies(messageId) hook
        - Real-time updates via 'thread.reply.new' WebSocket message
        - Only shown when user clicks "N replies" on a message
  </action>
  <verify>
    - `npx tsc --noEmit -p tsconfig.web.json` passes
    - Chat module loads at /chat route
    - Channel list displays channels and DMs
    - Selecting a channel shows message history
    - Sending a message appears in the list (requires running backend + WebSocket)
    - Thread panel opens when clicking reply count
  </verify>
  <done>Complete chat module with channel/DM list, real-time messaging via WebSocket, typing indicators, thread panel, and message composition. All chat features from Phase 3 are now accessible in the Desktop UI.</done>
</task>

<task type="auto">
  <name>Task 2: Notification bell, notification center, and desktop push</name>
  <files>
    desktop/src/renderer/src/api/hooks/useNotifications.ts
    desktop/src/renderer/src/modules/notifications/NotificationBell.tsx
    desktop/src/renderer/src/modules/notifications/NotificationCenter.tsx
    desktop/src/renderer/src/components/layout/Header.tsx
  </files>
  <action>
    FIRST: Read the OpenAPI spec for notification endpoints (added in Phase 4). The WebSocket delivers notification.new, notification.read, notification.read_all, notification.unread_count messages.

    1. Create `api/hooks/useNotifications.ts`:
       - useNotifications(params?): GET /api/v1/notifications (paginated list)
       - useUnreadCount(): GET /api/v1/notifications/unread-count
       - useMarkRead(): POST /api/v1/notifications/{id}/read mutation
       - useMarkAllRead(): POST /api/v1/notifications/read-all mutation
       - useNotificationPreferences(): GET /api/v1/notifications/preferences
       - useUpdatePreference(): PUT /api/v1/notifications/preferences mutation
       - useNotificationWebSocket(): custom hook that:
         a) Subscribes to 'notification.new' -> invalidate notifications query, increment unread count in cache
         b) Subscribes to 'notification.unread_count' -> update count in cache
         c) On 'notification.new': also trigger native desktop notification via window.electronAPI.showNativeNotification(title, body)
         d) Returns nothing (side-effect hook)

    2. Create `modules/notifications/NotificationBell.tsx`:
       - Bell icon (from lucide-react) with unread count badge
       - Badge: small red circle with count number (hide if count is 0)
       - Click toggles a popover/dropdown (use Popover from shadcn/ui):
         - Header: "Benachrichtigungen" with "Alle als gelesen markieren" link
         - List of recent notifications (max 10 in dropdown)
         - Each notification: icon by type (message, deal, mention, etc.), title, body preview, time ago, read/unread dot
         - Click notification: mark as read + navigate to relevant page
         - Footer: "Alle anzeigen" link to /notifications full page
       - Use useUnreadCount() for badge, useNotifications({ limit: 10 }) for dropdown
       - Use useNotificationWebSocket() for real-time updates

    3. Create `modules/notifications/NotificationCenter.tsx` (full page at /notifications):
       - Full list of all notifications with infinite scroll or pagination
       - Filter tabs: All, Unread
       - Each notification: similar card to bell dropdown but larger, with more detail
       - Batch actions: Mark all as read
       - Notification preferences section (or link to settings):
         - Per-event-type toggles (mute channel mentions, deal updates, etc.)
         - Quiet hours configuration
       - Use useNotifications, useMarkRead, useMarkAllRead, useNotificationPreferences hooks
       - Empty state: "Keine Benachrichtigungen" with illustration

    4. Update `components/layout/Header.tsx`:
       - Replace the placeholder notification bell with the NotificationBell component
       - Ensure NotificationBell is positioned correctly in the header (right side, before user dropdown)
       - Initialize useNotificationWebSocket() in the Header (or AppShell) so notification listening is always active

    5. Wire up native desktop notifications:
       - In the useNotificationWebSocket hook, when a 'notification.new' event arrives:
         a) Update query cache (unread count, notification list)
         b) If app is not focused (document.hasFocus() === false), call window.electronAPI.showNativeNotification(notification.title, notification.body)
         c) This uses the Electron Notification API (registered in main process IPC from 05-01)
  </action>
  <verify>
    - `npx tsc --noEmit -p tsconfig.web.json` passes
    - Notification bell visible in header with badge
    - Clicking bell opens popover with notification list
    - /notifications route shows full notification center
    - Native desktop notification appears when app receives notification.new event and is not focused (requires running backend with notification system)
  </verify>
  <done>Notification bell in header showing real-time unread count, dropdown with recent notifications, full notification center page with preferences, and native Electron push notifications for background events.</done>
</task>

</tasks>

<verification>
1. Navigate to /chat -> see channel list on left, message area on right
2. Click a channel -> messages load from backend
3. Type a message and press Enter -> message appears (requires running backend)
4. Typing indicator shows when another user types (requires two sessions)
5. Click "N replies" -> thread panel slides open on right
6. Notification bell shows unread count badge in header
7. Click notification bell -> dropdown shows recent notifications
8. Navigate to /notifications -> full notification center with filter tabs
9. When app is in background, native OS notification appears for new events
</verification>

<success_criteria>
- Chat module has channel list, message area, real-time messaging, typing indicators, and thread panel
- Notification bell in header shows real-time unread count via WebSocket
- Notification dropdown lists recent notifications with mark-as-read
- Notification center page has full list, filters, and preferences
- Native Electron push notifications fire for background events
- All real-time features use the WebSocket manager from 05-02
</success_criteria>

<output>
After completion, create `.planning/phases/05-desktop-app-shell/05-04-SUMMARY.md`
</output>
