---
phase: 12-rechnungen-finanzen
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - backend/internal/biz/quote/service.go
  - backend/internal/biz/quote/repository.go
  - backend/internal/biz/quote/postgres_repository.go
  - backend/internal/biz/quote/errors.go
  - backend/internal/biz/invoice/service.go
  - backend/internal/biz/invoice/repository.go
  - backend/internal/biz/invoice/postgres_repository.go
  - backend/internal/biz/invoice/errors.go
  - backend/internal/biz/creditnote/service.go
  - backend/internal/biz/creditnote/repository.go
  - backend/internal/biz/creditnote/postgres_repository.go
  - backend/internal/biz/creditnote/errors.go
  - backend/internal/biz/payment/service.go
  - backend/internal/biz/payment/repository.go
  - backend/internal/biz/payment/postgres_repository.go
  - backend/internal/biz/dunning/service.go
  - backend/internal/biz/dunning/repository.go
  - backend/internal/biz/dunning/postgres_repository.go
  - backend/internal/biz/dashboard/service.go
  - backend/internal/biz/dashboard/repository.go
  - backend/internal/biz/pdf/generator.go
  - backend/internal/biz/pdf/templates.go
autonomous: true
requirements:
  - FIN-01
  - FIN-02
  - FIN-04
  - FIN-07

must_haves:
  truths:
    - "User can create, update, and send quotes with line items and tax calculation"
    - "Invoices become immutable (JSONB snapshot) when sent, with gap-free sequential numbering"
    - "Credit notes reference an original invoice and use their own GS-prefix number sequence"
    - "Payments record against invoices and auto-transition status to paid when fully settled"
    - "Dunning records are created as drafts with configurable fees and Verzugszinsen"
    - "PDF generation produces clean A4 documents with Pflichtangaben in footer"
  artifacts:
    - path: "backend/internal/biz/quote/service.go"
      provides: "Quote CRUD with expiry, status transitions, tax calculation"
      exports: ["Service", "NewService"]
    - path: "backend/internal/biz/invoice/service.go"
      provides: "Invoice CRUD with GoBD immutability, sequential numbering, snapshot"
      exports: ["Service", "NewService"]
    - path: "backend/internal/biz/creditnote/service.go"
      provides: "Credit note CRUD with original invoice reference validation"
      exports: ["Service", "NewService"]
    - path: "backend/internal/biz/payment/service.go"
      provides: "Payment recording with auto-status transition"
      exports: ["Service", "NewService"]
    - path: "backend/internal/biz/dunning/service.go"
      provides: "3-level dunning with configurable intervals and fees"
      exports: ["Service", "NewService"]
    - path: "backend/internal/biz/pdf/generator.go"
      provides: "PDF generation for quotes, invoices, credit notes, dunning letters"
      exports: ["Generator", "NewGenerator"]
  key_links:
    - from: "backend/internal/biz/invoice/service.go"
      to: "backend/internal/biz/tax/calculator.go"
      via: "tax.Calculate() called during invoice create/update"
      pattern: "tax\\.Calculate"
    - from: "backend/internal/biz/invoice/service.go"
      to: "finance_number_sequences table"
      via: "SELECT FOR UPDATE for gap-free numbering"
      pattern: "NextInvoiceNumber"
    - from: "backend/internal/biz/payment/service.go"
      to: "backend/internal/biz/invoice/service.go"
      via: "invoice status transition on full payment"
      pattern: "invoiceRepo"
---

<objective>
Implement all business logic service packages for the finance domain: quote, invoice, credit note, payment, dunning, dashboard, and PDF generation.

Purpose: This is the core business logic layer -- thick services that enforce GoBD compliance (immutability, sequential numbering), calculate taxes via the tax engine from 12-01, generate PDFs with Pflichtangaben, and manage payment/dunning workflows.

Output: Complete service layer ready to be wired into gRPC server in plan 12-03.
</objective>

<execution_context>
@C:/Users/Luke/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Luke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-rechnungen-finanzen/12-RESEARCH.md
@.planning/phases/12-rechnungen-finanzen/12-CONTEXT.md
@.planning/phases/12-rechnungen-finanzen/12-01-SUMMARY.md
@backend/internal/models/finance.go
@backend/internal/biz/tax/calculator.go
@backend/internal/crm/deal/service.go
@backend/internal/crm/contact/service.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Quote, invoice, credit note, and payment service packages</name>
  <files>
    backend/internal/biz/quote/service.go
    backend/internal/biz/quote/repository.go
    backend/internal/biz/quote/postgres_repository.go
    backend/internal/biz/quote/errors.go
    backend/internal/biz/invoice/service.go
    backend/internal/biz/invoice/repository.go
    backend/internal/biz/invoice/postgres_repository.go
    backend/internal/biz/invoice/errors.go
    backend/internal/biz/creditnote/service.go
    backend/internal/biz/creditnote/repository.go
    backend/internal/biz/creditnote/postgres_repository.go
    backend/internal/biz/creditnote/errors.go
    backend/internal/biz/payment/service.go
    backend/internal/biz/payment/repository.go
    backend/internal/biz/payment/postgres_repository.go
  </files>
  <action>
    Create the four core service packages following the established repository pattern (interface + postgres implementation + service + errors).

    **Quote package (`internal/biz/quote/`):**

    Repository interface methods: Create, GetByID, List (with filters: status, date range, deal_id), Update, Delete, UpdateStatus, GetByDealID.

    PostgresRepository: Standard pgx implementation. List supports filtering by tenant_id + optional status, date range, deal_id. Uses JSONB for line_items storage (json.Marshal/Unmarshal with models.LineItem slice).

    Service methods:
    - `Create(ctx, input CreateInput) (*models.Quote, error)` -- validates line items non-empty, calls tax.Calculate(), stores results, sets valid_until from company_settings default_quote_validity_days. CreateInput has: CustomerName, CustomerAddress, CustomerEmail, CustomerUstIDNr, TaxMode, LineItems []models.LineItem, ValidUntil *time.Time, Notes, DealID *uuid.UUID, UserID.
    - `GetByID(ctx, id) (*models.Quote, error)`
    - `List(ctx, filter ListFilter) ([]*models.Quote, int, error)` -- ListFilter: Status, DealID, DateFrom, DateTo, Limit, Offset.
    - `Update(ctx, id, input UpdateInput) (*models.Quote, error)` -- only if status=draft. Recalculates tax.
    - `Delete(ctx, id) error` -- only if status=draft.
    - `Send(ctx, id, userID) error` -- transitions draft->sent, assigns quote number via NextNumber (prefix "AN", e.g., AN-2026-0001). Uses the same number sequence pattern as invoices.
    - `Accept(ctx, id) error` -- sent->accepted
    - `Reject(ctx, id) error` -- sent->rejected
    - `ExpireQuotes(ctx) (int, error)` -- bulk update: all sent quotes past valid_until -> expired

    Errors: ErrQuoteNotFound, ErrQuoteNotDraft, ErrQuoteNotSent, ErrNoLineItems.

    **Invoice package (`internal/biz/invoice/`):**

    Repository interface methods: Create, GetByID, List, Update, SendInvoice (sets number, snapshot, status), UpdateStatus, NextInvoiceNumber (gap-free via FOR UPDATE on finance_number_sequences), GetOverdue, GetByQuoteID.

    NextInvoiceNumber implementation (CRITICAL for GoBD):
    ```sql
    BEGIN;
    SELECT current_number FROM finance_number_sequences
      WHERE tenant_id = $1 AND document_type = 'invoice' AND fiscal_year = $2
      FOR UPDATE;
    UPDATE finance_number_sequences SET current_number = current_number + 1
      WHERE tenant_id = $1 AND document_type = 'invoice' AND fiscal_year = $2;
    COMMIT;
    -- Returns: RE-{year}-{padded_number} e.g., RE-2026-0001
    ```
    If no sequence row exists for the fiscal year, INSERT it with current_number=1 within the same transaction.

    Service methods:
    - `Create(ctx, input CreateInput) (*models.Invoice, error)` -- validates, calculates tax, sets due_date from payment terms. CreateInput: CustomerName/Address/Email/UstIDNr, TaxMode, LineItems, InvoiceDate, DeliveryDate, PaymentTermsDays, Notes, SourceQuoteID, UserID.
    - `GetByID(ctx, id) (*models.Invoice, error)`
    - `List(ctx, filter) ([]*models.Invoice, int, error)` -- filter by status, date range, overdue flag.
    - `Update(ctx, id, input) (*models.Invoice, error)` -- ONLY if status=draft. Recalculates tax. If status != draft, return ErrInvoiceImmutable (GoBD enforcement).
    - `Send(ctx, id, userID, companySettings) error` -- transitions draft->sent. Assigns sequential number. Builds complete snapshot (line items, tax breakdown, customer data, company data) as JSONB. Sets invoice_date if not set. After this, the invoice is IMMUTABLE.
    - `MarkPaid(ctx, id) error` -- sent/overdue->paid. Sets status to paid.
    - `Cancel(ctx, id, userID) error` -- draft/sent->cancelled. Cannot cancel paid invoices.
    - `DetectOverdue(ctx) (int, error)` -- bulk: all sent invoices past due_date -> overdue.
    - `CreateFromQuote(ctx, quoteID, userID) (*models.Invoice, error)` -- copy-and-link: reads quote, creates invoice with source_quote_id set, copies line items and customer data. Quote status must be "accepted". This is the quote-to-invoice conversion per user decision (copy-and-link pattern).

    Errors: ErrInvoiceNotFound, ErrInvoiceImmutable, ErrInvoiceNotDraft, ErrInvoiceAlreadyPaid, ErrInvoiceAlreadyCancelled.

    **CreditNote package (`internal/biz/creditnote/`):**

    Repository interface: Create, GetByID, List, SendCreditNote (assigns number), GetByInvoiceID.

    Service methods:
    - `Create(ctx, input) (*models.CreditNote, error)` -- validates original_invoice_id exists and is sent/paid/overdue (not draft/cancelled). Copies customer data from original invoice. Calculates tax. Input: OriginalInvoiceID, LineItems, TaxMode, Reason, UserID.
    - `GetByID(ctx, id) (*models.CreditNote, error)`
    - `List(ctx, filter) ([]*models.CreditNote, int, error)`
    - `Send(ctx, id, userID) error` -- draft->sent. Assigns credit note number via NextNumber (prefix "GS", e.g., GS-2026-0001) using the same sequence table with document_type='credit_note'.

    Errors: ErrCreditNoteNotFound, ErrCreditNoteNotDraft, ErrInvalidInvoiceForCredit.

    **Payment package (`internal/biz/payment/`):**

    Repository interface: Create, List (by invoice_id), Delete, SumByInvoiceID.

    Service methods:
    - `Record(ctx, input) (*models.Payment, error)` -- records payment. After recording, checks if sum of payments >= invoice gross_total. If so, calls invoice status transition to "paid". Input: InvoiceID, Amount (decimal), Date, Method, Reference, Notes, UserID.
    - `List(ctx, invoiceID) ([]*models.Payment, error)`
    - `Delete(ctx, id) error` -- only if invoice is not in a finalized state that prevents modification. After deletion, re-check if invoice should revert from paid to sent/overdue.

    All service constructors take: repository + any cross-service dependencies (e.g., payment.Service needs invoice repository for status check). Use interface injection, not concrete types. Follow the exact pattern from CRM deal/contact services.
  </action>
  <verify>
    - `cd backend && go build ./internal/biz/quote/` compiles
    - `cd backend && go build ./internal/biz/invoice/` compiles
    - `cd backend && go build ./internal/biz/creditnote/` compiles
    - `cd backend && go build ./internal/biz/payment/` compiles
    - `cd backend && go vet ./internal/biz/...` passes
  </verify>
  <done>
    Quote service supports full lifecycle (create, send, accept, reject, expire) with tax calculation. Invoice service enforces GoBD immutability (snapshot at send time, gap-free sequential numbering via SELECT FOR UPDATE). Credit notes reference original invoices with own GS-prefix numbering. Payment recording auto-transitions invoice status to paid when fully settled.
  </done>
</task>

<task type="auto">
  <name>Task 2: Dunning + dashboard services + PDF generator</name>
  <files>
    backend/internal/biz/dunning/service.go
    backend/internal/biz/dunning/repository.go
    backend/internal/biz/dunning/postgres_repository.go
    backend/internal/biz/dunning/errors.go
    backend/internal/biz/dashboard/service.go
    backend/internal/biz/dashboard/repository.go
    backend/internal/biz/dashboard/postgres_repository.go
    backend/internal/biz/pdf/generator.go
    backend/internal/biz/pdf/templates.go
  </files>
  <action>
    **Dunning package (`internal/biz/dunning/`):**

    Repository interface: Create, GetByID, List (by invoice_id, by status), UpdateStatus, GetConfig, UpdateConfig, GetOverdueInvoicesForDunning.

    Service methods:
    - `GetConfig(ctx, tenantID) (*models.DunningConfig, error)` -- returns config or creates default (14/14/14 days intervals, 0/5/10 EUR fees per user decision: admin-configurable, no hardcoded defaults). If no config exists, create one with these defaults.
    - `UpdateConfig(ctx, tenantID, input) (*models.DunningConfig, error)` -- admin updates intervals and fees.
    - `DetectAndCreateDunnings(ctx, tenantID) ([]*models.DunningRecord, error)` -- semi-automatic per user decision: finds overdue invoices, checks existing dunning level, creates next-level draft dunning if interval elapsed. Logic:
      - Invoice overdue + no dunning yet + days_overdue >= level1_days -> create Level 1 draft
      - Invoice has Level 1 sent + days_since_level1_sent >= level2_days -> create Level 2 draft
      - Invoice has Level 2 sent + days_since_level2_sent >= level3_days -> create Level 3 draft
      - Never auto-creates if max level (3) already sent
    - `Send(ctx, id, userID) error` -- draft->sent, sets sent_at timestamp
    - `List(ctx, filter) ([]*models.DunningRecord, error)` -- filter by invoice_id, status, level
    - `CalculateInterest(ctx, invoiceGrossTotal decimal.Decimal, dueDate time.Time, basiszinssatz decimal.Decimal, isB2B bool) decimal.Decimal` -- Verzugszinsen per BGB section 288: B2C = basiszinssatz + 5%, B2B = basiszinssatz + 9%. Pro-rata daily calculation from due_date to today.

    Errors: ErrDunningNotFound, ErrDunningNotDraft, ErrDunningMaxLevel.

    **Dashboard package (`internal/biz/dashboard/`):**

    Repository interface: GetDashboardMetrics (complex aggregation query).

    PostgresRepository: Single query (or 2-3 queries) that returns:
    - Revenue metrics: SUM of gross_total for invoices WHERE status IN ('sent','paid','overdue') (total_invoiced), SUM WHERE status='paid' (total_paid), SUM WHERE status IN ('sent','overdue') (total_outstanding), SUM WHERE status='overdue' (overdue_amount)
    - Pipeline metrics: COUNT of quotes WHERE status='sent' (quotes_pending), conversion rate (quotes accepted / quotes total), AVG invoice gross_total (average_deal_size)
    - Status breakdown: COUNT by invoice status (draft, sent, overdue, paid, cancelled)
    - Use date range filter (DateFrom, DateTo) from the request.
    - Also return: recent invoices (last 10), expiring quotes (valid_until within 7 days), pending dunnings (status=draft).

    Service: `GetDashboard(ctx, tenantID, dateFrom, dateTo) (*models.FinanceDashboard, error)` -- thin wrapper around repository, adds revenue forecast (simple: average monthly revenue * remaining months in year).

    **PDF Generator (`internal/biz/pdf/`):**

    Use maroto/v2 (github.com/johnfercher/maroto/v2). Install with `go get github.com/johnfercher/maroto/v2@latest`.

    `generator.go`:
    - `type Generator struct` with company settings for defaults.
    - `NewGenerator(settings models.CompanySettings) *Generator`
    - `GenerateQuotePDF(quote models.Quote) ([]byte, error)` -- returns PDF bytes
    - `GenerateInvoicePDF(invoice models.Invoice) ([]byte, error)` -- uses snapshot_data if available (sent invoice), otherwise live data (draft preview)
    - `GenerateCreditNotePDF(creditNote models.CreditNote) ([]byte, error)`
    - `GenerateDunningPDF(dunning models.DunningRecord, invoice models.Invoice, level int) ([]byte, error)`

    `templates.go` -- shared PDF layout functions:

    Per user decisions (clean minimal Stripe/Lexoffice aesthetic):
    - A4 portrait, 15mm margins
    - Header: company logo (left, loaded from URL or placeholder) + company details right-aligned (name, address, Handelsregister)
    - Recipient block: customer name + address (positioned per DIN 5008 window envelope standard if practical, otherwise top-left below header)
    - Document title: "Angebot" / "Rechnung" / "Gutschrift" / "Zahlungserinnerung" / "1. Mahnung" / "2. Mahnung" with document number and date
    - Line item table columns per user decision: Position | Bezeichnung | Menge | Einzelpreis | Gesamtpreis
    - Below table: Subtotal (Zwischensumme), per-rate tax lines (e.g., "MwSt 19%: 45.60 EUR", "MwSt 7%: 3.50 EUR"), Gesamtbetrag (bold)
    - For Reverse Charge: note "Steuerschuldnerschaft des Leistungsempfaengers (section 13b UStG)" below totals
    - For Kleinunternehmer: note "Kein Ausweis von Umsatzsteuer, da Kleinunternehmer gemaess section 19 UStG" below totals
    - Payment terms and notes section
    - Footer (every page): 3-column layout with Handelsregister | Steuernummer/USt-IdNr | Bank IBAN/BIC (Pflichtangaben per UStG section 14)
    - Accent color from company settings for horizontal rules and headings only
    - Font: Use maroto's built-in Helvetica (closest to sans-serif; custom font embedding is a stretch goal)
    - Thin 0.5pt borders on table, subtle row alternation if maroto supports it

    For dunning letters, tone varies by level per user decision:
    - Level 1 (Zahlungserinnerung): friendly, "wir moechten Sie hoeflich daran erinnern..."
    - Level 2 (1. Mahnung): formal, "trotz unserer Erinnerung..."
    - Level 3 (2. Mahnung / Letzte Mahnung): urgent, "sollte keine Zahlung erfolgen, sehen wir uns gezwungen, ein Inkassounternehmen zu beauftragen"
    - Include: Mahngebuehr (fee), Verzugszinsen (interest), Gesamtforderung (original + fee + interest)
  </action>
  <verify>
    - `cd backend && go build ./internal/biz/dunning/` compiles
    - `cd backend && go build ./internal/biz/dashboard/` compiles
    - `cd backend && go build ./internal/biz/pdf/` compiles
    - `cd backend && go vet ./internal/biz/...` passes
  </verify>
  <done>
    Dunning service supports 3-level semi-automatic dunning with configurable intervals, fees, and Verzugszinsen calculation. Dashboard aggregates revenue, pipeline, and status metrics with date range filtering. PDF generator creates clean A4 documents for quotes, invoices, credit notes, and dunning letters with all Pflichtangaben in footer, accent color theming, and proper tax mode notes.
  </done>
</task>

</tasks>

<verification>
- All 7 service packages compile: `cd backend && go build ./internal/biz/...`
- `go vet ./internal/biz/...` passes
- Quote service has Create, Send, Accept, Reject, ExpireQuotes methods
- Invoice service has Create, Send (with snapshot + sequential number), MarkPaid, Cancel, DetectOverdue, CreateFromQuote methods
- CreditNote service has Create, Send (with GS-prefix number) methods
- Payment service has Record (with auto-paid transition) method
- Dunning service has DetectAndCreateDunnings, CalculateInterest methods
- PDF generator has methods for all 4 document types
</verification>

<success_criteria>
All finance domain services are complete with proper GoBD enforcement (immutable sent invoices, gap-free numbering), tax calculation integration, 3-level dunning with configurable intervals, payment auto-status transitions, dashboard aggregation, and clean PDF generation with Pflichtangaben. The service layer is ready for gRPC wiring.
</success_criteria>

<output>
After completion, create `.planning/phases/12-rechnungen-finanzen/12-02-SUMMARY.md`
</output>
