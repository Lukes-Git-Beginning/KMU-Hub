---
phase: 12-rechnungen-finanzen
plan: 03
type: execute
wave: 3
depends_on:
  - 12-02
files_modified:
  - backend/internal/server/biz_grpc.go
  - backend/internal/gateway/route_biz.go
  - backend/internal/biz/datev/exporter.go
  - backend/internal/biz/datev/mapping.go
  - backend/internal/biz/datev/exporter_test.go
  - backend/cmd/biz/main.go
  - backend/cmd/gateway/main.go
  - backend/api/openapi.yaml
  - deploy/docker/docker-compose.yml
  - deploy/docker/Dockerfile.biz
autonomous: true
requirements:
  - FIN-05
  - FIN-06

must_haves:
  truths:
    - "Biz gRPC server exposes all FinanceService RPCs and maps domain errors to gRPC status codes"
    - "Gateway proxies ~30 HTTP routes under /api/v1/finance/* to the biz gRPC service"
    - "DATEV Buchungsstapel CSV export produces valid EXTF-format file with semicolon delimiter and correct header"
    - "Deal-to-quote-to-invoice flow works end-to-end via CRM gRPC + biz service"
    - "Biz service runs in Docker alongside all other services"
  artifacts:
    - path: "backend/internal/server/biz_grpc.go"
      provides: "gRPC server implementation for FinanceService"
      exports: ["BizGRPCServer", "NewBizGRPCServer"]
    - path: "backend/internal/gateway/route_biz.go"
      provides: "HTTP route registrar for finance endpoints"
      exports: ["BizRoutes", "NewBizRoutes"]
    - path: "backend/internal/biz/datev/exporter.go"
      provides: "DATEV Buchungsstapel CSV export in EXTF format"
      exports: ["Exporter", "NewExporter"]
    - path: "deploy/docker/Dockerfile.biz"
      provides: "Docker image for biz service"
      contains: "FROM golang"
  key_links:
    - from: "backend/internal/server/biz_grpc.go"
      to: "backend/internal/biz/*/service.go"
      via: "service method calls from gRPC handlers"
      pattern: "quoteService|invoiceService|creditNoteService"
    - from: "backend/internal/gateway/route_biz.go"
      to: "proto/biz/v1/biz_grpc.pb.go"
      via: "gRPC client calls"
      pattern: "bizv1\\.FinanceServiceClient"
    - from: "backend/cmd/gateway/main.go"
      to: "backend/internal/gateway/route_biz.go"
      via: "BizRoutes registered in gateway"
      pattern: "NewBizRoutes"
---

<objective>
Wire the business logic services to the network layer: gRPC server, HTTP gateway routes, DATEV export, deal-to-quote CRM integration, Docker deployment, and OpenAPI documentation.

Purpose: Makes all finance services accessible via HTTP API, enables the frontend to consume them, adds the DATEV export feature for Steuerberater, and connects the CRM deal pipeline to the quote/invoice flow.

Output: Fully functional biz service accessible via gateway, Docker-deployable, with DATEV export and CRM integration.
</objective>

<execution_context>
@C:/Users/Luke/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Luke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-rechnungen-finanzen/12-RESEARCH.md
@.planning/phases/12-rechnungen-finanzen/12-01-SUMMARY.md
@.planning/phases/12-rechnungen-finanzen/12-02-SUMMARY.md
@backend/internal/server/document_grpc.go
@backend/internal/gateway/route_document.go
@backend/internal/gateway/route_registrar.go
@backend/cmd/gateway/main.go
@deploy/docker/docker-compose.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: DATEV exporter + gRPC server + biz main.go completion</name>
  <files>
    backend/internal/biz/datev/exporter.go
    backend/internal/biz/datev/mapping.go
    backend/internal/biz/datev/exporter_test.go
    backend/internal/server/biz_grpc.go
    backend/cmd/biz/main.go
  </files>
  <action>
    **DATEV Exporter (`internal/biz/datev/`):**

    `mapping.go` -- SKR03 account mapping per research discretion recommendation:
    - Revenue 19%: account 8400 (Erloese 19% USt)
    - Revenue 7%: account 8300 (Erloese 7% USt)
    - Revenue 0% (EU): account 8125 (steuerfreie innergemeinschaftliche Lieferungen)
    - Revenue 0% (Kleinunternehmer): account 8195 (steuerfreie Umsaetze Inland)
    - Debitor accounts: 10000+ range (auto-generated per customer, using customer index)
    - BU-Schluessel: 3 for 19% USt, 2 for 7% USt, empty for 0%
    - Define as Go constants/maps.

    `exporter.go`:
    - `type Exporter struct` with fiscal year config
    - `NewExporter() *Exporter`
    - `Export(invoices []*models.Invoice, creditNotes []*models.CreditNote, fiscalYearStart time.Time, generatedAt time.Time) ([]byte, error)` -- returns CSV bytes

    CSV generation using `encoding/csv` with semicolon delimiter (`csv.Writer` with `Comma = ';'`):
    1. Write EXTF header line (line 1) per research code example -- format version 700, data category 21, "Buchungsstapel", format version 13, timestamp, source "KMU Hub", fiscal year start, account length 4.
    2. Write column header line (line 2): Umsatz (ohne Soll/Haben-Kz), Soll/Haben-Kennzeichen, WKZ Umsatz, Kurs, Basis-Umsatz, WKZ Basis-Umsatz, Konto, Gegenkonto (ohne BU-Schluessel), BU-Schluessel, Belegdatum, Belegfeld 1, Belegfeld 2, Skonto, Buchungstext, ...
    3. For each invoice (status=sent/paid/overdue): create booking records. Each line item becomes a booking line:
       - Umsatz: gross amount of line (net + tax)
       - S/H: "S" (Soll = debit) for revenue bookings
       - WKZ: "EUR"
       - Konto: debitor account (10000 + customer index)
       - Gegenkonto: revenue account (8400/8300/8125 based on tax rate)
       - BU-Schluessel: 3/2/empty based on rate
       - Belegdatum: invoice_date as DDMM (4 digits, no separator)
       - Belegfeld 1: invoice_number
       - Buchungstext: line item description (truncated to 60 chars)
    4. For credit notes: same pattern but with "H" (Haben = credit) and negative amounts.

    Encoding: UTF-8 with BOM (write `\xEF\xBB\xBF` prefix) for DATEV compatibility.

    `exporter_test.go` -- test cases:
    1. Single invoice with 1 line item at 19% -> verify CSV output contains correct accounts, amounts, BU-Schluessel
    2. Invoice with mixed rates (19% + 7%) -> verify 2 booking lines with correct accounts
    3. Credit note -> verify "H" Soll/Haben and negative amount handling
    4. Empty input -> verify header only, no booking lines
    5. EXTF header format -> verify line 1 fields match specification

    **gRPC Server (`internal/server/biz_grpc.go`):**

    Follow exact pattern from `document_grpc.go`:

    ```go
    type BizGRPCServer struct {
        bizv1.UnimplementedFinanceServiceServer
        quoteService      *quote.Service
        invoiceService    *invoice.Service
        creditNoteService *creditnote.Service
        paymentService    *payment.Service
        dunningService    *dunning.Service
        dashboardService  *dashboard.Service
        pdfGenerator      *pdf.Generator
        datevExporter     *datev.Exporter
        companySettingsRepo // for company settings CRUD
    }
    ```

    Implement all ~35 RPCs. Each handler:
    1. Extract request fields from protobuf message
    2. Convert proto types to domain types (string -> decimal.Decimal, proto timestamp -> time.Time, proto UUID string -> uuid.UUID)
    3. Call service method
    4. Convert domain result back to proto response
    5. Map errors: ErrNotFound -> codes.NotFound, ErrImmutable -> codes.FailedPrecondition, ErrNotDraft -> codes.FailedPrecondition, validation errors -> codes.InvalidArgument

    For PDF RPCs (GenerateQuotePDF, GenerateInvoicePDF, etc.): call pdf.Generator, return bytes in response.
    For DATEV RPC: call datev.Exporter.Export(), return CSV bytes + filename + record count.

    Company settings RPCs: direct repository calls (thin, no complex business logic -- just CRUD for admin configuration).

    **Complete `cmd/biz/main.go`:**

    Update the scaffold from 12-01 to wire all services together:
    1. Initialize all repositories (quote, invoice, creditnote, payment, dunning, dashboard, company settings)
    2. Initialize tax calculator (stateless, no deps)
    3. Initialize services with repo + cross-service deps:
       - quote.NewService(quoteRepo, taxCalc, numberSeqRepo, companySettingsRepo)
       - invoice.NewService(invoiceRepo, taxCalc, numberSeqRepo, companySettingsRepo)
       - creditnote.NewService(creditNoteRepo, invoiceRepo, taxCalc, numberSeqRepo)
       - payment.NewService(paymentRepo, invoiceRepo)
       - dunning.NewService(dunningRepo, invoiceRepo, paymentRepo, companySettingsRepo)
       - dashboard.NewService(dashboardRepo)
    4. Initialize PDF generator with company settings
    5. Initialize DATEV exporter
    6. Create BizGRPCServer with all services
    7. Register: `bizv1.RegisterFinanceServiceServer(grpcServer, bizGRPC)`
    8. Start gRPC on BizGRPCPort, health on BizHealthPort
  </action>
  <verify>
    - `cd backend && go test ./internal/biz/datev/ -v` -- all DATEV tests pass
    - `cd backend && go build ./internal/server/` compiles (biz_grpc.go)
    - `cd backend && go build ./cmd/biz/` compiles with all services wired
    - `cd backend && go vet ./internal/server/` passes
    - `cd backend && go vet ./cmd/biz/` passes
  </verify>
  <done>
    DATEV exporter produces valid EXTF CSV with correct header, semicolon delimiter, SKR03 account mapping, and UTF-8 BOM. gRPC server implements all ~35 FinanceService RPCs with proper error mapping. Biz binary initializes all service packages and starts on :50058.
  </done>
</task>

<task type="auto">
  <name>Task 2: Gateway routes + Docker + OpenAPI + CRM deal integration</name>
  <files>
    backend/internal/gateway/route_biz.go
    backend/cmd/gateway/main.go
    backend/api/openapi.yaml
    deploy/docker/docker-compose.yml
    deploy/docker/Dockerfile.biz
  </files>
  <action>
    **Gateway route registrar (`internal/gateway/route_biz.go`):**

    Follow exact pattern from `route_document.go`:

    ```go
    type BizRoutes struct {
        registry *ServiceRegistry
    }
    func NewBizRoutes(registry *ServiceRegistry) *BizRoutes
    func (b *BizRoutes) ServiceName() string { return "biz" }
    func (b *BizRoutes) getBizClient() (bizv1.FinanceServiceClient, error)
    func (b *BizRoutes) RegisterRoutes(r chi.Router, authMiddleware func(http.Handler) http.Handler)
    ```

    HTTP routes under `/api/v1/finance`:

    **Company Settings:**
    - GET /api/v1/finance/settings -- GetCompanySettings (admin/manager)
    - PUT /api/v1/finance/settings -- UpdateCompanySettings (admin only)

    **Quotes:**
    - POST /api/v1/finance/quotes -- CreateQuote
    - GET /api/v1/finance/quotes -- ListQuotes (query params: status, deal_id, date_from, date_to, limit, offset)
    - GET /api/v1/finance/quotes/{id} -- GetQuote
    - PUT /api/v1/finance/quotes/{id} -- UpdateQuote
    - DELETE /api/v1/finance/quotes/{id} -- DeleteQuote
    - POST /api/v1/finance/quotes/{id}/send -- SendQuote
    - POST /api/v1/finance/quotes/{id}/accept -- AcceptQuote
    - POST /api/v1/finance/quotes/{id}/reject -- RejectQuote
    - POST /api/v1/finance/quotes/{id}/convert -- ConvertQuoteToInvoice
    - GET /api/v1/finance/quotes/{id}/pdf -- GenerateQuotePDF (returns PDF bytes with Content-Type application/pdf)

    **Invoices:**
    - POST /api/v1/finance/invoices -- CreateInvoice
    - GET /api/v1/finance/invoices -- ListInvoices (query params: status, date_from, date_to, overdue, limit, offset)
    - GET /api/v1/finance/invoices/{id} -- GetInvoice
    - PUT /api/v1/finance/invoices/{id} -- UpdateInvoice
    - POST /api/v1/finance/invoices/{id}/send -- SendInvoice
    - POST /api/v1/finance/invoices/{id}/pay -- MarkInvoicePaid
    - POST /api/v1/finance/invoices/{id}/cancel -- CancelInvoice
    - GET /api/v1/finance/invoices/{id}/pdf -- GenerateInvoicePDF

    **Credit Notes:**
    - POST /api/v1/finance/credit-notes -- CreateCreditNote
    - GET /api/v1/finance/credit-notes -- ListCreditNotes
    - GET /api/v1/finance/credit-notes/{id} -- GetCreditNote
    - POST /api/v1/finance/credit-notes/{id}/send -- SendCreditNote
    - GET /api/v1/finance/credit-notes/{id}/pdf -- GenerateCreditNotePDF

    **Payments:**
    - POST /api/v1/finance/invoices/{id}/payments -- RecordPayment
    - GET /api/v1/finance/invoices/{id}/payments -- ListPayments
    - DELETE /api/v1/finance/payments/{id} -- DeletePayment

    **Dunning:**
    - GET /api/v1/finance/dunning -- ListDunnings (query params: status, invoice_id, level)
    - POST /api/v1/finance/dunning/detect -- DetectAndCreateDunnings (trigger overdue check)
    - POST /api/v1/finance/dunning/{id}/send -- SendDunning
    - POST /api/v1/finance/dunning/{id}/escalate -- EscalateDunning
    - GET /api/v1/finance/dunning/{id}/pdf -- GenerateDunningPDF
    - GET /api/v1/finance/dunning/config -- GetDunningConfig
    - PUT /api/v1/finance/dunning/config -- UpdateDunningConfig (admin)

    **Dashboard + Export:**
    - GET /api/v1/finance/dashboard -- GetFinanceDashboard (query: date_from, date_to)
    - POST /api/v1/finance/export/datev -- ExportDATEV (query: start_date, end_date) -- returns CSV download

    **Deal integration:**
    - POST /api/v1/finance/deals/{dealId}/quote -- Create quote from CRM deal (reads deal via CRM gRPC, pre-populates customer data from deal's contact/company)

    All routes use authMiddleware. Admin-only routes (settings update, dunning config update) additionally use `middleware.RequirePermission("finance", "admin")`. PDF and DATEV export routes set appropriate Content-Type and Content-Disposition headers.

    **Update `cmd/gateway/main.go`:**

    Add BizRoutes to the gateway service list:
    1. Register "biz" service address in ServiceRegistry with cfg.BizGRPCAddress
    2. Create `NewBizRoutes(registry)` and add to route registrars slice
    3. Follow exact same pattern as DocumentRoutes registration

    For the deal-to-quote route: the gateway needs access to both biz gRPC client AND CRM gRPC client. The BizRoutes handler reads the deal from CRM client, extracts contact/company info, then calls biz CreateQuote with pre-populated customer data. The gateway already has CRM client access, so pass it to BizRoutes constructor or access via ServiceRegistry.

    **Update `deploy/docker/docker-compose.yml`:**

    Add biz service:
    ```yaml
    biz:
      build:
        context: ../../backend
        dockerfile: ../deploy/docker/Dockerfile.biz
      ports:
        - "50058:50058"
        - "9098:9098"
      environment:
        DATABASE_URL: postgres://...
        JWT_SECRET: ...
        BIZ_GRPC_PORT: ":50058"
        BIZ_HEALTH_PORT: ":9098"
      depends_on:
        postgres:
          condition: service_healthy
      restart: unless-stopped
    ```

    Add biz GRPC address to gateway environment: `BIZ_GRPC_ADDRESS: "biz:50058"`

    **Create `deploy/docker/Dockerfile.biz`:**

    Follow exact pattern from existing Dockerfile.document (multi-stage build: Go builder -> scratch/alpine with binary).

    **Update `backend/api/openapi.yaml`:**

    Add all finance endpoints with request/response schemas. Follow existing OpenAPI patterns for CRM/Document endpoints. Include:
    - Quote, Invoice, CreditNote, Payment, DunningRecord schemas
    - LineItem, TaxBreakdown, CompanySettings schemas
    - All endpoint definitions with path parameters, query parameters, request bodies, and response types
    - PDF endpoints: responses with content type application/pdf
    - DATEV export: response with content type text/csv
  </action>
  <verify>
    - `cd backend && go build ./internal/gateway/` compiles (route_biz.go)
    - `cd backend && go build ./cmd/gateway/` compiles with BizRoutes
    - `ls deploy/docker/Dockerfile.biz` exists
    - `grep "biz" deploy/docker/docker-compose.yml` shows biz service
    - `grep "/api/v1/finance" backend/api/openapi.yaml` shows finance endpoints
    - `cd backend && go vet ./...` passes
  </verify>
  <done>
    Gateway exposes ~30 HTTP routes under /api/v1/finance/* proxying to biz gRPC service. Deal-to-quote flow reads CRM deal via existing gRPC client and creates pre-populated quote. Docker Compose includes biz service on :50058/:9098. OpenAPI spec documents all finance endpoints. Biz service is fully deployable.
  </done>
</task>

</tasks>

<verification>
- `cd backend && go build ./...` -- entire backend compiles
- `cd backend && go test ./internal/biz/datev/ -v` -- DATEV tests pass
- Gateway compiles with BizRoutes registered
- Docker Compose has biz service
- OpenAPI spec includes finance endpoints
- Deal-to-quote route exists in gateway
</verification>

<success_criteria>
The biz service is fully connected to the network: gRPC server handles all RPCs, gateway proxies HTTP routes, DATEV export produces valid EXTF CSV, CRM deal integration enables quote creation from deals, and Docker deployment is configured. The backend is API-complete for the frontend.
</success_criteria>

<output>
After completion, create `.planning/phases/12-rechnungen-finanzen/12-03-SUMMARY.md`
</output>
