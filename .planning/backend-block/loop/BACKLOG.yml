# Backend-Nachtloop — Arbeitsqueue (Lauf 11, NOCH NICHT VORBEREITET)
#
# Eine Unit = eine Iteration = ein Commit.
#
# status: todo | in_progress | done | blocked
# model:  sonnet (Default) | opus (architektonisch schwer, Proto, Security, Migration+RLS)
#
# ACHTUNG Treiber-Parsing: `model:` muss VOR `status:` stehen, und hinter `status: todo`
# darf kein Kommentar stehen (der Treiber zählt offene Units per Zeilen-Regex).
#
# Läufe 1+2 (58 Units): archive/lauf-1-2/ — Lauf 3 (61): archive/lauf-3/
# Lauf 4 (54): archive/lauf-4/ — Lauf 5 (41): archive/lauf-5/ — Lauf 6 (46): archive/lauf-6/
# Lauf 7 (71): archive/lauf-7/ — Lauf 8 (94): archive/lauf-8/ — Lauf 9 (37): archive/lauf-9/
# Lauf 10 (93, davon 91 done + 2 blocked): archive/lauf-10/ (jeweils inkl. logs/).
#
# Was der Loop NICHT zieht — geparkt oder gestrichen, mit Entscheidung und Zielort:
# BACKLOG-PARKED.yml. In DIESER Datei steht keine `blocked`-Unit. Wird eine Unit im Lauf
# blockiert, gehört ein `blocked_reason` dazu, der sagt, WORAUF sie wartet und WER das
# entscheidet.
#
# ------------------------------------------------------------------------------------------
# DIESE DATEI IST LEER — DAS IST ABSICHT.
#
# Lauf 10 ist am 2026-08-22 durchgelaufen, gemergt (`f87ffdcf`) und deployt. Sein Backlog
# liegt vollständig im Archiv. Bis Lauf 11 vorbereitet ist, steht hier nichts: der Treiber
# meldet dann "Keine offenen Units mehr im Backlog" und beendet nach null Iterationen, statt
# auf einer halben Queue loszulaufen.
#
# Kandidaten, Befunde und die entblockte Buchungsseiten-Unit stehen in BACKLOG-NEXT.yml.
# Sie sind VORGEMERKT, nicht ausgeschrieben. Vor dem nächsten Lauf werden sie dort mit
# scope/sources/done_when ausformuliert und hierher verschoben (nicht kopiert), Blöcke und
# Reihenfolge festgelegt, `coverage_start` gegen den dann aktuellen CI-Stand nachgezogen.
#
# Muster für die Vorbereitung: PROMPT-VORBEREITUNG-LAUF-8.md.
# Regel aus dem Kopf von BACKLOG-NEXT.yml, teuer gelernt: Ein Kopf, der einen Unit-Anhang
# behauptet, den es nicht gibt, ist kein Backlog.
# ------------------------------------------------------------------------------------------

units: []
