# Resume / Handoff — Tag 3 (2026-06-11): Luke an ZWEI PCs

> **Setup:** Luke arbeitet morgen parallel an seinem PC **und** an Dariens PC. **Luke hat volle Entscheidungsbefugnis** — Design-Entscheidungen und alle offenen Klärungsfragen darf er selbst treffen (Darien, 2026-06-10). Jede Entscheidung kurz im jeweiligen Review-Faden notieren („Entscheidung Luke: X, weil Y"), damit Darien nachvollziehen kann.
> **Stand heute Abend:** main = `9c0d75c`. **kontakte/finish-p7-p8, marathon/nico und marathon/dein-pc sind GEMERGT** — main trägt jetzt Kontakte P7/P8, Berichte-Sparklines und die komplette dokumente-Überarbeitung. Smoke auf Merge-Stand grün.

---

## ⛔ ERSTE AKTION an BEIDEN PCs
```bash
git fetch origin
git checkout main && git pull        # 9c0d75c oder neuer
```
**`marathon/dein-pc` und `marathon/nico` sind gemergt — NICHT mehr fortsetzen.** Neue Arbeit auf neuen Branches von main (Konvention unten).

---

## PC 1 — Lukes eigener PC: `marathon/luke-fe` rebasen + L-Lane fortsetzen

1. **ZUERST lesen:** `reviews/dokumente.md` → Abschnitt **„🟡 An Luke — documents.ts-Kollision"**. Kurzfassung: beim Rebase deine `mocks/handlers/documents.ts`-Ergänzungen **komplett verwerfen** (main-Version übernehmen) — deine data:-URLs waren in iframes blockiert (CSP), auf main funktioniert alles inkl. deiner Entity-Links und enriched-Felder. Deine vertraege-P7-Dokumentvorschau läuft auf main automatisch.
2. `git rebase origin/main marathon/luke-fe` — erwartete Konflikte: documents.ts (→ main-Version), i18n ×4 (additiv beide), module-settings-registry (additiv beide, **deine resolver-Verbesserung behalten**).
3. Nach Rebase: QA `qa-vertraege-dokumente.mjs` einmal **headed** gegenfahren (PDF-Vorschau jetzt sichtbar!), pushen → Darien/Luke merged nach main.
4. **Beschlossene Folge-Fixes (Darien 2026-06-10, in reviews/dokumente.md + vertraege.md):**
   - vertraege P4: Detail-Panel nach Bearbeiten aktualisieren (`setSelectedContract` nach Speichern)
   - vertraege: OnlyOffice-Bearbeiten aus dem Vertrag = **ja, als Folgephase** (WOPI-Token; CSP braucht dann die OnlyOffice-Domain in `frame-src` — siehe backend-gaps)
5. Danach L-Lane weiter: vertraege P3 (E-Signatur — **Entscheidung EES-eigen vs. Skribble darfst du treffen**), dashboard P4/P5, profil P4.

## PC 2 — Dariens PC: Strom-D-Lane, Branch `marathon/luke-d` (neu von main)

**Empfohlene Reihenfolge (Luke darf umpriorisieren):**
1. **zeiterfassung — Architektur-Entscheidung treffen + bauen.** Voller Befund in `reviews/zeiterfassung.md`: zwei parallele UIs (Live-`ZeiterfassungTab` API-backed vs. 10 unmounted Store-Views, UI-reich aber Mock) + zwei konkurrierende Topbar-Widgets. **Die Entscheidung liegt jetzt bei dir** (dein HR-Backend): tote Views an die HR-API anschließen vs. Live-Tab ausbauen + tote Views löschen. Danach ist das Modul wieder baubar (P1-Reste: eigene Shell, Projekt-Picker, Export — Backend-Gaps in backend-gaps.md §zeiterfassung).
2. **finanzen P1** — von Darien längst greenlit, nie gestartet: buchhaltung-Ordner löschen, ExpensesTab/TransactionsTab auf TanStack Query, Berichte-Tab-Entscheidung. Größtes ungestartetes Kernmodul.
3. Alternativ dokumente P2 (Volltextsuche + Metadaten — Volltext-Index ist aber dein Backend, ggf. erst Endpoint).

## Offene Klärungsfragen — jetzt in Lukes Entscheidungsgewalt
- **mails:** IMAP via Go-Proxy vs. Electron-Node (gated den ganzen mails-Neubau)
- **Q1–Q10** in `module-phase-plans.md` (Settings-Einstieg, Modul-Leiter-Modell, Modul-Reihenfolge nach den Lanes, Tiefe pro Modul …)
- LiveKit-/Video-Fragen (Token via Gateway, Recording-Storage) — ohnehin sein Gebiet
→ Getroffene Entscheidungen bitte ins ✅ ENTSCHEIDUNGS-LOG in `module-phase-plans.md` ergänzen.

## Für Nico (falls er auch da ist)
Gate ✅ grün, wiki-Block frei. Vorher Mini-Phase aus dem Review (`reviews/berichte.md`): Sparkline min–max normalisieren + Linienfarbe folgt Badge. Neuen main pullen, neuer Branch `marathon/nico-2`.

## Bau-Loop (unverändert, JEDE Phase)
bauen → i18n ×4 (`{var}`, ICU) → Demo-Handler → gescopter tsc (nur geänderte Dateien) → Playwright-QA + **Screenshots WIRKLICH ansehen** (Lehre heute: Lukes blockierte PDF-Vorschau war nur im Screenshot sichtbar; PDF-Checks **headed** fahren, headless hat keinen PDF-Viewer) → Review-Faden → ein Commit → push.
- Dev-Server killen: PowerShell `Get-NetTCPConnection -LocalPort 5173 -State Listen | % { Stop-Process -Id $_.OwningProcess -Force }`
- **Neue Regel:** `mocks/handlers/<modul>.ts` gehört der Modul-Lane — fremde Lane braucht Demo-Daten? Zurufen statt parallel bauen (heutige Doppelarbeit documents.ts).

## Heutige Lehren (Tag 2)
1. **Spec vs. Code:** dreimal bestätigt — vor jedem Modul echten Code-Stand prüfen (zeiterfassung: „fertige" Views waren toter Code; calendar/kontakte gestern dito).
2. **Screenshots ansehen ist nicht optional:** zwei False-Positives in QA-Scripts nur durch Hinsehen entdeckt (Toast-Match, Karten-Wrapper-Match) — und der CSP-Blocker.
3. **CSP:** `frame-src 'self' blob:` ist jetzt gesetzt (Dokument-Viewer). OnlyOffice-iframe braucht bei Scharfschaltung die Office-Domain ebenfalls in frame-src.
