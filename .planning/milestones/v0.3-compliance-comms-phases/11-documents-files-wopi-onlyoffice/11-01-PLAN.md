---
phase: 11-documents-files-wopi-onlyoffice
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/proto/document/v1/document.proto
  - backend/migrations/000043_create_document_tables.up.sql
  - backend/migrations/000043_create_document_tables.down.sql
  - backend/migrations/000044_create_wopi_locks.up.sql
  - backend/migrations/000044_create_wopi_locks.down.sql
  - backend/internal/models/document.go
  - backend/internal/models/search.go
  - backend/tools/document_deps.go
  - backend/cmd/document/main.go
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-04
  - DOC-05
  - DOC-06
  - DOC-07
  - DOC-08
  - DOC-10

must_haves:
  truths:
    - "Document service proto defines RPCs for folder CRUD, file CRUD, file versioning, sharing, tagging, entity linking, search, and virtual folder listing"
    - "Database tables exist for document_folders, document_files, document_file_versions, document_shares, document_tags, document_file_tags, document_entity_links, and wopi_locks"
    - "Go models exist for all document domain entities with proper JSON/DB tags"
    - "Document service binary scaffold starts and listens on gRPC :50057 and health :9097"
  artifacts:
    - path: "backend/proto/document/v1/document.proto"
      provides: "DocumentService gRPC definition with ~35 RPCs"
      contains: "service DocumentService"
    - path: "backend/migrations/000043_create_document_tables.up.sql"
      provides: "Document domain database schema"
      contains: "CREATE TABLE document_folders"
    - path: "backend/internal/models/document.go"
      provides: "Go structs for document domain"
      contains: "type DocumentFile struct"
    - path: "backend/cmd/document/main.go"
      provides: "Document service entry point"
      contains: "func main()"
  key_links:
    - from: "backend/proto/document/v1/document.proto"
      to: "backend/internal/models/document.go"
      via: "Proto message shapes match Go model fields"
      pattern: "DocumentFile.*filename"
    - from: "backend/migrations/000043_create_document_tables.up.sql"
      to: "backend/internal/models/document.go"
      via: "SQL columns map to Go struct fields"
      pattern: "document_files.*document.go"
---

<objective>
Create the document service data foundation: protobuf definition with ~35 RPCs covering folders, files, versioning, sharing, tagging, entity linking, search, and virtual folders. Database migrations for 8 tables. Go domain models. Go dependency retention. Service binary scaffold on :50057/:9097.

Purpose: Establishes the complete data layer and service contract that all subsequent document plans build upon.
Output: Proto file, 2 migrations, Go models, dependency tool file, service binary scaffold.
</objective>

<execution_context>
@C:/Users/Luke/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Luke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-documents-files-wopi-onlyoffice/11-RESEARCH.md
@backend/proto/email/v1/email.proto
@backend/cmd/email/main.go
@backend/internal/models/search.go
@backend/internal/chat/file/filestore.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Proto definition + Go dependency retention</name>
  <files>
    backend/proto/document/v1/document.proto
    backend/tools/document_deps.go
  </files>
  <action>
Create `backend/proto/document/v1/document.proto` with package `document.v1` and go_package `github.com/kmuhub/kmuhub/proto/document/v1;documentv1`.

Define `service DocumentService` with these RPCs:

**Folder operations (~8 RPCs):**
- CreateFolder(CreateFolderRequest) returns (CreateFolderResponse) — name, parent_id, space_type (personal/team/project), space_id
- GetFolder(GetFolderRequest) returns (GetFolderResponse)
- ListFolders(ListFoldersRequest) returns (ListFoldersResponse) — filter by parent_id, space_type, space_id
- UpdateFolder(UpdateFolderRequest) returns (UpdateFolderResponse) — rename, move (change parent_id)
- DeleteFolder(DeleteFolderRequest) returns (DeleteFolderResponse)
- GetFolderPath(GetFolderPathRequest) returns (GetFolderPathResponse) — returns breadcrumb path from root
- InitializeUserSpace(InitializeUserSpaceRequest) returns (InitializeUserSpaceResponse) — create default folders for new user
- InitializeTeamSpace(InitializeTeamSpaceRequest) returns (InitializeTeamSpaceResponse) — create default folders for team

**File operations (~10 RPCs):**
- GetFile(GetFileRequest) returns (GetFileResponse)
- ListFiles(ListFilesRequest) returns (ListFilesResponse) — filter by folder_id, owner_id, is_favorite, pagination, sort
- UpdateFile(UpdateFileRequest) returns (UpdateFileResponse) — rename, move to folder, toggle favorite
- DeleteFile(DeleteFileRequest) returns (DeleteFileResponse) — soft delete
- CopyFile(CopyFileRequest) returns (CopyFileResponse) — copy to another folder
- MoveFile(MoveFileRequest) returns (MoveFileResponse)
- GetFileDownloadURL(GetFileDownloadURLRequest) returns (GetFileDownloadURLResponse) — presigned MinIO URL
- CreateFileVersion(CreateFileVersionRequest) returns (CreateFileVersionResponse)
- ListFileVersions(ListFileVersionsRequest) returns (ListFileVersionsResponse)
- RevertFileVersion(RevertFileVersionRequest) returns (RevertFileVersionResponse)

Note: File upload is handled via HTTP multipart on the gateway (same pattern as chat file upload), NOT via gRPC streaming.

**Share operations (~4 RPCs):**
- ShareEntity(ShareEntityRequest) returns (ShareEntityResponse) — entity_type (file/folder), entity_id, user_id, permission (read/write)
- UnshareEntity(UnshareEntityRequest) returns (UnshareEntityResponse)
- ListShares(ListSharesRequest) returns (ListSharesResponse) — by entity
- ListSharedWithMe(ListSharedWithMeRequest) returns (ListSharedWithMeResponse) — files/folders shared with current user

**Tag operations (~5 RPCs):**
- CreateTag(CreateTagRequest) returns (CreateTagResponse) — name, color
- ListTags(ListTagsRequest) returns (ListTagsResponse)
- DeleteTag(DeleteTagRequest) returns (DeleteTagResponse)
- TagFile(TagFileRequest) returns (TagFileResponse) — add tag to file
- UntagFile(UntagFileRequest) returns (UntagFileResponse) — remove tag from file

**Entity link operations (~3 RPCs):**
- LinkFileToEntity(LinkFileToEntityRequest) returns (LinkFileToEntityResponse) — entity_type (contact/company/deal/project/task), entity_id
- UnlinkFileFromEntity(UnlinkFileFromEntityRequest) returns (UnlinkFileFromEntityResponse)
- ListFileEntityLinks(ListFileEntityLinksRequest) returns (ListFileEntityLinksResponse)

**Search operations (~2 RPCs):**
- SearchFiles(SearchFilesRequest) returns (SearchFilesResponse) — query string, tag_ids filter, folder_id scope, pagination
- ListVirtualFiles(ListVirtualFilesRequest) returns (ListVirtualFilesResponse) — source_type filter (chat/email/task), pagination

**WOPI operations (~2 RPCs):**
- GenerateWOPIToken(GenerateWOPITokenRequest) returns (GenerateWOPITokenResponse) — file_id, returns access_token + ttl
- GetWOPIDiscovery(GetWOPIDiscoveryRequest) returns (GetWOPIDiscoveryResponse) — cached discovery XML actions

Define message types following the email.proto pattern. Use string for UUID fields, google.protobuf.Timestamp for timestamps. Define enums:
- FolderSpaceType: FOLDER_SPACE_TYPE_PERSONAL, FOLDER_SPACE_TYPE_TEAM, FOLDER_SPACE_TYPE_PROJECT
- SharePermission: SHARE_PERMISSION_READ, SHARE_PERMISSION_WRITE
- VirtualFileSource: VIRTUAL_FILE_SOURCE_CHAT, VIRTUAL_FILE_SOURCE_EMAIL, VIRTUAL_FILE_SOURCE_TASK
- FileSortField: FILE_SORT_NAME, FILE_SORT_SIZE, FILE_SORT_DATE, FILE_SORT_TYPE
- SortDirection: SORT_ASC, SORT_DESC

Key messages to define:
- DocumentFolder: id, name, parent_id, space_type, space_id, is_system, icon, created_by, created_at, updated_at, file_count (computed)
- DocumentFile: id, folder_id, filename, mime_type, file_size, storage_key, thumbnail_key, current_version, owner_id, is_favorite, is_deleted, tags (repeated), created_at, updated_at
- DocumentFileVersion: id, file_id, version_number, version_label, storage_key, file_size, created_by, created_at
- DocumentShare: id, entity_type, entity_id, shared_with_user_id, shared_with_user_name, permission, shared_by, shared_by_name, created_at
- DocumentTag: id, name, color, file_count (computed), created_by, created_at
- DocumentEntityLink: id, file_id, entity_type, entity_id, entity_name (denormalized), linked_by, created_at
- VirtualFile: id, filename, mime_type, file_size, storage_key, source_type, source_id, source_name, uploaded_by, uploaded_by_name, created_at
- FolderPathSegment: id, name (for breadcrumb)
- FileSearchResult: file (DocumentFile), rank (float), snippet (string)

Also create `backend/tools/document_deps.go` with `//go:build tools` constraint to retain `code.sajari.com/docconv/v2` in go.mod before application code imports it. Follow the pattern from `backend/tools/email_deps.go`.

Run `go get code.sajari.com/docconv/v2` in the backend directory to add the dependency.
  </action>
  <verify>
    - `backend/proto/document/v1/document.proto` exists and contains `service DocumentService` with ~35 RPCs
    - `backend/tools/document_deps.go` exists with build constraint
    - All RPC names follow the pattern: VerbNoun(VerbNounRequest) returns (VerbNounResponse)
    - Proto file compiles: validate syntax is correct
  </verify>
  <done>Proto file defines DocumentService with RPCs covering all 7 functional areas (folders, files, shares, tags, entity links, search, WOPI). Dependency tool file retains docconv.</done>
</task>

<task type="auto">
  <name>Task 2: Database migrations + Go models + service scaffold</name>
  <files>
    backend/migrations/000043_create_document_tables.up.sql
    backend/migrations/000043_create_document_tables.down.sql
    backend/migrations/000044_create_wopi_locks.up.sql
    backend/migrations/000044_create_wopi_locks.down.sql
    backend/internal/models/document.go
    backend/cmd/document/main.go
  </files>
  <action>
**Migration 000043 (up):** Create document tables. Use the schema from the research EXACTLY, with these tables:

1. `folder_space_type` ENUM: 'personal', 'team', 'project'
2. `document_folders` — id (UUID PK), name (VARCHAR 255), parent_id (self-FK CASCADE), space_type (folder_space_type), space_id (UUID), is_system (BOOLEAN DEFAULT FALSE), icon (VARCHAR 50 DEFAULT 'folder'), created_by (FK users), created_at, updated_at. Indexes: idx_document_folders_parent, idx_document_folders_space.
3. `document_files` — id (UUID PK), folder_id (FK document_folders), filename (VARCHAR 255), mime_type (VARCHAR 255), file_size (BIGINT CHECK > 0), storage_key (VARCHAR 512), thumbnail_key (VARCHAR 512 nullable), current_version (INTEGER DEFAULT 1), owner_id (FK users), is_favorite (BOOLEAN DEFAULT FALSE), is_deleted (BOOLEAN DEFAULT FALSE), content_text (TEXT nullable), search_vector (TSVECTOR), created_at, updated_at, deleted_at (nullable). Indexes: idx_document_files_folder (WHERE NOT deleted), idx_document_files_owner, idx_document_files_search (GIN). Trigger: auto-update search_vector using `to_tsvector('german', filename || ' ' || content_text)`.
4. `document_file_versions` — id (UUID PK), file_id (FK document_files CASCADE), version_number (INTEGER), version_label (VARCHAR 255 nullable), storage_key (VARCHAR 512), file_size (BIGINT), created_by (FK users), created_at. UNIQUE(file_id, version_number). Index: idx_document_versions_file.
5. `document_shares` — id (UUID PK), entity_type (VARCHAR 20 CHECK IN file/folder), entity_id (UUID), shared_with_user_id (FK users), permission (VARCHAR 20 CHECK IN read/write), shared_by (FK users), created_at. UNIQUE(entity_type, entity_id, shared_with_user_id). Indexes: idx_document_shares_entity, idx_document_shares_user.
6. `document_tags` — id (UUID PK), name (VARCHAR 100 UNIQUE), color (VARCHAR 7 nullable), created_by (FK users), created_at.
7. `document_file_tags` — file_id (FK document_files CASCADE), tag_id (FK document_tags CASCADE), PRIMARY KEY (file_id, tag_id).
8. `document_entity_links` — id (UUID PK), file_id (FK document_files CASCADE), entity_type (VARCHAR 50), entity_id (UUID), linked_by (FK users), created_at. UNIQUE(file_id, entity_type, entity_id). Indexes: idx_document_entity_links_file, idx_document_entity_links_entity.

**Migration 000043 (down):** Drop all tables in reverse order, then drop the enum type.

**Migration 000044 (up):** Create WOPI lock table:
- `wopi_locks` — file_id (UUID PK, FK document_files CASCADE), lock_id (VARCHAR 1024), locked_by (FK users), created_at (DEFAULT NOW()), expires_at (DEFAULT NOW() + INTERVAL '30 minutes').

**Migration 000044 (down):** DROP TABLE wopi_locks.

**Go models** in `backend/internal/models/document.go`:
Define Go structs matching the tables. Follow existing model conventions (UUID fields, `json:"snake_case"` tags, pointer for nullable fields, time.Time for timestamps):
- `DocumentFolder` — all columns + computed `FileCount int` (json:"file_count")
- `DocumentFile` — all columns + `Tags []DocumentTag` (json:"tags") for joined tag data
- `DocumentFileVersion` — all columns
- `DocumentShare` — all columns + `SharedWithUserName string` and `SharedByName string` (denormalized from JOIN)
- `DocumentTag` — all columns + computed `FileCount int`
- `DocumentEntityLink` — all columns + `EntityName string` (denormalized)
- `VirtualFile` — id, filename, mime_type, file_size, storage_key, source_type (string: "chat"/"email"/"task"), source_id, source_name, uploaded_by, uploaded_by_name, created_at
- `WOPILock` — file_id, lock_id, locked_by, created_at, expires_at
- `FolderPathSegment` — id, name (for breadcrumbs)

Also update `backend/internal/models/search.go` to add `SearchEntityTypeFile SearchEntityType = "file"` if not already present, to support global search.

**Service scaffold** in `backend/cmd/document/main.go`:
Follow the `backend/cmd/email/main.go` pattern exactly:
- Load config, init slog JSON handler
- Connect to PostgreSQL via `database.NewPool`
- Connect to MinIO (same pattern as email)
- Initialize services (placeholder - just log "services initialized")
- Create gRPC server on :50057
- Create health/metrics HTTP server on :9097
- Graceful shutdown with signal handling

The document service runs as a SEPARATE binary (not shared with work service like calendar/video), because it has unique OS-level dependencies (docconv/poppler) that should be isolated in its own Docker image.
  </action>
  <verify>
    - Migration files exist at 000043 and 000044
    - `backend/internal/models/document.go` defines DocumentFolder, DocumentFile, DocumentFileVersion, DocumentShare, DocumentTag, DocumentEntityLink, VirtualFile, WOPILock structs
    - `backend/cmd/document/main.go` compiles: `cd backend && go build ./cmd/document/`
    - Migration SQL is syntactically valid (no obvious errors)
  </verify>
  <done>8 document tables + WOPI locks table defined in migrations. Go models cover all entities. Document service binary scaffold starts on :50057/:9097. SearchEntityTypeFile added to models.</done>
</task>

</tasks>

<verification>
- Proto file defines ~35 RPCs across 7 functional areas
- Migrations create 9 tables (8 document + 1 WOPI) with proper indexes, constraints, and triggers
- Go models have correct JSON tags and field types
- Service binary compiles and would start (may fail at runtime without DB, that's OK)
- docconv dependency retained in go.mod
</verification>

<success_criteria>
- `backend/proto/document/v1/document.proto` contains `service DocumentService` with 34+ RPCs
- `backend/migrations/000043_create_document_tables.up.sql` creates 8 tables with tsvector trigger
- `backend/migrations/000044_create_wopi_locks.up.sql` creates wopi_locks table
- `backend/internal/models/document.go` defines 8+ struct types
- `backend/cmd/document/main.go` compiles successfully
- `backend/tools/document_deps.go` retains docconv
</success_criteria>

<output>
After completion, create `.planning/phases/11-documents-files-wopi-onlyoffice/11-01-SUMMARY.md`
</output>
