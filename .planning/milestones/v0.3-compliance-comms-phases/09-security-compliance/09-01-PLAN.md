---
phase: 09-security-compliance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/proto/security/v1/security.proto
  - backend/proto/auth/v1/auth.proto
  - backend/migrations/000039_create_security_tables.up.sql
  - backend/migrations/000039_create_security_tables.down.sql
  - backend/migrations/000040_extend_users_2fa_locale.up.sql
  - backend/migrations/000040_extend_users_2fa_locale.down.sql
  - backend/internal/models/security.go
  - backend/internal/models/user.go
  - backend/go.mod
  - backend/go.sum
autonomous: true

must_haves:
  truths:
    - "Proto definitions exist for all security RPCs (2FA, audit, session, vault, GDPR, password)"
    - "Database tables exist for audit_log, recovery_codes, two_factor_policy, user_sessions, vault_secrets, gdpr_export_requests, gdpr_erasure_log, password_policies, password_history, ip_access_rules"
    - "User model extended with two_factor and locale fields"
    - "Go models exist for all security domain types"
  artifacts:
    - path: "backend/proto/security/v1/security.proto"
      provides: "SecurityService proto with audit, vault, GDPR, password RPCs"
      min_lines: 100
    - path: "backend/proto/auth/v1/auth.proto"
      provides: "Extended auth proto with 2FA and session RPCs"
      contains: "Setup2FA"
    - path: "backend/migrations/000039_create_security_tables.up.sql"
      provides: "Audit log, vault, GDPR, password, IP rules, sessions tables"
      min_lines: 50
    - path: "backend/migrations/000040_extend_users_2fa_locale.up.sql"
      provides: "2FA and locale columns on users table"
      contains: "two_factor_enabled"
    - path: "backend/internal/models/security.go"
      provides: "Go structs for all security domain types"
      min_lines: 80
  key_links:
    - from: "backend/proto/security/v1/security.proto"
      to: "backend/proto/auth/v1/auth.proto"
      via: "shared user_id references"
      pattern: "user_id"
---

<objective>
Create the data foundation for Phase 9: proto definitions for security/auth RPCs, database migrations for all security tables, Go model structs, and install new Go dependencies (pquerna/otp, wagslane/go-password-validator, golang.org/x/crypto/hkdf).

Purpose: Every subsequent plan depends on these proto definitions, database tables, and model types. This is the foundation layer.
Output: Compilable proto, runnable migrations, importable Go models, updated go.mod.
</objective>

<execution_context>
@C:\Users\Luke\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Luke\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-security-compliance/09-RESEARCH.md
@backend/proto/auth/v1/auth.proto
@backend/internal/models/user.go
@backend/migrations/000038_create_reactions_and_presence.up.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Proto definitions for security and auth extensions</name>
  <files>
    backend/proto/security/v1/security.proto
    backend/proto/auth/v1/auth.proto
  </files>
  <action>
    1. Create `backend/proto/security/v1/security.proto` defining `security.v1.SecurityService` with these RPCs:
       - Audit: CreateAuditEntry, ListAuditEntries, ExportAuditLog, VerifyAuditChain
       - Vault: GetVaultSecret, SetVaultSecret, ListVaultSecrets, DeleteVaultSecret
       - GDPR: RequestDataExport, ListDataExports, ApproveDataExport, DenyDataExport, GetExportDownload, PreviewErasure, ExecuteErasure
       - Password: GetPasswordPolicy, UpdatePasswordPolicy, ValidatePassword
       - IP Access: ListIPRules, CreateIPRule, DeleteIPRule

       Message types needed:
       - AuditEntry: id, sequence_num, timestamp, user_id, action, target, target_type, details (string/JSON), ip_address, user_agent, result, previous_hash, entry_hash
       - AuditFilter: date_from, date_to, user_id, action, result, offset, limit
       - VaultSecret: id, key_name, encrypted_value, key_version, description, created_by, created_at, updated_at (encrypted_value only in Set, never in List/Get responses -- return decrypted_value instead)
       - GDPRExportRequest: id, user_id, status, requested_at, reviewed_by, reviewed_at, review_note, download_token, download_expires_at
       - ErasurePreview: modules (repeated ModuleErasurePreview with module_name, record_count, action)
       - PasswordPolicy: id, min_length, min_entropy, max_age_days, prevent_reuse_count, require_uppercase, require_lowercase, require_digit, require_special
       - IPAccessRule: id, ip_cidr, rule_type (allow/block), description, created_by, created_at

       Use `google.protobuf.Timestamp` for time fields. Follow existing proto patterns (see auth.proto, work.proto).

    2. Extend `backend/proto/auth/v1/auth.proto` with new RPCs in AuthService:
       - 2FA: Setup2FA, Verify2FA, Validate2FALogin, Disable2FA, RegenerateRecoveryCodes, AdminReset2FA
       - Sessions: ListSessions, TerminateSession, TerminateAllSessions, GetTwoFactorPolicy, UpdateTwoFactorPolicy

       New message types in auth.proto:
       - Setup2FAResponse: qr_code_png (bytes), manual_secret (string)
       - Verify2FARequest: user_id, code
       - Verify2FAResponse: recovery_codes (repeated string)
       - Validate2FALoginRequest: pending_token, code, is_recovery_code
       - Validate2FALoginResponse: access_token, refresh_token, user (existing UserResponse)
       - LoginResponse: EXTEND existing to add requires_two_factor (bool), pending_token (string)
       - SessionInfo: id, user_id, device_name, device_type, ip_address, location, user_agent, is_current, last_active_at, created_at
       - TwoFactorPolicy: role_name, enforced, grace_period_days
       - AdminReset2FARequest: user_id, reason (mandatory string)

    3. Run protoc to generate Go code for both proto files. Follow existing generation pattern (check Makefile or existing gen commands).

    Use `option go_package = "github.com/kmuhub/kmuhub/proto/security/v1;securityv1";` for the new security proto.
  </action>
  <verify>
    - `cd backend && go build ./proto/security/v1/...` compiles without errors
    - `cd backend && go build ./proto/auth/v1/...` compiles without errors
    - Proto files have all RPCs listed above
  </verify>
  <done>Both proto files define all security RPCs and message types, generated Go code compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Database migrations + Go models + dependencies</name>
  <files>
    backend/migrations/000039_create_security_tables.up.sql
    backend/migrations/000039_create_security_tables.down.sql
    backend/migrations/000040_extend_users_2fa_locale.up.sql
    backend/migrations/000040_extend_users_2fa_locale.down.sql
    backend/internal/models/security.go
    backend/internal/models/user.go
    backend/go.mod
    backend/go.sum
  </files>
  <action>
    1. Install Go dependencies:
       ```bash
       cd backend
       go get github.com/pquerna/otp@v1.5.0
       go get github.com/wagslane/go-password-validator
       go get golang.org/x/crypto/hkdf
       ```

    2. Create migration `000039_create_security_tables.up.sql` with ALL security tables:
       - `audit_log` (id UUID PK, sequence_num BIGSERIAL, timestamp TIMESTAMPTZ NOT NULL DEFAULT NOW(), user_id UUID REFERENCES users(id), action VARCHAR(100) NOT NULL, target VARCHAR(255), target_type VARCHAR(50), details JSONB, ip_address INET, user_agent TEXT, result VARCHAR(20) NOT NULL DEFAULT 'success', previous_hash VARCHAR(64) NOT NULL DEFAULT '', entry_hash VARCHAR(64) NOT NULL)
         Indexes: idx_audit_log_timestamp (timestamp DESC), idx_audit_log_user_id (user_id), idx_audit_log_action (action), idx_audit_log_result (result), idx_audit_log_sequence (sequence_num DESC), idx_audit_log_timestamp_action (timestamp DESC, action)
       - `recovery_codes` (id UUID PK, user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE, code_hash VARCHAR(64) NOT NULL, used_at TIMESTAMPTZ, created_at TIMESTAMPTZ NOT NULL DEFAULT NOW())
         Index: idx_recovery_codes_user_id (user_id)
       - `two_factor_policy` (id UUID PK, role_name VARCHAR(50) NOT NULL, enforced BOOLEAN NOT NULL DEFAULT false, grace_period_days INT NOT NULL DEFAULT 14, updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(), updated_by UUID REFERENCES users(id))
         Unique index: idx_two_factor_policy_role (role_name)
       - `user_sessions` (id UUID PK, user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE, refresh_token_id UUID REFERENCES refresh_tokens(id) ON DELETE SET NULL, device_name VARCHAR(255), device_type VARCHAR(50), ip_address INET, location VARCHAR(255), user_agent TEXT, is_current BOOLEAN NOT NULL DEFAULT false, last_active_at TIMESTAMPTZ NOT NULL DEFAULT NOW(), created_at TIMESTAMPTZ NOT NULL DEFAULT NOW())
         Indexes: idx_user_sessions_user_id (user_id), idx_user_sessions_last_active (last_active_at DESC)
       - `vault_secrets` (id UUID PK, key_name VARCHAR(255) NOT NULL, encrypted_value TEXT NOT NULL, key_version INT NOT NULL DEFAULT 1, description TEXT, created_by UUID REFERENCES users(id), created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(), updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW())
         Unique index: idx_vault_secrets_key_name (key_name)
       - `gdpr_export_requests` (id UUID PK, user_id UUID NOT NULL REFERENCES users(id), status VARCHAR(20) NOT NULL DEFAULT 'pending', requested_at TIMESTAMPTZ NOT NULL DEFAULT NOW(), reviewed_by UUID REFERENCES users(id), reviewed_at TIMESTAMPTZ, review_note TEXT, export_data BYTEA, download_token VARCHAR(64), download_expires_at TIMESTAMPTZ, downloaded_at TIMESTAMPTZ)
         Indexes: idx_gdpr_exports_user_id (user_id), idx_gdpr_exports_status (status)
       - `gdpr_erasure_log` (id UUID PK, original_user_id UUID NOT NULL, anonymized_label VARCHAR(100) NOT NULL, executed_by UUID NOT NULL REFERENCES users(id), executed_at TIMESTAMPTZ NOT NULL DEFAULT NOW(), modules_affected JSONB NOT NULL, confirmation_hash VARCHAR(64) NOT NULL)
       - `password_policies` (id UUID PK, min_length INT NOT NULL DEFAULT 12, require_uppercase BOOLEAN NOT NULL DEFAULT false, require_lowercase BOOLEAN NOT NULL DEFAULT false, require_digit BOOLEAN NOT NULL DEFAULT false, require_special BOOLEAN NOT NULL DEFAULT false, min_entropy FLOAT NOT NULL DEFAULT 50.0, max_age_days INT, prevent_reuse_count INT NOT NULL DEFAULT 5, updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(), updated_by UUID REFERENCES users(id))
       - `password_history` (id UUID PK, user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE, password_hash VARCHAR(255) NOT NULL, created_at TIMESTAMPTZ NOT NULL DEFAULT NOW())
         Index: idx_password_history_user_id (user_id)
       - `ip_access_rules` (id UUID PK, ip_cidr CIDR NOT NULL, rule_type VARCHAR(10) NOT NULL CHECK (rule_type IN ('allow', 'block')), description TEXT, created_by UUID REFERENCES users(id), created_at TIMESTAMPTZ NOT NULL DEFAULT NOW())
         Index: idx_ip_access_rules_type (rule_type)

       Down migration: DROP TABLE in reverse order.

    3. Create migration `000040_extend_users_2fa_locale.up.sql`:
       ```sql
       ALTER TABLE users ADD COLUMN two_factor_enabled BOOLEAN NOT NULL DEFAULT false;
       ALTER TABLE users ADD COLUMN two_factor_secret_encrypted TEXT;
       ALTER TABLE users ADD COLUMN two_factor_pending_secret TEXT;
       ALTER TABLE users ADD COLUMN two_factor_enabled_at TIMESTAMPTZ;
       ALTER TABLE users ADD COLUMN locale VARCHAR(5) NOT NULL DEFAULT 'de';
       ```
       Down migration: DROP these columns.

    4. Update `backend/internal/models/user.go` to add the new fields to the User struct:
       - TwoFactorEnabled bool `json:"two_factor_enabled"`
       - TwoFactorSecretEncrypted string `json:"-"` (never exposed in JSON)
       - TwoFactorPendingSecret string `json:"-"` (never exposed in JSON)
       - TwoFactorEnabledAt *time.Time `json:"two_factor_enabled_at,omitempty"`
       - Locale string `json:"locale"`

    5. Create `backend/internal/models/security.go` with all domain model structs:
       - AuditEntry (matches the table columns)
       - AuditFilter (DateFrom, DateTo *time.Time, UserID *uuid.UUID, Action, Result string, Offset, Limit int)
       - RecoveryCode (ID, UserID uuid.UUID, CodeHash string, UsedAt *time.Time, CreatedAt time.Time)
       - TwoFactorPolicy (ID uuid.UUID, RoleName string, Enforced bool, GracePeriodDays int, UpdatedAt time.Time, UpdatedBy *uuid.UUID)
       - UserSession (ID, UserID uuid.UUID, RefreshTokenID *uuid.UUID, DeviceName, DeviceType string, IPAddress, Location, UserAgent string, IsCurrent bool, LastActiveAt, CreatedAt time.Time)
       - VaultSecret (ID uuid.UUID, KeyName, EncryptedValue string, KeyVersion int, Description string, CreatedBy *uuid.UUID, CreatedAt, UpdatedAt time.Time)
       - GDPRExportRequest (ID, UserID uuid.UUID, Status string, RequestedAt time.Time, ReviewedBy *uuid.UUID, ReviewedAt *time.Time, ReviewNote string, ExportData []byte, DownloadToken string, DownloadExpiresAt, DownloadedAt *time.Time)
       - GDPRErasureLog (ID uuid.UUID, OriginalUserID uuid.UUID, AnonymizedLabel string, ExecutedBy uuid.UUID, ExecutedAt time.Time, ModulesAffected map[string]string, ConfirmationHash string)
       - PasswordPolicy (ID uuid.UUID, MinLength int, RequireUppercase, RequireLowercase, RequireDigit, RequireSpecial bool, MinEntropy float64, MaxAgeDays *int, PreventReuseCount int, UpdatedAt time.Time, UpdatedBy *uuid.UUID)
       - PasswordHistoryEntry (ID, UserID uuid.UUID, PasswordHash string, CreatedAt time.Time)
       - IPAccessRule (ID uuid.UUID, IPCIDR, RuleType, Description string, CreatedBy *uuid.UUID, CreatedAt time.Time)
       - LoginResult (existing tokens + RequiresTwoFactor bool, PendingToken string)
       - ModuleErasurePreview (ModuleName string, RecordCount int, Action string)

       All GDPR export statuses as constants: StatusPending, StatusApproved, StatusDenied, StatusProcessing, StatusReady, StatusDownloaded, StatusExpired.
  </action>
  <verify>
    - `cd backend && go build ./internal/models/...` compiles
    - `cd backend && go vet ./internal/models/...` passes
    - Migration files exist: 000039 (up+down) and 000040 (up+down)
    - go.mod contains pquerna/otp, wagslane/go-password-validator, golang.org/x/crypto
  </verify>
  <done>All security tables defined in migrations, Go models compile, new dependencies installed. Foundation ready for service implementation.</done>
</task>

</tasks>

<verification>
- All proto files compile with protoc
- All Go model packages compile with `go build`
- Migration 000039 creates 10 tables with proper indexes
- Migration 000040 extends users table with 5 new columns
- go.mod includes pquerna/otp v1.5.0, go-password-validator, golang.org/x/crypto
</verification>

<success_criteria>
- Proto definitions exist for all 30+ RPCs across auth and security services
- Database migrations 000039-000040 are ready to run
- Go models cover all domain types needed by subsequent plans
- New Go dependencies are installed and importable
</success_criteria>

<output>
After completion, create `.planning/phases/09-security-compliance/09-01-SUMMARY.md`
</output>
