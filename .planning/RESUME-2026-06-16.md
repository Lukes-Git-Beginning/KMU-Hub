# Resume / Handoff — 2026-06-16 (Pilot-0-Readiness: Phase 1 done → WP-D als Nächstes)

> **ERSTE AKTION:** `git fetch origin && git checkout main && git pull --ff-only` — main muss `650e8690` (oder neuer) sein. Es wird parallel auf main gepusht (finanzen-FE) → **vor jedem eigenen Push pullen/rebasen**.
> **Plan-Datei (Master für diese Arbeit):** `~/.claude/plans/rolle-du-bist-mein-velvet-muffin.md` — „Pilot-0-Readiness: Terminbuchung + Passwort-Reset dichtmachen". WP-A/B/C erledigt, **WP-D/E/F offen**.

## Kontext (warum diese Arbeit)
Tiefen-Scan 2026-06-16 fand: die zwei Pilot-0-kritischen Flows (Online-Terminbuchung, Passwort-Reset) waren backend-fertig, aber FE/Web-Auslieferung fehlte. Dialer-Consent + Demo-`userName` waren bereits real geschlossen (kein Handlungsbedarf). **Pilot = allgemeiner erster Pilot, NICHT ZFA-spezifisch** (ZFA aktuell strittig) → alles pilot-agnostisch/slug-basiert bauen, keine Branchen-Hardcodes.

## Diese Session erledigt (Phase 1)
- **WP-A** `932fdee2` — Public-Booking erzeugt jetzt internes Kalender-Event (`grpcCalClientAdapter` in `backend/internal/server/calendar_grpc.go`, Owner-als-Actor; `TenantID` in `PublicBookingCalEventInput`). go build/vet/test grün. **Gepusht.**
- **WP-C** `650e8690` — Passwort-Reset-FE: `desktop/.../modules/auth/ForgotPasswordPage.tsx` + `ResetPasswordPage.tsx`, `api/auth-reset-client.ts` (unauth, handgeschrieben — types.ts hat die Pfade nicht), App.tsx-Routen (**ohne** GuestRoute), Login-Link, MSW (`mocks/handlers/auth.ts`), i18n ×4 (13 `auth.*`-Keys), `scripts/qa-auth-reset.mjs`. Screenshot-QA sauber. **Gepusht.**

## 🚨 WP-B HÄNGT LOKAL (uncommitted) — Deploy-Gate
SMTP-Startup-Assertion liegt **uncommitted** im Working-Tree (5 Dateien): `backend/internal/config/config.go` (neue `RequireSystemSMTP`-Requirement), `backend/cmd/auth/main.go` (opt-in), `deploy/docker/docker-compose.yml` (SMTP-Passthrough an auth), `deploy/docker/PRODUCTION_TEMPLATE` + `backend/.env.example` (Doku).
- **NICHT committen/pushen, bis `SYSTEM_SMTP_*` in `/opt/kmuhub/.env.production` gesetzt ist.** `COSMI_ENV=production` ist scharf → sonst crash-loopt der auth-Service beim nächsten CD-Deploy (kein Boot ohne SMTP-Host/User/Pass).
- **Luke-Aktion (SMTP-Setup — entsperrt WP-B + Mail-Versand). Reihenfolge: Env zuerst, Assertion-Commit danach.**
  1. **Provider wählen** — wegen EU-Datensouveränität EU/CH: Brevo (FR), Mailjet (FR), Scaleway TEM (FR) oder Infomaniak (CH); Free-Tier reicht für Pilot. (Postmark/SES technisch ok, aber US.)
  2. **Absender-Domain verifizieren** — `zentria.tech` beim Provider als Sender anlegen, die angezeigten **SPF + DKIM (+ DMARC)** DNS-Records setzen, sonst Spam.
  3. **Credentials holen** (Host, Port 587, User, Passwort/API-Key).
  4. **Auf dem Server:** `ssh -i ~/.ssh/hetzner_kmuhub deploy@178.104.38.195` → `/opt/kmuhub/.env.production`:
     ```
     SYSTEM_SMTP_HOST=<provider-host>
     SYSTEM_SMTP_PORT=587
     SYSTEM_SMTP_USER=<user-oder-api-key>
     SYSTEM_SMTP_PASSWORD="<passwort>"   # Quotes bei Sonderzeichen
     SYSTEM_SMTP_FROM=noreply@zentria.tech
     PASSWORD_RESET_BASE_URL=https://zentria.tech/reset-password   # zeigt auf die WP-E-Astro-Seite!
     ```
  5. **Dann** WP-B committen + pushen (CD deployt; auth liest Env beim Boot).
  - ⚠ HOST/USER/PASS/FROM können **jetzt** gesetzt werden (entsperrt WP-B + Forgot-Mail-Versand). `PASSWORD_RESET_BASE_URL` muss auf die **Web-Reset-Seite** zeigen — die existiert erst nach **WP-E** (Astro). Bis dahin ist der Klick-Flow aus der Mail nicht end-to-end.

## OFFEN — WP-D (NÄCHSTES, Desktop-FE, deploy-sicher)
**Booking-Admin-UI scharfschalten.** Backend-CRUD ist fertig (`backend/internal/gateway/route_booking.go:39-53`, gRPC `calendar_grpc.go:1689-1821`), aber FE ist reiner Mock.
- Neu: `api/booking-client.ts` + Hook `useBookingPages` (List/Create/Get/Update/Delete gegen `/api/v1/calendar/booking-pages`). ⚠ `api/types.ts` hat die booking-pages-Pfade evtl. nicht → wie bei auth handgeschriebener Client (Muster `api/security-client.ts`) **oder** `npm run api:generate` (prüfen, ob sauber).
- Umbau in `desktop/src/renderer/src/modules/kalender/KalenderPage.tsx`: `TerminbuchungTab`/`ExternalBookingPreview` (~Z.1098) von Mock-Konstanten (`MOCK_BOOKINGS` ~181, `BOOKING_SERVICES` ~156, `EXTERNAL_TIME_SLOTS` ~175, `handleBook` ~1137 = nur `toast.success`) auf echten Hook. Mindest-Ziel: **eine** Buchungsseite (Slug/Services/Verfügbarkeitsregeln) anlegbar + editierbar.
- MSW-Demo-Handler für booking-pages ergänzen (`mocks/handlers/calendar.ts` hat aktuell keine).

## OFFEN — WP-E (Astro, Repo `zentria-website`) + WP-F (Backend)
- **WP-E:** öffentliche Seiten auf zentria.tech (Astro 5, Vercel), rufen die public Cosmi-API:
  - `/book/[slug]` — `GET /api/v1/public/booking-pages/{slug}` + `…/availability` + `POST /api/v1/public/bookings` (unauth, Slot-Picker + Bestätigung).
  - **`/reset-password`** — Web-Landeseite für den E-Mail-Link (`PASSWORD_RESET_BASE_URL` zeigt dorthin; Desktop-Hash-Route kann den Mail-Link nicht empfangen). POSTet `/api/v1/auth/reset-password`.
- **WP-F (Backend, KMU-Hub):** Gateway-CORS-Allowlist für `/api/v1/public/*` um zentria.tech-Origin erweitern (Arch-Regel #7). ⚠ **Verifizieren:** wie der unauth Public-Pfad Slug→Tenant auflöst (RLS/sysctx) — sicherheitskritisch.

## ⚙ Arbeitsweise für die nächste Session (Luke-Vorgabe)
- **An Subagents delegieren, um das Haupt-Kontext-Fenster klein zu halten.** Große Erkundungen via `Explore` (read-only); große Bau-Wellen via `general-purpose`-Subagent (Sonnet, `CLAUDE_CODE_SUBAGENT_MODEL=sonnet`). Pause-Gate „NICHT pushen" an Subagents geben (siehe Memory `feedback_subagent_for_large_waves`). Max 3 parallel.
- **Build-+-Verify-Standard pro Phase:** bauen → i18n ×4 (`{var}`, ICU) → Demo-MSW-Handler (stateful wo Buttons wirken) → **LSP/gescopter tsc, NIE kalter tsc** (crasht/timeoutet projektweit) → Playwright-QA + **Screenshots WIRKLICH ansehen** → ein Commit + **vor Push pullen** → push.
- **QA-Rezept:** `npm run dev` (`:5173`, Demo-Mode = MSW + Auto-Auth), `node scripts/qa-<x>.mjs` (Vorlage `scripts/qa-auth-reset.mjs`: `addInitScript`-Electron-Stub, Raw-Key-Scan, Screenshots → mit Read ansehen). Danach Port killen: PowerShell `Get-NetTCPConnection -LocalPort 5173 -State Listen | % { Stop-Process -Id $_.OwningProcess -Force }`.

## Deferred / Follow-ups
- `npm run api:generate` (Spec hat forgot/reset/booking, `api/types.ts` driftet) — bewusst aufgeschoben.
- `.planning/SESSION-START.md` ist noch untracked (Prompt-Template) — bei Bedarf committen.

## UPDATE (2026-06-16, später Abend) — Pilot-0 Backend-Härtung
- **WP-D war bereits erledigt** (in der Buchungsseite-Live-Session gebaut): `api/booking-client.ts`,
  `useBookingPages`, `modules/kalender/booking/` (Panel/Editor/Preview), MSW-Handler. Die „FE ist reiner
  Mock"-Aussage oben ist veraltet. `zentria.tech/book/zentria-demo` ist live.
- **WP-F neu bewertet:** Die Buchungsseite läuft über das **BFF-Proxy-Pattern** (Astro-SSR server-to-server),
  nicht browser-direkt → **Browser-CORS für zentria.tech wird NICHT gebraucht** (der CORS-Punkt ist ein
  No-op). Der echte WP-F-Wert war die **Slug→Tenant-Sicherheit**: jetzt via global-eindeutigem Aktiv-Slug-
  Index (Migration 000149) abgesichert; voller Tenant-Diskriminator bleibt Option-B-deferred.
- **Email-Normalisierung erledigt** (war Pre-Launch-Follow-up): lower(trim) an allen Auth-Entrypoints +
  Migration 000148 (Backfill + Unique-Index auf lower(email) für users + invitations).
- **WP-B (SMTP-Assertion) bleibt weiter uncommitted** bis `SYSTEM_SMTP_*` auf dem Server steht (unverändert).
- ⚠ Vor Push der Migrationen 148/149: Prod-Pre-Flight-Queries (Dedup-Check) müssen 0 Zeilen liefern — siehe
  Kommentar-Header in den .up.sql-Dateien.
