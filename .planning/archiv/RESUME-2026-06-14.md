# Resume / Handoff — 2026-06-14 Abend (zeiterfassung fertig → nächste Phase)

> **ZUERST:** `git fetch origin && git checkout main && git pull` — main muss auf `0303a37` (oder neuer) stehen.
> Voller Modul-Verlauf mit Pfaden/Befunden: `.planning/reviews/zeiterfassung.md` (unten die P1–P5- + Fix-Einträge).

## Was heute fertig wurde: zeiterfassung P1–P5 + Darien-Feedback (8 Commits)

`eb60802` P1 Fundament+Konsolidierung · `9955ab0` P2 manuelle Einträge+Projekt/Kunde · `6c0c1c7` P3 Auswertungen(recharts) · `a7a51a0` P4 Moduleinstellungen+CSV-Export · `6ab4b8c` P5 Team+Genehmigung · `e966d6c` Fix (Wochensoll→HR, stateful Clock) · `0303a37` Tagesziel aus Vertrag abgeleitet.

**Modul ist auf voller Markt-Parität** (clockodo/Papershift/Harvest): Timer, manuelle Einträge, ArbZG, Stundenkonto, Kunde/Projekt/Leistung+billable, Pausen-/Arbeitszeit-Regeln, Auswertung+Export, Team-Zeiterfassung+Wochen-Freigabe. Alles HR-API-backed (mock-first), LSP 0, i18n ×4 Parität, Screenshot-QA grün.

## Offene Punkte an zeiterfassung (klein, optional)
- **Dead-Code-Cleanup (empfohlen als Erstes):** Die 10 toten Views unter `desktop/src/renderer/src/modules/profil/tabs/zeiterfassung/` + `stores/timetracking.ts` sind durch die HR-API-Portierung überflüssig. **NICHT blind löschen:** `modules/team/TeamPage.tsx` + `hooks/useTimerTick.ts` nutzen den Store noch — erst deren Nutzung prüfen/ablösen, dann löschen. (Header-Widget `dashboard/widgets/TeamWorktime` nutzt schon HR-API, ist safe.)
- Orphan-i18n-Keys `zeiterfassung.settings.personal.dailyTarget*` + `zeiterfassung.settings.rules.weeklyTarget` sind ungenutzt (harmlos) — bei i18n-Cleanup mitnehmen.
- Backend-Gaps gebündelt in `.planning/backend-gaps.md` §zeiterfassung (echte Endpoints für balance/projects/entries/analytics/team/weeks + `totalBreakMinutes` in Weekly-Summary + echte Vertrags-Wochenstunden statt `workDaysPerWeek×8`).

## Nächste Phase — Optionen (mit Darien klären)
1. **zeiterfassung Dead-Code-Cleanup** (klein, schließt das Modul sauber ab).
2. **Nächstes Kernmodul** — Kandidaten aus `module-phase-plans.md`: **finanzen** (größtes ungestartetes, aber Strategie geändert → Re-Plan nötig, `finanzen-buchhaltung-strategy.md`), **team** (HR-Backend frisch, settings-komplett), **work-FE P1** (Labels/Custom-Fields-Backend ist da).
   → Workflow: Markt-Abgleich (`cosmi-modul-marktvergleich.txt` + Web) → mit Darien besprechen → 5er-Phasen-Batch → bauen mit Loop.

## Arbeits-Loop (verbindlich, jede Phase) — hat sich heute bewährt
bauen → i18n ×4 (ICU `{var}`) → Demo-Mock (stateful wo Buttons wirken sollen!) → **LSP-Diagnose** (`mcp__ide__getDiagnostics` pro Datei, NICHT kalter tsc — der timeoutet projektweit) → Playwright-QA + **Screenshots WIRKLICH ansehen** (heute 4 echte Bugs nur so gefunden: team.ts-Doppelquelle, getrennt-statt-gestapelt-Bars, Pausen-NaN, statischer Clock) → **Design-/Polish-Review (impeccable) + direkt polieren** (Darien-Vorgabe) → Review-Faden → ein Commit + Push pro Phase.

## Wichtige Lehren (heute neu/bestätigt)
- **LSP statt kaltem tsc** als Typecheck-Gate ([[project_typecheck_slow]] aktualisiert).
- **Demo-Mocks stateful bauen** — statische Mocks lassen Buttons wirkungslos wirken.
- **Settings-Scope:** Pro-Mitarbeiter-Vertragsdaten (Wochensoll/Tagesziel) gehören in Team/HR, in Moduleinstellungen höchstens read-only persönlich ([[feedback_settings_scope_hr_data]]).
- **mocks/handlers/<modul>.ts gehört der Modul-Lane** — team.ts überschattete hr.ts (Handler-Reihenfolge in `mocks/handlers/index.ts` zählt, erste Übereinstimmung gewinnt).
- Darien checkt in **Cosmi live** gegen (`npm run dev` im `desktop/`, Electron-Fenster); nach Mock-Änderungen Fenster neu laden (Strg+R), HMR re-initialisiert In-Memory-Mock-State nicht.
