# „Anpassungen" (Self-Service-Customization) — Bau-Fortschritt

> SSOT-Konzept: `KONZEPT.md`. Recherche: `IST-A/B/C.md` + `MARKT-A/B/C.md`.
> Stand: 2026-07-21 (Session #25).

## Editor-Pivot — Rollout (ab Session #28, 2026-07-25)

> Spec = `EDITOR-PIVOT-SPEC.md`, Modul-Audit = `MODUL-AUDIT.md`. **Pilot = Helpdesk** (Darien 2026-07-25, weil state-basiert → sofort begehbar; Kontakte ist router-blockiert → Sonderweg später).
> Kern-Befund Audit: nur `kontakte` + `dialer` sind router-basiert; ~14 lohnende Module state-basiert → R2 dort geschenkt.

| Phase | Inhalt | Status | Commit |
|---|---|---|---|
| **Motor: R1-Nav** | `useBlocker(true)` in EditorWorkspace | ✅ (Session #27b) | `6511254b` |
| **P1** | Motor: EditableText `interactive`-Modus (Doppelklick-Rename für Reiter) + `useEditorGuard` (Mutationen no-op). Helpdesk instrumentiert: Reiter, Tabellen-Header, Statistik-Labels/Überschriften, Detail-Sektionen/Meta, KB-Badges. | ✅ **fertig + verifiziert** | _(dieser Commit)_ |
| **P2** | Wertelisten-Konsum: Helpdesk liest `ticket_priority` via `useModuleValueSet` (Motor: Draft-Wertelisten über EditorSurface in die Sandbox) → Wertelisten-Panel wirkt LIVE im Modul. _(Custom-Fields-Labels-in-place vertagt — Felder-Panel deckt Feld-CRUD; „Label im Detail klicken→editieren" = späterer Polish.)_ | ✅ **fertig + verifiziert** | _(dieser Commit)_ |
| **P3** | R4 Tab-Sichtbarkeit: neue Dimension `moduleAreas` (Typen→Resolver→Draft→Deploy→Rollback), Helpdesk liest sie (`useModuleAreas`), Editor-„Bereiche"-Panel mit Ein/Aus-Schaltern | ✅ **fertig + verifiziert** | _(dieser Commit)_ |
| **P4** | Chrome: leerer Properties-Zustand → selbsterklärender Kontext-Inspektor „So passt du an" (Klick→umbenennen · Doppelklick→Reiter · links Felder/Wertelisten/Bereiche) | ✅ **fertig + verifiziert** | _(dieser Commit)_ |
| **Rollout** | ~14 Module instrumentieren (Rich zuerst: finanzen/inventar/einkauf/vertraege/produktion/vermietung/formulare/work), ab Pilot per Sub-Agents parallelisierbar | ⏳ offen | — |

### P1 — Verifikations-Nachweis
- **Dateien:** `components/customization/EditorSurface.tsx` (+`interactive`-Prop, +`useEditorGuard`), `modules/helpdesk/HelpdeskPage.tsx` (Instrumentierung + Guards), `scripts/i18n-editor-pivot-p1.mjs` (+2 Keys ×4: `helpdesk.tabs.ticketsLabel`, `customization.editor.actionBlocked`), `tsconfig.customcheck.json` (+HelpdeskPage).
- **Gates:** scoped tsc **0 Fehler in EditorSurface + HelpdeskPage** (Rest = Alt-Baseline in mocks/handlers + sanitize, transitiv) · `eslint` beide Dateien clean · i18n +2 ×4 (echte fr/it, Du-Form) · **QA `qa-editor-helpdesk-p1.mjs` 6/6 PASS, 0 pageerrors, Bilder angesehen**: Reiter begehbar (Einfach-Klick) ✓ Tabellen-Header „Betreff"→„Anliegen" live + amber Ring + Zähler ✓ Reiter „Statistik"→„Auswertung" per Doppelklick ✓ „Neues Ticket" → Guard-Toast „Im Editor deaktiviert", Dialog bleibt zu ✓ keine rohen Keys ✓.
- **Muster für Rollout:** statische Labels → `<EditableText dkey=… />` (Einfach-Klick-Rename); Labels in Controls (Reiter/Karten) → `interactive` (Einfach-Klick navigiert, Doppelklick benennt um); Mutationen/rausführende Aktionen → `guard(handler)`; State-Navigation (Reiter/Detail öffnen) bleibt ungeguarded (begehbar).

### P2 — Verifikations-Nachweis
- **Motor:** `EditorSurfaceValue.valueSets` (Draft-Wertelisten) + Hook `useModuleValueSet(id)` (`resolveValueSet(id, false, draftOverlay)` — live Layer default⊕vendor⊕tenant, im Editor + Draft). `ModuleSandbox` reicht `valueSets` aus `useDraftConfig` in die Surface. WertelistenPanel-Doc aktualisiert (Module konsumieren jetzt).
- **Konsum:** `HelpdeskPage` baut `priorityLabels` aus `useModuleValueSet('ticket_priority')` (Fallback i18n) — Tabellen-Chips, Detail-Badge, Stats-Balken, Filter-Optionen, Neu-Ticket-Radio. **Erstes Modul überhaupt, das die Customization-Schicht liest** (schließt den „Konsum-Gap" für Priorität).
- **Gates:** scoped tsc 0 Fehler (EditorSurface/ModuleSandbox/HelpdeskPage/WertelistenPanel) · eslint clean · **QA `qa-editor-helpdesk-p2.mjs` 4/4 + Bild angesehen**: Panel zeigt Tenant „Rückfrage" ✓ Option „Mittel"→„Standard" → Tabellen-Chips LIVE „Standard" ✓ 0 pageerrors ✓.
- **Nebeneffekt (gewollt):** Live-Helpdesk zeigt jetzt die Tenant-Werteliste (low = „Rückfrage", Seed-Demodaten) statt der i18n-Enums — Beweis dass der Konsum end-to-end greift. Falls Standard gewünscht: Tenant-Seed in `customization.ts` entfernen.

### P3 — Verifikations-Nachweis
- **Neue Dimension `moduleAreas`** (moduleKey → areaKey → enabled, sparse, absent = an): Typen (`ModuleAreaMap`/`ModuleAreasOverlay` + Payload) · Resolver `resolveModuleAreas(moduleKey, base?, draftOverlay?)` (default-all-on ⊕ vendor ⊕ tenant ⊕ draft) · Draft-State + `setDraftModuleArea` + changeCount + buildPayload · **Deploy + Rollback** (applyDraftToTenant/snapshotTenant/restoreTenant erweitert → fährt dieselben Schienen wie Labels/Wertelisten/Felder) · `hasCustomization`/`clearAllCustomization`.
- **Motor + Konsum:** `EditorSurfaceValue.moduleAreas` + Hook `useModuleAreas(moduleKey)`; `ModuleSandbox` reicht Draft durch. `EditorModuleDef.areas` (Helpdesk = 3 Reiter; Kontakte = [] router-blockiert). Helpdesk filtert `visibleTabs` zusätzlich per `areaEnabled[key] !== false` (RBAC ∧ Bereich).
- **Editor-UI:** 4. Trio-Sektion „Bereiche" (`EditorTrioNav` + `EditorPropertiesPanel`) + neues `BereichePanel.tsx` (Ein/Aus-Schalter pro Reiter, Sichtbar/Ausgeblendet, Empty-State für Module ohne Areas).
- **Gates:** scoped tsc 0 Fehler (alle 10 P3-Dateien) · eslint clean · vitest 12/12 (Draft-Store) · i18n +8 ×4 · **QA `qa-editor-helpdesk-p3.mjs` 4/4 + Bild angesehen**: Bereiche-Panel 3 Schalter ✓ „Statistik" ausblenden → Reiter live weg aus der Modul-Leiste ✓ 0 pageerrors ✓.

### P4 — Verifikations-Nachweis
- **Kontext-Inspektor:** Der leere Properties-Zustand (Default, wenn keine Trio-Sektion gewählt) ist jetzt ein selbsterklärender Guide „So passt du an" (3 nummerierte Schritte mit Accent-Icons + Entwurf-Subtitle) statt „wähle links…" — passt zur Edit-in-place-Primärinteraktion. `customization.editor.props.empty` dadurch tot (belassen). Selektions-reaktiver Per-Element-Inspektor = späterer Ausbau.
- **Gates:** scoped tsc 0 Fehler (EditorPropertiesPanel) · eslint clean · i18n +5 ×4 · **QA `qa-editor-helpdesk-p4.mjs` 2/2 + Bild angesehen**: „So passt du an" + 3 Schritte ✓ keine rohen Keys, 0 pageerrors ✓.

**★ HELPDESK-PILOT KOMPLETT (P1–P4).** Muster steht für den Rollout über die Rich-Module (finanzen, inventar, einkauf, vertraege, produktion, vermietung, formulare, work), ab hier per Sub-Agents parallelisierbar.

### Feedback-Runde (Darien lokal-Review, 2026-07-25) — F1 + F2

| # | Feedback | Lösung | Status |
|---|---|---|---|
| **F1** | Farbe einer Werteliste-Option (z.B. „Kritisch" auf orange) wirkte NICHT im Modul (nur Label kam durch, Farbe hartkodiert) | Modul rendert Chips via neue `VsChip` mit Inline-Farbe aus der Werteliste (`color-mix` Tint), Fallback Tailwind → Umfärben wirkt live | ✅ |
| **F2** | Status (Offen/In Bearbeitung/…) nirgends bearbeitbar — nur Priorität war Werteliste | `ticket_status`-Value-Set (5 Status + Farben) + in helpdesk `valueSetIds` registriert; Modul konsumiert Label+Farbe (Tabelle/Detail/Statistik/Filter/Status-Dropdown, Picker iterieren aktive Optionen); **„+ Neue Option" im Wertelisten-Panel** (add für Priorität+Status) | ✅ |

- **Markt-Recherche (Darien-Wunsch):** Custom-Status ist Standard (Zendesk/Freshservice/Desk365) — anlegen/umbenennen/umfärben/ordnen/deaktivieren; neue Status mappen intern auf System-Kategorie (offen/gelöst/geschlossen) für SLA-Logik. **Entscheidung: gehört in die Editor-Wertelisten** (ein Tool, konsistent), nicht separate Settings. Neue Status default = „offen-artig" (sichere Semantik); feinere Kategorie-Zuordnung + getrennte Agent/Kunde-Labels = späterer Ausbau.
- **Gates:** scoped tsc 0 Fehler · eslint clean · i18n +2 ×4 · **QA `qa-editor-helpdesk-f1f2.mjs` 6/6 + Bilder angesehen**: 2 Wertelisten je „Neue Option" ✓ Chip trägt Inline-Farbe aus Set ✓ Status „Offen"→„Neu" live (5 Chips) ✓ „Kritisch" auf grün umgefärbt → Chip live grün ✓ 0 pageerrors ✓.
- **Rollout-Konsequenz:** dasselbe Muster (Status+jede Status-artige Liste als Value-Set, Chips via `VsChip`, Picker iterieren aktive Optionen) gilt für ALLE Module — `VsChip` beim Rollout nach `shared/` heben.

## v1 — Fundament-Trio (Overlay-basiert)

| Stufe | Inhalt | Status | Commit |
|---|---|---|---|
| **v1.0** | Overlay-Config-Fundament: Typen + `resolveConfig` (default→vendor→tenant + Provenance) + MSW (labels/value-sets) + RBAC-Key `admin:customization:manage` + Audit + i18n ×4 | ✅ **fertig + verifiziert** | `a43bffcb` |
| **v1.1** | Custom-Fields-Editor (vereinheitlicht, 5 Entitäten, 9 Feldtypen, Progressive Disclosure, Soft-Delete-Schutz) + „Anpassungen"-Hub als 9. Admin-Tab | ✅ **fertig + verifiziert** | `2bdd3407` |
| **v1.2** | Label-Override-Editor („Begriffe"-Tab, Provenance Standard/Von Zentria/Angepasst, Reset, Sprach-Auswahl) | ✅ **fertig + verifiziert** (Live-Wirkung gefixt) | `a57e11d7` + Fix |
| **v1.3** | Value-Sets-Editor (zentrale Wertelisten, Referenz-Auflösung in ≥1 Modul als Proof, Soft-Delete) | ⏳ offen | — |
| **v1.4** | „Anpassungen"-Hub (Admin-Fläche + 3 Editoren + Modul-Schnellzugriffe in `ModuleSettingsShell` + Vendor/Tenant-Herkunfts-Banner + Template-Stub) | ⏳ offen | — |

### v1.0 — Verifikations-Nachweis
- **Dateien:** `api/customization-types.ts` (neu), `mocks/data/customization.ts` (neu), `mocks/handlers/customization.ts` (neu), `handlers/index.ts` (+Registrierung), `config/capability-catalog.ts` (+Key), `mocks/data/rbac.ts` (+it_admin-Grant; admin via `catalogCapabilityKeys('admin')`), `mocks/data/audit-events.ts` (+3 Actions), `scripts/i18n-customization-v10.mjs` (32 Keys ×4), `scripts/qa-customization-resolver.mjs` (12-Test-Smoke), `tsconfig.customcheck.json`.
- **Resolver:** `resolveLabelOverrides(locale, base?)` + `resolveValueSet(id, base?)` → Wert + `provenance: 'default'|'vendor'|'tenant'` je Eintrag. tenant > vendor > default. `base=1` = reine Baseline (R-6-Muster).
- **Gates:** scoped tsc **0 Fehler in allen v1.0-Dateien** (8 Rest = Alt-Baseline in automation/crm/finance/hr, transitiv via index.ts) · `eslint` clean (selbst nachgeprüft, korrekter Pfad) · i18n 32 Keys ×4 mit echten fr/it (selbst stichprobe: Anpassungen/Customization/Personnalisation/Personalizzazione) · Smoke 12/12 (tenant>vendor>default, Provenance, base) · admin-Key-Kette selbst verifiziert.
- **Bewusst ausgeklammert:** Custom Fields (eigene BE-Persistenz → v1.1 andocken), Vendor-Session-Detection (`activeConfigLayer()` = immer 'tenant' bis R-5-GDAP-Wiring).

### v1.2 — Stand + offener Punkt (KRITISCH für nächste Session)
- **Funktioniert + verifiziert:** „Begriffe"-Editor (Sub-Tab im Anpassungen-Hub), Provenance-Badges (Standard grau / Von Zentria blau / Angepasst grün, R-6-Muster), Umbenennen/Reset/„Alle zurücksetzen", Sprach-Auswahl (de/en/fr/it), Persistenz via MSW, Audit-Events. i18n ×4 echt, scoped tsc 0 Fehler, eslint clean.
- **2 Fixes von mir (verifiziert):** (a) **Default-Anzeige** — „Cosmi-Standard" zeigte den gemergten Wert statt des echten Code-Defaults (getResourceBundle nach addResourceBundle kontaminiert). Fix: `getLabelDefault` liest einen Snapshot, der in `useLabelOverlay.captureDefaults` VOR dem Merge eingefroren wird. QA-bestätigt („Support-Desk / Cosmi-Standard: Helpdesk"). (b) **Tote Whitelist-Keys** — `nav.crm/nav.work/nav.admin.label` haben KEINEN Konsumenten im FE; die Sidebar rendert `layout.navItems.*`. Fix: Whitelist + Vendor-Seed + BegriffeTab-Gruppe auf die echten `layout.navItems.contacts/projects/tasks/team/finance/helpdesk/admin` umgestellt.
- **✅ GELÖST (Session #25, root-cause statt Symptom):** Die bisherige Diagnose („`changeLanguage(sameLocale)` erzeugt kein neues `t`, useMemo re-computed nicht") war **falsch**. Quellen-Verifikation: i18next 26 emittiert `languageChanged` auch bei gleicher Locale (`done()` läuft immer, `i18next.js:1981`); react-i18next 17 recomputt `t` bei jedem Revision-Bump (`useTranslation.js:77-82`) → `t` wechselt sehr wohl die Identität. **Echte Ursache: `i18next-icu` memoized kompilierte MessageFormat-Instanzen nach `lng.ns.key` (`i18nextICU.js:4235`) und liefert `bindI18n: ''` + `bindI18nStore: ''` als Default (`:4178-4181`) → `clearCache` wird NIE an ein Event gebunden.** Sobald die Sidebar `t('layout.navItems.contacts')` einmal rendert (cached „Kontakte"), liefert der ICU-Cache diesen Wert für immer — jeder `addResourceBundle`+Re-Render liest den veralteten Formatter. Der Editor (React Query, kein ICU) zeigt korrekt, die Sidebar (ICU-gecacht) bleibt stale. Betraf **beide** Fälle: Vendor-Bootstrap UND Tenant-Live.
  - **Fix (`i18n/i18n.ts`):** `.use(new ICU({ bindI18nStore: 'added removed', bindI18n: 'languageChanged' }))` — das Store-`added`-Event von `addResourceBundle` leert jetzt den ICU-Cache, `languageChanged` deckt Sprach-Wechsel. Ein Einzeiler an der Wurzel; `applyLabelOverlay`/Bootstrap unverändert.
  - **Isoliertes Experiment (kontrolliert):** DEFAULT-Config → `t()` bleibt „Kontakte" nach addResourceBundle+changeLanguage (STALE ✗); mit Fix → „Klienten" (WORKS ✓, greift schon nach addResourceBundle allein).
  - **QA `qa-customization-v12-final.mjs` 3/3 PASS + Screenshot angesehen:** P1 Sidebar zeigt „Patienten" (Vendor-Bootstrap) ✓ · P2 Umbenennen→„Klienten" → Sidebar **live „Klienten" ohne Reload** ✓. scoped tsc 0 Fehler in geänderten Dateien (nur 11 Alt-Baseline in hr/finance/automation/crm/sanitize) · eslint `i18n.ts` clean.

## Nächste Stufen = UI-Editoren (visuell + Screenshot-QA-pflichtig)
v1.1–v1.4 bauen sichtbare Oberflächen → Standard-Gate inkl. Playwright-Screenshot-QA + Bilder ansehen. Jede Stufe: Agent-Bau + Review + i18n ×4 + scoped tsc + eslint + Screenshot-QA + 1 Commit.

---

# NEU-AUSRICHTUNG: Modul-Editor (ab Session #26, 2026-07-22)

> **v1.3/v1.4 der alten Roadmap SUPERSEDED.** Darien-Richtung nach v1.0–v1.2: die Admin-Listen sind zu mager + modul-entkoppelt. Neu = **modul-zentrischer edit-in-place-Editor im eigenen Fenster** (Sandbox, isoliert vom Live). Recherche-Gate durchlaufen (`IST-EDITOR.md` + `MARKT-EDITOR.md` + `DRAFT-DEPLOY.md`), 4 Entscheide gefallen. **SSOT = `EDITOR-KONZEPT.md`.** Das Fundament v1.0–v1.2 BLEIBT die Maschinerie (Resolver, ICU-Live-Fix, Custom-Fields, Value-Sets, Audit, RBAC-Key); nur die UX wird neu.

**4 Entscheide (Darien 2026-07-22):** ① In-App-Overlay (self-contained für späteren Fenster-Umzug) ② nur Trio-Panel v1, Layout = Stufe 2 ③ Terminierung gleich in v1 (Job gemockt, Luke-Cron) ④ 2 Pilot-Module (Kontakte + Helpdesk).

## Editor-Bau-Phasen

| Phase | Inhalt | Status | Commit |
|---|---|---|---|
| **E-1 Fundament** | Draft-Schicht (4. Overlay-Ebene) im Resolver · `DraftConfigProvider` · Drafts-Store (save/deploy/schedule/rollback + Scheduler-Mock) · Audit-Actions | ✅ **fertig + verifiziert** | (dieser Commit) |
| **E-2 EditorFrame** | Overlay-Rahmen (self-contained) · Toolbar · Amber-Banner · Drei-Panel · echtes Modul in Sandbox · Footer | ✅ fertig — **in E-2b auf eigenes Fenster umgestellt** | `d64e8e1e` |
| **E-2b eigenes Fenster** | Overlay → echtes Electron-Fenster (Darien-Entscheid 2026-07-22: parallel arbeiten + live-vs-Entwurf vergleichen). IPC + Route + BroadcastChannel-Sync. Behebt Blur + Router-Nesting. | ✅ **fertig + verifiziert** | (dieser Commit) |
| **E-3 Begriffe-Panel** | Modul-Begriffe im Panel bearbeiten (Draft-verdrahtet) · **Live-Vorschau im Sandbox-Modul** · Provenance + Reset · Tipp-Lag-Debounce | ✅ **fertig + verifiziert** | (dieser Commit) |
| **E-3b Wertelisten-Panel** | Value-Sets im Panel editieren (Draft-verdrahtet, Optionen/Farben/Soft-Delete, In-Panel-Chip-Vorschau) | ✅ **fertig + verifiziert** | (dieser Commit) |
| **E-3c Felder-Panel** | Custom-Fields im Panel (Draft-verdrahtet, Snapshot-Diff-Deploy) + `helpdesk_ticket`-Entity | ✅ **fertig + verifiziert** | (dieser Commit) |
| **(später) Modul-Konsum** | Module lesen Customization-Schicht (Titel/Chips/Felder) → volle Live-Vorschau. **Darien 2026-07-22: editor-first, danach eigene Runde. NEU: 2 Tiefen — Tiefe 1 Listen-Titel/Chips, Tiefe 2 Custom-Fields in Detail-/Anlege-Formularen pro Modul (Darien-Punkt 2026-07-22)** | ⏳ nach Editor | — |
| **E-4 Modul-Galerie** | `AnpassungenHubPage` → Kacheln mit Dimensions-Chips + Live-Status · Rollout-Liste | ✅ **fertig + verifiziert** | (dieser Commit) |
| **E-5 Deploy-Dialog** | „Übernehmen" → Dialog (Jetzt/Terminiert + DatePicker + Ankündigung), Als-Entwurf, Sync-Broadcast bei Jetzt | ✅ **fertig + verifiziert** | (dieser Commit) |
| **E-5b Entwurf-Liste + Rollback** | gespeicherte Entwürfe/geplante Rollouts + Rollback-UI + Cross-Window-Draft-Mirror | ✅ **fertig + verifiziert** | (dieser Commit) |

### E-1 — Verifikations-Nachweis
- **Typen (`api/customization-types.ts`):** `ConfigProvenance` +`'draft'` (4. Stufe, gewinnt) · `LocaleLabelMap` exportiert · `CustomizationDraft`/`CustomizationDraftPayload`/`DraftStatus`(draft→scheduled→live→superseded)/`DeployMode`/`SaveDraftInput`/`DeployDraftInput`.
- **Resolver (`mocks/data/customization.ts`):** `resolveLabelOverrides(locale, base?, draftOverlay?)` + `resolveValueSet(id, base?, draftOverlay?)` — draft-Schicht gewinnt per Key/Option über tenant. Neu: `applyDraftToTenant()` (Promotion in tenant-Layer, nur Whitelist-Keys, zählt Applied) · `snapshotTenant()`/`restoreTenant()` (Rollback-Basis) · `TenantSnapshot`-Typ.
- **Drafts-Store (`mocks/data/customization-drafts.ts`, neu):** `saveDraft` (Blueprint, kein Impact) · `commitDraftNow`/`scheduleDraft`/`deployDraft` (3-Wege) · `promote()` (snapshot→apply→live, altes live→superseded) · `runDueScheduledDeploys()` (Scheduler-Mock, steht für Lukes Cron) · `rollbackDeploy()`/`canRollback()` · Audit `draft_saved/draft_deleted/deploy_live/deploy_scheduled/rolled_back`.
- **Provider (`modules/admin/anpassungen/editor/DraftConfigProvider.tsx`, neu):** React-Context+Reducer, hält Draft-Session-State, `setDraftLabel/resetDraftLabel/setDraftValueSet/resetDraftValueSet/resetAll/buildPayload/loadDraft`, `isDirty`+`changeCount`. Live-Preview: `applyLabelOverlay(resolveLabelOverrides(locale, false, draftLabels))` bei jeder Änderung. **R-1-Scrub:** beim Unmount `restoreLabelOverlay(locale, touchedKeys, resolveLabelOverrides(locale))` → Draft-only-Keys zurück auf Default (verhindert Live-Kontamination).
- **i18n-Helfer (`i18n/useLabelOverlay.ts`):** `restoreLabelOverlay(locale, scrubKeys, resolved)` neu (setzt Scrub-Keys auf `getLabelDefault`, re-applied dann das persistente Overlay).
- **BegriffeTab:** `draft`-Provenance-Badge (amber) ergänzt (Folge der Typ-Erweiterung).
- **Gates:** scoped tsc (`tsconfig.customcheck.json` +2 Dateien) **0 Fehler in allen E-1-Dateien** (11 Rest = Alt-Baseline sanitize/automation/crm/finance/hr) · `eslint` clean · **vitest `customization-draft.test.ts` 9/9 PASS** (draft>tenant-Merge, draft>default, base ignoriert draft, Value-Set-Draft-Option+Name, saveDraft-kein-Impact, commitDraftNow-Promotion, rollback-Restore, applyDraftToTenant-Whitelist-Filter, Scheduler due-vs-future).
- **E-1 hat KEINE UI** → statt Playwright ein Logik-Vitest. Screenshot-QA ab E-2 (EditorFrame sichtbar).
- **Luke-Paket (backend-gaps, nach FE-Bau):** `tenant_customization_drafts`-Tabelle (sparse, `scheduled_at`, Status-Maschine) · Promotion-Cron · Draft-Overlay serverseitig (nur Editor-Session) · Rollback modul-granular · Audit an Deploy-Routen · editierbares Manifest pro Modul.

### E-2 — Verifikations-Nachweis
- **Neue Dateien (`modules/admin/anpassungen/editor/`):** `EditorFrame.tsx` (near-fullscreen Radix-Dialog `96vw×94vh`, self-contained, Toolbar+Amber-Banner+Drei-Panel+Footer, Preview-Toggle blendet Panels aus, Footer-Buttons an `isDirty`/`saveDraft`/`commitDraftNow` verdrahtet) · `ModuleSandbox.tsx` (echtes Pilot-Modul in eigener QueryClient-Instanz + ErrorBoundary, `pointer-events-none` = view-only) · `EditorTrioNav.tsx` (Felder/Begriffe/Wertelisten + Live-Change-Count-Badges) · `EditorPropertiesPanel.tsx` (Bereichs-Intro + „Element wählen"-Empty-State) · `editorModules.ts` (Pilot-Registry Kontakte+Helpdesk). `AnpassungenHubPage` +temporäre Launch-Leiste (E-4 ersetzt sie durch die Galerie).
- **⚠ QA-Fund (wichtig, IST-EDITOR-Korrektur):** `MemoryRouter` in der Sandbox wirft **„You cannot render a `<Router>` inside another `<Router>`"** — die App hat schon `createHashRouter`/`RouterProvider` an der Wurzel, RR v6 verbietet Verschachtelung. **Fix:** kein MemoryRouter im In-App-Overlay; das Modul nutzt den ambienten App-Router (fließt durch das Radix-Portal), Cache-Isolation via Sandbox-QueryClient reicht, `pointer-events-none` verhindert Nav-Nebenwirkungen. MemoryRouter bleibt der Pfad für den späteren echten-Fenster-Modus (keine Verschachtelung dort).
- **Gates:** scoped tsc (`tsconfig.customcheck.json` +5 Dateien) **0 Fehler in allen E-2-Dateien** (13 Rest = Alt-Baseline inkl. jetzt transitiv sichtbarer KontaktePage/ImportContactsDialog) · `eslint` clean · i18n **+29 Keys ×4** (`scripts/i18n-editor-e2.mjs`, Du-Form, echte fr/it, ICU-Plural footer.changes) · **Playwright `qa-editor-e2.mjs` 5/5 PASS, 0 pageerrors, 4 Bilder angesehen:** Launch-Leiste ✓ Editor-Overlay mit Dialog+Amber-Banner+Trio-Nav+Footer + **echtes Kontakte-Modul rendert in Sandbox** (24 Kontakte, Kategorien, Suche) ✓ Toolbar-Titel „Patientenverwaltung bearbeiten" (Vendor-Override greift im Titel) ✓ Trio-Nav Begriffe→Bereichs-Intro ✓ Preview-Toggle→Panels aus, Canvas voll ✓ keine Raw-Keys ✓.

### E-2b — eigenes Fenster (Darien-Feedback nach E-2-Review)
**Darien-Review am Live-Build fand 2 Dinge:** (1) Helpdesk-Vorschau **verschwommen** bis zum Scrollen — Ursache: das zentrierte Overlay per `translate(-50%,-50%)` + Zoom-Animation rendert Text auf halben Pixeln (Blur bis Scroll-Repaint), **kein** Helpdesk-Bug. (2) Wunsch **eigenes Fenster** → parallel in Cosmi arbeiten + live-Stand vs. Entwurf vergleichen. **2 Entscheide:** ① Fenster ersetzt Overlay ② Cross-Window-Sync jetzt (BroadcastChannel). Beides behebt zugleich Blur **und** das Router-Nesting (beides waren Overlay-Artefakte).
- **Main-Prozess:** `ipc/editor-window.ts` (`editor:open-window`, `new BrowserWindow` 1280×860 → `#/editor-window?module=<key>`, 1:1 nach `compose.ts`) + Registrierung in `ipc/index.ts` + Preload-Bridge `editor.openWindow(key)` + Type.
- **Renderer:** `EditorWorkspace.tsx` (Editor-Content aus EditorFrame extrahiert, ohne Dialog — `EditorFrame.tsx` **gelöscht**) · `EditorWindowPage.tsx` (Route `#/editor-window`, liest `?module=`, rendert vollflächig, Close = `electronAPI.window.close()`/Web-Fallback navigate, unbekanntes Modul → Fallback) · Route in App.tsx + `/editor-window` in launch.ts GUEST_BOOT_ROUTES · AnpassungenHubPage-Kacheln → `electronAPI.editor.openWindow` (Web-Fallback: same-window navigate).
- **Cross-Window-Sync (`customization-sync.ts`):** Editor-Fenster funkt beim „Übernehmen" den Deploy-Payload via `BroadcastChannel('cosmi:customization')`; Hauptfenster-Listener (`useCustomizationSyncListener` in `DeskEnvironment`, nur Main-Shell) merged via `applyDraftToTenant` + `applyLabelOverlay` → Sidebar-Label wechselt live. Mit Lukes Backend = simpler Refetch; Channel bleibt für Instant-UX.
- **Gates:** scoped tsc Renderer **0 Fehler in E-2b-Dateien** (13 Alt-Baseline) + node-tsc Main/Preload **0 Fehler in editor-window/index/preload** (1 Alt-Baseline `main/index.ts` import.meta.env) · `eslint` alle 11 Dateien clean · i18n +1 Key ×4 (`i18n-editor-e2b.mjs`, unknownModule) · **Playwright `qa-editor-e2b.mjs` 5/5 PASS, 0 pageerrors, Bilder angesehen:** Helpdesk-Fenster **gestochen scharf von oben** (Blur weg) mit Toolbar+Banner+Trio+echtem Modul+Footer ✓ Kontakte-Fenster ✓ unbekanntes Modul → „Modul nicht gefunden" ✓ keine Raw-Keys ✓. **IPC-Fenster-Öffnen + Cross-Window-Sync live nur in Electron testbar** (Route-QA deckt das Rendering).

### E-3 — Begriffe-Panel + Live-Vorschau (der Pipeline-Kernbeweis)
**Architektur-Befund (wichtig, IST-EDITOR §Achse-2 bestätigt live):** KEIN Pilot-Modul konsumiert die Customization-Schicht — Titel sind hardcoded (`title="Helpdesk"`), Prio/Status laufen über modul-eigene i18n-Keys. Für echte Live-Vorschau muss das Modul den Wert aus der Schicht rendern. → **E-3 lean:** Begriffe-Panel Draft-verdrahtet + **Helpdesk-Titel auf `t('rbac.module.helpdesk')` verdrahtet** als Live-Beweis. Der globale i18n-Overlay (v1.2-ICU-Fix) trägt die Vorschau: Draft-Label → `restoreLabelOverlay` → Modul-`t()` re-rendert live.
- **`BegriffePanel.tsx`:** modul-gescopte Label-Liste (`EditorModuleDef.labelKeys`), aktuelle Locale, controlled Input schreibt in `setDraftLabel` (Draft-Layer, NICHT live-tenant), Provenance-Badge (default/vendor/tenant/**Entwurf** amber), „Standard: X"-Hinweis, Reset (`resetDraftLabel`), Mono-Key-Caption für Admins. In `EditorPropertiesPanel` bei Section „begriffe" gerendert (moduleKey durchgereicht).
- **Live-Preview-Fixes (2 echte Bugs):** (1) **Tipp-Lag** — `applyLabelOverlay` feuert `changeLanguage` = volles Sandbox-Re-Render pro Tastendruck → **120ms-Debounce** im Provider-Effekt (Input liest Draft-State synchron = responsiv, nur Vorschau trailt). (2) **Reset ließ Stale-Wert stehen** — `applyLabelOverlay` schreibt nur Nicht-Default-Keys, ein auf Default zurückgesetzter Key behielt den alten Override → Effekt nutzt jetzt `restoreLabelOverlay(scrub=alle touched Keys)` statt applyLabelOverlay (scrubbt erst auf Default, dann re-apply). (3) **Latenter E-1-Bug gefixt:** `customization.labels.provenance.draft` fehlte (Raw-Key-Gefahr, BegriffeTab-Draft-Badge nutzt ihn auch) → nachgelegt ×4.
- **Gates:** scoped tsc **0 Fehler in E-3-Dateien** (13 Alt-Baseline) · `eslint` clean · i18n +2 Keys ×4 (`i18n-editor-e3.mjs`) +1 (provenance.draft) · **Playwright `qa-editor-e3.mjs` 4/4 PASS, 0 pageerrors, Bild angesehen:** Begriffe-Panel rendert Inputs ✓ **Umbenennen „Helpdesk"→„Support-Center" → Sandbox-Modul-Header UND Toolbar-Titel live aktualisiert** + Zähler „1 Änderung" + Entwurf-Badge + Buttons aktiv ✓ Reset → wieder „Helpdesk" + „Keine Änderungen" ✓ keine Raw-Keys ✓.
- **★ ARCHITEKTUR-FORK — ENTSCHIEDEN (Darien 2026-07-22): editor-first.** Erst den Editor komplett (E-3b/E-3c Panels, E-4 Galerie, E-5 Deploy), dann **Modul-Konsum** (Module lesen die Schicht: Titel via `t(whitelistKey)`, Value-Set-Chips/Felder via Resolver) als eigene koordinierte Runde (FE ×32 Module + Lukes Backend). E-3 hat 1 Helpdesk-Titel exemplarisch verdrahtet.

### E-3b — Wertelisten-Panel
- **`WertelistenPanel.tsx`:** modul-gescopte Value-Sets (`EditorModuleDef.valueSetIds`: kontakte→deal_stages, helpdesk→ticket_priority). Pro Set: Name editierbar, Optionen (Label editierbar, 6 Farb-Swatches, Sichtbarkeit-Toggle = Soft-Delete mit Durchstreichung), Provenance-Badge + Reset, **In-Panel-Chip-Vorschau** der aktiven Optionen. Draft via `setDraftValueSet`/`resetDraftValueSet` (volle Set-Übernahme → Provenance 'draft'). In `EditorPropertiesPanel` bei Section „wertelisten".
- **In-Modul-Vorschau bewusst NICHT** (Modul-Konsum-Fork) — das Panel trägt die eigene Vorschau; die Prio-Chips im Helpdesk-List bleiben modul-hardcoded bis zur Konsum-Runde.
- **Gates:** scoped tsc **0 Fehler in E-3b** (13 Alt-Baseline) · `eslint` clean · i18n +6 ×4 (`i18n-editor-e3b.mjs`) · **Playwright `qa-editor-e3b.mjs` 4/4 PASS, 0 pageerrors, Bild angesehen:** Panel mit Set/Optionen/Vorschau ✓ Option „Hoch"→„Dringend" → Vorschau-Chip + Zähler + Entwurf-Badge live ✓ Reset stellt her ✓ keine Raw-Keys ✓.

### E-5 — Deploy-Dialog (der USP: terminiertes Deployment)
- **`DeployDialog.tsx`** (auf `DetailModal`): Zusammenfassung (Änderungs-Zähler + „Betrifft alle Nutzer") · Modus **Jetzt / Terminiert** (Karten-Toggle) · bei Terminiert `<input type=datetime-local>` (Default nächster Morgen 06:00) · optionale Ankündigung · Footer „Als Entwurf speichern / Abbrechen / Jetzt übernehmen|Rollout planen". Nutzt E-1-Store `deployDraft` (now→`commitDraftNow`+`publishCustomizationDeploy`-Broadcast · scheduled→`scheduleDraft` · draft→`saveDraft`), `onDeployed`=Fenster schließen. Footer „Übernehmen ▾" öffnet den Dialog (statt direkt zu committen).
- **Gates:** scoped tsc **0 Fehler in E-5** (13 Alt-Baseline) · `eslint` clean · i18n +13 ×4 (`i18n-editor-e5.mjs`) · **Playwright `qa-editor-e5.mjs` 4/4 PASS, 0 pageerrors, Bild angesehen:** Dialog mit Titel/Zusammenfassung/Modi/Ankündigung ✓ Terminiert→DatePicker+„Rollout planen" ✓ „Jetzt übernehmen"→Deploy feuert (Erfolgs-Toast) ✓ keine Raw-Keys ✓. **QA-Learnings:** Footer-Button vom Dev-`ProfileSwitcher` überlappt → `dispatchEvent('click')` statt Koordinaten-Klick · electronAPI-Stub macht `window.close()` zum No-op (Editor-Close nur in echtem Electron testbar) → Deploy via Toast verifiziert · Modus-Button-Accessible-Name enthält den Hinweis-Text.

### E-3c — Felder-Panel (Trio komplett) + Modul-Identität-Regel (Option 1)
**Custom-Fields draft-konsistent (Darien-Entscheid 2026-07-22):** Felder haben eigene BE-Persistenz (nicht im Overlay), also stagt der Editor pro Entity eine **Soll-Feldliste** im Draft (wie Value-Sets den ganzen Set halten); Deploy diff't gegen den Live-Store → create/update/delete. Ein Feld-Change fährt damit dieselben Deploy/Schedule/Rollback-Schienen wie Begriffe/Wertelisten.
- **`FelderPanel.tsx`:** modul-gescopt (`EditorModuleDef.fieldEntities`), Entity-Umschalter (kontakte = 4 CRM-Entities, helpdesk = 1), Feld-Zeilen (Typ-Icon/Pflicht-/**Neu**-/**Geändert**-Badge, Sichtbarkeit-Toggle=Soft-Hide durchgestrichen), Anlegen/Bearbeiten über den wiederverwendeten `FieldEditorModal` (draft-verdrahtet), Entfernen mit inUse-Konsequenz-Dialog + „Wird entfernt"-Restore-Sektion, Per-Entity-Reset. In `EditorPropertiesPanel` bei Section „felder".
- **Datenschicht (`custom-fields.ts`):** `applyDraftCustomFields` (Snapshot-Diff) · `snapshotCustomFields`/`restoreCustomFields` (Rollback) · **NEU `helpdesk_ticket`-Entity** (3 Seeds SLA-Stufe/Eskalationsgrund/Kontaktkanal, rein additiv, Lukes Tabellen unberührt). `CustomizationDraftPayload.customFields?` optional (nicht-breaking). `DraftConfigProvider` +`customFields`-State/`setDraftEntityFields`/Count. Deploy (`customization-drafts.ts` promote/rollback) + Cross-Window-Sync (`customization-sync.ts`) wenden Felder mit an.
- **★ Modul-Identität-Regel (Darien 2026-07-22, Option 1):** Modul-Namen sind FIX — nur INHALT anpassbar. `rbac.module.*` + `layout.navItems.*` aus `LABEL_WHITELIST` + Vendor-Seed entfernt (der Demo-Seed benannte fälschlich das Modul „Kontakte→Patientenverwaltung" + Sidebar-Nav um → Screenshot zeigte „Patienten" links). Editor-Titel jetzt „**Kontakte bearbeiten**" (statisch). editorModules `labelKeys` content-only (kontakte = 3 Objekt-Headings, helpdesk = [] → BegriffePanel-Empty-State). `BegriffeTab`-Gruppen getrimmt. `EditorWindowPage` bekommt `key={module.key}` (sauberer Remount bei Modul-Wechsel).
- **Gates:** scoped tsc **0 Fehler in allen E-3c/Option-1-Dateien** (13 Alt-Baseline) · `eslint` clean · **vitest 12/12 PASS** (+3 Feld-Tests: Deploy create/update/delete, Rollback) · i18n +6 (`i18n-editor-e3c.mjs`) +1 (`i18n-module-identity-fix.mjs`) ×4 · **Playwright `qa-editor-e3c.mjs` 8/8 PASS, 0 pageerrors, Bilder angesehen:** Sidebar-Nav „Kontakte" (nicht „Patienten") ✓ Helpdesk „Helpdesk bearbeiten" + 3 Seed-Felder ✓ Feld anlegen „Rückrufwunsch"→Neu-Badge+Zähler+4 Felder ✓ Toggle+Reset ✓ Kontakte „Kontakte bearbeiten" + 4-Entity-Umschalter + crm_contact-Felder ✓ Umschalten→Firmen/USt-IdNr. ✓ keine Raw-Keys ✓. **QA-Learning:** Hash-Nav zwischen Modulen lädt SPA nicht neu → `page.reload()` + `key`-Remount (Prod nutzt eh eigenes Fenster).

### E-4 (Galerie) + E-5b (Rollout-/Entwurfs-Liste + Rollback) — Editor damit komplett
**`AnpassungenHubPage.tsx` neu:** (E-4) Galerie-Kacheln pro Modul mit **Dimensions-Chips** (Begriffe/Wertelisten/Feld-Typen = das „Manifest auf einen Blick") + **Live-Status-Badge** (Standard / Angepasst / Rollout geplant, aus `listDrafts(moduleKey)`). (E-5b) **Rollout-/Entwurfs-Liste** unter der Galerie: Status-Badges (Entwurf/Geplant/Live/Ersetzt), Timestamp/Termin, **Zurückrollen** (live + `canRollback`), Öffnen (Entwurf→Editor), Löschen (Entwurf/Geplant). Rollback ruft `rollbackDeploy` + re-applied den Overlay im Hauptfenster (Sidebar revert) + refetch.
- **Cross-Window-Draft-Mirror (Mock; Lukes DB ersetzt ihn):** Deploy passiert im Editor-Fenster (eigener Heap), Liste lebt im Hauptfenster. `customization-sync.ts` mirrort jetzt Draft-Record (+ Rollback-Snapshot bei Deploy) via BroadcastChannel; `customization-drafts.ts` `ingestRemoteDraft`/`getDeploySnapshot`. Save/Schedule/Deploy broadcasten (`publishDraftMirror`/`publishCustomizationDeploy`).
- **Gates:** scoped tsc **0 Fehler** · `eslint` clean · vitest 12/12 (unverändert) · i18n +17 ×4 (`i18n-editor-e4-e5b.mjs`, ICU-Plural Chips) · **Playwright `qa-editor-e4-e5b.mjs` 5/5 PASS, 0 pageerrors, Bilder angesehen:** Galerie+Chips+Standard+leere Liste ✓ Deploy „Jetzt" → Kachel „Angepasst" + Rollout „Live" + Zurückrollen ✓ Rollback → „Ersetzt" + Kachel „Standard" + Toast ✓ „Als Entwurf speichern" → „Entwurf"-Zeile + Öffnen/Löschen ✓ keine Raw-Keys ✓. **QA-Learning (wichtig):** Der Deploy feuert im Editor korrekt (Toast), aber die Hub-Liste blieb leer → Ursache **nicht** Reload, sondern die App-QueryClient hält lange `staleTime` → Drafts-Query refetchte beim Zurück-Navigieren nicht (Broadcast wird verpasst während Hub unmounted). Fix: `staleTime:0` + `refetchOnMount:'always'` auf der Drafts-Query (R-5-Audit-Muster). Zusätzlich QA-Nav via `location.hash` statt `page.goto` (letzteres reloadet).
- **★ Editor damit funktional komplett** (bauen → Entwurf/terminiert deployen → zurückrollen). Offen bleibt nur die **Modul-Konsum-Runde** (Module lesen die Schicht, 2 Tiefen — siehe Tabelle) + Lukes Backend.
