---
tags: [i18n, frontend, lokalisierung]
updated: 2026-08-06
---
# Internationalisierung (i18n)

## Status: ✅ Migration abgeschlossen (2026-04-08), Bestand waechst mit

Migration react-intl → i18next komplett, strict TypeScript types. Der Key-Bestand ist seit der
Migration von 7.221 auf **12.044** gewachsen (gemessen 2026-08-06).

### Gemessener Stand 2026-08-06

| Sprache | Keys | |
|---|---|---|
| de (Source of Truth) | 12.044 | |
| en | 12.044 | vollstaendig |
| fr | 12.010 | ⚠ **34 Keys fehlen** |
| it | 12.010 | ⚠ **34 Keys fehlen** |

Die Luecke in fr/it ist deckungsgleich und betrifft die Dashboard-Modulkacheln
(`dashboard.modules.<modul>.name|description|stat`). Messkommando: die vier JSONs flach ziehen und
gegen `de` diffen — **BOM vorher strippen** (`.replace(/^﻿/,'')`), sonst wirft `JSON.parse`.

⚠ **Alle vier Dateien tragen ein UTF-8-BOM.** Das ist die Signatur von PowerShell `Set-Content`
(siehe Memory `feedback_powershell_setcontent_mojibake.md`). i18next verdaut es, aber jedes Skript,
das die Dateien direkt parst, muss es abfangen. Rewrites nur ueber `[System.IO.File]` mit
`UTF8Encoding($false)` oder das Edit-Tool.

## Bibliotheken
| Paket | Version | Zweck |
|-------|---------|-------|
| i18next | v26.0.3 | Core — flat keys, statisches Bundle |
| react-i18next | v17.0.2 | React Bindings (`useTranslation()` Hook) |
| i18next-icu | v2.4.3 | ICU Plural/Select-Syntax Support |

## Architektur

### Verzeichnisstruktur
```
desktop/src/renderer/src/i18n/
  i18n.ts              — Initialisierung (44 Zeilen, 4 statische Imports)
  i18next.d.ts         — Strict Type Declarations (resources → de.json)
  messages/
    de.json            — 12.044 Keys (Deutsch, Source of Truth)
    en.json            — 12.044 Keys (Englisch)
    fr.json            — 12.010 Keys (Franzoesisch, 34 offen)
    it.json            — 12.010 Keys (Italienisch, 34 offen)
```

### Initialisierung (i18n.ts)
- `keySeparator: false` — **kritisch**, flat dot-notation Keys
- `nsSeparator: false` — kein Namespace-Splitting
- `fallbackLng: 'de'` — Deutsch als Fallback
- ICU-Plugin fuer `{count, plural, ...}` Syntax
- 4 statische JSON-Imports (de/en/fr/it), kein Lazy Loading
- Kein Backend/HTTP-Loader (Electron — alle Resources lokal)

### Type Safety (i18next.d.ts)
```typescript
import type de from '@/i18n/messages/de.json'
declare module 'i18next' {
  interface CustomTypeOptions {
    defaultNS: 'translation'
    returnNull: false
    resources: { translation: typeof de }
  }
}
```
→ `t()` Calls werden zur Compile-Zeit gegen de.json validiert.

## Verwendungsmuster

### In React-Komponenten
```tsx
import { useTranslation } from 'react-i18next'

function MyComponent() {
  const { t } = useTranslation()
  return <h1>{t('crm.contacts.title')}</h1>
}
```

### In .ts Dateien (ausserhalb React)
```typescript
import i18next from 'i18next'
const label = i18next.t('common.save')
```

### Dynamische Keys (haeufig verwendet)
```tsx
// Template Literals — Keys muessen in JSON existieren
t(`security.admin.tab.${tab.key}`)
t(`header.connectionStatus.${status}.label`)
t(`kalender.form.recurrence.${opt}`)
```

### Datum/Zahlen-Formatierung
- `useFormatDate()` / `useFormatNumber()` Hooks
- Datei: `hooks/useFormatters.ts`
- Nutzt `Intl.DateTimeFormat` / `Intl.NumberFormat` mit aktuellem Locale

### LanguageSwitcher
- Datei: `components/header/LanguageSwitcher.tsx`
- 4 Locales: de, en, fr, it
- Nutzt `useLocaleStore` (Zustand) + `i18n.changeLanguage()`

## Schluessel-Konventionen

1. **Format:** `modul.bereich.label` (z.B. `crm.contacts.title`, `dashboard.widgets.revenue`)
2. **Marken-Namen hardcoded:** Cosmi, Zentria, Bexio, DATEV, Lexware, FinAPI — NIE in `t()` wrappen
3. **Technische Strings hardcoded:** CSS-Klassen, URLs, IDs, Enum-Values
4. **ICU-Format:** `{count, plural, one {# Eintrag} other {# Eintraege}}`

## Key-Verteilung (Top 10)
| Prefix | Keys |
|--------|------|
| settings | 979 |
| team | 367 |
| work | 323 |
| crm | 302 |
| finanzen | 289 |
| profil | 275 |
| admin | 270 |
| kontakte | 220 |
| meetings | 201 |
| dashboard | 187 |
| dialer | ~120 |

## Migrationshistorie (21 Commits)
1. i18next Infrastruktur + react-intl entfernt
2. Dashboard, CRM instrumentiert (78 Dateien)
3. Work, Profil, Settings, Kommunikation, Finanzen, Team (107 Dateien)
4. JSON-Extraktion (4 additions JSONs)
5. Kontakte + Chat + Wiki
6-11. Automatisierung, Dokumente, Admin, Meetings, Mails, Helpdesk, Buchhaltung, Kalender, Rapporte, Notifications
12. Loader Update
13. Component-Verzeichnisse (412 Keys, 9 JSONs, 46 Dateien)
14. Wave 2+3: Security, Features, Config, API Hooks
15-18. Settings Tabs, Integration Panels, Module Pages, Remaining Components
19. **Merge:** 46 additions → de.json (7.221 Keys), Loader 153→44 Zeilen
20. **Uebersetzungen:** EN/FR/IT (volle Paritaet)
21. **Strict Types:** i18next.d.ts → de.json resources

## Lessons Learned
- Agents verbrauchen bis zu 250k Tokens bei >10 Dateien — max 5-7 Dateien pro Agent
- JSON-Extraktion separat von Instrumentierung — sauberer und verifizierbar
- `keySeparator: false` muss explizit gesetzt sein, sonst Dot-Notation als Nesting interpretiert
- Uebersetzungen in 3 Chunks × 3 Agents parallel — effizient fuer grosse Key-Sets
- Merge-Script (Node) statt manuell — schnell und fehlerfrei
- Mock-Daten in Stores nicht instrumentieren — werden durch Backend-Daten ersetzt
- **ICU-Plural-Klammer-Bug (2026-04-18, Sprint 0 S0.7):** 18 Strings × 4 Sprachen hatten fehlende `}` in ICU-Expressions (`{count, plural, one {...} other {...}` — schlossen die aeussere Plural-Klammer nicht). Folge: Renderfehler bei `count != 0`. Fix in de/en/fr/it.json, Regressions-Test `i18n/__tests__/plural.test.ts` — jeder neue Plural-String wird damit gegatet.

## Verwandte Notes
- [[architektur]] — Frontend-Architektur, Demo Mode
- [[stack]] — Frontend-Bibliotheken (i18next Versionen)
- [[design]] — LanguageSwitcher Component
- [[milestones]] — i18n Sprint Meilenstein
- [[troubleshooting]] — i18n Lessons Learned
